# STATE.md — 基金戰情室 (Fund Dashboard)

> 極簡熱資料檔。完整 roadmap 見 `BACKLOG.md`；技術細節見 `ARCHITECTURE.md` / `SPEC.md` / `STRATEGY.md`。

## 專案定位
- **產品**：境外共同基金（保險型保單）戰情室 — TWD 換匯後績效 + 總經訊號 + 危機回測 + AI 策略建議
- **技術棧**：Streamlit + pandas + plotly/altair + FRED/Yahoo/FundClear + Google Sheets + Gemini API + NAS Squid Proxy
- **核心禁令**：🚫 全面排除 ETF / 個股，本系統專注共同基金
- **入口**：`app.py`（Streamlit Cloud 部署）

## 當前版本
- **v19.8_PlateauCornerFix**：治 walk-forward 找 corner vertex 退化解（OOS F1=0 root cause）— `services/multi_factor_optimization.py::evaluate_plateau` 雙修：(**F3**) 新增 `min_crossings: int = 2` kw（`DEFAULT_MIN_CROSSINGS=2` 新常量），grid 點 `n_crossings < min_crossings` 直接 `plateau = -inf` 剔除「整段只 1 次幸運觸發」偽贏家；(**F4**) plateau 公式從 `mean − λ·std` 改為 `(mean − λ·std) × sqrt(n_neighbors / max_n_neighbors)`，corner vertex 在 simplex 鄰居 ≈ n+1，內部點 ≈ 2n+1，自然懲罰角點；`find_plateau_optimum` 加 `np.isfinite(plateau_scores[idx])` 守門 — 全 -inf 回空 weights，walk-forward L489 既有 skip 邏輯零回歸；既有 `test_flat_perf_gives_constant_plateau` 改名 `test_flat_perf_interior_beats_corner_v19_8` 並更新斷言（corner penalty 預期）；新增 3 case：`test_low_n_crossings_filtered_to_minus_inf_v19_8_F3` / `test_corner_vertex_loses_to_interior_v19_8` / `test_all_minus_inf_returns_empty_v19_8`；測試 42 → 45；全 1254 pass / 2 skip 零回歸；ruff 全綠
- **v19.7_GoogleSheetsBackend**：治 Streamlit Cloud FS ephemeral — `services/macro_weights_store.py` 加 GS 後端抽象層；新增 `_gs_enabled / _gs_get_worksheet / _gs_load / _gs_save / _gs_delete` 5 個 private helpers，重用 `repositories/policy_repository.get_gspread_client` 既有認證；5 個公開 API（`load_active / load_pending / has_pending / save_pending / approve_pending / reject_pending`）皆改 dispatch：偵測到 `st.secrets["google_service_account"] + st.secrets["macro_weights_sheet_id"]` → 走 GS（worksheet `_macro_weights` 固定 2 列：B2=pending / B3=active）；否則 fallback FS（本地開發友善）；`save_pending` return type 擴成 `Path | str`，UI 端 `tab_crisis_backtest.py:964` 加 `hasattr(p, "name")` 兼容；測試 39 → 48（+9 新 `_FakeWorksheet` MagicMock 案：roundtrip / has_pending / approve / reject / overwrite / corrupt / empty fallback）；**user 部署需在 Streamlit Cloud secrets 加 `macro_weights_sheet_id` 並把 sheet 分享給既有 service account email**
- **v19.6_ParamFinderTabHoist**：**生產 / 消費分離** UI 重組 — 新增 `ui/tab_param_finder.py` 獨立 tab「🔬 回測找參數」，把多因子權重最佳化 + walk-forward OOS + Route C-1 提交為待審權重從 📉 危機回測室 Phase 3 hoist 出來；危機回測室 L548 移除 `_render_phase3_multi_factor_optimization` dispatch（function 本體保留供新 tab import）；`app.py` tab 列從 7 → 8（總經 / 組合 / 單一 / 危機回測 / **🔬 回測找參數**新 / 配置 / 診斷 / 說明）；資料流不變（事件清單從 `_PHASE1_CACHE_KEY` 讀，user 必先在危機回測室跑一次 Phase 1）；邏輯零變動只搬位置；ruff 全綠 + 既有 pytest 通過
- **v19.5_GeminiFix_FactorPoolCap**：兩個 walkthrough 痛點小修 — (A) `services/ai_advisor_pending.py:91` Gemini model `gemini-1.5-flash` → `gemini-2.5-flash`，對齊 `infra/llm.py:26` / `services/ai_service.py:31` 既有規範，解 Route C-1 提交時「AI 呼叫失敗：NotFound」（fallback 數學摘要本來就 OK，這只是把 AI 解讀補回來）；(C) `ui/tab_crisis_backtest.py:739` factor multiselect 加 `max_selections=6` 前端 cap — n=23 + step=0.1 simplex 點數會天文數字爆 sweep，UI 層先擋；caption 改「最多 6 個 — simplex 點數隨 n 指數成長」明示限制
- **v19.4_FactorPool23**：FACTOR_POOL 13 → 23，補 10 個 FRED 因子（**SAHM** 衰退觸發 / **SLOOS** 信用緊縮 / **LEI** 領先指標 / **PPI** 生產者物價 / **JOBLESS** 初領失業金 / **CONT_CLAIMS** 持續領 / **CONSUMER_CONF** Michigan 消費信心 / **PERMIT_HOUSING** 新屋建照 / **FED_BS** WALCL Fed BS / **INFL_EXP_5Y** T5YIE 通膨預期）；全 FRED source，沿用 v18.286 既有 `fetch_factor_series` lazy fetcher 不用加 adapter；新增 `TestV19_4FactorPool` 5 case 鎖死（新 key 全在 / source 全 fred / series_id+direction 對表 / series_id 唯一不重複 / note 不空）；UI 不變（user 一次最多選 6 個避免 sweep 指數爆炸）
- **v19.3_HotfixNestedExpander**：修 Route C-1 提交區巢狀 expander bug — `_render_pending_submit_section` 被 `_render_phase3_multi_factor_optimization` 的「🔬 多因子權重最佳化」expander 包住，submit 成功後又 `st.expander("🤖 AI 解讀...")` 觸發 `StreamlitAPIException: Expanders may not be nested inside other expanders.`；改用 `st.markdown` 粗體標題 + 直接 markdown 內文取代內層 expander（AI 解讀本來就 `expanded=True`，去掉摺疊不影響行為）
- **v19.2_HotfixWalkforwardEmptyIndex**：修 v18.296 引入的 walk-forward `TypeError` bug — `fetch_factor_series` 任一 source（yahoo / fred / calculated）抓失敗回的空 Series 之前用 pandas 預設 RangeIndex，下游 `_slice_series` 做 `index >= Timestamp` 比較直接爆 TypeError 全 Phase 3 多因子斷線；修法：所有 5 個失敗 return 改用 `pd.DatetimeIndex([])`（根因），`_slice_series` 加防禦 skip 非 DatetimeIndex 空 Series 並回對應 key 的空 DatetimeIndex（保險）；新增 `TestEmptyFactorSeriesIndexType` 3 個 case 鎖死 regression（fetch fail / slice mix / walk-forward mix）；全 37 case passed
- **v19.1_RouteC_C2_PanelLoadOverride**：Route C 第二階段「面板下游接管」— `services/macro_weights_store.py` 加 4 個 override helper：`apply_weight_overrides(ind)`（loop ind 用 active.json 蓋 weight，缺鍵不動，不深拷 Series）/ `get_weight_override(key, fallback)`（compute_score_row 單鍵查詢）/ `get_verdict_cutoffs()` / `get_phase_thresholds()`（讀 JSON 或 fallback）；5 個下游 wire：`ui/helpers/macro_helpers.py` 的 `calculate_composite_score` / `category_score` 入口呼叫 apply_weight_overrides，`composite_verdict` 改讀 `get_verdict_cutoffs()`；`services/macro_service.py::compute_cluster_signals` 入口呼叫 apply_weight_overrides；`services/macro_score_calibration.py::compute_score_row` 每 factor 改走 get_weight_override + total_w 即時 sum，`classify_phase` / `phase_accuracy` / `overall_accuracy` 預設 `thresholds=None` 走 `get_phase_thresholds()`；既有所有 hardcoded 數字保留為 fallback，active.json indicators 為空 / verdict_cutoffs null / phase_thresholds null → 行為完全跟 v19.0 一致；39 cases passed（21 個新 case + 18 個既有 case），全測 1171 passed 1 skipped 零回歸
- **v19.0_RouteC_C1_PendingReviewSchema**：Route C 第一階段「面板=回測 OOS 驗證結果」閉環落地 — 新增 `config/macro_weights_active.json` + `services/macro_weights_store.py`（5 個純函式：load_active / load_pending / save_pending / approve_pending / reject_pending + `build_payload_from_multifactor` 整理器）；新增 `services/ai_advisor_pending.py` 接 Gemini 產白話權重解讀（無 key fallback 純數學摘要）；危機回測室 Phase 3 多因子跑完顯示「📌 提交為待審權重」按鈕（**僅手動觸發**），呼叫 AI → save_pending；總經 Tab 頂部偵測 pending.json 顯示橘色 banner，user 點 ✅ 批准 → promote pending → active，或 ❌ 拒絕 → 刪 pending；面板載入邏輯不變（C-2 才接管 weight 來源）；**刪除 `.github/workflows/recalibrate_macro_score.yml`**（季頻自動寫權重 workflow 與 pending review 流程衝突，回測只能手動觸發）；18 cases passed
- **v18.296_AdvancedFactorPool13**：多因子權重最佳化 FACTOR_POOL 10 → 13；補強「債市流動性 + 高頻景氣 + 金融環境」三類進階因子（MOVE 公債波動率 yahoo `^MOVE` / NFCI 全國金融狀況 fred `NFCI` / 銅金比 calculated `HG=F÷GC=F`）；FactorSpec.source Literal 擴 `"calculated"`；新增 `fetch_factor_series(spec, years, fred_api_key)` lazy fetcher 統一三 source；UI 不再過濾 series_by_key，Run 時自動 lazy-fetch 缺漏因子；34 cases passed
- **v18.285_MultiFactorPlateauWalkForward**：Phase 3 加「🔬 多因子權重最佳化」expander — 綜合分數 S_t = Σ w_i × normalize(I_{i,t−1})（lag=1 防未來引用），simplex 權重 grid sweep 算 F1+Sharpe，**plateau 評分 = 鄰域 mean − λ × std**（不取單一最高 F1）；walk-forward 滾動 train/test 串 OOS 權益曲線；plotly 2D heatmap + 3D surface toggle；10 因子池（4 現有 + 6 raw FRED：PMI/CPI/FedFunds/M2/DXY/T10Y3M）；28 case 驗收
- **v18.284_PresetMatrixCompareTable**：配置模擬器 selectbox 上方加「📊 4 風格 × 4 階段 對照表（全展開）」expander — DataFrame 4 phases × 4 styles = 16 cells "D / C / S" 字串，read-only，方便 user 橫向比較再選 preset 套用；engine 加 `build_preset_matrix_df()` helper
- **v18.283_MT5StyleAutoCalibration**：Phase 3 加「🎯 MT5-style 自動校準」expander — walk-forward 4 折 grid sweep × 3 重 anti-overfit gate（折間票選 + drift>30% 回退 + cap 守門）；session-only override 機制；對齊 stock v18.164；1205 passed
- **v18.282_SignalPrecisionAnalysis**：Phase 3 加「📐 訊號精確率分析」forward-looking 區塊 — 解召回率單面向：遍歷歷史 crossings 算 TP/FP/精確率/誤報率/avg lead time；對齊 stock v18.163；1195 passed
- **v18.281_Phase3EdgeDetection**：Phase 3 訊號回看引擎 port stock v18.160 v2 edge detection（從非警戒「跨越到」警戒的轉折日），修「常態性已警戒 = 假預警」誤判；UI 加「v2 轉折偵測」標示
- **v18.280_StrategyPresets**：配置模擬器加 4 風格 × 4 階段 preset 矩陣（積極成長 / 穩健平衡 / 收益優先 / 防禦保本），UI 一鍵套用 4 階段 DRIP/CASH/STAY 比例
- **v18.279_MacroScoreCalibration**：VIX 閾值 OOS 自動校正（5 重 anti-overfit gate：walk-forward / 正則項 / 票選 / bootstrap CI / 末段 36 月 holdout）
- **同期合入**：v18.279p_Phase5StackOrder — Phase 5 AI 區塊壓底，render 順序固定為 Phase 4 → Phase E → Phase 5

## 目錄結構
- `services/` — 業務邏輯（macro / strategy / AI / cross-source / liquidity / calibration）
- `repositories/` — 資料源抓取（FRED / Yahoo / FundClear / Macro）
- `infra/proxy.py` — NAS Squid 中繼站；HTTP gateway（含 407 / 403 / 429 / timeout 完整 fallback）
- `ui/` — Streamlit 分頁元件（tab1~tab6 + components）
- `scripts/` — `update_macro_history.py`（每週 cron 抓 FRED + Yahoo Parquet）/ `calibrate_macro_score.py`（VIX OOS 自動校正）
- `data_cache/*.parquet` — fred_indicators / spx_history / twii_history / vix_history（每週日 cron 更新）
- `.github/workflows/` — update_macro_history / fetch_nav_cache / pr-check（v19.0_C1 移除 recalibrate_macro_score — 回測只能手動觸發）
- `config/macro_weights_active.json` / `config/macro_weights_pending.json`（v19.0_C1）— Route C 權重雙檔（pending 僅在有待審時存在）

## 即時健康指標
- **Test**：~1098 passed / 2 skipped / 0 failed（PR #169 修掉 3 個 pre-existing fail）
- **本日 PR**：#164 mk_clock 衰退誤判 / #165 Phase E 引擎 / #166 proxy 429 backoff / #167 Phase 5 壓底 / #170 VIX OOS 校正
- **協議**：Core Protocol v2.0（PR #168，嚴禁自動 Merge）
