# 實機驗收清單 — Layer 1~4（2026-08-15）

**用法**：照順序做，每項只要回報「對 / 不對 + 你看到什麼」。
不必寫長，一句話就夠 —— 我要的是「畫面上實際長什麼樣」，不是你的判斷。

**先決條件**：

```powershell
Set-Location -LiteralPath "D:\01.Github\20260813\基金"
python -m compileall -q ui services shared scripts
python -m pytest tests/ -q 2>&1 | Select-Object -Last 15
```

全綠再上線。有紅的先貼給我。

---

## 🔴 第一優先：會動到你真實資料的三件事

這三項排最前面，因為它們**寫進你的帳本或 Google Sheet**。
其餘都只是顯示。

### A1. 落帳目標認不得會擋下（E13）

📋 我的管理室不需要。到 **📊 配置 & 帳本 → 💼 ③ 持倉戰情（T7 帳本）→ A. 新投入**。

1. 選「既有基金」模式 → 應看到「落帳目標」兩個選項：`💡 暫存為方案` / `✅ 直接套用主帳本`
2. 選 `💡 暫存為方案`，填一筆小額，按試算
3. **確認：主帳本沒有變**，而是出現在「方案比較」區

> 這裡原本是用選項字串開頭那個 💡 判斷要不要寫進真實帳本。
> 只要有人把文案的 emoji 換掉，「試算」就會直接寫進主帳本。

**要回報**：試算後主帳本的總市值有沒有變？

---

### A2. 借基金缺資料會被擋（D2）

**📊 配置 & 帳本 → T7 → C. 轉換再平衡** 裡的「從其他保單借」。

1. 挑一檔**沒抓到淨值或匯率**的基金來借（狀態是 ❌ 的那種）
2. **應該看到紅色 ⛔ 訊息**，說明缺什麼、要去哪裡補

> 原本會憑空給 NAV=10.0、FX=31.0。10.0 剛好是基金常見的發行價，看起來特別像真的。

**要回報**：擋下來了嗎？訊息看不看得懂？

---

### A3. 補歷史淨值改成兩段（E6）

**📋 我的管理室 → 📥 補歷史淨值** → 展開。

1. 挑一檔基金 → 選級別 → **現在應該只有「① 🔍 預覽（只抓不寫）」**
2. 按預覽 → 看到抓到幾筆、起訖日期、**計價幣別**、以及與既有資料的重疊分析
3. 才會出現「② ✍️ 確認寫入」

**加分測試**（如果你有一檔已經補過歷史的）：
故意選**另一個級別**（不同幣別或配息/累積相反）→ 按預覽 →
**應該看到紅色 ⛔ + 幾筆日期的淨值對照**，且**沒有**確認鈕。

> 這是整批最危險的一條。nav_history 的去重只看「代碼 + 日期」，
> 錯的級別寫進去之後，正確的就**永遠寫不進來**，而健診會拿錯的去算報酬率。
> 那個分頁在說明書上標「🚨 絕對不要刪，無法從任何來源重建」。

**要回報**：兩段流程有沒有出現？衝突偵測有沒有抓到？

---

## 🟠 第二優先：新欄位看不看得懂

### B1. 「評分覆蓋」欄（Layer 3-C）

**💊 組合健診** 跑一輪 → 看大表，「評分覆蓋」在「4D Grade」**左邊**。

應該看到三種樣子：

- `✅ 5/5`
- `⚠️ 2/5（缺 風險報酬、波動）`
- `➖ 4/4（台幣，無匯率風險）`

**要回報**：
1. 你的 25 檔裡，各出現幾種？
2. `⚠️` 的那些，你看得出「這個等第只能當參考」嗎？
3. 欄名 hover 的說明講得夠不夠白？

---

### B2. 「淨值樣本」欄（F12）

同一張表，在「評分覆蓋」左邊。格式 `30 筆 · 42 天`，短窗會帶 ⚠️。

**要回報**：有幾檔帶 ⚠️？那幾檔是不是右邊 Sharpe / 波動 / 最大回檔整排空白？

---

### B3. 1Y 含息報酬不再有離譜值（E1）

同一張表，看「1Y 含息 %」和右邊的「1Y 來源」。

**要回報**：
1. **還有沒有 +190% / +201% 這種數字？**（先前那兩檔台幣基金）
2. 有沒有哪一檔的「1Y 來源」寫著 `—（僅 N 天資料，不足以推算一年）`？
3. 有沒有出現 `⚠️ 數值異常大` 的標記？

---

### B4. 批次「部分成功」三態（A1）

**📦 批次分析** 跑幾檔（含你知道會失敗的）。

摘要應該是**四格**：檔數 / ✅ 完全成功 / ⚠️ 部分成功 / ❌ 失敗。

**要回報**：有沒有出現「⚠️ 部分成功」？備註寫了什麼？

---

## 🟡 第三優先：舊 bug 的驗收（V1~V5）

### C1. 組合 KPI 與表格檔數一致（E2）

**📊 配置 & 帳本** 最上方的 KPI 卡「N 檔」，對照下面表格的列數。

> 先前是 KPI 說 25、表格只有 8。原因是 KPI 只判 `loaded`，
> 而抓失敗的基金也是 `loaded=True`。

**要回報**：兩個數字一樣嗎？

---

### C2. T7 抓不到報價不再兩種說法（A8）

T7 帳本表，找**今天抓不到淨值或匯率**的部位。

應該看到：市值欄 `⬜ 無今日報價`、損益留白，
上方一則 ⚠️ 說「有 N 檔沒有計入總市值與總報酬」。

> 先前總表說「損益 0、報酬 0.00%」（像打平），
> 帳本表說「市值 0、報酬 −100%」（像賠光）。同一個情況兩個相反的答案。

**要回報**：有沒有這種部位？兩處說法一致嗎？

---

### C3. 匯率用猜的會講出來（D3）

T7 帳本面板**最上方**。若有部位的匯率抓不到，
應該看到黃色警告列出「哪幾檔的台幣金額是用猜的匯率算的」。

**要回報**：有出現嗎？（沒出現代表匯率都抓得到，也是好消息）

---

### C4. 健診滑桿已移除（C2 / V5）

**💊 組合健診** 上方，原本「吃本金閾值 %」滑桿**應該不見了**，只剩「本金（TWD）」。

**要回報**：還在嗎？

---

### C5. 手動匯率不再預填 32（D1）

**🔍 個基深掘** 查一檔**非美元**基金（日圓 / 南非幣最好），
且要在匯率自動抓取失敗時才會出現。

若出現手動匯率欄：應該是**空的**（不是 32.0），且下方試算整塊留白 + 一則 ⛔ 說明。

**要回報**：能不能重現？欄位是空的嗎？

---

## 📊 最後：跑完 25 檔的覆蓋率盤點

```powershell
chcp 65001
notepad codes.txt      # 把你真正的 25 個代號放進去，一行一個
python scripts\audit_6d_coverage.py --from-file codes.txt | Out-File -Encoding utf8 6d_coverage.txt
Get-Content 6d_coverage.txt
```

**要回報**：最後兩段（覆蓋率總結 + 基金規模原始字串）。

我最想看的是**抓取失敗率** —— 目前小樣本是三成上下。
如果 25 檔跑出來真的三成抓不到，那 L4 的週報推送就不能假設資料是完整的，
設計要跟著改（「這週有 8 檔沒資料，以下建議不含它們」）。

---

## 回報格式

不用照抄整份，這樣就好：

```
A1 對
A2 沒遇到（沒有失敗的基金可借）
A3 對，衝突偵測有抓到，差 12%
B1 ✅ 18 檔 / ⚠️ 5 檔 / ➖ 2 檔，看得懂
B3 沒有 +200% 了，有 3 檔顯示「僅 N 天」
C1 都是 23
...
```

**看到不對的地方，把畫面上的文字原樣貼給我**（不用截圖，文字就好）。
