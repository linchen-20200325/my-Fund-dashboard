# 架構例外與待判定登記簿(EXCEPTIONS)

<!-- CONSTITUTION-FILE -->
<!-- 上面那一行是給機器看的：`tests/test_constitution_file_refs.py` 以它認定「哪些檔是憲法」。
     它刻意**不是**散文句式 —— 措辭會被改寫，標記不會。**搬檔或改名時請一起帶走。** -->

> **本篇是 `CLAUDE.md`(資料完整性憲法)的一部分,2026-09-08 從該檔搬出,內容一個位元組都沒有改。**
> 搬出來的是兩張表:**`8.2.A` 已知例外清單**(§8.2 分層硬規則的合法豁免)與
> **`8.3.P` 尚未判定**(待派工查證的登記簿)。它們仍受 `CLAUDE.md` 全部條文拘束,
> **本篇不是一份新憲法,是同一部憲法的一個檔案分割**。

## 位階宣告(讀本篇任何一條之前先讀這一段)

**`CLAUDE.md` 的 `§-2` 總管派工準則 + `§-1.5` 虛擬軟體公司運行規範 + `§-1` 停手準則,凌駕本篇全篇。**
本篇 `8.2.A` / `8.3.P` 兩節屬 `CLAUDE.md` 所稱的 `§0`~`§8` 射程,
`§-1` 標題「凌駕 §0~§8」對本篇**照樣成立**。

⚠️ **為什麼要有這一段(不是儀式,是搬檔案當場戳破的一個洞)**:
`CLAUDE.md §-2` 的標題寫的是「凌駕**本檔**其餘各節」—— 它的效力範圍是用「**本檔**」界定的。
內容一旦搬出 `CLAUDE.md`,**`§-2` 就不再以自己的文字管轄本篇**;
同理 `§-1.5.0` 明寫「位階**靠每節標題後的明文宣告**」,而那個機制是**檔案內生的、跨不了檔**。
→ 所以位階必須由**下**往**上**宣告,由本篇自己認領。
**這個寫法不是本輪發明的**:`PROCESS.md` 開頭就是這樣做的
(「**§-2 總管派工準則 + §-1.5 虛擬軟體公司運行規範 + §-1 停手準則(見 CLAUDE.md)凌駕全篇**」),
本篇照抄該前例。

⛔ **本段不改變任何一條的效力,也不新增任何一條。** 它只是把「原本靠同檔關係成立的管轄」
在跨檔之後用明文接回去。**若 user 認為 `§-2` 的射程本來就不該跟著內容走,以 user 為準,本段即刻撤銷。**

## 節號:刻意沿用 `8.2.A` / `8.3.P`,沒有重編

- **沿用的理由**:`§-1` 標題寫「凌駕 §0~§8」—— 沿用原編號,那個射程在**字面上**才成立;
  一旦重編成別的號,本篇就掉出 `§-1` 的字面範圍。
- ⛔ **本篇刻意不使用 `§1`~`§8` 這一組編號。** 該命名空間在本 repo **已被四份文件同時佔用**
  (`CLAUDE.md` / `PROCESS.md` / `SPEC.md` / `ARCHITECTURE.md`),再加一份只會讓碰撞更糟。
- **跨檔引用一律寫 `` `檔名 §X` ``**(例:`` `CLAUDE.md §8.2` ``);**自指一律寫「本篇」**。
  ⚠️ 這一條是照著 `PROCESS.md` 的**優點**抄的,**它的破綻刻意不抄** ——
  `PROCESS.md` 開頭同一段裡「§-1.5 第二條」指 `CLAUDE.md`、「§1 / §4 / §5」指它自己,
  而 `CLAUDE.md` **也有** §1/§4/§5,全靠上下文硬撐。

## ⭐ 全域宣告:本篇搬進來的兩塊裡,那些「相對指代」該怎麼讀

**兩塊內容是逐位元組搬過來的,一個字都沒改**(理由見下方「搬運方式」)。
代價是:塊內原本寫給「同一個檔」的相對指代,跨檔之後會指到空氣。
**本篇採 `CLAUDE.md` 自己的既有處置 —— 單點全域宣告,不逐處改字。**
(該辦法出自 `CLAUDE.md §-1.5.1a`「條號位移」表與 `§-1.5.1c`「三版編號體系對照」表,
兩處都寫明「**刻意不逐處改字**……**單點宣告比散改十幾處不容易出錯**」。)

### (1) 「本檔」在本篇兩塊裡指什麼

**預設:凡指憲法者,一律指 `CLAUDE.md`,不是本篇。**
搬進來的塊在寫下的當天住在 `CLAUDE.md` 裡,「本檔」寫的就是它。

⛔ **但「本檔」在本篇裡有三種所指,不得機械取代。** 下列三類逐一列出
(本輪人工逐處判讀,非 grep 取代)。

**第一類:指程式碼檔,與憲法無關** ——

| 位置 | 「本檔」實際指 |
|---|---|
| `8.2.A` · `EX-UICACHE-1` 列 · ~~`ndc.py`~~ 那一個 bullet 的「**本檔無失效路徑,也不需要**」 | **`ui/helpers/macro/ndc.py`** |
| `8.3.P` · `P-WSTOREWRITE-1` 列的「**待答**:本檔要不要照同一個讀寫分離切法處理」 | **`services/macro/weights_store.py`** |
| `8.3.P` · `P-AIKEYCI-1` 列的「該註記自陳『那不是**本檔**能保證的事』」 | 該註記所在的**程式碼檔**(引號內是轉述,不是本篇的自指) |

**第二類:指「本篇這個檔自己」 —— 預設映射到 `CLAUDE.md` 會讓它指向空氣** ⚠️

⚠️ **這一類是 2026-09-08 第二輪稽核抓出來的,本輪初稿漏了。** 它們**不是**在講憲法某一節,
而是在講「**我這行字所在的這個檔**」;套上「本檔 ＝ `CLAUDE.md`」的預設之後,**敘述會變成假的**:

| 位置 | 「本檔」實際指 | 套預設之後為什麼壞掉 |
|---|---|---|
| `8.2.A` · `EX-AI-1` 列的「全 repo 唯二的 `🧬` 命中是**本檔這一行自己**與 `STATE.md` 的章節標題」 | **`EXCEPTIONS.md`(本篇)** | **實測**:`git grep -c "🧬" -- CLAUDE.md` 在搬走後 → **0**(正對照:同指令在 `ba3b06d` → **2**)。指到空氣。 |
| `8.3.P` 表尾「~~…~~ **「本檔沒有第九個待判定項」本組未查證、也不宣稱**」 | **`EXCEPTIONS.md`(本篇)** | 映射到 `CLAUDE.md` 之後**恆真**(該檔已經沒有這張表),承重的「**別信這張表窮舉**」整個消失。 |
| `8.3.P` 表尾「**「本檔沒有第十八個待判定項」本組未查證、也不宣稱**」 | 同上 | 同上 |
| `8.3.P` 表尾「**「本檔沒有第二十二個待判定項」本組未查證、也不宣稱**」 | 同上 | 同上 |

→ **這四處一律讀作「本篇 `EXCEPTIONS.md`」。**
⚠️ **只有自指那一半是本輪新壞的;「唯二」那個計數在 `ba3b06d` 就已經不準了**
(實測 base 當時 `CLAUDE.md` 2 ＋ `STATE.md` 2 ＝ **4** 個 `🧬` 命中,不是 2)——
那是**既有問題**,且它落在逐位元組搬移的區塊內,**本輪不改**(改了就不再是純搬移)。
**兩件事分開讀:自指壞掉是本輪造成的、由本節修好;計數不準是既有的、留給後人。**

⚠️ ⭐ **這一類是怎麼漏掉的,值得記一筆**:本 PR 自己替 `P-UISUBPROC-1` 抓出了
「跨檔之後這句會變成**空洞為真**、線索整條消失」這個模式,**卻沒有把同一把尺往內用在自己的宣告上** ——
**同一輪、同一份 diff、同一個病。** 這正是本篇 `8.2.A` 驗證段 ④ 的既有教訓
(**條件只往外用、不往內用**)在本檔上的又一次發作。

**第三類:指憲法** —— 即上面那條預設(`CLAUDE.md`),不另列。

⚠️ **上面兩張表都是本輪逐處判讀的結果,不是窮舉宣稱。** `CLAUDE.md` 全檔 75 處「本檔」中
**至少 4 處**指的是程式碼檔而非憲法(另兩處在本篇射程外) —— **同一個詞在同一張表裡有三種所指**,
**任何一次機械取代都會製造新的假話**。後人要動這裡,請照樣逐處讀。

### (2) 「A 類 / B 類 / C 類」一律指 `CLAUDE.md §-1.5.1c 判定 2`

`8.3.P` 的 `P-UIHTTP-1` / `P-UIGSPREAD-1` / `P-UISUBPROC-1` 三列反覆引用
**A 類(連線診斷 ping)/ B 類(UI 層私有資料抓取)/ C 類(其他對外往返)** ——
**那三個分類的定義留在 `CLAUDE.md §-1.5.1c 判定 2`,沒有跟著搬過來。**

→ 本篇內所有 `A 類` / `B 類` / `C 類`,**一律讀作 `CLAUDE.md §-1.5.1c 判定 2` 的 A/B/C 類**。
⚠️ **兩處特別要緊,因為它們原本連個 `§` 都沒帶**:
- `P-UISUBPROC-1` 的「`subprocess` 形態的對外往返在**本檔**尚無任何判定先例」——
  那個「先例」就是 `CLAUDE.md §-1.5.1c 判定 2` 的 **A 類**。跨檔之後這句會變成
  **空洞為真**(本篇當然沒有先例),**線索整條消失**。
- `P-UISUBPROC-1` 的「上一輪自己點名在 **C 類**」與段末「它不是靠掃描找到的,而是**上一輪自己點名在 C 類**」——
  同上,「C 類」在本篇無定義。

### (3) 其他留在 `CLAUDE.md`、本篇會引用到的東西(非窮舉)

`§-2` / `§-2.A`(含**舊條文保留不刪 ＋ 加刪除線 ＋ 兩邊理由並陳**的慣例)、`§-1`、`§1` Fail Loud、
`§2.1` SSOT、`§2.4` Freshness、`§3.3` 反捏造、`§8.2` 分層硬規則、`§8.3` 灰色地帶(F-GRAY-*)、
`§8.1` step 6、`§8.4` 步驟 4、`§-1.5.1c` v3 全文與判定 1~7 —— **全部留在 `CLAUDE.md`,本篇未搬。**

⚠️ **`§8.2.A.0` 從來就不在 `CLAUDE.md` 裡**,它出自姊妹 repo `my-stock-dashboard`;
搬進來的塊內已有多處就地註明這件事,**本篇未改也未修**。

## 搬運方式(可自驗:這是一次純搬移)

- **兩塊皆逐位元組搬移,內容零修改。** 連標題層級的不一致都刻意保留:
  `8.2.A` 那塊的開頭仍是**粗體行**(不是 markdown 標題)、`8.3.P` 那塊的開頭仍是 `####` h4 ——
  **那是搬運痕跡,不是漏改**。把它們「順手整理成一致的標題層級」會讓這次搬移
  不再能用「內容有沒有變」一句話驗完,那個代價遠大於美觀。
- **驗法**:對 `CLAUDE.md` 的搬出前版本取同樣行區間,與本篇對應區塊做 `diff` → 應為空。
- ⚠️ 因此**塊內所有「本輪 / 本節 / 上一輪 / 本表」等相對指代,講的都是它在 `CLAUDE.md` 裡的那個上下文**,
  不是本篇。上面第 (1)(2) 節處理的是其中會指到空氣的那幾種。

## 為什麼要搬(理由只有一個,而且不是 CI)

⛔ **不是「快撞到 `check-added-large-files` 上限」—— 那個理由是假的,已於本輪一併更正**
(見 `CLAUDE.md §-2.A` 的容量登記)。該 hook 沒帶 `--enforce-all`,
邏輯是 `if not enforce_all: filenames_filtered &= added_files()`,而
`added_files()` ＝ `git diff --staged --name-only --diff-filter=A` —— **只看新增(A),不看修改(M)**。
`CLAUDE.md` 是**已追蹤檔**、每次都是 M ⇒ **那道 512,000 的牆對它結構上永遠不觸發。**

✅ **真正的理由:每一次開工的載入成本。**
`CLAUDE.md` 搬出前 **403,887 bytes**(約 12 萬 tokens),**每一次任務都整份載入**。
本次從它裡面搬走 **128,665 bytes(佔原檔 31.9%)**;
⚠️ **邊界慣例要講明,否則下一個人量出來會對不上**(2026-09-08 第二輪稽核量到 `128,666`,
差 1 byte,雙方都沒錯):本數字是**兩塊各自不含末尾換行**;
**含塊間分隔換行 ＝ `128,666`**、**兩塊各含末尾換行 ＝ `128,667`**。
**三個都對,差別只在邊界怎麼取** —— 引用時請連取法一起寫。
扣掉原位置留下的兩段指標與同輪寫入的容量更正,`CLAUDE.md` **淨減 29.5%**
(**403,887 → 284,652**)。
⚠️ **兩檔相加反而變多**(`448,611`,比原本多 `44,724`)—— 多出來的是本輪新寫的東西
(位階宣告、全域指代宣告、兩段指標、容量更正、新登記的 `P-PHANTOMSEC-1`)。
**省下的是「每次開工的載入成本」,不是「repo 的總字數」;後者其實增加了。**
而且搬走的正是**成長引擎** —— 2026-09-08 兩輪(`093d273` → `2ccb63a` → `ba3b06d`)全檔合計
**+48,857** bytes,其中 `§8.2.A` **+25,337**、`§8.3.P` **+19,792**,兩者合計 **45,129 ＝ 92.4%**。

⚠️ **一處就地更正,不要照抄派工單**(**有意識的更正,不是漏刪** · 日期 **2026-09-08** ·
決策者:**架構與文件組**):本輪派工單寫的是「**其餘每一節一個 byte 都沒動**」——
**本組實測推翻**:其餘各節同期合計 **+3,728 bytes**(`271,494` → `275,222`),**不是 0**。
(那 3,728 主要就是 `CLAUDE.md §-2.A` 那兩筆容量登記自己長出來的。)
**派工單的方向完全正確**(這兩塊確實是成長引擎,92.4% 是實測),**被權衡掉的只有它的全稱強度** ——
「其餘一個 byte 都沒動」是一句**能被一條指令推翻的全稱句**,正是 `CLAUDE.md §-1.5.1c 判定 2`
方法教訓 (2) 明文禁止寫進憲法的那種。**改成分類敘述,不改結論。**

(⚠️ 以上為**會漂移的量測值**,量測日 **2026-09-08**;要用請現場量 `wc -c`,不要引用本行。
**驗法**:`git show <rev>:CLAUDE.md | wc -c`,並以內容定位兩塊的起訖行取區間。)

---

## 程式碼側的「回指註解」現況（本節是 `8.2.A` 末句第 2 項的配套）

`8.2.A` 末句要求:新增例外**必須**「**(2) 對應檔案加註解指回此表**」。
拆檔把「此表」搬了家,那些回指註解因此要一起看:

- **多數不必動(實測 2026-09-08)**:回指註解裡**只寫 ID**(`EX-*` / `P-*` / `GAP-*`)的那一批,
  **天生跨檔安全** —— ID 不含檔名,搬到哪裡都指得到。**本輪一個字都沒改。**
- **需要改的是寫死檔名的那批**:本輪把 `CLAUDE.md §8.2.A` / `§8.2.A.1` / `§8.3.P`
  改指 `EXCEPTIONS.md`。**機械取代命中 32 處,人工複核後回退 3 處,再依客戶紅線回退 1 處
  (`ui/tab6_manual.py`,見下表),最終落地 `EXCEPTIONS.md` 出現 29 次 / 17 檔**
  (量測日 2026-09-08,實測 —— 前三次回退中的第 1 項在同一行補回了一個
  `EXCEPTIONS.md §8.2.A`,故 `32 − 3 + 1 − 1 = 29`)。
  ⚠️ **三處機械取代改錯、已就地回退**,逐條記明以免後人重蹈:
  1. `ui/helpers/portfolio/policy_admin_section.py` 的「分層(`CLAUDE.md §8.2` / §8.2.A)」——
     **`§8.2` 分層硬規則留在 `CLAUDE.md`**,只有 `§8.2.A` 搬走;機械取代把兩者一起改掉了。
  2. `tests/test_batch2_top_card_grid.py` 內**引述 2026-09-05 那一輪自己說過的話**
     (「本批動不了 `CLAUDE.md`……」)—— **那句在當時為真,改引文＝竄改歷史。**
  3. `ui/helpers/fund_research/code_finder.py` 內**已加刪除線的退役紀錄** ——
     描述的是 2026-08-31 當時的狀態,依「舊條文保留不刪」**不改寫**。
  → **教訓**:回指註解裡的檔名有三種身分 —— **現況指標**(要改)、
  **歷史引文**(不能改)、**已退役紀錄**(不能改)。**一條 `sed` 分不出它們。**

### ⛔ 三筆**已知過期、且本輪刻意不修**的回指註解(舊分頁內)

| 檔案 | 該行現況(指標仍指 `CLAUDE.md`) | 為什麼不修 |
|---|---|---|
| `ui/tab2_single_fund.py` | `# EX-PASSTHRU-1(v19.377):同 ai.py,fetch_stock_news self-contained news fetcher(見 CLAUDE.md §8.2.A)` | **客戶紅線:舊版 Tab 代碼絕對禁止修改**,一個字都不准動 |
| `ui/tab5_data_guard.py` | `# EX-PASSTHRU-1(v19.377):同 data_registry,thin FRED 揭露日 helper 診斷用(見 CLAUDE.md §8.2.A)` | 同上 |
| `ui/tab6_manual.py` | `# 這與 \`CLAUDE.md §8.2.A.1\` 記載的失效模式同型:**條件只往外用、不往內用。**` | 同上。⚠️ **本輪初稿改過這一行(純註解、零行為變更),經稽核指出後已還原** —— 見下方「本輪踩到紅線的那一次」。 |

#### ⚠️ 本輪踩到紅線的那一次(據實記錄,不美化)

本輪初稿把 `ui/tab6_manual.py` 那一行的 `CLAUDE.md` 改成 `EXCEPTIONS.md`。
**那是純註解、零行為變更**(本組與稽核各自確認過),**但它是舊分頁的程式碼**,
而客戶原話是「**絕對禁止直接覆寫、修改或破壞現有線上正常運作的舊版 Tab 代碼**」。
**已還原**(`git show origin/main:ui/tab6_manual.py > ui/tab6_manual.py`),
並以 **sha256 逐檔比對 16 個 `ui/tab*.py`** 確認全部與 `origin/main` 完全相同。

⚠️ **為什麼「看起來無害」不是理由 —— 這一段比那一行重要**:
1. **同一條紅線在同一天已經擋掉一個明顯正確的修正**(舊 ④ 帳本 `get_latest_nav` 走錯層的 import,
   改一行就好、L2 facade 早就存在),那次也是只登記、沒有動它。
   **如果註解可以改、import 不可以改,那條線就變成由執行者逐案解釋 —— 那不是客戶給的規矩。**
2. **驗收手段會被毀掉**:舊分頁的驗收一直是 **sha256 逐檔比對**,又便宜又不會誤判;
   **一旦開了「註解例外」,這個檢查就沒有了**,要改成逐行判讀「這算不算行為變更」。
3. ⚠️ **本組在還原後的第一次自檢寫了一個空轉的迴圈**
   (`git ls-tree ... -- 'ui/tab*.py'` 這個 pathspec **回 0 個檔**,迴圈一次都沒跑,
   卻印出「✅ 全部相同」)。**改用 `grep '^ui/tab.*\.py$'` 後才真的比到 16 個檔,並加了正對照**
   (故意比一個一定不同的檔,證明迴圈真的會抓到差異)。
   → **一個沒有正對照的綠燈,和沒有檢查是同一件事** —— 同 `8.2.A` 驗證段的既有教訓。

⚠️ **相對地,下列檔案本輪判定為【不在紅線內】,已改指新檔**:
`ui/helpers/**`(如 `ui/helpers/fund_research/code_finder.py`、`ui/helpers/io/data_registry.py`、
`ui/helpers/portfolio/policy_admin_section.py`)、`ui/components/**`、`ui/views/**`。
⚠️ **這三個路徑本輪初稿寫成了相對於 `ui/helpers/` 的短路徑,被本 PR 自己修好的那支守衛當場抓到**
(`test_constitution_has_no_dangling_file_references` → 3 筆「檔案不存在」)——
**順帶證明它真的在看新檔,不是只在看 `CLAUDE.md`。**
**理由**:客戶紅線寫的是「舊版 **Tab** 代碼」,**helper / component / 新頁不是 Tab 檔**;
且 helper 是**新舊共用的基礎設施**,凍結它會讓**新頁也拿到過期指標**。
⛔ **這是 AI 總管的判斷,不是客戶明示**(§-2 規則 6)——
若客戶認為 helper 也該凍結,本段即刻撤銷、那些回指一併還原。

⚠️ **兩筆都「指得到、但會多繞一趟」,不是死連結**:`CLAUDE.md` 的 `8.2.A` 原位置
**刻意留了指標段、節號也沒改**,照舊寫法找過來的人會落在那一段並被轉過來。
**登記在這裡是為了讓它可被稽核,不是為了讓它被忘掉。**
⚠️ **觸發點**:哪天客戶解除那兩個檔的凍結、或那兩行因其他任務被碰到 → **順手改掉**。

---

**8.2.A 已知例外清單**(豁免 §8.2 硬規則的特定模式,需明確標註理由):

| ID | 檔:行 | 例外規則 | 理由 |
|---|---|---|---|
| **EX-CACHE-1** | L1 全層(實際適用 v19.244 R12 audit:1 fetcher `repositories/hot_money_repository.py` `@st.cache_data(ttl=TTL_30MIN)` ×2;`news_repository.py` docstring 明說「不在這層 cache」,純 fetcher) | `@st.cache_data` / `@_ttl_cache` 條件 import | Streamlit Cloud cache 是部署架構核心,提供跨 session 共享 + TTL 自動失效,functools.lru_cache 不等價。**允許**在 L1 模組頂部寫 `try: import streamlit as st / except ImportError: 定義 no-op fallback decorator`,前提:**完全不用** `st.session_state` / `st.error()` / `st.markdown()` 等真 UI 呼叫。Fund 端 `@_ttl_cache(ttl_sec=N)` 為 custom 實作不依賴 streamlit,本例外主要適用 `@st.cache_data` 直接用法。 |
| **EX-AI-1** | `services/ai_service.py` 全檔 public 函式 | LLM 輸出回 **str** 而非 dataclass | 既有 multiple caller 全部以 st.markdown 渲染字串,改 dataclass 需大規模 migration。**緩解措施**:所有 AI 字串強制帶視覺旗標(`### 🧬 AI ... **使用模型**: <model>`),caller 可用 string prefix 偵測;module docstring 強制宣告「禁止從 LLM 字串萃取數字當 data input」。違反此 caller 規則 = §2.2 反捏造違憲,須立刻修。<br>📌 **2026-09-08 條文與實作已脫節,逐道核對如下**(**有意識的更正,不是漏刪** · 日期 **2026-09-08** · 決策者:**AI 總管** · 依平行獨立調查組指出並由本組重跑複驗)。⛔ **本次刻意不對上面那三道緩解措施加刪除線,理由必須先講**:(1) 它們的句子寫成**規範**(「**強制**帶視覺旗標」「docstring **強制**宣告」,末句還自稱「此 caller **規則**」),而不是「我們現在有這些東西」的描述 —— **劃掉一條規範 ＝ 放寬這個例外的條件**,那正是本檔與本輪派工單都明禁的事;(2) 逐道查完之後,**三道裡有兩道其實還活著,只是不住在條文寫的地方** —— **劃掉一句為真的話,是往另一個方向說謊**(§-2.A 慣例:只有**被權衡掉的條文**才劃線)。**故本註只補事實,一個字都不刪。**<br>**① 視覺旗標(`### 🧬 AI … **使用模型**: <model>`)—— 在條文寫的那個層級不存在。**實測 `git grep -n "🧬" -- '*.py'` 與 `git grep -n "使用模型" -- '*.py'` **兩條皆 0 命中**(全 repo 唯二的 `🧬` 命中是本檔這一行自己與 `STATE.md` 的章節標題,都不是實作)。取數鏈的終點 `infra/llm.py::_call_gemini` 成功路徑是 `return txt or "⚠️ Gemini 回傳空結果"` —— **回的是把 `candidates[0].content.parts` 的 text 接起來的原始 LLM 文字,沒有任何包裝、沒有模型名、沒有標記**;`services/ai_service.py::_gemini` 只是 thin delegate,**不加東西**。⚠️ **但「AI 標示」本身沒有消失,它整層下移到了 L3 渲染層,而且不是同一個東西**:四個消費者都在字串**外面**畫 `🤖` 標題/caption(`ui/helpers/ai_summary.py`、`ui/helpers/io/freshness.py`、`ui/views/page_01_macro.py`、`ui/tab3_t7_ledger.py` 皆有 `🤖` 命中),`ai_summary.py` 甚至有一行 `st.caption(f"💡 模型：{_key_note}…")` —— ⚠️ 但 `_key_note` 實測是 `f"{_n_keys} 把 key 輪替"` 或 `"Gemini"`,**是 provider／key 數,不是模型識別碼**;而且它是**字串旁邊的 caption**,不是**字串裡面的 prefix** → **`startswith` 結構上偵測不到**。⇒ **條文說的東西(給 caller 偵測用、長在字串本體上)沒有;實際有的是給人看的、長在畫面上的標示。兩者不可互相頂替。**<br>**② 「caller 可用 string prefix 偵測」—— 機制是活的,但它偵測的是另一個前綴。**沒有任何 caller 偵測 `🧬`／`使用模型`(① 不存在,無從偵測);但 `ui/tab3_t7_ledger.py` 確實有 `isinstance(_mk_txt, str) and _mk_txt.startswith(("❌", "⚠️"))` —— 那吃的是 `infra/llm.py` 的**失敗訊息契約**(各失敗分支回 `❌…`／`⚠️…`,`services/ai_service.py::_gemini` 的 docstring 也把這份契約寫成 API 的一部分)。⇒ **prefix 偵測是活的,但它答的是「這次失敗了嗎」,不是本列要它答的「這段字是 AI 生的嗎」。**<br>**③ 「module docstring 強制宣告禁止從 LLM 字串萃取數字」—— 禁令沒被刪,是搬到了下游。**它**不在**本列第 2 格指名的 `services/ai_service.py`(該檔 413 行,`grep -nE "禁止\|萃取\|數字"` **0 命中**;寬掃 `grep -nE "###\|模型\|model\|旗標\|prefix\|前綴\|標記\|LLM\|不得\|嚴禁\|禁"` 只命中 3 行,最接近的一行是 `_gemini` docstring 的「失敗:degraded message(⚠️/❌ 開頭)」,那是**失敗標記**,不是本列說的那個旗標);**它在 `repositories/ai_cache.py` 的模組 docstring**,逐字為「§EX-AI-1:只存 / 回顯 AI 自己的 markdown 字串,**不從中萃取數字當 data input**。」另有一道**給人看的**同義防線在 L3:`ui/views/page_01_macro.py` 在 `st.markdown(_text)` 之後接 `st.caption("⚠️ AI 生成內容僅供參考，數字一律以上方各區塊的讀數為準。…")`。⇒ **「強制」二字不成立** —— 它不是全體 AI 輸出模組的硬性要求,是**一個持久化模組自己寫的**;**但「這條禁令不見了」同樣不成立。**（⚠️ 本段兩條 `grep` 指令裡的 `\|` 是**為了不切斷表格欄而 escape 的**，實跑時請去掉反斜線 —— 沿用 §-1.5.1c 判定 4 對「融資餘額」那條指令的既有寫法。）<br>⚠️ **嚴重度據實下修:這是文件脫節,不是活的反捏造違憲。**`services/ai_service.py` 的**四個** production 消費者(`ui/helpers/ai_summary.py`、`ui/helpers/io/freshness.py`、`ui/tab3_t7_ledger.py`、`ui/views/page_01_macro.py`;`app.py` 只 import `get_gemini_keys`,拿的是 key 清單不是 AI 字串)**逐一讀過,全部只把字串交給 `st.markdown` 渲染,沒有一處從中萃取數字**。⚠️ **但今天不違憲靠的是那四個 caller 剛好都只渲染,不是靠任何機器規則** —— **沒有守衛在守這件事。**⚠️ **而且路已經鋪好,只差有人踩上去**:`services/ai_service.py::_gemini` 的 `force_json: bool = False` → `infra/llm.py` 的 `response_format="json"`(Gemini `responseMimeType`／OpenAI `json_object`,**native JSON mode**)**production 0 caller,實測命中全在 `infra/llm.py` 與 `services/ai_service.py` 的定義與傳遞、以及 `tests/`**;**一旦有人打開它,下一步幾乎必然是 `json.loads()`,那就是活的違憲。**<br>⛔ **本列因此不是「沒有緩解措施的豁免」,而是「緩解措施還在,但住的地方跟條文寫的不一樣」** —— ⚠️ **兩種讀法都不要跳過**:若把上面三道讀成**規範**,現況是**規範未被落實**(① 完全沒有,②③ 落在別的層);若讀成**描述**,現況是**描述不準**。**兩種讀法指向同一件事:一個只讀本列的人,會以為「回 str 的風險已經被本列的緩解措施圍住了」—— 沒有被圍住,至少不是被本列寫的那些東西圍住的。****該把緩解措施搬回條文寫的層級、還是該把條文改寫成描述實況,是總管裁決,已依 §8.3.P 格式登記為 `P-AIFLAG-1`。**<br>⚠️ **本組未查證、不宣稱的部分(§-2 規則 6)**:**(i)** 本組**沒有**回頭讀 v11.0 C-17 之前的 history,**故不宣稱這三道緩解措施「當初存在、後來被刪」** —— 本註只說**今天在哪裡、不在哪裡**;替它編一個「當初有」的故事,與它現在的不準確是同一種造假。**(ii)** 「**沒有任何 caller 從 AI 字串萃取數字**」是**取決於有沒有漏看**的全稱句,本組是逐一讀那四個消費者得到的,**只能當待驗事項**,⛔ **不得**被引用為已查證的事實去替本列結案;上列字表也掃不到動態組出的字串、`getattr`／別名 re-export 等非字面寫法(§-1.5.1c 判定 2 的方法教訓:**字表選錯,再跑一百次也會漏同一處**)。⚠️ 本段由**架構與文件組單組**實測產出(平行獨立調查組先指出射程問題、總管複驗,本組依 §-2.A 第 7 款**逐條重跑**才落筆),**仍未經第二組對本段成文內容獨立驗證**。 |
| ~~**EX-POLICY-1**~~(v19.212 P0-3-#4 退役) | ~~`services/allocation_simulator.py:34-97`~~ | ~~`DEFAULT_PHASE_SCRIPT` + `STRATEGY_PRESETS` 保 inline~~ | **退役原因**:~~`ui/tab_allocation_simulator.py`~~ consumer 已在 P0-2 v18.x 刪除,~~`services/allocation_simulator.py`~~ 6/6 fn 全 dead(production 0 caller),v19.212 整檔 866 LOC 拔毒(含 2 test 孤兒)。EX-POLICY-1 例外對象消失,退役。 |
| **EX-CRUD-1** | UI 直呼以下 L1 repository:`policy_repository` / `snapshot_repository` / `ledger_repository` / `batch_checkpoint` / `pool_repository` / `portfolio_perf_repository`(v19.407 補登前四;v19.428 補登 pool;v19.430 補登 portfolio_perf;Google Sheets / 本地 JSON 持久化) | L3 UI 可直接 import L1 CRUD repository | §8.2 規則「L3 不得直呼 L1 — cache 才能集中」的核心理由是**外部 HTTP fetcher 的 TTL cache 須集中管理**。本組 repository 為**本地持久化**(read+write 同檔),**無 `@_ttl_cache` / `@st.cache_data` 裝飾**,亦無外部 HTTP I/O — 不存在「cache 分散」問題。為純 CRUD 加 L2 pass-through wrapper = §8.1 step 6「用不到的抽象」反例。`ui/helpers/cloud_io.py` / `v2_editor.py` / `oauth_state.py` + `ui/tab3_portfolio.py` / `tab3_t7_ledger.py` 直接 import 為允許用法;`ui/tab_batch_analysis.py` 直呼 `repositories/batch_checkpoint.py`(批次分析磁碟續存,v19.407)為同精神新增。**v19.428**:`repositories/pool_repository.py`(換股顧問選股池,GS worksheet + 本地 JSON fallback,read+write 同檔、無 cache 裝飾、無外部 HTTP)由 `ui/helpers/fund_grp_health/switch_advisor_section.py` 直呼,同精神新增(檔頭已註解指回本表)。**v19.430**:`repositories/portfolio_perf_repository.py`(組合績效永久快照,GS worksheet `_portfolio_perf_history` + 本地 JSON fallback,read+write 同檔、無 cache 裝飾、無外部 HTTP)由同一 `switch_advisor_section.py` 直呼,同精神新增。F-H6 v19.79 決策。<br>📌 **2026-09-08 前提句事實登記:上面那句前提對成員 `pool_repository` 不成立**(**有意識的更正,不是漏刪** · 日期 **2026-09-08** · 決策者:**AI 總管**)。⛔ **本次只登記這個縫,不裁決它該歸哪一條例外,也不動任何成員的資格** —— **前提句刻意不加刪除線**:它是本例外**賴以成立的條件**(「無 cache 裝飾 ⇒ 不存在 cache 分散問題」),**劃掉它 ＝ 把這個例外的門檻拿掉**,那會讓任何 L1 都能被收進來;要改的是「這個成員該不該留在這裡」,不是「條件還要不要」。<br>**實測三件事,逐一分開讀,不要合併**:**(1)** `repositories/pool_repository.py` **確實有 cache 裝飾** —— 同檔 `@_st_pool.cache_data(ttl=_TTL_POOL, show_spinner=False)` 套在 `_cached_pool_map()` 上(`_TTL_POOL` 走 `shared/ttls.py` 的 `TTL_30MIN`,SSOT 沒問題),另有 `_clear_pool_cache()` 呼叫 `.clear()`。⇒ **本列前提句的「無 `@st.cache_data` 裝飾」對這個成員為假。****(2)** **該檔自己貼的是 `EX-CACHE-1`,不是無登記** —— 裝飾點上方就地寫著「EX-CACHE-1:App 端走 st.cache_data(跨 rerun 共享 + TTL);headless 退無快取」,而它的 module docstring **同時**自陳「§8.2 EX-CRUD-1:本地持久化 CRUD(讀+寫同檔、**無 TTL cache**、無外部 HTTP fetcher)」 —— **同一個檔的兩處自我登記互相矛盾**,這一點是本次才被讀出來的。**(3)** **但 `EX-CACHE-1` 的成員清單裡沒有它** —— 本表 `EX-CACHE-1` 該列第 2 格寫的是「實際適用 v19.244 R12 audit:**1 fetcher** `repositories/hot_money_repository.py`」。<br>⇒ **它落在兩條例外的縫裡**:`EX-CRUD-1` 的前提句**排除**它,`EX-CACHE-1` 的成員清單**沒有**它。⚠️ 順帶一提,它**也不是「不上網」** —— 同檔走 `get_gspread_client(...)` → `open_by_key(...)`,是往返 Google Sheets API 的**遠端呼叫**;那正是 §8.3.P **`P-UIGSPREAD-1`** 尚未裁決的那個問題,⛔ **本註不對該問題表態,也不得被引用來替它結案**。<br>⚠️ **本組未查證的部分(§-2 規則 6)**:**本組沒有逐一檢查本列其餘五個成員**(`policy_repository` / `snapshot_repository` / `ledger_repository` / `batch_checkpoint` / `portfolio_perf_repository`)**是否也帶 cache 裝飾** —— 那是「有沒有漏看」型的問題,依 **§-2 規則 4／5** 須**另派獨立一組**,且**不可由本組自己查自己**(本組正是登記這一列的人)。⛔ **故本註只說「前提句對 `pool_repository` 不成立」,不說「只有它一個」,也不說「其餘都沒問題」。****已依 §8.3.P 格式登記為 `P-POOLCACHE-1`(含其餘成員的清點義務)。** |
| **EX-PASSTHRU-1**(v19.251 補登 3 + v19.273 補登 1 + v19.377 B2c 補登 2 entry + **v19.531 Phase 1.2 補登 1**) | UI 直呼以下 L1 facade fetcher(~~共 8 組~~ → **共 9 組**;2026-08-28 新增 `fetch_benchmark_close`)⚠️ **「9 組」是含已退役成員的累計數,不是現行有效數** —— 其中 `get_latest_fx`(v19.247 R16 升級)與 `fetch_fund_by_key`/`fetch_nav_history_long`(v19.314 退役)都已加刪除線,**現行有效只有 7 組**。8→9 的增量與本表既有計數慣例一致(退役成員保留在列、計數不回頭扣),**故刻意不改數字**(改了反而與慣例不一致);此註為 2026-08-28 稽核點名後補,防讀者誤判。:<br>- **`repositories.fund.tdcc_search_fund`**(~~`ui/tab2_single_fund.py:147`~~ → **`ui/helpers/fund_research/code_finder.py::_search`** lazy import,單一 caller / 單一 call site)— 多 endpoint(TDCC 3-2 + 3-4)整合 + dedup + nav merge + keyword match。**2026-08-31 WP-F 路徑更正**(**有意識的更正,不是漏刪** · 日期 **2026-08-31** · 決策者:**AI 總管**)。⚠️ **舊登記在寫下的當天是對的,不是當初寫錯** —— `ui/tab2_single_fund.py` 當時確實是`tdcc_search_fund` 的唯一 UI 呼叫點,登記它完全正確。**被權衡掉的是它的前提**:七→五接線(#744)移除了 `render_single_fund_tab` 的舊入口,該函式內那份摺疊搜尋框因此成為 production 恆不觸發的死碼(唯一 caller `ui/tab_fund_research.py::_render_single_mode()` 永遠持有 `SHARED_SEARCH` 旗標),已於同批**整段實體刪除** —— **舊登記路徑自那一刻起指向一個不存在的呼叫點**,而真正活著的 `code_finder.py` **從未登錄**(＝本節末句明禁的「未經登錄的軟例外」)。故本次補正。⚠️ **不寫行號**:沿用姊妹 repo `my-stock-dashboard` §8.2.A.0 規則 1(**本檔無 §8.2.A.0 一節**,該規則出自姊妹 repo;同 `EX-UICACHE-1` 該列的既有註記)——「檔案路徑 + 符號名 + 模式描述」登記,行號在任何一次重構後就失效,而重構**不會**觸發本清單更新。**本次被更正的舊登記正是行號寫法的實例。**⚠️ **本批把 UI 呼叫點由 2 個收回 1 個(2 → 1),不是新增一個** —— 故本列末段的升級觸發條件「**本 fetcher 出現第二個 UI caller**」**目前未命中**(方向相反:fan-out 變小,不是變大),EX-PASSTHRU-1 對本成員繼續成立。**守衛**:`tests/test_fund_research_merge.py::test_code_finder_is_the_only_search_entry`(AST 數 `ui/**` 內的 `tdcc_search_fund` 呼叫點必須恰好 1;刻意**不用字串 grep** —— `code_finder.py` 的 docstring 自己就提到這個符號名,grep 會被騙)。⚠️ 該守衛守的是**呼叫點數量**,**不守**本登記路徑本身的正確性 —— 本 repo 目前沒有任何機器規則在守「例外表寫的路徑存不存在」,讀者請據此打折信任本欄路徑。📌 **2026-09-08 狀態變更登記:本 fetcher 多了一個 L2 消費者(#828)**(**有意識的狀態變更,不是漏刪** · 日期 **2026-09-08** · 決策者:**AI 總管**)。**本次不退役本成員,也不改本列任何既有文字 —— 只補登記**;為什麼一個字都不劃掉,見 (5)。(1) **狀態本身**:`repositories.fund.tdcc_search_fund` 自 #828 起多了一個 **L2 消費者** —— **`services/fund_search.py::search_funds`**(thin facade:lazy import、不加工、不快取、不 try/except)。新 ⑧ **`ui/views/page_03_research.py`** **經由該 L2 取數,不直呼 L1**(本組實測:該檔 module-level `from services.fund_search import ... search_funds`,全檔對 `tdcc_search_fund` 只有 docstring 與註解提及、**沒有呼叫**;守衛掃 `ui/**` 得到的唯一命中仍是 `ui/helpers/fund_research/code_finder.py`,正面佐證這一點)。(2) **為什麼「還沒」退役 —— 這是半個 R16**:UI 直呼**仍然只有** `ui/helpers/fund_research/code_finder.py::_search` 一處,而那一處**沒有**改走 L2(客戶明令不得改動線上運作中的舊 Tab)。對照本列 `get_latest_fx` 的退役前例 —— 它是把 **9 檔 / 18 處 UI caller 全數 migrate 到 L2 之後**才升級退役的;本成員只完成了「**L2 入口已經存在**」那一半,**「所有 caller 都走 L2」這個退役前提尚未成立**。→ **EX-PASSTHRU-1 對本成員繼續有效,本次不退役。**(3) ⚠️ **升級觸發條件的射程缺口 —— 這才是本次登記的重點**:本列末段的條件寫的是「**本 fetcher 出現第二個 UI caller**」,它問的是 **UI** 的 caller 數,**結構上看不到 L2 消費者**。這次長出來的正是 L2,所以那句話**字面恆不命中**,而且**方向相反** —— 新增 L2 不是違規,正是本列自己指定的處置方向(該條件原文:「fan-out 一旦出現…應比照 R16 上提 L2」)。**守衛同型**:`tests/test_fund_research_merge.py::test_code_finder_is_the_only_search_entry` 只走訪 `ui/**` 底下的 `.py`,**結構上掃不到 `services/fund_search.py`**。⚠️ **而且那條守衛的射程缺口有兩個,兩個都要記(本組實測,不是靜態推論)**:**(i)** 它的走訪寫成 `for _p in sorted(ROOT.glob("ui/**/*.py")):` —— **`services/` 一個檔都不在其中**(本組跑同一條 glob,對 `services/` 命中 **0**);**(ii)** 它只數 `ast.Call`(同檔 `if not isinstance(_n, ast.Call): continue`),因此**一級函式引用**形態數不到 —— 本組以 `ast` 實測 `_ex.submit(tdcc_search_fund, kw)` 這種寫法,**被數成 0 個呼叫**(~~該處只留下一個裸 `ast.Name` 節點~~ → ⛔ **2026-09-08 措辭更正:是兩道過濾、兩個節點,不是一道**(**有意識的更正,不是漏刪** · 日期 **2026-09-08** · 決策者:**AI 總管**)。**結論一字未改**(該形態確實被數成 0),**錯的是機制描述**。**本組以 `ast` 實跑 `_ex.submit(tdcc_search_fund, kw)` 的節點與該守衛的兩道判斷,逐節點列出**:外層 `_ex.submit(...)` **確實是一個 `ast.Call`**,**通過**了第一道 `if not isinstance(_n, ast.Call): continue`;擋掉它的是**第二道** —— 它的 `_n.func` 是 `ast.Attribute`,`getattr(_n.func, "id", None) or getattr(_n.func, "attr", None)` 解析成 **`'submit'`**,`!= 'tdcc_search_fund'`。而 `tdcc_search_fund` 自己是**引數位置的裸 `ast.Name`**,那一個才是被**第一道**擋掉的。⇒ **兩個不同的節點各被一道不同的過濾擋下,合計 0 命中**;對照組 `results = tdcc_search_fund(kw)` 在同一段複刻邏輯下**命中 1**。**舊表述的用意仍然成立**(它要說的是「這個形態守衛看不見」,那是對的);**被權衡掉的是它的機制描述** —— 它把「外層有一個 Call 但名字對不上」講成「連 Call 都沒有」,會讓下一個想補這條守衛的人補錯地方:**只放寬 `isinstance` 那一道是沒用的,真正要處理的是「函式被當成引數傳給包裝器」這件事**。⚠️ **本更正只改這句括號的措辭,不改守衛一個字,也不建議放寬它**(改 `.py` 不在本批授權內)。)。⇒ **#828 加了一個 L2 消費者,這條守衛照樣綠燈** —— 它與那句「第二個 **UI** caller」**是同一個盲點的兩個副本**,不是兩道獨立防線。⚠️ **本組是以 `ast` 複刻該守衛的走訪邏輯實跑,並非執行 pytest 本身**(本環境未安裝 pytest,且不得 `pip install`);複刻結果:`ui/**` 唯一命中為 `ui/helpers/fund_research/code_finder.py`。⇒ **條文與守衛都不會因為長出一個 L2 消費者而有任何反應** —— 一個只讀本列的人,不會知道這支 fetcher 已經有 L2 入口了。**本段登記就是那個缺席的反應。**⚠️ **連帶提醒一句本列既有文字的讀法**:上面那句「本批把 UI 呼叫點由 2 個收回 1 個…方向相反:fan-out 變小,不是變大」講的是 **2026-08-31 WP-F 那一批**,對那一批**仍然為真**;但**不得**被讀成「本 fetcher 的 fan-out 至今只縮不長」—— 2026-09-08 它確實長了,只是長在 **L2 方向**,而那句話的框架(UI 呼叫點)看不到那個方向。⚠️ 這與 §8.2.A.1 驗證段 ④ 記載的失效模式同型:「**條件只往外用、不往內用**」—— 條件只朝一個方向問,另一個方向長出東西時它什麼都看不到。**本次刻意不改那句條文、也不改守衛**(改條文屬總管裁決;改 `.py` 不在本批授權內),只把缺口寫出來。⚠️ **補一件結構事實:這條觸發條件是「列級」的,不是本成員專屬** —— 它寫在本列**理由欄的最末段**,由本列**所有現行成員共用**(該列自陳現行有效 7 組;**本組未逐一重數,沿用該列自己的數字**)。而它括號裡引的「(a) 的理由即失效」,那個 (a) 是寫在 **`fetch_benchmark_close` 的理由段**裡的「1 caller / 1 call site」—— **一條列級的條件,掛著一個成員級的理由**。這正是它對本成員失準的結構原因:本成員的 fan-out 長在 **L2**,而 (a) 問的是 **UI**。📌 **一則本組實測、但不在本 bullet 射程內、本組不處置的觀察**:同列另有三個現行成員在**登記當天就已經寫著兩個 UI 呼叫點**(`fetch_market_news`、`fred_get_next_release_date`、`fetch_stock_news`,各自括號內都是兩條 `ui/…` 路徑)—— 也就是「出現**第二個** UI caller」這句話對它們**一出生就已經成立**。⛔ **本次不動那三個成員,也不對其合憲與否表態**(檔案邊界只到本 bullet),僅具名回報總管,**總管已表示另行派工**。它之所以寫在這裡,是因為它是**同一條列級條件**的判別力證據,**不是**那三個成員的稽核結論。(4) ✅ **補一條可被觸發的退役條件(正例,不是只講禁令)**:**當 `ui/helpers/fund_research/code_finder.py::_search` 也改走 `services.fund_search.search_funds` 時,本 fetcher 即無任何 UI 直呼、R16 式退役條件成立 → 應退役「本成員」(＝把 `tdcc_search_fund` 這一個 bullet 整段加刪除線並註明理由);`EX-PASSTHRU-1` 整列與其餘成員不受影響、不得一起退役。**(5) ⚠️ **本次刻意不動的三處,逐一寫出理由,免得被讀成漏改**:**(a)** 本 bullet 開頭那句「**單一 caller / 單一 call site**」**刻意不加刪除線** —— 在本列自己的框架(表頭「UI 直呼以下 L1 facade fetcher」)之內它**今天仍然為真** —— 本組以 `ast` 複刻該守衛的走訪邏輯實跑,`ui/**` 唯一命中仍是 `ui/helpers/fund_research/code_finder.py`(⚠️ **複刻,不是跑 pytest 本身**);**劃掉一句為真的話,是往另一個方向說謊**,而本檔的刪除線慣例針對的是「被權衡掉的條文」,不是仍然成立的事實。⚠️ **但它的論證用途已被事件超越,這一點必須講明**:同列 `fetch_benchmark_close` 把「**1 caller / 1 call site**」當成理由 (a) ——「fan-out 在此不存在,故不必上提 L2」;而本成員的 fan-out **已經長出來了**,處置**正是建 L2**。→ **數字仍為真,它曾支撐的那個推論不再適用。** ── **(b)** 本列表頭的「**共 9 組**」**不動** —— 那個計數的定義是「**UI 直呼**的 L1 facade fetcher 組數」,本次新增的是 **L2 消費者**,不落在該定義內;動它反而會讓那個數字開始說謊。**(c)** 守衛 `test_code_finder_is_the_only_search_entry` **一個字未改** —— 本批不得改任何 `.py`,且它守的「UI 層恰好一個呼叫點」今天仍然是對的需求。📌 **本次一併帶進來的取數頻率缺口,另立登記**:`services/fund_search.py` 檔內已具名的 **`GAP-SEARCH-CACHE-1`** 原本只活在該檔 docstring 裡,**沒有由誰查、沒有觸發點** —— 正是 §8.3.P 前言所指「待查證沒有出口 ＝ 實質永久豁免」的形狀,本輪依該節格式搬入正式住所(與 `P-WHERECONTENT-1` 同型:實作組自己揭露、但登記在錯的地方)。⛔ **本 bullet 不重述其內容**(§2.1:同一件事只准一份真相源),請直接讀 §8.3.P 該列。⚠️ **本段的驗證範圍與未驗部分(§-2 規則 6)**:上列每一條路徑與符號**本組已實測**(指令:`git grep -n "tdcc_search_fund" -- '*.py'` 與 `git grep -n "fund_search" -- '*.py'`,命中須**人工逐一判讀**是呼叫、import 名單還是註解/docstring)。已判讀出的兩筆**非呼叫**命中:`fund_fetcher.py` 是 legacy re-export shim 的 import 名單、`ui/tab2_single_fund.py` 是沿革註解。⛔ **本組不宣稱「全 repo 只有這兩個消費者」** —— 那是取決於「有沒有漏看」的全稱句,且上列字串指令**掃不到**動態 import、`getattr`、別名 re-export 等非字面寫法(同 §-1.5.1c 判定 2 的方法教訓:**字表選錯,再跑一百次也會漏同一處**)。**故本段只給已知分類:UI 直呼 1 處 ＋ L2 消費者 1 處,不是窮舉。**⚠️ 本段由**架構與文件組單組**產出,**未經第二組獨立驗證**。<br>- ~~`repositories.fund.get_latest_fx`~~ **(v19.247 R16 升級)**:9 caller files / 18 call sites 全 migrate 至 L2 `services.fund_service.get_latest_fx`(thin facade 呼 L1 實作,L1 業務 0 改動)<br>- `repositories.news_repository.fetch_market_news`(~~`ui/tab1_macro.py:1188`~~ → **`ui/tab1_macro.py::render_macro_tab`**,module-level import,**且是自根目錄相容 shim `fund_fetcher` 進來的**(`from fund_fetcher import fetch_market_news`),呼叫形態為 `_ex_ml.submit(fetch_market_news, max_per_feed=5)`(ThreadPoolExecutor,**不是**直呼)/ ~~`ui/tab3_t7_ledger.py:2710`~~ → **`ui/tab3_t7_ledger.py::render_t7_section`**,module-level `from repositories.news_repository import fetch_market_news`,在「📰 抓取近期國際財經新聞 + 系統性風險事件 (RSS)」spinner 內直呼)— 5 RSS feeds 並聯 + keyword filtering + systemic risk classify + dedup + sort<br>- ~~**`repositories.fund.fetch_fund_by_key` / `fetch_nav_history_long`**(`ui/tab_crisis_backtest.py`)~~ **(v19.314 退役)**:危機回測 UI 整功能拔除,此 2 條 UI 直呼例外一併退役<br>- **`repositories.fund.diagnose_fx_sources`**(~~`ui/tab5_data_guard.py:832` lazy~~ → **`ui/tab5_data_guard.py::render_data_guard_tab`**,「🔍 跑 FX 來源診斷」按鈕(`key="_d5_fx_btn"`)內 lazy `from repositories.fund import diagnose_fx_sources`)— Tab5 資料看板 diagnostic,L1 內部用 + UI 直呼合理<br>- ~~**`repositories.macro_tw_local_repository.{fetch_ndc_signal_history,fetch_tw_pmi_local,fetch_tw_export_yoy,fetch_foreign_consecutive_days}`**(`ui/tab1_macro.py:821` lazy,4 fn 一組)— TW 本地總經 self-contained L1 fetcher(FinMind 單源),v19.197 P1-4 從 `macro_tw_local_fetch.py` 下沉 repositories,UI 直呼取數後在 L3 端 regime 判讀。**v19.268 D8 #7 後**:UI 端 `_safe_tw()` wrapper 已加 schema 驗證(`validate_ndc_signal_dict` 等),取數即驗,純 fetch facade 無 L2 業務需上提。~~<br>　→ **2026-09-01 登記事實更正**(**有意識的更正,不是漏刪** · 日期 **2026-09-01** · 決策者:**AI 總管**)。**上段登記的四項具體事實中,有三項今日為假**,逐一列出(全部本組實測):<br>　**① 呼叫點路徑是假的。** `ui/tab1_macro.py` **完全沒有** import `macro_tw_local_repository`(實測 `git grep -n "macro_tw_local" -- 'ui/**'`:命中僅 `ui/helpers/io/data_registry.py` 的**註解與 session_state 讀取**、以及 `ui/helpers/macro/ndc.py`)。~~**`ui/tab1_macro.py:821` 那一行今日是 `if not FRED_KEY:`,與 TW-local 取數無關。**~~→ ⛔ **2026-09-01 同日更正:上句的行號斷言為假**(決策者:**AI 總管**)。實測 `:821` 是一行**註解**,`if not FRED_KEY:` 在 **`:826`**,差 5 行。**「`:821` 沒有那個 import」這個結論不變**,錯的是我為了佐證它、順手又報了一個**沒核對過的行號**。⛔ **而這句話就寫在一段大談「行號會失效」的文字裡** —— **本節在講的那個病,在講它的那句話裡當場發作。**故本次改為**不寫行號**:`ui/tab1_macro.py` **全檔**未 import `macro_tw_local_repository`(驗證指令見下,不依賴任何行號)。<br>　**② 「4 fn 一組」是假的,現行有效只有 1 個。** 實測 production 呼叫點:**只有 `fetch_ndc_signal_history` 還活著**,唯一 UI 呼叫點是 **`ui/helpers/macro/ndc.py::_fetch_ndc_score`**(函式內 lazy import,單一 caller / 單一 call site);另三個(`fetch_tw_pmi_local` / `fetch_tw_export_yoy` / `fetch_foreign_consecutive_days`)**除定義處外無任何 import 或呼叫**,唯二命中為 `services/macro/_helpers.py::_TAB1_TTL_CACHE_NAMES` 的**字串名**(快取清除清單,不是 import)與 `shared/schemas.py` 的註解。<br>　**③ `_safe_tw()` schema 驗證那句是假的。** 實測 `git grep -n "_safe_tw" -- '*.py'` **0 命中**;`validate_ndc_signal_dict` 除 `shared/schemas.py` 的定義與 `tests/` 外**無 production consumer**。→ **「取數即驗」現況不成立**,該句**不得再被引用為「本組成員已有出口驗證」的理由**。<br>　**④ 唯一仍然成立的是分層判定本身**:`ndc.py` 對 L1 的呼叫是**純取數委派**(取回 dict → 同檔純函式 `ndc_score_from_result` 取 `score_latest`,無多源 fallback、無跨 fetcher TTL 統一、無結果加工),**EX-PASSTHRU-1 對 `fetch_ndc_signal_history` 繼續成立**。且**檔案端回指註解是有的** ——`ui/helpers/macro/ndc.py` 檔頭 docstring 自陳「EX-PASSTHRU-1:UI 可直呼此自足 TW-local fetcher」。<br>　⚠️ **這是同一個坑的第二次,不是新病**:本列 `tdcc_search_fund` 那一項已於 **2026-08-31** 因**完全相同的失效模式**被更正過 —— **登記寫死行號 → 重構後路徑指向一個不存在的呼叫點,而真正活著的那個從未登錄(＝本節末句明禁的「未經登錄的軟例外」)**。當時該項已就地寫下「**行號在任何一次重構後就失效,而重構不會觸發本清單更新**」這個教訓,**卻沒有把同一把尺套到同一列的其他成員** —— 若當時套了,本項會在那一輪就被抓到。**這正是 §8.2.A.1 驗證段 ④ 記載的「條件只往外用、不往內用」,在同一個表、同一列上第二次發生。**⚠️ 且該項當時已自陳「**本 repo 目前沒有任何機器規則在守『例外表寫的路徑存不存在』**」—— **本次即為該缺口的第二個實例**。<br>　**本次登記(不寫行號,沿用姊妹 repo `my-stock-dashboard` §8.2.A.0 規則 1:檔案路徑 + 符號名 + 模式描述)**:**`repositories.macro_tw_local_repository.fetch_ndc_signal_history`**(**`ui/helpers/macro/ndc.py::_fetch_ndc_score`** lazy import,單一 caller / 單一 call site)— TW 本地總經 self-contained L1 fetcher(FinMind `TaiwanBusinessIndicator` 單源),v19.197 P1-4 從 `macro_tw_local_fetch.py` 下沉 repositories;UI 取回後在同檔純函式取 `score_latest`(9~45),供深度強化模組動態門檻用(v19.459)。純 fetch facade,無 L2 業務需上提。<br>　⛔ **另三個成員的去留:待客戶裁決,本組不作處置建議,亦不得被引用為刪除或復活的依據。**依派工說明,它們的存廢卡在一組**互相牴觸的客戶決定**(總經面板退役 vs. 9 源 PMI 賽跑核准),屬 §-1.5.1c `03`-2 ② **業務需求衝突**,已送客戶、**尚未答覆**。⚠️ **本組未查證到那兩個客戶決定的原始出處**(全 repo `.md` 搜不到「2026-06-09」或「總經 Tab 充滿矛盾」字樣),依 §-2 規則 6 **只轉述派工說明、不宣稱為已查證事實**;唯一本組實測到的是`CLAUDE.md §2.1` 記載的「**user 2026-07-12 核准 9 源賽跑**」。**在客戶答覆之前:本列不刪除該三個成員的登記、也不宣稱它們合憲** —— 它們現況是「**無 UI 呼叫點,故本例外對其空轉**」。⚠️ 依 **§-1** 本次**不動任何 `.py`**;本更正**不構成動工授權**。<br>　📌 **本更正不觸碰、也不解決 §8.3.P `P-NDCCACHE-1`**:那一列問的是`ui/helpers/macro/ndc.py::_cached_ndc_score` 在 **UI 層 `@st.cache_data` 快取外部 HTTP**、且與 L1 既有 `@_ttl_cache(TTL_15MIN)` **兩層同 TTL 疊加**該怎麼辦 —— **那是快取歸屬問題**;本列處理的是 **UI 直呼 L1 的分層問題**。**兩者是同一個檔案上的兩個獨立問題,`P-NDCCACHE-1` 維持未判定。**⚠️ **不得**引用本更正主張 `P-NDCCACHE-1` 已結案。<br>　⚠️ **本更正全部為單組實測,未經第二組獨立驗證**(§-2 規則 6);其中「另三個 0 caller」與「`_safe_tw` 0 命中」是**取決於有沒有漏看**的全稱句,**只能當待驗事項**。**驗證指令(repo 根執行)**:`git grep -n "macro_tw_local" -- 'ui/**'`、`git grep -n "fetch_tw_pmi_local\|fetch_tw_export_yoy\|fetch_foreign_consecutive_days" -- '*.py'`、`git grep -n "_safe_tw\|validate_ndc_signal_dict" -- '*.py'`(命中須**人工逐一判讀**是 import、字串名還是註解)。<br>- **`repositories.macro_repository.fred_get_next_release_date`**(~~`ui/helpers/io/data_registry.py:139` lazy~~ → **`ui/helpers/io/data_registry.py::_update_data_registry`**,函式內 lazy import as `_fred_next_rel`(動態 `next_release_date` TTL)/ ~~`ui/tab5_data_guard.py:1237` lazy~~ → **`ui/tab5_data_guard.py::render_data_guard_tab`**,FRED 揭露日診斷區塊內 lazy import as `_diag_next_rel`)— thin FRED 揭露日 helper(動態 TTL 計算 / Tab5 診斷用),無 L2 業務邏輯,UI 直呼取數。**v19.377 B2c 補登**(全域排毒 Wave B2 架構越權查緝點名;user 核准混合案:thin pass-through 登例外而非建 facade,避 §8.1 step 6「用不到的抽象」)。<br>- **`repositories.news_repository.fetch_stock_news`**(~~`ui/helpers/fund_grp_health/ai.py:232`~~ → **`ui/helpers/fund_grp_health/ai.py::_render_per_fund_news_expanders`**,函式內 lazy import,「📡 抓持股新聞」按鈕後對前 6 大持股逐一呼叫 / ~~`ui/tab2_single_fund.py:1300`~~ → **`ui/tab2_single_fund.py::render_single_fund_tab`**,「📰 個股新聞面」區塊內 lazy import as `_fetch_stk`)— self-contained 個股新聞 fetcher(feedparser Google News 逐股搜尋),與**已登錄兄弟** `fetch_market_news` 同精神(self-contained news fetcher,UI 直呼);為兩兄弟一致處理故一併登錄。**v19.377 B2c 補登**。<br>- **`repositories.macro.yf.fetch_benchmark_close`**(`ui/components/mk_dashboard.py::_get_benchmark_series` lazy import,單一 caller / 單一 call site)— 投資組合衛星對比基準(SPY / QQQ)近 9 個月日線收盤。**v19.531 Phase 1.2 補登**,起因是憲法 §8.3.P **`P-UIHTTP-1`** 登記的違憲:原 `_get_benchmark_series` 在 **UI 層** `import yfinance` 直抓 + `st.session_state["mk_bench_cache"]` 自建快取,同時命中 §-1.5.1c v3 §01 三層圖 UI 框的兩個動詞「私自**存放**或**抓取**」,且與 `repositories/macro/yf.py::fetch_yf_close` 重複實作同一個來源(Yahoo 日線收盤)。⚠️ **但本次搬遷做到的是「不再新增一套」,不是「全站收斂成一套」** —— 2026-08-28 稽核實測本 repo **至少另有兩套獨立的 Yahoo v8 chart 實作**(`repositories/fund/sources.py::_src_yahoo_finance_nav`,urlopen 直打 query2、不走 `infra.proxy`;`scripts/fetch_nav_cache.py::fetch_yahoo_finance_history`),外加 `repositories/financial_repository.py` 真的 `import yfinance`(季財報)。repo 自己早有紀錄:`shared/api_endpoints.py` 檔頭「✅ YF query2 Morningstar URL 真 dupe」。**v3 §01-2 在本 repo 尚未達成,不得宣稱已達成。**取數與快取已下沉 L1(TTL 吃上游 `@_ttl_cache(TTL_10MIN)`,走 `shared/ttls.py` SSOT,**本層不疊第二層**)。<br>📌 **2026-09-08 行號式登記全面退場:本列**活的**行號登記,實測 7/7 全部指錯**(**有意識的更正,不是漏刪** · 日期 **2026-09-08** · 決策者:**AI 總管**)。**本次只改「呼叫點寫在哪」,不改任何成員的豁免資格、不改任何條件、不改升級觸發條件。****逐一實測(指令 `git show origin/main:<檔> \| sed -n '<N>p'`,⚠️ 該 `\|` 是**為了不切斷表格欄而 escape 的**,實跑時請去掉反斜線;輸出照抄)**:~~`ui/tab1_macro.py:1188`~~ → 一句關於 `ui/helpers/render_state.py` 的註解;~~`ui/tab3_t7_ledger.py:2710`~~ → `"B 新 FX": (`;~~`ui/tab5_data_guard.py:832`~~ → **空行**;~~`ui/helpers/io/data_registry.py:139`~~ → `    }`;~~`ui/tab5_data_guard.py:1237`~~ → `where=_key_where)`;~~`ui/helpers/fund_grp_health/ai.py:232`~~ → `except Exception as e:`;~~`ui/tab2_single_fund.py:1300`~~ → 一句關於警語數字算錯的註解。⚠️ **這七個引用本身也一律劃線**(沿用本檔既有處置:**只把路徑劃掉、句子照留** —— 劃掉的是「這個 `檔:行` 現在指得到那個符號」這個宣稱,**不是**「今天那一行長什麼樣」這個量測)。**理由是本組自己踩到的**:初稿把這七筆寫成**活的**引用,於是一段**主張「不要再寫行號」的文字裡,自己新增了七個會過期的行號** —— **與 2026-09-01 那次就地更正同型**(該處原文:「**本節在講的那個病,在講它的那句話裡當場發作**」)。⚠️ 且它們**騙得過守衛**:七個檔都在、七個行號都界內,`tests/test_constitution_file_refs.py` **不會紅燈**;是本組跑該守衛的 `parse_references()` 逐筆列出 `struck` 旗標才看見的。**綠燈沒有幫上任何忙。**⚠️ **七個檔案全部存在、七個行號全部在檔尾之內** —— 所以 `tests/test_constitution_file_refs.py` **結構上不會紅燈**(該守衛自己的 docstring 已把這個缺口寫成「缺口 2｜只驗檔案在不在、行號界不界內,**不驗語意**」)。**這七筆不是守衛抓到的,是人工逐行讀出來的。**⚠️ **舊登記在寫下的當天是對的,不是當初寫錯** —— 被推翻的是它們的**前提**(那幾個檔此後各自被改過,而**重構不會觸發本清單更新**);**被權衡掉的是「行號式 evidence」這個寫法本身**,不是那些成員的豁免資格。故本次一律改成 §8.2.A.0 規則 1 的「**檔案路徑 ＋ 符號名 ＋ 模式描述**」,**不寫新行號**(⚠️ **本檔無 §8.2.A.0 一節**,該規則出自姊妹 repo `my-stock-dashboard`;同本列 `tdcc_search_fund` 與 `EX-UICACHE-1` 兩處的既有註記)。**這已是本列第三次踩同一個坑**(2026-08-31 `tdcc_search_fund`、2026-09-01 TW-local 四 fn、本次七筆)——⚠️ 前兩次都在**同一列**就地寫下「行號會過期」這個教訓,**卻都只修了被點名的那幾筆、沒有把同一把尺套到同列其餘成員**;若當時套了,這七筆會在那兩輪就被抓到。**這正是 §8.2.A.1 驗證段 ④ 記載的「條件只往外用、不往內用」,在同一列上第三次發生。**<br>📌 **新登記怎麼定位出來的(方法,不是結論)**:先 `git grep -n "<符號名>" -- 'ui/**'` 取得候選,再以 **`ast`** 走訪該檔、找出**包住該行的最內層 `FunctionDef`/`ClassDef`**(⛔ **不用固定行數的上下文窗** —— §-2.A 第 4 款:固定窗會把相隔數百行的兩段接在一起印)。⚠️ **一個順帶查到、與本次更正無關但會影響後人讀本列的事實**:`ui/tab1_macro.py` 拿到 `fetch_market_news` 的路徑**不是** `repositories.news_repository`,而是**根目錄相容 shim `fund_fetcher`**(`fund_fetcher.py` 內 `from repositories.news_repository import fetch_market_news  # noqa: F401`)——也就是說,**任何寫成「`ui/**` 不得 import `repositories.*`」的規則,結構上都攔不到這個呼叫點**。⚠️ **本段全部為架構與文件組單組實測,未經第二組獨立驗證**(§-2 規則 6);「本列**沒有第八筆**活的行號登記」是**取決於有沒有漏看**的全稱句,本組是以 `grep -oE '[A-Za-z0-9_/]+\.py:[0-9]+'` 掃本列後**人工逐一判讀**得到的,該字表**掃不到**行號寫在反引號外、寫成中文敘述、或以 `L123` 等其他形式表達的引用。**故本段只給已知分類,不宣稱窮舉。** | L3 UI 直接 import L1 facade(~~共 8 組~~ → **共 9 組**);v19.247 R16 後 `get_latest_fx` 已上提 L2 wrapper | **v19.273 Phase 2 TOP 3 補登原因**:`tab1_macro.py:821` 4 個 TW macro local fetcher UI 直呼為 v19.197 P1-4 下沉後的既有 pattern,屬「self-contained L1 fetcher + UI 直呼取數」(F-GRAY-2 同精神),原僅有 v19.197 commit 紀錄未在例外表登錄,PHASE1_AUDIT_DELTA.md TOP 3 點名補登避免讀例外表者誤判為違憲。**v19.251 補登原因**:R8 EX-L1ORCH-1 退役註腳口頭認可 `tab_crisis_backtest.fetch_fund_by_key`,但未在例外表正式登錄;另 `fetch_nav_history_long` / `diagnose_fx_sources` 同屬 lazy import + 單一 caller pattern,一併補登。**v19.247 R16 部分升級記錄**:9 UI caller 全 migrate `from services.fund_service import get_latest_fx`,L2 thin facade 呼 L1 實作(允許 L2→L1 方向)。L1 業務 0 改動。**v19.531 Phase 1.2 補登原因(`fetch_benchmark_close`)**:此列**不是**把既有違憲就地合法化 —— 取數實作已**實體搬離** UI 檔、UI 端只剩一行委派。選「登例外」而非「建 L2 facade」的理由(⚠️ **以下是本組對本例外既有精神的解讀,不是逐條引用** —— 本列**沒有**編號條件,2026-08-28 稽核指出前一版寫的「判定條件 ①/②」在憲法裡查無此物、是**懸空引用**,已撤;有 `①②③④` 編號條件的是**另外兩列** `EX-UICACHE-1` / `EX-CISCRIPT-1`):(a) **1 caller / 1 call site**,`get_latest_fx` 當年觸發 R16 升級的 9 檔 / 18 處 fan-out 在此不存在;(b) caller 端**零後處理**(無多源 fallback、無跨 fetcher TTL 統一、無結果加工),對應本列既有成員反覆用過的措辭「**純 fetch facade 無 L2 業務需上提**」;(c) L1 已自帶 `@_ttl_cache`,cache 已集中,不存在分散問題;(d) 為純 pass-through 加一層 L2 wrapper 正是 §8.1 step 6「用不到的抽象」的反例 —— 與本列 `fred_get_next_release_date`(v19.377 B2c)**user 核准的混合案**同一個處理「thin pass-through 登例外而非建 facade,避 §8.1 step 6『用不到的抽象』」。**守衛**:`tests/test_benchmark_fetch_migration_v19531.py`(18 條)。⚠️ **不得宣稱其中有「四條 fail-closed 斷言」——那是高估,2026-08-28 稽核實測推翻**:「UI 不得 import yfinance」與「不得再持有 `mk_bench_cache`」兩條屬**形態偵測**,用 `importlib.import_module('yfinance')` + 把快取 key 改名 `mk_bench_cache_v2` 即可完全繞過(實測復辟違憲後那兩條仍綠燈)。**真正 fail-closed 的是 sentinel 那條**(`test_ui_benchmark_path_delegates_to_l1`,patch L1 驗往返),它擋下了該繞道 —— 縱深防禦沒破,但描述必須誠實。兩條形態偵測的 docstring 已就地寫明此繞道。<br>**升級觸發條件**:user 明確要求集中 cache、新增 source、後處理 bug、**或本 fetcher 出現第二個 UI caller**(fan-out 一旦出現,(a) 的理由即失效,應比照 R16 上提 L2)。F-H6 v19.79 原決策 + R15/R16 對齊。 |
| ~~**EX-L1ORCH-1**~~(v19.240 R8 升級退役) | ~~L1 fund orchestrator import L2 `calc_metrics`~~ | ~~抓 + 打包 facade~~ | **退役原因**:v19.240 深挖發現實際違憲 3 個 L2 symbol(`calc_metrics` + `reconcile_fund_annual_return` + `reconcile_dividend_yield`)+ 大量 L2 業務判斷(perf 注入決策 / window 閾值 / % vs decimal 換算 / 對帳)push 回 L1,**升級觸發條件 (a)+(b) 均達標**。R8 採方案 (b) 拆 return + L2 wrapper:`services.fund_service.finalize_fund_metrics()` + `fetch_fund_by_key_enriched` / `fetch_fund_from_moneydj_url_enriched` 兩 wrapper 上提 L1 業務邏輯,L1 純化為 raw fetch + packaging。L1→L2 violation 從 3 → 0。Migrate 4 個 caller site(`ui/helpers/v2_editor.py` / `services/moneydj_fetcher.py` x3),其餘 `tab_crisis_backtest.py` 用 raw `fetch_fund_by_key`(只取 series 不需 metrics)。 |
| **EX-UICACHE-1**(2026-08-28 補登) | **依模式判定,不列行號**(沿用姊妹 repo `my-stock-dashboard` §8.2.A.0 規則 1;⚠️ **本檔無 §8.2.A.0 一節**,該規則出自姊妹 repo)—— L3 `ui/**` 內以 `@st.cache_data` 自建薄快取,且**同時**滿足下列 4 條:<br>① **TTL 走 `shared/ttls.py` SSOT**,檔內**不寫死秒數**(§3.3);<br>② ~~被快取的是**本地持久化讀取**(Google Sheets / 本地 JSON),**非外部 HTTP 取數**~~ → **2026-08-28 第三輪稽核改寫(有意識的更正,不是漏刪;決策者:稽核回修組)**:**被快取的必須是「憲法既有認定為『本地持久化 CRUD』的那一類讀取」(＝ EX-CRUD-1 的射程),而不是「不經網路」。**<br>　⚠️ **據實講清楚,不再用括號含混過去**:`Google Sheets` 的讀取是**經 gspread SDK 往返 Google Sheets API 的遠端網路呼叫**(逐成員鏈路實測見下方驗證段 **③**),**不是**本地檔案 I/O。舊表述把它寫進「本地持久化」的括號裡,會讓讀者以為它不上網 —— **那是假的**。<br>　⛔ **本例外賴以成立的,是 EX-CRUD-1「此類 read+write 同檔的持久化 CRUD 不存在 cache 分散問題」這個【既有認定】;而該認定本身目前登記為【尚未查證】(§8.3.P **`P-UIGSPREAD-1`**)。該列若判定 gspread 直呼**不**等同「本地持久化」,**本例外當場失去依據,須整列重審**(已寫入 `P-UIGSPREAD-1` 的觸發點)。**在該查證完成前,本條件只能當『暫行依據』,不得當已查證的事實引用。**;<br>③ **有明確的寫後失效路徑**,且該路徑**真的被寫入端呼叫**(不是只定義不接線);<br>④ 快取只擋「重複讀」,**不改變任何顯示語意**。<br>**2026-08-28 實測符合者 4 處 / 2 檔**(量測日;「會漂移的量測值標日期」同樣沿用姊妹 repo §8.2.A.0 規則 4)。⚠️ **第三輪稽核補:這 4 處不是同一種強度** —— **只有 `_cached_nh_status` 一處無條件成立**(其鏈路完全不上網);**另 3 處(`_cached_load_policy_v2` / `_cached_list_policies` / `_cached_nh_coverage`)快取的都是 gspread 遠端往返,屬【有條件成立】,條件就是改寫後的條件 ② 所繫的 `P-UIGSPREAD-1`。** **讀本列時不得把 4 處當成同一個確定性。**⚠️ **掃描指令找到 5 個裝飾點,其中 1 個(`ndc.py`)經逐一驗證後不符條件 ②,已移出本例外(見下)** ——**「掃到幾個」不等於「符合幾個」,不得直接把掃描列數當成例外成員數**:<br>- `ui/helpers/v2_editor.py` — `_cached_load_policy_v2` / `_cached_list_policies`(`TTL_1MIN`),失效走 `_invalidate_cache()`(~~檔內 4 個寫入點實呼~~ → **2026-08-28 第三輪稽核更正,實測**:檔內共 **4 個呼叫點**,其中**只有 2 個是真寫入端**(「➕ 新增保單」與「首次使用 wizard」,兩處都在 `write_policy_v2(...)` 之後);另 **2 個不是寫入** —— 「🔄 重試（清快取）」是**讀取失敗後重試**、「📥 重新讀回（從雲端讀最新）」是**純讀**(該處就地註解自陳 v19.451 已移除寫入按鈕)。**條件 ③ 仍然滿足**(確有真寫入端呼叫),**但「4 個寫入點」這個數字是假的** —— 這是同一份 diff 內第三次「把未查證的計數寫成實測」,故就地更正並留痕);<br>- `ui/tab5_data_guard.py` — `_cached_nh_status` / `_cached_nh_coverage`(`TTL_5MIN`),失效走 `_clear_nh_caches()`(**2026-08-28 第三輪稽核實測**:全 repo `1` 個 production 呼叫點,位於「📥 匯入到 nav_history」按鈕內、`import_csv_text(...)` 回傳 `written > 0` 之後 —— **確為真寫入端,條件 ③ 成立**;另有 1 處在 `tests/` 內作為守衛斷言。⚠️ 這一項是**跟著隔壁那個被抓到說謊的計數一起重驗的** —— **同一次更正必須把同一把尺對全部同類項目重跑,不能只修被點名的那一個**);<br>- ~~`ui/helpers/macro/ndc.py` — `_cached_ndc_score`(`TTL_15MIN`,**寫法為 `@_st_mod.cache_data`**)。⚠️ **本檔無失效路徑,也不需要** —— NDC 為**月頻唯讀**指標,UI 端**不存在寫入動作**,故第 ③ 條以「**無寫入端**」滿足,不是「漏做」。**它與前 4 個的豁免理由不同,讀表時不要混為一談。**~~<br>　→ ⛔ **2026-08-28 同日經稽核移出本例外**(**有意識的更正,不是漏刪**;依據:**實測**,鏈路見下)。**理由:它不滿足本列自訂的條件 ②。** 實測鏈路:`_cached_ndc_score()` → 同檔 `_fetch_ndc_score()` → `repositories/macro_tw_local_repository.py::fetch_ndc_signal_history` → `fetch_url(FINMIND_BASE, params=...)` —— **被快取的是外部 HTTP(FinMind API)。**<br>　📌 **2026-08-28 第三輪稽核同步註（移出結論不變，改的是引用的理由）**:上句原本引用的是~~條件 ② 舊表述「本地持久化讀取(Google Sheets / 本地 JSON)」~~,而**該舊表述本身已在同日被改寫**(理由:它把 gspread 遠端往返含混成「本地持久化」)。**以改寫後的條件 ② 重判,結論一樣是「移出」,而且理由更乾淨**:FinMind API **完全不在 EX-CRUD-1 的射程內**(既非 read+write 同檔的持久化 CRUD,也不是本 repo 任何既有例外已認定的對象)。<br>　⚠️ **但要誠實記一筆**:當初拿條件 ② 把 `ndc.py` 踢出去時,**同一把尺沒有對留下來的 4 個成員各套一次**—— 若當時套了,會當場發現其中 3 個也是遠端往返,條件 ② 的措辭問題本來在那一輪就該被抓到。**這就是本輪要修的『不對稱裁決』本身。**<br>　⚠️ **它同時當場命中本列自己寫的升級觸發條件**:(1)「開始快取**外部 HTTP 取數** → 那一刻起 cache 必須回 L1 集中」**本來就已經成立**;(3)「同一份資料長出第二層快取(兩層 TTL 疊加 = 失效語意不可推理)」—— `fetch_ndc_signal_history` 在 **L1 已帶 `@_ttl_cache(ttl_sec=TTL_15MIN)`**(實測),UI 再疊一層 `TTL_15MIN`。<br>　**一個例外不能收一個當場觸發自己升級條件的成員** —— 那等於例外把自己寫的門檻當不存在。**現況:登記為待判定,見 §8.3「尚未判定(待派工查證)」;依 §-1 無觸發,本批不動工、不改任何 `.py`。** | L3 UI 自建 `@st.cache_data` 薄快取層 | **與姊妹 repo `my-stock-dashboard` 的 `V-SMART-CACHE-1`(判定為**違憲待修**)差別在哪 —— 這是本列存在的理由,務必看清楚**:那一列的病是 **(a) TTL 全是 inline literal、未走 SSOT(同時違 §3.3)** ＋ **(b) 快取的是外部 HTTP 取數,本該在 L1 集中**。~~**本列(移出 `ndc.py` 後的 4 處)兩個病都沒有**:TTL 全部 import 自 `shared/ttls.py`;被快取的是 **Google Sheets / 本地 JSON 持久化讀取**,與 **EX-CRUD-1 同一族**(該例外已認定此類 read+write 同檔的本地持久化不存在「cache 分散」問題)。~~<br>→ ⛔ **2026-08-28 第三輪稽核改寫(有意識的更正,不是漏刪;決策者:稽核回修組)**:**「兩個病都沒有」這句對 4 個成員中的 3 個為假,不得留著。** 逐成員實測(指令與輸出見下方驗證段 **③**):<br>　- **病 (a)(TTL 寫死 inline literal)確實不存在** —— 4 處 TTL 全部 import 自 `shared/ttls.py`。**這半句仍然成立。**<br>　- **病 (b)(快取外部往返)對 3/4 成員成立**:`_cached_load_policy_v2` → `repositories/policy/v2.py::load_policy_v2`(`_with_quota_retry(client.open_by_key, ...)` ＋ `ws.get_all_records()`)、`_cached_list_policies` → 同檔 `list_policy_worksheets`(`client.open_by_key` ＋ `sh.worksheets()`)、`_cached_nh_coverage` → `services/nav_history_gs.py::coverage_status` → `load_points`(`ws.get_all_values()`) —— **三者都是往返 Google Sheets API 的遠端呼叫**。**唯一真的沒有外部往返的成員是 `_cached_nh_status`**(→ `status()`,只讀 `get_secret(...)`)。<br>　⛔ **故本列與 V-SMART-CACHE-1 的正確差別是**:**少了病 (a);病 (b) 並非不存在,而是靠 EX-CRUD-1 的既有認定被豁免,而該認定尚未查證(`P-UIGSPREAD-1`)。**<br>　⚠️ **「與 EX-CRUD-1 同一族」這個援引本身有一道前提裂縫,必須就地講明,不得靠它自證**:EX-CRUD-1 的免責推理前提是「**無 `@_ttl_cache` / `@st.cache_data` 裝飾** → 不存在『cache 分散』問題」,而**本列的成員正好就是加了 `@st.cache_data` 的那一類** —— **把 EX-CRUD-1 的結論搬過來,同時違反了那個結論賴以成立的前提。**<br>　⚠️ **同一份 `CLAUDE.md` 裡,`P-UIGSPREAD-1` 原文已明文寫「在查證完成前不得引用該主張」,而本列正在引用它替 3 個成員背書** —— 這一點在前兩輪都沒有被指出,故在此釘死:**本列現階段的成立與否,全繫於 `P-UIGSPREAD-1` 的裁決與上述前提裂縫,不得再以「兩個病都沒有」或「同族 EX-CRUD-1」自證合憲。**<br>⚠️ **補正紀錄(誠實揭露,§-2 規則 6)**:上面這句「**兩個病都沒有**」在本例外**補登當下並不成立** —— 當時成員含 `ui/helpers/macro/ndc.py`,而它快取的正是**外部 HTTP**,也就是 V-SMART-CACHE-1 的病 (b)。**這句正是本列用來與姊妹 repo 那條「違憲待修」劃清界線的關鍵句,卻對其中一個成員為假。** 移出 `ndc.py` 後本句才成立。**教訓:例外的自訂條件必須逐一成員實跑驗證,不得以整組宣稱代替** —— 依姊妹 repo §8.2.A.0 規則 5,豁免理由必須是「**為什麼這個位置是對的**」;把一個不符條件的成員收進來,就是**把違憲寫成合憲**。<br>**為什麼不下沉 L2(這才是關鍵)**:現行失效語意是「**UI 的寫入動作觸發清除**」——`_invalidate_cache` / `_clear_nh_caches` 都由**寫入端就地呼叫**。下沉 L2 後,**L2 拿不到 UI 的寫入事件**,只能改成純 TTL 過期 → 使用者存檔後畫面會顯示**寫入前的舊值**,直到 TTL 到期。**那是把「§1 不顯示過期值」換成「等一分鐘」,是行為退化,不是架構改善。**<br>**事故依據(就地記載於 `ui/tab5_data_guard.py` 該快取上方,非本表事後補述)**:未加此層前,任一分頁的任一次互動都會重打 Google Sheets;**25 檔實測「rerun 起算 17 秒後 `WebSocket onclose`」,2026-08-14 實機兩次重現**。移除本層 = 回到該事故。<br>**升級觸發條件(任一命中即脫離本例外、須搬 L1/L2)**:(1) 開始快取**外部 HTTP 取數**(那一刻起 cache 必須回 L1 集中);(2) TTL 改成**檔內寫死秒數**(違 §3.3,本例外前提消失);(3) 同一份資料在 **L2 也長出第二層快取**(兩層 TTL 疊加 = 失效語意不可推理);(4) 快取開始**改變顯示語意**(例如失敗時回舊值卻不標 `is_stale`,違 §2.4)。 |
| **EX-CISCRIPT-1**(2026-08-28 補登) | `scripts/fetch_nav_cache.py`(**782 LOC**,量測日 2026-08-28)—— **CI/離線精簡環境**用的獨立 NAV 抓取實作,與 L1 `repositories/fund/sources.py` **同源同目的但平行實作**(檔內 9 個 `fetch_*`:TDCC ×2 / MoneyDJ ×2 / AllianzGI / SITCA / CnYES / Yahoo / 銀行平台)。字面命中 v3 §01-2「同一個資料來源全站只能有一處取數實作」。<br>**它與 L1 的唯一共用點**:lazy `from repositories.fund.sources import YF_MORNINGSTAR_CHART_URL`(**單一 URL 常數**,v19.230 P1-2 已收 SSOT);另有 lazy `from repositories.policy_repository import ...`(有憑證時才載)。**除此之外抓取邏輯全部自帶。** | `scripts/**` 得平行實作一份精簡取數,不與 L1 合併 | **① 合併會把 streamlit 依賴鏈拉進精簡環境(這是既有的、有紀錄的設計決定,不是本次發明)**:`repositories/fund/sources.py` 的 module-load 會 `from bs4 import BeautifulSoup`,整個 `repositories/fund` package 一起被拉起;`STATE.md` 就地記載「**CI 精簡環境不能 import sources.py——會拉 streamlit——故平行精簡實作**」。該 script 刻意零重依賴到**連 `requests` 都在runtime `pip install`**。<br>**② 網路前提不同**:L1 走 **NAS Squid proxy**(§2.1 T3 來源多需台灣 IP);此 script 設計為在**GitHub Actions(美國 IP)** 執行,兩者可達的來源集合本來就不一樣。<br>**③ 它是 `cache/nav/{CODE}.json` 的唯一產出者,而該快取被活的 L1 讀取**:`save_cache()` 寫入 `<repo根>/cache/nav/`,而 `repositories/fund/nav_metrics.py::fetch_nav` 在**所有 live URL 全敗後、return 空之前**呼叫 `sources._src_cache_files()` 取它當**最終降級**(v19.319 接線,回歸網 `tests/test_cache_nav_fallback.py` + `tests/test_nav_cache_quality_gate.py`)。→ 合併它 = 動到一條**仍在被讀**的降級鏈,屬 §8.4 步驟 4 明禁的自作主張大重構。<br>**升級觸發條件(任一命中即重新評估)**:(1) `repositories/fund/sources.py` 變成**可在精簡環境 import**(bs4/streamlit 依賴消失)——理由 ① 當場失效;(2) `_src_cache_files` 從 `fetch_nav` 降級鏈**移除**——理由 ③ 失效,此時該問的是「**還要不要留這支 script**」;(3) 兩邊解析邏輯**出現行為分歧**(同一檔同一天 NAV 不一致)——那是 SSOT 破裂,**必須立刻收斂**,不得再引本例外。 |

> 📌 **EX-UICACHE-1 / EX-CISCRIPT-1 兩列的驗證指令與已知限制（2026-08-28 補登時就地記錄）**
>
> **本段存在的理由**：這兩列的理由句裡有「**共 N 處**」「**唯一產出者**」這種**窮舉句**，
> 依 §-2 規則 6 必須附上**產生它的那條指令**，否則後人無從複驗、也無從發現它何時開始說謊。
> **下列指令均為 2026-08-28 於 repo 根目錄實跑，本處為從實跑處照抄。**
>
> **① EX-UICACHE-1 的成員掃描（`ui/**` 的 `@*.cache_data` 裝飾點）**：
>
> ```
> git grep -nE "^[[:space:]]*@[A-Za-z_][A-Za-z0-9_]*\.(cache_data|cache_resource)" -- 'ui/**'
> ```
>
> 當時輸出 **5 行**（`ndc.py` ×1、`v2_editor.py` ×2、`tab5_data_guard.py` ×2）。
> ⚠️ **但 EX-UICACHE-1 的成員是 4 個,不是 5 個** —— `ndc.py` 那一個經逐一驗證後**不符條件 ②**（快取的是外部 HTTP，見上表該列的移出註記），已移出本例外。
> ⛔ **這條指令回答的是「哪裡有 UI 層快取」，不是「哪些符合本例外」。**
> **把掃描列數直接當成例外成員數，正是本例外補登當下犯的錯**；
> **掃描給候選，逐一驗證才給成員** —— 中間那道人工驗證不可省。
> ⚠️ **這條指令刻意不寫死 `@st.`** —— `ui/helpers/macro/ndc.py` 用的是 **`@_st_mod.cache_data`**
> （streamlit 以別名 import），**`@st\.cache_data` 這種寫法掃不到它**。
> **這正是本輪更正 1 的同一個病**（字表假設了單一拼法），且**同一輪內就再犯一次、又被同一招抓到**
> —— 故在此就地釘死：**掃裝飾器一律用「別名不敏感」的樣式，不要寫死模組名。**
>
> **② EX-CISCRIPT-1「`cache/nav/` 的唯一產出者」**：
>
> ```
> git grep -nE "CACHE_DIR|cache/nav" -- '*.py' | grep -v tests/
> ```
>
> ⚠️ **這條刻意很寬，輸出數十行，必須人工逐一判讀**（同 ① 的精神：寧可多抓不可漏抓）。
> 當時逐一判讀後：**寫入 `cache/nav/` 的只有 `scripts/fetch_nav_cache.py`**
> （`CACHE_DIR = Path(__file__).parent.parent / "cache" / "nav"` ＋ `save_cache()` 的 `cache_file.write_text(...)`）；
> **讀取端**為 `repositories/fund/sources.py::_src_cache_files`。
> 兄弟腳本 `scripts/accumulate_nav_tw.py` **不寫這個目錄**（它 `append_points` 寫 Google Sheets）。
>
> ⛔ **判讀這條輸出時最容易犯的錯：`cache/nav/` 與 `cache/nav_history/` 是兩個不同的目錄，不要混為一談。**
> 前者＝本列所指、由 `scripts/fetch_nav_cache.py` 產出、被 `_src_cache_files` 當最終降級讀取；
> 後者＝ `services/nav_history_store.py`（Tab6 手動 CSV 匯入）與 `repositories/fund/nav_metrics.py`
> 的本地 disk cache，**產出者與消費者都不同**。上列輸出兩者都會命中，
> **把 `nav_history` 的寫入端誤算成「`cache/nav` 的第二個產出者」，就會得出「理由 ③ 不成立」的錯誤結論。**
>
> ⚠️ **一項與本列理由有關、必須據實揭露的現況（不改變本列判定，但改變它的急迫性）**：
> **~~`.github/workflows/fetch_nav_cache.yml`~~ 在本次量測時已不存在**（實測 `ls .github/workflows/` 無此檔），
> `STATE.md` 記載該 workflow 的 `schedule` cron 已被拔除、**script 保留供手動觸發**。
> → **因此「它是一條每日在跑的生產鏈」這句話，以 2026-08-28 的實況而言並不成立，本表不得如此宣稱**；
> 本列成立靠的是**理由 ①（依賴隔離）與理由 ③（產出物仍被活的降級鏈讀取）**，**不靠**「每日在跑」。
> 現行每日 NAV 是 `.github/workflows/weekly_nav_backfill.yml` → `scripts/weekly_nav_backfill.py` → 雲端
> `nav_history`，**與 `cache/nav/` 是兩條不同的鏈**，不要混談。
>
> ⚠️ **由此引出一個本組不予裁決的開放問題**：v3 §01-2 對「已無排程、僅手動觸發」的 script
> 該如何處置（是「仍有用途」還是「Orphaned 待清理」）—— **本組不下判定**，理由有三：
> (a) **§-1 無觸發**（本批是純文件更正，沒有任務碰到這支 script）；
> (b) **它不是 0 caller** —— `scripts/accumulate_nav_tw.py` 與 `scripts/migrate_nav_caches_to_sheet.py`
> 都以 `importlib` 載入它、復用 `_discover_fund_codes()` 當基金代碼 SSOT，且 `docs/NAV_NAS_CRON_SETUP.md`
> 也指向它；**「沒有排程」不等於「沒有人用」**；
> (c) 依 **§-2 規則 4**，這種「還有沒有人用」的問題**取決於有沒有漏看**，
> **必須另派獨立一組查證，且不可由本組自己查自己**。
> ⛔ **在該查證完成前，不得引用本段去刪除或合併這支 script。**
>
> 📌 **2026-08-28 補正：本開放問題已登記為 §8.3.P 的 `P-NAVCACHE-1`，附「由誰查」與「觸發點」。**
> **原因（稽核指出）**：上面那句「⛔ 在該查證完成前不得引用本段…」**沒有指定由誰查、也沒有觸發點** ——
> 配上 §-1「沒觸發不動工」，會讓這個「待查證」**無限期存續**，實質變成一張**永久豁免**：
> 「因為還沒查所以不能動」，而「查」永遠不會被排進來。**待查證必須有出口，否則它就是豁免。**
> ⚠️ 原句**保留不刪**（它本身沒錯，只是不完整）；補的是**它缺的那一半**。
>
> ---
>
> **③ EX-UICACHE-1 條件 ② 的逐成員鏈路實測（2026-08-28 第三輪稽核補；本段是條件 ② 改寫的依據）**
>
> **為什麼要有這一段**：條件 ② 原本寫「被快取的是**本地持久化讀取**（Google Sheets / 本地 JSON），
> **非外部 HTTP 取數**」，並以此把 `ndc.py` 踢出例外。**踢 `ndc.py` 那一步是對的；
> 錯的是這把尺沒有對留下來的 4 個成員也套一次。** 本段補上那次遺漏的逐成員驗證。
>
> ```
> grep -rn "@st\.cache_data\|@_st_mod\.cache_data" --include=*.py ui/
> grep -n "def load_policy_v2" -A 20 repositories/policy/v2.py
> grep -n "def list_policy_worksheets" -A 8 repositories/policy/v2.py
> grep -n "^def status\|^def load_points\|^def coverage_status" -A 20 services/nav_history_gs.py
> grep -rn "_invalidate_cache" --include=*.py .
> ```
>
> **逐一判讀結果（4 個成員，全部實跑，非推論）**：
>
> | 成員 | 快取的實際鏈路 | 是否遠端往返 |
> |---|---|---|
> | `ui/helpers/v2_editor.py::_cached_load_policy_v2` | → `repositories/policy/v2.py::load_policy_v2` → `_with_quota_retry(client.open_by_key, sheet_id)` ＋ `_with_quota_retry(ws.get_all_records)` | **是**（gspread → Google Sheets API） |
> | `ui/helpers/v2_editor.py::_cached_list_policies` | → `repositories/policy/v2.py::list_policy_worksheets` → `client.open_by_key` ＋ `sh.worksheets()` | **是** |
> | `ui/tab5_data_guard.py::_cached_nh_coverage` | → `services/nav_history_gs.py::coverage_status` → 同檔 `load_points` → `ws.get_all_values()` | **是** |
> | `ui/tab5_data_guard.py::_cached_nh_status` | → `services/nav_history_gs.py::status` → 只呼叫 `infra.config.get_secret(...)`（`google_service_account` / `NAV_SHEET_ID` / `FRED_API_KEY`） | **否** ✅ |
>
> ⛔ **結論（已回寫進上表該列，兩處都改）**：「被快取的非外部 HTTP」這句**只對 1/4 成員成立**。
> 條件 ② 因此改寫為「**必須落在 EX-CRUD-1 已認定的『本地持久化 CRUD』射程內**」，
> 並就地標明**該認定尚未查證**（§8.3.P `P-UIGSPREAD-1`）。
>
> ⚠️ **本段要留給後人的方法教訓（比數字重要）**：這把尺（條件 ②）**被不對稱使用了** ——
> 拿它去**排除**一個成員（`ndc.py`）時很嚴格，卻**沒有拿同一把尺去檢查留下來的 4 個**。
> **例外表最常見的失效模式不是「條件寫錯」，是「條件只往外用、不往內用」。**
> 往後任何一次「依條件 X 移出某成員」的動作，**必須同時把條件 X 對全部留任成員重跑一次**，
> 並把逐成員結果寫下來（就像上表）——**只寫結論不寫逐成員，等於沒驗。**
>
> **④ `_invalidate_cache` 的 4 個呼叫點逐一判讀（同上，實跑 `grep -rn "_invalidate_cache" --include=*.py .`）**
>
> | 呼叫點所在區塊 | 前一個動作 | 算不算「寫入端」 |
> |---|---|---|
> | 「➕ 新增保單」按鈕 | `ensure_policy_worksheet(...)` ＋ `write_policy_v2(...)` | **是（真寫入）** |
> | 「首次使用 wizard」建保單＋首檔基金 | `write_policy_v2(...)` | **是（真寫入）** |
> | 「🔄 重試（清快取）」 | `_cached_list_policies` **讀取失敗**後的重試 | 否（讀取重試） |
> | 「📥 重新讀回（從雲端讀最新）」 | 無寫入；就地註解自陳 **v19.451 唯讀化、已移除寫入按鈕** | 否（純讀） |
>
> → **條件 ③「該路徑真的被寫入端呼叫」仍然滿足**（有 2 個真寫入端），
> **但上表原寫的「檔內 4 個寫入點實呼」是把呼叫點數當成寫入點數，數字為假**，已就地更正。
>
> **同一把尺對另一個成員也重跑一次（這一步上兩輪沒做，是本輪的重點）**：
> ```
> grep -rn "_clear_nh_caches" --include=*.py .
> ```
> → 全 repo **3 行**：`ui/tab5_data_guard.py` 的**函式定義** 1 行、**production 呼叫** 1 行、
> `tests/test_audit_20260814_batch01.py` 的**守衛斷言** 1 行。
> 唯一的 production 呼叫位於「📥 匯入到 nav_history」按鈕內、
> `import_csv_text(...)` 回傳 `written > 0` 的分支之後 —— **確為真寫入端，條件 ③ 成立，原敘述無誤**。
> ⛔ **但「原敘述無誤」是這次驗出來的，不是上兩輪驗出來的。**
> **更正一個被點名的數字時，必須把同一把尺對全部同類項目重跑** ——
> 否則下一輪抓到的就是「你只修了被指出的那一個」。
>
> ---
>
> ⚠️ **⑤ 一項本批客觀上做不到、但必須就地揭露的缺口（§-2 規則 6；前兩輪完全沒有提及）**
>
> 本節末句自訂：新增例外**必須**：(1) 在此表登錄、(2) **對應檔案加註解指回此表**、(3) PR 描述附理由。
>
> **實測**：
> ```
> grep -rn "EX-UICACHE-1\|EX-CISCRIPT-1" --include=*.py .
> ```
> → **0 命中**（對照組：`grep -rln "EX-CRUD-1\|EX-PASSTHRU-1\|EX-CACHE-1" --include=*.py .`
> 在 `ui/` 與 `services/` 下有多檔命中，既有例外都有回指註解）。
>
> → **`EX-UICACHE-1` / `EX-CISCRIPT-1` 兩列目前 (1)(3) 已做、(2) 未做。**
> **原因**：這三輪的授權範圍都是**純文件更正、不得改任何 `.py`**，(2) 在本批**客觀上做不到**。
> ⚠️ **但「做不到」不等於「不用講」** —— 前兩輪一次都沒提，等於默默漏掉一條本節自訂的硬性要求。
> **觸發點（任一命中即在該次任務內補上，屬 §-1.5.1c 判定 3 所稱「本次弄到的東西」的收尾義務，不必另行請示）**：
> (1) 有任務碰到 `ui/helpers/v2_editor.py`；(2) 有任務碰到 `ui/tab5_data_guard.py`；
> (3) 有任務碰到 `scripts/fetch_nav_cache.py`；(4) user 直接點名。
> ⛔ **在補上之前，這兩列屬本節末句所稱「未完整登錄」的狀態**，讀者請據此打折信任。

**符合 EX-CACHE-1 的標準寫法**:
```python
try:
    import streamlit as st
except ImportError:
    class _NoOpST:
        @staticmethod
        def cache_data(*args, **kwargs):
            if args and callable(args[0]):
                return args[0]
            return lambda f: f
        cache_resource = cache_data
    st = _NoOpST()  # noqa
```

新增例外**必須**:(1) 在此表登錄、(2) 對應檔案加註解指回此表、(3) PR 描述附理由。**禁止**未經登錄的潛在「軟例外」。

---

#### 8.3.P 尚未判定（待派工查證）—— 2026-08-28 新增

> **本小節存在的理由**：本檔原本沒有「待判定」的落點，於是「**待查證**」的事項散在各處，
> 寫成「⛔ 在查證完成前不得引用本段…」這種**沒有指定由誰查、也沒有觸發點**的句子。
> 配上 §-1「沒觸發不動工」，那種寫法會讓待查證事項**無限期存續**，
> 實質上變成一張**永久豁免**——「因為還沒查，所以不能動」，而「查」永遠不會被排進來。
> **本小節把那些事項集中登記，並強制每一列都要有：① 待答問題、② 由誰查、③ 觸發點。**
>
> ⚠️ **登記 ≠ 動工**（§-1）：本小節不構成主動動工授權，也**不得**被引用來發動巡邏式清查。
> ⚠️ **登記 ≠ 豁免**：登記在此的項目**不是**「已判定合憲」，只是「**還沒判定**」。
> **不得**引用本小節主張任一列合憲或違憲。

| ID | 待答問題 | 由誰查（§-2 規則 4：獨立分派，不可自己查自己） | 觸發點（任一命中即應派工） |
|---|---|---|---|
| **P-NAVCACHE-1** | `scripts/fetch_nav_cache.py`（見 §8.2.A.1 **EX-CISCRIPT-1**）已無排程、僅手動觸發。v3 `01`-2 對「**已無排程、僅手動觸發**」的 script 該如何處置——是「仍有用途」還是「Orphaned 待清理」？<br>**已知事實（實測）**：它**不是 0 caller**——`scripts/accumulate_nav_tw.py` 與 `scripts/migrate_nav_caches_to_sheet.py` 以 `importlib` 載入它、復用 `_discover_fund_codes()`；`docs/NAV_NAS_CRON_SETUP.md` 亦指向它。**「沒有排程」不等於「沒有人用」。** | **獨立一組**，**不得**由當初補登 EX-CISCRIPT-1 的同一組承接 | (1) 有任務碰到 `scripts/fetch_nav_cache.py` 或 `cache/nav/`；(2) `repositories/fund/nav_metrics.py::fetch_nav` 的降級鏈移除 `_src_cache_files`；(3) 兩支兄弟 script 不再 `importlib` 載入它；(4) user 直接點名 |
| **P-NDCCACHE-1** | `ui/helpers/macro/ndc.py::_cached_ndc_score` 在 **UI 層快取外部 HTTP**（→ `fetch_ndc_signal_history` → `fetch_url(FINMIND_BASE)`），且該 L1 fetcher **已自帶 `@_ttl_cache(ttl_sec=TTL_15MIN)`** → **兩層同 TTL 疊加**。<br>2026-08-28 已**移出 EX-UICACHE-1**（不符其條件 ②）。**待答**：它是該（a）直接刪掉 UI 這層、由 L1 既有 cache 承擔，還是（b）另有 UI 層非快取不可的理由（例如 `_ttl_cache` 與 `st.cache_data` 的跨 session 語意差異）需另立例外？<br>📌 **2026-09-01 補註（狀態不變，本列仍為未判定）**：同日更正了 §8.2.A.1 `EX-PASSTHRU-1` 對 **同一個檔案** 的登記（該例外原把 TW-local 四個 fetcher 記在 `ui/tab1_macro.py`，實測真正的呼叫點是 `ui/helpers/macro/ndc.py::_fetch_ndc_score`，且四個裡只有 `fetch_ndc_signal_history` 還活著）。⛔ **那是「UI 直呼 L1」的分層問題，與本列的「UI 層快取歸屬」是兩個獨立問題** —— **本列一字未改、仍未判定**，**不得**引用該更正主張本列已結案。 | **獨立一組**，**不得**由 2026-08-28 補登 EX-UICACHE-1 的同一組承接 | (1) 有任務碰到 `ui/helpers/macro/ndc.py` 或 `repositories/macro_tw_local_repository.py`；(2) 實際出現「NDC 分數顯示過期／不一致」的 bug；(3) user 直接點名 |
| ~~**P-UIHTTP-1**~~ **✅ `_get_benchmark_series` 部分已處置（2026-08-28，Phase 1.2）** ⚠️ **不是「UI 層已無 Yahoo 直抓」** | ~~`ui/components/mk_dashboard.py::_get_benchmark_series` 以 `yfinance` 在 **UI 層直抓外部行情** ＋ `st.session_state` **自建快取**，產出顯示給 user 的 `Benchmark_Lag` 標籤與對比圖（登記於 §-1.5.1c 判定 2 **B 類**）。**它是 v3 `01`-1「主動搬遷」的真實適用對象。**<br>**待答**：搬遷方案為何（取數下沉 `repositories/`、快取歸屬哪一層、`bench_ticker` 由 UI 傳入的介面怎麼切）？~~<br>**（上段為登記當時的原文，加刪除線保留 —— 這是狀態更新，不是漏刪。）**<br>**處置結果**：取數實作已搬至 L1 **`repositories/macro/yf.py::fetch_benchmark_close`**；`st.session_state["mk_bench_cache"]` **已移除**，快取改吃上游 `fetch_yf_close` 的 `@_ttl_cache(TTL_10MIN)`（TTL 走 `shared/ttls.py` SSOT，**不疊第二層**）；`bench_ticker` 仍由 UI 傳入，介面即 `fetch_benchmark_close(ticker)`，UI 端 `_get_benchmark_series` 只剩一行委派。分層走 **UI 直呼 L1 + 登記 EX-PASSTHRU-1**（第 9 組成員，理由見該列）。<br>**兩件順帶修好的事**：(1) 原實作 `except Exception` 一把抓後回 None，**吞掉了包含 schema 違反在內的所有錯**；現在走上游既有契約 —— 抓取失敗印出原因並回空序列，**schema 違反則當場 raise**（§1）。(2) 原 session 快取**無 TTL**，一個 session 開多久就用多舊的基準（違 §2.4「超過 TTL 應重新抓取」）；現在 10 分鐘過期。**這兩項是刻意的行為變更，不在「零行為變更」的宣稱範圍內。**<br>**守衛**：`tests/test_benchmark_fetch_migration_v19531.py`（18 條，含 11 條經突變驗證會轉紅的 fail-closed 斷言）。<br>⚠️ **範圍收窄聲明（2026-08-28 稽核要求）**：本列原文圈定的就只有 `_get_benchmark_series`，處置也只做到它。**「結案」二字會讓後人誤以為 UI 層已經沒有 Yahoo 直抓 —— 並沒有。**實測 `ui/sidebar.py` 的「🔍 測試 Proxy 連線」按鈕仍以 `requests` 直打 `query1.finance.yahoo.com/v8/finance/chart/USDTWD=X`（6 個 endpoint 之一）。<br>📌 **該處不另立新的待判定項，因為它已經被判定過了**：見 §-1.5.1c 判定 2 **A 類 A1**，已歸類為「連線診斷 ping —— 回應內容丟棄、不產生任何業務指標 → 不是 v3 所指的私有資料抓取」，處置為「登記為邊緣案例、不動工」。**再開一個 `P-` 項會等於宣稱它「尚未判定」，與 A1 自相矛盾。**⚠️ 此為本組判斷，**與稽核建議不同**（稽核要求另立待判定項），已在 PR 具名回報請總管裁決；若總管認為 A1 的分類本身該重審，那要動的是 A1 那一列，不是在這裡疊一個新項。<br>**commit / PR**：見本輪 PR 描述。 | ~~**獨立一組**（實作與稽核分離，§-1.5 v1 第一條第 3 點）~~ → 已由資料工程組執行；**稽核仍須另一組**（§-2 規則 4，實作者不得自己驗收自己） | ~~(1) 有任務碰到 `ui/components/mk_dashboard.py` 或 `ui/tab3_portfolio.py` 的波段觀測站區塊；(2) 基準指數取數出現 bug；(3) user 直接點名~~ → **觸發點 (1) 已命中並執行完畢**。⚠️ **本列不再是待判定項**，但**尚未經第二組獨立稽核**（§-2 規則 6） |
| **P-UIGSPREAD-1** | `ui/**` 多處直呼 gspread client（如 `ui/tab3_portfolio.py` 的 `_sa.open_by_key(...)` 存取探測），**實際是往返 Google Sheets API 的網路呼叫**。**待答**：這是否等同既有 **EX-CRUD-1** 所述的「本地持久化 CRUD」而受其豁免，還是屬 v3 `01` 所禁的「UI 層私自抓取」？（登記於 §-1.5.1c 判定 2 **C 類**）<br>⛔ **2026-08-28 第三輪稽核補：本列不是孤立的待辦，`EX-UICACHE-1` 整列掛在它上面。** 該例外的條件 ② 已改寫為「被快取的必須落在 EX-CRUD-1 的『本地持久化 CRUD』射程內」，而其 **4 個成員中有 3 個快取的正是 gspread 讀取**（`load_policy_v2` / `list_policy_worksheets` / `coverage_status`→`load_points`，逐成員鏈路見 §8.2.A.1 驗證段 ③）。**本列若判定 gspread 直呼不等同「本地持久化」，`EX-UICACHE-1` 當場失去依據、須整列重審。**<br>⚠️ **另有一道前提裂縫必須一併裁決**：EX-CRUD-1 的免責推理前提是「**無 `@_ttl_cache` / `@st.cache_data` 裝飾** → 不存在 cache 分散問題」，而 `EX-UICACHE-1` 的成員**正好就是加了 `@st.cache_data` 的那一類** —— 也就是說，即使本列判 gspread 算「本地持久化」，**EX-CRUD-1 的結論仍不當然可以搬給一個違反其前提的對象**。兩件事要分開查、分開答。 | **獨立一組**（§-2 規則 4/5：此問題取決於「有沒有漏看」）；**且不得由 2026-08-28 補登／回修 `EX-UICACHE-1` 的任一組承接**（實作與稽核分離） | (1) 有任務碰到上述任一 UI 檔的 gspread 呼叫；(2) 有人要據此擴大或收窄 EX-CRUD-1；(3) **有人要引用或依賴 `EX-UICACHE-1`（含把新成員收進該例外）**；(4) user 直接點名 |
| **P-UISUBPROC-1**（2026-08-28 第三輪稽核補登） | `ui/sidebar.py::render_sidebar()` 內「♻️ 強制同步 GitHub 最新邏輯」按鈕 `import subprocess`，跑 `git rev-parse --short HEAD` 與 **`git ls-remote origin main`**（後者**實際連到 GitHub**）比對部署版本，結果以 st.success / st.warning ＋ Reboot 連結顯示（登記於 §-1.5.1c 判定 2 **C 類**）。<br>**待答**：它算不算 v3 `01`-1 所禁的「UI 層私有資料抓取」？**兩邊都有話說，故本組不裁決** —— 傾向「不算」的理由：取回的是 **commit SHA**，用途是**部署版本診斷**，不產生任何業務指標，形態接近 A 類 ping；傾向「算」的理由：它**確實對外往返**，且 **A 類三個成員全是 HTTP ping、本項不是**，`subprocess` 形態的對外往返在本檔尚無任何判定先例，**不得直接套用 A 類結論**。<br>⚠️ **本列補登的直接原因**：上一輪把它點名在 C 類、寫「**本段不下判定**」，卻**沒有給任何 `P-` 登記、沒有指定由誰查、沒有觸發點** —— 那正是本小節前言所指「**待查證沒有出口 ＝ 實質永久豁免**」的形態，**只修了一半**。 | **獨立一組**（§-2 規則 4/5：本題同時取決於「有沒有漏看其他非 HTTP client 形態的對外往返」）；**不得由 2026-08-28 登記 C 類的同一組承接** | (1) 有任務碰到 `ui/sidebar.py` 的版本比對區塊；(2) 有人要據此擴大或收窄 **A 類「連線診斷 ping」** 的定義；(3) `ui/**` 再出現第二處 `subprocess` 對外往返；(4) user 直接點名 |
| **P-YFDUPE-1**（2026-08-28 Phase 1.2 稽核後補登） | `repositories/fund/sources.py::_src_yahoo_finance_nav`（`urllib.request.urlopen` 直打 `query2.../v8/finance/chart/`、**不走 `infra.proxy`**、自帶 parser）與 `repositories/macro/yf.py::fetch_yf_close` 是否構成 v3 `01`-2 所禁的「**同一個資料來源**兩套取數實作」？<br>**已知事實（實測）**：兩者都打 Yahoo v8 chart、都取 `indicators.quote[0].close`；但 host 不同（query2 vs query1）、傳輸路徑不同（urlopen 直連 vs NAS proxy）、symbol 語意不同（Morningstar `{secId}.F` 基金 vs 一般 ticker）。第三套在 `scripts/fetch_nav_cache.py::fetch_yahoo_finance_history`。repo 既有紀錄：`shared/api_endpoints.py` 檔頭「✅ YF query2 Morningstar URL 真 dupe」。<br>**為什麼要問**：若判定為「同一來源」，§8.2.A **EX-PASSTHRU-1** 對 `fetch_benchmark_close` 的升級觸發條件會被牽動（該列寫「新增 source」即須重新評估是否上提 L2）。<br>⛔ **這是判斷題，不是查證題** —— 總管裁決，執行組不得自行認定。 | **總管裁決**（非查證；若需佐證再派獨立組） | (1) 有任務碰到 `repositories/fund/sources.py` 的 Yahoo 段；(2) 有人要據此擴大或收窄 EX-PASSTHRU-1；(3) 有人要發動 Yahoo 取數收斂；(4) user 直接點名 |
| **P-BENCHTZ-1**（2026-08-28 Phase 1.2 登記，不修） | `repositories/macro/yf.py::fetch_benchmark_close`對 `ticker` **零驗證**，且 `normalize()` 隱含假設「交易所開盤時刻落在當日 UTC 內」。**開盤早於 00:00 UTC 的交易所會被 normalize 到前一天**（ASX ~23:00Z、NZX ~21:00Z）。<br>**現況不是 bug**：UI selectbox 只有 SPY / QQQ（美股，13:30Z），射程外。**待答**：擴充 ticker 清單前要怎麼處理（改用交易所時區 normalize？白名單？）<br>⛔ 依 **§-1**（無觸發、無 bug）**本輪不修**，只登記；已就地寫進該函式 docstring 的「已知陷阱」。 | **獨立一組**（實作與稽核分離） | (1) 要把基準 ticker 擴到美股以外；(2) 出現基準日期偏移一天的實際回報；(3) user 直接點名 |
| **P-WHERECONTENT-1**（2026-09-05 由 #777 實作組登記於 `tests/test_batch2_top_card_grid.py` 的註解內，本輪搬入正式住所） | `where=`（線框 Rule 04 的「去哪補」）的**內容對不對**，目前**沒有任何機器規則在守**；#777 補的是「**有沒有帶** `where`」的射程，不是內容。<br>**守得到什麼**：`tests/test_batch2_top_card_grid.py` 的 `test_where_is_mandatory` 與 `test_not_ready_card_must_say_where` 驗「站點有沒有帶 `where`」——#777 讓後者第一次看得見 `ui/views/page_01_macro.py` 的卡片 dict。<br>**守不到什麼，以及為什麼**：驗內容的是同檔 `test_every_where_names_something_that_exists_on_screen`，而它**以 `「」` opt-in**、且只讀 `where=` 運算式裡的**字面值**（`_str_consts()`）。`ui/views/page_01_macro.py` 的指路幾乎全部走 `ui/views/page_01_macro.py::_where_to_load` 與 `ui/helpers/story_nav.py::where_to_find` 這種**執行期組出來的值**，AST 取不到字面值 → 規則看不到。<br>**已知事實（本組實測，量測日 2026-09-05，於 `15af51b`；驗證指令見下）**：該檔被 `_where_sites()` 收到 **20** 個站點 —— **19 個是執行期呼叫**（`card-dict` 13 ＋ `card-dict-item` 1 ＋ `not_ready` 3 ＋ `empty_state` 2），**1 個是字面值**（`not_ready(…, where="Streamlit Cloud → Settings → Secrets 的 FRED_API_KEY")`）。而**那唯一的字面值沒有 `「」`** → **該檔對內容規則貢獻的 `「」` 段落數為 `0`**（全 repo 共 19 段，全部來自別的檔）。<br>→ **也就是說：該檔 20 個指路，內容一個都沒被驗過 —— 覆蓋率是 `0/20`。**<br>📌 **2026-09-05 續記：本列的觸發點 (1) 已命中，數字擴大近一倍**（**狀態更新，不是漏刪** · 登記者：**Track A 批次三-B 實作組**，依總管指示補登；上一段的 `0/20` **原文一字未改**，那是 `15af51b` 當時的量測）。PR #781（① 詳細區四塊灰態改寫成真內容）碰到了 `ui/views/**` 的灰態卡，正是觸發點 (1) 所指的那件事。**本組實測（量測日 2026-09-05，於該 PR 的 `0b33e18` 之後）**：該檔 `_where_sites()` 站點 **21 → 39**（`card-dict` 13→20、`card-dict-item` 1→1、`not_ready` 6→17、`empty_state` 1→1；值型別 `Call` 19→**35**、`JoinedStr` 1→2、`Constant` 1→2）。⚠️ **可被內容規則看見的 `「」` 段落數仍然是 `0`**（同一支 `_str_consts()` ＋ `_WHERE_QUOTED` 實測）→ **覆蓋率由 `0/20` 變成 `0/39`**。⚠️ **本列的問題因此變大了，不是變小** —— 新增的 18 站全部走 `_where_to_load()` 這種執行期值，而該 PR 另有一處**原本是字面值的手抄**（AI 送出鈕）在同批被改成共用變數，**那一站也因此從內容規則的視野裡消失**（該 PR 以自己的執行期斷言補上替代覆蓋，見其 `test_the_ai_remedy_names_the_button_that_is_actually_rendered`）。⚠️ **本續記為單組實測，未經第二組驗證**（§-2 規則 6）；「21」與「39」是**同一支 `_where_sites()` 在 `origin/main` 與該 PR 上各跑一次**得到的，與上一段的「20」差 1 是**量測基準 commit 不同**，本組**未追查**那 1 站是何時新增的。<br>**續記的驗證指令（repo 根目錄，單行）**：`python3 -c "import sys; sys.path.insert(0,'tests'); import tests.test_batch2_top_card_grid as M; print(len([x for x in M._where_sites() if x[0]=='ui/views/page_01_macro.py']))"`<br>**要答的是**：該補一條「執行期指路」的驗法（例如在 AppTest 下渲染後，比對畫面上真的有那顆鈕），還是承認靜態規則到此為止、改用別的手段？<br>📌 **2026-09-05 射程擴充（狀態更新，不是漏刪；登記者：Track A 批次三-B 實作組，依總管指示）**：本列原本只管「**指路（`where=`）的內容**對不對」，**自本次起擴為「灰態三要素的內容對不對」** ——標題、缺什麼、去哪補**三項都適用**。**理由：同一個病的兩面，同一個「靜態規則到此為止」的結論，不必開新列。**<br>**現成反例（本組實測，可複跑）**：把 `ui/views/page_01_macro.py::_detail_long()` 的灰態改成 `not_ready("流動性、信用與情緒均在正常區間，本輪無異常，不需要補任何資料。", where=_where_to_load())` —— 它**有 `⬜`、有「（請先到：」**，`tests/test_wf01_detail_zone_order.py` 的兩條斷言都滿足 →`pytest tests/test_wf01_detail_zone_order.py tests/test_batch2_top_card_grid.py tests/test_render_state_color_separation.py` → **683 passed, 32 skipped，零紅燈**。⚠️ **形式滿分、內容全反** —— 在完全沒有資料的情況下告訴使用者「一切正常、不需要補」。**那是符號檢查，不是語意檢查；把單位切到單一字元也是同樣結果。**<br>⚠️ **「#777 擴大射程後沒有新紅燈」不等於「指路都是對的」** —— 它只表示那裡沒有手抄字串。<br>⚠️ **兩處既有敘述經本組實測不成立，就地更正**（**有意識的更正，不是漏刪** · 日期 **2026-09-05** · 決策者：**DOC-P1 文件與治理組**）：**(a)** 本輪派工單寫 ~~「`ui/views/page_01_macro.py` 的 14 處 `where` 全是執行期組出來的值」~~ → **執行期值實為 19 處**，「14」只涵蓋 dict-carried 那一半，漏掉 5 個同樣是執行期值的直接呼叫；**(b)** 實作組原登記寫 ~~「20 個站點裡，14 個 dict-carried 與 **4 個**直接呼叫」（＝18）~~ → **直接呼叫的執行期值是 5 個，合計 19**。**兩邊在寫下時都指向同一個真問題、方向完全正確**，被權衡掉的只是它們的**數字與射程**；而**真正該記的數字是覆蓋率 `0/20`，不是 14 也不是 18** —— 因為那個唯一的字面值同樣沒被驗到（無 `「」`）。**那才是本列存在的理由。**<br>**驗證指令（repo 根目錄執行，單行）**：`python3 -c "import tests.test_batch2_top_card_grid as M; print([(fn, type(w).__name__) for f,l,fn,w in M._where_sites() if f=='ui/views/page_01_macro.py'])"`（`Call` ＝ 執行期值、`Constant` ＝ 字面值，須人工判讀）。<br>⚠️ **本列數字為本組單組實測，未經第二組獨立驗證**（§-2 規則 6）；**「該檔沒有第 21 個站點」本組不宣稱** —— `_where_sites()` 自己就看不到三種形狀（`dict(...)` 建構式、帶 `**other` 的 dict、在別的函式建好再傳進來的 dict，見 `_card_spec_sites()` 的 docstring）。 | **獨立一組**（§-2 規則 4）；**不得**由 2026-09-05 擴大本組規則射程的那一組（#777 · GUARD-W1）承接，**也不得**由 #772 寫下原始「去哪補」規則的那一組承接 —— 兩者都會變成自己驗自己。⚠️ **本輪搬運組（DOC-P1）同樣不承接**（本列的數字更正出自本組，見左欄 (a)(b)）。 | (1) 有任務碰到 `ui/views/**` 的**灰態卡**（含 `ui/views/page_01_macro.py::_render_deferred_blocks` 與各卡片建構器）；(2) 有任務碰到 `ui/views/page_01_macro.py::_where_to_load` 或 `ui/helpers/story_nav.py` 的指路產生邏輯；(3) **有人要引用或擴大「去哪補」這組規則**（含把新成員收進 `WHERE_MISSING_EXEMPT` / `WHERE_NAME_EXEMPT` / `NOT_READY_CARD_MISSING_EXEMPT`，或調動三個錨點下限）；(4) 出現「灰卡指到一顆當下不存在的按鈕」的實際回報（`_where_to_load()` 的 docstring 記載 2026-09-05 修過**一次**同型 bug，當時是靠 AppTest 實測抓到的，**不是**靠這組規則的任何一條）；(5) 有人要把 `where=` 從執行期組值改回字面值（那會讓內容規則突然「看得到」，屆時現況即刻改變）；(6) **user 直接點名**。 |
| **P-GREYSCENARIO-1**（2026-09-05 由第三輪獨立稽核抓出，Track A 批次三-B 實作組依總管指示登記） | 灰態／誠實度斷言**只覆蓋「全有」與「全無」兩個 session 端點**，中間那一大片**零斷言**。<br>**守得到什麼**：`tests/test_wf01_detail_zone_order.py::test_a_block_with_nothing_to_show_goes_grey_and_says_where` 只渲染 `{_SK_IND: {}}` 一個 session；`test_each_block_renders_real_content_not_just_a_heading` 只渲染一份「四塊都有料」的 session。<br>**守不到什麼**：兩端點之間的狀態全部沒有被渲染過 —— **部分子指標 `_err`**、**dict 在但值全 `None`**、**雷達回一個全 ⬜ 的 dict**（不是缺鍵）、**`source_ok=False`** 等。⚠️ **把一個沒被渲染的畫面切一千段，還是零段** —— 粒度補不了情境。<br>**現成反例（本組實測，可複跑）**：把 `ui/views/page_01_macro.py::_detail_inflection()` 裡「5 組拐點訊號全部沒取到」那句整塊結論換成 `st.success("✅ 5 組拐點訊號全部未觸發，目前沒有拐點風險。")` → ~~同上三個測試檔~~ → **683 passed, 32 skipped，零紅燈**。⚠️ **2026-09-05 就地補：本列原本寫「同上三個測試檔」，那是指向鄰列的指代** ——表格一旦重排、或鄰列 `P-WHERECONTENT-1` 被改寫，這個反例就懸空了。**本節存在的理由就是「待查證要有出口」，出口不該繫在鄰居身上。****自足的複跑指令（repo 根目錄，量測日 2026-09-05，本組實跑）**：`python3 -m pytest tests/test_wf01_detail_zone_order.py tests/test_batch2_top_card_grid.py tests/test_render_state_color_separation.py`（**未套反例的基線同樣是 683 passed, 32 skipped** —— 本反例的重點正是**套上去也不會變**）。⚠️ **那句話正是該函式自己的註解明令禁止的**（原文：「不得讓使用者以為『5 個都沒亮紅燈 ＝ 沒有拐點風險』（§1）」）。**根因不是粒度，是情境**：這條路要 `_SK_TP` **有值**才走得到，而灰態斷言那一輪 `_SK_TP` 根本不存在。<br>**要答的是**：要不要改用參數化／property-based fixture 枚舉狀態？⚠️ **這不是「再補一層斷言」，是換一種測試方法** —— 本頁 6 個 session key、每個至少 6 種形狀，組合數以萬計，逐一手寫斷言不可行。<br>⚠️ **粒度那一半已經到底，不要再往那個方向補**：`render_cards()` 的一張卡就是畫面最小視覺單位，往下沒有更小的東西可切（2026-09-05 總管裁決）。<br>⚠️ **本列數字與反例為單組實測，未經第二組獨立驗證**（§-2 規則 6）；「中間狀態只有這幾種」**本組不宣稱**。 | **獨立一組**（§-2 規則 4）；**不得**由 Track A 批次三-B 實作組承接，**也不得**由 2026-09-05 三輪稽核的任一組承接 —— 兩者都會變成自己驗自己。 | (1) 有任務碰到 `ui/views/**` 的灰態斷言；(2) **有人要擴大或引用這組守衛**（含把新區塊收進 `_GREY_BLOCKS`、或調動 `_grey_units()` 的切法）；(3) 出現「**畫面說沒事、實際沒抓到**」的實際回報；(4) **客戶／user 直接點名**。 |
| **P-SINKGRAIN-1**（2026-09-07 登記） | `tests/test_wf02_health_no_writes.py::_sink_targets` 是**函式粒度的名字哨兵**：`ast.walk` 命中 `_WRITE_PRIMITIVES`（含 `to_csv`）就把**整顆函式**換成 recorder，**不看分支、不看可達性、不看引數**。<br>**已知事實（本組實測）**：`ui/helpers/fund_grp_health/rotation.py::_render_pairs_body` 的 `to_csv` 包在 `if offer_download:` 裡，而 ② 委派的入口 `render_rotation_section` **硬編 `offer_download=False`**（傳 `True` 的是批次那一支 `render_rotation_section_from_df`）；同 PR 的另一條守衛 `_callgraph_sheet_writes` 對 `render_rotation_section` 是 **hits 0** —— **兩條守衛判定相反**。② 因此把「輪動配對」列進 `ui/views/page_02_health.py::DROPPED_FOR_ZERO_WRITE`（該常數的 2026-09-07 更正註已就地記載此機制）。<br>**待答**：守衛要不要、以及能不能分辨「分支／引數／可達性」？<br>⛔ **不是問「要不要把 `to_csv` 從清單拿掉」** —— 它是依前次稽核**刻意補進**名字清單的（判準：名字含不含糊）；放寬清單**不在本列射程內**，該常數的註記亦明禁「在合併壓力下放寬守衛」。 | **獨立一組**（§-2 規則 4）；**不得**由 `_sink_targets` 的作者組承接，**也不得**由 2026-09-07 更正該段機制的 ② 實作組承接。 | (1) 有任務碰到 `_sink_targets` / `_WRITE_PRIMITIVES`；(2) 有人要把 `render_rotation_section` 移回 `DELEGATED_ENTRIES`；(3) 同型偽陽性在別的槽再出現一次；(4) **user 直接點名**。 |
| **P-RETRYKIND-1**（2026-09-07 登記） | 非暫時性狀態碼**目前會被重試**，且**文件與行為不符**。<br>**已知事實（本組實測）**：`infra/source_backoff.py::kind_for_status` 對 4xx 不與 5xx 分開（`status >= 400` → `server_error`）；`infra/gspread_retry.py::kind_for_gspread_error` 把 **404 / 407** 改判 `unreachable`；而 `GSPREAD_RETRYABLE_KINDS` ＝ `{"server_error", "unreachable"}` ⇒ **400 / 404 / 407 落在可重試集合內**。同檔 `with_gspread_retry` 的 docstring 卻寫「非 `retry_kinds` 內的分類（429 / 403 / 404 / 407）**第一次失敗就直接拋出，不重試**」。<br>**既有缺陷、非新引入**；現況已被 `tests/test_nav_append_retry.py::test_known_gap_non_transient_statuses_are_currently_retried` 以 ratchet 釘住（該測試自陳「釘的是現況，不是應然」）。<br>**待答**：要收窄分類（4xx 與 5xx 分開／404、407 不入可重試集），還是把 docstring 改成照實寫？ | **獨立一組**（§-2 規則 4）；**不得**由寫下該 ratchet 的 #802 NAV 重試實作組承接。 | (1) 有任務碰到 `infra/source_backoff.py` 或 `infra/gspread_retry.py`；(2) 有人要調動 `GSPREAD_RETRYABLE_KINDS` 或 `kind_for_status` 的分類；(3) 出現「永久性錯誤被重打」的實際回報；(4) **user 直接點名**。 |
| **P-WSTOREWRITE-1**（2026-09-07 登記） | **讀取名的函式底下藏著建分頁 ＋ 寫格。**<br>**已知事實（本組實測）**：`services/macro/weights_store.py` 的 `load_active` 在 GS 後端走 `_gs_load` → `_gs_get_worksheet()`，而該函式會 `sh.add_worksheet(title=_GS_WORKSHEET, ...)` ＋ `ws.update("A1:C3", ...)`；`get_weight_override` / `get_verdict_cutoffs` / `get_phase_thresholds` 三支都呼叫 `load_active`。<br>**與已修的兩處同型**：`repositories/pool_repository.py::_ws`（#796 改為「只有寫入路徑才確保 schema」）與 `repositories/portfolio_perf_repository.py::_ws`（#800 拆出 `_ws_for_write`，`_ws` 保證零寫入）。<br>**待答**：本檔要不要照同一個讀寫分離切法處理，還是有它非在讀路徑建分頁不可的理由？ | **獨立一組**（§-2 規則 4）；**不得**由 #796 或 #800 兩組承接 —— 那會變成拿自己的結論驗自己的延伸。 | (1) 有任務碰到 `services/macro/weights_store.py`；(2) **有人要引用 #796／#800 的「讀路徑零寫入」結論**；(3) 出現總經權重面板意外建分頁／改格的回報；(4) **user 直接點名**。 |
| **P-CALLSTATIC-1**（2026-09-07 登記） | `tests/test_portfolio_perf_render_no_writes.py::_calls()` **只走 `ast.Call`**（取 `Name.id` / `Attribute.attr`）⇒ **別名規避**、以及**把寫入方法當引數傳給包裝器**（裸 `ast.Attribute`，不在 Call 的 func 位置）在**靜態層全綠**。該檔 docstring 已自陳「本條是**結構**輔助，不是主守衛：改個別名就繞得過去」，實際靠**行為層**哨兵兜住。<br>⚠️ **「那兩顆突變在行為層皆轉紅」為 #800 回報，本組未實跑**（本環境無 pytest，未 `pip install`）—— 本組只靜態核對了 `_calls()` 的走訪範圍與該 docstring 的自陳。<br>**待答**：靜態層要不要補到看得見這兩種形態，還是明訂它永遠只是 code review 用的輔助、並在檔內寫死「不得以它為由簡化行為層」？ | **獨立一組**（§-2 規則 4）；**不得**由 #800 實作組承接。 | (1) 有任務碰到該檔或 `_snapshot_control` / `append_snapshot`；(2) **有人要以「靜態層有守」為由簡化或刪除行為層哨兵**；(3) 別處要照抄這個 `_calls()` 形狀當守衛；(4) **user 直接點名**。 |
| **P-AIKEYCI-1**（2026-09-07 登記） | ② 的「AI 跨檔評論」區塊在零寫入守衛下不紅，**靠的是環境變數不存在**。<br>**已知事實（`ui/views/page_02_health.py` 該 `safe_section` 上方就地記載，本組已讀）**：先擋住的是 `_render_ai_cross_fund_evaluation` 在呼叫 widget **之前**的早退 `if not _key: st.caption("⬜ 未設定 GEMINI_API_KEY…"); return` ⇒ **CI 沒有那把 key，所以走不到下游**。該註記自陳「那不是本檔能保證的事」，並明寫**若哪天 CI 設了那把 key，這一支就會往下走**，屆時 `ai_cache.save`（`tempfile.mkstemp` ＋ `os.replace` 的**本機磁碟**寫，不是 Google Sheet）會不會被碰到**必須重驗**。<br>**待答**：這個前提要不要收進守衛自己控制的範圍（例如明示把 key 清掉再渲染），還是接受它是環境相依、只在文件登記？ | **獨立一組**（§-2 規則 4）；**不得**由 ② 持倉體檢實作組（#801）承接。 | (1) **CI secrets 加入 `GEMINI_API_KEY`**（該註記自己指定的重驗時點）；(2) 有任務碰到 `ui/helpers/fund_grp_health/ai.py` 的早退或 `ai_cache`；(3) 有人要把該區塊移進／移出 `DROPPED_FOR_ZERO_WRITE`；(4) **user 直接點名**。 |
| **P-CONCURGUARD-1**（2026-09-07 登記） | `tests/test_ci_concurrency_main_push_guard.py` 有**兩個誤紅風險**（方向皆 fail-closed，不是漏放）。<br>**(a) 語意正確的等價寫法會紅，而且死錯地方**：把 `concurrency.group` 改寫成 `format(...)` 會轉紅 —— 但**死在 `_tokenize` 的逗號上**（本組實測：`format('{0}', github.sha)` 在 pos 12 的 `,` 即拋「無法解析的字元」，因為 `_TOKEN_RE` 沒有逗號 token），**不是**死在 `_Parser` 那句「用到函式 …() —— 本求值器沒有實作」。⇒ **那個分支目前沒有任何測試走到，錯誤訊息會把人指去錯的方向。**<br>**(b) GitHub 合法簡寫會紅**：`concurrency: <字串>` 被 `_concurrency()` 的 `isinstance(cc, dict)` 斷言擋掉；而該寫法下 `cancel-in-progress` 預設 false ⇒ **對 main 其實是安全的**。<br>**待答**：求值器要不要支援逗號／函式與字串簡寫，還是明訂「本 repo 只准用 mapping ＋ 已實作文法」並把錯誤訊息改成講真正的原因？ | **獨立一組**（§-2 規則 4）；**不得**由 #803 實作組承接。 | (1) 有任務碰到 `.github/workflows/pr-check.yml` 的 `concurrency:`；(2) 有人要改寫成函式形式或字串簡寫；(3) 該守衛出現讀不懂的紅燈；(4) **user 直接點名**。 |
| **P-GRIDPRESENCE-1**（2026-09-07 登記） | **沒有任何守衛在斷言「② 這一頁真的畫出了 3 欄網格」。**<br>**已知事實（本組實測）**：`tests/test_ui_grid_contract.py` 的 `test_grid_columns_must_be_three_or_be_registered` 與 `test_grid_exempt_total_matches_the_table` 量的是「**欄數不是 3 的 `st.columns` 站點**」及其豁免總數（`GRID_EXEMPT_CALL_TOTAL == 90`）；而 `ui/views/page_02_health.py` **一個 `st.columns` 呼叫都沒有**（`st.columns` 在該檔只出現在 docstring 的敘述句裡），鐵則 01 一律走 `ui.helpers.ia.render_cards` ⇒ **拿掉 `render_cards` 呼叫並不改變那兩條所量測的東西**。`tests/test_wf02_health_skeleton.py` 只把 `render_cards` 列進 `_LAYOUT_TOOLKIT` **豁免**，**不斷言它被呼叫**。<br>⚠️ **「整個刪掉仍全綠」為來源輪回報，本組未實跑 pytest**（本環境無 pytest）—— 本組只靜態核對了上述量測對象與豁免關係。<br>**待答**：鐵則 01 的「**有沒有真的畫出來**」要用什麼驗（AppTest 渲染後數欄數？還是斷言委派站點存在）？ | **獨立一組**（§-2 規則 4）；**不得**由 ② 持倉體檢實作組（#801）承接，**也不得**由 `tests/test_ui_grid_contract.py` 的作者組承接。 | (1) 有任務碰到 `ui/views/**` 的 `render_cards` 站點或該契約檔；(2) **有人要調動 `GRID_EXEMPT_SITES` / `GRID_EXEMPT_CALL_TOTAL`**；(3) 出現「某頁網格整塊消失沒人發現」的回報；(4) **user 直接點名**。 |
| **GAP-SEARCH-CACHE-1**（2026-09-08 由 #828 實作組具名登記於 `services/fund_search.py` 的模組 docstring 內，本輪搬入正式住所）⚠️ **刻意沿用該檔已具名的 ID，不另取 `P-` 開頭的新名** —— 一件事兩個名字，下一個人 grep 哪一個都只找得到一半（§2.1 SSOT）；且 `services/fund_search.py` 與 `ui/views/page_03_research.py` 兩檔都已用這個字串回指，改名會同時弄斷兩個指標，而本輪**不得改任何 `.py`**。**本表的 ID 欄從未規定必須以 `P-` 開頭。** | `repositories.fund.tdcc_search_fund` 的**取數頻率**沒有落點：**L1 的快取只蓋一半，蓋到的那一半永不過期；沒蓋到的那一半每次呼叫都真的送一次 HTTPS。**<br>**已知事實（本組逐行讀 `repositories/fund/sources.py` 實測，不是轉述）**：<br>· `_tdcc_cache` 是**裸 dict**（無 TTL、無 eviction），只蓋三個 TDCC endpoint 的 HTTP 回應；<br>· ✅ **失敗不會 poison** —— `_tdcc_get()` 的 `except` 在寫 cache **之前**就 `return []`（符合 v3 `02`「只快取成功結果」）；<br>· ⚠️ 但成功路徑另有一種永久性問題：上游回 200 卻不是 list 時，`data if isinstance(data, list) else []` 會把 `[]` **永久**寫進 cache；<br>· ⛔ **FundClear 備援分支完全沒有快取**（且 `except Exception: pass`）——它只在 TDCC 兩個 endpoint 都沒命中時才跑，**那條路每一次呼叫都真的送一次 `timeout=8` 的 HTTPS**；<br>· ⚠️ 就算 TDCC 命中，比對／去重／NAV 併入那幾個迴圈**仍然每次重跑** —— 被快取的是回應，不是迴圈。<br>**呼叫頻率：新 ⑧ 與舊 ③ 不同，而這一半是新頁造成的（本組實測）**：舊 ③ `ui/helpers/fund_research/code_finder.py::_search` **只在 `if do_search and keyword.strip():` 之內**打 L1，結果存進 `st.session_state["tdcc_results"]`；新 ⑧ `ui/views/page_03_research.py::_render_results` 在 `_applied_query()` 非 `None` 通過之後，**每一次 render 都呼叫一次 `search_funds()`、不存結果**。而 `app.py` 用 `st.tabs`（**一次 run 渲染全部分頁**）⇒ **使用者只要送出過一次「TDCC 名錄命不中」的關鍵字，此後 App 裡任何一次 widget 互動都會再送一次未快取的 FundClear 請求** —— 而「名錄命不中」正是空狀態那一屏，**也正是使用者最會反覆操作的那一屏**。<br>⚠️ **與 §8.2.A.1 `EX-UICACHE-1` 就地記載的事故同型**：「未加此層前，任一分頁的任一次互動都會重打 Google Sheets；25 檔實測 rerun 起算 17 秒後 `WebSocket onclose`，2026-08-14 實機兩次重現」。⛔ **本組只指出形狀相同，未實測本例是否會重現該事故** —— 不得把這句讀成「已經量到會斷線」。<br>**待答**：正解該放哪一層？三條路各被既有條文堵住一段 ——**(a)** **不得**在 L2 `services/fund_search.py` 加 TTL：與 L1 那層疊加後沒有人推理得出「畫面上這份清單多舊」，而且**下層根本沒有 TTL** ⇒ 變成「上層會過期、下層永不過期」，比單純疊加更難推理；**(b)** **不得**在 UI 層用 `@st.cache_data` 包外部 HTTP（`EX-UICACHE-1` 升級條件 (1) 明禁）；**(c)** **家在 L1**（`tdcc_search_fund` 的 FundClear 分支該有自己的快取／退避），**但那會動到舊 ③ 正在跑的路徑，屬 §8.4 步驟 4 的範圍決定，且客戶明令不得改動線上運作中的舊 Tab**。⇒ **三條路都需要一次拍板，不是實作組能自行決定的 —— 這正是本列必須有出口的理由。**<br>⚠️ **歸屬先講清楚，免得被讀成「新 PR 弄壞的」**：上面「已知事實」那五條是 **L1 既有缺陷**，#828 之前就在；**#828 帶進來的只有呼叫頻率那一半**。<br>⚠️ **本列由架構與文件組單組實測，未經第二組獨立驗證**（§-2 規則 6）；「L1 只有這一處沒快取」與「本例會不會重現該事故」**本組未查證、也不宣稱**。 | **獨立一組**（§-2 規則 4）；**不得**由 #828 實作組（造成呼叫頻率那一半的那一組）承接，**也不得**由本輪搬運組（架構與文件組）承接 —— 兩者都會變成自己驗自己。⚠️ 其中 **(c) 那條路不是查證題，是範圍決定**（§8.4 步驟 4）：須由總管附推薦方案送客戶拍板，**不得由執行組自行認定**。 | (1) 有任務碰到 `repositories/fund/sources.py` 的 `_tdcc_get` ／ `tdcc_search_fund` ／ FundClear 備援分支；(2) 有任務碰到 `services/fund_search.py` 或 `ui/views/page_03_research.py::_render_results`；(3) **有人要在這三層任一層加快取、TTL 或退避**（含把它收進 `EX-UICACHE-1`，或主張本頁已合憲）；(4) 出現「搜尋很慢／畫面斷線／被來源限流」的實際回報；(5) 舊 ③ `ui/helpers/fund_research/code_finder.py::_search` 改走 L2（那一刻本 fetcher 只剩一個消費者，收斂成本最低，同 §8.2.A.1 該成員的退役條件）；(6) **user 直接點名**。 |
| **P-AIFLAG-1**（2026-09-08 登記；起因見 §8.2.A.1 `EX-AI-1` 該列的 2026-09-08 逐道核對） | **`EX-AI-1` 的條文與實作住在不同層級 —— 要把實作搬回條文寫的地方，還是把條文改寫成實況？**<br>**已知事實（本組實測，逐道分開）**：**①** 視覺旗標（`🧬` / `使用模型`）在 `*.py` **0 命中**，`infra/llm.py::_call_gemini` 成功路徑回的是**未經包裝的原始 LLM 文字**（無模型名、無標記）；實際存在的 AI 標示**整層下移到 L3 渲染層**（四個消費者的 `🤖` 標題／caption），而 `ui/helpers/ai_summary.py` 那行 `st.caption("💡 模型：…")` 帶的是 **key 數／provider，不是模型識別碼**，且是**字串旁的 caption 而非字串內的 prefix**，`startswith` 偵測不到。**②** 「caller 用 prefix 偵測」機制**是活的**，但 `ui/tab3_t7_ledger.py` 偵測的是 `("❌", "⚠️")` **失敗訊息契約**，不是「這段是 AI 生的」。**③** 「禁止從 LLM 字串萃取數字」的禁令**沒被刪、是搬到下游** —— 在 `repositories/ai_cache.py` 的 module docstring；本列第 2 格指名的 `services/ai_service.py` **沒有**這句（0 命中）。<br>**⭐ 一併要答的第二題（本組實測，這是本列真正的時效性所在）**：`services/ai_service.py::_gemini` 的 `force_json` → `infra/llm.py` 的 `response_format="json"`（Gemini `responseMimeType` / OpenAI `json_object`，**native JSON mode**）**production 0 caller，只有 `tests/` 在用**。**要不要就地封死或加守衛？** —— 一旦有人打開它，下一步幾乎必然是 `json.loads()`，那就是活的反捏造違憲。<br>⚠️ **嚴重度：目前是文件脫節，不是活的違憲** —— 四個 production 消費者逐一讀過，全部只 `st.markdown` 渲染，**沒有任何一處萃取數字**。⚠️ **但那是靠那四個 caller 剛好都只渲染，不是靠任何機器規則** —— **沒有守衛在守這件事。**⛔ **「沒有萃取」是單組實測 ＋ 總管複驗，且取決於「有沒有漏看」，依 §-2 規則 6 只能當待驗事項，不得被引用為已查證的事實去替 `EX-AI-1` 結案。** | **獨立一組**（§-2 規則 4/5）；**不得**由 2026-09-08 寫下該列更正的架構與文件組承接，**也不得**由同日先指出射程問題的那個平行獨立調查組承接 —— 兩者都會變成自己驗自己 | (1) 有人要打開 `force_json`、或對任何 provider 傳 `response_format="json"`；(2) 有人要在 AI 回傳字串上做**任何解析**（`json.loads` / 正則 / `float()` / `int()` / `split()`）；(3) 有人要引用或擴大 `EX-AI-1`（含把新模組收進它）；(4) `services/ai_service.py` 長出第五個 production 消費者；(5) user 直接點名 |
| **P-POOLCACHE-1**（2026-09-08 登記；起因見 §8.2.A.1 `EX-CRUD-1` 該列的 2026-09-08 前提句登記） | **`repositories/pool_repository.py` 落在兩條例外的縫裡，它該歸哪一條？**<br>**已知事實（本組實測）**：`EX-CRUD-1` 整段豁免推理只靠一句前提 ——「無 `@_ttl_cache` / `@st.cache_data` 裝飾，亦無外部 HTTP I/O ⇒ 不存在『cache 分散』問題」；而該檔**有** `@_st_pool.cache_data(ttl=_TTL_POOL…)`（`TTL_30MIN`）＋ `_clear_pool_cache()`，**且**走 `get_gspread_client(...)` → `open_by_key(...)`（**遠端 Google Sheets API 往返**）。該檔**自己貼的是 `EX-CACHE-1`**（裝飾點上方就地註解），但 **`EX-CACHE-1` 的成員清單沒有它**（該列第 2 格寫「實際適用…**1 fetcher** `repositories/hot_money_repository.py`」）；同時它的 module docstring 又自陳「§8.2 EX-CRUD-1…**無 TTL cache**」—— **同一個檔的兩處自我登記互相矛盾**。<br>**要答的是**：(i) 它該收進 `EX-CACHE-1`、留在 `EX-CRUD-1` 但改寫前提、還是另立一條？(ii) **`EX-CRUD-1` 其餘五個成員**（`policy_repository` / `snapshot_repository` / `ledger_repository` / `batch_checkpoint` / `portfolio_perf_repository`）**是否也帶 cache 裝飾**？—— **本組刻意未查**，因為那是「有沒有漏看」型的問題，且本組正是登記這一列的人（§-2 規則 4：不可自己查自己）。<br>⚠️ **與 `P-UIGSPREAD-1` 有交集但不是同一題**：那一列問「gspread 直呼算不算 EX-CRUD-1 的『本地持久化』」，本列問「一個**帶 cache 裝飾**的成員還算不算 EX-CRUD-1」。**兩題可以各自成立、也可以互相推翻，須分開答。** | **獨立一組**（§-2 規則 4/5）；**不得**由 2026-09-08 登記本縫的架構與文件組承接 | (1) 有任務碰到 `repositories/pool_repository.py` 或 `ui/helpers/fund_grp_health/switch_advisor_section.py`；(2) **有人要引用、擴大或收窄 `EX-CRUD-1` 或 `EX-CACHE-1`（含把新成員收進任一條）**；(3) `P-UIGSPREAD-1` 作出裁決（兩題互相牽動）；(4) user 直接點名 |
| **P-LEDGERNAV-1**（2026-09-08 登記） | **一個未登錄的軟例外：`ui/tab3_t7_ledger.py` 直呼帶 TTL cache 的 L1 `get_latest_nav`，而 L2 facade 早就存在。**<br>**已知事實（本組實測）**：`ui/tab3_t7_ledger.py::render_t7_section` 內 `from fund_fetcher import get_latest_nav as _nav_now`；`fund_fetcher.py` 是根目錄相容 shim，re-export 的是 L1 `repositories/fund/fx_and_main.py::get_latest_nav`，而該函式帶 `@register_cache` ＋ `@_ttl_cache(ttl_sec=TTL_5MIN, maxsize=128)` —— **形狀完全是 `EX-PASSTHRU-1`，但憲法全檔對 `get_latest_nav` 0 命中**，正是 §8.2.A.1 末句明禁的「**未經登錄的軟例外**」。<br>**⛔ 但本組判斷它不該直接補進 `EX-PASSTHRU-1`，理由是實測出來的，不是原則性的**：**L2 facade 已經存在** —— `services/fund_service.py::get_latest_nav`（v19.511），其 docstring 逐字寫著「mirror get_latest_fx R16 `EX-PASSTHRU-1` 升級。**UI(L3)不直呼 L1 fetcher，走本 thin wrapper**（§8.2）」；而 `ui/helpers/portfolio/fee_deduction.py::_make_nav_fx_fn` **已經走 L2**，就地註解「v19.511 稽核:走 L2 facade,不直呼 L1」。⇒ **這不是一個沒被審過的位置，是一次已完成的遷移漏掉的那一個** —— 把它補進 `EX-PASSTHRU-1` ＝ **把 repo 自己已經決定該走 L2 的東西就地合法化**。<br>⭐ **同一個 import 區塊裡兩行相鄰，一行走 L2、一行走 shim**：`from services.fund_service import get_latest_fx as _fx_now` ／ `from fund_fetcher import get_latest_nav as _nav_now`。**為什麼會漏掉這一個**：它走的是**根目錄 shim `fund_fetcher`**，⇒ **任何寫成「`ui/**` 不得 import `repositories.*`」的規則，結構上都攔不到它**。<br>**要答的是**：(i) 該把它 migrate 到既有的 L2 facade（＝完成 v19.511 那次遷移），還是補登 `EX-PASSTHRU-1`？(ii) 若選前者 —— ⛔ **客戶明令不得改動線上運作中的舊分頁程式碼**，故這是**業務規則衝突**，需客戶裁決，不是純技術決定。(iii) 是否該補一條「經由 `fund_fetcher` shim 迂迴直呼 L1」的守衛？<br>⚠️ **本組未查證**：**除這一處外，`ui/**` 是否還有其他經 `fund_fetcher` shim 迂迴直呼 L1 fetcher 的位置** —— 該 shim 有 18 條 `noqa: F401` re-export，逐一清點屬「有沒有漏看」型問題，須另派獨立組。⛔ **故本列只說「這一處」，不宣稱「只有這一處」。** | **獨立一組**（§-2 規則 4/5）；**不得**由 2026-09-08 登記本列的架構與文件組承接 | (1) 有任務碰到 `ui/tab3_t7_ledger.py` 的 T7 帳本試算區塊，或 `services/fund_service.py::get_latest_nav`；(2) **有人要引用或擴大 `EX-PASSTHRU-1`（含把新成員收進它）**；(3) 有人要清點或退役 `fund_fetcher.py` 的 re-export shim（§8.3 F-GRAY-1 已寫明「57 caller 都遷完就該實體刪除」）；(4) 出現 T7 帳本市值與別處對不上的實際回報；(5) user 直接點名 |
| **P-PASSTHRUSCOPE-1**（2026-09-08 登記；缺口本體由 #829 自陳於 `EX-PASSTHRU-1` 該列，本輪依本節格式補上出口） | **`EX-PASSTHRU-1` 的升級觸發條件與其守衛，有兩個已被證實的射程缺口，目前沒有由誰查、沒有觸發點。**<br>**已知事實（#829 實測、本組重跑複驗）**：`tests/test_fund_research_merge.py::test_code_finder_is_the_only_search_entry` —— **(i)** 走訪寫成 `ROOT.glob("ui/**/*.py")`，**`services/` 一個檔都不在其中**（本組實跑同一條 glob：命中 **123** 檔，落在 `services/` 的 **0** 檔；而 `services/fund_search.py` 確實存在、確實呼叫 `tdcc_search_fund`）；**(ii)** 只數 `ast.Call` 且比對 `func` 的名字，因此**函式被當成引數傳給包裝器**的形態數不到（本組以 `ast` 逐節點複刻：`_ex.submit(tdcc_search_fund, kw)` → **0 命中**；對照組 `tdcc_search_fund(kw)` → **1 命中**）。⇒ 它與本列那句「**第二個 UI caller**」**是同一個盲點的兩個副本，不是兩道獨立防線**。<br>**要答的是**：(i) 該把走訪擴到 `services/**`（或全 repo），還是承認這條守衛只管 UI、另立一條管 L2？(ii) `ast.Call` 那一道要不要補「函式被當引數傳遞」的形態？(iii) **列級的升級觸發條件掛著一個成員級的理由**（條件問「UI caller 數」，括號引的 (a) 卻是 `fetch_benchmark_close` 的「1 caller / 1 call site」）—— 這個結構要不要拆開？<br>⛔ **這是「該不該給出口」的登記，不是「守衛擋錯了」** —— 該守衛守的「UI 層恰好一個呼叫點」今天仍然是對的需求，**本列不得被引用來放寬它的任何一條斷言**。<br>⚠️ **本列的立案理由與 #829 替 `GAP-SEARCH-CACHE-1` 立案的理由逐字相同**：本節前言的「**待查證沒有出口 ＝ 實質永久豁免**」。#829 把自己發現的**取數頻率**缺口搬進了本節，卻把自己發現的**守衛射程**缺口留在一格表格裡 —— **不留出口，就是「條件只往外用、不往內用」的第三次**。 | **獨立一組**（§-2 規則 4）；**不得**由 #828 組、**不得**由 #829 實作組或其稽核組、**也不得**由 2026-09-08 補上本出口的架構與文件組承接 | (1) **有人要引用 `EX-PASSTHRU-1` 的升級觸發條件**（含據它主張某成員該／不該上提 L2）；(2) 有任務要動 `tests/test_fund_research_merge.py`；(3) `repositories.fund.tdcc_search_fund` 再長出任何消費者（UI 或 L2 皆算）；(4) 有人要在別處複製這條守衛的走訪寫法；(5) user 直接點名 |

⚠️ **本表全部由單組（2026-08-28 文件更正組）登記，未經第二組獨立複驗**（§-2 規則 6）。
表中「已知事實」欄位為實測，「待答問題」為本組判斷；~~**「本檔沒有第五個待判定項」這句話本組未查證、也不宣稱**~~。
→ **2026-08-28 第三輪稽核更新（有意識的更正，不是漏刪）**：本表已由 4 列增為 **5 列**（新增 `P-UISUBPROC-1`），故原句的「第五個」措辭已失效；
**其語意照舊、且更要緊**——~~**「本檔沒有第六個待判定項」這句話本組同樣未查證、也不宣稱**~~
→ **2026-08-28 Phase 1.2 再更新（狀態變更，不是漏刪）**：本表再增 2 列為 **7 列**（新增 `P-YFDUPE-1` / `P-BENCHTZ-1`，均為本輪搬遷與其稽核過程中發現）。~~**同一句話照樣往下推：「本檔沒有第八個待判定項」本組未查證、也不宣稱。**~~
→ **2026-09-05 DOC-P1 再更新（狀態變更，不是漏刪）**：本表再增 1 列為 **8 列**（新增 `P-WHERECONTENT-1`）—— 故上句的「第八個」措辭已失效，**其語意照舊、且更要緊**：**同一句話照樣往下推：「本檔沒有第九個待判定項」本組未查證、也不宣稱。**
→ **2026-09-05 Track A 批次三-B 再更新（狀態變更，不是漏刪）**：本表再增 1 列為 **9 列**（新增 `P-GREYSCENARIO-1`）—— 故上句的「第九個」措辭已失效，**其語意照舊**：~~**同一句話照樣往下推：「本檔沒有第十個待判定項」本組未查證、也不宣稱。**~~
→ **2026-09-07 F27 文件與治理組再更新（狀態變更，不是漏刪）**：本表再增 **7 列**為 **16 列**（新增 `P-SINKGRAIN-1` / `P-RETRYKIND-1` / `P-WSTOREWRITE-1` / `P-CALLSTATIC-1` / `P-AIKEYCI-1` / `P-CONCURGUARD-1` / `P-GRIDPRESENCE-1`）—— 故上句的「第十個」措辭已失效，**其語意照舊**：~~**同一句話照樣往下推：「本檔沒有第十七個待判定項」本組未查證、也不宣稱。**~~
→ **2026-09-08 架構與文件組再更新（狀態變更，不是漏刪）**：本表再增 **1 列**為 **17 列**（新增 `GAP-SEARCH-CACHE-1`）—— 故上句的「第十七個」措辭已失效，**其語意照舊**：**同一句話照樣往下推：「本檔沒有第十八個待判定項」本組未查證、也不宣稱。**
→ **2026-09-08 架構與文件組（同日第二輪）再更新（狀態變更，不是漏刪）**：本表再增 **4 列**為 **21 列**（新增 `P-AIFLAG-1` / `P-POOLCACHE-1` / `P-LEDGERNAV-1` / `P-PASSTHRUSCOPE-1`）—— 故上句的「第十八個」措辭已失效，**其語意照舊**：**同一句話照樣往下推：「本檔沒有第二十二個待判定項」本組未查證、也不宣稱。**
⚠️ **這四列是怎麼被找到的 —— 三列是「憲法自己在說謊」，一列是「已經自陳卻沒有出口」，都不是掃描找到的**：`P-AIFLAG-1` / `P-POOLCACHE-1` / `P-LEDGERNAV-1` 來自一次**專門去核對憲法既有記載是否仍為真**的派工（總管先自行實跑、把指令與輸出寫進派工單，執行組再逐條重跑）——⚠️ **而重跑當場推翻了派工單自己的一條子宣稱**：派工單寫「`EX-AI-1` 三道緩解措施**全部不存在**」，實測是**其中兩道還活著、只是搬了家**（禁令在 `repositories/ai_cache.py`、prefix 偵測改吃失敗契約）；成因是**那條指令只掃了 `services/ai_service.py` 一個檔，卻拿它去支撐一句全 repo 的結論** —— **§-1.5.1c 判定 2 記載的同一個病（字表／射程選錯，結論必錯），這次犯在派工單上。**（該子宣稱由平行的獨立調查組指出、總管複驗後撤回並改寫，執行組依 **§-2.A 第 7 款**「別人指出你這裡錯了，寫進永久記錄前同樣要查證」**逐條重跑**才落筆 —— **這一次「別人」是總管，同樣不照收。**）`P-PASSTHRUSCOPE-1` 則是**上一輪自己把缺口寫進表格、卻沒給出口**，由本輪補上 —— 與 `P-UISUBPROC-1`（2026-08-28）**完全同型**。⇒ **本表至此已有五種來源機制**：稽核推翻全稱句、稽核逐行讀出、實作組自陳、稽核連續打穿守衛，以及**本輪這一種：回頭核對憲法自己的記載**。⚠️ **第五種尤其值得記**：這三列指向的東西**在憲法裡已經寫了很久**，`tests/test_constitution_file_refs.py` 也**一直是綠的** —— 因為它只驗「檔案在不在、行號界不界內」，**不驗那一行是不是它自稱的東西**（該守衛自己的 docstring「缺口 2」已寫明）。**綠燈從來不代表憲法說的是真的。**
⚠️ **`GAP-SEARCH-CACHE-1` 是怎麼被找到的 —— 與 `P-WHERECONTENT-1` 同一種機制（第三種）**：它同樣是**實作組在自己的 PR 裡主動揭露、並具名寫進原始碼**的缺口，同樣**沒有由誰查、沒有觸發點**，由本輪在做一件**別的事**（把該 PR 新增的 L2 消費者登記進 §8.2.A.1 `EX-PASSTHRU-1`）時順帶撿到。⚠️ **本組沒有掃過還有沒有第二個同型的缺口**；也**沒有**回頭查該 PR 其他檔案裡是否另有具名登記。⚠️ 依 `P-WHERECONTENT-1` 那一列記載的教訓（「**照抄一份自我揭露，等於把它的錯一起搬進憲法**」），本組**沒有照抄** `services/fund_search.py` 的原文：上表「已知事實」五條與「呼叫頻率」兩條，**本組逐條回到原始碼重讀後才寫下**（`_tdcc_cache` 的宣告與寫入點、`_tdcc_get` 的 `except` 位置、FundClear 分支的 `timeout` 與 `except`、`ui/helpers/fund_research/code_finder.py` 的 `if do_search` 閘門、`ui/views/page_03_research.py` 的 `_applied_query()` 閘門與 `search_funds()` 呼叫點、`app.py` 的 `st.tabs`）。**逐條重讀的結果與該檔自陳一致，本輪未發現需要更正的數字或機制** —— ⚠️ 但這句話只涵蓋**本組重讀過的那幾條**，不涵蓋該檔 docstring 的其餘敘述。
⚠️ **`P-GREYSCENARIO-1` 是怎麼被找到的 —— 第四種機制，前三種都不是**：它是**獨立稽核連續三輪打穿同一份守衛**的產物。前兩輪打穿的是**粒度**（整頁 → 逐塊 → 逐小節 → 逐卡，每一輪實作組都補得上）；**第三輪打穿的是情境**，而那一次補不上了 —— 補粒度對它完全無效（**把一個沒被渲染的畫面切一千段，還是零段**）。**總管據此裁決：粒度到此為止，情境與語意兩半停止再補、改為登記。**⚠️ **這一列因此不是「還沒做的事」，是「這條路線的已知上限」** ——引用它之前請先讀懂這個差別：它不會被「再補一層斷言」解決。
⚠️ **這兩列是怎麼被找到的，同樣值得記一筆**：`P-YFDUPE-1` 是**獨立稽核推翻本組一句全稱句**（「本 repo 唯一的 Yahoo 取數實作」）時撿出來的；`P-BENCHTZ-1` 是稽核逐行讀新程式碼時發現的埋雷。**兩件都不是本組自己掃到的** —— 再一次印證上一段那句：本表的完整性靠「有人回頭讀」，不靠掃描。
⚠️ **`P-UISUBPROC-1` 是怎麼被找到的，值得記一筆**：它不是靠掃描找到的，而是**上一輪自己點名在 C 類、卻沒有給出口**，由第三輪稽核逐句回讀時撿回來。
⚠️ **`P-WHERECONTENT-1` 是怎麼被找到的 —— 這是本表的第三種機制，與上面兩種都不同**：它既不是靠掃描找到的，也不是靠稽核回讀撿回來的 —— 它是 **#777 實作組在自己的 PR 裡主動揭露「我只守到一半」**，並且**自己指出「登記在測試檔裡是錯的地方」**、要求把它搬進本節。**這是本表目前唯一一列由「產生該缺口的那一組自己送進來」的。**⚠️ **但它不推翻下面那句結論，反而替它補了一個限制條件**：本組搬運時逐項重跑，發現該登記的**兩個數字都不準**（見該列左欄 (a)(b)）—— **自我揭露解決的是「有沒有出口」，不保證「寫下的內容是對的」**。**照抄一份自我揭露，等於把它的錯一起搬進憲法。**
⚠️ **2026-09-07 新增七列是怎麼被找到的，以及本組驗到哪裡（§-2 規則 6，據實揭露）**：七列**全部**是同一天多輪實作與獨立稽核**在各自的 PR／檔案裡自陳的守衛缺口**，由總管彙整、本組轉登記 —— **不是本組掃出來的**，本組也**沒有**回頭掃有沒有第八個。<br>**本組已就地實測（讀原始碼／AST／實跑小片段）**：`P-SINKGRAIN-1`（`_sink_targets` 走訪範圍、`offer_download` 的分支與硬編值、`DROPPED_FOR_ZERO_WRITE` 註記）、`P-RETRYKIND-1`（三處分類與 `GSPREAD_RETRYABLE_KINDS`、docstring 原文）、`P-WSTOREWRITE-1`（`load_active` → `_gs_get_worksheet` → `add_worksheet`／`update` 整條、三個 getter 的呼叫）、`P-CALLSTATIC-1`（`_calls()` 只走 `ast.Call`）、`P-AIKEYCI-1`（該檔就地註記原文）、`P-CONCURGUARD-1`（**逗號實測**：`_TOKEN_RE` 無逗號 token、`format('{0}', github.sha)` 於 pos 12 拋；`_concurrency()` 的 `isinstance(cc, dict)` 斷言）、`P-GRIDPRESENCE-1`（兩條契約的量測對象、`page_02_health` 零 `st.columns` 呼叫、`_LAYOUT_TOOLKIT` 是豁免不是斷言）。<br>⛔ **本組未實跑任何 pytest**（本環境無 pytest，且未 `pip install`）—— 故 `P-CALLSTATIC-1` 的「兩顆突變在行為層皆轉紅」與 `P-GRIDPRESENCE-1` 的「刪掉 `render_cards` 仍全綠」**只是轉述來源輪的回報**，已於各該列就地標明。**七列皆單組登記，未經第二組獨立複驗。**
**這代表本表的完整性靠的是「有人回頭讀」，不是任何一次掃描** —— 讀者請據此打折信任本表的窮舉性。

---

### ⭐ 本篇搬入之後**新增**的待判定項（2026-09-08 拆檔輪，**不是搬進來的**）

> ⛔ **刻意與上面那張表分開放，不合併計數。** 上表 21 列是 2026-09-08 拆檔前就在
> `CLAUDE.md` 裡的，**逐位元組搬過來、一列都沒動**；下面這一列是**拆檔這一輪才登記的**。
> 把兩批混在一起數，會讓「上表是純搬移」這個可自驗的事實變成不可驗
> —— 那正是 `CLAUDE.md §-2.A` 開頭「**不要把兩批合併計數**」講的同一件事。
>
> ~~**計數**：上表 **21 列** ＋ 本段 **3 列** ＝ 本篇目前共 **24 列**。~~
> → **2026-09-08 #833 就地更正（有意識的更正，不是漏刪；由第五輪獨立稽核抓出）：**
> ~~**計數**：上表 **21 列** ＋ 本段 **16 列** ＝ 本篇目前共 **37 列**。~~
> → **2026-09-09 #835 第四輪就地更正（有意識的更正，不是漏刪）：新增 `P-HEDGESEMANTIC-1`。**
> ~~**計數**：上表 **21 列** ＋ 本段 **17 列** ＝ 本篇目前共 **38 列**。~~
> → **2026-09-09 #835 第五輪就地更正（有意識的更正，不是漏刪）：新增 `P-GATEDLAYOUT-1`**
>   （**一列裝三筆同族**：手動補資料標題消失 ＋ `_where()` 前綴指向舊分頁 ＋ ⚠️D）。
> ~~**計數**：上表 **21 列** ＋ 本段 **18 列** ＝ 本篇目前共 **39 列**。~~
> → **2026-09-09 ⑥ 那一批就地更正（有意識的更正，不是漏刪）：新增 2 列**
> 　（`P-P02JARGON-1` ／ `P-P02DELEGJARGON-1`，兩者都在 ⑥ 的畫面上、但**分屬不同的檔案邊界**，故**刻意不合成一列** —— 前者改得動、只是不在本批清單裡，後者住在被委派的舊模組裡、本批**動不了**。合成一列會讓「誰能修」這件事消失）。
> **計數**：上表 **21 列** ＋ 本段 **20 列** ＝ 本篇目前共 **41 列**。
> （本段 14 ＝ 拆檔輪 3
> ＋ **#834 3**〔`P-SECLABEL-1` / `P-TILEARGS-1` / `P-FETCHPOINTER-1`，2026-09-08 隨 #834 併入 main〕
> ＋ #833 第三輪 3〔`P-CNHALIAS-1` / `P-NAVSERIES-1` / `P-ADRUNKNOWN-1`〕
> ＋ #833 第五輪 2〔`P-ALIASSHAPE-1` / `P-STALEDIV-1`〕
> ＋ #833 第六輪 3〔`P-COUNTGUARD-1` / `P-MIXEDROWDATE-1` / `P-SAMESOURCE-1`〕
> ＋ **#835 2**〔`P-P05VERDICT-1`，2026-09-08 ⑦ 結論層那一批；
> `P-FLOORSLACK-1`，2026-09-08 第二輪稽核抓出的**既有債**，⛔ 本批未碰〕。）
>
> ⚠️ **這個計數與 #834 的立場不一致，據實寫明，不要當成沒發生**：
> #834 在下方那則註記裡寫「**本輪刻意不宣告『現在共 N 列』**——
> 每加一列就要回頭改一次的計數，本身就是下一個會過期的東西」。**那個顧慮完全成立。**
> **但本分支選擇繼續宣告**，理由是 #833 第六輪加了
> `test_the_two_p_tables_declared_counts_match_what_is_actually_there`：
> **它會在宣告值與機器實數不符時轉紅** —— 也就是「回頭改一次」這件事**不再靠人記得**。
> ⚠️ **然而那道守衛是 fail-open 的**（見 `P-COUNTGUARD-1`）——
> **它擋得住「數字忘了改」，擋不住「塞進一列它認不出來的」。**
> ⇒ **兩邊的理由都成立，本檔並陳，留給後人決定要不要收斂成一種。**
> ⚠️ **`P-COUNTGUARD-1` 就是在講「數這張表的那道守衛是 fail-open」** ——
> **本行的計數由那道守衛驗，而那道守衛擋不住格式略異的列。** 兩件事一起讀。
>
> ⛔ **我做錯了什麼，寫清楚**：#833 第三輪把三列（`P-CNHALIAS-1` / `P-NAVSERIES-1` /
> `P-ADRUNKNOWN-1`）**加進了上面那張表** —— 也就是**自稱「逐位元組搬過來、一列都沒動」的那一張**。
> 於是「上表是純搬移」這個**可自驗的事實**當場變成假的，而**下面這句計數一個字都沒改**，
> 變成 `21 + 3 = 24`、實際 `24 + 3 = 27`。**沒有任何守衛在數這張表，所以 CI 綠是預期的。**
> ⚠️ **而這一段自己寫的理由，正是我踩到的那件事**：「把兩批混在一起數，會讓『上表是純搬移』
> 這個可自驗的事實變成不可驗。」**寫在正上方三行，我還是踩了。**
>
> ✅ **修法：把那三列搬到本段（它們本來就該在這裡 —— 本段的定義就是「本篇搬入之後新增的」）**，
> 上表**還原成與 `origin/main` 逐位元組相同**。
> **⇒「上表是純搬移」重新變回可自驗**，驗法不變：
> `diff <(git show origin/main:EXCEPTIONS.md) EXCEPTIONS.md` **在上表區間內應為空**。
> ⛔ **刻意不選「只把 24 改成 27」** —— 那只修好數字，沒有修好
> **「上表可不可以被一句 `diff` 驗完」**這個真正承重的性質。
>
> 📌 **被搬下來的那則註記，內容保留在這裡（原文寫在上表，隨那三列一起搬過來）** ——
> 依本 repo「舊條文保留不刪」的慣例，它記載的事故不因換位置而消失：
> 「⚠️ **這三列一度被登記錯地方**：#833 回修組先寫進了 `CLAUDE.md` 的 §8.3.P，
> 因為它**只看了自己那條分支**（fork 自 `ba3b06d`，早於 #831 拆檔）就下了
> 『`EXCEPTIONS.md` 在本 repo 不存在』這個結論 —— **與它同一輪正在更正的 R2**
> （『這個沙箱沒有 pytest』，實際是只查了一個直譯器）**是同一個失效模式：
> 查了一個地方，寫出一句關於全體的話**。該誤登造成 `CLAUDE.md` 與 main 的合併衝突
> （`f7c4c17` 對 main 是 0 衝突區塊、`d54764c` 變成 1），已於併入 main 時把
> `CLAUDE.md` 還原成 main 版、三列改置於本檔正確位置。」
> ⚠️ **而第五輪抓到的是它的下一段**：那一輪把「放到 `EXCEPTIONS.md`」做對了，
> **卻放進了錯的那一張表** —— **同一件事連錯兩層，第二層四輪都沒有人看到。**
>
> ~~⚠️ 照上表末尾的既有句式往下推：**「本篇沒有第二十五個待判定項」本組未查證、也不宣稱。**~~
> → **2026-09-08 就地更正（計數同步，不是漏刪）**：
> ~~⚠️ 照上表末尾的既有句式往下推：**「本篇沒有第三十個待判定項」本組未查證、也不宣稱。**~~
> → **2026-09-08 第六輪就地更正（計數同步，不是漏刪）**：
> ~~⚠️ 照上表末尾的既有句式往下推：**「本篇沒有第三十三個待判定項」本組未查證、也不宣稱。**~~
> → **2026-09-08 併入 #834 後同步（計數同步，不是漏刪）**：
> ⚠️ 照上表末尾的既有句式往下推：**「本篇沒有第三十六個待判定項」本組未查證、也不宣稱。**

| ID | 待答問題 | 由誰查（§-2 規則 4：獨立分派，不可自己查自己） | 觸發點（任一命中即應派工） |
|---|---|---|---|
| **P-PHANTOMSEC-1**（2026-09-08 拆檔輪登記） | **`§8.2.A.1` 是一個被大量引用、但從來沒有被定義過的節號。**<br>**已知事實（本組實測，量測日 2026-09-08，於 `ba3b06d` 的 `CLAUDE.md`）**：<br>　• **定義數 ＝ `0`** ——`grep -cE '^#{1,4}.*8\.2\.A\.1\|^\*\*8\.2\.A\.1' CLAUDE.md` → **0**。全檔沒有任何標題或粗體行宣告過這一節。<br>　• **引用數 ＝ 19 或 22，看你怎麼數**：`grep -c` → **19**（＝**含有它的行數**）；`grep -o … \| wc -l` → **22**（＝**出現次數**）。<br>　⚠️ **本輪把這個差異查清楚了，不是「兩個數字都寫、不知道哪個對」** —— **兩個都對，只是在數不同的東西**（有 3 行各出現兩次）。**承重的不是 19 也不是 22，是那個 `0`。**<br>　• 程式碼側另有 **10 筆**寫 `CLAUDE.md §8.2.A.1` 的註解（實測，2026-09-08；6 檔：`ui/helpers/portfolio/policy_admin_section.py` ×2、`ui/tab6_manual.py`、`tests/test_dual_track_p05_gates.py`、`tests/test_wf02_health_golive.py`、`tests/test_fund_search_facade.py` ×2、`tests/test_wf03_research_skeleton.py` ×3），也就是**這個幽靈節號已經外溢到 repo 裡**。<br>**它實際被當成什麼在用**：從上下文看，`§8.2.A.1` 被用來指「**例外表本身 ＋ 表後那段驗證指令與逐成員實測**」（例如「`§8.2.A.1` 驗證段 ④」確實指得到本篇 `8.2.A` 表後 `> 📌` 那個 blockquote 的第 ④ 項）。**所以它不是打錯字，是一個從未正式命名的區塊被反覆引用。**<br>**要答的是**：（a）補一個 `8.2.A.1` 的正式定義（把「驗證段」正名），（b）把 19/22 筆引用改指 `8.2.A`，還是（c）承認它是既成慣例、就地寫明它等於什麼？<br>⛔ **本輪刻意不順手補定義。** 補定義會**改動搬進來的那兩塊**，而本輪自己宣稱「逐位元組搬移、內容零修改」——**一邊宣稱純搬移、一邊順手改內容，那個宣稱就作廢了**。且依 §-1 本輪無此觸發。<br>⚠️ **同類但**不同**的一筆，順帶記明以免混淆**：`§8.2.A.0` **同樣在 `CLAUDE.md` 裡 0 定義**（引用 16 次），但**那一筆是已知且已就地揭露的** —— 檔內多處明寫「**本檔無 §8.2.A.0 一節**，該規則出自姊妹 repo `my-stock-dashboard`」。**`8.2.A.0` 是有揭露的缺席，`8.2.A.1` 是沒有揭露的缺席** —— 本列登記的是後者。<br>⚠️ **本列數字為本組單組實測，未經第二組獨立驗證**（§-2 規則 6）；「**全 repo 沒有第三個這種幽靈節號**」本組**沒有查證、也不宣稱**。 | **獨立一組**（§-2 規則 4）；**不得**由 2026-09-08 拆檔輪（架構與文件組）承接 —— 本列由本組登記，自己查自己不算第二雙眼睛。 | (1) 有任務要**新增或改寫**本篇 `8.2.A` 的任一列（屆時「這一節到底叫什麼」會當場擋路）；(2) 有人要**擴大或收窄**「驗證段」那組逐成員驗證條件；(3) 程式碼側再出現第 11 筆 `§8.2.A.1` 註解；(4) 有人要對 `§8.2.A.0` 做同類處置（兩者應一起決定，不要只修被點名的那一個 —— 本篇 `8.2.A` 驗證段 ④ 的既有教訓）；(5) **user 直接點名**。 |
| **P-FROZENTABREF-1**（2026-09-08 拆檔輪登記；起因見本篇「程式碼側的『回指註解』現況」一節） | **舊分頁被凍結，於是它們裡面的文件指標會永久過期，而且沒有出口。**<br>**已知事實（本組實測，量測日 2026-09-08）**：客戶紅線「**絕對禁止直接覆寫、修改或破壞現有線上正常運作的舊版 Tab 代碼**」使 `ui/tab*.py`（**16 個檔**，sha256 逐檔比對確認本輪零改動）**不得修改**。其中 **3 個檔**各有一行註解仍指向 `CLAUDE.md §8.2.A` / `§8.2.A.1`（`tab2_single_fund.py` / `tab5_data_guard.py` / `tab6_manual.py`），而那兩節的內容**已經搬到本篇**。<br>⚠️ **它們不是死連結**：`CLAUDE.md` 原位置刻意留了指標段、節號也沒改，照舊寫法找過來的人會被轉過來 —— **代價是多繞一趟，不是找不到。**<br>📌 **順帶查了但沒問題的一批（2026-09-08 第二輪稽核指出，本組重測）**：另有數個凍結分頁**只寫裸 `EX-*` / `P-*` ID、不帶 `CLAUDE.md` 前綴**（本組實測 **6 檔**：`ui/tab2_single_fund.py` ×2、`ui/tab3_portfolio.py`、`ui/tab6_manual.py`、`ui/tab_batch_analysis.py`、`ui/tab_fund_grp_health.py`、`ui/tab_settings_diag.py`）——**裸 ID 搜 ID 就會落在本篇，完全不受搬檔影響，不必也不該動。**<br>⚠️ **與稽核報的「7 檔」有一處差異，據實寫明**：稽核把 `ui/tab5_data_guard.py:1511` 也算進來，但本組實測**那一行帶 `CLAUDE.md` 前綴**（它已經列在上面那張三筆過期表裡），依「裸 ID ＝ 不帶檔名前綴」的定義**不算裸 ID**。**兩邊都不算錯，是分類口徑不同；承重的是機制（裸 ID 天生跨檔安全），不是 6 還是 7。**<br>**要答的是**：（a）等舊分頁退場時一併更新（目前的預設）；（b）另立一個「凍結檔的文件指標」例外機制，讓這類純註解更新可以被批准；還是（c）承認凍結檔的文件指標本來就會過期、改由母檔的指標段負責轉址（＝現況，只是把它寫成明文政策）？<br>⛔ **本輪不裁決，只登記 —— 但這一列存在的意義就是給它一個出口。** 依本節前言：**待查證沒有出口 ＝ 實質永久豁免**；一個「因為不能改所以永遠不改」的過期指標，正是那個形狀。<br>⚠️ **一個相鄰、且**尚未**由客戶明示的判斷**：本輪把 `ui/helpers/**` / `ui/components/**` / `ui/views/**` 判定為**不在紅線內**（理由：紅線寫的是「舊版 **Tab** 代碼」，且 helper 是新舊共用的基礎設施，凍結它會讓新頁也拿到過期指標）。**那是 AI 總管的判斷，不是客戶明示**（§-2 規則 6），**一併列在這裡待客戶覆核**。<br>⚠️ **本列為單組實測與判斷，未經第二組獨立驗證**；「**舊分頁裡只有這 3 行過期指標**」是取決於「有沒有漏看」的全稱句，**本組不宣稱**（本輪只掃了 `CLAUDE.md §8.2.A` / `§8.3.P` 這一組字樣，**沒有掃其他章節的指標**）。 | **獨立一組**（§-2 規則 4）；**不得**由 2026-09-08 拆檔輪（架構與文件組）承接 —— 那條紅線就是本組踩到的，自己查自己不算第二雙眼睛。 | (1) **舊分頁退場或被客戶解除凍結**（屆時順手改掉，屬同一次任務的收尾義務）；(2) 有人要**擴大或收窄**「舊版 Tab 代碼」這條紅線的射程（含 helper 算不算）；(3) 憲法再發生一次**搬節**，使凍結檔內的指標又多一筆過期；(4) 有人主張「純註解不算修改」而要動任一 `ui/tab*.py`；(5) **user／客戶直接點名**。
| **P-SPLITSLACK-1**（2026-09-08 登記，**同日經第三輪稽核大幅改寫**） | **數量下限量的是「引用筆數」，我們怕的是「內容被搬走」，兩者不成比例。**<br>**已知事實（本組實測，量測日 2026-09-08）**：<br>　• `CLAUDE.md` 的 **`§-1.5` 整節 ＝ 全檔 49.6%**（客戶三份逐字頒布原文 v1/v2/v3 ＋ 兩則裁決 ＋ 七個判定），**卻只含 28 筆活的 Tier-1 引用** ⇒ 整節剪進一個沒標記、沒登記的檔，**三道數量下限一個都不會動**（改版前實測 20 passed 全綠）。<br>　• 以當時的下限，**最大可靜默搬走的連續區塊是 `CLAUDE.md` 的 56.9%**。<br>　• ⚠️ **守衛自己寫的補救建議「把下限提高到現值的 75~80%」是一句放寬指令** —— `CLAUDE.md` 現值 185、現行下限 150，而 185×0.75 ＝ **138 比現行下限還低**；照著做 → 最大可搬走區塊由 **56.9% 變成 73.8%**（本組實測，與稽核量到的 73.5% 一致）。**已改為 `max(現行下限, 現值×0.8)`「只升不降」。**<br>　• ⚠️ **餘裕可以被「製造」**：加一段「現行實作路徑」索引（全是真檔案）就會讓活引用變多、餘裕變大 —— **而加索引正是真正的拆檔 PR 會做的事**（本 PR 自己就加了數段，`EXCEPTIONS.md` 的活引用因此一路 242 → 251 → 259）。**搬走半部憲法之後，這個檔可以看起來比之前更安全。**<br>**本輪已做的處置（不是全解）**：新增兩條**內容清單**主防線 —— `test_declared_sections_all_present`（23 個章節名）與 `test_declared_registry_ids_all_present`（32 個 `EX-*`/`P-*`/`GAP-*` 列 ID）。**兩者都是比對名單，不是門檻，不會因為檔案長大而失效。**<br>**2026-09-08 第四輪稽核之後又補的兩條**：<br>　• **位元組下限 ＋ 同一個 ratchet**（`_MIN_BYTES_PER_FILE`，只升不降）——因為 (a) 被證明**不是規避形狀，而是本 PR 自己建立的慣例**：稽核把本 PR 寫進 `CLAUDE.md` 的指標段句子**逐字拿去、只改日期與檔名**，保留全部 10 個 `§-1.5*` 標題、內文搬進未登記的檔 ⇒ **139,089 bytes（48.9%）消失、23 passed 全綠**；對照組（只少留一個標題）→ **紅**。**紅與隱形之間只差一行。**<br>　⚠️ **本組原本反對用位元組數（理由：會隨檔案長大而失效），該理由已不成立** ——那個問題被本檔三段之前的 `_PER_FILE_FLOOR_MAX_SLACK` 解掉了。**反對的理由被自己的程式碼推翻。**<br>　• **登記簿 ID 改判「必須是該格的開頭 token」**（見下方缺口 (d)）。<br>**仍然沒解的**：<br>　（a-殘）**掏空到位元組下限以上仍可行** —— 位元組下限有餘裕（現值×0.8），餘裕之內的掏空看不到；ratchet 只固定上界、不讓它變成 0。<br>　（b）**沒有登記在清單裡的散文**（例如某一節新寫的長篇論證）仍可被靜默搬走。<br>　（c）數量下限的餘裕仍然存在，只是不再是唯一防線。<br>　**（d）⭐ 登記簿 ID 檢查驗的是「ID 出現在某個第一格」，不是「它的定義列還在」**（**2026-09-08 第四輪稽核新發現，本組先前未登記**）。滿足它有兩種方式：**(i) 指標列點名該 ID**；**(ii) 順帶的交叉引用**。**實測：32 個 ID 裡有 6 個處於別名狀態**（`EX-AI-1` / `EX-CRUD-1` / `EX-PASSTHRU-1` / `EX-UICACHE-1` / `P-AIKEYCI-1` / `P-WSTOREWRITE-1`），它們的定義列**合計約佔 `EXCEPTIONS.md` 三分之一**（含 `EX-PASSTHRU-1` 一列約 31 KB）。⚠️ **而且本 PR 自己把別名數從 3 提到 6** —— 靠它新增的那張指代宣告表。<br>　　**本輪已堵住 (ii)**：改判「ID 必須是該格的**開頭 token**」，32/32 仍滿足（drop-in、零誤報）。**(i) 仍未堵** —— 一列刻意寫成 `\| **EX-CRUD-1** —— 已搬至 X \|` 的指標列照樣是開頭 token。⛔ **(i) 這一半到今天仍然是開的,而且本組實跑確認過**:稽核的 `mut_d.py` 把 `EX-PASSTHRU-1` 的**定義列(約 31 KB)**整列搬出去、只留一列點名該 ID 的指標列 → **仍然全綠**。<br>　　⚠️ **但它已經被位元組下限「封頂」**:那一刀是 **30,894 B**,而當時距離位元組下限只剩約 **1.7 KB** ——**再多切一點就會紅**。**(i) 沒有被堵住,但它的可用額度已經被限制在位元組餘裕之內。**<br>　　⚠️ **(b) 同樣仍開**:稽核的 `mut_b.py` 把最大的一段「無標題、無 ID」純散文(**18,284 B ＝ `CLAUDE.md` 的 6.4%**)搬進未登記的檔 → **仍然全綠**。**同樣受位元組餘裕封頂。**<br>　　→ **現行的殘餘攻擊面 ＝ 位元組下限的餘裕(現值×0.2)**,而 ratchet 讓那個餘裕**不會隨檔案長大而放大**。**這是「有界」,不是「關閉」。**<br>　⚠️ **一個「靠形狀判斷」的修法，稽核自己試過並失敗了，記在這裡免得有人重新發明**：規則「>50 KB 且含 `§` 標題的未登記 `.md` 就紅」**抓不到 (a)** ——因為**掏空正好把標題留在原地**，被搬走的那一大塊**反而沒有標題**。**⇒ 形狀判斷在這裡結構上行不通：搬走的東西長得最不像憲法。**<br>⛔ **不得從全綠推論出「憲法不會被靜默搬走」** —— 這道守衛已被三組獨立稽核分別繞過 **5 次、1 次、7 次**，每一次都在前一組「全部轉紅」之後。<br>⚠️ **本列刻意不寫死任何餘裕數字**（**2026-09-08 更正**：舊版寫「`CLAUDE.md` 35、`EXCEPTIONS.md` 52、合計 87」——**`52` 在本 PR 任何一顆 commit 上都不曾為真**，實測各 commit 為 34/42、34/51、35/59。**記載漂移這個病，發生在為了記錄這個病而開的這一列裡。**）**要現值請現場量**：<br>　`python3 -c "import importlib.util as u,pathlib; s=u.spec_from_file_location('g','tests/test_constitution_file_refs.py'); g=u.module_from_spec(s); s.loader.exec_module(g); print({f:(sum(1 for r in g.parse_references(pathlib.Path(f).read_text('utf-8')) if not r.struck and r.is_tier1), lo) for f,lo in g._MIN_LIVE_TIER1_PER_FILE.items()})"`<br>**要答的是**：（a）把「內容形狀」的清單再往下延伸（每節的關鍵小節／逐字封存區塊也上清單）；（b）改成內容雜湊台帳（每節 hash，變動須具名）；還是（c）承認靜態守衛到此為止，改以「拆檔一律走 PR ＋ 換一組稽核」的流程手段補？<br>⚠️ **本列為單組實測，未經第二組驗證**；「**沒有第八種繞過**」本組**不宣稱** —— 前七組全部是稽核想出來的，**本輪只是重跑並修好**。 | **獨立一組**（§-2 規則 4）；**不得**由 2026-09-08 拆檔輪（架構與文件組）承接，**也不得**由提出這七組的第二／第三輪稽核組承接。 | (1) 有人要**再拆一次憲法**；(2) 有人要**調整任一下限或 ratchet 帶寬**；(3) ratchet 第一次紅燈；(4) 出現第八組繞過；(5) 有人要**新增頂層章節或登記簿列**（屆時要同步兩份清單）；(6) **user／客戶直接點名**。
| **P-SECLABEL-1**（2026-09-08 由 #834 第二輪獨立稽核抓出，#834 實作組依總管指示登記；⚠️ **數字是實作組自己重量的，不是照抄稽核**） | `ui/helpers/story_nav.py::_SECTION_LABELS` 的漂移鎖 `tests/test_story_nav.py::test_section_labels_match_merged_pages` 走的是 **`_want in _src`（子字串）**，所以把 SSOT 的字**截短**成一個仍然出現在目的地檔案裡的前綴，**照樣全綠** —— 而截短的指路會把使用者送去找一個畫面上不存在的標題。<br>**待答**：這 13 個 key 的鎖要不要一律升級成**逐字相等**？還是只升級承重的那幾個、其餘明文接受「只鎖存在、不鎖完整」？<br>**已知事實（本組實測，量測日 2026-09-08，方法見下）**：**本批修完後 7 / 13 個 key 可以被無聲截短** —— `manage` / `diag` / `nav_status` / `nav_manual` / `switch` / `pf_add` / `pf_perf`。<br>⚠️ **兩個數字都要記，不要只記結果**：**修之前是 8 / 13**（上列七個 ＋ **`pf_ledger`**）。本批把 `pf_ledger` 變成「去哪填本金」的唯一答案之後，它成為**承重** key，故在 `tests/test_wf04_add_holdings.py::test_the_principal_pointer_is_not_a_dead_end` 裡把它鎖成**逐字相等**（突變 N4 實測：截短成「💼 持倉戰情」→ **轉紅**）。**8 → 7 是本批唯一動到的那一格**，其餘六個一字未動。另 6 個會被抓到：`fund` / `batch` / `manual`（`test_story_nav.py::`test_where_to_find_carries_the_owning_tab_and_ordinal` 對整串做相等比對）、`pf_load`（`tests/test_wf04_add_holdings.py::test_the_load_pointer_is_not_a_dead_end`）、`pf_policy_admin`（同檔 `test_the_policy_admin_pointer_is_not_a_dead_end`，#834 本批新增）、`pf_ledger`（同檔 `test_the_principal_pointer_is_not_a_dead_end`，#834 第二輪新增）。<br>⚠️ ~~**本列的數字與稽核轉述的「12/13」不同**~~ → **2026-09-08 第三輪更新：`12/13` 已撤，兩組獨立量到同一個數**（#834 實作組與**第三輪**獨立稽核各自逐 key 實跑，皆得 **7/13**，且 SILENT 的名單逐字相同：`manage`／`diag`／`nav_status`／`nav_manual`／`switch`／`pf_add`／`pf_perf`）。第二輪的 `12/13` 出自**推論**（自陳只實測 4 個、其餘 6 個依同一條 `_want in _src` 推），而推論漏掉了 `test_where_to_find_carries_the_owning_tab_and_ordinal` 這條**逐字相等**的既有斷言。⚠️ **`7` 仍是上界、不是 CI 的真數**：兩組都有 7 個候選檔在本地收集不了（AppTest ×2、需 pandas ×1、需 numpy ×1、其餘 module 級失敗），**CI 的真數 ≤ 7，兩組都給不出更硬的數**。**本組逐 key 實跑 13 次**（每次在 repo 副本裡把該 key 的字截成前綴、跑完 23 個提到 `_SECTION_LABELS` / `section_label` / `where_to_find` 的測試檔，比對 FAILED **id 集合**相對基線有沒有增加）。<br>⚠️ **本組第一次跑出來的是「13/13 全部無聲」，那是假的** —— harness 少了 `--continue-on-collection-errors`，7 個檔在收集階段就 abort、`returncode=2`、**一條測試都沒跑**，而正則從空輸出裡找不到 FAILED 就判成「沒抓到」。**已加一道正對照**（基線若跑不到 100 條就 abort）並重跑，才得到 8/13。**這一筆刻意寫進來**：本表最容易出現的假數字，就是這種「harness 壞了、看起來像結論」的。<br>⚠️ **本量測的射程限制（誠實揭露，不是免責）**：沙箱**沒有 streamlit**，23 個候選檔裡有 7 個在收集階段就掛掉。**只被「需要 streamlit 的測試」保護的 key，在本量測裡會顯示成 SILENT，但在 CI 可能是安全的** —— 已知一例：`test_section_label_for_batch_is_the_wireframe_wording` 對 `fund` / `batch` 做 `==` 比對，本地跑不到（那兩個 key 是被**另一條**測試抓到的，與它無關）。~~**故 8 是本地量到的上界，不是 CI 的真實數。**~~ → ⛔ **2026-09-08 第五輪就地更正（有意識的更正，不是漏刪 · 由 #834 第四輪獨立稽核 N6 指出）：本格自相矛盾，同時寫著「`7` 仍是上界」與「`8` 是本地量到的上界」。****擇一結果：以 `7` 為準。** 理由：`8` 是**本批修 `pf_ledger` 之前**的數，`7` 是**修之後**的數，而本格描述的是**現況**。上一輪把兩個時點的數字並排寫，卻沒有標明哪一個是現況，讀者只能猜。**現行讀法：修前 8 / 13 → 修後 7 / 13，而「上界」講的是修後那個 7**（兩組獨立實跑皆得 7、SILENT 名單逐字相同）。**「上界」二字的意思不變**：本地有 7 個候選檔收集不了，**CI 的真數 ≤ 7**。 | **獨立一組**（§-2 規則 4）；⛔ **不得**由 #834 實作組承接（本列的數字出自本組），**也不得**由 #834 第二輪稽核承接（12/13 那個數字出自它）—— 兩者都會變成自己驗自己。 | (1) 有人要**擴大或引用** `_SECTION_LABELS` 的漂移鎖（含新增 key）；(2) 有任務碰到 `tests/test_story_nav.py::test_section_labels_match_merged_pages`；(3) 出現「**指路指到一個畫面上找不到的標題**」的實際回報；(4) 舊分頁開始退場（那會讓 `pf_*` 這幾個 key 一起需要重新指向）；(5) **user 直接點名**。 |
| **P-TILEARGS-1**（2026-09-08 由 #834 **第五輪**獨立稽核 D4 抓出；#834 實作組依總管指示登記，**四顆突變均由本組獨立重現、附畫面實測**） | `ui/views/page_04_portfolio.py::_status_tiles` 內對 `_loaded`／`_all` 的 **5 個用法只有 1 個被守到**（`invested_hint` 的三個實參，由 `tests/test_wf04_add_holdings.py::test_the_call_site_feeds_invested_hint_the_right_three_numbers` 守）。**其餘四個運算式改一個 token，畫面就會說謊，而全套守衛與 CI 全綠。**<br>**待答**：這四個要不要一起納入同一條「呼叫端實參」守衛？還是四格狀態列該有各自的行為守衛？<br>**已知事實（本組實測，量測日 2026-09-08；每顆附 landed sha ＋ `ast.parse` ＋ restored，CTRL 25 passed 證明不恆紅）**：<br>　`M17` `_amounts` 吃 `_all` → 畫面 **`💰 總投入 NT$501,000`**，而**同一畫面下一行**寫「只加**已載入**的 1 檔；另有 1 檔…**沒有**算進去」（正解 `NT$1,000`）。<br>　`M18` `_sum` 不濾正數 → **`NT$-4,000`**（正解 `NT$1,000`）—— 一個**負的**總投入。<br>　`M19` `_n_priced = len(_amounts)` → 金額全 0 時畫 **`NT$0`**，正解是 **`—`**。⚠️ **這一顆正是該函式自己 docstring 明文警告的那件事**：「「0 元」與「不知道投了多少」在畫面上長得一樣，意思差到相反」。<br>　`M22` `_pids` 吃 `_loaded` → **`📋 保單 1 張 / 2 檔`**（正解 `2 張 / 2 檔`）。<br>**四顆全部存活**：`tests/test_wf04_add_holdings.py` **25 passed**（本組實跑）。<br>⚠️ **既有債，不是本 PR 造成的（本組獨立複驗，非照抄稽核）**：四個運算式在 merge-base `8af4005` 上**逐字已存在**（`git show` 逐條命中 1），且 `git diff 8af4005 HEAD` 對這四行**零命中** —— **本 PR 一個字都沒動它們**。故稽核判定不擋、總管採納。<br>⛔ **本輪只登記，不動工**（`CLAUDE.md §8.4 步驟 4`；且本輪授權為「純記錄修正 ＋ 登記，零 production 改動」）。<br>⚠️ **稽核另有三顆（`M20`／`M21`／`M23`）它原以為是洞、查下去 CI 擋得住，故意不算進本列** —— 本列是**四顆**，不是七顆。本組已獨立複驗其中 `M20`（`_render_no_holdings` 餵 `_holdings()`）：`tests/test_wf04_add_holdings_render.py` 的`EMPTY_TITLE_PENDING` / `EMPTY_TITLE_NONE` **兩條確實會紅**，**擋得住**。 | **獨立一組**（§-2 規則 4）。⛔ **不得由 #834 實作組承接**（本列四顆的重現與畫面證據都是本組跑的）；⛔ **也不得由 #834 第一～第五輪稽核任一組承接**（D4 出自第五輪稽核）。 | (1) 有任務碰到 `ui/views/page_04_portfolio.py::_status_tiles`；(2) 有人要**擴大或引用**「呼叫端實參要有人守」這條規則（含把新成員收進 `test_the_call_site_feeds_invested_hint_the_right_three_numbers`）；(3) 出現「**總投入的數字與旁邊的範圍說明對不上**」或「**保單張數少報**」的實際回報；(4) 四格狀態列要新增第五格（那會再長出一批沒人守的實參）；(5) **user 直接點名**。 |
| **P-FETCHPOINTER-1**（2026-09-08 由 #834 第三輪獨立稽核 A3 抓出；#834 實作組依總管指示登記。⚠️ **上一輪這件事只寫在 PR 描述、本表零命中** —— 那正是本節前言所稱「待查證沒有出口 ＝ 實質永久豁免」的形狀，本列即為補正） | `ui/views/page_04_portfolio.py` 的 `_render_concentration` 與 `_render_dividend_calendar_body` 兩處灰態指向 `where_to_find("pf_add")`（舊 ④「➕ 加入與管理基金」）。**待答**：它們的正確目的地是什麼？<br>**已知事實（本組實測，量測日 2026-09-08）**：① 這兩處問的是「**抓不到成分股／產業配置**」與「**抓不到配息紀錄**」，**不是** `invest_twd` —— 故**不屬**第二輪擋下的那個「同一欄位兩個相反答案」矛盾（本組與稽核判定一致）。② ~~**但它們在自己出現的狀態下也是死的**：兩者只在 `_holdings()` **非空**（＝全部已載入）時才走灰態分支，而 `pf_add` 區段（`ui/tab3_portfolio.py` 第 1420–2133 行）唯一的載入鈕 `key="btn_pf_load_all"`（第 1644 行）**被 `if not_loaded:`（第 1633 行）包住** —— **每一筆都已載入時它不渲染**。~~ → ⛔ **2026-09-08 第五輪就地更正（有意識的更正，不是漏刪 · 由 #834 第四輪獨立稽核 N5 指出，#834 實作組獨立複驗）——舊表述有兩個錯，而且方向都讓這個登記比實際更悲觀**：<br>　**(a) 「`_holdings()` 非空 ＝ 全部已載入」這個等價不成立。** `_holdings()` 濾的是 `loaded and not load_error`，**它非空只代表「至少一檔可用」，不代表「沒有未載入的」**。**混合狀態**（1 檔已載入 ＋ 1 檔還沒抓）下兩處灰態照走，而 `not_loaded` **非空** ⇒ **`btn_pf_load_all` 有渲染** ⇒ **`pf_add` 在那個狀態下不是死指路**。（本組複驗：該鈕的閘門鏈實測為 `['not_loaded', 'not pf']`。）<br>　**(b) 行號 `1420–2133` 尾界溢出。** 本組實測 `with _sec_add:` 的 `lineno=1419`、`end_lineno=`**`2123`**，而 **`2125` 起是 `with _sec_ledger:`** —— 舊登記的 `2133` 已經跨進帳本那一區。⚠️ 稽核給的是 `1419–2124`，本組量到 `2123`（`end_lineno` 取的是 `with` 主體最後一行），**差 1 行，本組據實記自己量到的數**。<br>　⚠️ **不寫新行號了**（`CLAUDE.md` 既有處置）：行號在任何一次重構後就失效，而重構**不會**觸發本表更新 —— **這一列自己就是行號寫法的實例**。現行定位：**`with _sec_add:` 區塊內、`key="btn_pf_load_all"` 那顆鈕，閘門是 `if not_loaded:`（在 `if not pf:` 的 else 分支裡）**。<br>　✅ **仍然成立的那一半**：「**全部都已載入**」時 `not_loaded` 為空 ⇒ 該鈕確實不渲染；「🗑️ 清空抓取快取」確實住在 `policy_admin_section.py`（＝ `pf_policy_admin`）**不在 `pf_add`**。**待答問題一字未變。**唯一能逼出重抓的槓桿「🗑️ 清空抓取快取」住在 `policy_admin_section.py`（＝ `pf_policy_admin`），**不在 `pf_add`**。⚠️ **本組獨立複驗過稽核這兩點**（AST：區段邊界、按鈕的 enclosing `if`、快取鈕所在檔），**不是照抄**。<br>⛔ **本批刻意不修**（`CLAUDE.md §8.4 步驟 4`：不擅自擴大範圍）——而且**本組不知道正確答案**：`pf_load` 只是猜測，**未查證**；抓不到成分股有可能根本不是「去哪按」能解的（來源沒有那筆資料）。**「我不知道」比一個沒驗過的目的地好**，所以這裡只登記問題，不寫答案。 | **獨立一組**（§-2 規則 4）。⛔ **不得由 #834 實作組承接** —— 那個觀察與那個未驗證的 `pf_load` 猜測都是本組產出的，本組去查等於自己驗自己。⛔ **也不得由 #834 第一／二／三輪稽核任一組承接**。 | (1) 有任務碰到 `_render_concentration` 或 `_render_dividend_calendar_body`；(2) 有任務碰到 `ui/tab3_portfolio.py` 的 `btn_pf_load_all` 或「🗑️ 清空抓取快取」；(3) 有人要**擴大或引用**「灰態指路必須在它出現的狀態下有效」這條規則（本 PR 第三輪為 💰 那格建立的判準）；(4) 舊 ④ 開始退場；(5) **user 直接點名**。 |
| **P-CNHALIAS-1**（2026-09-08 由 #833 第二輪獨立稽核抓出，本輪依本節格式補上出口） | **`comparable_ccy()` 沒有把人民幣收成一個命名空間 —— 別名 `人民幣(CNH)` 仍然自成一格，而守衛結構上看不到它。**<br>**已知事實（本組實測，不是轉述）**：`services/currency.py::CCY_NORMALIZE` 既有條目 `"人民幣(CNH)": "CNH"`，而 `normalize_ccy` 是**單次查表**（`CCY_NORMALIZE.get(_u, _u)`），故 `mode="iso"` 下它**停在 `CNH`、不會再轉成 `CNY`**。實測 `comparable_ccy('人民幣(CNH)')` → `'CNH'`、`comparable_ccy('CNY')` → `'CNY'`；端到端（基金宣告 `人民幣(CNH)`、逐筆宣告 `CNY`）→ `income_blocked == 'currency_conflict'`，**且畫面幣別欄印 `CNH`** —— **與 #833 B1 修掉的那個症狀一模一樣，只是入口換成別名。**<br>⛔ **而 #833 的守衛結構上就看不到它**：`tests/test_wf03_research_invest_calc.py::test_the_same_currency_written_two_ways_is_never_a_clash` 以 `if comparable_ccy(_other) != _canon: continue` 分組配對 —— **拿被測函式自己去定義「哪些寫法算同一種幣」**。`人民幣(CNH)` 與 `CNY` 因此被分進兩組、**永遠不會被配成一對**。**這是循環論證：任何被 `comparable_ccy` 分錯組的別名，都會被這條守衛自動跳過。**<br>**要答的是**：(i) 等價類該由誰定義（外部固定表？`CCY_NORMALIZE` 的遞移閉包？）；(ii) `人民幣(CNH)` 該收斂成 `CNY`，還是「離岸／在岸本來就該分開」而應留著；(iii) 守衛的配對來源要不要改成不依賴被測函式。<br>→ ⭐ **2026-09-08 第五輪就地升級（有意識的更正，不是漏刪；決策者：#833 實作組，起因為第五輪獨立稽核 M1，但下列矩陣是本組自己重跑的、沒有照抄）**：<br>**本列原本寫成「別名自成一格」，那個講法太輕 —— 它實際印在畫面上的是一句『⚠️ 資料疑義：兩邊不一致』，而那兩邊是同一種幣的兩種寫法。那是一句印給使用者看的假話，同時把他要的數字擋掉（`income_blocked=currency_conflict`）—— 屬 §1，不是「可後補」。**<br>**本組實跑矩陣（三個版本、`_dividend_caption` 逐一實跑；格式為「基金 vs 逐筆 ⇒ `origin/main` ／ `b37fce2` ／ 那兩行之後 ⇒ 結論」，⚠️ **刻意不用表格** —— 表格的直線符號會把外層這一格切開）**：<br>　• `人民幣(CNH)` vs `CNY` ⇒ 幣別未知 ／ 幣別未知 ／ **⚠️ 資料疑義** ⇒ **軟→硬，新的假指控**<br>　• `人民幣(CNH)` vs `人民幣` ⇒ 幣別未知 ／ 幣別未知 ／ **⚠️ 資料疑義** ⇒ **軟→硬，新的假指控**<br>　• `CNH` vs `CNY` ⇒ ⚠️ 資料疑義 ／ ⚠️ 資料疑義 ／ **✅ 全部以 CNY 計價** ⇒ **修好一個既有的假指控**<br>　• `RMB` vs `CNY` ⇒ ⚠️ 資料疑義 ／ ⚠️ 資料疑義 ／ ⚠️ 資料疑義 ⇒ **沒變（main 就這樣）**<br>　• `RMB` vs `人民幣` ⇒ 幣別未知 ／ 幣別未知 ／ **⚠️ 資料疑義** ⇒ **軟→硬**<br>　• `NTD` vs `TWD` ⇒ ⚠️ 資料疑義 ／ ⚠️ 資料疑義 ／ ⚠️ 資料疑義 ⇒ **沒變（main 就這樣）**<br>　• `NTD` vs `台幣` ⇒ 幣別未知 ／ 幣別未知 ／ **⚠️ 資料疑義** ⇒ **軟→硬**<br>⇒ **那兩行不是單向變壞**：它**修好了 `CNH` 這一族**，同時**把三組別名從軟的未知變成硬的假指控**。**兩邊都要講，只講一邊都是不誠實的。**<br>⚠️ **`RMB` / `NTD` 是另一個機制，不是同一個** —— 它們**根本不在 `CCY_NORMALIZE` 裡**，而 L0 `normalize_iso_ccy` **只檢查形狀**（`len==3 and isascii and isalpha`，實測該函式全身就這一行），所以 `RMB` / `NTD` 被當成合法 ISO **原樣放行並印上畫面** —— 而兩者都**不是** ISO 4217（正解是 `CNY` / `TWD`）。**已另立 `P-ALIASSHAPE-1`。**<br>⚠️ **一個順帶查到、與匯率有關的**（本組實測 `normalize_ccy(mode="yf")`）：`RMB` / `NTD` 在 yf 模式下**同樣原樣通過** ⇒ `fx_rate_to_twd('RMB')` 會去問 **`RMBTWD=X`**（不存在的 Yahoo 符號）⇒ 匯率查不到 ⇒ 整塊灰。**那也在 main 上就成立，不是本 PR 造成的。**<br>✅ **一項對修法有用的實測**：若把 `CCY_NORMALIZE` 的 `人民幣(CNH)` 改指 `CNY`，**匯率那一側不會變** —— `normalize_ccy('CNY', mode='yf')` 仍回 `CNH` ⇒ 仍問 `CNHTWD=X`。**⇒ 這個修法是 FX-safe 的**（B1 當初的四條紅線之一因此不受影響）。<br>⛔ **本輪為什麼沒有修**：兩個機制的根都在 **`services/currency.py` 這張共用表**，而它另有三個消費者（`ui/helpers/fund_grp_health/investment.py`、`ui/helpers/fund/checkup.py`、**`ui/tab2_single_fund.py`**）。改表**不必編輯**那些檔，但**會改變它們的行為** —— 其中一個還是客戶紅線凍結的舊分頁。**那是 scope 決定（§8.4 步驟 4），不是我能自己拍的**；且總管本輪放行的範圍寫死在「那個 `else` 分支的原因判斷」。**證據已備齊，等總管派工。**<br>⚠️ **本輪已就地釘住現況**（`tests/test_wf03_research_invest_calc.py::`test_two_spellings_of_one_currency_are_still_accused_of_clashing_today`）：**釘的是「今天長這樣」，不是「這樣是對的」** —— 一旦有人修好它，那條會轉紅，逼下一個人回來讀本列。<br>⚠️ **本列不得被讀成「B1 沒修好」** —— B1 修掉的是 `mode="yf"` 與 L0 兩個命名空間的不一致，那一半**已由七顆突變證實會轉紅**；本列是**同一條路上剩下的一個別名**。<br>⚠️ **production 可達性未查證**：該別名全 repo 只出現在 `CCY_NORMALIZE` 字典本身，**上游會不會真的吐出這個字串，本組沒有查、也不宣稱**（同 §-1.5.1c 判定 2：字表以外的形態掃不到）。 | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組承接**（本列指出的正是該組自己守衛的盲點，會變成自己驗自己），**也不得由 #833 第一～五輪稽核的任一組承接**。⚠️ **2026-09-08 補（S2）**：原本只寫「第一輪／第二輪」，**而本列的第 1 欄在第六輪被大幅改寫**（補進三版矩陣、`RMB`/`NTD` 的機制、FX-safe 實測）——**改了內容卻沒同步排除條款，是同一張表上的第二處同型缺口**（第一處是 `P-ADRUNKNOWN-1`）。 | (1) 有任務碰到 `services/currency.py::CCY_NORMALIZE` 或 `services/fund_invest_calc.py::comparable_ccy`；(2) **有人要引用或擴大「幣別命名空間已統一」這個結論**（含把新幣別別名加進字典）；(3) `_CCY_YF_OVERRIDES` 新增第二種幣別；(4) 出現「人民幣（或任何雙代碼幣別）配息試算被擋掉」的實際回報；(5) **user 直接點名**。 |
| **P-NAVSERIES-1**（2026-09-08 由 #833 第二輪獨立稽核抓出） | **`services/fund_invest_calc.py::fund_nav` 的「⛔ 不拿 `result["series"]` 末值頂替」是文件寫死、零守衛。**<br>**已知事實（本組實跑突變確認）**：把該函式改成缺 `metrics.nav` / `nav_latest` 時退到 `series` 末值 → **`tests/test_wf03_research_invest_calc.py` ＋ 四支 wf03 測試檔合計 `1 failed, 376 passed`，突變前後逐字相同**（那 1 failed 是 `test_the_rate_lookup_still_asks_for_the_yf_code` 的 pandas 環境限制，與本突變無關）⇒ **零條守衛咬到**。<br>**而那是真的行為改變，不是無害重構**：同一顆突變下，一份只有 `series` 沒有 `metrics.nav` 的回傳，`units_blocked` 由 `'no_nav'` 變成 `''`、畫面印出 **83,333.33 單位** —— 也就是**憑第二個淨值來源算出一個數字並印給使用者**，正是該函式 docstring 自己寫著要防的事（「那會讓同一個畫面上出現兩個淨值來源，而它們可能不相等」）。<br>⚠️ **現行程式是對的，缺的是守衛** —— 本列**不是**說 `fund_nav` 有 bug。<br>**要答的是**：補一條「缺 nav 時不得從 `series` 取值」的行為守衛（例如餵一份只有 `series` 的回傳、斷言 `no_nav`），還是承認這條只能靠 review？ | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組承接**（那條 docstring 與其零守衛狀態都出自該組），**也不得由 #833 第二輪稽核組承接**（發現者不驗自己的發現）。 | (1) 有任務碰到 `services/fund_invest_calc.py::fund_nav`；(2) **有人要引用該 docstring 的「不拿末值頂替」當成已被保護的事實**；(3) 深度區出現「淨值來源對不上」的實際回報；(4) **user 直接點名**。 |
| **P-ADRUNKNOWN-1**（2026-09-08 由 #833 第二輪獨立稽核抓出） | **未知 `adr_source` 的 fallback 借用了一段具體的機制描述 —— 上游真多一層時，畫面會講一個具體而錯誤的算法。**<br>**已知事實（本組實測）**：`ui/views/page_03_research.py::_invest_compare` 以 `_ADR_SOURCE_LABELS.get(_src, _ADR_SOURCE_LABELS[ADR_FROM_RECORDS])` 取標籤，而 `ADR_FROM_RECORDS` 那一句是**具體算法**：「系統把**最近 12 個月的配息加總 ÷ 淨值**回推的年化配息率」。餵一個不認得的來源實跑，畫面照樣斬釘截鐵印出那句話。<br>⚠️ **fail-safe 的那一半是對的**（未知來源**不會**被升格成「掛牌」，該行為由 `test_only_the_official_rate_is_ever_called_listed` 的第四個參數釘住）；**錯的是它借的那一句話太具體** —— §1 的要求是「不知道就說不知道」，不是「不知道就套用最近一個已知的機制」。<br>**要答的是**：fallback 該改成中性句（例如「這個對照值不是掛牌公布值，本頁無法判斷它是怎麼算出來的」），還是乾脆在遇到未知來源時**不顯示對照值**？<br>⚠️ **今天不可達**：`_resolve_adr_with_fallback` 目前只回三個標籤，第四種只有上游新增時才會出現 —— 故本列是**預防性登記**，不是活的錯誤。 | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組承接**（那個 fallback 是該組寫的），**也不得由 #833 第二輪稽核組承接**（發現者不驗自己的發現）。⚠️ **2026-09-08 補（S1）**：本列原本**只排除了實作組**，其餘四列都寫了後半句、**只有它漏**；而那是實作組**逐列自查**時發現的，**不是任何機器規則抓到的** ——「發現者不驗自己的發現」目前**沒有守衛**，全靠人記得。 | (1) `services/health/dividend.py::_resolve_adr_with_fallback` 新增或改名任何一層 fallback；(2) 有任務碰到 `ui/views/page_03_research.py::_ADR_SOURCE_LABELS`；(3) 出現「畫面說的配息率算法與實際不符」的回報；(4) **user 直接點名**。 |
| **P-ALIASSHAPE-1**（2026-09-08 由 #833 第五輪獨立稽核點名為 M1，本組重驗後另立一列） | **`normalize_iso_ccy` 只驗形狀不驗有效性，於是 `RMB` / `NTD` 這種「看起來像 ISO 但不是 ISO」的字串被原樣放行、印上畫面、並拿去問匯率。**<br>**已知事實（本組實測，非轉述）**：`shared/data_quality.py::normalize_iso_ccy` 全身就是 `return _u if (len(_u)==3 and _u.isascii() and _u.isalpha()) else ""` —— **repo 內沒有任何 ISO 4217 白名單**（以 `grep -rnE` 掃 `ISO_4217` 與 `_ISO_CCY` 兩個字樣，全 repo 只命中 `_ISO_CCY_LEN: int = 3`）。⇒ `RMB` / `NTD` / 任何三個英文字母都會被當成幣別代碼。<br>**造成三件事（皆實測）**：(i) 畫面把 `RMB` / `NTD` 當幣別代碼印給使用者，而兩者都不是 ISO 4217；(ii) 與另一側的 `CNY` / `TWD` 比對 → **⚠️ 資料疑義的假指控**（`RMB` vs `CNY`、`NTD` vs `TWD` 在 `origin/main` 上就已如此）；(iii) `fx_rate_to_twd` 會去問 **`RMBTWD=X`／`NTDTWD=X`**（不存在的符號）⇒ 匯率查不到 ⇒ 整塊灰。<br>⚠️ **這一列與 `P-CNHALIAS-1` 是兩個不同的機制，刻意不合併**：`P-CNHALIAS-1` 是**表裡有、但單次查表停在半路**；本列是**表裡根本沒有、而形狀檢查放它過去**。**合併成一列會讓其中一個的修法看起來已經涵蓋另一個。**<br>⚠️ **本列在 `origin/main` 上就成立，不是 #833 造成的**（上列矩陣的 `RMB`/`NTD` 兩行三個版本相同）。<br>**要答的是**：(i) 要不要引入 ISO 4217 白名單（那會改變 L0 的契約，影響所有呼叫端）；(ii) 還是把 `RMB` / `NTD` 補進 `CCY_NORMALIZE`（治標，下一個沒登記的別名會再犯）；(iii) 「比對兩邊時，只要有一邊認不得就說『幣別未知』而不是『不一致』」要不要成為通則？<br>⚠️ **本列為本組單組實測，未經第二組驗證**（§-2 規則 6）；**「上游真的會吐 `RMB`／`NTD` 嗎」本組沒有查、也不宣稱。** | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組（本組）承接**，**也不得由 #833 第一～五輪稽核的任一組承接**。 | (1) 有任務碰到 `shared/data_quality.py::normalize_iso_ccy` 或 `services/currency.py::CCY_NORMALIZE`；(2) **有人要引用「幣別已經正規化過了」這個結論**；(3) 出現「畫面印出一個沒看過的幣別代碼」或「匯率查不到但那一檔明明是常見幣別」的實際回報；(4) 與 `P-CNHALIAS-1` **應一起決定**（只修被點名的那一個 ＝ 本篇 `8.2.A` 驗證段 ④ 的既有教訓）；(5) **user 直接點名**。 |
| **P-STALEDIV-1**（2026-09-08 由 #833 第五輪獨立稽核點名為 M2） | **「最近一筆實際配息」沒有時效窗，也不在畫面上顯示那一筆是哪一天的。**<br>**已知事實（稽核回報，本組**未**重跑，依 §-2 規則 6 據實標明）**：一檔唯一的配息紀錄若是 2019 年的，投資試算仍會印「每個月大約領 N 元」，且**不顯示日期**。<br>**本組實測到的機制（這一半有查）**：`services/health/dividend_calc.py::latest_dividend_per_unit` 依日期降序取第一筆 `amount > 0`，**沒有任何時間上界**；`monthly_dividend_from_records` 直接用它乘持有單位。⇒ **這是全站 SSOT 的既有性質，不是 #833 引入的**（②／③ 兩頁共用同一支）。<br>⚠️ **本頁現行文案已經講了一半**：「季配或年配的基金，沒有配息的那幾個月實際是 0 —— 要看真正的節奏，往上看『配息紀錄』那張表」。**但它講的是節奏，不是新舊** —— 一筆七年前的配息，照這句話讀起來仍然像是「這一檔現在的常態」。<br>**要答的是**：(a) 加時效窗（超過 N 個月就不當「最近一筆」用）—— 那會改**全站** SSOT 的行為；(b) 只在畫面上把那一筆的**日期**印出來（本頁可自決、不動 SSOT）；(c) 承認這是既有性質、由配息表那張表負責讓使用者自己看日期（＝現況）？<br>⛔ **依 §-1 本輪不動工**：無 user 指派、無實際 bug 觸發，且 (a) 會動到 ②／③ 共用的 SSOT。 | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組（本組）承接**（本頁的文案是本組寫的），**也不得由 #833 第五輪稽核組承接**（發現者不驗自己的發現）。 | (1) 有任務碰到 `services/health/dividend_calc.py::latest_dividend_per_unit` 或 `monthly_dividend_from_records`；(2) 有任務碰到 ②／③ 任一頁的每月配息推估；(3) 出現「這個月配息數字是從好幾年前那一筆算的」的實際回報；(4) **有人要引用「本頁的月配息是最近一筆實配推的」當成已具時效性的事實**；(5) **user 直接點名**。 |
| **P-COUNTGUARD-1**（2026-09-08 由 #833 第四輪差異稽核抓出；本組**自己重跑驗證後**登記，不是照抄） | **⛔ `test_the_two_p_tables_declared_counts_match_what_is_actually_there` 是 fail-open —— 它擋不住「上表被改動」這件事本身。**<br>**它是為什麼加的**：#833 第三輪把三列新登記塞進了上面那張自稱「逐位元組搬過來」的表，而下面的計數一字未改 ⇒「上表是純搬移」由**可自驗**變成**不可驗**，且**四輪 CI 全綠**。<br>**⛔ 但它只咬得到「它自己的正則認得出來的列」**（那條正則要求：行首直線符號 → 選配的刪除線 → **粗體包住的 ID**）——**認不出來的列對它就是不存在**。<br>**已知事實（本組實跑四顆突變，非轉述；判定式＝『`head -463` 與 `origin/main` DIFFERS 但守衛全綠』）**：<br>　• **A** 往上表塞一列、ID **不加粗體** ⇒ `head -463` **DIFFERS**、守衛 **25 passed 全綠** ⛔ **fail-open**<br>　• **B** 往上表塞一列、ID 用**反引號**包 ⇒ 同上 ⛔ **fail-open**<br>　• **C** **不動列數**，只把既有一列的**內文抽換** ⇒ 同上 ⛔ **fail-open**<br>　• **D ⭐正對照** 塞一列**格式正常**的 ⇒ 守衛 **1 failed** ✅（證明 harness 沒壞，是守衛的射程問題）<br>⚠️ **這比實作組上一輪自陳的射程界線更嚴重**：它當時寫「只數列數、不看欄內容」，**實測連列數都繞得過** —— 因為「列數」數的是**它認得的列**，不是**表上的列**。<br>**要答的是**：(a) 改成拿 `head -463` 的 **sha256** 與 `origin/main` 直接比（最直接，但要處理「main 前進之後怎麼辦」）；(b) 把列的辨識改成「表格區間內**每一行以直線符號開頭的都算一列**」（不依賴 ID 的排版格式）；(c) 承認靜態守衛到此為止，改以流程手段（拆檔一律走 PR ＋ 換一組稽核）補？<br>⛔ **本輪只登記，不動工** —— 補它要**重新設計那道守衛**，是另一批的事；且本輪授權只到阻擋 1 那兩格字。<br>⚠️ **不得因為「有一道守衛在那裡」就認為上表被守住了** —— **它是 fail-open 的。** | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組（本組）承接**（那道守衛是本組寫的），**也不得由 #833 第一～四輪稽核的任一組承接**（發現者不驗自己的發現）。 | (1) **有人要引用「上表是純搬移」這個宣稱**（含據它主張某段內容沒被動過）；(2) 有任務要動 `tests/test_constitution_file_refs.py` 的任一道下限或清單；(3) 有人要**再拆一次憲法**（與 `P-SPLITSLACK-1` 應一起決定）；(4) 上表再被塞進任何一列；(5) **user 直接點名**。 |
| **P-MIXEDROWDATE-1**（2026-09-08 由 #833 第四輪差異稽核抓出） | **混合列時，畫面那句「沒有一筆帶得出日期」會被畫面自己推翻。**<br>**機制（本組實測）**：`_income_basis_gap` 的判定是「把日期補成合法值再問一次 `latest_dividend_per_unit`」—— 補完回得出來 ⇒ 判成**卡日期**。但若配息列是**混合**的（例如「有日期但金額 0」＋「有金額但無日期」），判定仍會落在 `no_date`，而**配息表上那一列是有日期的** ⇒ 畫面說「沒有一筆帶得出日期」，使用者往上一看**就看得到日期**。<br>⚠️ **稽核讀完四個 parser ＋ Step 4 整份取代邏輯，找不到上游會產出這種混合列的路徑，故不阻擋。**<br>⛔ **據實寫明：上游可達性未證實。** **不得**把這一列讀成「已確認不會發生」——它的狀態是「**找不到產出路徑**」，不是「**證明不存在**」（同 §-1.5.1c 判定 2：字表以外的形態掃不到）。<br>**要答的是**：(a) 把判定從「二分（金額／日期）」改成「逐列說明」（例如「有 N 筆有日期但金額不是正數、M 筆有金額但沒日期」）；(b) 在混合時退回一句不指名成因的中性句；(c) 承認上游不會產出、只留登記？<br>⚠️ **本列的機制由本組實測，上游可達性由稽核判讀，兩者都未經第二組驗證**（§-2 規則 6）。 | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組（本組）承接**（那個判定是本組寫的），**也不得由 #833 第四輪稽核組承接**（發現者不驗自己的發現）。 | (1) 有任務碰到 `ui/views/page_03_research.py::_income_basis_gap`；(2) **有人要引用「四種成因各自為真」這個結論**；(3) 任一配息 parser 改變它產出的列形狀（尤其開始允許缺日期或非正金額）；(4) 出現「畫面說沒有日期、表上明明有」的實際回報；(5) **user 直接點名**。 |
| **P-SAMESOURCE-1**（2026-09-08 由 #833 第四輪差異稽核抓出；⚠️ **這是本 repo 第三次同型**） | **「同源」是一個關於接線的宣稱，而它只有行為守衛、沒有結構守衛。**<br>**同型前例（三次，全部在本 repo）**：<br>　• #833 **X4／Y2** —— 對帳的單位約定與「這裡假設同幣別」那句 §1 揭露，**拿掉之後測試 0 紅**；<br>　• #833 **N-5** —— `_income_basis_gap` 的 docstring 自陳「筆數與配息表**同源**、**結構上不可能對不上**」，實測**零守衛**（把筆數改成 raw list 長度，十條守衛一條都沒紅）；<br>　• #834 —— 「**同源 ⇒ 不可能不一致**」的同一句宣稱，同樣沒有結構守衛。<br>⚠️ **共同形狀**：程式碼用一句散文宣告「這兩個值出自同一個地方，所以不可能分歧」，**然後沒有任何東西在守那個「出自同一個地方」**。一旦有人把其中一側換成另一個來源，**宣稱與實作就分家，而畫面不會有任何跡象**。<br>**要答的是**：(a) 要不要有一條**通用**的「同源宣稱必須附結構守衛」規則（例如 AST 驗兩處確實呼叫同一個 symbol）；(b) 還是逐案補行為守衛就夠？(c) 三次都是**稽核**抓到的、沒有一次是掃描找到的 —— 要不要先做一份「散文裡的同源宣稱」清單？<br>⚠️ **前兩次的行為守衛已補**（#833 本輪），**第三次（#834）本組沒有查證**，只轉述。 | **獨立一組**（§-2 規則 4）；⛔ **不得由 #833 實作組（本組）承接**（三次裡有兩次的宣稱是本組寫的），**也不得由 #833 第一～四輪稽核、或 #834 任一組承接**。 | (1) **有人要在程式碼或文件裡寫下「同源／同一個來源／結構上不可能不一致」這一類宣稱**；(2) 有任務碰到上列三處任一；(3) 出現第四次同型；(4) 有人要引用其中任一句當成「已被保護」的事實；(5) **user 直接點名**。 |

⚠️ **`P-PHANTOMSEC-1` 是怎麼被找到的**：它**不是**靠讀憲法讀出來的，也不是稽核回讀撿到的 ——
它是 **2026-09-08 拆檔輪在盤點「哪些節號會跟著搬、哪些留下」時掉出來的**：
盤點到 `8.2.A.1` 時發現**它根本沒有『在哪裡』可言**，因為它從來不存在。
→ **這是本表的第五種發現機制：不是有人去查它，是有人被迫去數它。**
**一次搬家會逼你把每樣東西都拿起來一次** —— 那正是幽靈節號唯一會現形的時刻。

⚠️ **`P-FROZENTABREF-1` 是怎麼被找到的 —— 又是一種不同的機制，而且對本表的完整性很不利**：
它**不是**任何人去查文件查出來的，是**獨立稽核在核對「檔案邊界有沒有被越過」時撞出來的** ——
稽核比對本 PR 動到哪些檔，發現 `ui/tab6_manual.py` 在清單裡。
→ **也就是說：這一列之所以會存在，是因為有人先違規了。**
**如果本輪沒有踩到那條紅線，凍結檔內那三行過期指標會繼續沒有出口地躺著，沒有任何人會去登記它。**
⚠️ 這對本表的窮舉性是一個**壞消息**，值得寫下來：
上表末尾那句「**本表的完整性靠的是有人回頭讀，不靠任何一次掃描**」——
本列顯示它其實還更弱一階：**有時候靠的是有人剛好犯錯。**

⚠️ **`P-SPLITSLACK-1` 是怎麼被找到的 —— 第六種機制，而且是最貴的一種**：
它既不是掃描、不是回讀、不是搬家時被迫盤點、也不是有人犯錯 ——
**是獨立稽核把守衛當成攻擊目標，一口氣做出五組繞過，全部跑成綠燈。**
⚠️ **值得記的不是「稽核很厲害」，是這件事的順序**：
本輪的第三道守衛是**上一輪才剛加上去、而且上一輪也做過突變測試並全部轉紅的**。
**它通過了設計它的人想得到的每一個突變，然後被另一組在半小時內繞過五次。**
→ **一個守衛「經過突變測試」不等於「擋得住」** —— 只等於「擋得住**它作者想得到的那些**」。
**這是本表所有列裡，唯一一個由『換一組人來攻擊』才可能被發現的缺口。**

→ **2026-09-08 #834 第二輪回修再更新（狀態變更，不是漏刪）**：本表再增 1 列（新增 **`P-SECLABEL-1`**）。⚠️ **本輪刻意不宣告「現在共 N 列」** —— 上面那一串「第九個／第十八個／第二十二個」的沿革已經示範過：**每加一列就要回頭改一次的計數，本身就是下一個會過期的東西**，而本表整節的重點是「別信這張表窮舉」。**同一句話照樣往下推：「本表沒有下一個待判定項」本組未查證、也不宣稱。**

| **P-P05VERDICT-1**（2026-09-08 由 #835 實作組登記，總管裁決「不鬆守衛」後歸檔） | ⑦ 設定與診斷（`ui/views/page_05_settings.py`）新增的 **🧾 ① 結論層**，**說得出「不知道」，說不出線框那句帶判定的結論**。<br>**線框逐字要的兩句**（客戶 2026-09-08 拍板 `docs/wireframes/draft-four-page-content.html` §3）：狀態 (1)「🔴 **還不能。有 2 項沒設定完**，會讓 ① 到 ④ 出現空白或舊資料。」／狀態 (2)「🫐 **大致可以，但有 2 個來源今天沒更新。**」<br>**為什麼畫不出來（三條，互相咬住）**：(a) 兩句都**要先讀東西**；(b) 該 View **唯一被允許的 L2 入口**是 `services.nav_history_gs`（`tests/test_wf05_settings_skeleton.py::_ALLOWED_SERVICE_MODULES`），而 `infra.config.get_secret` 是 **L0 banned root** ⇒ **讀 secret 的唯一合法門是 `nav_history_gs.status()`**，它回傳的 `missing` 與 `diag["st_secrets_alive"]` **恰好就是線框那兩張卡要的東西**；(c) **但 `status()` 在閘門之前一次都不准被呼叫** —— `test_the_nav_block_is_grey_and_reads_nothing_before_the_gate` 斷言 `_calls == {"backend": 0, "coverage": 0}`，而結論層排在所有閘門之前。<br>**已知事實（#835 與獨立稽核各自 AST 實測，兩邊一致）**：`status()` 的呼叫集合裡**只有 `infra.config.get_secret`，沒有任何網路呼叫** ⇒ **那條守衛對 `status()` 而言，比它自陳的目的（「首屏一次 Google Sheets 往返都沒有」）寬** —— 往返的是 `coverage_status()`，不是 `status()`。<br>**待答**：要不要把那條守衛從「兩支都不准叫」收窄成「**只有 `coverage_status()` 不准叫**」，讓結論層畫得出線框那句判定？⚠️ 若要做，**必須連帶處理** `test_opening_the_gate_reads_exactly_once`（它斷言勾了之後 `backend` 恰好 1 次，結論層先叫一次就變 2 次）。<br>⛔ **總管 2026-09-08 已裁決「本輪不鬆」，理由寫在這裡讓後人能推翻**：線框那兩張 ASCII 圖畫的是**舊 ⑤ 下架之後**的樣子 —— 那時閘門都拆了，才讀得到、才說得出判定。**現在拆守衛去換一句文案，會把「延遲載入」這個客戶拍板的原則反過來，而且拆了回不去。**<br>📌 **連帶登記（同一個根因，不另立一列）**：⑦ 結論層那**三張卡**因此**與線框 §1「每張卡 ＝ 一件要做的事」不同** —— 現行畫的是「哪一塊回答哪一個問題」的目錄。**總管裁決：接受為暫行**，已就地寫進 `_conclusion_cards()` 的註記。**⛔ 不准為了對齊線框去畫沒量過的紅燈**（`CLAUDE.md §1` 比「對齊線框」高）。**這一格與線框的差異由總管在交付報告裡對客戶明白揭露。** | **獨立一組**（`CLAUDE.md §-2` 規則 4）；⛔ **不得**由 #835 實作組承接（結論就是它寫的），**也不得**由 2026-09-08 那兩輪獨立稽核的任一組承接 —— 兩者都會變成自己驗自己。 | **⭐ 觸發點主線：舊 ⑤（`ui/tab_settings_diag.py`）下架那一批** —— 那一刻四顆雙軌閘門消失、線框那兩張圖的前提才成立，本列必須在那一批內重新裁決。<br>其餘：(2) 有人要動 `test_the_nav_block_is_grey_and_reads_nothing_before_the_gate` 或 `test_opening_the_gate_reads_exactly_once`；(3) `services/nav_history_gs.py::status` 開始有網路呼叫（那時「它不連線」這個前提翻轉，**方向相反**：該收更緊，不是放寬）；(4) 有人要把 ⑦ 的結論卡改成線框那三張；(5) **客戶／user 直接點名**。 |

⚠️ **`P-P05VERDICT-1` 是怎麼被找到的 —— 又是「產生缺口的那一組自己送進來」，但這一次不完整**：
PR #835 實作組在自己的 PR 與被測檔的 `(D-6)` 裡主動揭露了它，**這一半做對了**；
⚠️ **上一行刻意寫成「PR #835」而不是行首的「#835」** —— 行首一個 `#` 會被
`tests/test_constitution_file_refs.py::_p_table_counts` 當成**章節標題**，
於是它後面新增的登記列**全部落到章節之外、數不進計數**（本輪實測：
`P-FLOORSLACK-1` 因此被判在 `sec='#835 實作組…'` 底下，宣告 37／實際 36 → 紅）。
**是那條計數守衛抓到的，不是人讀出來的。**
**沒做的是「給它一個出口」** —— 它只寫在 PR 描述與程式碼註解裡，
**沒有登記進本節**，而 `CLAUDE.md §8.3.P` 前言逐字寫著
「**待查證沒有出口 ＝ 實質永久豁免**」。是總管在複驗時要求補登的。
⚠️ **值得記的是這件事的形狀**：它與上表 `P-WHERECONTENT-1` **完全同型** ——
那一列也是實作組自陳「我只守到一半」、也是**自己指出「登記在測試檔裡是錯的地方」**。
**同一個 repo、同一個月、同一種病，第二次。** 自我揭露解決的是「有沒有看到」，
**它不會自動變成一個有出口的登記** —— 那需要另一個人來問「所以誰去查、什麼時候查」。

⚠️ **上表 `P-WHERECONTENT-1` 的觸發點 (1) 已命中，續記寫在這裡（2026-09-08 · #835）**
——⛔ **刻意不寫進那一列本身**：上表自稱「**逐位元組搬過來、一列都沒動**」，
而 `diff <(git show origin/main:EXCEPTIONS.md) EXCEPTIONS.md` 在上表區間內為空**是可自驗的事實**；
往那一列補字會把它變成假的（本篇上方已經記載過一次同型事故）。
**本段是散文、不是表格列，所以不進計數**（`_ROW_ID_RE` 只認 `| **P-…**` 開頭的列）。

- **觸發點 (1)（「有任務碰到 `ui/views/**` 的**灰態卡**」）於 #835 命中**：
  該 PR 在 `ui/views/page_05_settings.py` 新增了 🧾 ① 結論層，含**三張 `STATE_NOT_READY` 的灰卡**。
- **本組實測（**自己重跑一次**，不是照抄稽核的數字；量測日 2026-09-08）**：
  該檔被 `_where_sites()` 收到的站點 **8 → 13**
  （`not_ready` 7→9、`empty_state` 1→1、**`card-dict` 0→3**）；
  值型別 `JoinedStr` 6→6、**`Call` 2→7**，**`Constant` 兩邊都是 0**。
- ⚠️ **可被內容規則看見的 `「」` 段落數，兩邊都是 `0`** ——
  **覆蓋率由 `0/8` 變成 `0/13`**。⛔ **本列的問題因此變大了，不是變小**。
- **驗證指令（repo 根目錄，單行；`PYTHONPATH` 需含 pytest 才 import 得動該測試檔）**：
  `python3 -c "import sys,collections; sys.path.insert(0,'tests'); import test_batch2_top_card_grid as M; s=[x for x in M._where_sites() if x[0]=='ui/views/page_05_settings.py']; print(len(s), dict(collections.Counter(x[2] for x in s)), dict(collections.Counter(type(x[3]).__name__ for x in s)))"`
- ⚠️ **本續記為單組實測，未經第二組獨立驗證**（`CLAUDE.md §-2` 規則 6）；
  「該檔沒有第 14 個站點」**本組不宣稱** —— `_where_sites()` 自己就看不到三種形狀
  （`dict(...)` 建構式、帶 `**other` 的 dict、在別的函式建好再傳進來的 dict）。

| **P-FLOORSLACK-1**（2026-09-08 由 #835 第二輪獨立稽核抓出；#835 實作組**自己重跑驗證後**登記，不是照抄） | **憲法檔位元組下限的「餘裕 ratchet」自己可以被一行改掉，而它守的正是「不要一行改掉」。**<br>`tests/test_constitution_file_refs.py::_PER_FILE_FLOOR_MAX_SLACK = 1.6` 是那條 ratchet 的全部力量 —— 它讓「下限離現值太遠」變成一次紅燈。**但它是一個裸常數，沒有任何東西在守它。**<br>**已知事實（實測，三道正對照齊全）**：把它改成 `99.0` → `tests/test_constitution_file_refs.py` **18 passed、突變存活**（`landed` sha `14bf11d2a99b`→`854b8b1c511f`、`parses`、`restored` 皆成立）。⇒ **任何人被那條紅燈擋住時，最省事的解法就是把它改大，而不是把下限調上去。**<br>⚠️ **而那條守衛自己就寫著「⛔ 不要改大它來閉嘴」** —— **一句寫下來的禁令，沒有牙。**<br>⚠️ **2026-09-09 就地更正措辭（有意識的更正，不是漏刪 · 決策者：AI 總管，依第三輪獨立稽核）**：舊表述說那句話在「**註解**」裡 —— **不精確**。**實測**：它在 `tests/test_constitution_file_refs.py::test_guard_still_sees_the_whole_constitution` 那條 **assert 的失敗訊息字串**裡，不是註解。**指控成立、位置寫錯**：一句寫在**失敗訊息**裡的禁令，只有在**已經紅燈**時才看得到 —— 而改大那個常數的人，正是**為了讓紅燈消失**才來改它的，**他看到那句話的時候，那句話正在被他繞過。** ⇒ **比寫在註解裡更沒有牙，不是比較有牙。**<br>✅ **2026-09-09 同輪：本列已補上程式碼回指**（`tests/test_constitution_file_refs.py` 的 `_PER_FILE_FLOOR_MAX_SLACK` 上方）。**補的是回指，不是修那條守衛** —— 本列仍為**未動工的待判定項**，三個待答方向一字未變。⚠️ **補回指的理由**：本列先前是全 repo `P-` 登記裡**唯一**在程式碼中零回指的一列（2026-09-09 實測 **36 列中 35 列有回指**），而 `§8.2.A.1` 明文要求「對應檔案加註解指回此表」—— **一份讀不到的登記，等於沒有登記。**（這與 #835 本輪 ⛔1 被擋下的是**同一個病**：`page_05_settings.py` 也寫了「⛔ 線框那三張卡的數字一個都不准畫」，五個裡只有一個有守衛。）<br>**待答**：要怎麼替它長牙？三個方向本組**都沒有評估過**，只列出來不作推薦：(a) 把 `1.6` 釘進另一個獨立的守衛（但那個守衛自己又是一個裸常數 —— **會不會只是把問題往上推一層？**）；(b) 改成「**只准變小**」的單向 ratchet（要有地方存上一次的值）；(c) 承認它是**組織紀律而不是機器規則**，改成 CI 之外的 review checklist。<br>⛔ **#835 本批刻意未碰**：`CLAUDE.md §-1`（無 bug 觸發、無 user 指派）＋ `§8.4 步驟 4`（不擴大 scope）；且它**不是本批造成的**（`origin/main` 上就是這樣）。**登記，不動工。** | **獨立一組**（`CLAUDE.md §-2` 規則 4）；⛔ **不得**由 #835 實作組承接（本輪剛把 `EXCEPTIONS.md` 的下限依那條守衛的處方上調 `131167`→`169138`，等於自己驗自己）。 | (1) 有任務碰到 `tests/test_constitution_file_refs.py` 的下限或餘裕機制；(2) **有人被那條紅燈擋住**（那是最可能被順手改大的一刻，也是本列最該被讀到的一刻）；(3) 有人要新增第三個受保護的憲法檔；(4) **客戶／user 直接點名**。 |
| **P-HEDGESEMANTIC-1**（2026-09-09 由 #835 第三輪回修就地登記、第四輪總管裁決升為 `P-` 列） | **「承諾了結果必須對沖」這條規則的 fail-closed，量的是【長度】不是【語意】。**<br>`ui/views/page_05_settings.py::_FAILURE_ALLOWANCE`（現值「或為什麼查不到」）是那條規則的唯一措辭，三處以上的承諾句共用它。守它的是 `tests/test_wf05_settings_skeleton.py` 的 `assert len(_FAILURE_ALLOWANCE) >= 4`。<br>**已知事實（實測，三道正對照齊全）**：把它改成 `"或或或或"` → **155 passed、突變存活**。四個無意義的字通過長度檢查，也通過所有 `.count(...)` 檢查（四處都會印出它），於是「每一句承諾都有對沖」**在形式上成立、在語意上全空**。<br>⚠️ **病史（同一個洞被治了兩次，兩次都沒治到）**：第二輪把 `_FAILURE_ALLOWANCE` 掏空成 `""` → `"任何字串".count("")` 回傳**字元數＋1**，讓 `>= 3` **恆真、突變存活**；第三輪據此補上 `len(...) >= 4`。**但那是把「量錯的東西」換成「量另一個錯的東西」** ——它要保證的是「**這句話真的在對沖失敗**」，而長度量不到那個。<br>⭐ **它為什麼躲過了本 PR 新加的每一層封閉集合（第四輪一併併入本列）**：`_CARD_TEXT_PINNED` 與 `_VERDICT_PINNED` 兩份逐字底本**刻意保留 `{fail}` 佔位符不展開**（不抄那個值，理由寫在該表），所以 `_FAILURE_ALLOWANCE` 換成什麼，對兩份底本都是**透明的**。<br>⚠️ **同型佔位符盤點（本組實測，第四輪稽核複驗）—— `{fail}` 是唯一沒有出處的那一個**：`{gate}` → `DIAG_GATE_LABEL`，**被線框逐字表釘住**（`_wf_line_runs()` 整行相等）；`{block}` → `BLOCK_HEALTH`，**被 `assert BLOCK_HEALTH == "資料來源健康度"` 逐字釘住**；`{fail}` → `_FAILURE_ALLOWANCE`，**只有一個長度下限，沒有任何值的出處**。<br>**待答**：三個方向本組**都沒有評估過**，只列出來不作推薦：(a) 把它一起釘進底本（但那會讓底本抄一份值，正是那兩份表刻意避開的）；(b) 改驗「它出現在承諾句的同一句話裡」（**仍是形態不是語意**）；(c) 承認語意驗不到，改成人工 review 項並在底本旁寫明。<br>⛔ **#835 未動工**：`CLAUDE.md §-1`（無 bug 觸發）＋ `§8.4 步驟 4`（不擴大 scope）。**登記，不動工。** | **獨立一組**（`CLAUDE.md §-2` 規則 4）；⛔ **不得**由 #835 實作組承接（那個常數與兩份底本都是本組寫的），**也不得**由 #835 第一～第四輪獨立稽核的任一組承接 —— 兩者都會變成自己驗自己。 | (a) 有任務碰到 `_FAILURE_ALLOWANCE` 或那幾處對沖句；(b) **有人要擴大或引用「承諾了結果必須對沖」那條規則**（含把新成員收進 `_PROMISES_A_RESULT`）；(c) 出現「**畫面承諾打開就看得到、實際是紅框**」的實際回報；(d) **客戶／user 直接點名**。 |
| **P-GATEDLAYOUT-1**（2026-09-09 由 #835 第五輪獨立稽核抓出兩筆；#835 實作組**逐筆自己重跑驗證後**登記，併入 #835 第四輪自行登記的 ⚠️D —— **三筆同族，合成一列**） | **閘門打開、或舊 ⑤ 還在的時候，⑦ 的【版面】與【指路】會失效 —— 三處，全部是既有債。**<br>**⭐ 為什麼合成一列而不是開三列（本組判斷，總管可拆）**：三筆的**觸發點是同一個**（舊 ⑤ 下架那一批），**處置也是同一個決定**（那一批內一起重裁）。拆成三列會讓讀者以為要派三組獨立查證，實際上是**一個決定**；而合成一列的風險是「⚠️D 被埋掉」，故三筆**各自具名、各自附實測**，不做摘要。<br><br>**(1)｜勾「🗄️ 載入手動補資料」之後，`### 手動補資料` 這個標題整個消失。**<br>**本組實測**（`AppTest` 真渲染，委派拋例外）：`gate=False` → 標題 **1** 次、紅框 0；`gate=True` → 標題 **0** 次、紅框 **1**。<br>**機制**：`_render_backfill()` 開頭 `_heading_slot = st.empty()` 佔位，**只有 gate 關的分支**會 `_heading_slot.markdown(f"### {nav_manual_label()}")`；gate 開時標題由委派的 `render_nav_manual_section()` 自己畫，**委派炸掉 ⇒ 兩邊都沒畫**。<br>⚠️ **它與那一塊自己的灰態文案直接牴觸**：灰態逐字寫「**勾它只能看到這一塊在新版版面上的位置**，看不到能用的表單」—— **勾下去之後，「位置」也看不到了。**<br>⚠️ **maintain／manual 兩塊沒有這個問題**（標題畫在 `st.expander` 外面、無條件）。<br>**既有債（本組實測）**：`origin/main` 上就有 `_heading_slot` 與 `_heading_slot.markdown(...)`（2 命中）；**#835 全 diff 對那幾行命中 0 次**。<br><br>**(2)｜`_where()` 的前綴指向【舊】分頁，而舊分頁沒有那一塊。**<br>**本組實測**：`where_to_find("settings")` ＝ `⑤ ⚙️ 設定與診斷`（**舊** tab），故 `_where(BLOCK_KEYS)` ＝ `⑤ ⚙️ 設定與診斷 → 連線與金鑰`，而**本頁是 ⑦**。<br>⚠️ **而舊 ⑤ 沒有叫「連線與金鑰」的區塊** —— `grep -c` 給 5 命中，**但 AST 逐一判讀後：非-docstring 字串裡 0 命中，5 個全是註解／docstring 在談未來的 T18 重組**。（**這一筆本身就是本 PR 的主題**：`grep` 數的是行，不是意思。）<br>**既有債（本組實測）**：`_where(` 的**真實呼叫點 2 個**（`BLOCK_KEYS` / `nav_manual_label()`），**兩個都在 `origin/main` 上**；head 多出來的那 1 個命中是 #835 第四輪自己寫的**註解**，不是新呼叫點。<br><br>**(3)｜⚠️D：`_render_nav_status` 的 backend-off 指路指向「連線與金鑰」，而那一塊今天答不了 Google 授權。**<br>與結論層 🔑 卡被第二輪擋下的是**同一個病**（指到一個答不出來的區塊），差別只在**它不在本批建立的規則射程內** —— `test_every_card_points_at_the_block_that_can_actually_answer_it` 只走 `_conclusion_cards()`，**結構上看不到 `_render_nav_status` 裡的這一處**。<br>**既有債（本組實測）**：`origin/main` 上這一行就是 `where=_where(BLOCK_KEYS)`。#835 第四輪已在該行上方就地登記，本列把它收進同一個家族。<br><br>**待答（三筆共用一個問題）**：舊 ⑤ 下架之後，(1) 的 `_heading_slot` 兩段式還需不需要？(2)(3) 的指路前綴要改成 ⑦、還是那兩塊本來就該重新指向別處？⛔ **本組不作推薦** —— 那一批會同時動到區塊歸屬，先決定歸屬再決定指路才有意義。<br>⛔ **#835 未動工**：`CLAUDE.md §-1`（無 bug 觸發、無 user 指派）＋ `§8.4 步驟 4`（**scope 是總管的決定，2026-09-09 已明確裁定不擴大**）。**登記，不動工。** | **獨立一組**（`CLAUDE.md §-2` 規則 4）；⛔ **不得**由 #835 實作組承接（(3) 是本組登記的、(1)(2) 是本組驗的），**也不得**由 #835 第一～第五輪獨立稽核的任一組承接。 | **⭐ 觸發點主線：舊 ⑤（`ui/tab_settings_diag.py`）下架那一批** —— 那一刻四顆雙軌閘門消失、`where_to_find("settings")` 的指向也必須重裁，**三筆一起重看**。<br>其餘：(2) 有任務碰到 `_render_backfill` 的 `_heading_slot`、或 `_render_nav_status` 的 backend-off 分支；(3) 有人要改 `ui/helpers/story_nav.py` 的 `settings` 那一筆對映；(4) 出現「勾了閘門之後找不到那一塊」或「照指路去了卻沒有那個區塊」的實際回報；(5) **客戶／user 直接點名**。 |
| **P-P02JARGON-1**（2026-09-09 由 ⑥ 那一批（本 PR）實作組登記；**是本組自己寫的守衛在自己的射程邊界上撞到的**，不是掃描找到的） | **⑥ 畫面上還有兩處「我們的進度／內部術語」，而它們都不在本批的改動範圍內。**<br>**本組實測（量測日 2026-09-09；指令見 `tests/test_wf02_health_wireframe.py` 那一族）**：<br>　· **`取數` ×1** —— 「診斷條件」表單上方那句「條件改完按「套用」才重算 —— 拖滑桿的當下不會觸發任何**取數**」。<br>　· **`程式碼` ×3（gate=OFF）／×2（gate=ON）＋ `模組` ×1（只在 gate=ON）** —— 委派區閘門的標籤「載入逐檔健診與互斥分析（與舊分頁同一份**程式碼**）」、它的灰態把標籤引述兩次、以及勾開後那句「本區直接沿用既有的健診**模組**…」。<br>⚠️ **兩者的處置不同，⛔ 不要合併讀**：`取數` 那一句在本批的守衛射程**之內**（委派區標題以上），已用**棘輪**釘住「只准變少」；`程式碼`／`模組` 在射程**之外**（委派區標題以下），本批**完全沒有守它們**。<br>**射程外的理由，據實寫**：(a) 那一段是**原封委派進來的舊模組**，客戶紅線是「絕對禁止修改現有線上正常運作的舊版 Tab 代碼」——**本批對那些字動不了**；(b) **本機量到的數字對那一段是假的** —— 本機沒有 `pandas`／`numpy`／`plotly`，委派進來的區塊全部拋例外只印一行紅框，**CI 上它們會真的畫出東西**，字數與用詞完全不同。**拿一個 CI 量不到的數字當棘輪起點，就是本 repo 記載過的那種假守衛。**<br>**要答的是**：(a) 這幾句要不要改成處境語言（`程式碼`／`模組` 那三句是**⑥ 自己的檔案**寫的，改得動，只是不在本批的改動清單裡）；(b) 委派區以下（舊模組自己畫的字）要不要納入裁決 4 的射程 —— 那會**跨進舊分頁的檔案邊界**，屬客戶 gate。<br>⛔ 依 `CLAUDE.md §-1` 本輪不動工；⛔ 也**不得**引用本列主張那幾句合規。 | **獨立一組**（§-2 規則 4）；⛔ **不得由 ⑥ 那一批（本 PR）實作組承接** —— 那幾個字是本組自己選擇不改的，由本組來判等於自己驗自己。 | (1) 有任務碰到 `ui/views/page_02_health.py::_render_filter_form` 或 `::_render_delegated_sections`；(2) **有人要擴大或引用裁決 4 的「畫面用語」規則**（含把新成員收進 `_BANNED_ON_SCREEN` 或調動 `_SCREEN_JARGON_DEBT`）；(3) 舊 ② 下架、委派區整段消失（屆時 `程式碼`／`模組` 那三句會自然歸零，本列剩一半）；(4) **user 直接點名**。 |
| **P-P02DELEGJARGON-1**（2026-09-09 由 ⑥ 那一批（本 PR）實作組登記；**是「兩個世界都要跑」那條 ⑦ 教訓當場撿到的**，gate=OFF 完全看不到它） | **⑥ 勾開委派區之後，畫面上出現一句指向「本頁不存在的欄位」的灰態。**<br>**本組實測（`gate=ON`，量測日 2026-09-09）**，逐字：<br>　`⬜ 至少需 2 檔基金才能比對互斥性（請先到：本頁的「基金代號」欄位（多貼幾檔後重按「🩺 開始健診」））`<br>⛔ **⑥ 沒有「基金代號」欄位，也沒有「🩺 開始健診」按鈕** —— 客戶 **2026-09-05** 裁決「**不保留手動輸入基金代號**」，持股一律從組合帶入（見 `ui/views/page_02_health.py::_render_filter_form` 的 docstring，且 `tests/test_wf02_health_skeleton.py` 有一條專門擋自由文字輸入框的守衛）。**那句指路指的是舊 ② 的畫面。**<br>⚠️ **這正是本 session 貫穿九輪的那個缺陷類別**（指路指到「該狀態下不存在／做不到那件事」的地方），**只是它住在被委派的舊模組裡**（`ui/components/mutual_exclusion.py`），⑥ 只是把它原封畫出來。<br>**要答的是**：(a) 由 ⑥ 在委派前後補一句更正？（那會變成同一塊有兩句互相矛盾的指路）(b) 改舊模組讓指路可傳入？（**跨進舊分頁的檔案邊界，屬客戶紅線**）(c) 認定「委派區的內容與舊 ② 同源同碼」已由該區 caption 揭露、由舊 ② 下架那一批一併解決？<br>⛔ 依 `CLAUDE.md §-1` 本輪不動工；⛔ 且本批**不得**改任何 `ui/tab*.py` 或被委派的舊模組。 | **獨立一組**（§-2 規則 4）；⛔ **不得由 ⑥ 那一批（本 PR）實作組承接**。 | (1) 有任務碰到 `ui/components/mutual_exclusion.py`；(2) 有任務碰到 ⑥ 的委派區；(3) **舊 ② 下架那一批**（屆時這句指路的落點必須一併重驗）；(4) 出現「照著灰態去找『基金代號』欄位卻找不到」的實際回報；(5) **user 直接點名**。 |
