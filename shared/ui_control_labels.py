"""shared/ui_control_labels.py — 「去哪補」文案指名的**控制項標籤 SSOT**(L0,純常數)。

## 為什麼需要它(2026-09-04 第四輪稽核 R4-F9)

線框 `docs/wireframes/ia-wireframe.html` Rule 04 規定空狀態三要素:**標題 / 缺什麼 /
去哪補**。「去哪補」只有在它**指名的那個控制項真的長那個樣子**時才有用;實測當時
三則文案全部指錯:

| 文案寫的 | 畫面上實際是 | 為什麼是錯的 |
|---|---|---|
| 「📡 載入總經資料」 | 「🔄 更新總經資料」 | 那是**首次載入前**的字;而快覽網格**只在 `macro_done` 為真時才渲染** —— 使用者看得到那句「去哪補」的每一刻,按鈕上的字都**不是**它指名的那一個 |
| 「🔄 強制重抓」 | 「🆕 強制重抓最新（清快取）」**且是一個 checkbox**,勾了還要按送出鈕 | 使用者會去找一顆按鈕,找不到 |
| 側欄「🔄 全域刷新」 | 「🧹 全域刷新（清所有快取 + 落地檔）」 | emoji 與字都不同 |

**根因與 `shared/session_keys.py` 完全相同**:同一個字串被抄在兩個地方,而其中一份
(文案)**沒有任何機制**會在另一份(控制項)改字時跟著改。當時的守衛只斷言
「文案裡有『強制重抓』這個子字串」—— 那條斷言在**兩邊都改壞**時照樣綠燈。

故把標籤收成 L0 常數:**渲染控制項與撰寫「去哪補」文案讀同一份**,
`tests/test_batch2_top_card_grid.py` 另有守衛比對「文案指名的字串 ∈ 實際渲染的標籤」。

⚠️ **本模組只收「被『去哪補』文案指名的」控制項**,不是全站按鈕字典 ——
收成字典會變成一份沒人維護的第二真相源(§8.1 step 6「用不到的抽象」)。
"""
from __future__ import annotations

#: Tab ① 總經載入表單的送出鈕 —— **字是動態的**,依 `st.session_state.macro_done`。
#: 快覽卡網格只在 `macro_done` 為真時渲染,故卡片的「去哪補」**必須**指名下面那個
#: `AGAIN` 版本;指名 `FIRST` 版本 = 指一個當下不存在的按鈕。
MACRO_LOAD_BTN_FIRST: str = "📡 載入總經資料"
MACRO_LOAD_BTN_AGAIN: str = "🔄 更新總經資料"

#: 同一個表單內的「強制重抓」**勾選框**(不是按鈕:勾完還要按送出鈕才會跑)。
MACRO_FORCE_REFETCH_CHECKBOX: str = "🆕 強制重抓最新（清快取）"

#: 側欄的全域刷新按鈕(清所有快取 + 落地檔)。
SIDEBAR_GLOBAL_REFRESH_BTN: str = "🧹 全域刷新（清所有快取 + 落地檔）"

#: ⑤ 設定與診斷的兩顆刷新鈕。
DATA_GUARD_HOT_MONEY_BTN: str = "📥 立即更新外資 / USDTWD"
DATA_GUARD_RELOAD_MACRO_BTN: str = "🔄 重新載入總經"

#: ⑥ 持倉體檢的「逐檔體檢表」**區塊抬頭**（`st.markdown("#### …")`）。
#:
#: ⭐ **它為什麼在這裡（2026-09-09，被 CI 逼出來的，不是預先設計的）**
#: 本模組開頭那句「**渲染控制項與撰寫「去哪補」文案讀同一份**」講的正是這個處境：
#: ⑥ 有**兩塊灰態**（組合健康總分、結論層的「判不出來」那一群）把使用者指向這張表，
#: 而抬頭本身由 :func:`ui.views.page_02_health._render_health_table` 畫出來 ——
#: **同一個字串三個地方**，其中兩份沒有任何機制會在第三份改字時跟著改。
#:
#: ⚠️ **這一筆是 `tests/test_batch2_top_card_grid.py::
#: test_every_where_names_something_that_exists_on_screen` 在 CI 上抓到的**：
#: 當時那句指路寫的是「下方『**② 依據**』的逐檔體檢表」，
#: 而畫面上那個標題的全名是「**🧾 ② 依據 — 憑什麼這樣說**」——
#: **名字對不上，使用者照著找會找不到。**
#: ⛔ 修法**不是**把標題改短去迎合文案（標題是照客戶拍板的線框寫的），
#: **也不是**把它加進 `WHERE_NAME_EXEMPT`（那條逃生路要求寫出
#: 「為什麼這個位置是對的」，而這裡的理由只會是「還沒修」）——
#: **是讓指路指到一個真的存在、而且三邊同源的名字。**
#:
#: ⚠️ **「② 依據」那一層本身刻意不收進本模組**：它是**閱讀分層的標題**，
#: 不是使用者要去操作的控制項；而本模組開宗明義「**只收被『去哪補』文案指名的控制項**，
#: 不是全站按鈕字典」。指路要指的是**那張表**（他要看的東西），不是那一層。
HOLDINGS_HEALTH_TABLE_BLOCK: str = "逐檔體檢表"
