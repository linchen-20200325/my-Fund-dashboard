"""ui/helpers/fund_grp_health/columns.py — 健診大表 / 批次大表**共用** column_config SSOT。

問題(2026-08-06 稽核 🔴 必修 4):組合健診大表(Tab②)有一份 40+ 欄的
`TextColumn(width=)` + `help=` 設定;**批次大表(Tab③)用同一組欄位卻裸 render**
(`st.dataframe(df, ...)` 無 column_config)—— 48 個中文欄名全無 tooltip,而最長的
「備註」欄放的是**唯一的失敗原因揭露**(§1),實測會被截斷且不換行,使用者看到
「NameResolut…」無從判斷該不該按「🔁 重試失敗檔」。

本模組把設定抽成純函式,兩張表同源:
- `base_column_config()`      ③ 實際購買結果(持有 meta / 全期實際·年化 %)
- `health_column_config()`    ① 健康分析 + σ/捕捉/換標/景氣/匯率(原 `_health_cfg`)
- `dividend_column_config()`  ② 配息相關(原 `_div_cfg`)
- `extra_column_config()`     σ位階 / 買賣點 / 風險對比等原本**沒有** cfg 的欄
- `batch_column_config()`     批次專屬:狀態 / 備註 / 淨值日期 / 淨值新鮮度
- `unified_column_config(batch=)` 以上合併(dict 後者覆蓋前者)

⚠️ 只放「顯示設定」,不含任何門檻運算 —— help 字串裡的數字一律從
`shared/*` SSOT import 進來格式化,禁止寫死(§3.3)。

⚠️ **help 的讀者是完全不懂基金與總經的人**(user 原則「多做說明」)。
本檔的 help 字串一律白話,**不得**出現:憲法章節號、內部版號、函式名 / 內部欄位名、
外站頁面代號。要留給開發者的技術脈絡請寫在**註解**裡(註解不會被使用者看到)。
由 `tests/test_help_plain_language.py` 掃描把關 —— 那條測試讀的是**執行期產生的
help 字串**,不是原始碼,所以註解可以照常寫技術細節。
"""
from __future__ import annotations


def _cc():
    from streamlit import column_config
    return column_config


def base_column_config() -> dict:
    """③ 實際購買結果欄(原 `tab_fund_grp_health._render_health_table` 內的 `_col_cfg`)。"""
    cc = _cc()
    return {
        "code": cc.TextColumn("代號", width="small"),
        "基金名": cc.TextColumn("基金名", width="medium"),
        # MoneyDJ 幣別欄的來源頁是 wb05(基本資料);頁面代號寫在註解,不寫進 help。
        "ccy": cc.TextColumn("幣別", width="small",
            help="這檔基金用哪種錢計價(USD 美元 / TWD 台幣 …),取自 MoneyDJ 基本資料頁的"
                 "「計價幣別」。抓不到 → 這一檔整列標失敗,**不會**猜成美元。"),
        "幣別偵測": cc.TextColumn("幣別偵測", width="small",
            help="「自動」= 由 MoneyDJ 的計價幣別欄判定。目前只有這一條路,沒有人工指定。"),
        "fx_spot": cc.NumberColumn("FX", format="%.4f", width="small",
            help="匯率:1 元原幣可以換到幾元台幣(現在的即期匯率)。台幣計價的基金固定 1.0000。"),
        "principal_ccy 🧮": cc.NumberColumn("原幣本金 🧮", format="%,.0f",
            help="台幣本金 ÷ 買進當天的匯率 = 換成原幣後的本金。"
                 "🧮 表示這是本站幫你換算的,不是官方欄位。"),
        "units 🧮": cc.NumberColumn("單位 🧮", format="%,.2f",
            help="原幣本金 ÷ 買進當天的淨值 = 你手上持有幾個單位(基金的「股數」)。"),
        "配息次數": cc.NumberColumn("配息次數", format="%d", width="small",
            help="買進日之後、MoneyDJ 有記錄的配息**次數**(不是「每年配幾次」)。"),
        "累積 TWD 配息 🧮": cc.NumberColumn("累積 TWD 配息 🧮", format="%,.0f",
            help="每一次配息 × 持有單位 × **當時**的匯率,全部加起來 = 至今總共領到多少台幣。"),
        "年均配息 TWD 🧮": cc.NumberColumn("年均配息 TWD 🧮", format="%,.0f",
            help="上面的累計台幣配息 ÷ 持有年數 = 平均一年領到多少台幣。"),
        # v19.180:全期實際(不年化,短歷史也顯示真實累計值)
        "配息率% (全期實際)": cc.NumberColumn(
            "配息率% (全期實際)", format="%.2f %%",
            help="從買進日到現在,配息總共領了本金的百分之幾(**不**換算成每年)。"
                 "持有時間很短也照顯示真實累計數。不參與燈號判定。"),
        "淨值% (全期實際)": cc.NumberColumn(
            "淨值% (全期實際)", format="%.2f %%",
            help="從買進日到現在,淨值累計漲跌了百分之幾(**不**換算成每年)。"
                 "持有時間很短也照顯示真實累計數。不參與燈號判定。"),
        "含息% (全期實際)": cc.NumberColumn(
            "含息% (全期實際)", format="%.2f %%",
            help="把上面兩欄加起來:淨值漲跌 + 配息,就是這段期間真正賺賠了百分之幾"
                 "(**不**換算成每年)。不參與燈號判定。"),
        # v19.148/v19.180:年化 3 軸(< 0.5 年顯示 None,避免幻象);verdict 仍走 1Y  SSOT
        "配息率% (年化)": cc.NumberColumn(
            "配息率% (年化·對買進成本)", format="%.2f %%",
            help="(累積配息 / **買進時的本金** / 持有年數)× 100。需持有 ≥ 0.5 年才顯示。"
                 "⚠️ 與「年化配息率 %」欄**除的東西不一樣**:本欄除的是你當初的買進成本,"
                 "那一欄(MoneyDJ 官方公布值)除的是**現在的淨值**。不參與燈號判定。"),
        "淨值% (年化)": cc.NumberColumn(
            "淨值% (年化)", format="%.2f %%",
            help="淨值累計漲跌 ÷ 持有年數 = **平均每年**漲跌百分之幾。需持有 ≥ 0.5 年才顯示"
                 "(太短就換算成年,數字會被放大成假象)。不參與燈號判定。"),
        # v19.492 正名(顯示層,dict key 不動):此欄是**全期報酬 ÷ 持有年數**,非最近一年 →
        #   舊 header「含息%(年化)」與健診摘要表「1Y 含息 %」(近 12 月實際)易混,改「全期年化」。
        "含息% (年化)": cc.NumberColumn(
            "含息% (全期年化)", format="%.2f %%",
            help="**全期**含息報酬 ÷ 持有年數 = 這段持有期間**平均每年**真正賺賠百分之幾。"
                 "⚠️ 這**不是最近一年** —— 與健診摘要表的「1Y 含息 %」(近 12 個月實際)不同,"
                 "持有 3-5 年的基金兩者可能差很大,跨檔比較別拿錯欄。需持有 ≥ 0.5 年才顯示。不參與燈號判定。"),
        "吃本金燈號 (1Y · )": cc.TextColumn(
            "吃本金燈號 (1Y · )", width="medium",
            help="「配息是不是在挖你自己的本金」:拿**近一年含息報酬**和 MoneyDJ 官方公布的"
                 "**年化配息率**比 —— 賺的比配的少,等於配息有一部分是把本金退還給你。"
                 "與下方「健診摘要表」用同一套算法,兩處不會給出不同結論。"),
        # v19.153: 3-3-3 原則(長線核心資產輔助)
        # 3 年平均年化的推導:metrics.ret_3y(3 年累計 %)開立方根 → (1+R)^(1/3)-1。
        # 內部欄位名寫在註解,help 只講白話。
        " 3-3-3 篩": cc.TextColumn(
            " 3-3-3 篩", width="medium",
            help="挑「長期抱得住的核心基金」用的門檻:成立滿 3 年以上,而且過去 3 年"
                 "**平均每年**賺超過 7%。✅ 兩項都過 / ❌ 有一項明確沒過 / "
                 "⬜ 資料不夠(查不到成立日或查不到 3 年報酬,**不算它沒過**)。"
                 "「平均每年」是把 3 年的累計報酬開立方根攤平算出來的。"
                 "這欄只是長線選股輔助,**不是**吃本金的主判定。"),
        # v19.492:雙演算法對帳旗標(worst-of-3)—— 個基深掘頁本來就有逐項 chip,大表補上決策面。
        "對帳": cc.TextColumn("對帳", width="small",
            help="**這檔的 Sharpe / 1Y報酬 / 配息率,自己算的和 MoneyDJ 官方值對不對得起來?**"
                 "取三項裡最差的一態:⚠️ 有矛盾 = 至少一項自算與官方差很多(別直接信那個數字,"
                 "點進單一基金深掘頁看是哪一項、差多少)/ ✅ 一致 = 對得起來 / ✅ 部分 = 有的對得起來、"
                 "有的只有單一來源無從對照 / ⬜ 單源 = 三項都只有一個來源,無法交叉驗證 / ⬜ — = 沒有指標。"),
        "倉位": cc.TextColumn("倉位", width="small",
            help="MoneyDJ 頁面上直接標示的建議倉位,原樣顯示,本站不重算。"),
        "最高經理費%": cc.TextColumn("最高經理費%", width="small",
            help="公開說明書寫的經理費**上限**,原文照抄。"
                 "刻意不轉成數字 —— 有些基金寫的是區間,硬轉會讓你誤讀成單一值。"),
        "配息頻率": cc.TextColumn("配息頻率", width="small",
            help="多久配一次息(月配 / 季配 / 半年配 / 年配)。優先用本站從實際配息記錄"
                 "歸納出來的頻率;歸納不出來時,直接顯示 MoneyDJ 頁面上的原文。"),
        "換匯資訊 🧮": cc.TextColumn("換匯資訊 🧮", width="medium",
            help="買進當下 100 萬台幣換到多少原幣、用的是哪一天的匯率 —— "
                 "留下換匯痕跡,方便你回頭核對績效裡有多少是匯率造成的。"),
    }


def health_column_config() -> dict:
    """① 健康分析 + σ 捕捉 / 換標 / 景氣 / 匯率欄(原 `_health_cfg`)。"""
    cc = _cc()
    from shared.signal_thresholds import (  # v19.419 捕捉率 help 文字用(SSOT,非 magic)
        CAPTURE_MIN_MONTHS as _CAP_MIN,
        CAPTURE_ROBUST_MONTHS as _CAP_ROB,
        GRADE_CUTOFFS_4D as _GRADE_CUT,
        ROTATION_BUY_SIGMA as _ROT_BUY,
        ROTATION_SELL_SIGMA as _ROT_SELL,
    )
    # 換標策略分 help 文字用的綠燈門檻(SSOT,不在 help 字串寫死,§3.3)
    from shared.switch_thresholds import SWITCH_GREEN_SCORE as _SW_GREEN
    # Sharpe 自算樣本門檻(help 文字要照實說「自算需 ≥ N 筆」,不寫死數字)
    from services.fund_service import MIN_OBS_SHARPE_SORTINO as _MIN_SS
    # 稽核 F12「淨值樣本」欄的 ⚠️ 切點 — 與 _nav_sample_label 同源(§3.3 禁 inline)
    from shared.signal_thresholds import NAV_SHORT_WINDOW_MAX_DAYS as _NAV_SHORT_MAX
    # Layer 3-C「評分覆蓋」欄:最少幾維才給等第(SSOT,不在 help 寫死數字)
    from shared.signal_thresholds import GRADE_4D_MIN_FACTORS as _GRADE_MIN_F
    return {
        "code": cc.TextColumn("代號", width="small"),
        "基金名": cc.TextColumn("基金名", width="medium"),
        # v19.327:核心/衛星資產分類(類別 +  3-3-3 兩層,見「分類依據」欄)
        "基金類別": cc.TextColumn("基金類別", width="small",
            help="MoneyDJ 頁面上寫的投資標的 / 基金類型原文,是判定核心或衛星的依據之一。"),
        "核心/衛星": cc.TextColumn("核心/衛星", width="small",
            help="這檔在你的組合裡該扮演什麼角色。🟦 核心 = 分散、穩、可以放大部位長抱;"
                 "🟠 衛星 = 集中在某個主題或產業、波動大,只適合放小部位;"
                 "⬜ 待定 = 基金類別和 3-3-3 兩條路都判不出來(**不亂扣**)。"),
        "分類依據": cc.TextColumn("分類依據", width="small",
            help="上一欄是怎麼判出來的:「類別」= 看基金類型的關鍵字;"
                 "「3-3-3」= 通過長線篩選所以算核心;「—」= 資料不足。"),
        # 稽核 F12(2026-08-14):右邊 Sharpe / σ / Max DD / 3Y 5Y 年化留白時,
        # 表上原本沒有任何一欄能讓使用者分辨「這檔沒這個風險」與「樣本不夠算」。
        "淨值樣本": cc.TextColumn("淨值樣本", width="small",
            help="這一列右邊所有數字,是拿**幾筆淨值、橫跨幾天**算出來的。"
                 f"⚠️ = 跨度不到 {_NAV_SHORT_MAX} 天 —— 這種通常是只抓到網站首頁的"
                 "「近 30 日淨值表」,不是這檔真的只有一個月歷史。"
                 f"此時 Sharpe(自算需 ≥ {_MIN_SS} 筆)、σ、Max DD、3Y / 5Y 年化 "
                 "**會整批留白**,那是樣本不足,**不代表這檔沒有波動或沒有風險**;"
                 "同列的 4D Score 仍會用少數幾個面向給分,別把它當完整體檢。"
                 "⬜ = 連淨值序列都沒拿到,整列數字都不可用。"),
        # Layer 3-C(2026-08-14):等第旁邊必須看得到分母。
        # `GRADE_4D_MIN_FACTORS` 表示湊得出 N 維就給等第 —— 沒有這一欄,
        # 一個 2/5 撐起來的 A 和一個 5/5 的 A 在表上完全同形。
        "評分覆蓋": cc.TextColumn("評分覆蓋", width="medium",
            help="右邊那個等第是**靠幾個面向算出來的**。"
                 "健康度看 5 件事:配息夠不夠、風險換到多少報酬、走勢、波動大小、匯率風險。"
                 f"✅ = 五項齊全;⚠️ = 只有部分(括號寫缺哪幾項,最少 {_GRADE_MIN_F} 項就會給等第);"
                 "➖ = 台幣計價,**本來就沒有匯率風險**(不是缺資料,分母已扣掉那一項)。"
                 "**⚠️ 的等第請當參考就好** —— 兩項算出來的 A 和五項算出來的 A "
                 "在這一欄之外看起來一模一樣。"),
        # 分數切點走 shared/signal_thresholds.GRADE_CUTOFFS_4D(SSOT,不在 help 寫死)
        "4D Grade": cc.TextColumn("4D Grade", width="small",
            help=(f"把右邊的 4D Score 換成好懂的等第:A ≥ {_GRADE_CUT[0]} 分、"
                  f"B ≥ {_GRADE_CUT[1]}、C ≥ {_GRADE_CUT[2]}、D ≥ {_GRADE_CUT[3]}、"
                  f"F 低於 {_GRADE_CUT[3]}。「—」= 有效資料太少,不給評等。")),
        "4D Score": cc.NumberColumn("4D Score", format="%.1f", width="small",
            help="健康度總分 0-100,由 4 個面向加權:賺多少(報酬)、配息有沒有賺回來"
                 "(配息覆蓋)、承受一分波動換到多少報酬(Sharpe)、波動有多大(σ)。"
                 "**某個面向沒資料就留白,不會拿 0 分充數**(0 分會被誤讀成「很差」)。"),
        # v19.496 A1:同類相對品質分(peer-relative,≠ 4D Score 的絕對分)
        "相對品質分": cc.TextColumn("相對品質分", width="medium",
            help="**這檔在同類裡的相對品質**(0-100,越高越前段)。6 個面向各自排名再加權:"
                 "報酬 25(同類官方大母體排名優先)+ Sharpe 25 + 下檔波動 20 + 操盤 10 + "
                 "配息可持續 10 + 費用率 10。⚠️ 這是**相對分、不是絕對分** —— 「本組 N 檔」是指這"
                 "一組同伴裡的位置(健檢/帳本=你的持倉＋選股池、批次=你上傳那批);「・僅參考」= 同組"
                 "少於 4 檔、樣本太少。某個面向沒資料就退出計算、不會拿 0 分充數。"
                 "**紅線(吃本金 / 3-3-3)不算進這一分**,要另外看。"),
        # ⚠️ 原 help 斷言「自計算(NAV 序列);**非** MoneyDJ 公布值」—— 與事實相反:
        #    `services/fund_service.py` 的 `_sharpe_out` 優先序是
        #    **wb07 一年 > wb07 六個月 > 本地自算**,只要 MoneyDJ wb07 有值(境外基金常態)
        #    這欄顯示的就是官方公布值;wb07 **六個月**命中時欄名還寫死「1Y」。
        #    → 標籤去掉硬掛的 1Y,期間/來源改由旁邊的「Sharpe 來源」欄逐檔照實顯示。
        "Sharpe 1Y": cc.NumberColumn("Sharpe", format="%.2f",
            help=("**每多承受一分波動,換到多少報酬**,越高越划算。"
                  "取值順序:**MoneyDJ 官方風險表(一年期)> 官方風險表(六個月期)> 本站自算**"
                  f"(自算需要至少 {_MIN_SS} 筆淨值)。這一檔實際用哪一個,看右邊"
                  "「Sharpe 來源」欄。⚠️ 同一欄可能**混不同長度的期間**(六個月不等於一年),"
                  "跨檔比大小前先看來源。4D 評分與換標策略分都吃這個值。")),
        # 值由 unified.sharpe_provenance_by_code 產出;顯示字串的 SSOT 是該模組頂部的
        # SHARPE_SRC_* 常數(2026-08-10 已白話化,不再印外站頁面代號)。
        # 本 help 是那組常數的說明書,改常數請同步改這裡。
        "Sharpe 來源": cc.TextColumn("Sharpe 來源", width="small",
            help=("左邊那個 Sharpe **到底是誰算的、量了多長的期間**。"
                  "開頭寫「MoneyDJ 官方」= 直接用官方風險表公布的現成數字"
                  "(後面接量測期間:1 年 / 6 個月;⚠️ 只有 6 個月那種期間比別檔短,"
                  "跨檔比大小要小心);開頭寫「自算」= 官方沒給,本站用最近 N 個交易日的"
                  "淨值自己算(後面接實際天數);⬜ — = 完全沒有值。"
                  "本欄照實顯示,不重算、不猜。")),
        "Sortino": cc.NumberColumn("Sortino", format="%.2f",
            help="和 Sharpe 同一個概念,但**只罰下跌的波動** —— 漲得兇不扣分,跌得兇才扣分。"
                 "越高越好;沒資料 → 留白。"),
        "Calmar": cc.NumberColumn("Calmar", format="%.2f",
            help="平均每年賺的 ÷ 期間內最深的那一次跌幅。越高 = 你每忍受一分套牢,換到越多報酬。"),
        "Alpha %": cc.NumberColumn("真實收益 %", format="%.2f %%",
            help="含息報酬率 − 年化配息率 = **配息之外真正多賺的部分**。"
                 "⚠️ 這不是教科書上那個對照大盤的 Alpha,只是本站的自訂算法。"),
        "費用率 %": cc.NumberColumn("費用率 %", format="%.2f %%",
            help="每年會從你的錢裡扣掉的總費用比例(MoneyDJ 揭露值);查不到總費用率時"
                 "退而顯示經理費。越低越好。"),
        "Max DD %": cc.NumberColumn("Max DD %", format="%.2f %%",
            help="期間內從最高點跌到最低點的最大跌幅(負值)—— 「最慘的時候會賠多少」。"
                 "淨值資料太短算不出來 → 留白,此時換標策略分會把這一項排除在分母外。"),
        "3Y 年化 %": cc.NumberColumn("3Y 年化 %", format="%.2f %%",
            help="過去三年**平均每年**賺多少。本站算不出來時,改用 MoneyDJ 官方績效表的"
                 "三年期累計報酬開立方根換算。左邊「 3-3-3 篩」吃這個值,"
                 "抓不到時 3-3-3 顯示 ⬜ 資料不足(**不會**直接判它沒過)。"),
        "5Y 年化 %": cc.NumberColumn("5Y 年化 %", format="%.2f %%",
            help="過去五年**平均每年**賺多少(本站算不出來時,改用 MoneyDJ 官方績效表的"
                 "五年期累計報酬換算)。"),
        # 「 3-3-3」(services/health/report.build_health_analysis_row 的欄)在大表被
        # unified._UNIFIED_FRONT 的「 3-3-3 篩」取代 → 這裡曾有一份 0 consumer 的
        # column_config(永遠被 `k in df.columns` 濾掉)。已移除,避免留著假裝有揭露。
        # 該欄目前唯一的 production 消費者是 Tab2 的 metric 卡(不吃 column_config)。
        # v19.414 經理人操作能力;v19.419 放寬門檻 6→3(help 註明參考值,§1 誠實)
        "上檔捕捉%": cc.NumberColumn("上檔捕捉%", format="%.1f %%",
            help=("**大盤上漲的那些月份**,這檔基金跟上了幾成(基金漲幅 ÷ 大盤漲幅 × 100)。"
                  "越高 = 行情好時越追得上。"
                  f"要漲的月份和跌的月份**各**至少 {_CAP_MIN} 個月才算得出來;"
                  f"只有 {_CAP_MIN}–{_CAP_ROB - 1} 個月時樣本太少,只能當參考。")),
        "下檔捕捉%": cc.NumberColumn("下檔捕捉%", format="%.1f %%",
            help="**大盤下跌的那些月份**,這檔基金跟著跌了幾成。"
                 "越低 = 行情差時越抗跌(這欄和上檔捕捉% 相反,低才是好)。"),
        "操盤評分": cc.NumberColumn("操盤評分", format="%d",
            help=("把上檔、下檔捕捉率合成一個 0–100 分,看**經理人會不會做**"
                  "(追漲追得上、殺盤又跌得少 → 高分)。"
                  f"要漲、跌月份**各**至少 {_CAP_MIN} 個月;"
                  f"只有 {_CAP_MIN}–{_CAP_ROB - 1} 個月時可信度低。"
                  "**跨檔比大小前務必先看右邊「捕捉樣本」欄** —— 只有 3 個下跌月算出的 92 分,"
                  "和 40 個下跌月算出的 92 分,在這一欄長得一模一樣。")),
        # 捕捉率樣本旗標:低信心必須逐列可見,不能只寫在欄位 help(缺料須帶旗標)
        "捕捉樣本": cc.TextColumn("捕捉樣本", width="small",
            help=(f"左邊三欄**實際上是用幾個月算出來的**:✅ = 漲、跌月都有 {_CAP_ROB} 個月"
                  f"以上(夠穩);⚠️ = 有一邊只有 {_CAP_MIN}–{_CAP_ROB - 1} 個月"
                  f"(樣本少、雜訊大,只能參考);⬜ = 不到 {_CAP_MIN} 個月或找不到可比的大盤"
                  " → 左邊三欄留白。")),
        # v19.420 vs 大盤%(近1Y純價格報酬差;純淨值對純指數,公平不含息)
        "vs 大盤%": cc.NumberColumn("vs 大盤%", format="%+.1f %%",
            help=("近一年這檔比大盤多賺(正)或少賺(負)幾個百分點。"
                  "台幣計價的比台股大盤、美元計價的比 S&P 500。"
                  "兩邊都只看價格漲跌、都不算配息,所以是公平的比法。"
                  "⚠️ **不是台幣也不是美元計價的基金一律留白** —— 用歐元基金的報酬去減"
                  "美股指數的報酬,等於把匯率漲跌算成經理人的功勞,寧可不給也不給錯的。"
                  "兩邊重疊的歷史不到一年 → 改用全部重疊期間,見右邊「vs 大盤期間」欄。")),
        "vs 大盤期間": cc.TextColumn("vs 大盤期間", width="small",
            help=("左邊那個「vs 大盤%」**實際上量的是多長的期間**:近 1 年 / "
                  "⚠️ 全期(兩邊重疊的歷史不到一年,**這種跨檔不能直接比大小** —— "
                  "3 個月的 +14% 和整整一年的 +14% 不是同一件事)/ ⬜ 無(留白)。")),
        # 高/低基期切點走 shared/signal_thresholds 的輪動 σ SSOT(不在 help 寫死數字)
        "基期": cc.TextColumn("基期", width="small",
            help=("現在的淨值離「這段期間的最高點」有多遠,用波動的倍數(σ)來量:"
                  f"🔴 高基期(σ ≥ {_ROT_SELL:+.1f},貼近高點、現在買偏貴)/ ⚪ 中性 / "
                  f"🟢 低基期(σ ≤ {_ROT_BUY:+.1f},跌得夠深,有機會回升)/ ⬜ 資料不足。"
                  "⚠️ 與上方「🎯 選基金(低基期)」篩選器**不是同一套定義**:那邊用的是"
                  "**正數的下跌深度**、倍數還可以自己調,本欄是**負數**、而且門檻固定,"
                  "所以同一檔可能一邊標 ✅ 一邊標 ⚪。"
                  "淨值完全不動(多半是停售)→ ⬜,不會被當成高基期。"
                  "點欄名可排序,一次挑出所有高基期或低基期的標的。")),
        # v19.423 換標決策(策略燈號 + 策略分;獨立於 4D,專為買賣/換標設計)
        "策略燈號": cc.TextColumn("策略燈號", width="small",
            help=("這檔該不該換掉的紅綠燈。🔴 賣出 / 轉換(近一年含息是負的、而且 Sharpe 也"
                  "是負的;或嚴重在吃本金)/ 🟡 觀望 / "
                  f"🟢 續抱、可考慮加碼(分數 ≥ {_SW_GREEN}、吃本金燈號健康,"
                  "**而且四個面向的證據都齊全**)/ ⬜ 資料不足。"
                  "⚠️ 缺「最大跌幅」或「vs 大盤%」時,**把缺的面向全部當 0 分算,仍然要 ≥ "
                  f"{_SW_GREEN}** 才給綠燈 —— 證據不齊一律降成 🟡,不給加碼訊號。"
                  "可用篩選一次挑出所有紅燈的檔。")),
        "換標策略分": cc.NumberColumn("換標策略分", format="%d",
            help=("專門為「該不該換掉」設計的 0–100 分,四個面向加起來:"
                  "近一年含息 35 分 + Sharpe 30 分 + 最大跌幅 20 分 + vs 大盤 15 分。"
                  "⚠️ **分母不是固定 100**:缺最大跌幅(淨值太短算不出)或缺 vs 大盤時,"
                  "那一項會**退出分母**再換算回 0–100(沒資料 ≠ 表現差),"
                  "所以 100 分有可能只是「有證據的 65 分裡拿滿 65 分」—— "
                  "實際分母是多少、缺哪幾項,請看右邊「策略分覆蓋」欄。"
                  "**這一分與左邊的 4D 健康度是兩套東西**;缺 Sharpe 或缺含息報酬 → 留白。"
                  "⚠️ 與**單一基金深掘頁**的「🎯 建議動作」是**不同引擎**:那邊吃 4D 品質 × "
                  "Z-Score 位階,本欄吃 報酬 / 風險 / vs大盤 加權 —— 同一檔兩頁可能給不同結論,別跨頁互比。")),
        "策略分覆蓋": cc.TextColumn("策略分覆蓋", width="medium",
            help=("左邊那個分數**是用幾分的滿分算出來的**(缺資料一定要講):"
                  "✅ 100/100 = 四項證據齊全;"
                  "⚠️ 65/100(缺 …) = 滿分被縮水了,分數只反映有證據的部分,跨檔比大小要小心;"
                  "「·不給綠燈」= 缺的那幾項就算全拿 0 分也跨不過綠燈門檻 → 燈號已強制降 🟡;"
                  "⬜ 未評分 = 最關鍵的兩項(近一年含息 / Sharpe)都沒有。")),
        # 景氣適配:依資產屬性 + 捕捉能力對照當前景氣;參考傾向,非買賣建議
        "景氣適配": cc.TextColumn("景氣適配", width="small",
            help=("拿這檔的資產類型 + 抗跌 / 追漲能力,對照**現在的景氣位階**"
                  "(頁面最上方那個景氣字卡):✅ 順風(現在的環境對它有利)/ "
                  "⚠️ 逆風 / ⚪ 全景氣(核心、平衡型,哪個階段都還行)/ "
                  "⬜ 無法判定(查不到類別,或景氣位階還沒偵測出來)。"
                  "**只是傾向參考,不是買賣建議,也不是叫你配幾 %**。")),
        "適配傾向": cc.TextColumn("適配傾向", width="small",
            help="這檔基金的資產屬性最適合哪一段景氣(復甦 / 擴張 / 高峰 / 衰退);"
                 "「全景氣」= 平衡多重型,哪一段都還可以。"),
        # 淨值×匯率二維買賣切換(外幣基金;台幣計價 ➖)
        "匯率位階": cc.TextColumn("匯率位階", width="small",
            help=("現在的美元兌台幣匯率,和**近一年的平均**比起來算高還是低。"
                  "「台幣強」= 換美元比較便宜,現在進場划算;「台幣弱」= 換回台幣比較多,"
                  "現在出場划算;中間就是「中性」。"
                  "後面帶 `*` 表示可用的匯率資料較短,只能參考。"
                  "➖ = 台幣計價的基金不需要換匯;⬜ = 不是美元計價或缺資料。"
                  "**只是傾向參考,不保證買在低點賣在高點**。")),
        "淨值×匯率": cc.TextColumn("淨值×匯率", width="medium",
            help=("把「淨值貴不貴」和「匯率划不划算」兩件事合起來看:"
                  "🟢 兩邊都便宜(淨值低 + 台幣強 = 進場時機好)/ "
                  "🔴 兩邊都貴(淨值高 + 台幣弱 = 出場時機好)/ "
                  "⚪ 觀望(一好一壞,或都在中間)。"
                  "➖ = 台幣計價;⬜ = 無法判定。匯率比淨值更難預測 —— **不是買賣建議**。")),
    }


def dividend_column_config() -> dict:
    """② 配息相關欄(原 `_div_cfg`)。"""
    cc = _cc()
    return {
        "code": cc.TextColumn("代號", width="small"),
        "基金名": cc.TextColumn("基金名", width="medium"),
        "1Y 含息 %": cc.NumberColumn("1Y 含息 %", format="%.2f %%",
            help="近一年**真正賺賠**多少 %:淨值漲跌 + 領到的配息一起算。"
                 "這個數字是哪來的、可不可信,**一律看右邊「1Y 來源」欄** —— "
                 "數字大不代表好,曾經出現過台幣基金印 +200% 的情況"
                 "(抓錯幣別或除息切割造成),那種會在來源欄標 ⚠️。"),
        # 值來自 services/fund_total_return.compute_1y_total_return 的 source label;
        # 顯示字串的 SSOT 是該模組頂部的 SRC_* 常數(2026-08-10 已白話化)。
        # 本 help 是那組常數的說明書,改常數請同步改這裡。
        # 稽核 🟠-4/🟠-5(2026-08-14):新增 SRC_TOO_SHORT / SRC_IMPLAUSIBLE_SUFFIX
        # 兩態必須寫進來;width 由 small 改 medium —— 警語 18 個字掛在 small 欄上
        # 會被截掉,等於閘做了、旗標帶了,卻落在看不到的位置(同「備註」欄的教訓)。
        "1Y 來源": cc.TextColumn("1Y 來源", width="medium",
            help="左邊那個數字是怎麼來的,可靠度由高到低:"
                 "「MoneyDJ 官方」= 直接抄官方績效表,最可信;"
                 "「自算(還原含息淨值)」= 官方沒給,本站用淨值 + 配息記錄還原;"
                 "「自算含息」= 本站自己算的含息報酬(若括號寫「僅 N 天窗口」,"
                 "代表只有 N 天資料換算成一年,跨檔比大小要小心);"
                 "「自算(僅淨值,不含配息)」= 只算淨值漲跌、**沒把配息算進去**;"
                 "「自算(N 天資料外推年化)」= 資料最少的推估,最不可靠;"
                 "「績效表(來源未標註)」= 有數字但沒標來源,分不出官方還是自算;"
                 "「—(僅 N 天資料,不足以推算一年)」= **有淨值但歷史不到半年**,"
                 "不夠推算一年報酬所以左邊留白 —— 這不是「這檔沒資料」,"
                 "更不是「這檔沒賺錢」;"
                 "「—」= 完全沒資料。"
                 "另外,只要看到 **⚠️ 數值異常大** 的字樣,代表左邊那個數字超出"
                 "一般基金的合理範圍(通常是幣別抓錯或除息沒還原)—— "
                 "**先核對幣別再看結論,不要直接拿它排序挑基金**。"),
        "年化配息率 %": cc.NumberColumn("年化配息率 %(對現值)", format="%.2f %%",
            help="MoneyDJ 官方公布的年化配息率 —— 用**現在的淨值**當分母算的。"
                 "⚠️ 與「配息率% (年化)」欄除的東西不同,那一欄除的是你的買進成本,"
                 "所以兩個數字不一樣是正常的。"),
        # 每月配息金額(TWD 現金)= 最近一筆實配 × 持有單位 × 匯率(來源同「配息來源」欄)
        "每月配息 (TWD)": cc.NumberColumn("每月配息 (TWD)", format="%.0f",
            help="每個月大概可以領到多少**台幣現金** = 最近一筆配息 × 你的持有單位數 × 匯率。"
                 "健診分頁是全部基金都假設投入 100 萬台幣來比較;"
                 "「組合配置」分頁則用各檔實際投入的本金。"),
        # 每月配息單位數 = 最近一筆實際配息 × 持有單位 / NAV(真實記錄優先);
        # 真實記錄缺 → 年化配息率估算 fallback,「配息來源」欄註記真實/估算
        "每月配息單位數": cc.NumberColumn("每月配息單位數", format="%.2f",
            help="如果把每月領到的配息**再買回同一檔**,大約可以多買幾單位"
                 "(= 每月配息金額 ÷ 現在的淨值)。"
                 "優先用 MoneyDJ 的實際配息記錄算;沒有記錄時改用年化配息率估算"
                 "(是哪一種看「配息來源」欄)。健診分頁全部以 100 萬台幣為基準比較;"
                 "「組合配置」分頁用各檔實際投入本金。"),
        "配息來源": cc.TextColumn("配息來源", width="small",
            help="上兩欄的配息數字怎麼來的。「真實」= 用最近一筆實際配息記錄算,比較準;"
                 "「估算」= 查不到記錄,改用年化配息率 ÷ 12 平均攤到每個月 —— "
                 "季配 / 年配的基金某些月份其實是 0 元,估算值只是平均值,別當成每月一定領得到。"),
        "吃本金燈號 (1Y·)": cc.TextColumn("吃本金燈號 (1Y·)", width="medium",
            help="「配息是不是在挖你自己的本金」:近一年含息報酬比不上年化配息率,"
                 "就代表配給你的錢有一部分是本金退還。與健診大表同一套算法。"),
        "換標的建議": cc.TextColumn("換標的建議", width="medium",
            help="綜合 4 條檢查(報酬、配息、吃本金、跌幅)給的換不換結論;"
                 "把滑鼠移到格子上可以看到判定細節。"),
    }


def extra_column_config() -> dict:
    """σ 位階 / 風險對比 / 買賣點欄 —— 原本**完全沒有** column_config 的一批。

    值皆為 by-code data 函式預先格式化好的**字串**(缺 → '—'),故一律 TextColumn。
    """
    cc = _cc()
    return {
        "現價": cc.TextColumn("現價", width="small",
            help="最新一筆淨值(用基金自己的計價幣別,不是台幣)。"),
        "HWM": cc.TextColumn("HWM", width="small",
            help="過去約一年(252 個開盤日)裡出現過的**最高淨值**,也就是這段期間的天花板。"),
        "距 HWM %": cc.TextColumn("距 HWM %", width="small",
            help="現在的淨值離那個最高點還差幾 %。負數 = 目前在高點下方(也就是回檔了多少)。"),
        "σ rank": cc.TextColumn("σ rank", width="small",
            help="用「波動的倍數」來量現在離高點多遠:−1 代表跌了大約一個平常的波動幅度,"
                 "數字愈負 = 跌得愈深。"
                 "淨值完全不動(停售 / 清算)→ 顯示 '—',**不會給 0** "
                 "(0 會被誤讀成「就在高點附近」)。"
                 "⚠️ 本欄是**距 252 日高點的 σ**;單一基金深掘頁的位階用的是 **Z-Score(近 3 年常態)**,"
                 "口徑不同,兩頁可能給不同結論,別跨頁直接互比。"),
        "HWM 位階": cc.TextColumn("HWM 位階", width="medium",
            help="把左邊的數字翻成一句話:接近高點 / −1 倍波動可以觀察 / −2 倍波動可考慮加碼 / "
                 "−3 倍以上算深度超跌。開頭是 ⬜ 就是算不出來"
                 "(淨值不足 30 天、資料太少、或淨值完全沒波動)。"
                 "⚠️ 本欄與「基期」欄是**同一個 σ rank 換算的**,只是切點不同"
                 "(位階看 −1/−2/−3σ 三段、基期看高/中/低)—— 所以同一列可能「基期=中性」"
                 "但「位階=−1 觀察」,兩者**不矛盾、只是分得細不細**。"),
        "σ (年化%)": cc.TextColumn("σ (年化%)", width="small",
            help="一年上下震盪的幅度有多大(標準差),數字愈小愈穩。"
                 "優先用本站自己算的;算不出來才取 MoneyDJ 官方風險表的值。"),
        "Beta": cc.TextColumn("Beta", width="small",
            help="大盤動 1%,這檔大約跟著動幾 %(>1 比大盤兇、<1 比大盤溫和)。"
                 "取自 MoneyDJ 官方風險表,對照的是他們自己選的基準,本站不重算。"),
        "資產屬性": cc.TextColumn("資產屬性", width="small",
            help="買賣點模型把這檔歸成哪一類資產(股票 / 債券 / 平衡…);"
                 "還沒偵測出景氣位階時顯示 '—'。"),
        "操作訊號": cc.TextColumn("操作訊號", width="medium",
            help="買賣點模型依**現在的景氣位階**給的操作傾向;景氣還沒偵測出來 → '—'。"),
        "買 3 (深跌)": cc.TextColumn("買 3 (深跌)", width="small",
            help="第 3 段(最深)的分批買進參考價 —— 跌到這裡才進場的那一段。"),
        "買 1 (小跌)": cc.TextColumn("買 1 (小跌)", width="small",
            help="第 1 段(最淺)的分批買進參考價 —— 小回檔就可以開始佈局的那一段。"),
        "賣 1 (小漲)": cc.TextColumn("賣 1 (小漲)", width="small",
            help="第 1 段(最早)的分批賣出參考價 —— 小漲就可以開始減碼的那一段。"),
        "賣 3 (大漲)": cc.TextColumn("賣 3 (大漲)", width="small",
            help="第 3 段(最高)的分批賣出參考價 —— 漲到這裡建議收手的那一段。"),
        "現價位階": cc.TextColumn("現價位階", width="medium",
            help="現在的價格落在上面那五個參考價的哪一段;查不到淨值 → '—'。"),
        # v19.483 稽核 A1/A2:原本這欄**完全沒有** help,使用者看到「🟢 燈一(+20%)」卻無從
        # 得知它跟左邊「基期」是不同錨點、可能不一致。補上說明 + 交叉提醒。
        "加碼水位": cc.TextColumn("加碼水位", width="medium",
            help="老師「235 精準加碼」:用**週淨值**的 20 週布林 + 4/13/52 週均線 + VIX,判定"
                 "該加碼的水位 —— ⚪ 巡航=定期定額照常 / 🟢 燈一(小跌)🟡 燈二(急跌)🔴 燈三(深水)"
                 "=跌越深加越多 / 💰 停利=超漲(布林上軌)。⚠️ 它的錨點是**近 20 週均值**,和左邊"
                 "「基期 / σ rank」的**一年 HWM** 不同,回答的是不同問題(基期看離年度高點多遠、"
                 "加碼水位看近期急跌),兩者**可能不一致、且都對**。週資料 < 20 週 → ⬜ 資料不足(不硬給)。"),
    }


def batch_column_config() -> dict:
    """批次大表專屬欄:狀態 / 備註 / 淨值日期 / 淨值新鮮度。

    「備註」是**唯一的失敗原因揭露**(§1),必須給足寬度,否則
    「NameResolutionError…」被截成看不出是網路問題還是基金停售,
    使用者無從判斷該不該按「🔁 重試失敗檔」。
    """
    cc = _cc()
    return {
        # 稽核 A1(2026-08-14):補第四態「⚠️ 部分成功」。
        # 舊 help 只寫三態,但 unified.py 遇到 ①~④ 欄組算爆時原本是**無條件**標
        # 「✅ 成功」—— 那一列在 400 列裡看起來跟正常列一模一樣,使用者只會讀成
        # 「這檔沒有這些資料」並據此換標。現在改標「⚠️ 部分成功」,help 同步說明。
        "狀態": cc.TextColumn("狀態", width="small",
            help="✅ 成功 / ⚠️ 部分成功 / ❌ 抓取失敗 / ⚠️ 代號無效。"
                 "抓失敗的檔**仍然完整留在表裡**,數值欄留白 —— 不會偷偷丟掉,也不會填 0 充數。"
                 "「⚠️ 部分成功」= 淨值有抓到,但某一組欄位(健康分析 / 配息 / σ 風險 / "
                 "策略燈號)算到一半出錯而留白 —— **留白不代表這檔沒有這項特性**,"
                 "缺哪一組寫在「備註」。"),
        "備註": cc.TextColumn("備註", width="large",
            help="這一檔為什麼失敗、或哪幾組欄位沒算出來(原文最多 100 字)。"
                 "看起來像連不上網站 / 找不到網址 → 多半是暫時性的,可按「🔁 重試失敗檔」;"
                 "寫「淨值抓不到」/「幣別未知」/ 被網站擋下(403)→ 多半是這檔已停售,"
                 "或該保單專屬網頁擋了我們,**重試通常沒用**;"
                 "寫「以下欄組計算失敗、留白」→ 該列狀態是「⚠️ 部分成功」,"
                 "**看數字前先讀這裡列的欄組名**。"),
        "淨值日期": cc.TextColumn("淨值日期", width="small",
            help="MoneyDJ 上這檔**最新一筆淨值是哪一天**的(不是我們去抓資料的時間)。"),
        "淨值新鮮度": cc.TextColumn("淨值新鮮度", width="medium",
            help="今天(台北時間)距離上面那個淨值日期幾天。"
                 "🟢 / 🟠 = 正常 —— 基金淨值本來就慢個 1~3 天才公布,週末假日不更新也正常;"
                 "🔴 = 明顯太舊,**這檔很可能已經停售或清算**。"
                 "此時同一列的基期、操盤評分、策略燈號全都是用**已經不動的舊淨值**算出來的,"
                 "別當成現況。點欄名排序可以一次挑出所有 🔴。"),
    }


def unified_column_config(*, batch: bool = False) -> dict:
    """健診大表(batch=False)/ 批次大表(batch=True)的完整 column_config。

    合併順序:base(③)→ health(①)→ dividend(②)→ extra(σ/)→ batch 專屬。
    後者覆蓋前者;`code` / `基金名` 三份定義相同故無爭議。
    呼叫端仍需自行 `{k: v for k, v in cfg.items() if k in df.columns}` 過濾。
    """
    cfg = {
        **base_column_config(),
        **health_column_config(),
        **dividend_column_config(),
        **extra_column_config(),
    }
    if batch:
        cfg.update(batch_column_config())
    return cfg
