"""⑤ 設定與診斷 —— **(A) 路線的委派殼**：只做版面與編排，功能一律呼叫既有舊模組。

客戶方針（2026-09-06，(A) 路線拍板）
------------------------------------
新五頁**只做版面呈現與互動排版**，寫入邏輯**原封不動呼叫既有舊模組**。

⚠️ **⑤ 是五頁裡唯一一頁在 (A) 拍板之前就寫成獨立重寫的**，所以本檔 2026-09-06
由「骨架 + 灰態 + 自寫 Form」改寫成**委派殼**。
**舊實作不是被刪掉，是被換成呼叫舊模組的 public 入口** —— 逐塊對照見下表。

⭐ **2026-09-08 新增「🧾 ① 結論」與「🧾 ② 依據」兩層**（客戶拍板線框
`docs/wireframes/draft-four-page-content.html` §3）—— 見下方 **(D-6)**。
下表的「順序」自本次起指的是 **② 依據之下的五塊**，它們的相對順序**一格未動**。

===== ====================== =====================================================
順序   線框區塊（`<h4>` 逐字）  本檔怎麼實現
===== ====================== =====================================================
0a     🧾 ① 結論              **本檔實作**（:func:`_render_conclusion`）——
                              一句話 ＋ 三張卡，**零外部呼叫**。見 (D-6)
0b     🧾 ② 依據              **本檔實作**（一行 :data:`EVIDENCE_HEADING`）——
                              線框 §1 四層閱讀順序的分界標題
1      資料來源健康度          **委派** `ui.tab5_data_guard.render_data_guard_tab()`
                              （前置 `_update_data_registry()`；整塊在 gate 之後）
2      NAV 累積狀態            **保留本檔實作**（總管裁決 2，理由見下方 (D-4)）
3      連線與金鑰              **委派** `render_policy_admin_bridge(sheet_client=None)`
                              ＋ `render_fetch_diag_from_session()`
4      手動補資料              **委派** `render_nav_manual_section()`（三條寫入路徑）
5      使用手冊                **委派** `ui.tab6_manual.render_manual_tab()`
6      🗄️ 資料維護與通報      **委派** `ui.tab_manage.render_manage_tab()`
                              **最底部的進階折疊區**（客戶 2026-09-07 裁決）
                              ⚠️ **線框沒有給它位置**，見下方 (D-5) 的登記
===== ====================== =====================================================

⚠️ **2026-09-07 順序變更（有意識的政策變更，不是漏刪 · 決策者：客戶）**
   舊順序 ~~「…4 手動補資料 →（4.5）🗄️ 資料維護與通報 → 5 使用手冊」~~
   —— 客戶 2026-09-07 逐字裁決：「🗄️ 資料維護與通報 決策【**保留**】在 ⑤ 的**最底部**
   作為**進階折疊區（`st.expander`）**。理由：系統維護與通報屬管理設定範疇，
   折疊收攏即可，不影響主視覺。」
   **舊擺法的理由仍然成立**（它跟著「從哪裡搬來」的檔案順序走，且不動線框五塊的相對序）；
   **被權衡掉的是它的視覺成本** —— 那一塊展開後很長，夾在「手動補資料」與「使用手冊」
   之間會把使用手冊推到很下面。
   ⛔ **這次裁決沒有推翻 (D-5) 的登記**：它裁的是「這一塊放在哪、怎麼收」，
   **不是**「線框裡有沒有它」。**(D-5) 照舊有效、照舊是登記在案的偏離。**
   ⛔ **也沒有動到線框五塊彼此的相對順序**（1→2→3→4→5 一格未變）。

線框同時釘死了本頁的**職責邊界**：

> 回答一個問題：**資料本身可不可信、要不要我補？** 平常不用進來；出事時第一個進來。

⛔ **因此本頁不放**（線框「這裡不放什麼」逐字）：
   「**快取與退避狀態不做成畫面**」「任何投資判斷 → **01 ~ 04**」。
   → 本檔**沒有任何快取／退避狀態的區塊**，由
   `tests/test_wf05_settings_skeleton.py::test_the_page_does_not_render_cache_or_backoff_state` 釘住。
   ⚠️ **這條只管本檔自己畫的東西** —— 被委派的 `render_data_guard_tab()` 內部有沒有
   快取相關的顯示，本檔管不到，也不該管（(A) 路線：舊模組原封不動）。

⭐ 五則總管裁決 —— 理由寫在這裡，是為了讓後人**能推翻它**
==========================================================

(D-1) 「使用手冊」那張卡的 `dim` **不是灰態**
---------------------------------------------
**實測依據**（`grep -n 'dim' docs/wireframes/ia-wireframe.html`，全檔僅 5 個命中）：
`:193/:198` 是 CSS 兩行；`:379`／`:552` 各配著一個 `灰態` chip；
**`:706`（使用手冊那一處）`card span3 dim` 沒有 `灰態` chip**，內文「**純文字**」。
→ `dim` 在這份線框裡承擔**兩種**意思：配 chip ＝「還沒接上」，沒配 chip ＝**視覺降權**。

⛔ **因此使用手冊不得畫成灰態佔位。** 自 (A) 路線起這一條**更沒有藉口** ——
   說明書的真內容（`ui/tab6_manual.py`，十章）**一直都在**，只是舊版沒有委派過去。

⚠️ **2026-09-07 雙軌期間的例外，就地登記（(D-1) 的理由一個字都沒有被推翻）**
（**有意識的變更，不是漏刪** · 決策者：**AI 總管** · 客戶 2026-09-07 雙軌並行原則）：
:func:`_render_manual` **加了 Checkbox Gate**，本頁的使用手冊**預設不顯示內容**。
**這不違反 (D-1)，因為 (D-1) 禁的是「假的未載入」** —— 東西明明出得來卻說出不來。
**雙軌期間它真的出不來**：舊 ⑤ 無條件畫同一份說明書，兩份一起畫會撞
`ui/tab1_macro.py::render_indicator_map` 那張**不帶 key** 的 `plotly_chart`，
使用者**零點擊**就會看到一則紅字（CI 實測，見 :func:`_manual_not_loaded_note`）。
灰態本文把「舊 ⑤ 已經有一份完整的」與「勾下去會怎樣」兩件事都寫在畫面上。
⛔ **代價要記，不要假裝沒有**：⑦ 的使用手冊在雙軌期間預設看不到內容 ——
   那是用「一則零點擊的紅字」換來的。**舊 ⑤ 一下架，四顆 gate 應一起拿掉。**

(D-2) 本頁**沒有頁面層級的空狀態**；空狀態只可能出現在單一區塊
--------------------------------------------------------------
**線框事實**：Tab 05 **沒有畫任何空狀態區塊**（實測全檔 `class="empty"` 唯一一處在
`:486`，落在 Tab 02 的 panel）。

**以下是總管的判斷，不是線框寫的 —— 寫在這裡讓後人能推翻**：
本頁**只有「NAV 累積狀態」可能真的空**；其餘四塊委派給舊模組，
**舊模組自己會處理它們的空／未設定狀態**（本檔不替它們判斷，那會變成第二份真相源）。

空狀態**只在「gate 勾了 → 後端已啟用 → 真的讀到、而且一筆都沒有」時出現**。
⛔ **不得**把它綁在 `portfolio_funds` 上：`coverage_status()` 讀的是**整張雲端 sheet**，
與那個 session 鍵毫無關係 —— 一個雲端已經累積三年的人會被告知「還沒列入任何基金」。
→ 守衛：:func:`~tests.test_wf05_settings_skeleton.test_the_empty_state_only_appears_after_a_successful_read`。

(D-3) 線框裡的示意值**一個都不准畫**
------------------------------------
`18 源 · 2 異常`／`42 檔 · 最長 6.2 年`／`正常` **全是線框示範版面用的假數字**。
NAV 那一塊現在會印**真的**「N 檔 · 共 M 筆」—— 那不是示意值，它有出處
（:func:`services.nav_history_gs.coverage_status` 回傳的逐檔點數）。
⛔ **但「最長 X 年」永遠不會回來**：不是因為它是線框的假數字，
而是因為 `span_days = last - first` **單獨出現就會說謊**（見 :func:`coverage_line`）。
⚠️ **黑名單的已知代價**：`_PINNED_FAKE_VALUES` 收了字面值 `"42 檔"`，
而本頁會印真的「N 檔」—— **若哪天 fixture 剛好是 42 檔，那條守衛會誤紅**。
它是**潛在風險，不是現行風險**（現行 fixture 檔數為 1 / 2 / 0）。

(D-4) ⭐ **區塊 2 不委派回舊模組 —— 舊的那一塊會印假數字**（總管裁決 2）
----------------------------------------------------------------------
舊的 NAV 累積狀態是 `ui.helpers.settings_diag.nav_history_section.render_nav_status_section()`
→ `ui.tab5_data_guard.render_nav_accumulation_status()`，它把 `coverage_status()` 回傳的
`span_days` **原封放進一張 DataFrame 的兩個欄位**：「涵蓋天數」與「≈年」。

**問題出在上游**：`services/nav_history_gs.py::coverage_status` 在日期 parse 失敗時
`_span = 0`（它自己那一行的註解就寫著「**跨度未知**，點數仍誠實回報」），
而 `norm_date_key()` **刻意讓壞日期的原字串通過**（「不靜默丟資料（§1：不猜）」）
—— 所以壞日期真的會走到畫面上。

**本組端到端實測（注入假 worksheet，非推論；指令與逐字輸出見 PR）**：

======================================  ==================  ==================
上游回傳                                  舊塊畫出來            真相
======================================  ==================  ==================
`BBB first=113/01/02 last=2025-06-01`   涵蓋天數 **0** · ≈年 **0.0**   **約 1.4 年**
`DDD first=last=2024-05-05`             涵蓋天數 **0** · ≈年 **0.0**   **真的 0 天**
======================================  ==================  ==================

**兩者在畫面上長得一模一樣。** 委派回舊的 ＝ 把一個**已知的假數字**重新放回線上，
直接違反客戶常設紅線與 `CLAUDE.md §1`（錯誤的數字比沒有數字更危險）。
→ 故本塊**保留本檔實作**：:func:`span_days_or_unknown` 用兩端自己重算並與上游對帳，
對不上就誠實回「算不出天數」。

⚠️ **這一塊是 (A) 路線的唯一例外，而且是刻意的** —— 它**不是**「順手重寫比較好看」，
   是「委派過去會說謊」。**任何其他塊都不得引用本例外來自行重寫。**
✅ **旗標粒度夠細，兩份 NAV 不會同時出現（本組實測，不是推論）**：
   `merge_context.NAV_HISTORY` 由本檔在委派 `render_manage_tab()` 與
   `render_data_guard_tab()` 時持有，而那兩支各自**只跳過自己那一塊 NAV**
   （`ui/tab_manage.py` 跳 `_sec_nav_backfill()`；`ui/tab5_data_guard.py` 跳
   `render_nav_accumulation_status()` ＋ `render_nav_statement_csv_import()`），
   **其餘照畫**。舊的 `render_nav_status_section()` 本檔**根本不呼叫**，
   所以那份假數字連渲染的機會都沒有。
   → 守衛：:func:`~tests.test_wf05_settings_skeleton.test_the_page_renders_exactly_one_nav_status_block`。

(D-5) ⚠️ **「🗄️ 資料維護與通報」在線框裡沒有位置 —— 登記，不吞掉**
------------------------------------------------------------------
線框 Tab 05 的「從哪裡搬來」**逐字列了 `ui/tab_manage.py`**，但五個 `<h4>` 裡
**沒有任何一塊叫得出「選股池」「除息行事曆」「換股通報」**。三種可能的處置：

(a) 塞進「手動補資料」那一塊 → **畫面會說謊**：選股池 CRUD 不是「補資料」；
(b) 整個不畫 → **等於刪功能**（線框自己說要從那個檔搬來）；
(c) **另立一個區塊、就地登記線框沒有給它位置**。

**本檔選 (c)**，理由是 (a) 違 §1、(b) 違線框自己的「從哪裡搬來」。
⛔ **這是總管級的裁決，不是實作細節** —— 它在客戶拍板的版面上**多了一塊**，
   已具名回報，**在客戶／總管裁決落下之前，本區塊的存在本身是登記在案的待驗事項**。
   ⚠️ **不得**把本段讀成「⑤ 可以隨意加區塊」。

(D-6) ⭐ **結論層說得出「不知道」，說不出線框那句「還不能／大致可以」——
      這是一個登記在案的缺口，不是完成品**
      ⇒ **已登記為 `EXCEPTIONS.md §8.3.P` 的 `P-P05VERDICT-1`**
------------------------------------------------------------------------
📌 **回指登記：`P-P05VERDICT-1`**（`EXCEPTIONS.md §8.3.P`）。
   `EXCEPTIONS.md §8.2.A.1` 末句明訂新增登記**必須**三件事都做：
   (1) 在表上登錄、(2) **對應檔案加註解指回該表**、(3) PR 描述附理由。
   ⚠️ **第一版只做了 (1)(3)** —— 實測 `grep -rn 'P-P05VERDICT-1' --include=*.py .` **回 0**，
   而對照組 `P-WHERECONTENT-1` 有 6 檔、`P-UIGSPREAD-1` 有 3 檔回指。
   **而且沒有檔案邊界的理由**：本批本來就在改這個檔。**本輪補上。**
線框 §3 的兩張圖各給了一句**帶判定**的結論：

- 狀態 (1)：「🔴 **還不能。有 2 項沒設定完**，會讓 ① 到 ④ 出現空白或舊資料。」
- 狀態 (2)：「🫐 **大致可以，但有 2 個來源今天沒更新。**」

**本檔畫不出那兩句，原因不是懶，是一條互相打架的約束，逐條寫在這裡讓後人能推翻**：

1. **那兩句都要先讀東西**：「有幾項沒設定完」要讀 secret；「幾個來源今天沒更新」
   要跑一次註冊表更新 ＋ 逐源查詢。
2. **本檔唯一被允許的 L2 入口是 `services.nav_history_gs`**
   （`tests/test_wf05_settings_skeleton.py::_ALLOWED_SERVICE_MODULES`）。
   `infra.config.get_secret` 是 **L0，banned root**，本檔**不得**直呼
   （`test_the_page_never_reaches_into_the_data_layer`）。
   ⇒ **讀 secret 的唯一合法門是 `nav_history_gs.status()`** ——
   它的回傳裡就有 `diag["st_secrets_alive"] = bool(get_secret("FRED_API_KEY"))`
   與 `missing`（`google_service_account` / `NAV_SHEET_ID`），
   **恰好就是線框那兩張卡要的東西**。
3. **但 `status()` 在 gate 沒勾之前一次都不准被呼叫**：
   `test_the_nav_block_is_grey_and_reads_nothing_before_the_gate` 斷言
   `_calls == {"backend": 0, "coverage": 0}`，而結論層排在所有 gate **之前**。

⚠️ **這裡有一個真的、開放的問題，本組不自行拍板**：
   `status()` **實測只讀 secret、不連線**（它整支只呼叫 `infra.config.get_secret`，
   憲法 `EX-UICACHE-1` 的驗證段 ③ 也把它列為那批成員裡**唯一沒有遠端往返**的一支）。
   而那條守衛自陳的目的是「**首屏一次 Google Sheets 往返都沒有**」——
   **`status()` 不是 Google Sheets 往返，只有 `coverage_status()` 是。**
   ⇒ **那條守衛對 `status()` 而言比它自己宣稱的目的寬**。

⛔ **本批刻意不動它**，理由有二，兩條都要看：
   (a) **鬆守衛換文案是本 repo 明令禁止的方向** —— 派工單逐字：
       「不准為了轉綠 skip／刪／放寬測試」。要鬆它必須是**先有裁決、再改守衛**，
       不是**先想要那句話、再回頭鬆守衛**。
   (b) **鬆了之後就回不去了**：那條守衛是這一頁選 Checkbox Gate（而不是
       UI 層 `@st.cache_data`）**唯一真正買到的東西**；一旦允許「結論層可以先讀一點點」，
       下一個人會照著加第二個、第三個。**留但書等於留引用點**（`CLAUDE.md §-2` 規則 6）。

**⇒ 待總管／客戶裁決的問題（本檔不作處置建議，也不構成動工授權）**：
   要不要把那條守衛從「兩支都不准叫」收窄成「**`coverage_status()` 不准叫**」，
   讓結論層可以用 `status()` 畫出線框那句「有 N 項沒設定完」？
   ⚠️ 若要做，**必須連帶處理**：`test_opening_the_gate_reads_exactly_once`
   （它斷言勾了之後 `backend` 恰好 1 次 —— 結論層先叫一次就變 2 次）。

**本檔現行的替代做法（誠實版，不是縮水版）**：
   結論層回答同一個問題，但答案是「**還不知道，而且這是要按哪一下才會知道**」，
   三張卡各自說明「**哪一塊回答哪一個問題**」。
   ⚠️ 那不是空話：本頁六塊**全部**在講自己（「尚未載入…」「舊分頁已經在跑同一塊…」），
   **在本次之前沒有任何一個地方說過哪一塊回答哪一個問題**。

⛔ **線框那三張卡的數字一個都不准畫**（`0.4 秒` / `3 天前` / `42 檔` / `16 源` /
   `5 檔`）—— 同 (D-3)，它們是線框示範版面用的假數字。

⛔⛔ **本頁不可以被當成四大鐵律的示範頁 —— ②③④ 照抄前必讀**
=============================================================

**四大鐵律** ＝ ① 固定 3 欄自適應網格　② `st.form` 防重繪　③ 三態顏色分離
④ 首屏無冗餘占位。**本頁在 ①②④ 上沒有任何可抄的東西**，理由是量出來的，不是謙虛：

**AST 實測（量測日 2026-09-06；`origin/main` 的舊版 vs 本檔，同一支掃描器）**：

====================  ======  ======  ==================================================
符號                    舊版     本檔     它是什麼
====================  ======  ======  ==================================================
`ia.card_row`           1       **0**   **鐵律 ① 的落點**（固定 3 欄自適應網格）
`ia.applied_form`       1       **0**   **鐵律 ② 的落點**（`st.form` 防重繪）
`ia.state_card`         2       **0**   三態卡
`st.columns`            0       0       本檔從來沒有自建網格
`st.form`               0       0       本檔從來沒有自寫表單
====================  ======  ======  ==================================================

⚠️ **2026-09-08 就地更正：上表的 `ia.card_row` / `ia.state_card` 兩列自本次起不再是
   「本頁沒有網格可看」的證據（有意識的更正，不是漏刪 · 決策者：AI 總管，依線框 §3）。**
   **上表的數字一格未改，它記的是量測日 2026-09-06 的實況**；改的是**它撐不撐得起
   下面那個結論**。新增的 :func:`_render_conclusion` 走
   `ui.helpers.ia.cards.render_cards()` → `ia.layout.card_grid()` → `st.columns(GRID_COLS)`
   —— **本頁自本次起真的有一個走共用元件的三欄網格**（鐵律 ①），
   而且它是**經由 `ia`**，不是自建（`st.columns` 在本檔仍然是 **0**，
   `test_the_page_draws_no_grid_form_or_tabs_of_its_own` 照舊守得到）。
   ⛔ **鐵律 ②（`st.form`）那一列一個字都沒有變**：本頁仍然沒有任何表單，
      「空掃」對它**仍然成立**。**不要把這一段讀成「四盞燈現在有內容了」。**

**改寫成委派殼之後，版面變成一串 `st.markdown("### …")` 平鋪 ＋ 六次 `safe_section()`。**
於是：

- **鐵律 ①②**：`tests/test_ui_grid_contract.py` 與 form 相關守衛對本檔是**空掃**
  —— 檔內 `st.columns` / `st.form` / `card_row` / `applied_form` 全是 0，
  **沒有東西可查**（本檔自己的 `test_the_page_draws_no_grid_form_or_tabs_of_its_own`
  同理：它驗的是「**不准有**」，不是「**有而且對**」）。
- **鐵律 ④**（首屏無冗餘占位）：那條守衛的 scope 是 `app.py`。
  ⚠️ ~~而**本檔尚未接線**（`app.py` 一個字未動），**射程為零**。~~
  → **2026-09-07 事實更正（有意識的更正，不是漏刪 · 決策者：AI 總管 · 依據：實測）**：
  **本檔已於 #814「雙軌並行」接進 `app.py` 的第 ⑦ 格**（見下方「本檔的接線狀態」）。
  ⛔ **但「射程為零」這個結論本組沒有重驗** —— 它取決於那條守衛的 scope 到底怎麼寫，
  而本輪沒有去讀那條守衛。**只更正已被實測推翻的那半句（「尚未接線」），
  不對另一半（「射程為零」）背書。**
- **鐵律 ③**（三態顏色分離）：本檔確實走 `render_state` 的 `not_ready()` /
  `safe_section()` / `system_error()`，**但**獨立紅隊 2026-09-06 回報：
  手繪 6 種紅色形狀、**存活 5 種**（fail-open）。⚠️ **這一項本組沒有重現**
  （本環境無 streamlit，跑不了那組突變），依 `CLAUDE.md §-2` 規則 3／7
  **只轉述、不背書** —— 但足以說明它**不是**一個可以拿來背書的示範。

⛔ **所以，一句話**：**②③④ 不得把「本頁四盞燈全綠」當成鐵律達標的證據。**
   四盞燈全綠是因為**沒有東西可查**，不是因為查過了。
   要看鐵律 ①② 真正長什麼樣，去看**還在用 `card_row` / `applied_form` 的那些頁**。

⛔ **本輪刻意不把版面改回 `card_row` / `applied_form`** —— 那是**版面異動**
   （§-1.5.4 兩步判定的第 1 步「版面結構」＋ v3 `03`-2 ①），
   **必須先送客戶線框草稿**，不在本輪授權內。**只寫清楚，不動版面。**

⚠️ **本頁沒有 `render_story_nav()`，這是刻意的，不是漏做**
===========================================================
`story_nav.render_story_nav()` 的第一行是
``if _as_tab_key(current) not in _VALID: return`` —— 而決策動線只有**四站**
（`macro` / `health` / `research` / `portfolio`），`render_flow_nav` 的 docstring
自己就寫著「**⑤ 設定與診斷不在其中**」。照抄 ①②③④ 那一行進來，會得到一個
**看起來有做、實際是 no-op** 的呼叫。**本檔不放那一行。**

⚠️ **本檔的接線狀態（2026-09-07 實測）**
-------------------------------------
~~本批尚未接進 `app.py`（客戶明令舊分頁不動、不接線、不下架），
所以本檔現在**沒有 production caller** —— 這是**刻意的中間狀態**，不是漏接。~~

⚠️ **2026-09-07 事實更正：本檔已經接線了，「沒有 production caller」是假的**
（**有意識的更正，不是漏刪** · 日期 **2026-09-07** · 決策者：**AI 總管**）。
**實測**：`git grep -n "page_05_settings" app.py` → `app.py` 有
``from ui.views.page_05_settings import render_settings_and_diagnostics``；
同檔 `with tab_preview_settings:` 內呼叫 `render_settings_and_diagnostics()`。
也就是說本頁**現在真的會在 production 每一次 script run 被渲染**。
**舊表述在它寫下的當天是對的**（那一批確實只寫檔、沒接線，`app.py` 一個字未動）；
**被推翻的是它的前提** —— #814「雙軌並行」把本頁掛上了 `app.py` 的第 ⑦ 格，
而**那一批沒有回頭改本檔的自我描述**。
⛔ **這一句不是無害的過期**：本檔多處推論建立在「反正還沒接線、射程為零」之上，
   而本輪要修的四個線上缺陷，全部是「已經接線」才會發生的。

**現行**：本檔掛在 `app.py` 的第 ⑦ 格（`tab_preview_settings`，`[新] 設定與診斷`），
與**仍然在線上**的舊 ⑤（`ui/tab_settings_diag.py`）**同時渲染** ——
`st.tabs` 一次 run 會把**所有分頁的 body 全部執行過**，「⑦ 沒被點開」
**不代表它沒渲染**。本檔與舊 ⑤ 委派同一批舊模組，故本檔四個區塊都掛了
Checkbox Gate（見各該函式）。⛔ **客戶明令舊 Tab 一個位元組都不准動**，
所以修法一律落在本檔（新頁側），不得去改 `ui/tab*.py`。

⚠️ **接線批次必讀：`FETCH_DIAG` 要由 `app.py` 持有，不是本檔**
---------------------------------------------------------------
`merge_context.FETCH_DIAG` 的作用是「⑤ 持有 → **個基頁**那份抓取診斷不畫」。
Streamlit 的 `st.tabs` 是**同一次 script run 渲染全部分頁**，所以要壓住個基頁那份，
持有範圍必須**涵蓋整個分頁列的渲染**（現行 `app.py` 就是這樣包住五個分頁本體的）。
⛔ 在本檔內部包一層 `settings_page_owns(FETCH_DIAG)` **對個基頁毫無作用** ——
   那會變成一個「看起來有做、實際 no-op」的呼叫，正是本檔對 `render_story_nav`
   拒絕照抄的同一個病。**故本檔刻意不包。**
"""
from __future__ import annotations

import datetime as _dt
from typing import Any

import streamlit as st

from services.nav_history_gs import (
    coverage_status as fetch_nav_coverage,
    status as fetch_nav_backend_status,
)
from ui.helpers.ia.cards import STATE_NOT_READY, render_cards
from ui.helpers.ia.empty_state import empty_state
from ui.helpers.render_state import not_ready, safe_section
from ui.helpers.settings_diag.merge_context import (
    DATA_GUARD_HEADER,
    MANAGE_HEADER,
    MANUAL_HEADER,
    NAV_HISTORY,
    settings_page_owns,
)
from ui.helpers.settings_diag.fetch_diag_section import render_fetch_diag_from_session
from ui.helpers.settings_diag.policy_admin_bridge import render_policy_admin_bridge
from ui.helpers.story_nav import section_label, tab_label, where_to_find

# ── session 鍵名（本檔自己的命名空間）────────────────────────────────────────
#: 使用者的持股來源。既有 session 契約，由 ④ 的加入基金流程／`ui/helpers/cloud_io.py` 寫入。
#: ⚠️ 這個字串是**別人定義**的鍵名，本檔只讀不寫 —— 不要在這裡「順手改個好名字」。
_SK_PORTFOLIO: str = "portfolio_funds"

#: 「資料來源健康度」整塊的 Checkbox Gate 鍵。
#:
#: ⚠️ **刻意不沿用舊 ⑤ 的 `sd_diag_gate`**（`ui/tab_settings_diag.py::_render_diag_section`）。
#:    舊 ⑤ **仍然接在 `app.py`**（本批不接線、不下架），兩頁共用同一個 widget key
#:    在「兩頁同時被渲染」的那一刻會直接拋 `StreamlitDuplicateElementKey`。
#:    **gate 這件事一格未鬆**（總管裁決 3）：擋的東西、預設值、灰態文案全部照舊，
#:    換掉的只有 key 的命名空間。
_SK_DIAG_GATE: str = "v05_diag_gate"

# ── 區塊名 ────────────────────────────────────────────────────────────────
#: 線框 Tab 05 `<h4>` 逐字。**SSOT `_SECTION_LABELS` 沒有這個 key**，
#: 本批**刻意不新增 key**（不在檔案邊界內，同 `page_04_portfolio.py::BLOCK_POLICY`）。
BLOCK_HEALTH: str = "資料來源健康度"
#: 線框 Tab 05 `<h4>` 逐字。**SSOT 沒有這個 key**（理由同上）。
BLOCK_KEYS: str = "連線與金鑰"
#: 線框 Tab 05 `<h4>` 逐字。⚠️ **與 `section_label("manual")`（「📖 說明書」）並存的
#: 第二份真相源，尚未裁決** —— 兩個名字很可能指同一塊東西
#: （`manual` 的所屬分頁在 `_SECTION_TO_TAB` 裡正是 `settings`）。
#: 本檔取**線框字面**：線框是客戶看過並拍板的那份視覺，
#: 用 `section_label("manual")` 會在畫面上印出「📖 說明書」——那與客戶拍板的字不一樣。
#: ⛔ **但這一條沒有被裁決過，本檔不宣稱它是對的**（已具名回報總管）。
BLOCK_MANUAL: str = "使用手冊"

#: gate 的標籤 —— **線框 §3 狀態 (1) 逐字**：「🔭 查一次資料來源狀態」。
#:
#: ⚠️ **2026-09-08 換文案（有意識的變更，不是漏刪 · 決策者：客戶，線框 §3 拍板）**
#:    舊值 ~~`"🔭 載入資料診斷"`~~，舊註記 ~~「**與舊 ⑤ 逐字相同**（總管裁決 3：
#:    gate 一格未鬆）」~~。
#:
#:    **「gate 一格未鬆」今天依然成立，一個字都沒有被推翻**：擋的東西
#:    （`_update_data_registry()` ＋ `render_data_guard_tab()`）、預設值（不勾）、
#:    key 的命名空間（:data:`_SK_DIAG_GATE`）**全部沒動** —— 換掉的只有畫面上那行字。
#:
#:    **被權衡掉的是「與舊 ⑤ 逐字相同」這個性質。** 線框 §0 推翻 1 逐字：
#:    「留下來的三顆**要換文案** —— 現在的字在講『為什麼系統不幫你做』（進度語言），
#:    該換成『這一項要花多久、你要不要現在花』（處境語言）」。
#:    「**載入**」是系統在講自己；「**查一次**」是在講使用者要付的那一下。
#:
#: ⚠️ **一個副作用，據實寫在這裡，不要讓它變成別人的意外**：
#:    舊 ⑤（`ui/tab_settings_diag.py`）那一顆**仍然叫舊名字**
#:    —— 客戶紅線「舊 Tab 一個位元組都不准動」，本批不碰它。
#:    所以畫面上這兩顆 checkbox **自本次起不再同名**。
#:    ✅ 這反而**解掉**一個既有的觀測缺口：`tests/test_dual_track_p05_gates.py`
#:    檔尾第 4 項與 `tests/test_dual_track_widget_key_collision.py` 檔尾第 1 項
#:    都登記「兩顆同名的『🔭 載入資料診斷』…`_tick()` 依標籤找，兩顆同名只會勾到第一顆」。
#:    ⛔ **但本批沒有去改那兩條登記**（不在檔案邊界內），也**沒有**去驗
#:    「現在分得開了，所以那個路徑可以測了」—— **登記，不宣稱已解。**
DIAG_GATE_LABEL: str = "🔭 查一次資料來源狀態"

#: 逐檔明細的展開器標題（線框：「雲端歷史涵蓋度，**逐檔可展開**」）。
#: ⚠️ **刻意是固定字串、不帶檔數** —— 它在守衛的 `_units()` 裡是一個**單位名**，
#:    帶了檔數就會隨資料變動，單位邊界會跟著漂。
NAV_DETAIL_LABEL: str = "逐檔明細"

#: 讀雲端 NAV 累積狀態的 **Checkbox Gate**。勾起來才會去讀一次。
#:
#: ⚠️ **為什麼是 gate 而不是 `@st.cache_data`（總管裁決，理由寫在這裡讓後人能推翻）**：
#:    (a) 在 `ui/**` 自建 `@st.cache_data` 會替憲法例外 `EX-UICACHE-1` 新增一個成員，
#:        而那個例外的成立**繫於一個尚未裁決的問題**（`EXCEPTIONS.md §8.3.P` 的 `P-UIGSPREAD-1`）；
#:    (b) `coverage_status()` 內部走 `load_points(None)` —— **一次讀完整張 sheet**，
#:        而 L2 那層**沒有**任何快取，UI 再疊一層就會變成 `P-NDCCACHE-1` 的同型。
#:    → gate 同時解掉這兩件事：**沒勾就一次都不讀**，勾了才讀，而且讀的責任留在 L2。
#: ⚠️ **2026-09-08 換文案（有意識的變更，不是漏刪 · 決策者：客戶，線框 §3 拍板）**
#:    舊值 ~~`"讀取雲端 NAV 累積狀態"`~~ → 線框 §3 狀態 (1) 逐字「讀一次雲端累積狀態」。
#:    上面那段「為什麼是 gate 而不是 `@st.cache_data`」的理由**一個字都沒有被推翻**；
#:    換掉的只有畫面上那行字（「讀取」＝系統在講自己 → 「讀一次」＝在講使用者要付的那一下）。
NAV_GATE_LABEL: str = "讀一次雲端累積狀態"

#: 「跨度」在畫面上的**唯一措辭**。具名是為了讓守衛認得出「哪一行在講跨度」。
#: ⚠️ 用「首末相距」而不是「跨度 N 年」：`span_days = last - first`，
#:    它**不代表中間連續** —— 兩個點相距六年也會是六年。措辭本身要說出這件事。
SPAN_PHRASE: str = "首末相距"
#: 點數的單位字。**跨度出現的地方一定要有它**（見 :func:`coverage_line`）。
POINTS_UNIT: str = "筆"

#: gate 沒勾時的灰態本文 —— **線框 §3 狀態 (1) 逐字**。
#: **不講任何數量**，因為我們一次都還沒讀。
#:
#: ⚠️ **2026-09-08 換文案（有意識的變更，不是漏刪 · 決策者：客戶，線框 §3 拍板）**
#:    舊值 ~~「尚未讀取雲端 NAV 累積狀態 —— 這是一次會往返 Google Sheets 的讀取，
#:    所以預設不做；勾上面那個選項才會讀一次。」~~
#:    **舊表述的內容其實已經很接近**（它有講「會往返 Google Sheets」＝成本），
#:    **被權衡掉的是它的開頭**：「**尚未**讀取」是**進度語言** —— 它把使用者放在
#:    「系統還沒做完」的位置上，而事實是「**這件事要花錢，所以在等你決定**」。
#:    線框 §3「這一頁改了什麼」逐字：灰態文案講「**要花多久、為什麼預設不做**」
#:    而不是「尚未載入」。
_NOT_LOADED_NOTE: str = (
    "還沒去查。查一次會連上你的 Google 試算表讀一遍，所以預設不做。")

#: 「資料來源健康度」gate 沒勾時的灰態本文 —— **線框 §3 狀態 (1) 逐字**。
#:
#: ⚠️ **2026-09-08 換文案（有意識的變更，不是漏刪 · 決策者：客戶，線框 §3 拍板）**
#:    舊值 ~~「資料來源健康度尚未載入（避免每次互動都重跑註冊表更新與匯率抓取）」~~，
#:    舊註記 ~~「**逐字沿用舊 ⑤ 的理由**」~~。
#:    **舊表述講的是真的**（那一次註冊表更新與匯率抓取確實存在，見 :func:`_render_source_health`），
#:    **被權衡掉的是它的視角**：括號裡那句是**寫給工程師看的實作理由**
#:    （「避免每次互動都重跑」＝系統在解釋自己的設計），使用者要的是
#:    「**這一下要花我幾秒、不按會怎樣**」。新文案把同一件事改成使用者的處境，
#:    並多講了一句舊版沒有的話：**沒遇到問題時可以完全略過**。
#:
#: ⚠️ **副作用同 :data:`DIAG_GATE_LABEL`**：舊 ⑤ 那一句仍是舊文案
#:    （「**資料診斷**尚未載入（…）」），本批不碰它 —— 線框 §0 推翻 1 已經實測登記過
#:    「括號內完全相同、主詞不同（sha256 實測不同）」，自本次起**連括號內也不同了**。
_DIAG_NOT_LOADED_NOTE: str = (
    "還沒去查。查一次會更新資料註冊表、並抓一次即時匯率，"
    "大約要幾秒；沒遇到問題時可以完全略過。")

#: 「手動補資料」gate 的標籤。
#:
#: ⚠️ ~~**這是本頁第二顆同型的 gate**（第一顆是 `MAINTAIN_GATE_LABEL`）~~ ——
#:    **2026-09-07 就地更正（有意識的更正，不是漏刪 · 決策者：AI 總管）**：
#:    它**當時**確實是第二顆；**現在本頁共有四顆**（維護區 / 手動補資料 /
#:    使用手冊 / 抓取診斷細節）。**序數是會漂移的量測值**，本行改為只記成因。
#:    成因：舊 ⑤ 也在跑同一支委派，兩份同時載入會撞 Streamlit 重複元件鍵。
BACKFILL_GATE_LABEL: str = "🗄️ 載入手動補資料"

#: 「🗄️ 資料維護與通報」gate 的標籤。
MAINTAIN_GATE_LABEL: str = "🗄️ 載入資料維護與通報"

#: 「使用手冊」gate 的標籤（**2026-09-07 第二輪雙軌修補新增**）。
#:
#: ⚠️ **這是本頁第三顆同型的 gate，而且是唯一一顆「使用者一次都不必點就已經在噴紅字」
#:    的**：`render_manual_tab()` 在舊 ⑤ 與新 ⑦ **兩邊都沒有 gate**，
#:    而它底下 `ui/tab1_macro.py::render_indicator_map` 的 `st.plotly_chart`
#:    **不帶 key** → 兩份的自動 element ID 完全相同 → `StreamlitDuplicateElementId`。
#: ⛔ **不得帶 `key=`**（會命中 `test_the_page_writes_only_its_own_session_key`）；
#: ⛔ **不得放進 `st.form`**。正例就是同檔 `_render_maintain` 那顆。
MANUAL_GATE_LABEL: str = "📖 載入使用手冊"

#: 「🔍 抓取診斷細節」gate 的標籤（**2026-09-07 第二輪雙軌修補新增**）。
#:
#: ⚠️ **這一顆與前三顆不同族：它擋的東西「不會報錯」** —— 舊 ⑤ 與新 ⑦ 都無條件呼叫
#:    `render_fetch_diag_from_session()`，兩份都畫得出來、Streamlit 一聲都不吭，
#:    畫面上就是**兩塊一模一樣的「🔍 抓取診斷細節」**。
#: ⛔ **`app.py` 的 `settings_page_owns(FETCH_DIAG)` 救不了這一個** —— 那支旗標的
#:    設計前提是「全站只有一個消費者」（它壓的是 ③ `ui/tab2_single_fund.py` 那份），
#:    雙軌之後消費者變成兩個（舊 ⑤ ＋ 新 ⑦），**而旗標分不出新舊**。
#:    修在旗標那一層要動 `ui/helpers/settings_diag/merge_context.py` 與 `app.py`，
#:    兩者都在本批的檔案邊界外，故修在新頁側。
FETCH_DIAG_GATE_LABEL: str = "🔍 載入抓取診斷細節"


#: 後端未啟用時的灰態本文開頭。⚠️ **這不是「沒有資料」，是「我們沒辦法去看」**（§1）。
_BACKEND_UNAVAILABLE_NOTE: str = (
    "讀不到雲端 NAV 歷史 —— 累積功能所需的設定不完整，"
    "所以**這一格不知道你累積了多少**（不是「你沒有累積」）。缺少：")

#: 真的讀到了、但一筆都沒有時的空狀態三要素。
_EMPTY_TITLE: str = "雲端 NAV 歷史目前一筆都沒有"
_EMPTY_MISSING: str = (
    "已經讀到雲端了，但 nav_history 裡沒有任何一筆紀錄 —— "
    "可能是還沒開始累積，也可能是那張工作表還沒建立")
_EMPTY_FOOTER: str = "開始累積之後，這裡會逐檔列出點數與首末日期。"


# ══════════════════════════════════════════════════════════════════════════
# 🧾 ① 結論層 —— 本頁原本缺的那一層（2026-09-08，客戶拍板線框 §3）
# ══════════════════════════════════════════════════════════════════════════
#: 結論層的標題。**與 ⑥ `page_02_health.py::CONCLUSION_HEADING` 同一個層級、
#: 同一種寫法**（`###`）—— 本 repo 已經有一套四層閱讀順序（線框 §1），
#: 這裡不發明第二套。**破折號後面那半是線框 §3 逐字。**
#:
#: ⚠️ 寫成常數而不是 inline 字面值：守衛要拿它比對「結論在依據**前面**」，
#: 抄一份字面值到測試裡就是第二份真相源（§2.1）。
CONCLUSION_HEADING: str = "### 🧾 ① 結論 — 現在能不能信畫面上的數字"

#: 依據層的標題（線框 §3 逐字）。它是線框 §1「四層閱讀順序」的第三層 ——
#: 客戶第二句話要的那個分界：**上半是「你要知道的」，下半是「憑什麼」**。
EVIDENCE_HEADING: str = "### 🧾 ② 依據 — 逐項狀態"

#: 結論層那句話 —— **這一頁一次都還沒去查**時。
#:
#: ⛔ **為什麼不是線框寫的那句「🔴 還不能。有 2 項沒設定完」（讀完再改，這是本批最重要的取捨）**
#:    線框狀態 (1) 的標題句與三張卡（`FRED 金鑰` / `Google 授權` / `網路連線`）
#:    **需要先去讀東西才說得出來**：金鑰齊不齊要讀 secret，網路通不通要打三個來源。
#:    而本檔**唯一被允許的 L2 入口**（:data:`_ALLOWED_SERVICE_MODULES` ＝
#:    `services.nav_history_gs`）那兩支，**在 gate 沒勾之前一次都不准被呼叫** ——
#:    那是 `tests/test_wf05_settings_skeleton.py::`
#:    `test_the_nav_block_is_grey_and_reads_nothing_before_the_gate` 釘死的性質
#:    （`_calls == {"backend": 0, "coverage": 0}`），也是這一頁選 Checkbox Gate
#:    **唯一真正買到的東西**：首屏一次往返都沒有。
#:    ⇒ **要照線框那句寫，就得先鬆掉那條守衛。本批不鬆。**
#:    （`status()` 實測**只讀 secret、不連線**，所以「鬆掉它是不是對的」是一個
#:     真的、開放的問題 —— 但那是**總管級的取捨**，不是實作組自己決定的事。
#:     **已具名回報，見模組 docstring 的 (D-6)。**）
#:
#: ⛔ **也不准退而求其次去畫線框那三張卡的數字**（`0.4 秒` / `3 天前` / `16 源`）——
#:    (D-3)：那些是線框用來示範版面的假數字，而這一頁的職責正是回答
#:    「資料可不可信」，在**這一格**放假數字是最壞的一種（`CLAUDE.md §1`）。
#:
#: ⇒ **本檔採用的是同一個問題的誠實答案**：不知道，而且說出「要按哪一下才會知道」。
#: ⛔⛔ **2026-09-08 回修：舊表述在今天到得了的狀態下是假的（有意識的更正，不是漏刪 ·
#:    決策者：AI 總管，依獨立稽核 ＋ 總管自己的 AST 實測）。三個獨立的假，逐條列**：
#:
#:    舊值 ~~「**還不知道 —— 這一頁一次都還沒去查。** 底下每一塊各管一件事，
#:    **都要你按一下才會去查**；按之前這一格說不出「能」或「不能」…」~~
#:
#:    **(a) 「這一頁一次都還沒去查」是一句整頁全稱句，而本層看不到整頁。**
#:        :func:`_diag_gate_is_on` 只讀得到 :data:`_SK_DIAG_GATE` 一個鍵；
#:        :data:`NAV_GATE_LABEL` 那一顆**沒有帶 `key=`**（帶了會違反
#:        `test_the_page_writes_only_its_own_session_key`）。
#:        ⇒ **使用者勾了 NAV gate、沒勾 diag gate** 時，同一輪已經往返 Google Sheets、
#:        真數字就印在下面，而這一句還在說「一次都還沒去查」。
#:    **(b) 「都要你按一下才會去查」與 gate 狀態無關，預設狀態就假。**
#:        本頁六塊裡有**四塊**的那顆鈕按下去**不是「去查」** —— 它們自己的灰態原文寫著
#:        「勾上面那個選項，這一塊就會變成**紅色錯誤**」／「**看不到能用的表單**」／
#:        「「指標地圖」那一小塊會**變成紅字**」／「畫面上會出現**兩塊一模一樣的**」。
#:        **這半句不依賴任何 gate、也不依賴任何渲染，它單純就是錯的。**
#:    **(c) 本檔早就把 (a) 診斷得一字不差** —— 見 :data:`_CARD_NAV_NOTE` 的長註記
#:        （「這張卡卻會繼續說「還沒去查」，那是一句當場就假掉的話」）——
#:        **然後只修了卡片，沒修結論**，而結論的宣稱範圍比卡片大一整頁。
#:        **那正是本 repo 憲法反覆記載的「只改被點名的那一處」。**
#:
#: ⛔ **修法是【收窄宣稱】，不是補 `key=`**（總管 2026-09-08 硬界線）：
#:    補 `key=` 要拿掉一條守衛去換一句話 —— 本檔 :func:`_diag_gate_is_on` 自己寫過
#:    「⛔ 不要為了讓這一格更聰明而去替它們補 `key=`」，總管採納並升為硬界線。
#: ⇒ **現行只講它真的看得到的那一件事：那一顆它自己擁有的開關的狀態。**
#:    其餘各塊**各自**在自己的位置上寫自己的狀態，這一格明說「我看不到、也不替它們發言」。
_VERDICT_UNCHECKED: str = (
    "**這一格只回答一件事：「{gate}」這個開關現在是關的，所以資料來源狀態還沒查。** "
    "其餘各塊**各自**寫了自己現在的狀態 —— 這一格**看不到它們，也不替它們發言**"
    "（它們的開關沒有留下這一格讀得到的痕跡）。")

#: 結論層那句話 —— 資料來源狀態**已經查過**時（＝ :data:`_SK_DIAG_GATE` 勾著）。
#:
#: ⛔ **這一句刻意不下判定，理由不是保守，是 §2.1**：逐源的最後成功時間與資料日期
#:    由被委派的 `render_data_guard_tab()` 畫，把它壓成一句「還可以／不可以」
#:    等於在同一頁上造第二份真相 —— 而那兩份**只要有一次不同步，使用者會信上面那句**
#:    （它排在前面、而且比較短）。
#: ⚠️ **這是一個登記在案的缺口，不是完成品**：見模組 docstring 的 (D-6)。
#: ⛔ **2026-09-08 同輪回修（有意識的更正，不是漏刪）**：舊值
#:    ~~「資料來源已經查過了 —— **逐源結果**在底下「{block}」那一塊。…」~~
#:    **承諾了一個它保證不了的東西**：`_render_source_health` 走 `safe_section()`，
#:    委派拋例外時底下是**一個紅框**，沒有任何逐源結果。
#:    ⚠️ **同型的半修一次**：:data:`_CARD_HEALTH_CHECKED` 那張卡早就寫了
#:    「結果（**或失敗原因**）」，**結論卻沒寫** —— 又一次只修一半。
_VERDICT_CHECKED: str = (
    "**這一格只回答一件事：「{gate}」這個開關已經打開了。** "
    "查到什麼、{fail}，在底下「{block}」那一塊 —— 這一格**不**替它壓成一句話"
    "（壓了就會變成第二份跟底下不同步的真相，而使用者會信排在前面的那一句）。"
    "其餘各塊同樣**各自**寫自己的狀態，這一格看不到它們。")

#: 結論層三張卡。**每一張回答「這個問題的答案住在哪一塊、現在有沒有答案」**，
#: **不是**「答案是什麼」。
#:
#: ⛔⛔ **暫行，而且與線框不同 —— 這是一個登記在案的規格偏離，不是完成品**
#:    （**總管 2026-09-08 裁決：接受為「暫行」，但要就地寫明它是暫行、以及為什麼被迫如此**；
#:    總管另已表示會在交付報告裡對客戶明白揭露這一格與線框不同）。
#:
#:    **線框 §1 明寫**：「〈3 欄卡：每張卡 ＝ **一件要做的事**，不是一個指標〉」，
#:    §3 的三張卡是 `🔴 FRED 金鑰` / `🔴 Google 授權` / `🟢 網路連線`。
#:    **本檔畫的是「哪一塊回答哪一個問題」＝ 一張目錄，不是三件要做的事。**
#:
#:    **為什麼被迫如此**：要畫線框那三張，得先知道金鑰齊不齊、網路通不通 ——
#:    那要讀東西，而本層在所有閘門之前、一次都不准讀（完整理由見 (D-6)）。
#:    ⛔ **不准為了對齊線框去畫沒量過的紅燈**：畫一張沒量過的 `🔴 FRED 金鑰`
#:    是 `CLAUDE.md §1` 的造假，**那一條比「對齊線框」高**。
#:
#: ⇒ **這一格的正解不在本批**：等 (D-6) 有裁決、或舊 ⑤ 下架讓閘門可以拆之後，
#:    這三張卡要**整組換掉**成線框那三張。**在那之前它是暫行的目錄。**
#: 📌 **登記在 `EXCEPTIONS.md §8.3.P` 的 `P-P05VERDICT-1`**（連帶欄），
#:    觸發點主線：**舊 ⑤ 下架那一批**。
#:
#: ⭐ **為什麼這個暫行版仍然有內容、不是三張空卡**：
#:    這一頁現在有六塊，六塊**都在講自己**（「尚未載入…」「舊分頁已經在跑同一塊…」），
#:    **沒有任何一個地方說「哪一塊回答哪一個問題」**。
#:    使用者出事時第一個進來，看到的是六塊灰字，然後不知道要按哪一顆。
#:    這三張卡就是那份對照表。
_CARD_HEALTH_TITLE: str = "🔭 資料來源健康度"
_CARD_HEALTH_ASKS: str = "回答：畫面上那些數字是不是今天抓到的。"
_CARD_HEALTH_UNCHECKED: str = (
    f"{_CARD_HEALTH_ASKS}還沒去查 —— 查一次要幾秒。")
#: ⭐ **「失敗對沖」的唯一措辭，具名共用（2026-09-08 第二輪回修）。**
#:
#:    被委派的 `render_data_guard_tab()` 可能整塊炸掉 —— 那時底下是
#:    :func:`safe_section` 畫的**一個紅框**，不是任何結果。
#:    **凡是承諾「打開之後看得到什麼」的句子，都必須同時允許失敗。**
#:
#: ⛔ **做成常數、而且三處共用，是刻意的**：第一輪回修時
#:    :data:`_CARD_HEALTH_CHECKED` 已經寫了「（或失敗原因）」，
#:    **但結論與 🔑 卡都沒有** —— 同一把尺沒有對它們重跑。
#:    共用一個常數之後，**少寫一處就是少一個引用點，守衛看得見**
#:    （`test_anything_that_promises_a_result_also_allows_failure`）。
#: ⛔ **本常數是一個【已登記的待判定項】：`EXCEPTIONS.md §8.3.P` 的 `P-HEDGESEMANTIC-1`。**
#:    守它的只有一個**長度**下限（`len(...) >= 4`）—— **量不到語意**：
#:    實測換成 `"或或或或"` → **155 passed 存活（2026-09-09 第三輪快照數）**，
#:    「每一句承諾都有對沖」形式成立、語意全空。
#:    ⚠️ **第五輪於 head 重跑：機制仍然存活（161 passed）** —— **數字會隨測試數漂移，
#:    機制沒有變**。引用時請看機制，不要引用那個數字（§8.2.A.0 規則 4）。
#: ⚠️ 兩份逐字底本（`_CARD_TEXT_PINNED` / `_VERDICT_PINNED`）**刻意保留 `{fail}` 不展開**，
#:    所以它換成什麼，對那兩份底本都是**透明的** —— 那是底本的設計取捨，不是它們壞了。
#: ⛔ 改它之前請先讀那一列（有「待答／由誰查／觸發點」三欄）。
_FAILURE_ALLOWANCE: str = "或為什麼查不到"

#: ⚠️ **刻意不承諾「下面會有什麼」，只承諾「這一輪去查了」。**
_CARD_HEALTH_CHECKED: str = (
    f"{_CARD_HEALTH_ASKS}這一輪已經去查了 —— 查到什麼、{_FAILURE_ALLOWANCE}，在下面那一塊。")

_CARD_NAV_TITLE: str = "🗂️ 雲端 NAV 累積"
#: ⛔ **這一句刻意「與 gate 狀態無關」，而且那是修過的（讀完再改）**：
#:    初版寫「**還沒去查** —— 查一次會連上你的 Google 試算表。」，
#:    而 :data:`NAV_GATE_LABEL` 那一顆 gate **沒有帶 `key=`**
#:    （帶了會違反 `test_the_page_writes_only_its_own_session_key`），
#:    所以結論層**讀不到它勾了沒有**。使用者一旦把它勾起來，
#:    Streamlit 會在下一輪把那顆 checkbox 還原成「已勾」——
#:    **這張卡卻會繼續說「還沒去查」，那是一句當場就假掉的話。**
#:    （本組在 off-repo 真渲染時看到的：`_diag_gate_is_on()` 為真的那一輪，
#:     這張卡仍印「還沒去查」。**AST 看不到它，只有真的把頁面跑起來才看得到。**）
#:    → 現行文案只講**機制**（「它要按一下才會去讀」），機制不隨勾沒勾而改變。
#: ⛔⛔ **2026-09-09 第三輪回修：這張卡是「承諾了結果卻沒有對沖」的第四個站點
#:    （有意識的更正，不是漏刪 · 決策者：AI 總管，依獨立稽核）。**
#:
#:    舊值末句 ~~「它要按一下才會去讀你的 Google 試算表。」~~ ——
#:    **「答案在下面那一塊」是一句承諾，而那一塊會失敗。**
#:    **本組實測的那個世界**（`NAV_GATE_LABEL` 勾起 ＋ `fetch_nav_coverage` 拋例外）：
#:    底下出現的是 :func:`safe_section` 的**一個紅框**，`[Error]` 元素 1 個、
#:    而這張卡仍在說「答案在下面那一塊」—— **在那個畫面上它是假的。**
#:
#: ⛔ **這一筆的重點不是「少寫了一句對沖」，是【同一把尺沒有對第四個對象重跑】**：
#:    第二輪回修時 :data:`_FAILURE_ALLOWANCE` 已經被抽成共用常數、
#:    結論 ＋ 🔭 卡 ＋ 🔑 卡三處都補上了，**唯獨這一張沒有**，
#:    而那條守衛的下限剛好寫 `_WANT = 3` —— **數字等於「已經補好的那幾個」，
#:    於是它結構上看不到第四個。**（`EXCEPTIONS.md §8.2.A.1` 驗證段 ④：
#:    更正一個被點名的項目時，必須把同一把尺對**全部**同類項目重跑。）
#: ⚠️ **本卡「與 gate 狀態無關」那個設計沒有被推翻**（`NAV_GATE_LABEL` 沒有 `key=`、
#:    這一層讀不到它勾了沒有）—— 對沖講的是**那一塊會不會失敗**，不是**開關勾了沒有**，
#:    兩者正交。**改的只有「承諾要不要對沖」，不是「要不要講狀態」。**
_CARD_NAV_NOTE: str = (
    "回答：長期指標（年化、最大回撤）有沒有足夠的歷史可以算。"
    "答案在下面那一塊 —— 它要按一下才會去讀你的 Google 試算表，"
    f"讀到什麼、{_FAILURE_ALLOWANCE}，都在那裡。")

_CARD_KEYS_TITLE: str = "🔑 金鑰與連線"
#: ⛔⛔ **2026-09-08 回修：舊表述把使用者指到一個裝不下那個答案的地方
#:    （有意識的更正，不是漏刪 · 決策者：AI 總管，依獨立稽核）。**
#:
#:    舊值 ~~「…**先看這一塊再看上面。**」~~ ＋ `where=_below(BLOCK_KEYS)`。
#:    **而「說那一塊回答不了金鑰」的人就是本檔自己** —— :func:`_render_keys` 的
#:    docstring 逐字寫著「「API 金鑰狀態 / NAS Proxy 測試」仍住在
#:    `render_data_guard_tab()` 深處……**這是已知缺口，不是漏做**」。
#:
#:    **總管實測（本組已逐條重跑，不是轉述）**：
#:    - :func:`_render_keys` 全身只有兩件事：`render_policy_admin_bridge(sheet_client=None)`
#:      ＋ 一顆 gate 後面的 `render_fetch_diag_from_session()` —— **兩者都不是金鑰**；
#:    - `merge_context.POLICY_ADMIN` **production 0 個持有點**
#:      （`git grep POLICY_ADMIN -- '*.py' | grep -v ^tests/` 命中全在定義／`__all__`／
#:      docstring／`policy_admin_bridge` 自己的檢查，**`app.py` 一次都沒持有**）
#:      → 那一支**恆走灰態、指去舊 ④**；
#:    - 金鑰與 Proxy 實際住在 `ui/tab5_data_guard.py` 的
#:      `### ④ 🔑 API 金鑰狀態` 與 `### ③ 🌐 NAS Proxy 中繼站狀態`
#:      —— 那支由**「資料來源健康度」**那一塊委派，在 :data:`DIAG_GATE_LABEL` 之後。
#:
#: ⛔ **修法是【照實說它今天裝不下、答案在哪】，不是把金鑰從 tab5 抽出來**
#:    —— 抽出來要動舊模組，**本批明令不准**；而線框 §3 末項自己寫的是
#:    「連線與金鑰**要真的裝得下它的標題**……**登記，不動工**」——
#:    **線框授權的是「不動工」，不是「加一張卡宣稱它已經裝得下了」。**
#: ⛔⛔ **2026-09-08 第二輪回修：這張卡原本是常數、不翻面，而它講的是
#:    **同一顆**已經被上面四行讀過的開關（有意識的更正，不是漏刪 · 決策者：AI 總管）。**
#:
#:    舊值最後一句 ~~「要先打開那個開關才看得到。」~~ **在開關已經打開時是假的** ——
#:    同一個畫面上，結論那一行說「這個開關**已經打開了**」，四行之後這張卡說「**要先打開**」。
#:
#: ⛔ **它不能引用 :data:`_CARD_NAV_NOTE` 那個免責**：那張卡之所以可以講「機制、不講狀態」，
#:    是因為 :data:`NAV_GATE_LABEL` **沒有 `key=`、讀不到**。
#:    **🔑 卡沒有這個免責** —— 它講的是 :data:`DIAG_GATE_LABEL`，
#:    而那顆的狀態 :func:`_diag_gate_is_on` **就在上面四行被讀過**。
#: ⇒ **會讀得到卻不讀，就是本檔自己批判過的「一句當場就假掉的話」。**
_CARD_KEYS_ASKS: str = (
    "回答：上面那兩件事**能不能做**。"
    "⚠️ **底下「連線與金鑰」今天回答不了金鑰** —— 它裝的是保單管理的指路與抓取診斷開關；"
    "**API 金鑰與 NAS Proxy 的狀態住在「{block}」的委派深處**，")
#: ⛔⛔ **2026-09-09 第四輪回修：這是「承諾了結果卻沒對沖」的【第五個】站點
#:    （有意識的更正，不是漏刪 · 決策者：AI 總管，依第四輪獨立稽核）。**
#:
#:    舊值末句 ~~「要先打開那個開關才看得到。」~~ ——「**才看得到**」是一句承諾：
#:    它對使用者說「按下去就會看到」。而按下去之後委派可能整塊炸掉，
#:    底下是 :func:`safe_section` 的紅框。
#: ⛔ **這一筆的重點是【它在哪個世界】**：這一支是**開關還沒打開**時的文案 ——
#:    也就是**使用者第一次進站看到的那一個畫面**。而上一輪補的那條守衛
#:    `test_anything_that_promises_a_result_also_allows_failure`
#:    **只跑「開關已開」那一個世界** ⇒ 它結構上看不到這一句。
#:    **本組實測**：gate=False 時整個結論層的對沖只有 **1** 句（NAV 卡那一句），
#:    而 `要先打開那個開關才看得到` 就印在同一個畫面上。
#: ⇒ **同一把尺沒有對「另一個世界」重跑**，與 W2 是同型（那次是沒對第四張卡重跑）。
_CARD_KEYS_UNCHECKED: str = _CARD_KEYS_ASKS + "要先打開那個開關才看得到（{fail}）。"
#: ⚠️ **開關已開的那一支必須帶 :data:`_FAILURE_ALLOWANCE`（必修 B）**：
#:    委派炸掉時底下是紅框，「就看得到」會變成第二句假話。
_CARD_KEYS_CHECKED: str = _CARD_KEYS_ASKS + "而那個開關已經打開了 —— 往下捲就看得到（{fail}）。"


def nav_status_label() -> str:
    """「NAV 累積狀態」—— **走 SSOT，不抄線框字面**（`_SECTION_LABELS["nav_status"]`）。

    ⚠️ 做成函式而不是 module 層常數，是為了讓 `section_label()` 的
    §1 Fail Loud（未知 key 直接 `KeyError`）發生在**渲染當下**而不是 import 期 ——
    import 期炸掉會讓整個 `ui.views` 套件無法載入，連帶打死其他四頁。
    """
    return section_label("nav_status")


def nav_manual_label() -> str:
    """「手動補資料」—— **走 SSOT，不抄線框字面**（`_SECTION_LABELS["nav_manual"]`）。"""
    return section_label("nav_manual")


def maintain_label() -> str:
    """「🗄️ 資料維護與通報」—— **走 SSOT**（`_SECTION_LABELS["manage"]`）。

    ⚠️ 這一塊在線框 Tab 05 裡**沒有位置**，見模組 docstring 的 (D-5)。
    名字仍然走 SSOT，是因為它**確實有一個 key**（不像 `BLOCK_HEALTH` 那三個）——
    有 key 卻手抄字面，是本 repo 已經發作過三次的那個病。
    """
    return section_label("manage")


def _backfill_gate_label() -> str:
    """「手動補資料」gate 的標籤（理由同 :func:`_maintain_gate_label`）。"""
    return BACKFILL_GATE_LABEL


def _backfill_not_loaded_note() -> str:
    """「手動補資料」gate 沒勾時的灰態本文。

    ⛔ **規格同 :func:`_maintain_not_loaded_note`：把真正的原因寫出來，
       不准改寫成「效能考量」之類的含混說法**（總管 2026-09-07 明令）。
    """
    return (
        f"尚未載入本頁自己的手動補資料 —— 舊的「{tab_label('settings')}」分頁"
        "**已經在跑同一塊**（三條寫入路徑都在裡面），同一次畫面更新載入兩份會撞到 "
        "Streamlit 的重複元件鍵（`_nh_*` / `navhist_import_*` 等 11 個）。"
        f"⚠️ **勾上面那個選項，這一塊就會變成紅色錯誤**"
        "（CI 實測訊息：`StreamlitAPIException: multiple identical forms with "
        "key='navhist_import_form'`）—— 勾它只能看到這一塊在新版版面上的位置，"
        "**看不到能用的表單**。"
        f"要真的補資料，請到舊的「{tab_label('settings')}」分頁，那一份完好、隨時可以用。")


def _maintain_not_loaded_note() -> str:
    """「🗄️ 資料維護與通報」gate 沒勾時的灰態本文。

    ⛔ **這段話刻意把真正的原因寫出來，不准改寫成「效能考量」之類的含混說法**
       （總管 2026-09-07 裁決 2 明令）。使用者看到同一塊東西在舊 Tab 有、
       在新 Tab 要自己勾，如果我們給的理由是假的，他只會以為新頁功能比較少。

    ⚠️ **是函式不是常數，而且分頁名走 `tab_label('settings')`** ——
       本檔第一版把「⚙️ 設定與診斷」六個字直接寫進字串，被
       `tests/test_wpf_five_tab_wiring.py` 的**兩條**守衛同時抓到
       （黑名單向：活字串命中現行分頁名；形態向：「「X」分頁」的指路形狀
       沒有經過 story_nav 求值）。**那兩條是對的** —— 手抄的分頁名在下一次
       改名時會變成死指路，本 repo 同型 bug 已發作三次。
    """
    return (
        f"尚未載入本頁自己的維護區 —— 舊的「{tab_label('settings')}」分頁"
        "**已經在跑同一個維護區**，同一次畫面更新載入兩份會撞到 Streamlit 的"
        "重複元件鍵（`divcal_gen` / `manage_notify_preview` / `pool_*` 等）。"
        f"⚠️ **勾上面那個選項，這一塊就會變成紅色錯誤**"
        "（CI 實測訊息：`StreamlitDuplicateElementKey: key='divcal_gen'`；"
        "選股池那一份另外被降級成一則黃框「選股池管理載入失敗 … `pool_add_form`」）"
        "—— 勾它只能看到這一塊在新版版面上的位置，**看不到能用的維護區**。"
        f"要真的用，請到舊的「{tab_label('settings')}」分頁，那一份完好、隨時可以用。")


def _maintain_gate_label() -> str:
    """「🗄️ 資料維護與通報」gate 的標籤（區塊名走 SSOT，不手抄）。

    ⚠️ 標籤 ＝ :data:`MAINTAIN_GATE_LABEL`；本函式存在的理由是讓
    **灰態指路**與 **checkbox 本體**吃同一個字串 —— 兩邊各寫一份，
    改一邊就會指到一個畫面上不存在的選項（本 repo 同型 bug 已發作三次）。
    """
    return MAINTAIN_GATE_LABEL


def _manual_gate_label() -> str:
    """「使用手冊」gate 的標籤（理由同 :func:`_maintain_gate_label`）。"""
    return MANUAL_GATE_LABEL


def _manual_not_loaded_note() -> str:
    """「使用手冊」gate 沒勾時的灰態本文。

    ⛔ **規格同 :func:`_maintain_not_loaded_note`：把真正的原因與「勾下去會怎樣」
       兩件事都寫在畫面上，不准只留在 `help=` 的 tooltip 裡**（總管 2026-09-07 明令）。

    ⚠️ **本則刻意只點名「指標地圖」一項，不寫「全部都會撞」** —— CI 實測（run
       34147466074，`abecc1c`）在預設載入時**只噴一則**紅字「指標地圖載入失敗」。
       同一支 `render_manual_tab()` 底下另有 4 個 `st.dataframe` ＋ 1 個
       `st.plotly_chart` 同樣不帶 key（本組 AST 實測），**但它們沒有撞** ——
       內容不同 ⇒ 自動 ID 不同。**那是實測結果，不是保證**：哪天內容改成一樣就會撞。
    """
    return (
        f"尚未載入本頁自己的使用手冊 —— 舊的「{tab_label('settings')}」分頁"
        "**已經在跑同一份說明書**（十章 ＋ 錨點目錄，一個字都不少），"
        "而它底下的「指標地圖」用的 `st.plotly_chart` **沒有帶 key**，"
        "同一次畫面更新畫兩份會撞到 Streamlit 自動產生的 element ID。"
        f"⚠️ **勾上面那個選項，本頁會載入自己的一份，但「指標地圖」那一小塊會變成紅字**"
        "（CI 實測訊息：`StreamlitDuplicateElementId: multiple plotly_chart elements "
        "with the same auto-generated ID`）；其餘章節照常顯示。"
        f"要看完整的說明書，請到舊的「{tab_label('settings')}」分頁。")


def _fetch_diag_gate_label() -> str:
    """「🔍 抓取診斷細節」gate 的標籤（理由同 :func:`_maintain_gate_label`）。"""
    return FETCH_DIAG_GATE_LABEL


def _fetch_diag_not_loaded_note() -> str:
    """「🔍 抓取診斷細節」gate 沒勾時的灰態本文。

    ⚠️ **這一則的措辭與另外三則不同，因為它擋的東西不會報錯** ——
       重複渲染在畫面上是**兩塊一模一樣的面板**，Streamlit 一聲都不吭。
       ⛔ **「不噴錯」不等於「沒事」**：使用者分不出哪一份是活的，
       而這一頁的職責正是回答「資料可不可信」（§1：錯誤的數字比沒有數字更危險，
       兩份一樣的數字則是讓人不知道該信哪一份）。
    """
    return (
        f"尚未載入本頁自己的抓取診斷細節 —— 舊的「{tab_label('settings')}」分頁"
        "**已經在跑同一塊**，而 `app.py` 的 `FETCH_DIAG` 旗標**分不出新舊兩個消費者**"
        "（它的設計前提是「全站只有一個」，雙軌之後前提破了）。"
        f"⚠️ **勾上面那個選項，畫面上會出現兩塊一模一樣的「🔍 抓取診斷細節」** ——"
        "**不會報錯**，但你分不出哪一份是活的。"
        f"要看抓取診斷，請到舊的「{tab_label('settings')}」分頁。")


def _where(block: str) -> str:
    """本頁內的指路。**回傳的必須是一個「地方」。**

    `render_state.not_ready()` 會把它包成「（請先到：…）」——
    也就是說回傳值會變成一句**祈使句的受詞**。塞一句狀態陳述進去
    （「目前只有 X 是完整的」）會產生一句**不可執行的指令**：那是 ③
    `ui/views/page_03_research.py` 2026-09-05 被獨立紅隊實測抓到的錯。

    ⚠️ 分頁名走 `where_to_find()`，**不手抄**；區塊名由呼叫端傳進來，
    不在這裡再抄一份（手抄的指路在本 repo 已經指錯三次）。
    ⚠️ **刻意不用「」把 `block` 括起來**：`tests/test_batch2_top_card_grid.py::`
    `test_every_where_names_something_that_exists_on_screen` 只對 ``「」`` 內的
    **字面值**比對「畫面上有沒有這個字」，而它的字表不收 `st.caption`。
    """
    # ⛔ **已登記：`EXCEPTIONS.md §8.3.P` 的 `P-GATEDLAYOUT-1` 第 (2) 筆。**
    #    `where_to_find("settings")` ＝ `⑤ ⚙️ 設定與診斷` —— **那是【舊】分頁**，本頁是 ⑦；
    #    而舊 ⑤ **沒有**叫「連線與金鑰」的區塊（AST 實測：非-docstring 字串 0 命中，
    #    `grep -c` 的 5 命中全是註解在談未來的 T18 重組）。
    #    ⛔ **既有債**（兩個呼叫點都在 `origin/main` 上），**#835 未修**。

    return f"{where_to_find('settings')} → {block}"


def _holdings() -> list[dict[str, Any]]:
    """使用者**目前持有**的基金。讀既有 session 契約，**不自己取數**。

    ⚠️ **這裡刻意`不`做 `loaded` 過濾**，與 `page_02_health.py` / `page_04_portfolio.py`
    的同名函式**故意不同** —— ②④ 問的是「**拿這幾檔去算**」，沒載入的算進去會生出
    一個不完整的結論（§1）；⑤ 的 NAV 累積狀態問的是「**雲端歷史涵蓋了哪幾檔**」，
    而「已列入但還沒抓回來」正是這一頁最該顯示的那一種。
    """
    _cur = st.session_state.get(_SK_PORTFOLIO)
    return [_f for _f in _cur if isinstance(_f, dict)] if isinstance(_cur, list) else []


def _as_int(value: Any) -> int | None:
    """能算出整數就回它，**算不出來回 `None`，不回 `0`**。

    ⛔ **`0` 是一個宣稱（「一筆都沒有」），`None` 是「我不知道」** —— §1 的分界就在這裡。
    """
    if value is None:
        return None
    try:
        return int(value)
    except (TypeError, ValueError):
        return None


def _parse_day(value: Any) -> "_dt.date | None":
    """把一端的日期字串 parse 成 `date`；**parse 不出來回 `None`，不猜**。"""
    try:
        return _dt.date.fromisoformat(str(value).strip())
    except (TypeError, ValueError):
        return None


def span_days_or_unknown(first: Any, last: Any, reported: Any) -> int | None:
    """跨度**可不可信** —— 不可信回 `None`。**全檔唯一決定「要不要印跨度」的地方。**

    ⛔ **為什麼不能直接用上游的 `span_days`（＝總管裁決 2 的全部理由）**
    ----------------------------------------------------------------
    `services/nav_history_gs.py::coverage_status` 在日期 parse 失敗時
    **把「未知」編成 `0`** —— 它自己那一行的註解就寫著「**跨度未知**，點數仍誠實回報」。
    而 `norm_date_key()` **刻意讓壞日期的原字串通過**（「不靜默丟資料（§1：不猜）」），
    所以壞日期真的會走到這裡。

    **本組端到端重現（`coverage_status(_sheet=…)` 注入假 worksheet，非推論）**：

    ====================================================  =====================
    上游回傳                                                真實跨度
    ====================================================  =====================
    `BBB {'first': '113/01/02', 'last': '2025-06-01', 'span_days': 0}`   **約 1.4 年**
    `DDD {'first': '2024-05-05', 'last': '2024-05-05', 'span_days': 0}`  **真的 0 天**
    ====================================================  =====================

    **兩者在舊塊的畫面上長得一模一樣**（涵蓋天數 0 · ≈年 0.0）。

    做法：**用兩端自己重算一次**（同一條算式，所以上游算得出來時兩者必然相等）
    ---------------------------------------------------------------------
    1. **任一端 parse 不出來 → 回 `None`。** 上游的 `0` 在這種情況下是「放棄」不是「零」。
    2. **兩端都 parse 得出來，但與上游回報的值不一致 → 也回 `None`。**
       兩邊用的是同一條算式，不一致代表**至少有一個前提不成立**。
       ⚠️ 這個分支在今天的 L2 下不會發生，它是**兩層之間的契約檢查**。
    3. 兩端都 parse 得出來且一致 → 回那個數字。

    ⛔ **不要改成「`first != last` 而 `span_days == 0` 就當作未知」那種啟發式。**
    它有一個**真的偽陽性**。

    ⚠️ **2026-09-06 就地更正：舉的例子是假的（有意識的更正，不是漏刪）。**
    舊表述：~~「`date.fromisoformat` 在 3.11+ 接受 `2024-1-1` 這種變體，所以
    `"2024-01-01"` 與 `"2024-1-1"` **字串不同、都 parse 得出來、而且是同一天**」~~
    —— **實測推翻**（`datetime.date.fromisoformat` 逐版跑過，非推論）：

    ============  ==========  ============  ==============  ==============
    Python         `2024-1-1`  `20240101`    `2024-W01-1`    `2024-01-01`
    ============  ==========  ============  ==============  ==============
    3.10.20        ValueError  ValueError    ValueError      2024-01-01
    3.11.15        ValueError  2024-01-01    2024-01-01      2024-01-01
    3.12.3         ValueError  2024-01-01    2024-01-01      2024-01-01
    3.13.12        ValueError  2024-01-01    2024-01-01      2024-01-01
    ============  ==========  ============  ==============  ==============

    **3.11 放寬的是大部分 ISO-8601 格式，`2024-1-1` 這種未補零的欄位不在其中。**
    ⚠️ **同一個 PR 的守衛檔早就量過並寫下了正解**
    （:func:`~tests.test_wf05_settings_skeleton.test_span_days_or_unknown_is_a_pure_decision`
    的 docstring），**但本檔沒有跟著改** —— 一份 diff 內同時放著一個假宣稱與它的反證。

    **舊表述的顧慮仍然成立，被推翻的只有例子**：`"20240101"` 與 `"2024-W01-1"`
    **都 parse 得出來、都等於 `2024-01-01`、字串卻不同** —— 那條啟發式的偽陽性是真的，
    只是觸發它的是這兩個寫法。本函式不用那條啟發式，所以沒有那個偽陽性。

    ⚠️ **本函式仍然守不到的（照實列）**：上游 `first`/`last` 取的是**字串排序**的頭尾
    （`sorted(_ds)`），混入非 ISO 字串時**頭尾可能不是時序上的頭尾**。
    「兩個都 parse 得出來、順序卻是錯的、而且差值剛好為正」這種情況**抓不到**。
    ⛔ **這一段不在本批的射程內**（要修得動 L2 的排序），**登記，不是沒看到**。
    """
    _a, _b = _parse_day(first), _parse_day(last)
    if _a is None or _b is None:
        return None
    _computed = (_b - _a).days
    if _computed < 0 or _computed != _as_int(reported):
        return None
    return _computed


def coverage_headline(coverage: dict[str, Any]) -> str:
    """涵蓋度的一句話總結 —— **只講數量，不講跨度**。

    ⛔ **這裡刻意不放「最長 N 年」**（線框的示意值正是「42 檔 · 最長 6.2 年」）。
    理由不是「線框的數字是假的」（那由 D-3 管），而是**跨度單獨出現會說謊**。
    跨度只在 :func:`coverage_line` 裡出現，而且**永遠與點數同行**。

    ⚠️ 純函式、無 I/O —— 這樣「這句話怎麼算出來的」可以被單獨測。
    """
    _readable = 0
    _points = 0
    _unknown = 0
    for _e in coverage.values():
        # ⛔ **兩種「讀不出來」都要算進 `_unknown`，不能只防一種**：
        #    `{'AAA': None, 'BBB': 'x', 'CCC': [1]}` 若無聲丟棄會印成
        #    「**0 檔 · 共 0 筆**」，那是一句**斷言**（你什麼都沒累積），
        #    而事實是我們收到了三筆讀不懂的東西（§1）。
        if not isinstance(_e, dict) or (_n := _as_int(_e.get("points"))) is None:
            _unknown += 1
            continue
        _readable += 1
        _points += _n
    # ⚠️ **「可讀取」三個字是承重的**：沒有它，「0 檔」會被讀成「你一檔都沒累積」。
    _head = f"可讀取 {_readable} 檔 · 共 {_points} {POINTS_UNIT}"
    if _unknown:
        _head += f"（另有 {_unknown} 檔讀不出來，未計入）"
    return _head


def coverage_line(code: str, entry: dict[str, Any], *, held: bool = False) -> str:
    """一檔的明細行。**全檔唯一被允許讀 `span_days` 的地方。**

    ⛔ **跨度永遠與點數同行，這是本函式存在的唯一理由**：
    `span_days` 是 `last - first`，**中間有沒有斷掉它一個字都沒說**。
    單獨印一個「6.2 年」會被讀成「我有六年的完整歷史」，而真相可能是兩個點。

    ⚠️ **守衛靠的是「機制」不是「這一行長怎樣」**：
    (a) AST 驗 `"span_days"` 這個鍵**只在本函式內被讀**；
    (b) 對任意輸入驗「輸出裡只要有 :data:`SPAN_PHRASE`，就一定有點數」。

    Parameters
    ----------
    held : 這一檔是不是**這個 session 已列入**的持倉。
           ⚠️ 只是一個標記；**沒列入不代表使用者沒有它**（`portfolio_funds`
           開站不自動載入），所以沒有標記的那些**不寫任何否定的話**。
    """
    _points = _as_int(entry.get("points"))
    # ⛔ **不准直接用上游的 `span_days`** —— 它把「未知」編成 `0`。
    _span = span_days_or_unknown(entry.get("first"), entry.get("last"),
                                 entry.get("span_days"))
    _first = str(entry.get("first") or "").strip() or "?"
    _last = str(entry.get("last") or "").strip() or "?"
    _mark = " ・本 session 已列入" if held else ""
    if _points is None:
        # ⛔ **點數算不出來 → 這一行連跨度都不印。**
        return f"- `{code}`{_mark}：這一筆的點數讀不出來（原始值 {entry.get('points')!r}）"
    if _span is None:
        return (f"- `{code}`{_mark}：**{_points} {POINTS_UNIT}**"
                f"（{_first} → {_last}，首末日期算不出天數）")
    return (f"- `{code}`{_mark}：**{_points} {POINTS_UNIT}**"
            f"（{_first} → {_last}，{SPAN_PHRASE} {_span} 天）")


def coverage_lines(coverage: dict[str, Any], held_codes_: set[str]) -> list[str]:
    """逐檔明細的全部行，**依代碼排序**（穩定順序，不隨 dict 插入序漂移）。

    ⛔ **讀不懂的條目也要有一行**：整個濾掉的話，「收到三筆讀不懂的東西」與
    「什麼都沒有」在畫面上一模一樣，而且會畫出一個**空的**展開器（違鐵則 04）。
    ⚠️ **排序 key 走 `str(...)`**：代碼理論上可能不是字串，混型別直接 `sorted()` 會炸。
    """
    _out: list[str] = []
    for _c, _e in sorted(coverage.items(), key=lambda _kv: str(_kv[0])):
        if not isinstance(_e, dict):
            # ⚠️ 只印**型別**不印值：一個壞掉的條目可能是很大的東西。
            _out.append(f"- `{_c}`：這一筆讀不出來"
                        f"（收到 {type(_e).__name__}，不是預期的欄位表）")
            continue
        _out.append(coverage_line(_c, _e, held=str(_c).upper() in held_codes_))
    return _out


def held_codes() -> set[str]:
    """這個 session 已列入的基金代碼（大寫）。**只用來加標記，不用來過濾。**

    ⛔ **不得**拿它去縮小 `coverage_status()` 的範圍：雲端累積的是**整張表**，
    用一個「開站不會自動載入」的 session 鍵去過濾，等於把使用者真的有、
    但這個 session 還沒載入的那些**藏起來**（§1：不知道 ≠ 沒有）。
    """
    return {str(_f.get("code", "")).strip().upper()
            for _f in _holdings() if str(_f.get("code", "")).strip()}


def _below(block: str) -> str:
    """**同一頁往下捲**的指路（線框 §3 逐字的形狀：「去哪補 ─ 下方「X」」）。

    ⚠️ 與 :func:`_where` 刻意不同，兩個都要留
    ------------------------------------------
    :func:`_where` 產生的是「**⚙️ 設定與診斷 → X**」—— 那是給**別頁**看的座標，
    它前面掛著分頁名。結論層那三張卡**就長在這一頁的最上面**，
    對它們講「請先到 ⚙️ 設定與診斷」是一句**繞回原地**的指令。

    ⛔ **不要「統一」成一個** —— 那會讓其中一邊開始說謊。本 repo 對指路的既有教訓是
    「指到一個不存在的按鈕比沒有指路更糟」；**指到使用者已經站著的地方**是同一族的錯。

    ⚠️ **刻意不用 `where_to_find()`**：那支回的是**分頁**名，而本函式指的是
    **同一頁裡的一個區塊**。⚠️ **`block` 由呼叫端傳進來**，不在這裡再抄一份字面值。
    """
    return f"下方「{block}」"


def _diag_gate_is_on() -> bool:
    """資料來源健康度的 gate 現在勾著沒有。**只讀 session，不呼叫任何取數。**

    ⚠️ **這是結論層唯一拿得到的「狀態」，而且它是免費的** ——
    :data:`_SK_DIAG_GATE` 是本頁**自己**寫進 session 的那一個鍵
    （`test_the_page_writes_only_its_own_session_key` 明訂本頁只准寫它）。

    ⛔ **另外三顆 gate 讀不到，這是刻意的，不是漏做**：它們**沒有帶 `key=`**
       （帶了就會違反上面那條守衛），所以 session 裡根本沒有它們的值。
       ⛔ **不要為了讓這一格更聰明而去替它們補 `key=`** —— 那是拿一條守衛換一句話。

    ⚠️ **時序上這是「上一輪」的值**：結論層排在 checkbox 之前，
       Streamlit 在 script run 開始時就把上一輪的 widget 值放回 session，
       所以第一次進站是 `False`（正確），勾起來之後那一輪起是 `True`（正確）。
    """
    return bool(st.session_state.get(_SK_DIAG_GATE))


def _conclusion_cards() -> list[dict[str, Any]]:
    """結論層三張卡的定義。**純函式、不畫東西、不讀任何外部資料** —— 這樣它可以被單獨測。

    ⛔ **三張全是 `STATE_NOT_READY`，這不是偷懶，是這一頁今天的實況**：
       本頁六塊裡有四塊在 Checkbox Gate 後面、一塊（連線與金鑰）預設只畫一句灰色指路。
       **預設載入的 ⑦ 身上一個真數字都沒有。**
       畫一張 `STATE_OK` 的綠卡需要先知道「它是好的」，而我們一次都還沒查
       —— `state_card()` 的 docstring 逐字：**「不要在未載入時填一個假的東西」**。
       ⚠️ 反過來說，**這三張灰卡本身就是本頁最誠實的一句話**：
          「你現在看到的不是『沒問題』，是『還沒查』。」

    ⚠️ **`where` 一律走 :func:`_below`**：三張卡指的都是**同一頁下面**那一塊。
    """
    # ⭐ **兩張卡都吃同一顆開關的狀態** —— 🔑 卡在第二輪回修前是常數、不翻面，
    #    而它講的正是這一顆（見 :data:`_CARD_KEYS_UNCHECKED` 的回修註記）。
    _on = _diag_gate_is_on()
    _health_note = _CARD_HEALTH_CHECKED if _on else _CARD_HEALTH_UNCHECKED
    _keys_note = ((_CARD_KEYS_CHECKED if _on else _CARD_KEYS_UNCHECKED)
                  .format(block=BLOCK_HEALTH, fail=_FAILURE_ALLOWANCE))
    return [
        {"title": _CARD_HEALTH_TITLE, "note": _health_note,
         "state": STATE_NOT_READY, "where": _below(BLOCK_HEALTH)},
        {"title": _CARD_NAV_TITLE, "note": _CARD_NAV_NOTE,
         "state": STATE_NOT_READY, "where": _below(nav_status_label())},
        # ⚠️ **指路刻意不是 `BLOCK_KEYS`** —— 見 :data:`_CARD_KEYS_NOTE` 的回修註記：
        #    金鑰與 Proxy 住在「資料來源健康度」的委派深處，不在「連線與金鑰」那一塊。
        {"title": _CARD_KEYS_TITLE, "note": _keys_note,
         "state": STATE_NOT_READY, "where": _below(BLOCK_HEALTH)},
    ]


def _render_conclusion() -> None:
    """🧾 ① 結論 —— **一句話 ＋ 三張卡，沒有第四樣東西**（線框 §1 四層閱讀順序）。

    ⛔ **這一層的驗收標準是「有沒有回答那個問題」，不是「有沒有更多資訊」。**
       客戶第二句原話是「**我覺得舊 UI 資訊太多**」—— 在這裡加第四張卡、
       加一張表、加一段教學，就是把密度搬回來。

    ⚠️ **本層一次外部呼叫都沒有，這是硬性的**（理由見 :data:`_VERDICT_UNCHECKED`
       的長註記）：`fetch_nav_backend_status()` / `fetch_nav_coverage()`
       在兩顆 gate 之前**一次都不准被叫**，而結論層排在所有 gate 之前。

    📌 **本層畫不出線框那句帶判定的結論，已登記為 `EXCEPTIONS.md §8.3.P` 的
       `P-P05VERDICT-1`**（觸發點主線：**舊 ⑤ 下架那一批**）。完整理由見 (D-6)。
    """
    st.markdown(CONCLUSION_HEADING)
    # ⚠️ **`where=` 不是可選的**（`tests/test_batch2_top_card_grid.py::`
    #    `test_where_is_mandatory`，`WHERE_MISSING_EXEMPT` 是空的、而且應該保持空的）。
    #    線框 Rule 04 的三要素是「標題 / 缺什麼 / **去哪補**」，
    #    而 `render_state.py` 自己寫著「沒有它，占位只是把『消失』換成『灰色的消失』」。
    # ⭐ **兩個分支指去不同的地方，這是刻意的**：
    #    還沒查 → 指**那一顆要按的開關**（使用者的下一步就是它）；
    #    查過了 → 指**結果住的那一塊**（開關已經按過，再指它就是繞回原地）。
    # ⭐ **兩句都必須把 :data:`DIAG_GATE_LABEL` 帶進本文，這是硬要求不是文風**：
    #    這一層唯一量得到的東西就是那一顆開關，**一句沒有指名自己量了什麼的結論，
    #    不是結論，是斷言**。守衛：`test_the_verdict_names_the_only_switch_it_can_see`。
    if _diag_gate_is_on():
        not_ready(_VERDICT_CHECKED.format(gate=DIAG_GATE_LABEL, block=BLOCK_HEALTH,
                                          fail=_FAILURE_ALLOWANCE),
                  where=_below(BLOCK_HEALTH))
    else:
        not_ready(_VERDICT_UNCHECKED.format(gate=DIAG_GATE_LABEL),
                  where=_below(DIAG_GATE_LABEL))
    render_cards(_conclusion_cards())


# ══════════════════════════════════════════════════════════════════════════
# 五個線框區塊（＋ 一個線框沒有給位置的，見 (D-5)）
# ══════════════════════════════════════════════════════════════════════════

def _render_source_health() -> None:
    """區塊 1｜資料來源健康度 —— **委派** `render_data_guard_tab()`，整塊在 gate 之後。

    線框：「每源最後成功時間與資料日期。」chip：「三態」。

    ⭐ **gate 的理由（本檔最重要的一條，總管裁決 3）**：
    `render_data_guard_tab()` 的開頭有一次**無條件的匯率抓取**
    （~~線框已點名~~ → **2026-09-06 就地更正，有意識的更正，不是漏刪**：
    點名它的是 **`docs/wireframes/fund-wireframe-final.html`**
    「每次操作都跑的匯率抓取」那一格，**不是**本檔其他各處所稱的
    `ia-wireframe.html` —— 實測 `grep '匯率' docs/wireframes/ia-wireframe.html` **0 命中**，
    整份 `ia-wireframe.html` 唯一的「匯」是 Tab 05 那句「CSV **匯入**淨值歷史」。
    **事實沒有變**（那一次抓取真的存在，見 `ui/tab5_data_guard.py::render_data_guard_tab`
    的 `get_latest_fx("USDTWD")`，不在任何按鈕之後）；
    改的是**指路指到哪一份文件**：本檔**凡是指名檔案的地方，指的都是**
    `ia-wireframe.html`（實測 —— 本檔出現過的 wireframe 檔名，除了本註新增的這一個
    之外**只有它**），所以不寫檔名會讓人去那一份裡面找一個 0 命中的東西。
    ⚠️ **這句刻意只講「指名檔案的地方」** —— 本檔「線框」二字出現 45 次，
    絕大多數沒有帶檔名，**本組沒有逐處讀完再判斷它們各自指誰**，故不作全稱宣稱），
    ＋ caller 契約要求先跑 `_update_data_registry()`。在單頁的 ⑤ 裡，這些會跟著
    本頁**每一次互動**重跑 —— gate 讓重運算留在使用者的一次點擊之後。
    ⛔ **拿掉這個 checkbox ＝ 打開 ⑤ 就對外取數。**

    ⚠️ **本檔不畫「18 源 · 2 異常」** —— 那是線框的示意值（D-3）。在這一頁上尤其毒：
       使用者進來就是要問「我的資料可不可信」，一個假的「2 異常」會直接被當成答案。
       ⛔ 而且 (A) 路線之下**根本沒有理由畫它**：真的來源清單由被委派的舊模組畫。
    ⚠️ **`NAV_HISTORY` 在這裡也要持有，而且這一項最容易漏** —— 所有權是
       thread-local ＋ context manager 作用域，別的區塊那個 `with` 一離開就還原了。
       漏掉它的後果不是報錯，是「🗂️ NAV 歷史匯入與累積狀態」**又在這裡畫一次**，
       而且沒有任何東西會叫。
    """
    _open = st.checkbox(
        DIAG_GATE_LABEL,
        key=_SK_DIAG_GATE,
        # ⛔ **2026-09-08 回修（有意識的更正，不是漏刪 · 決策者：AI 總管，依線框 §0 推翻 1）**
        #    舊值 ~~「診斷區**載入**時會更新資料註冊表並抓取匯率等即時狀態。
        #    沒遇到問題時可以完全略過（診斷頁自己的說明原文）。」~~
        #    —— 標籤與灰態都換成處境語言了，**`help=` 卻還在講「載入」**（系統在講自己）。
        #    **它會被漏掉不是巧合**：`_PROGRESS_WORDS` 那條守衛只掃 label ＋ note，
        #    **結構上看不到 `help=`** —— 本輪把文案與守衛射程一起修
        #    （`test_the_two_remaining_gates_stopped_talking_about_themselves_in_the_tooltip_too`）。
        #    ⚠️ 舊值括號裡自陳「診斷頁自己的說明原文」，那個**沿用**的理由仍然成立，
        #    只是被權衡掉了：沿用一份進度語言的原文，等於把要換掉的東西抄進來。
        help="按下去會更新資料註冊表、並抓一次即時匯率，大約要幾秒。"
             "沒遇到問題時可以完全略過。",
    )
    if not _open:
        # ⚠️ 指路指的是**上方那個 checkbox**，不是本區塊自己 —— 這一則是
        #    本頁少數「去了真的有用」的指路：勾起來就會載入。
        #    字面吃 :data:`DIAG_GATE_LABEL`，**不手抄**（手抄的那一刻它就開始漂移）。
        not_ready(_DIAG_NOT_LOADED_NOTE, where=f"上方「{DIAG_GATE_LABEL}」")
        return

    # caller 契約（`ui/tab5_data_guard.py` 的 docstring）：呼叫前先更新 data_registry。
    from ui.helpers.data_registry import _update_data_registry
    from ui.tab5_data_guard import render_data_guard_tab

    _update_data_registry()
    # ⑤ 已畫區塊標題 → 診斷頁不再畫自己的 `##` 頁面大標（其餘一行不動）。
    with settings_page_owns(DATA_GUARD_HEADER, NAV_HISTORY):
        render_data_guard_tab()


def _render_nav_status() -> None:
    """區塊 2｜NAV 累積狀態 —— **本檔實作，刻意不委派**（總管裁決 2，理由見 (D-4)）。

    線框逐字：「雲端歷史涵蓋度，逐檔可展開。」

    四種狀態，**一次只顯示一種**（順序不可顛倒）
    ---------------------------------------------

    ===========================  ==================================================
    狀態                          畫面
    ===========================  ==================================================
    gate 沒勾（**預設**）           ⬜ 尚未讀取（:data:`_NOT_LOADED_NOTE`）
    後端未啟用                     ⬜ 讀不到 ＋ **缺哪幾把 secret**
    讀到了、但一筆都沒有             空狀態（:func:`empty_state`）
    讀到了、有資料                   總結一句 ＋ 逐檔明細（展開器）
    ===========================  ==================================================

    ⛔ **「後端未啟用」必須排在「一筆都沒有」前面，這是總管裁決也是 §1。**
    `coverage_status()` 在**未啟用**與**工作表不存在**時**都回 `{}`** ——
    它自己的 docstring 逐字寫著「呼叫端須據此顯示『未啟用』而非『0 點』」。
    **本組已實測那條路徑**：`load_points()` 開頭
    ``if _sheet is None and not is_enabled() and oauth_client is None: return []``
    → `coverage_status()` 的 ``if not _pts: return {}``。**兩條路真的會匯流成同一個 `{}`。**

    ⚠️ **本塊不自己接 `try/except`**：`coverage_status()` 在來源冷卻期／真 I/O 失敗時
       **會拋 `NavHistoryError`**（那是 L2 刻意的 §1 行為，不是 bug）。
       由 :func:`render_settings_and_diagnostics` 幫本塊單獨包一層 `safe_section()`。
       自己 `except` 會變成吞例外（§1 違憲）。
    """
    _open = st.checkbox(
        NAV_GATE_LABEL,
        value=False,
        help="讀一次 Google Sheets 的 nav_history；沒勾就完全不連線。",
    )
    if not _open:
        # ⚠️ 同上：指的是上方那個 checkbox，字面吃 :data:`NAV_GATE_LABEL`。
        not_ready(_NOT_LOADED_NOTE, where=f"上方「{NAV_GATE_LABEL}」")
        return

    # ⛔ 先問「能不能看」，再問「看到什麼」—— 順序顛倒就會把「未啟用」講成「0 點」。
    _backend = fetch_nav_backend_status()
    if not _backend.get("enabled"):
        _missing = ", ".join(str(_m) for _m in (_backend.get("missing") or [])) or "（未回報）"
        not_ready(
            f"{_BACKEND_UNAVAILABLE_NOTE}{_missing}。"
            # ⚠️ **刻意不寫「這兩把」** —— `status()` 的 `missing` 可能只有一項。
            "這些是部署環境的 secret，畫面上改不了。",
            # ⚠️ **已登記：`EXCEPTIONS.md §8.3.P` 的 `P-GATEDLAYOUT-1` 第 (3) 筆**
            #    （2026-09-09 第四輪就地登記、第五輪收進該列；`§-1` ＋ §8.4 步驟 4）：
            #    這個指路指向「連線與金鑰」，而**那一塊今天回答不了 Google 授權**
            #    —— 與結論層 🔑 卡被擋下的是**同一個病**（指到一個答不出來的區塊）。
            # ⛔ **但它不是本批寫的**：實測 `origin/main` 上這一行就是 `_where(BLOCK_KEYS)`
            #    ⇒ **既有債**。順手修它會擴大 scope（§8.4 步驟 4：範圍由客戶決定）。
            # ⚠️ **本批建立的規則射程只到「稽核點名的那三張結論卡」**
            #    （`test_every_card_points_at_the_block_that_can_actually_answer_it`）——
            #    **它結構上看不到這一處**，因為那條只走 `_conclusion_cards()`。
            where=_where(BLOCK_KEYS))
        return

    _coverage = fetch_nav_coverage()
    # ⚠️ **展開器開不開，看的是「有沒有可渲染的行」，不是「dict 空不空」**：
    #    `{'AAA': None}` 這種**非空但整包讀不懂**的回傳，若照 dict 判斷會畫一個
    #    **空的**展開器（違鐵則 04）。現在每一個條目都會產出一行，
    #    所以 `_lines` 空 ⟺ `_coverage` 空 —— **這條性質是結構保證的，不是巧合。**
    _lines = coverage_lines(_coverage, held_codes())
    if not _lines:
        empty_state(_EMPTY_TITLE, _EMPTY_MISSING,
                    where=_where(nav_manual_label()),
                    footer=_EMPTY_FOOTER)
        return

    st.markdown(f"##### {coverage_headline(_coverage)}")
    with st.expander(NAV_DETAIL_LABEL, expanded=False):
        st.caption(
            f"「{SPAN_PHRASE}」量的是**第一筆到最後一筆**的日曆天數，"
            "**不代表中間每天都有**；能不能算長期指標看的是點數。")
        for _line in _lines:
            st.markdown(_line)


def _render_keys() -> None:
    """區塊 3｜連線與金鑰 —— **委派**保單管理橋接 ＋ 抓取診斷細節。

    線框：「Google 授權、Proxy、API 金鑰。」

    ⛔ **不畫「正常」** —— 線框示意值（D-3），而且是三個之中**最危險**的一個：
       它是一句**系統對自己健康狀態的斷言**。使用者會據此排除掉
       「是不是我金鑰過期」這個真正的原因。

    ⛔ **本檔不切換 `POLICY_ADMIN` 旗標**（總管指示，且有硬前置未解）：
       今天 `app.py` 一次都沒有持有它 → `render_policy_admin_bridge()` 只畫一句灰色指路，
       它掛的那一整支 Google Sheets 寫入是**死碼**。順手打開它會讓一整批寫入路徑活過來，
       而 `policy_admin_bridge` 的 docstring 明列了**三條尚未處置的硬前置**
       （session_state 先寫後讀耦合 / `sheet_client` 無 SSOT / oauth snapshot 紀律）。
       → 守衛：:func:`~tests.test_wf05_settings_skeleton.test_the_page_never_opens_the_policy_admin_flag`。

    ⚠️ **「API 金鑰狀態 / NAS Proxy 測試」仍住在 `render_data_guard_tab()` 深處**
       （舊 ⑤ 的模組 docstring 早已登記這一點）—— 抽出屬 tab5 的拆分批次，
       **(A) 路線明令不得改舊模組**，故本塊只承接兩個已可承接的對象。
       **這是已知缺口，不是漏做。**
    """
    render_policy_admin_bridge(sheet_client=None)
    # ⭐ **2026-09-07 第二輪雙軌修補：抓取診斷加 Checkbox Gate。**
    # **有意識的變更，不是漏刪**（決策者：AI 總管，客戶 2026-09-07 雙軌並行原則）。
    #
    # **舊寫法**（原地保留、加刪除線，不刪）::
    #
    #     ~~render_fetch_diag_from_session()~~      # ← 無條件，改到 gate 之後
    #
    # **舊寫法的理由一個字都沒有被推翻**：「個基頁那份由 `FETCH_DIAG` 旗標控制，
    # 而那個旗標**必須由 `app.py` 持有**才壓得住它，本檔刻意不在這裡包」——
    # 那句話**今天依然為真**，本次也**沒有**在這裡包旗標。
    #
    # **被權衡掉的是它的一個前提：「持有旗標 ⇒ 全站只剩一份」。那個前提破了。**
    # 旗標壓的是 ③（`ui/tab2_single_fund.py`）那一份，它只認「⑤ 這個頁面有沒有持有」，
    # **分不出「⑤」是舊的還是新的**。雙軌之後舊 ⑤ `_render_conn_section` 與本函式
    # **各畫一份**，畫面上就是兩塊一模一樣的「🔍 抓取診斷細節」。
    #
    # ⛔ **這一個與另外三個 gate 最大的差別：它不會報錯。**
    #    Streamlit 一聲都不吭 —— 沒有任何「只驗紅框 / 只驗例外」的守衛看得見它。
    #    **不噴錯不等於沒事**：使用者分不出哪一份是活的。
    # ⛔ **為什麼不改旗標**：那要動 `ui/helpers/settings_diag/merge_context.py`
    #    與 `app.py`，兩者都在本批的檔案邊界外（另一組正在 `app.py` 上）。
    # ⛔ **也不是直接把這一行拿掉**：那會讓「⑤ 有沒有抓取診斷」變成一句要靠註解
    #    維持的約定；gate 留著，舊 ⑤ 哪天下架把 gate 拿掉即可（**可逆**）。
    #
    # ⚠️ **刻意不帶 `key=`**（同 `_render_maintain` / `_render_backfill`）：
    #    帶了會命中 `tests/test_wf05_settings_skeleton.py::`
    #    `test_the_page_writes_only_its_own_session_key`（本頁只准寫 `_SK_DIAG_GATE`）。
    # ⚠️ **刻意不放進 `st.form`**：form 內的 checkbox 要按送出鍵才生效，
    #    那會讓 gate 變成兩段式，且 `st.form` 本身就是本批在解的衝突源之一。
    if not st.checkbox(
            _fetch_diag_gate_label(), value=False,
            help=f"舊「{tab_label('settings')}」分頁已經在畫同一塊；"
                 "兩份同時載入不會報錯，但畫面上會有兩塊一模一樣的面板。"):
        # ⚠️ 指路吃 :func:`_fetch_diag_gate_label`，**不手抄**。
        not_ready(_fetch_diag_not_loaded_note(),
                  where=f"上方「{_fetch_diag_gate_label()}」")
        return

    # ⚠️ 個基頁那份由 `FETCH_DIAG` 旗標控制，而那個旗標**必須由 `app.py` 持有**
    #    才壓得住它（理由見模組 docstring 最後一段）。本檔刻意不在這裡包。
    render_fetch_diag_from_session()


def _render_backfill() -> None:
    """區塊 4｜手動補資料 —— **委派** `render_nav_manual_section()`（三條寫入路徑）。

    線框逐字：「CSV 匯入淨值歷史、一鍵補抓、逐檔結果。**寫入類動作，全部 Form 封裝。**」

    ⚠️ **三條路徑實測不等價，一條都不能砍**（`nav_history_section` 的模組 docstring
       有逐條對照表）：① 一鍵自動補全（連外抓 → 本地 cache ＋ 雲端）、
       ② 對帳單 CSV（單檔、代碼手填、**只寫雲端**、吃兩欄 CSV）、
       ③ 本地基底 CSV（多檔、代號讀自 CSV、寫本機 cache ＋ 雲端）。

    ⚠️ **一個 Form 封裝上的硬限制，據實寫明**：③ 底下的「📤 下載當前 cache 為 CSV」
       用的是 `st.download_button`，Streamlit **在原始碼層面無條件禁止它出現在
       `st.form` 內**。那是平台限制，不是選擇 —— 完整說明在被委派函式的 docstring。

    ⛔ **本檔不再自己畫一個「按了不會寫」的假 Form。** 改寫前的版本按下送出鍵只把
       選擇寫進 session、**沒有任何 I/O**，並靠一行灰態自陳「實際補抓功能尚未接上」。
       (A) 路線之下那個取捨消失了：**真的寫入功能一直都在舊模組裡**，委派過去即可。
    """
    from ui.helpers.settings_diag.nav_history_section import render_nav_manual_section

    # ⚠️ 這裡**不需要**持有 `NAV_HISTORY`：`render_nav_manual_section()` 是直接呼叫
    #    三個 helper，它們自己不查旗標（本組 AST 實測：三支函式體內 0 個旗標呼叫）。
    #    旗標要持有的地方是**委派整頁的那兩處**（區塊 1 與 4.5）。
    #
    # 📌 **2026-09-06 就地登記：這一行與舊 ⑤ 的呼叫方式不同，不要改回去。**
    #    **AST 實測（同一支掃描器，兩檔對照）**：
    #      舊 ⑤ `ui/tab_settings_diag.py::_render_maintain_section`
    #           → `render_nav_manual_section()` 包在
    #             `settings_page_owns(MANAGE_HEADER, NAV_HISTORY)` **裡面**；
    #      本檔 `_render_backfill` → `owns = NONE`（**裸呼叫**）。
    #    **為什麼不改回去**：那兩支旗標在這裡**沒有消費者** ——
    #    `MANAGE_HEADER` 只被 `ui/tab_manage.py::render_manage_tab` 讀（本函式不呼叫它），
    #    `NAV_HISTORY` 只被 `render_manage_tab` / `render_data_guard_tab` 讀
    #    （本函式呼叫的三個 helper 體內 0 個旗標呼叫，見上）。
    #    在舊 ⑤ 那邊它是**分區共用同一個 `with`** 帶進來的副作用，不是這一塊需要的。
    #    ⚠️ **登記它，是因為新舊逐行對照時這一格會多一個要用腦的差異** ——
    #       下一個做對照的人不必再推一次。**語意未變、無害；但別把「無害」讀成「一樣」。**
    # ⭐ **2026-09-07 雙軌並行：加 Checkbox Gate。**
    # （原寫 ~~「本頁第二顆同型的 gate」~~ —— **序數會漂移，2026-09-07 第二輪之後
    #   本頁共四顆**；就地更正，不是漏刪。）
    # **有意識的變更，不是漏刪**（決策者：AI 總管，客戶 2026-09-07 雙軌並行原則）。
    #
    # **為什麼非加不可**：舊 ⑤（`ui/tab_settings_diag.py::_render_maintain_section`）
    # **無條件**呼叫同一支 `render_nav_manual_section()`。雙軌並行之後兩頁同一次 run
    # 都會跑到它，而它底下有 **11 個具名 widget key**
    # （`_nh_sel` / `_nh_upload_csv` / `_nh_update_btn` / `_nh_dl_btn` / `_nh_clear_btn` /
    #  `_nh_upload_csv_form` / `_nh_backfill_all_form` / `navhist_import_code` /
    #  `navhist_import_file` / `navhist_import_name` / `navhist_import_form`）
    # → **Streamlit 重複 key 會炸**，`[新]` 這一塊會整塊變紅。
    #
    # ⚠️ **這一塊與維護區不同，它是客戶拍板線框 Tab 05 五個 `<h4>` 之一**，
    #    所以「要不要 gate」是**送過總管、拿到裁決才做的**，不是本組自行決定。
    #    裁決理由（2026-09-07）：(1) gate 只影響 `[新]` 預覽分頁，客戶簽核的舊 ⑤
    #    一格未動（blob 前後相同）；(2) 不 gate 的後果更糟 —— 客戶要拿這個分頁驗收，
    #    一打開就有一塊紅等於沒交；(3) **可逆** —— 舊 ⑤ 哪天真的下架，
    #    重複 key 消失，這個 gate 可以直接拿掉。
    #
    # ⛔ **不得改用 `st.expander(expanded=False)`**：收合的 expander body 照樣執行，
    #    擋不住重複 key（本 repo 自證見 :func:`_render_maintain` 的註記）。
    # ⛔ **不得改成「不畫這一塊」**：那是刪功能，不是解衝突。
    #
    # ⭐ **標題的所有權隨 gate 切換 —— 這一段最容易改壞，讀完再動**：
    #    `### 手動補資料` 在**全頁六塊裡是唯一一個不由 `render_settings_and_diagnostics`
    #    畫的**（它由被委派的 `render_nav_manual_section()` 自己畫，
    #    見 `tests/test_wf05_settings_skeleton.py::test_each_block_heading_is_drawn_exactly_once`
    #    的 docstring —— 那條記載的正是「兩邊各畫一次」這個真的發生過的 bug）。
    #    加了 gate 之後，gate 關著時委派不會跑 → **標題會整個消失**（那條守衛的「0 次」方向）。
    #    故：**gate 關 → 由本函式畫；gate 開 → 由委派方畫。兩種狀態都恰好一次。**
    #    ⚠️ 用 `st.empty()` 佔位是為了讓標題**排在 checkbox 之前** ——
    #       否則 checkbox 會落在上一塊的區段裡（`_units()` 以 `### ` 切段），
    #       「手動補資料」那一段就只剩一句灰字，
    #       `test_the_delegated_blocks_have_real_content` 會轉紅。
    # ⛔ **已登記：`EXCEPTIONS.md §8.3.P` 的 `P-GATEDLAYOUT-1` 第 (1) 筆。**
    #    **gate 開時本檔不填這個 slot**（標題交給委派畫）—— 委派炸掉 ⇒ **兩邊都沒畫**。
    #    實測：`gate=True` ＋ 委派拋例外 → `### 手動補資料` **0 次**、紅框 1。
    #    ⚠️ 那與這一塊自己的灰態承諾（「勾它**只能看到位置**」）直接牴觸。
    #    ⛔ **既有債，#835 未修**（§8.4 步驟 4，scope 由總管決定）。
    _heading_slot = st.empty()

    # ⚠️ **刻意不帶 `key=`**：帶了會命中
    #    `tests/test_wf05_settings_skeleton.py::test_the_page_writes_only_its_own_session_key`
    #    （widget `key=` ＝ 寫 session_state，本頁只准寫 `_SK_DIAG_GATE`）。
    #    同檔先例：`_render_nav_status()` 的 `NAV_GATE_LABEL` gate 與
    #    `_render_maintain()` 的 gate 都是這樣寫的。
    if not st.checkbox(
            _backfill_gate_label(), value=False,
            help=f"舊「{tab_label('settings')}」分頁已經在跑同一塊；"
                 "兩份同時載入會撞 Streamlit 的重複元件鍵。"):
        _heading_slot.markdown(f"### {nav_manual_label()}")
        # ⚠️ 指路吃 :func:`_backfill_gate_label`，**不手抄**。
        not_ready(_backfill_not_loaded_note(),
                  where=f"上方「{_backfill_gate_label()}」")
        return

    render_nav_manual_section()


def _render_maintain() -> None:
    """區塊 6｜🗄️ 資料維護與通報 —— **委派** `render_manage_tab()`，**收在折疊區裡**。

    ⭐ **客戶 2026-09-07 逐字裁決**：「決策【**保留**】在 ⑤ 的**最底部**作為**進階折疊區
    （`st.expander`）**。理由：系統維護與通報屬管理設定範疇，折疊收攏即可，不影響主視覺。」
    → 本函式**只有位置與包裝改了**；**委派對象、旗標、內容一個字都沒動**
      （見 commit 訊息與 PR 的新舊委派集合對帳）。
    ⚠️ 舊標號 ~~「（4.5）」~~ 已失效（**有意識的變更，不是漏刪** · 2026-09-07 · 決策者：客戶）
      —— 它當時表達的是「夾在 4 與 5 中間、線框沒給它位置」，
      而**現在它排在最後**。⛔ 但「線框沒給它位置」這件事**沒有改變**，見下一段。

    ⚠️ **線框 Tab 05 沒有這一塊，這是登記在案的偏離**（見模組 docstring 的 (D-5)）。
    線框的「從哪裡搬來」逐字列了 `ui/tab_manage.py`，但五個 `<h4>` 裡沒有任何一塊
    叫得出「選股池」「除息行事曆」「換股通報」。塞進「手動補資料」會讓畫面說謊；
    整個不畫等於刪功能。**故另立一塊並就地登記。**

    ⚠️ **持有 `MANAGE_HEADER` ＋ `NAV_HISTORY` 兩支**：
    - `MANAGE_HEADER` → 管理室不再畫它自己的 `##` 頁面大標（⑤ 已畫區塊標題）；
    - `NAV_HISTORY`  → 管理室**跳過** `_sec_nav_backfill()`（那一塊已由區塊 4 畫）。
      ⛔ 漏掉它的後果是「🗄️ 補歷史淨值」在同一頁**畫第二次**，而且不報錯。
      ✅ **粒度實測**：`render_manage_tab()` 只跳過那一段，選股池／除息行事曆／
      換股通報**照畫** —— 這正是總管裁決 2 能成立的前提。

    📌 **一個已知的副作用寫入，登記在此，但不由本檔處置**（總管 2026-09-06 裁決）：
    `render_manage_tab()` → `_sec_pool()` → `repositories/pool_repository.py::list_pool()`
    第一行呼叫的 `_ws()` 會在讀取路徑上 `add_worksheet` / `ws.update("A1", …)` 補 header
    —— **讀一下就寫回去**。客戶 2026-09-06【永久授權】原則一明令「查詢/搜尋一律唯讀」，
    該處已由 **P0-B 組（PR #796，branch `claude/fund-readonly-sn42bh`）** 在切。
    ⛔ **本檔不得為了避開它而改寫、包一層或改成不呼叫舊模組** —— 那是繞道，不是修好；
       也**不得**去動 `repositories/pool_repository.py`：那是 P0-B 的檔案邊界
       （§-1.5 v1 第一條第 2 點 File Boundary ＋ `PROCESS.md §3` 寫入端序列化）。
    """
    from ui.tab_manage import render_manage_tab

    # ⭐ **客戶 2026-09-07 裁決：收進折疊區、預設收合**（「折疊收攏即可，不影響主視覺」）。
    # ⚠️ **巢狀 expander 的疑慮，據實寫明，不要靠記憶重新推一次**：
    #    `render_manage_tab()` **傳遞地**含 `st.expander`
    #    （`_sec_notify` 的「📖 每週自動通報 — NAS 設定步驟」是無條件的；
    #    `_sec_nav_backfill` 那條被 `NAV_HISTORY` 旗標跳掉）。
    #    本 repo 多處註記寫著「Streamlit 禁止 expander 巢狀」
    #    （`ui/helpers/v2_editor.py` / `ui/tab1_macro_longterm.py` / `tests/test_app_smoke.py` T2）。
    #    **但本頁早就在做同一件事**：區塊 5 `_render_manual()` 把
    #    `render_manual_tab()`（檔內 2 個**無條件** `st.expander`）包在 expander 裡，
    #    而本檔的守衛是**真的 AppTest**、跑在**會擋 merge 的 fast lane**
    #    （`tests/test_wf05_settings_skeleton.py` 無 `slow` 標記），且 `origin/main` 是綠的。
    #    → 也就是說，在本 repo 釘的 streamlit（`requirements.txt`：>=1.59.1,<1.60.0）下，
    #      這個巢狀**實測不會炸**；本塊與區塊 5 是**同一個形狀**。
    #    ⚠️ **這是從 repo 既有證據推出來的，本組沒有在本機重現**（本環境無 streamlit）——
    #      真正的裁判是 CI 的 fast lane，不是這段註解。
    #    ⛔ 若哪天 streamlit 把限制加回來，**先紅的會是區塊 5，不是這一塊**。
    # ⭐ **2026-09-07 雙軌並行：折疊區**之內**再加一道 Checkbox Gate。**
    # **有意識的變更，不是漏刪**（決策者：AI 總管，裁決 2）。
    #
    # **舊寫法**（原地保留、加刪除線，不刪）—— 注意**被劃掉的只有中間那一層**::
    #
    #     with st.expander(maintain_label(), expanded=False):        # ← 保留
    #     ~~    with settings_page_owns(MANAGE_HEADER, NAV_HISTORY):~~
    #     ~~        render_manage_tab()~~                            # ← 改到 gate 之後
    #
    # ⚠️ **`st.expander` 本身一個字都沒動** —— 本段第一版寫成「expander 升級成
    #    Checkbox Gate」，那句話是**假的**：expander 還在，gate 是**疊在它裡面**。
    #    （寫下當時本組確實把 expander 拿掉了，後來發現
    #     `tests/test_wf05_settings_skeleton.py::test_the_maintain_block_is_folded_and_collapsed_by_default`
    #     直接釘著客戶逐字要的那個包裝，才改成兩者並存 —— 敘述隨之更正。）
    #
    # **舊寫法的理由一個字都沒有被推翻**：客戶 2026-09-07 逐字裁決
    # 「保留在 ⑤ 的最底部作為進階折疊區（`st.expander`）……折疊收攏即可，
    # 不影響主視覺」—— 那個包裝**原封不動還在**，gate 只是多擋一層 body 執行。
    #
    # **被權衡掉的是它的一個前提：「折疊 ＝ 不載入」。那個前提是假的。**
    # `st.expander` **收合時 body 照樣執行**（本 repo 自證：
    # `tests/test_app_apptest.py::test_tab6_manual_renders_key_sections` 驗的正是
    # 收在 `expanded=False` 裡的說明書內容，它在 `origin/main` 是綠的）。
    #
    # **為什麼非改不可**：舊 ⑤（`ui/tab_settings_diag.py::_render_maintain_section`）
    # **無條件**呼叫同一支 `render_manage_tab()`。雙軌並行之後兩頁同一次 run 都跑，
    # 而 `render_manage_tab()` 無條件畫 `_sec_pool()` / `_sec_dividend_calendar()` /
    # `_sec_notify()`，其中帶具名 key（`pool_*` ×10 / `divcal_gen` /
    # `manage_notify_preview`）→ **Streamlit 重複 key 會炸**
    # （`ui/helpers/ia/gated_form.py` 自陳：「全站唯一，Streamlit 會在重複時炸掉」）。
    # 折疊擋不住，`if` 才擋得住。
    #
    # ⛔ **為什麼不是改舊模組的 key**：客戶 2026-09-07 明文
    #    「絕對禁止直接覆寫、修改或破壞現有線上正常運作的舊版 Tab 代碼」——
    #    `ui/tab_manage.py` 與 `ui/tab_settings_diag.py` 本批 **blob 前後相同**。
    # ⛔ **也不是把這一塊從新頁拿掉**：那是刪功能，不是解衝突（裁決 3 (b)）。
    #
    # ⚠️ **本 gate 只解「預設載入就雙跑」那一族**。使用者若**同時**勾起舊 ⑤ 與本頁
    #    的其他 gate（例如兩邊的「載入資料診斷」），仍會碰到重複 key ——
    #    **已知、未解、本批不處理**，逐一具名登記在 PR 描述。
    # ⚠️ **折疊區保留、gate 疊在它裡面 —— 兩個約束同時成立，不是二選一。**
    #    客戶 2026-09-07 逐字要的是「進階折疊區（`st.expander`）」那個**包裝**，
    #    而 gate 解的是「**body 會不會執行**」。兩件事不衝突：
    #    expander 照畫（收合、不佔主視覺），委派則被 gate 擋在 `if` 後面。
    #    ⛔ 拿掉 expander 會讓
    #       `tests/test_wf05_settings_skeleton.py::test_the_maintain_block_is_folded_and_collapsed_by_default`
    #       轉紅（它同時驗 `expanded=False` **與**「委派真的落在那個 `with` 裡」），
    #       而那條釘的是**客戶逐字裁決**，不是實作細節。
    with st.expander(maintain_label(), expanded=False):
        # ⚠️ **刻意不帶 `key=`** —— 本檔的
        #    `tests/test_wf05_settings_skeleton.py::test_the_page_writes_only_its_own_session_key`
        #    把「widget 帶 `key=`」算成**寫 session_state**（streamlit 會代為寫入），
        #    而本頁只准寫 `_SK_DIAG_GATE` 一個鍵。gate 只需要「**這一次 run 有沒有勾**」，
        #    不需要跨 run 保存 → 直接用回傳值當條件即可。
        #    **同檔先例**：`_render_nav_status()` 的 `NAV_GATE_LABEL` gate 就是這樣寫的
        #    （`value=False`、不帶 `key=`），本條照抄它，不另發明第二套寫法。
        if not st.checkbox(
                _maintain_gate_label(), value=False,
                help=f"舊「{tab_label('settings')}」分頁已經在跑同一個維護區；"
                     "兩份同時載入會撞 Streamlit 的重複元件鍵。"):
            # ⚠️ 指路吃 :func:`_maintain_gate_label`，**不手抄**（手抄那一刻就開始漂移）。
            not_ready(_maintain_not_loaded_note(),
                      where=f"上方「{_maintain_gate_label()}」")
            return

        with settings_page_owns(MANAGE_HEADER, NAV_HISTORY):
            render_manage_tab()


def _render_manual() -> None:
    """區塊 5｜使用手冊 —— **委派** `render_manual_tab()`。**靜態文字，不是灰態**（D-1）。

    線框逐字：「指標定義、門檻由來、常見誤讀。**純文字，不佔首屏**。」

    ⛔ **這一塊不准畫成灰態**（依據見 (D-1)）。改寫前的版本只放了三行**目錄**，
       而真內容（`ui/tab6_manual.py`，十章 + 錨點目錄）一直都在 —— (A) 路線委派過去。
    ⚠️ **「不佔首屏」怎麼落地**：本塊排在很後面、且**收在 `st.expander` 裡預設不展開**。
       ⚠️ **2026-09-07 就地更正（有意識的更正，不是漏刪 · 決策者：客戶）**：
          原寫 ~~「本塊排在**最後**」~~ —— 客戶同日把「🗄️ 資料維護與通報」移到**最底部**，
          所以本塊**不再是最後一塊**。**「不佔首屏」這個結論一字未變**
          （它靠的是「排在很後面 ＋ 預設收合」，不是「排在最末」）；
          改的只是那個**會說謊的位置描述**。
       線框的 `dim`（虛線框 + 灰標題）是 HTML 的視覺降權手法，在 Streamlit 沒有等價物；
       「預設收合」是本 repo 拿得到、且**語意相同**（在，但不搶版面）的那一個。
       ⚠️ **這是本組挑的落地方式，不是線框指定的** —— 線框只說「不佔首屏」。
    """
    from ui.tab6_manual import render_manual_tab

    with st.expander(BLOCK_MANUAL, expanded=False):
        # ⭐ **2026-09-07 第二輪雙軌修補：折疊區**之內**再加一道 Checkbox Gate。**
        # **有意識的變更，不是漏刪**（決策者：AI 總管，客戶 2026-09-07 雙軌並行原則）。
        #
        # **舊寫法**（原地保留、加刪除線，不刪）—— **被劃掉的只有最後兩行**::
        #
        #     with st.expander(BLOCK_MANUAL, expanded=False):     # ← 保留
        #     ~~    with settings_page_owns(MANUAL_HEADER):~~
        #     ~~        render_manual_tab()~~                     # ← 改到 gate 之後
        #
        # **為什麼非加不可（這是四個 gate 裡唯一一個「零點擊就已經在噴紅字」的）**：
        # 舊 ⑤（`ui/tab_settings_diag.py::_render_manual_section`）**無條件**呼叫
        # 同一支 `render_manual_tab()`，本函式在雙軌之前也是無條件 ——
        # **兩邊都沒有 gate**。而 `st.tabs` 一次 run 會把所有分頁 body 全部執行過，
        # 於是使用者**打開網站、什麼都還沒點**就會在 ⑦ 看到一則紅字
        # 「指標地圖載入失敗 — `StreamlitDuplicateElementId`」。
        # 根因：`ui/tab6_manual.py` 復用 `ui/tab1_macro.py::render_indicator_map`，
        # 那支的 `st.plotly_chart` **不帶 key** → 兩份的自動 element ID 完全相同。
        #
        # ⛔ **`st.expander(expanded=False)` 擋不住**：收合的 expander **body 照樣執行**
        #    （本 repo 自證：`tests/test_app_apptest.py::test_tab6_manual_renders_key_sections`
        #     驗的正是收在 `expanded=False` 裡的說明書內容，它在 `origin/main` 是綠的）。
        #    **只有「gate 沒過就提前 return」才真的不呼叫。**
        # ⛔ **不是改 `ui/tab1_macro.py` 幫那個 chart 補 `key=`**：客戶 2026-09-07 明文
        #    「絕對禁止直接覆寫、修改或破壞現有線上正常運作的舊版 Tab 代碼」。
        # ⛔ **也不是把這一塊從新頁拿掉**：那是刪功能，不是解衝突。
        #
        # ⚠️ **與模組 docstring (D-1) 的關係，就地講清楚，不要以為它被推翻了**：
        #    (D-1) 說「使用手冊**不准畫成灰態佔位**」，理由是「它是**現在就能出的
        #    靜態文字**，畫成『未載入』是對使用者說一句假話」。
        #    **那個理由今天依然成立，而且本改動沒有違反它** ——
        #    (D-1) 禁的是**假的**「未載入」（東西明明出得來卻說出不來）；
        #    本 gate 沒勾時畫面上那句話是**真的**：本頁這一份**確實還沒載入**，
        #    而且**一載入就會有一塊變紅**。灰態本文（:func:`_manual_not_loaded_note`）
        #    把「舊 ⑤ 已經有一份完整的」與「勾下去會怎樣」兩件事都寫了出來。
        #    ⛔ **代價要記**：雙軌期間，⑦ 的使用手冊**預設看不到內容**。
        #       那是本改動用「一則零點擊的紅字」換來的，不是免費的。
        #       舊 ⑤ 一下架，這個 gate 就該連同另外三個一起拿掉（**可逆**）。
        #
        # ⚠️ **刻意不帶 `key=`**（本頁只准寫 `_SK_DIAG_GATE`）、
        #    **刻意不放進 `st.form`**（form 內的 checkbox 要按送出鍵才生效）——
        #    正例就是同檔 `_render_maintain` 那顆已經在線上跑的 gate。
        if not st.checkbox(
                _manual_gate_label(), value=False,
                help=f"舊「{tab_label('settings')}」分頁已經在跑同一份說明書；"
                     "兩份同時載入會撞到「指標地圖」那張圖的自動 element ID。"):
            # ⚠️ 指路吃 :func:`_manual_gate_label`，**不手抄**。
            not_ready(_manual_not_loaded_note(),
                      where=f"上方「{_manual_gate_label()}」")
            return

        # ⑤ 已畫區塊標題 → 說明書不再畫自己的 `##` 頁面大標（其餘一行不動）。
        with settings_page_owns(MANUAL_HEADER):
            render_manual_tab()


def render_settings_and_diagnostics() -> None:
    """渲染「⑤ 設定與診斷」整頁 —— 五塊照線框順序，功能全部委派舊模組。

    ⚠️ **每個區塊各自走 `safe_section()`，這是本頁最重要的一條紀律。**
    合併成一頁之後六個區塊共用同一次 script run：管理室當掉會一併帶走
    🔭 資料診斷與 📖 說明書，而那兩塊正是使用者出事時要去的地方
    （本頁一句話職責：「出事時第一個進來」）——
    **把診斷跟故障綁在同一條命上，等於在最需要它的時候把它拿走。**
    `safe_section` **不吞例外**（§1）：它走 `system_error()` 顯式紅框 ＋ traceback。

    ⚠️ **沒有 `render_story_nav("settings")`** —— 它會靜默 no-op，理由見模組 docstring。
    ⚠️ **沒有頁面層級的空狀態**（D-2）—— 與 ④ 刻意不同。
    ⚠️ ~~**本批尚未接進 `app.py`**，所以現在**沒有 production caller**。~~
    → **2026-09-07 事實更正（有意識的更正，不是漏刪 · 決策者：AI 總管 · 依據：實測
      `git grep -n "page_05_settings" app.py`）**：**本函式就是 `app.py` 第 ⑦ 格的
      進入點**（`with tab_preview_settings:` → `render_settings_and_diagnostics()`）。
      舊表述在寫下當天為真；被推翻的是它的前提（#814 雙軌並行接線，未回頭改本檔）。
      **同一句話在本檔出現三處**（模組 docstring 兩處 ＋ 本處），**三處一起更正** ——
      只修被點名的那一句，等於留兩句一樣的假話在旁邊。
    """
    st.markdown(f"## {tab_label('settings')}")
    # 線框 Tab 05 的職責宣告 ＋「這裡不放什麼」。
    # ⚠️ 指路的顆粒度**跟著線框走**：線框寫「任何投資判斷 → 01 ~ 04」（整個分頁）。
    st.caption(
        "回答一個問題：**資料本身可不可信、要不要我補？** 平常不用進來；"
        f"出事時第一個進來 —— 任何投資判斷在 {where_to_find('macro')} "
        f"到 {where_to_find('portfolio')} 之間。")

    # ── 🧾 ① 結論 —— **排在所有依據之前**（線框 §1 的四層閱讀順序）────────
    # ⚠️ **各自一層 `safe_section()`，同六個區塊** —— 結論層炸掉不得帶走下面的依據，
    #    否則使用者「出事時第一個進來」看到的是一整頁紅框。
    safe_section("結論", _render_conclusion)

    st.markdown(EVIDENCE_HEADING)

    # ── 線框 Tab 05 的五塊，順序逐字照線框 ────────────────────────────────
    # ⚠️ **區塊標題一律 `### `，理由是它要跟被委派模組的頂層標題同一級**：
    #    `render_data_guard_tab()` / `render_manage_tab()` / `render_nav_manual_section()`
    #    的頂層區塊全部用 `### `。用 `#### ` 會讓**我的區塊標題比它裝的東西還小**，
    #    用 `## ` 會與頁面大標同級。這個階層是被委派模組決定的，(A) 路線不改它們。
    st.markdown(f"### {BLOCK_HEALTH}")
    safe_section(BLOCK_HEALTH, _render_source_health)

    st.markdown(f"### {nav_status_label()}")
    safe_section(nav_status_label(), _render_nav_status)

    st.markdown(f"### {BLOCK_KEYS}")
    safe_section(BLOCK_KEYS, _render_keys)

    # ⚠️ **區塊 4 刻意不由本檔畫標題** —— `render_nav_manual_section()` 自己會畫
    #    `### {section_label("nav_manual")}`（`nav_history_section.NAV_MANUAL_HEADING`），
    #    **與本檔會畫的那一行同一份 SSOT、同一級、逐字相同**。本檔再畫一次的結果是
    #    畫面上連著出現兩個一模一樣的「手動補資料」。
    #    ⛔ **另一條路（改成直接呼叫底下三個 helper）本檔不走**：那會把「這一塊裝哪三條
    #       寫入路徑」複製成第二份真相源（§2.1），而它正是 `nav_history_section`
    #       模組 docstring 花了一整張表在守的東西。
    #    → 守衛：`test_each_block_heading_is_drawn_exactly_once`（六個區塊逐一驗，
    #       0 次 ＝ 那一塊不見了、2 次 ＝ 畫重複了，**兩個方向都紅**）。
    safe_section(nav_manual_label(), _render_backfill)

    st.markdown(f"### {BLOCK_MANUAL}")
    safe_section(BLOCK_MANUAL, _render_manual)

    # ── 頁面最底部：進階折疊區 ──────────────────────────────────────────
    # ⭐ **客戶 2026-09-07 裁決：這一塊【保留】，但移到最底部、收進 `st.expander`。**
    #    客戶原話：「系統維護與通報屬管理設定範疇，折疊收攏即可，不影響主視覺。」
    # ⚠️ 線框沒有給這一塊位置（(D-5)），本次裁決**沒有推翻那個登記** ——
    #    它裁的是「這一塊放在哪、怎麼收」，不是「線框有沒有它」。**(D-5) 照舊有效。**
    # ⚠️ **標題仍由本檔畫 `### `，而且畫在 expander 外面** —— 與區塊 5（使用手冊）
    #    同一個做法：`st.expander` 的標題**不是** `### ` 標題，
    #    `test_each_block_heading_is_drawn_exactly_once` 數的是 `### `。
    #    把 `st.markdown` 挪進 expander 內會讓那條紅燈（0 次 ＝ 這一塊不見了）。
    st.markdown(f"### {maintain_label()}")
    safe_section(maintain_label(), _render_maintain)
