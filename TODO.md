# TODO.md — 專案進度追蹤清單

> **用途**：把客戶（2026-08-28）交辦的三階段工作，逐項對照本 repo 的**實測現況**後，
> 落成一份可逐格打勾的階層式進度表。它是「**現在做到哪、下一步卡在誰身上**」的單一看板，
> **不是**規格書（規格看 `CLAUDE.md` / `PROCESS.md` / `SPEC.md`），也**不是**授權書
> （動工授權看 `CLAUDE.md §-1`：沒有 user 指派、沒有實際 bug 觸發 → 停手等指令）。

**建立日期：2026-08-28**

**沿革**：
- **v1**（PR #727 → main `a81e460`）建立本檔。
- **v2**（2026-08-28）：**Phase 1.4 死碼刪除（PR #730 → main `149d1dd`）之後的狀態同步**。
  本次更新的所有舊敘述**一律加刪除線保留**，後面接狀態更新 —— 它們在寫下的當下**都是真的**，
  失效的原因是**世界前進了**，不是原文寫錯。**刪掉它們會讓後人以為本檔當時漏看。**
  ⚠️ **本次同步的基準點**：`git log --oneline -6 origin/main` 實跑（2026-08-28）——
  `149d1dd` #730 Phase 1.4／`b00eabf` #729 三鐵律守衛／`a81e460` #727 本檔 v1／
  `592b1fa` #728 三份線框進 `docs/wireframes/`。
- **v3**（2026-08-28）：**Phase 1 收尾、Phase 2 解除阻擋後的狀態同步**。
  處置沿用 v2：**舊敘述一律加刪除線保留，不整行刪除**，後面接狀態更新 + 日期 + 決策者。
  ⚠️ **本次同步的基準點**：`git log --oneline -12 origin/main` 實跑（2026-08-28），
  v2 之後 main 前進了 **4 個 commit**（**新→舊**，`git log` 原序）——
  | commit | PR | 內容 |
  |---|---|---|
  | `d6f5f87` | #726 | UI3b：6 個純函式 UI 元件 + `tests/test_ui3b_components.py`（**已合併**，不再是「壓著不合併」） |
  | `0178e4b` | #732 | Phase 1.2：基準指數取數自 UI 層下沉 L1（`repositories/macro/yf.py::fetch_benchmark_close`） |
  | `1acd089` | #733 | `services/` 純度機器守衛（`tests/test_services_purity_contract.py`），**登記 45 項債** |
  | `2036896` | #731 | 本檔 v2 |
  ⚠️ **順序陷阱（就地記一筆）**：`d6f5f87`（PR #726）**PR 編號最小、但 commit 最新** ——
  它是 2026-08-28 才被放行合併的舊分支。**用 PR 編號推時序會推錯**，一律以 `git log` 為準。
- **v3 修正輪**（2026-08-28 同日，**決策者：總管（採納獨立稽核建議）**）：
  獨立稽核複驗 v3 後，**兩項採納、就地修正**（稽核逐項核對了 40 餘個 hash／PR／路徑／行號／數字，
  **沒有一個是假的** —— 兩項都不是造假，是**措辭過寬**與**打勾粒度**）：
  1. **`~~PR 描述本身~~`** 那一處**無聲改寫**補加刪除線（見 2.2 該格）；
     連帶更正 v3 PR 描述裡「37 行全部仍在檔內」的說法 —— **實際是 36/37**（方法缺陷見附錄 C 盲點 17）。
  2. **1.3 拆成 1.3-a／1.3-b，`Phase 1 總項` 退回 `- [ ]`** ——
     原本的「重新定義父項 + 就地警語」處置**架空了維護規則第 2 條**（完整論證見 Phase 1 總項該處）。
  ⭐ **這兩項都是本檔自己的規則抓到本檔自己** —— 值得記，不值得藏。

⚠️ **本檔內的量測值會漂移（檔案大小、檔數、分頁數、分支是否存在、grep 命中數），引用前請現場複驗。**
本檔所有「現況」欄位均標明取得證據的**指令或路徑**，就是為了讓後人能自己重跑一次，而不是相信這張表。

---

## 維護規則

1. **每完成一小項才打勾**（`- [ ]` → `- [x]`）。父項只有在**所有子項都打勾**之後才能打勾。
2. **未完成不得跳級**：不得因為「下一項比較好做」就先勾下一項；有 `阻擋者` 的項目，
   在阻擋者未打勾前一律維持 `- [ ]`。
3. **每一項都必須有三欄資訊**：`現況`（實測 + 證據）／`裁決或待辦`／`阻擋者`。
   三欄缺一，該項視為**尚未可動工**。
4. **不得把「客戶原文」直接當現況填進來**。客戶清單有數項對不上本 repo（見下方「§0 客戶清單校正」），
   照抄等於把姊妹 repo 的狀態寫成本 repo 的狀態。
5. **改狀態時要附證據**：把 `現況` 一欄換掉時，同時換掉它後面的指令／路徑／commit hash。
   只改結論不改證據 = 讓這張表開始說謊。
6. **不確定就寫「待查證」**，不要猜一個狀態填進去。對照 `CLAUDE.md §1`：
   **錯誤的數字比沒有數字更危險**；對照 `§-2` 規則 6：**沒查證的宣稱比沒有宣稱更危險**。
7. **全稱句要自標**（「只有這一處」「全都查過了」「沒有其他地方受影響」）——
   依 `CLAUDE.md §-2` 規則 6，這類句子要嘛派一組獨立驗，要嘛就地標明「單組結論，未經第二組驗證」。

---

## §0 客戶清單校正（動工前必讀）

**總管實測發現：客戶交辦的 Phase 1 清單，有數項源自姊妹 repo `my-stock-dashboard`，不是本 repo 的實況。**
以下為逐項對照，**證據均為本組實測**：

| 客戶清單項目 | 實測結果 | 證據 |
|---|---|---|
| 1.1 建立 `src/data/{macro, stock, fund, portfolio}` | **本 repo 無 `src/` 目錄**。姊妹 repo `my-stock-dashboard` **已有** `src/data/`，其子目錄為 `core daily etf macro news notify portfolio proxy sector_flow stock` —— 客戶點名的 macro / stock / portfolio 都在裡面。本 repo 的對應物是 **`repositories/`**（已有 `fund/` `macro/` `policy/` 三個領域子目錄） | 本 repo：`ls -d src` → `No such file or directory`；`ls -d repositories/*/` → `repositories/fund/ repositories/macro/ repositories/policy/`。姊妹 repo：`ls src/data/` |
| 1.4 刪除 `stock_etf_dashboard/`（2,658 行） | **兩個 repo 都沒有這個目錄。** 姊妹 repo 的 git 歷史顯示它**已被刪除**；本 repo **從未有過** | 本 repo：`ls -d stock_etf_dashboard` → `No such file or directory`。姊妹 repo：同樣不存在，且 commit `e4e03f6`「刪除 stock_etf_dashboard/：先驗後刪，附可達性硬證明」 |
| 1.4 `services/fund_batch.py` | ~~**本 repo 確實存在**（10161 bytes，量測日 2026-08-28）。憲法 `§8.2` 記載為「v19.406 批次攤平器；v19.413 RETAINED-LEGACY」~~ → **2026-08-28 狀態變更（決策者：user；不是原文寫錯，是本檔寫下之後被刪了）**：**已於 PR #730（main `149d1dd`）實體刪除**（−221 行），實測 production 0 caller。**客戶此項成立、已完成** | 刪除前：`ls -l services/fund_batch.py`。刪除後實測：`ls services/fund_batch.py` → No such file；`git show --stat 149d1dd` |
| 1.4 「29 個無效測試模組」 | ~~**待查證** —— 正由**獨立稽核組**實測中，本組手上**沒有清單**~~ → **2026-08-28 結案：客戶的「29」是錯的數字。** 稽核組複現了它的產生方式（**只看模組路徑字面有無出現在別的檔案**），並實測至少 **5 個假陽性**（`repositories/ai_cache.py`、`repositories/batch_checkpoint.py` 等**其實都有人用**）。改用正確方法（納入**相對 import／barrel re-export／動態載入／workflow 與 docs 引用**）算出來是 **11 個可刪**，已全數刪除 | `git show --stat 149d1dd`（19 檔／−3,733 行，11 production 模組 + 8 孤兒測試） |
| 1.4 「停用的 3 個總經指標模組」 | ~~**待查證**~~ → **2026-08-28 結案：查不到對應對象。** 兩條線索都已失效 —— `fetch_tw_export_yoy` 早在 **v19.355** 改走海關 opendata、**活得好好的**；`fetch_yf_forward_pe` / `fetch_multpl_pe` **上週已刪**；總經指南針三層鏈的佔位檔**也早已刪完**。⭐ **稽核組沒有為了湊數字硬找三個交差** —— 這正是本檔 §0 那條「實際找到幾個就報幾個」的紅線被真正執行了一次 | 稽核組回報；`CLAUDE.md §2.2` 對 `fetch_yf_forward_pe` / `fetch_multpl_pe` 的退役註記 |

⚠️ **關於數量的硬規則**：客戶寫的 29 / 3 是**目標數字，不是驗收標準**。
**實際找到幾個就報幾個，不得為湊數字而刪。** 一個「為了湊到 29」而被刪掉的活檔案，
比留著 29 個死檔案嚴重得多（`CLAUDE.md §-1.5.1c 判定 3`：判「孤兒」要拿 caller 實測，
不能靠檔名或註解自稱）。

### 總管裁決：本 repo 做「取數抽離集中」，但**不改目錄名**

**裁決內容**：Phase 1.1 的「建立 `src/data/...`」在本 repo **不執行**；Phase 1 真正要做的是 **1.2 取數抽離集中**。

**理由（三條）**：
1. **三層架構本 repo 已經有**：`repositories/`（取數層）／`services/`（純計算層）／`ui/`（視圖層）。
   客戶要的分層目標已達成，差別只在**命名**。
2. **改名的代價與效益不成比例**：改目錄名要動數百個 import，**零使用者可見效益**；
   而真正的問題（**取數邏輯散在分頁裡**）改完名之後**依然存在**。
3. **憲法依據**：`CLAUDE.md §-1.5.1c 判定 1` 已就此裁定 —— v3 條文中的 `src/data/` 是
   **分層概念，不是字面路徑**。依據是 v3 條文**首句自陳「AI 總管不寫死硬編碼目錄」**，
   且其路徑列舉處寫的是「**例如**」。

⚠️ **此為總管裁決，客戶可推翻。** 若客戶的本意是「字面改目錄名」，那是一個 **scope 決定**，
走 `CLAUDE.md §8.4 步驟 4`（總管推薦方案：**不改**，理由如上）。請客戶裁示。

---

## Phase 1：資料庫管理、死碼清理與架構解耦（最優先）

**進度總覽（2026-08-28 更新，實測）**

| 子項 | 狀態 | 一句話 |
|---|---|---|
| 1.1 目錄分類 | ✅ **已符合**（不改目錄名） | 架構已達成，只是命名不同；總管裁決，客戶可推翻 |
| 1.2 取數抽離集中 | ~~🔨 **已派工開工**~~ → ✅ **主目標已完成** | ~~分支 `claude/fund-phase1-extract-sn42bh`，目標 `P-UIHTTP-1`~~ → **PR #732 → main `0178e4b`**：`_get_benchmark_series` 取數下沉 L1、UI 層自建快取移除 |
| 1.3 核心計算純化 | ~~⏳ **仍待查證**~~ → ⚠️ **1.3-a 查證已結案；1.3-b 修復未做**（2026-08-28 拆項，父項維持未打勾） | ~~本組的 grep 字表不完整，須獨立一組重掃~~ → **PR #733 → main `1acd089`** 以 AST 全量掃描取代 grep，**查出 45 項現況違規並登記**；該 PR 明文「**刻意不修任何一項**」 |
| 1.4 死碼實體刪除 | ✅ **完成** | PR #730 → main `149d1dd`，19 檔 / −3,733 行 |
| 1.5 FundClear/TDCC 保留 | ✅ **維持不動** | 但**證據已換**（見 1.5，原引的第二個檔已被刪） |

~~⚠️ **Phase 1 總項尚未可勾** —— **1.2 未完成、1.3 未查證**。
依維護規則第 2 條：**未完成不得跳級**，Phase 2 / Phase 3 的落地仍受此拘束。~~

~~→ **2026-08-28 v3 狀態變更（決策者：總管；不是原文寫錯，是兩項都前進了）**：
**1.2 主目標已合併（PR #732）、1.3 的查證已結案（PR #733）** → Phase 1 總項可勾，
**Phase 2 的阻擋因此解除**。~~
→ **2026-08-28 修正輪更正（決策者：總管（採納獨立稽核建議）；不是原文寫錯，
是處置被推翻之後這一句沒有跟著改）**：前半段**兩項仍然成立**（1.2 已合併、1.3 的查證已結案），
但「**Phase 1 總項可勾**」**已不成立** —— 1.3 已拆成 1.3-a／1.3-b，
**1.3-b（修復 45 項）未完成**，依維護規則第 1 條總項**維持 `- [ ]`**。
「**Phase 2 的阻擋因此解除**」**仍然成立**，但它靠的是**總管的放行決定**，
**不是**子項全綠（見下方 `Phase 1 總項` 的狀態句）。

~~⛔ **但「Phase 1 打勾」的實際含義必須寫清楚，否則這張表會開始說謊**（維護規則第 5 條）：~~
→ **2026-08-28 修正輪更正**：`Phase 1 總項` **現已未打勾**，故下表**不再是**「打勾的含義」，
改讀作「**這兩個子項各自完成了什麼／沒完成什麼**」（1.3 那一列即 **1.3-a 完成／1.3-b 未完成**）。
⚠️ **維護規則第 5 條的要求一字未減**：含義必須寫清楚，否則這張表會開始說謊。

| 子項 | 打勾的是什麼 | **沒有**打勾的是什麼 |
|---|---|---|
| 1.2 | `P-UIHTTP-1` 的 `_get_benchmark_series` 部分**已搬遷並合併** | `P-UIGSPREAD-1` / `P-UISUBPROC-1` **兩項裁決仍未開始**；`P-YFDUPE-1` / `P-BENCHTZ-1` 為本輪**新登記**的待判定項 |
| 1.3 | **「services/ 到底乾不乾淨」這個問題已經有答案**（B4 結案）—— 答案是**有 45 項違規** | **一項都沒修。** PR #733 檔頭自陳「本批刻意不修任何一項違規」，理由是搬 gspread 持久化服務出 L2 會動到 NAV 歷史鏈，屬 `§8.4 步驟 4` 的**範圍決定，需客戶核准** |

~~⚠️ **這是總管的判斷，不是本組的**，且它與維護規則第 2 條有張力，據實揭露而非美化：
規則 2 說「未完成不得跳級」。1.3 的**字面目標**（核心計算「純化」）**沒有達成** ——
達成的是它的**前置查證**。總管把 1.3 重新定義為「查證完成即可放行」，
把**修復本體**移出 Phase 1、登記為需客戶裁示的 scope 項（見**附錄 D**）。
**客觀證據**：PR #726 原本明文「壓著不合併，等 Phase 1 全部完成」，
而它已於 2026-08-28 15:00 UTC **合併**（`d6f5f87`）—— 也就是總管**已經**放行了這道 gate。~~
⛔ **不得**把本項打勾讀成「`services/` 已經純淨」。**它不純淨，而且我們現在確切知道有 45 處。**
（⚠️ 上句的「本項打勾」自 2026-08-28 起改指 **1.3-a**，見下方修正說明。）

> 📌 **2026-08-28 修正（決策者：總管（採納獨立稽核建議）；不是原文寫錯，是處置方式被推翻）**
>
> **上面那段加線的論證有兩個問題，兩個都成立：**
>
> 1. ⭐ **「重新定義父項」這個處置本身，架空了維護規則第 2 條。**
>    稽核指出：**這份文件裡已經有正確做法，只是 1.3 沒用上** ——
>    **1.4** 遇到同型處境（有些完成、有些 N/A、有些結案）時是**拆成 1.4-a/b/c/d 各自打勾**；
>    而 1.3 面對「查證完成、修復未做」，選擇了**改寫父項的定義**。
>    **同一份文件兩種做法不一致，而 1.4 那種同時滿足規則 1、規則 2 與現實。**
>    原處置（打勾＋就地限定）解決的是「**讀者會不會被騙**」，
>    **沒有解決「規則 2 還剩多少效力」** —— 若看板可以靠**把題目縮小到已完成的那一半**來打勾，
>    **規則 2 對任何一項都失去約束力**。
> 2. ⭐ **「客觀證據」那句是循環論證。** #726 是**總管自己放行**的；
>    拿它當「放行條件已滿足」的**獨立**證據，等於**同一個決定被看兩次**。
>    **正確讀法**：它是**總管確實做出該放行決定**的硬證據
>    （#726 自陳的放行條件是「等 Phase 1 全部完成」，而總管於 `15:00:40Z` 放行了它）——
>    **它證明「決定已作出」，不證明「條件已滿足」。**
>
> **處置**：**1.3 拆成 1.3-a（查證，已完成）／1.3-b（修復，未完成）**，
> **`Phase 1 總項` 依維護規則第 1 條退回 `- [ ]`**。
> ⚠️ **退勾不影響任何進行中的工作** —— #726 已合併、2.1-③/④ 已派工，
> **那個勾現在不 gate 任何東西，退回去成本是零。**

- [ ] **Phase 1 總項**（所有子項打勾後才可勾此項）
  ~~→ **2026-08-28 v3：已放行**（含義與保留事項見上表）~~
  ~~⚠️ 括號內那句**規則本身沒有失效**，加線只是因為它已被上表的「打勾的是什麼／沒有打勾的是什麼」取代 ——~~
  ~~**1.3 嚴格說並非「子項完成」，而是「子項被重新定義後完成」**，故不適用原括號的字面條件。~~
  → **2026-08-28 修正（決策者：總管（採納獨立稽核建議））：括號那句規則恢復字面效力，
  本項退回 `- [ ]`。** 舊表述加線保留，因為它記錄的是「**曾經有人想用重新定義父項來繞過它**」。

  **狀態句（不是 checkbox，不參與打勾）**：
  > **Phase 1 已由總管放行（2026-08-28）**，放行條件為「1.3 的**查證**完成」而非「1.3 完成」。
  > **Phase 2 的阻擋因此解除；1.3-b 移出 Phase 1、登記為 D-1。**
  > ⚠️ 「**已放行**」與「**已完成**」是兩件事：看板上未打勾的是**後者**。

  - [x] **1.1 動態資料庫分類**　→ **N/A（架構已符合，只是命名不同）**
        - **現況**：本 repo 已有 `repositories/{fund,macro,policy}/` 領域子目錄，
          外加 `services/`（純計算）與 `ui/`（視圖）。**無 `src/` 目錄**。
          證據：`ls -d src` → No such file；`ls -d repositories/*/`。
        - **裁決**：**不改目錄名**（總管裁決，理由見 §0；`§8.4 步驟 4` scope，**客戶可推翻**）。
          依 `CLAUDE.md §-1.5.1c 判定 1`，v3 的 `src/data/` 是分層概念不是字面路徑。
        - **實際要做**：**無**。本項標 `[x]` 的意思是「**已判定不需動工**」，不是「已完成搬遷」。
        - **阻擋者**：無。
        - ⚠️ 若客戶推翻此裁決 → 本項退回 `- [ ]`，並重新走 `§8.1` 架構規劃。

  - [x] **1.2 取數抽離集中**　⭐ **這是 Phase 1 真正要做的事**
        → **2026-08-28 v3：主目標已完成並合併（PR #732 → main `0178e4b`）。**
        - ~~**現況**：憲法 `§8.3.P` 已登記 **5 項待判定**（實測 `CLAUDE.md` 該節，量測日 2026-08-28），~~
          → **2026-08-28 v3 更正（不是原文寫錯，是本檔寫下之後該表長大了）**：
          憲法 `§8.3.P` 現為 **7 項待判定**（實測 `grep -oE "P-[A-Z]+-1" CLAUDE.md | sort -u`
          → `P-BENCHTZ-1` / `P-NAVCACHE-1` / `P-NDCCACHE-1` / `P-UIGSPREAD-1` / `P-UIHTTP-1` /
          `P-UISUBPROC-1` / `P-YFDUPE-1`；`CLAUDE.md` 表尾註記亦已同步為「7 列」）。
          ⭐ **新增的 2 列是本次搬遷過程中被稽核撿到的，不是掃描掃到的**
          （`P-YFDUPE-1` 是稽核推翻本組一句全稱句時撿出來的；`P-BENCHTZ-1` 是稽核逐行讀新程式碼時發現的埋雷）
          —— 對照維護規則第 7 條：**全稱句的壽命，取決於有沒有人回頭讀。**
          其中與本項直接相關的是：
          - ~~**`P-UIHTTP-1`** —— `ui/components/mk_dashboard.py::_get_benchmark_series`
            在 **UI 層用 `yfinance` 直抓外部行情**（實測該檔第 201–202 行
            `import yfinance as yf` / `yf.Ticker(ticker)`）＋ **`st.session_state` 自建快取**
            （實測第 195 行 `cache_key = "mk_bench_cache"`）。
            它產出的 `Benchmark_Lag` 是**顯示給使用者的業務標籤**，不是連線診斷 ping。
            → **這是 v3 `01`-1「主動搬遷」在本 repo 唯一已確認的適用對象。**~~
            → ✅ **2026-08-28 v3：已搬遷並合併（PR #732 → main `0178e4b`）。**
            **實測證據（本組自行重跑，非轉述 PR 描述）**：
            - `grep -n "def fetch_benchmark_close" repositories/macro/yf.py` → **第 156 行命中**
              （取數實作已住進 L1，與 `fetch_yf_close` 同檔 —— 選這個檔的理由是
              「本 repo 對 Yahoo 日線收盤的取數實作就住在這裡」，不是「基準屬於總經」）。
            - `grep -n "yfinance\|yf\.Ticker\|mk_bench_cache\|fetch_benchmark_close" ui/components/mk_dashboard.py`
              → **UI 層只剩第 209–210 行的一行委派**（`from repositories.macro.yf import fetch_benchmark_close`
              / `return fetch_benchmark_close(ticker)`），其餘命中全在**說明搬遷歷史的 docstring**。
              **`st.session_state["mk_bench_cache"]` 已不存在。**
            - 守衛：`ls -l tests/test_benchmark_fetch_migration_v19531.py` → **16,263 bytes**；
              `git show --stat 0178e4b` → 5 檔 / +487 / −27。
            - 分層處置：**UI 直呼 L1 + 登記 `EX-PASSTHRU-1` 第 9 組**（1 caller / 1 call site、
              caller 端零後處理、L1 已自帶 cache），**不建 L2 facade**（`§8.1 step 6`「用不到的抽象」反例）。
            ⚠️ **兩件刻意的行為變更，不在「零行為變更」的宣稱範圍內**（PR 描述自陳，本組照轉未複驗）：
            (1) 原 `except Exception` 一把抓連 schema 違反一起吞掉 → 現在 schema 違反**當場 raise**（`§1`）；
            (2) 原 session 快取**無 TTL** → 現在吃上游 `@_ttl_cache(TTL_10MIN)`，10 分鐘過期（`§2.4`）。
            ⚠️ **範圍收窄，不得誤讀為「UI 層已無外部直抓」**：本項處置的**只有** `_get_benchmark_series`。
            `ui/sidebar.py` 的「🔍 測試 Proxy 連線」按鈕仍以 `requests` 直打 Yahoo，
            但它已由憲法 `§-1.5.1c 判定 2` **A 類 A1** 判定為「連線診斷 ping、不產生業務指標」，
            **不是**本項的漏網。
          - **`P-UIGSPREAD-1`** —— `ui/**` 多處直呼 gspread client，
            **是否等同 EX-CRUD-1 的「本地持久化 CRUD」而受豁免，尚未裁決**。
          - **`P-UISUBPROC-1`** —— `ui/sidebar.py` 以 `subprocess` 跑 `git ls-remote origin main`
            對外往返，**算不算「私有資料抓取」尚未裁決**。
        - **待辦**：
          1. ~~**`P-UIHTTP-1` 搬遷方案設計**（取數下沉 `repositories/`、快取歸屬哪一層、
             `bench_ticker` 介面怎麼切）→ 依 `§8.1` 出架構規劃，**這一步禁止寫 code**。~~
             → ✅ **已完成（PR #732）。** 三個問題的實際答案：取數下沉
             `repositories/macro/yf.py`；快取**不新增**、吃上游既有 `@_ttl_cache`（**刻意不疊第二層**）；
             `bench_ticker` **仍由 UI 傳入**，介面即 `fetch_benchmark_close(ticker)`。
          2. **`P-UIGSPREAD-1` 裁決**（獨立一組；憲法明訂**不得**由補登／回修 `EX-UICACHE-1` 的組承接）
             —— **仍未開始**。
          3. **`P-UISUBPROC-1` 裁決**（獨立一組；憲法明訂**不得**由登記 C 類的同一組承接）
             —— **仍未開始**。
          4. 🆕 **`P-YFDUPE-1` 裁決**（**總管裁決，非查證題**）：
             `repositories/fund/sources.py::_src_yahoo_finance_nav`（`urlopen` 直打 query2、
             **不走 `infra.proxy`**）與 `repositories/macro/yf.py::fetch_yf_close`
             是否構成 v3 `01`-2 所禁的「同一資料來源兩套取數實作」。**仍未裁決。**
             ⚠️ 本項會牽動 `EX-PASSTHRU-1` 對 `fetch_benchmark_close` 的升級觸發條件。
          5. 🆕 **`P-BENCHTZ-1`**（**登記，不修**）：`fetch_benchmark_close` 對 `ticker` 零驗證，
             `normalize()` 隱含假設「開盤落在當日 UTC 內」→ 開盤早於 00:00 UTC 的交易所
             （ASX / NZX）會被 normalize 到前一天。**現況不是 bug**（UI 只有 SPY / QQQ），
             依 `§-1` 無觸發不修。
          ⚠️ **第 2~5 項不阻擋本子項打勾**：依憲法 `§8.3.P` 前言「**登記 ≠ 動工授權**」
          ＋ `§-1`「沒有 user 指派、沒有實際 bug 觸發 → 停手等指令」，它們是**待判定登記項**，
          不是 Phase 1 的未完成工作。**此為總管判斷，本組未獨立複驗。**
        - ~~**進度（2026-08-28 更新）**：⭐ **已派工開工** —— 分支
          `claude/fund-phase1-extract-sn42bh`，**目標為 `P-UIHTTP-1`**（上列待辦第 1 項）。
          ⚠️ **本組未讀其內容、未查證其進度**（本任務只維護 `TODO.md`，不讀他組工作區）；
          此處僅記「已開工」這個狀態，**不代表已完成**。`P-UIGSPREAD-1` / `P-UISUBPROC-1`
          兩項裁決**仍未開始**。~~
          → **2026-08-28 v3：該分支的工作已合併進 main（PR #732 → `0178e4b`），
          分支本身在 origin 上已不存在**（實測 `git ls-remote --heads origin` →
          只有 `data` / `main` / `push-assets` 三條）。
          ⚠️ **教訓沿用 v2 盲點 8 的同一條**：**分支名是過程的痕跡，不是進度本身。**
          該問的是「那件事做完了沒有」，不是「那個分支還在不在」。
          `P-UIGSPREAD-1` / `P-UISUBPROC-1` 兩項裁決**確認仍未開始**。
        - **阻擋者**：~~無（已開始）。~~ → **無（已完成）。**
          ⚠️ 但 **`EX-UICACHE-1` 整列掛在 `P-UIGSPREAD-1` 上**（憲法原文），
          故 `P-UIGSPREAD-1` **未裁決前，不得把新成員收進 `EX-UICACHE-1`**。
        - ⚠️ **`P-UIHTTP-1` 的搬遷屬架構改動，動工前須過 `§8.1`；若同時改到畫面結構，
          另須先出 UI 線框草稿送客戶拍板**（`§-1.5.1c 03`-2 ①）。本組判斷：純取數下沉
          **不改變畫面**，故應**不觸發**草稿 gate —— 但**此為本組單組判斷，未經第二組驗證**，
          實作組動工前須自行複核。

  - [ ] **1.3 核心計算純化**　~~⚠️ **打勾的是「查證」，不是「純化」——先讀完這一格再引用**~~
        → **2026-08-28 修正（決策者：總管（採納獨立稽核建議）；不是原文寫錯，是處置方式被推翻）**：
        **父項退回 `- [ ]`，改拆成 1.3-a／1.3-b 兩個子項各自打勾**（做法對齊 **1.4** 的既有先例）。
        **舊表述加線保留** —— 它誠實揭露了「打勾的只有一半」，**它沒有騙人**；
        被推翻的不是它的誠實，是「**用一段警語去撐一個粒度錯誤的勾**」這個處置。
        ⭐ **警語擋得住「讀者被騙」，擋不住「規則第 2 條被架空」** —— 這兩件事要分開看。
        **以下 `現況`／`實測證據`／`待辦`／`阻擋者` 四欄為 1.3-a 與 1.3-b 共用的證據本體，未改動。**
        - ~~**現況**：`services/` 依憲法 `§8.2` 已明定「L2 Service 不得 import
          `requests` / `httpx` / `beautifulsoup` / `feedparser`」。
          **本組實測**：`grep -rnE "(^|[[:space:]])(import (requests|httpx|feedparser|bs4)|from (requests|httpx|feedparser|bs4)[. ])" services/ --include=*.py`
          → **僅 2 行命中，且兩行都是註解／docstring**
          （`services/fx_regime_service.py:17`、`services/fund_screening.py:6`，內容是
          「不 import requests / httpx / bs4」這種自述句），**無任何真實 import 陳述**。~~
          → **2026-08-28 v3 狀態變更（決策者：總管；不是原文寫錯，是那條 grep 的射程被證明太窄）**：
          **PR #733 → main `1acd089` 已用 AST 全量解析（78 檔、0 parse 失敗）取代字面 grep，
          結果是 45 項現況違規。** 舊敘述「無任何真實 import 陳述」**在它自己的字表內為真** ——
          錯的不是那次量測，是**那個字表問錯了問題**（它只問「有沒有 import requests」，
          而本 repo 的 I/O 是走 gspread SDK、檔案系統、`infra.llm` 間接 HTTP 出去的）。
          **這正是舊敘述自己在下一段預告過的失效模式，一字不差地發生了。**
        - ~~⚠️ **單組結論，未經第二組驗證。** 且該指令**明確未涵蓋**：
          `urllib` / `socket` / `subprocess` / 第三方 SDK（`yfinance` / `gspread` / `feedparser` 以外者）／
          `importlib` 動態載入／經 `infra.proxy` 等本 repo 內部封裝再包一層的間接呼叫。
          **不得**把「這條 grep 跑過了」讀成「`services/` 已確認純淨」。
          （方法論教訓見 `CLAUDE.md §-1.5.1c 判定 2`：同一個字表缺陷在該節連錯兩次。）~~
          ⭐ **這一段是本檔目前為止最值得留著的一格 —— 它預言了自己的失效，而且說中了。**
          舊敘述自己列出「該指令未涵蓋」的清單裡就有 **SDK 與內部封裝的間接呼叫**，
          而 PR #733 實測查出的 45 項，**主體正是 gspread SDK 與 `infra.llm` 的間接 HTTP**。
          **加線的是它的「未查證」狀態，不是它的判斷 —— 它的判斷完全正確。**
          ⚠️ **本輪的新結論同樣是單組**：PR #733 自標「單組產出，未經第二組獨立複驗」，
          且「`services/` 沒有第 46 處」這句話**該 PR 與本組都沒有查證，也都不宣稱**。
        - **實測證據（本組自行重跑，非轉述）**：
          - `ls -l tests/test_services_purity_contract.py` → **55,984 bytes / 1,003 行**；
            `git show --stat 1acd089` → **1 檔 / +1,003**，**零 production code 改動**。
          - 債務總量釘死在測試裡：該檔 `test_debt_tables_are_debt_not_approval()`
            `assert total <= 45`，就地註明「**2026-08-28 建表當下的實測總量 = 45。只准往下，不准往上。**」
          - 45 項的分布（8 張豁免表）：`IMPORT_DEBT` / `FS_WRITE_DEBT` / `FS_READ_DEBT` /
            `GSPREAD_DEBT` / `UI_DEBT`（實測 = 空）/ `SERVICES_TO_UI_DEBT`（實測 = 空）/
            `L1_TO_L2_DEBT` / `NAMESPACE_INJECTION_DEBT`。
        - ⛔ **三件必須寫明的事，否則本項會被誤讀成「services/ 已純淨」**：
          1. ⭐ **一項都沒修。** PR #733 明文「**本批刻意不修任何一項違規**」——
             理由是把 gspread 持久化服務搬出 L2 會動到 NAV 歷史鏈，
             屬 `§8.4 步驟 4` 的**範圍決定，需客戶核准，不在該批授權內**。
          2. ⭐ **豁免表是技術債的可見化，不是核准。** 該檔就地寫明這 45 項
             「**≠ 核准，也 ≠ 走完 §8.2.A 例外登錄程序**（那三步這些項目都沒走）」。
             守衛的作用是 **ratchet：擋住債再長大**，不是宣告債已清。
          3. ⭐ **守衛本身有已揭露的盲區**（該檔 docstring 自陳，本組照轉未複驗）：
             `globals()` 注入的 3 個 shim 是**靜態分析的絕對盲區**；經 `repositories`／`infra`
             的間接 I/O **只看一層**；gspread 的 `.update()` 因與 `dict.update` 撞名而未收進字表；
             動態 import；`open(p, mode)` 的 mode 是變數時一律歸「讀」。
             且該 PR 自標「**單組產出，未經第二組獨立複驗**」。
        - ⚠️ **一筆被守衛抓到、值得記住的事**：`services/nav_history_gs.py` 檔頭自陳的豁免理由是
          「**不在 §8.2「L2 禁 requests/httpx/bs4/feedparser」清單**」，並拿 `auto_search_store_gs`
          當先例 —— **兩檔互為背書，兩檔都沒登錄在 §8.2.A**。
          **那不是豁免理由，那是規則字表的漏洞被拿來當通行證**，
          正是 `§8.2` 末句明文禁止的「未經登錄的軟例外」。
        - ~~**待辦**：派**獨立一組**用**涵蓋所有網路形態**的字表重掃 `services/`，
          並比對是否有經內部封裝的間接 I/O。**在該複掃完成前，本項不得打勾。**~~
          → **已完成（PR #733）。** **現行待辦：45 項債的修復**，已移出 Phase 1、
          登記於**附錄 D-1**，需客戶 scope 裁示（`§8.4 步驟 4`）。
        - **阻擋者**：~~無（查證可立即開始）；但若查出違反者，其修復**排在 1.2 之後**
          （同屬架構解耦，避免兩組同時改 `services/` 撞檔 —— `§-1.5.1c 00` 多 Agent 派工防撞）。~~
          → **2026-08-28 v3**：查證已完成；**修復本體的阻擋者是客戶的 scope 裁示**。
          ⚠️ **舊表述的「排在 1.2 之後」仍然成立且仍要遵守** —— 1.2 已完成，該序列條件已滿足；
          但**防撞的理由沒有失效**：修 45 項債會動到 `services/**`，
          與 Phase 2 的分頁重組**是不同的檔案邊界，兩者可平行；與任何其他改 `services/` 的批次則不可**。

        **子項（2026-08-28 新增；粒度對齊 1.4 的既有先例）**

        - [x] **1.3-a 查證：`services/` 是否零 I/O**
              → **已結案（PR #733 → main `1acd089`）**：AST 全量掃 **78 檔**（0 parse 失敗），
              **答案是「不是」—— 45 項違規**。證據本體見上方四欄；附錄 B 的 **B4** 同步結案。
              - **阻擋者**：無（已完成）。
        - [ ] **1.3-b 修復：45 項純度債**
              → **未開始。** PR #733 明文「**本批刻意不修任何一項違規**」。
              - **阻擋者**：⭐ **客戶的 scope 裁示**（見**附錄 D-1**）——
                搬 gspread 持久化服務出 L2 **會動到 NAV 歷史鏈**，屬 `§8.4 步驟 4` 的範圍決定。
              - ⛔ **本子項未打勾這件事本身就是重點**：它讓「`services/` 還沒純化」
                **留在看板上**，而不是被父項的一句警語吸收掉。

  - [x] **1.4 死碼實體刪除**　→ **已完成（PR #730 → main `149d1dd`，2026-08-28）**
        - **總結**：**11 個 production 0 caller 模組 ＋ 8 個孤兒測試 ＝ 19 檔 / −3,733 行**，分 3 波刪除。
        - **測試對帳（總管親自實測，非轉述）**：
          - main `b00eabf`（刪除**前**）：**14 failed / 5574 passed / 17 skipped**
          - 分支（刪除**後**）：**14 failed / 5463 passed / 17 skipped**
          - **差額 111 ＝ 被刪掉的測試數**；**failed 維持 14 且逐一同名**
            （12 個 `test_dividend_anchor_v19527` ＋ 2 個 `test_dividend_calendar`，
            全是沙箱缺 `holidays` 套件的**既有假紅**，與本批無關）。
          - ⚠️ **failed 數相同不等於沒事** —— 這裡站得住腳是因為**逐一同名比對過**，不是因為數字一樣。
        - [x] **1.4-a `stock_etf_dashboard/`（2,658 行）→ N/A，本 repo 不存在**
              - **現況**：`ls -d stock_etf_dashboard` → `No such file or directory`（本 repo）。
                姊妹 repo `my-stock-dashboard` **同樣不存在**，且其 git 歷史有
                commit `e4e03f6`「刪除 stock_etf_dashboard/：先驗後刪，附可達性硬證明」。
              - **裁決**：**本項在本 repo 無適用對象，標 N/A。** 客戶此項源自姊妹 repo。
              - **阻擋者**：無。
        - [x] **1.4-b `services/fund_batch.py` → 已刪除（PR #730）**
              - ~~**現況**：**檔案存在**（10161 bytes，量測日 2026-08-28）。
                憲法 `§8.2` L2 Service 列記載「v19.406 批次攤平器；**v19.413 RETAINED-LEGACY**」。
                **待辦**：由**獨立一組**實測 production caller 數，判定是活碼還是孤兒。
                **阻擋者**：無（查證可立即開始）；**刪除動作**須等 caller 實測完成。~~
              - → **2026-08-28 狀態變更（決策者：user）：已完成。不是原文寫錯，是本檔寫下之後它被刪了。**
                caller 實測結果為 **production 0 caller**，已於 **PR #730（main `149d1dd`）實體刪除**（−221 行）。
                證據：`git show --stat 149d1dd`；`ls services/fund_batch.py` → No such file。
              - ⭐ **這一項正好驗證了舊敘述當時堅持的那條紀律**：檔頭自稱
                「**RETAINED-LEGACY … 保留不刪**」，而依 `§-1.5.1c 判定 3`
                **「已自稱 Archive」不是免驗證的通行證** —— 最後是**實測 caller** 才把它判成孤兒，
                **不是**因為它自稱 legacy 就留、也**不是**因為它自稱 legacy 就刪。**舊敘述加線保留，因為它記錄的是這條紀律。**
              - **阻擋者**：無（已完成）。
        - [x] **1.4-c 「29 個無效測試模組」→ 已結案：客戶的數字是錯的，實際是 11 個**
              - ~~**現況**：**本組沒有清單。** 僅知 `tests/*.py` 共 **326 個檔**
                （`ls tests/*.py | wc -l`，量測日 2026-08-28）—— 這是**總數**，不是無效數。
                **待辦**：等獨立稽核組回報**具名清單**。
                **阻擋者**：**獨立稽核組的清單**。清單未到 → **不動工**。~~
              - → **2026-08-28 狀態變更（決策者：user）：清單已到並已執行完畢。**
              - ⭐ **客戶的「29」被推翻，理由要記住（比數字重要）**：稽核組**複現了那個 29 是怎麼算出來的**
                —— 它**只看模組路徑字面有沒有出現在別的檔案裡**。該方法實測至少 **5 個假陽性**
                （`repositories/ai_cache.py`、`repositories/batch_checkpoint.py` 等，**其實都有人用**）。
                正確方法必須同時涵蓋 **相對 import / barrel re-export / 動態載入 / workflow 與 docs 引用**，
                據此算出的可刪數是 **11 個**。
              - **實際刪除**：**11 個 production 0 caller 模組 ＋ 8 個孤兒測試 ＝ 19 檔 / −3,733 行**，
                分 **3 波**（波與波之間有真實依賴，故不可一次砍）：
                - **第 1 波**（葉節點、互不依賴）：`services/ai_advisor_pending.py`、
                  `services/cross_source_compare.py`、`services/switch_state_machine.py`、
                  `services/fund_batch.py`、`services/calibration/risk.py`、
                  `services/calibration/signal_threshold.py`、`repositories/tw_macro_repository.py`
                - **第 2 波**：`services/tdcc_nav_accumulate.py`、`repositories/tdcc_nav_opendata.py`、
                  `services/macro_signal_lookback.py`
                - **第 3 波**：`services/macro/signal_lookback.py`
              - 證據：`git show --stat 149d1dd`（PR #730 → main `149d1dd`）。
              - ✅ **§0 的紅線「實際找到幾個就報幾個，不得為湊數字而刪」在本項被真正執行了一次**
                —— 沒有為了湊到 29 而多刪 18 個。
              - **阻擋者**：無（已完成）。
        - [x] **1.4-d 「停用的 3 個總經指標模組」→ 已結案：查無對應對象，N/A**
              - ~~**現況**：**本組沒有清單，也未查證是哪三個。**
                **待辦**：同 1.4-c，等具名清單。
                **阻擋者**：**獨立稽核組的清單**。~~
              - → **2026-08-28 狀態變更（決策者：user）：稽核組查不到對應對象，本項判為 N/A。**
              - **兩條線索都已失效（逐一列出，不含糊帶過）**：
                1. `fetch_tw_export_yoy` —— 早在 **v19.355** 就改走**海關 opendata**，**活得好好的**，不是停用模組。
                2. `fetch_yf_forward_pe` / `fetch_multpl_pe` —— **上週已刪**（見 `CLAUDE.md §2.2` 該列的退役註記）。
                3. 總經指南針三層鏈的佔位檔 —— **也早已刪完**。
              - ⭐ **最重要的一點：稽核組沒有為了湊數字硬找三個交差。**
                這正是 §0 那條紅線（「客戶給的是**目標數字**，不是**驗收標準**」）在實務上第二次生效。
                **一個為了湊到 3 而被刪掉的活模組，比留著 3 個死模組嚴重得多。**
              - **阻擋者**：無（已完成）。
        - ⚠️ **1.4 全項共同的動工紅線**（`CLAUDE.md §-1` ＋ `§-1.5.1c 判定 3`）：
          - GC（垃圾清理）是「**任務內的收尾義務**」，**不是主動巡邏授權**。
            ⛔ **不得**引用本項發動「全 repo 掃孤兒檔」的巡邏。
          - **刪程式碼／死碼／Archive 檔 → 內部自決**（git 保留完整歷史 ＝ 可逆）。
          - **刪正式生產資料**（Google Sheets 帳本／保單、歷史快照、線上 DB 表）→ **必須請示客戶**。
          - **正式下架仍在被使用的既有功能** → **必須請示客戶**（「孤兒 ≠ 沒人用」）。

  - [x] **1.5 FundClear / TDCC 官方淨值路線保留**
        - ~~**現況**：兩條路線的實作**都在**：`repositories/fundclear_offshore.py`、
          `repositories/tdcc_nav_opendata.py`（`ls repositories/`，量測日 2026-08-28）。~~
          → ⚠️ **2026-08-28 更正（決策者：user；不是原文寫錯，是本檔寫下之後檔案被刪了）**：
          **舊敘述所列的第二個檔 `repositories/tdcc_nav_opendata.py` 已於 PR #730 實體刪除**
          （連同 `services/tdcc_nav_accumulate.py`，屬第 2 波），**實測 production 0 caller**。
          ⚠️ **本項結論（兩條官方淨值路線都還在）仍然成立，但理由必須換掉**：
          **TDCC 取數本身沒有消失**，它住在 **`repositories/fund/sources.py`**
          （實測該檔 `__all__` 含 `tdcc_get_agents` / `tdcc_search_fund` / `_src_tdcc_meta` /
          `_tdcc_resolve_fund_name`，且第 587 行起有 TDCC→cnyes 名稱橋接的實作）。
          被刪掉的是**另一條 0 caller 的 opendata 旁支**，不是主線。
          **現況（實測 2026-08-28 覆核）**：`repositories/fundclear_offshore.py` **仍在**；
          TDCC 走 `repositories/fund/sources.py`。憲法 `§2.1` 記載 FundClear（境外主）／
          TDCC（境內主）為 T1 官方來源，**該記載未受影響**。
          ⚠️ **這一處不在交辦點名的 5 處之內，是本組逐檔比對時抓到的第 6 處失效** ——
          方法教訓見附錄 C 盲點 7。
        - **裁決**：**本項是「不動」** —— 官方淨值路線暫留、排入備援接線。故直接標 `[x]`。
        - ⚠️ **一處據實揭露**：總管交辦時把本項出處寫為「**憲法決定二**」，
          但本組實測 `grep -rn "決定二" *.md docs/*.md` → **0 命中**。
          故本項的出處目前只能記為「**總管口述交辦**」，**未在 repo 文件中查得該編號**。
          若「決定二」另有出處（非 `.md` 檔、或在 PR / commit 描述內），請補上；
          在補上之前，**不得**把「憲法決定二」當成可引用的條文編號。
        - **阻擋者**：無。

---

## Phase 2：介面動線整併與元件化

~~- [ ] **Phase 2 總項**~~
→ **2026-08-28 v3 狀態變更（決策者：總管）：阻擋已解除，Phase 2 進行中。**
**解除依據（客觀事實，非宣稱）**：Phase 2 的阻擋者原本是「Phase 1 全項打勾 ＋ 測試通過」；
PR #726 自陳「壓著不合併，等 Phase 1 全部完成」，而它已於 **2026-08-28 15:00 UTC 合併**
（`d6f5f87`，`merged: true`，經 **GitHub API 實測** PR #726 狀態）——
**總管已經放行了這道 gate**，本檔只是把該事實記下來。
📌 **2026-08-28 補註（決策者：總管（採納獨立稽核建議））—— 這句話證明的範圍要收窄**：
它證明的是「**放行的決定已經作出**」，**不是**「**放行的條件已經滿足**」。
**兩者不同**：`Phase 1 總項` 現已依維護規則第 1 條**退回 `- [ ]`**（**1.3-b 未完成**），
而 **Phase 2 的阻擋仍然解除** —— 因為解除阻擋靠的是**總管的放行決定**，不是子項全綠。
⚠️ **不得**反推「Phase 2 開工了 ⇒ Phase 1 完成了」。

- [ ] **Phase 2 總項**　🔨 **進行中**（子項見下；父項須全部子項打勾後才可勾）

  - [ ] **2.1 分頁動線整併**　🔨 **進行中：7 → 5**
        - **現況（重要校正，v3 覆核仍成立）**：客戶原文稱「5 大分頁」，**實測是 7 個頂層分頁**：
          `app.py` 的 `st.tabs(...)` 解出 **7 個** —— `tab_macro` / `tab_health` / `tab_batch` /
          `tab_single` / `tab_portfolio` / `tab_manage` / `tab_ref`。
          **另有巢狀第二層**：`tab_ref` 之內再開 `st.tabs(["🔭 資料診斷", "📖 說明書"])`。
          證據：`grep -n "st.tabs" app.py`；`app.py` 該處上方註解自陳
          「故 **7 個分頁名**全部收進 `story_nav._TAB_LABELS`」。
          → **2026-08-28 v3 覆核（證據更新，結論未變）**：`grep -n "st.tabs" app.py`
          → **第 231 行**（7 個頂層）＋ **第 334 行**（`tab_ref` 內巢狀 2 個），
          **仍為 7 個，尚未變動。**
        - ⭐ **v3 新增：五分頁對映已有客戶拍板的線框可依循。**
          出處：`docs/wireframes/fund-wireframe-final.html` **第 02 節**
          （標題實測為「**提案：7 個分頁 → 5 個，照決策順序排**」，該檔第 218–220 行）。
          該節把「新結構」定義為**把分頁順序排回客戶實際做決定的順序**：
          ① 現在該進攻還防守 → ② 我手上這組健康嗎 → ③ 這一檔／這一批怎樣 →
          ④ 那我要怎麼配・記帳 → ⑤ 設定與診斷。
          ⭐ **線框就地寫明「功能一個都不刪，只換位置」** —— 引用本節時不得把它讀成刪功能授權。
        - **待辦**：**任何**分頁合併／拆分／改名／欄位增減 → 依 `CLAUDE.md §-1.5.1c 03`-2 ①
          **動工前必須先出文字線框草稿（Wireframe）送客戶拍板**。
          ⛔ **不得**以「v3 禁止重複分頁」或「這只是小改動」為由跳過草稿 gate。
          ✅ **本項的草稿 gate 應已過** —— `docs/wireframes/fund-wireframe-final.html` 即該草稿，
          已於 PR #728（main `592b1fa`）進版控。
          ⚠️ **「客戶拍板」這件事的證據強度要講清楚，不要含糊**：本組看到的是
          **commit 標題自陳**「三份**客戶拍板**線框進版控」＋檔名 `-final`，
          **沒有看到客戶拍板的原始紀錄**（那不在 repo 內）。
          依 `§-2` 規則 6：**這是轉述，不是本組查證的事實** ——
          動工前請由承接組向客戶確認一次，不要靠這一行。
          ⚠️ **但草稿拍板只覆蓋「線框畫了的東西」**：施工中若出現線框沒畫到的版面決定，
          **仍須回頭補草稿**，不得以「大方向拍過了」概括。
        - **阻擋者**：~~**客戶對線框草稿的拍板**。草稿未拍板 → 不得寫任何 UI 程式碼。~~
          → **2026-08-28 v3：草稿已拍板並進版控（PR #728 → `592b1fa`），本阻擋解除。**

        **五分頁子項（依線框第 02 節的去向表拆分；⚠️ 對映為本組逐格轉錄，未經第二組驗證）**

        | 新分頁 | 原分頁（線框列出的行數） | 去向 | 本檔子項 |
        |---|---|---|---|
        | ① 🌐 市場定調 | 🌐 市場定調（1,537+1,734 行） | **不動** | 2.1-① |
        | ② 💊 組合健診 | 💊 組合健診（1,217 行 + 十餘子模組） | **不動，升格為「健診唯一出處」** | 2.1-② |
        | ③ 🔍 基金研究 | 🔍 個基深掘（2,637 行）＋ 📦 批次分析（532 行） | **合併** | 2.1-③ |
        | ④ 📊 我的配置 | 📊 配置 & 帳本（6,403 行） | **保留配置與帳本，其餘搬走** | 2.1-④ |
        | ⑤ ⚙️ 設定與診斷 | 📋 我的管理室（556 行）＋ 📖 參考／診斷（2,383 行） | **合併** | 2.1-⑤ |

        - [ ] **2.1-① 🌐 市場定調（不動）**
              - **現況**：線框第 02 節去向欄寫「**① 不動**」，理由是盤點認定它是
                「**全站職責講得最清楚的一頁**」，一句話職責直接沿用。
              - **待辦**：分頁本身不動；**但線框同一格就地註明「頁內的圈號同樣要清理
                （六套裡有兩套就在這頁）」** —— 該清理屬第 1 批「統一語言」，不屬本子項。
              - **阻擋者**：無。
              - ⚠️ **「不動」不等於「已完成」**：本子項要打勾的是「**接線後這一頁確認未被改壞**」，
                不是「我們決定不碰它」。故維持 `- [ ]` 直到接線驗收。
        - [ ] **2.1-② 💊 組合健診（不動，升格唯一出處）**
              - **現況**：線框去向欄寫「**② 不動，升格為「健診唯一出處」**」，
                理由實測轉錄：「健診三表現在在**兩個分頁各畫一次**，
                『配置 & 帳本』的說明文字**自陳「與組合健診完全同源」**」。
              - **待辦**：把「配置 & 帳本」那一份重複的健診三表**移除**，讓 ② 成為唯一出處。
                ⚠️ **這是刪掉畫面上的東西 → 屬版面異動**，須確認落在已拍板線框的射程內；
                線框第 02 節寫「功能一個都不刪，只換位置」，**移除重複視圖與刪功能不是同一件事**，
                但**該判定屬結論**，依 `§-2` 規則 5 須派工複核，不由動手的一方自認。
              - **阻擋者**：**2.1-④**（重複的那一份住在 ④，兩邊會改到同一批檔案 ——
                `§-1.5.1c 00` 多 Agent 派工防撞）。
        - [ ] **2.1-③ 🔍 基金研究（個基深掘 ＋ 批次分析 合併）**　🔨 **已派工，未合併**
              - **現況**：**本日已派工**，分支後綴 `...-wpc`。
                **實測 `git ls-remote --heads origin` → origin 上只有 `data` / `main` / `push-assets`
                三條，該分支尚未推送。**
                ⛔ 依維護規則第 6 條 ＋ 本檔 v2 盲點 8 的教訓：**未合併的東西不得打勾**，故維持 `- [ ]`。
                ⚠️ **本組未讀其工作區、未查證其進度**（本任務只維護 `TODO.md`）。
              - ⚠️ **這一格是本輪最需要客戶注意的一格**：線框第 02 節**自己標明
                「批次分析併去哪裡」是真的要客戶拍板，不是既定事實**，並列為 **Q6**。
                **線框就地給出的程式碼查驗結果（實測轉錄）**：批次分析那張大表的欄位設定
                **定義在「組合健診」的模組裡**（`fund_grp_health/columns.py:494`），
                而**唯一的外部呼叫者就是批次分析**（`tab_batch_analysis.py:439`）；
                批次分析從組合健診 **import 5 個模組**，**個基深掘對這套設定零引用**。
                → **就血緣而言，批次分析是「組合健診的 400 檔版」，不是「個基深掘的批次版」。**
                線框仍先照「掛在 ③」畫，理由是**資訊架構而非程式血緣**
                （② 的一句話職責是「我手上這幾檔」，而 400 檔批次掃的是「你還沒買的東西」；
                把要跑 30~40 分鐘的批次任務放進每天看持倉的那頁，會毀掉 ② 那句話）。
              - **待辦**：**Q6 需客戶拍板**（③ 或 ②）。⚠️ 若客戶選 ②，本子項的範圍會整個改變。
              - **阻擋者**：無（已開工）；但 **Q6 的答案會改變本子項的定義**。
        - [ ] **2.1-④ 📊 我的配置（保留配置與帳本，其餘搬走）**　🔨 **已派工，未合併**
              - **現況**：**本日已派工**，分支後綴 `...-wpd`。**同 2.1-③，實測 origin 上尚未推送。**
                ⛔ **未合併不得打勾**，維持 `- [ ]`。**本組未讀其工作區。**
              - **現況（線框）**：原「配置 & 帳本」**6,403 行，是七頁中最大的一頁**；
                線框第 03 節的 ④ 定義了保留與搬走的切割。
                ⭐ 線框第 03 節前言就地寫明：「**講不出一句話職責的頁面，就是還沒設計好；
                這正是現在『配置 & 帳本』的狀況。**」—— 本子項的驗收標準因此包含
                「**新的 ④ 講得出一句話職責**」。
              - **待辦**：依線框第 03 節 ④ 執行；搬出去的部分分別落到 ② 與 ⑤。
              - **阻擋者**：無（已開工）。
        - [ ] **2.1-⑤ ⚙️ 設定與診斷（我的管理室 ＋ 參考／診斷 合併）**
              - **現況**：**尚未派工。** 線框去向欄理由實測轉錄：「兩者都是『設定與檢查』，
                **不在決策動線上**。**NAV 匯入現在同時存在於這兩頁**」。
              - **待辦**：合併兩頁；**NAV 匯入的重複入口收斂為一處**。
              - **阻擋者**：⭐ **2.1-④** —— ④ 要「其餘搬走」，而**搬出去的東西有一部分的目的地就是 ⑤**。
                **④ 未定案前動 ⑤，會出現「搬到一半的東西沒有落點」。**
        - [ ] **2.1-⑥ `app.py` 7 → 5 接線**
              - **現況**：`app.py` 第 231 行仍是 7 個 `st.tabs`（實測，2026-08-28）。
              - **待辦**：待 ①~⑤ 五頁全部就位後，改 `app.py` 的分頁註冊；
                同步 `story_nav._TAB_LABELS`（`app.py` 該處註解自陳「**7 個分頁名**全部收進」——
                改成 5 個時**這裡會一起壞**，不是可選的收尾）。
              - **阻擋者**：⭐ **2.1-① ~ 2.1-⑤ 全部** —— 接線是最後一步，
                任何一頁沒就位，接線就會把使用者帶到半成品。
              - ⚠️ 憲法既有共識（線框第 06 節就地記載，**本 repo 已發作過兩次**）：
                「分頁**自稱的名字**」與「**分頁列上的名字**」對不上 → 接線時兩邊必須同時改。
        - [ ] **2.1-⑦ 五套 gate 旗標 → 一套**
              - **現況（線框第 05 節實測轉錄）**：因為分頁切換只是前端效果、
                七頁內容每次操作都會全部重跑，所以**每個分頁各自發明了一套守門旗標** ——
                **市場定調、組合健診、配置頁、個基深掘、批次分析各一套，五種行為、五個出錯面**。
                線框同時指出**現成的最佳實作已經存在**：配置頁的**持倉指紋快取**是全站唯一
                「指紋不含時間、內容變了才重算、另給一顆顯式重算鈕」的完整設計，**可直接當統一方案的藍本**。
              - **待辦**：五套收斂為一套，**以持倉指紋快取為藍本**。
              - **阻擋者**：⭐ **2.1-⑥（接線）** —— 線框第 06 節把它排在**第 3 批、風險最高、排最後**，
                並就地寫明理由：「**動的是每個分頁的執行條件，最容易改出「該顯示卻沒顯示」**」。
              - ⛔ **驗收方式是硬性的，不是建議**：線框寫「**建議排在版面定案之後單獨做，
                且每頁分開驗收**」。→ **五頁逐頁驗，不得一次改完一起驗。**
              - ⚠️ **本子項不改任何畫面結構 → 屬內部自決**（`§-1.5.1c 03`-1「Checkbox Gate 延遲載入」）；
                但**若順手調整版面，那部分要拆出來另走草稿 gate**（`§-1.5.4` 禁止夾帶）。

  - [ ] **2.2 UI 元件化（6 個新元件）**
        - **現況**：已有 **6 個新 UI 元件**的工作在進行中，總管交辦時指為分支
          `claude/fund-ui3b-components-sn42bh`。
          ~~⚠️ **實測校正**：`git ls-remote --heads origin | grep -iE "ui3b|sn42bh"`
          在 origin 上**只找到 `claude/fund-wireframe-docs-sn42bh` 一個分支**，
          **`claude/fund-ui3b-components-sn42bh` 尚未推送到 origin**（量測日 2026-08-28）。
          → 該工作目前應存在於**某個 agent 的本地 clone**，尚未上遠端。~~
          → **2026-08-28 狀態更新（不是原文寫錯，是時序差）**：上面那段在**量測當下為真**，
          但**該分支已於本檔量測之後推送到 origin，並已開出 PR #726**。
          **舊敘述加線保留、不刪**，因為它記錄的是「本檔的量測發生在推送之前」這個事實 ——
          刪掉會讓後人以為本組當時漏看。
          **新事實（實跑 `git ls-remote --heads origin` 覆核）**：
          `claude/fund-ui3b-components-sn42bh` **存在於 origin**（`de721ef`），
          對應 **PR #726**（**draft**，標題「UI3b：新增 6 個純函式 UI 元件（誠實性優先）+ 41 條突變驗證過的測試網」）。
          ~~該 PR 內文自陳「**本 PR 壓著不合併**，等 Phase 1 全部完成並通過測試後才放行」，
          **與本檔下方『裁決』欄一致**（理由：**Phase 1 未完成不得跳級**，見本檔維護規則第 2 條）。~~
          → ✅ **2026-08-28 v3 狀態變更（決策者：總管；不是原文寫錯，是 gate 被放行了）**：
          **PR #726 已合併** —— 經 **GitHub API 實測** PR #726 →
          `state: closed`／`draft: false`／`merged: true`／`merged_at: 2026-08-28T15:00:40Z`／
          `merged_by: linchen-20200325`；落在 main 為 **`d6f5f87`**（7 檔 / +1,965）。
          ~~**這就是「Phase 1 已完成」最硬的客觀證據** —— 該 PR 自己寫的放行條件是
          「等 Phase 1 全部完成並通過測試」，而總管放行了它。~~
          → **2026-08-28 修正（決策者：總管（採納獨立稽核建議）；這是循環論證，不是筆誤）**：
          **#726 是總管自己放行的**，拿它當「放行條件已滿足」的**獨立**證據，
          等於**同一個決定被看兩次**。**正確讀法**：
          **這是「總管確實做出該放行決定」的硬證據** —— #726 自陳的放行條件是
          「等 Phase 1 全部完成」，而總管於 `15:00:40Z` 放行了它。
          ⭐ **它證明「決定已作出」，不證明「條件已滿足」。**
          **本組仍未查證其程式碼內容**（本任務只新增／維護 `TODO.md`，不讀他組工作區）——
          上表所引 PR 標題與合併時機均取自 ~~PR 描述本身~~ → **PR 描述與 API 回傳**，
          **不是**對其實作正確性的背書。
          📌 **2026-08-28 補加刪除線（決策者：總管（採納獨立稽核建議））**：v3 編修時把
          「本身」直接換成「與 API 回傳」**沒有加線**，屬**無聲改寫**。新表述**更準確**
          （本輪確實多用了一次 API），保留的是**舊措辭**，不是改回去。
          ⚠️ **值得記一筆的是它為什麼沒被抓到**：本組 v3 用的驗證法是「**被刪行的內容是否仍在檔內**」，
          而「本身」兩字在檔內別處巧合出現 → **字元沒消失，但那個「表述」消失了**。
          **檢查「字元有沒有不見」驗不出「說法有沒有被換掉」。**
        - **實測落地內容（本組自行 `git show --stat d6f5f87` 重跑）**：`ui/components/` 下
          **6 個新元件** —— `macro_light_board.py`（184）／`health_quadrant_scatter.py`（248）／
          `nav_sigma_channel.py`（212）／`column_group_tabs.py`（162）／`formula_card.py`（192）／
          `allocation_donut_card.py`（185），＋ `tests/test_ui3b_components.py`（782 行）。
          `ls ui/components/` 覆核：六檔均在。
          ⚠️ **數字有兩個版本，據實並列不擇一**：commit 標題寫「**41 條突變驗證過的測試網**」，
          PR 內文寫「**68 條、全部通過突變驗證**」與「`test_ui3b_components.py`：**68 passed**」。
          本組研判 41 ＝ 突變條數、68 ＝ 測試總數，**但這是推測，未複驗** —— 標為待查證。
        - ~~**裁決（總管明訂）**：**壓著不合併** —— **等 Phase 1 全部通過測試才放行**。~~
          → **2026-08-28 v3：已放行並合併。**
        - ~~**阻擋者**：**Phase 1 全項打勾 ＋ 測試通過**。~~
          → **2026-08-28 v3：阻擋解除。**
        - ⛔ **但本子項仍維持 `- [ ]`，理由必須寫明**：PR #726 自陳
          「**本批只新增檔案，不改任何既有 caller —— 接線是下一批的事**」
          「**尚未做（刻意）**：接線既有 caller、以及任何既有檔案的修改」。
          → **6 個元件已存在，但畫面上還看不到它們。**
          **「元件建立」完成 ≠「UI 元件化」完成**；接線併入 2.1 的五分頁重組執行。
        - **現行阻擋者**：**2.1 各子項**（接線與版面重組是同一批工作，分開做會撞檔）。
        - ⚠️ 依 `CLAUDE.md §4`（Auto-Ship 常設授權的三道邊界）：merge 免請示，
          但 **CI 綠 ＋ 獨立稽核通過是硬前提**，「不必再問一次」不等於「不必先驗」。
        - ⚠️ **PR #726 自己登記了 3 件要總管裁決的事，尚未見裁決**（照轉，未複驗）：
          (1) 再平衡偏差門檻與 4D Grade→status 對映**不是常數 SSOT**，建議後端具名
          `REBALANCE_WARN_PCT` / `REBALANCE_CRIT_PCT` / `FOUR_D_GRADE_LEVELS`；
          (2) 「0% 那片用極細環」**做不到而不做假**（plotly 對 `value=0` 不畫弧，
          要看得到細環只能餵假的極小值 = `§1` 禁止的捏造）→ 若客戶堅持要有那圈灰線，
          **屬版面設計變更，應走草稿 gate**；
          (3) `column_group_tabs` 預設 `mode="tabs"` 的重繪成本，接線時若量到有感再改參數。

  - [ ] **2.3 Streamlit 效能防護**（防重繪 `st.form`、Checkbox Gate 延遲載入）
        - **現況**：**待查證。** 本組**未跑任何 UI 稽核**，不宣稱本 repo 現況符合或不符合。
          （憲法 `§-1.5.1c 判定 7` 亦把本 repo 的 3 欄網格／Checkbox Gate 現況
          **登記為待稽核項**，明訂「不得在未稽核前宣稱本 repo 已符合」。）
        - **待辦**：派獨立一組做 UI 效能稽核，產出**具名清單**（哪些分頁無 gate、哪些重運算無 form 包）。
        - **阻擋者**：~~無（稽核可立即開始）；**修復**排在 Phase 1 之後（同 2.2 的放行條件）。~~
          → **2026-08-28 v3 更新**：Phase 1 已放行，該條件已滿足。
          **現行**：稽核仍可立即開始；**但「五套 gate 旗標 → 一套」這一半已移入 2.1-⑦**
          （它與分頁重組是同一批工作，且線框把它排在**第 3 批、最後做、每頁分開驗**）。
          本項剩下的是 **`st.form` 防重繪**與**其餘重運算的 gate 現況稽核**。
        - ⚠️ **手段自決，版面問客戶**：「用 Checkbox Gate 把重運算擋在點擊之後」屬**內部自決**
          （畫面結構不變）；「把分頁改排成 3 欄網格」屬**版面異動 → 先出草稿**。

---

## Phase 3：測試守衛與資料真實性

📌 **2026-08-28 v3：Phase 3 全項維持未打勾，這是刻意的，不是漏更新。**
依維護規則第 2 條「**未完成不得跳級**」：Phase 2 剛解除阻擋、五個分頁子項一個都還沒合併，
而 3.1 的阻擋者明文是「Phase 1 與 Phase 2 的**修復本體**」——**沒有修復就沒有可突變的對象**。
⚠️ 本輪確實有兩批新測試進 main（PR #733 的 22 條純度守衛、PR #726 的 68 條元件測試），
**但它們不是 Phase 3 的交付** —— 它們分別是 1.3 的查證工具與 2.2 的自帶守衛，
**已就地記在那兩格，不在此重複計功**（同一件事記兩個地方 = 進度被灌水）。

- [ ] **Phase 3 總項**

  - [ ] **3.1 突變測試（拿掉修復必須轉紅燈）**
        - **現況**：本 repo 已建立「**拿掉修復，測試必須轉紅**」的慣例
          （憲法 `§-1.5.1c 03`-1 明列「突變測試（拔掉修復邏輯必須轉為紅燈）」為內部自決項）。
        - **待辦**：對 Phase 1 / Phase 2 產出的每一個修復，補上會轉紅的守衛測試。
        - **阻擋者**：**Phase 1 與 Phase 2 的修復本體**（沒有修復就沒有可突變的對象）。

  - [ ] **3.2 四大鐵律的機器守衛**　→ **部分完成：三鐵律守衛已上線（PR #729），但不等於已符合**
        - ~~**現況**：總管交辦所述 —— **四大鐵律中有三條目前零機器守衛**（已實測），
          守衛正在建，指為分支 `claude/fund-ui3b-guards-sn42bh`。~~
          → **2026-08-28 狀態變更（決策者：user；不是原文寫錯，是狀態前進了）**：
          **守衛已合併進 main（PR #729 → `b00eabf`）**，**不再是「正在建」**。
          新增 3 個測試檔、**206 條測試**：`tests/test_ui_grid_contract.py`、
          `tests/test_ui_rerun_contract.py`、`tests/test_first_screen_contract.py`
          （證據：`git show --stat b00eabf`）。
        - ⛔ **兩件必須據實寫明的事，否則這一項會被誤讀成「已符合」**：
          1. ⭐ **守衛擋的是「往後不准變糟」，不是「現在已符合」。**
             實測 **109 個 `columns()` 呼叫中只有 21 個是 3 欄**，其餘 **88 個非 3 欄進了豁免表**。
             **豁免表是待辦的可見化，不是批准** —— 讀這一項時**不得**把「守衛綠燈」讀成「版面已達標」。
          2. ⭐ **鐵律 3（三態顏色）沒有在這批修。**
             它的守衛目前 **fail-open**（**包成 helper 就隱形**，掃不到）。
             **總管裁決：單獨排一批。** 翻成 fail-closed 的影響估算：
             **約 20 個站點會轉紅，其中約 8 個是真發現、約 8 個是需先建別名解析的噪音。**
             ⚠️ 這個估算**本組未複驗**，照轉。
        - **分支狀態（v1 舊敘述整段作廢，加線保留）**：
          ~~⚠️ 實測校正（同 2.2）：該分支在 origin 上不存在（`git ls-remote --heads origin`
          當時只有 `claude/fund-wireframe-docs-sn42bh`；v1 更新輪已就地更正該舉證所列的分支清單，
          結論「guards 分支未推送」當時仍成立），應仍在某個 agent 的本地 clone。~~
          → **2026-08-28 v2：整段作廢，理由是它問錯了問題。**
          `claude/fund-ui3b-guards-sn42bh` 這個分支名**在 origin 上仍查無**（實測覆核），
          但**守衛工作根本不是經該分支落地的** —— **實際落地的是 PR #729（`b00eabf`），已在 main**。
          故「那個分支在哪」**已不影響任何判斷**。
          ⚠️ **教訓**：v1 追著「分支存不存在」問，但真正該問的是「**那件事做完了沒有**」。
          **分支名是過程的痕跡，不是進度本身。**
        - ⚠️ **「四大鐵律具體是哪四條」本組仍未查證**（v1 即標明，**本次未解**）。
          「三條零守衛」那半已由 PR #729 處理掉，見上。→ 見附錄 B **B8**。
        - **待辦（尚未完成的部分）**：
          1. ~~補齊三鐵律守衛~~ → **已完成（PR #729）**。
          2. **鐵律 3（三態顏色）的守衛翻成 fail-closed** —— 總管裁決單獨排一批，**尚未開始**。
             → **2026-08-28 v3：狀態未變，仍未開始**；已一併登記於**附錄 D-2**，
             以免它只存在於這一行、沒有出口（對照 `CLAUDE.md §8.3.P` 前言：
             **待查證沒有出口 ＝ 實質永久豁免**）。
          3. **88 個非 3 欄豁免項的收斂** —— 豁免表只是可見化，**還沒有人去收**。
          4. 由承接組**具名列出**四大鐵律各是哪一條（本組仍未查證是哪四條）。
        - **阻擋者**：無（守衛本身已上線）；
          但**鐵律 3 那一批**依總管裁決單獨排，**尚未排入**。
          ⚠️ 原「合併順序同 2.2」那句**對 PR #729 已不適用**（它已經合併了）；
          **對 Phase 2 的 #726 仍然適用**，見 2.2 段。

  - [ ] **3.3 資料真實性驗證（`§1` Fail Loud）**
        - **現況**：憲法 `§1` 已定「寧可炸掉，不可造假」；`§-1.5.1c 02` 另加四條具體要求
          （全 0 序列隱藏該欄不畫假地平線／單位混用強制標「資料疑義」／
          只快取成功結果、失敗退避／介面狀態灰 vs 紅嚴格分離）。
          `§-1.5.1c 判定 4` 已逐條登記本 repo 的符合狀態，其中
          **「失敗時退避，不連續轟炸來源」登記為「未符合 → 新增待辦」**。
        - **待辦**：**待查證** —— 憲法該處另記載有一組平行派工正在實作
          `shared/backoff_policy.py` ＋ `infra/source_backoff.py`，
          但當時**尚未提交、尚未合併、尚未經獨立 QA 稽核**。
          本組**未查證其現況**（本任務不讀他組工作區）。
          → 承接者須**現場確認該實作是否已合併**，再更新本項狀態。
        - **阻擋者**：無（查證可立即開始）。
        - ⚠️ **落地時的已知張力**（憲法就地記載，不得抹掉）：
          `repositories/fund/fx_and_main.py` 的 **positive-only 快取是 v18.275 的刻意設計**
          （`None` 不入 cache，避免 poisoning）。加失敗冷卻**等於把它反過來** ——
          落地時**必須同時滿足兩邊**（冷卻要短於成功 TTL、過期後必須真的重試），
          **不得**以「v3 要退避」為由把 v18.275 的設計理由抹掉。

---

## 附錄 A：Agent 分工對照

客戶指定四種角色。它們對應本 repo 憲法 `CLAUDE.md §-2` 的「**執行 AI**（subagent）」編制 ——
**是編制，不是另一套與 `§-2` 平行的體系**：

| 客戶指定角色 | 憲法對應（`§-1.5.1c 00` 內部虛擬 Agent 團隊） | 本 repo 的典型工作面 |
|---|---|---|
| **資料工程** | 資料工程組 (Data Stewards) | `repositories/**` 取數、清洗、時序對齊、快取設計 |
| **計算服務** | 計算服務組 (Engineers) | `services/**` 純 Python 指標公式、評等、回測、再平衡 |
| **前端 UI** | 前端/UI 組 | `ui/**` Streamlit 視圖、防重繪 `st.form`、Checkbox Gate |
| **紅隊稽核** | 品管與紅隊稽核組 (QA/Audit) | 交付前模擬試用、邊界與相容性測試、突變測試 |

**四條不可鬆動的規則**（全部出自 `CLAUDE.md §-2`，本表只是複述，衝突時以憲法為準）：

1. **每一次任務都要派工**（規則 1）—— 不論大小，調查／實作／測試／稽核都派 subagent。
   ⚠️「這件事小到不用派工」**這個判斷本身就是最常出錯的那一步**（憲法載有三次實證）。
2. **總管不自己寫實作**（規則 2）—— 總管的產出是規格、判斷、複驗結論、給客戶的報告。
3. ⭐ **實作與稽核必須分開派工，不可同一組自己查自己**（規則 4）——
   本檔多處已就地標明「**不得由某某組承接**」（例如 `P-UIGSPREAD-1` 不得由補登／回修
   `EX-UICACHE-1` 的組承接），那些**不是客套話，是硬性限制**。
4. **總管自己的結論也要驗**（規則 6）—— 總管產出的全稱句同樣沒有第二雙眼睛，
   要嘛派一組獨立驗，要嘛**明說「這是我自己看的，沒有第二組驗過」**。
   ⚠️「明說沒驗過」**不是萬用免責**：標註後該宣稱只能當**待驗事項**，
   **不得**作為後續動作的前提，也**不得**寫進 commit message／PR 描述當成已完成的事實。

---

## 附錄 B：本檔目前標為「待查證」的項目（動工前必須先解掉）

| 編號 | 待查證內容 | 誰來查 |
|---|---|---|
| ~~B1~~ **已結案** | ~~「29 個無效測試模組」的**具名清單**（本組只知 `tests/*.py` 共 326 檔，非無效數）~~ → **2026-08-28 結案**：客戶的「29」經稽核組複現其產生方式後判定**數字錯誤**（至少 5 個假陽性），正確數為 **11 個**，已於 PR #730 全數刪除。詳見 1.4-c | ~~獨立稽核組~~ 已完成 |
| ~~B2~~ **已結案** | ~~「停用的 3 個總經指標模組」的**具名清單**（本組完全未查證是哪三個）~~ → **2026-08-28 結案：查無對應對象，判為 N/A**。兩條線索皆失效（`fetch_tw_export_yoy` v19.355 已改走海關 opendata 且仍在用；`fetch_yf_forward_pe`/`fetch_multpl_pe` 上週已刪；指南針佔位檔早已刪完）。**稽核組未為湊數字硬找三個交差。** 詳見 1.4-d | ~~獨立稽核組~~ 已完成 |
| ~~B3~~ **已結案** | ~~`services/fund_batch.py` 的 production caller 數（「RETAINED-LEGACY」是自稱，須實測）~~ → **2026-08-28 結案**：實測 **production 0 caller**，已於 PR #730 實體刪除。⭐ **是實測 caller 判定的，不是因為它自稱 legacy** —— 該紀律見 1.4-b | ~~獨立一組~~ 已完成 |
| ~~B4~~ **已結案（答案是「不是」）** | ~~`services/` 是否真的零 I/O（本組的 grep 字表**未涵蓋** `urllib` / `socket` / `subprocess` / 其他 SDK / 動態 import / 內部封裝間接呼叫）~~ → **2026-08-28 結案**：PR #733（main `1acd089`）以 **AST 全量解析 78 檔**取代字面 grep，答案是 **`services/` 不是零 I/O —— 實測 45 項違規**（gspread 遠端往返含寫入、檔案系統讀寫、經 `infra.llm` 的間接 HTTP、L1→L2 上行 import、`globals()` 執行期注入）。⭐ **本項的價值不在數字，在於它證實了舊字表的失效模式**：舊 grep 只問「有沒有 import requests」，而本 repo 的 I/O 是走 **SDK／檔案系統／內部封裝**出去的 —— **問錯問題的量測，跑幾次都是同一個答案**。⚠️ **查證結案 ≠ 修復**，45 項一項都沒修，見**附錄 D-1** | ~~獨立一組~~ 已完成（PR #733） |
| B5 | `P-UIGSPREAD-1`：`ui/**` 直呼 gspread 是否等同 EX-CRUD-1 的「本地持久化 CRUD」 | 獨立一組，**不得**由補登／回修 `EX-UICACHE-1` 的組承接 |
| B6 | `P-UISUBPROC-1`：`ui/sidebar.py` 的 `subprocess` 對外往返算不算「私有資料抓取」 | 獨立一組，**不得**由登記 C 類的同一組承接 |
| B7 | Phase 2.3：本 repo UI 的 `st.form` 防重繪／Checkbox Gate 現況（本組**未跑任何 UI 稽核**） | 獨立一組 |
| B8 | Phase 3.2：「四大鐵律」具體是哪四條（本組**仍未查證**）。~~哪三條零守衛~~ → **2026-08-28 部分結案**：**三條的守衛已上線**（PR #729 → `b00eabf`，3 檔 206 條測試）；**鐵律 3（三態顏色）仍 fail-open**，總管裁決單獨排一批。⚠️ **守衛上線 ≠ 已符合**（88 個非 3 欄在豁免表內）。**「四條各是哪一條」仍為待查證** | 承接守衛工作的那一組 |
| ~~B9~~ **部分結案** | ~~Phase 3.3：失敗退避實作（`shared/backoff_policy.py` / `infra/source_backoff.py`）**是否已合併**（本組未查證）~~ → **2026-08-28 實測：兩個檔案在 main 上都已存在**（`ls shared/backoff_policy.py infra/source_backoff.py` 皆命中）。⚠️ **「檔案在」不等於「已接線、已生效、已通過稽核」** —— 本組**只驗了檔案存在**，**未讀其實作、未跑測試、未查證它與 `fx_and_main.py` v18.275 positive-only 設計的張力是否已處理**。**該張力仍是 3.3 的硬要求，見該項。** | 承接組現場確認**接線與張力處理**（存在性已驗） |
| B12 🆕 | `P-YFDUPE-1`：`repositories/fund/sources.py::_src_yahoo_finance_nav`（`urlopen` 直打 query2、**不走 `infra.proxy`**）與 `repositories/macro/yf.py::fetch_yf_close` 是否構成 v3 `01`-2 所禁的「同一資料來源兩套取數實作」。⚠️ **本 repo 至少還有第三套**（`scripts/fetch_nav_cache.py::fetch_yahoo_finance_history`）＋ `repositories/financial_repository.py` 真的 `import yfinance`。**Phase 1.2 做到的是「不再新增一套」，不是「全站收斂成一套」** | **總管裁決**（判斷題，非查證題；需佐證再派獨立組） |
| B13 🆕 | `P-BENCHTZ-1`：`fetch_benchmark_close` 對 `ticker` 零驗證，`normalize()` 隱含假設「開盤落在當日 UTC 內」→ ASX / NZX 會被 normalize 到**前一天**。**現況不是 bug**（UI 只有 SPY / QQQ），依 `§-1` 不修，**只登記** | 獨立一組（實作與稽核分離） |
| B14 🆕 | PR #726 的測試條數：commit 標題寫「**41 條突變驗證**」、PR 內文寫「**68 條**」與「68 passed」。本組研判 41 ＝ 突變條數、68 ＝ 測試總數，**但這是推測，未複驗** | 承接接線的那一組（順手確認即可） |
| B10 | 1.5「憲法決定二」的出處（本組 `grep -rn "決定二" *.md docs/*.md` → **0 命中**） | 總管補上出處，或改記為口述交辦 |
| B11 | ~~`claude/fund-ui3b-components-sn42bh` 與 `claude/fund-ui3b-guards-sn42bh` 兩分支的內容與進度（實測 origin 上**均不存在**，應仍在本地 clone）~~ → **2026-08-28 更正（不是原文寫錯，是時序差；「均不存在」在量測當下為真）**：現為 **guards 一個未推送**。**components 已推送到 origin**（`de721ef`）並開出 **PR #726**（draft、壓著不合併，理由是 **Phase 1 未完成不得跳級**）→ 其**內容**仍待查證，但**進度**已明。**`claude/fund-ui3b-guards-sn42bh` 仍未推送**，該半項**維持待查證**。 ~~→ **2026-08-28 v2 再更新**：**components 的 PR #726 仍為 open / draft、仍壓著不合併**（狀態未變）；~~**guards 那半項已無意義** —— 守衛工作**不是**經該分支落地的，而是 **PR #729（`b00eabf`）已合併進 main**，故該分支名是否存在**已不影響任何判斷**（見 3.2 段）。 → **2026-08-28 v3 結案**：**PR #726 已合併**（`d6f5f87`，`merged_at 2026-08-28T15:00:40Z`），其**進度**已完全確定；**內容仍未讀**（本組只驗檔案存在與 `git show --stat`，未讀實作、未跑測試）。**實測 `git ls-remote --heads origin` → origin 上只剩 `data` / `main` / `push-assets` 三條**，本檔歷來追過的所有 `claude/fund-*` 分支**全部已不存在**。⭐ **v2 的教訓在本輪第二次生效：分支名是過程的痕跡，不是進度本身。** | 各該承接組 |

---

## 附錄 D：本輪（v3）登記但**未做**的技術債

> **本節存在的理由**：v3 這一輪有大量「**查出來了、但沒修**」的東西。
> 如果只把它們寫在各該子項的內文裡，它們會**隨著那一格被打勾而消失在視線外** ——
> 那正是 `CLAUDE.md §8.3.P` 前言要防的形態：「**待查證沒有出口 ＝ 實質永久豁免**」。
> 故集中登記於此，每一項都要能回答：**債在哪、誰來處理、什麼時候觸發。**
>
> ⛔ **每一項都適用同一條紅線 ——「登記 ≠ 動工授權」**（`CLAUDE.md §-1`）：
> user 沒明確指派、沒有實際 bug 觸發 → **停手等指令**。
> **不得**因為「TODO.md 附錄 D 寫了」就去做其中任何一項 ——
> 那正是 `§-1` 明列的「❌ 機械式清 TODO list 充數」與「❌ 因為文件寫了就提議」。
> ⚠️ **登記也不是豁免**：登記在此**不代表**已判定合憲或可接受，只代表「**已知、未處理**」。

| 編號 | 債在哪 | 規模（實測） | 為什麼這輪沒做 | 觸發點 |
|---|---|---|---|---|
| **D-1**<br>（＝ **1.3-b**） | `services/` 純度：AST 全量掃描查出的現況違規（gspread 遠端往返含寫入／檔案系統讀寫／經 `infra.llm` 的間接 HTTP／L1→L2 上行 import／`globals()` 執行期注入） | **45 項**，釘在 `tests/test_services_purity_contract.py::test_debt_tables_are_debt_not_approval` 的 `assert total <= 45`（ratchet：**只准往下，不准往上**） | PR #733 明文「**本批刻意不修任何一項違規**」——把 gspread 持久化服務搬出 L2 **會動到 NAV 歷史鏈**，屬 `§8.4 步驟 4` 的**範圍決定，需客戶核准**，不在該批授權內 | (1) **客戶就 scope 裁示**；(2) 有任務碰到 `services/nav_history_gs.py` / `auto_search_store_gs.py` 等登記在案的檔；(3) 有人要**調高**那個 `<= 45` 的上限（**那一刻就是本項到期日**） |
| **D-2** | 鐵律 3（三態顏色）的守衛目前 **fail-open** —— **包成 helper 就隱形**，掃不到 | 翻成 fail-closed 的影響估算：**約 20 個站點會轉紅，其中約 8 個是真發現、約 8 個是需先建別名解析的噪音**。⚠️ **此估算本組未複驗，照轉**（來源：守衛組，見 3.2） | **總管裁決單獨排一批**（理由：它會一次翻紅約 20 處，混進其他批次會讓歸因失效 —— 對照 `PROCESS.md §3` 相依序列化） | (1) 該批被排入；(2) 有任務碰到 `ui/helpers/render_state.py` 或三態呈現；(3) 出現「該紅沒紅／該灰卻紅」的實際回報 |
| **D-3** | `STATE.md` 的**存檔紀錄停在 2026-08-12** | 實測 `grep -n "^## " STATE.md \| head` → 最新一筆存檔標題為「**✅ 2026-08-12 存檔：📋 我的管理室…（v19.433・PR #625）**」。⚠️ **要分清兩件事**：檔案**本身**有被後續 commit 動過（`git log -1 -- STATE.md` → `149d1dd`，2026-08-28，就地補了事實更正），**但沒有新增任何一筆存檔** —— 也就是 **PR #625 之後這一整輪的工作（含 #722~#733）一筆都沒進 STATE.md** | 本輪各批的授權範圍都是**單一主題**（守衛／刪碼／搬遷／本檔），**補存檔不在任何一批的檔案邊界內**（`§-1.5` 第一條第 2 點 File Boundary；夾帶進來會同時違反 `§-1.5.3 C` 禁止夾帶） | (1) 有任務碰到 `STATE.md`；(2) user 直接點名；(3) 下一次有人要靠 `STATE.md` 回答「最近做了什麼」時發現它答不出來 |
| **D-4** | main 上有 **106 個既存 commit 的訊息含 session URL**（`claude.ai/code`） | 實測（2026-08-28，本 clone）：`git log origin/main --format=%H \| wc -l` → **182** 個 commit；`git log origin/main --grep="claude.ai/code" --format=%h \| wc -l` → **106**；另 `--grep="session_"` → **113**。⚠️ **106 與 113 是兩個不同的字串各自的命中數，不是同一份清單的兩種算法**，本組**未比對兩者的交集與差集** | ⛔ **清理需改寫已推送的歷史（`force-push`）**，而依 `CLAUDE.md §-1.5.1a 接合 A1`，**`git push --force` 與重寫已推送歷史明文不在 merge 常設授權內，須逐案請示客戶** —— **不在既有授權範圍** | **只有一個**：**客戶明示要求**。⛔ **任何人都不得以「順手清乾淨」為由發動** —— 那會重寫 182 個 commit 中的 106 個，是本 repo 目前可想像的**最大單一破壞性操作** |

⚠️ **本附錄為單組登記，未經第二組獨立複驗**（`CLAUDE.md §-2` 規則 6）。
「**本輪只有這 4 項技術債**」這句話**本組沒有查證，也不宣稱** ——
上表是**本輪讀到的**，不是**本輪存在的**。

---

## 附錄 C：本檔的產製方法與盲點（誠實揭露，依 `CLAUDE.md §-2` 規則 6）

**方法**：於本 repo 的獨立 clone 內實跑下列指令，逐項判讀後落筆；
凡未實跑者一律寫「待查證」，**不猜狀態**。

```
ls -d src                                   # → No such file or directory
ls -d repositories/*/                       # → fund/ macro/ policy/
ls -d stock_etf_dashboard                   # → No such file or directory
ls -l services/fund_batch.py                # → 10161 bytes（量測當下；PR #730 後該檔已刪除）
grep -n "yfinance\|yf\.Ticker\|mk_bench_cache" ui/components/mk_dashboard.py
grep -rnE "(^|[[:space:]])(import (requests|httpx|feedparser|bs4)|from (requests|httpx|feedparser|bs4)[. ])" services/ --include=*.py
grep -n "st.tabs" app.py                    # → 7 頂層 + tab_ref 內巢狀 2
ls tests/*.py | wc -l                       # → 326（量測當下；PR #729 +3、PR #730 −8 後，2026-08-28 覆核為 321）
git ls-remote --heads origin | grep -iE "ui3b|sn42bh"
grep -rn "決定二" *.md docs/*.md            # → 0 命中
grep -n "P-UIHTTP-1\|P-UIGSPREAD-1\|P-UISUBPROC-1\|P-NAVCACHE-1\|P-NDCCACHE-1" CLAUDE.md
```

（姊妹 repo `my-stock-dashboard` 側為**唯讀**查證：`ls src/data/`、`git log --oneline -1 e4e03f6`。）

**已知盲點（不要當成「全都查過了」）**：

1. ⭐ **本檔全部內容由單一組產出，未經第二組獨立複驗**（`§-2` 規則 6）。
2. **本組只讀 `CLAUDE.md` 的相關節次與上列指令的輸出**，
   **未通讀** `PROCESS.md` / `SPEC.md` / `ARCHITECTURE.md` / `STATE.md` / `BACKLOG.md` / `docs/*.md`。
   若那些檔另有與本表牴觸的敘述，本次**沒查到**。
3. **本組未讀任何他組的工作區**（Phase 2.2 的 6 個元件、Phase 3.2 的守衛、
   Phase 3.3 的退避實作），故那三項的**內容**是待查證，不是「已知」。
   ⚠️ ~~故那三項的內容與**進度**全部是待查證~~ → **2026-08-28 更正（不是原文寫錯，是時序差）**：
   「**進度**也全部未知」這半句**已不成立** —— Phase 2.2 的**進度**現已明
   （分支已推送、PR #726 draft、壓著不合併），見 2.2 段與 B11。
   但其**內容仍未讀**；Phase 3.2 / 3.3 則**內容與進度兩者都仍是待查證**。
   ⚠️ 本項是被**這一次更正**抓到的第 4 處連帶失效 —— 交辦時只點名 3 處；
   **一個事實變了，要把同一把尺對全檔同類敘述重跑一次**，不能只改被點名的那幾行。
4. **本組未跑任何測試、未跑任何 UI 稽核**。本檔一個字都不宣稱「本 repo 已符合 X」。
5. **`services/` 純淨性的 grep 字表不完整**（見 B4）。
   **能被一條 grep 推翻的全稱句，不該寫成結論** —— 故本檔在該處寫的是分類敘述加明列未涵蓋範圍，
   而不是「`services/` 已確認乾淨」。
6. **本檔不構成動工授權。** 依 `CLAUDE.md §-1`：user 沒明確指派時 → **停手等指令**。
   ⛔ **不得**因為「TODO.md 上寫了」就去做某一項 —— 那正是 `§-1` 明列的
   「❌ 機械式清 TODO list 充數」與「❌ 因為文件寫了就提議」。

### v2 更新輪（2026-08-28，Phase 1.4 後同步）新增的方法教訓

7. ⭐ **「一個事實變了，要把同一把尺對全檔重跑」—— 本輪又抓到一處交辦沒點名的失效。**
   交辦點名 5 處，本組逐檔比對後發現**第 6 處**：**§1.5 引用的
   `repositories/tdcc_nav_opendata.py` 也在 PR #730 被刪了**。
   若只改被點名的 5 處，§1.5 會留下一個指向不存在檔案的「證據」——
   **一份會說謊的進度表，比沒有進度表更危險**（對照 `CLAUDE.md §8.2.A.0` 立節理由）。
   ⚠️ 這是**同一個教訓在本檔第二次生效**（第一次見 v1 更新輪的盲點 3）。
   **往後每次狀態同步，都要把變動事實對全檔同類敘述掃一遍，不能只改被點名的行。**

8. ⭐ **「rebase 之後，基線要重量」——本輪的真實案例。**
   Phase 1.4 執行組回報的測試基線 **5368** 是在守衛（PR #729，**+206 測試**）合併**之前**量的，
   **rebase 後沒有重量**。→ **數字本身內部一致，但基線對不上**。
   總管親自重量後為：刪除前 **5574 passed**、刪除後 **5463 passed**、差額 **111**。
   ⚠️ **失效模式要記住**：兩個數字各自都沒錯，錯的是**它們不是在同一個世界量的**。
   **rebase / merge 之後，任何「前後對比」的基線都必須重新量一次，不得沿用 rebase 前的數字。**

9. **本輪同樣是單組產出，未經第二組獨立複驗。**
   本輪**實跑覆核**的有：`git log --oneline -6 origin/main`、`git show --stat 149d1dd`、
   `git show --stat b00eabf`、`git show --stat 592b1fa`、被刪檔案的 `ls` 存在性、
   `ls tests/*.py | wc -l`（→ **321**）、`repositories/fund/sources.py` 的 TDCC symbol。
   **未複驗**的有（照轉，標明出處）：測試 pass/fail 數字（總管親自實測）、
   「29 的產生方式與 5 個假陽性」（稽核組）、「109 個 `columns()` 中 21 個是 3 欄」（守衛組）、
   「鐵律 3 翻 fail-closed 約 20 站點轉紅、約 8 真 8 噪音」（估算，未複驗）。

### v3 更新輪（2026-08-28，Phase 1 收尾／Phase 2 解除阻擋後同步）的方法與盲點

> 上面 1~9 點是 **v1／v2 兩輪**的方法與盲點，**一字未刪未改、全部仍然有效**。
> 本小段是**第三輪（v3）**自己的，**不覆蓋**前面任何一點。

**本輪實跑的指令（在本 repo 的獨立 clone 內；未實跑者一律寫「待查證」，不猜狀態）**

```
git log --oneline -12 origin/main            # → 見沿革 v3 的四筆
git show --stat 1acd089 / 0178e4b / d6f5f87  # → 逐筆核對檔案清單與行數
git log -1 --format=%B <hash>                # → 讀完整 commit 訊息，不只讀標題
git ls-remote --heads origin                 # → 只有 data / main / push-assets 三條
grep -n "def fetch_benchmark_close" repositories/macro/yf.py          # → :156
grep -n "yfinance|yf.Ticker|mk_bench_cache|fetch_benchmark_close" ui/components/mk_dashboard.py
ls -l tests/test_services_purity_contract.py tests/test_benchmark_fetch_migration_v19531.py
sed -n '985,1003p' tests/test_services_purity_contract.py             # → assert total <= 45
grep -oE "P-[A-Z]+-1" CLAUDE.md | sort -u    # → 7 項（v2 當時是 5 項）
grep -n "st.tabs" app.py                     # → :231（7 個）+ :334（巢狀 2 個），未變
ls ui/components/                            # → 6 個新元件均在
ls tests/*.py | wc -l                        # → 324（v2 量測為 321）
grep -n "^## " STATE.md | head               # → 最新存檔仍是 2026-08-12
git log origin/main --format=%H | wc -l      # → 182
git log origin/main --grep="claude.ai/code" --format=%h | wc -l       # → 106
sed -n '218,300p' docs/wireframes/fund-wireframe-final.html           # → 第 02 節五分頁去向表
```

（PR 狀態經 GitHub API 實測：`pull_request_read #726` → `merged: true`／`merged_at 2026-08-28T15:00:40Z`。）

**本輪已知盲點（不要當成「全都查過了」）**

10. ⭐ **本輪的核心判斷「Phase 1 可以打勾」不是本組作的，本組只是把它記下來。**
    真正發生的事是：**總管放行了 PR #726**（該 PR 自己寫的放行條件是「等 Phase 1 全部完成」）。
    本組做的是**找出那個決定的客觀痕跡**並據實記錄，**不是**自行判定 Phase 1 已完成。
    ⚠️ 這件事有一處**與維護規則第 2 條的真實張力，已在 Phase 1 總項就地揭露、未美化**：
    **1.3 的字面目標（純化）沒有達成，達成的是它的前置查證。** 若客戶認為 1.3 該退回 `- [ ]`，
    本檔應照改 —— **那不會是「本檔寫錯」，是「總管的 scope 判斷被推翻」。**
    → ✅ **2026-08-28 當天就發生了，而且結局比本段預測的更值得記**：
    **獨立稽核組推翻了那個處置**，總管採納 —— **1.3 拆成 1.3-a／1.3-b，`Phase 1 總項` 退回 `- [ ]`**。
    ⭐ **三件事要分清楚，不要記成一件**：
    (a) **本段的自我揭露有效** —— 它把張力寫在最顯眼的地方，稽核才有東西可以抓；
    (b) **但「揭露」不等於「處置正確」** —— 本組揭露了張力，卻選了一個**架空規則第 2 條**的處置
        （靠警語撐住一個粒度錯誤的勾）。**誠實不能替代正確。**
    (c) **推翻它的不是客戶，是稽核** —— 本段當時預設「只有客戶能推翻總管」，
        實際上**第二組眼睛就夠了**。這正是 `§-2` 規則 4「平行派工、不可自己查自己」的用處。
    ⚠️ **同一輪稽核還抓到本組另一個錯**：v3 PR 描述寫的「37 行內容全部仍在檔內」
    **實際是 36/37**（見盲點 17）。**那句話本身就是一個沒被第二組驗過就寫進記錄的全稱句。**

11. **本組未讀任何他組的工作區、未跑任何測試、未跑任何 UI 稽核**（同 v1 盲點 3/4）。
    本輪新增的所有「已完成」判定，證據**一律是 main 上的 commit 與檔案實測**，
    **不是**對其實作正確性的背書。特別是：
    - PR #732 自陳的「兩件刻意行為變更」（schema 違反改 raise、快取加 TTL）**本組未驗**；
    - PR #733 自陳的守衛盲區（`globals()` 注入、間接 I/O 只看一層、`.update()` 未收字表）**本組未驗**；
    - PR #726 的 6 個元件**本組未讀一行實作**，只驗了檔案存在與行數。

12. ⭐ **`...-wpc` / `...-wpd` 兩個工作包：本組只知道「已派工」，其餘一無所知。**
    **實測 origin 上沒有這兩條分支**（`git ls-remote --heads origin`）——
    這**只證明它們沒推上遠端**，**不證明**它們的進度、內容或正確性。
    依維護規則第 6 條 ＋ v2 盲點 8：**未合併的東西不得打勾**，故 2.1-③ / 2.1-④ 維持 `- [ ]`。

13. **Phase 2 五分頁的拆分，是本組逐格轉錄線框第 02 節的去向表得來的**，
    **不是**本組自行設計的工作分解。⚠️ **轉錄可能有損** ——
    線框第 03 節還有五頁逐頁的細部線框（本輪只讀了 ① 的開頭），
    各子項的**實際範圍以線框第 03 節為準，不以本表為準**。
    ⚠️ 且線框自己標明 **Q6（批次分析掛 ② 還是 ③）尚待客戶拍板** —— 2.1-③ 的定義**還沒定案**。

14. **本輪同樣只讀 `CLAUDE.md` 的相關節次、`docs/wireframes/fund-wireframe-final.html` 第 02 與 05/06 節、
    `STATE.md` 的標題列，以及上列指令的輸出。**
    **未通讀** `PROCESS.md` / `SPEC.md` / `ARCHITECTURE.md` / `BACKLOG.md` / 其餘 `docs/*.md`
    （同 v1 盲點 2，狀態一字未變）。若那些檔另有與本表牴觸的敘述，本次**沒查到**。

15. ⭐ **一個本輪差點踩到的順序陷阱，記下來防重犯**：
    `d6f5f87`（**PR #726**）是 main 上**最新**的 commit，但它的 **PR 編號比 #730~#733 都小** ——
    因為它是舊分支被壓著、最後才放行的。
    **若用 PR 編號排時序，會得出「#726 在 #733 之前」的錯誤結論**，
    進而把「Phase 2 備料已合併」誤讀成「它在 Phase 1 完成前就偷跑了」。
    **一律以 `git log` 的順序為準，PR 編號只是識別碼、不是時間軸。**

16. **本輪同樣是單組產出，未經第二組獨立複驗**（`§-2` 規則 6）。
    本輪的全稱句**逐條列出，不得被引用為已查證的事實**：
    - 「v2 之後 main 只前進了這 4 個 commit」（`git log` 單點可自驗，**風險低**）；
    - 「origin 上只有 3 條分支」（單次 `ls-remote`，**會隨時漂移**）；
    - 「`services/` 的違規只有 45 項」—— ⚠️ **這句連 PR #733 自己都不宣稱**
      （其原文：「`services/` 沒有第 N+1 處」這句話本組沒有查證，也不宣稱）；
    - 「本輪的技術債只有附錄 D 那 4 項」（**單組登記，未查證**）；
    - 「UI 層已無外部直抓」—— ⛔ **本檔明確不作此宣稱**，見 1.2 的範圍收窄聲明。

17. ⭐ **本輪最該記住的方法缺陷：本組的「舊敘述有沒有被保留」驗證法本身漏了一處。**
    **本組 v3 的查法**：把每個被刪行的內容拿去新檔搜尋，看**字元有沒有消失**。
    它避開了「逐行 grep 會被跨行刪除線騙過」那個坑（**這部分是對的**），
    **但它驗不出「表述被換掉、而字元剛好在別處出現」這一種**。
    **實際漏掉的那一處**：`~~PR 描述本身~~ → PR 描述與 API 回傳` ——
    「本身」兩字在檔內別處巧合出現，本組的檢查因此通過，
    **但「取自 PR 描述本身」這個表述在新檔中確實不存在，且當時沒有加刪除線。**
    **稽核用「貪婪最長區塊切分」重驗才抓到**（37 行中 36 行斷點落在插入新狀態處、內容完整；此為唯一例外）。
    ⭐ **教訓（比那一行本身重要得多）**：
    **「檢查字元有沒有不見」驗不出「說法有沒有被換掉」。**
    要驗「舊敘述是否保留」，必須驗的是**連續表述是否可在新檔中被完整對上**，
    不是**字元的存在性**。**本檔 v2 盲點 7 說「要把同一把尺對全檔重跑」——
    這一次的問題是那把尺本身量錯了東西。**
    ⚠️ 且本組把那句 **36/37 的結論當成 37/37 寫進了 PR 描述**（未經第二組驗證的全稱句，
    `§-2` 規則 6 點名的形態）。**已於本輪更正；檔案端加線保留。**

    **本輪換上的驗證法（兩階段，寫下來供後人直接重跑）**：
    - **階段 1**：以「**在新檔中只出現一次**」的最長子字串當**唯一錨**定出視窗，
      在視窗內跑 `difflib.SequenceMatcher.get_opcodes()`；
      **只有 `equal` / `insert` → 舊表述完整保留**；出現 `delete` / `replace` → 進階段 2 複查。
    - **階段 2**：把該行**定位到它所在的那一列／那一段**，做**字元順序子序列**檢查
      （舊行每個字元都能在新行中依序找到 ＝ 純插入）。
    - ⚠️ **兩階段缺一不可，這本身就是本輪的教訓**：
      **只用階段 1** 會在「重複字元多的表格列」上誤報（`| B4 |` 那列就是，實際是純插入）；
      **只用階段 2** 太寬鬆（字元散落各處也會通過）——**那正是 v3 原本那個查法的毛病**。
    - **實跑結果（本輪修正後 vs v2 基線 `origin/main`）**：被刪 **36** 行 →
      完全原封 14 ／ 純插入 22 ／ **違規 0**，即 **36/36**。
      ⚠️ **行數由 37 變 36 不是漏算**：本輪把 `Phase 1 總項` 那一行**原封還原**（退回 `- [ ]`），
      它因此**不再是被刪的行**。
    - **對照組（v3 修正前 `b30f0a0`）**：37 行 → **違規 3**，其中一項正是稽核抓到的「本身」。
      ⚠️ **據實記一筆差異**：本組的工具在對照組上 flag **3** 項，稽核回報 **1** 項；
      另外 2 項是 `- [ ]` → `- [x]` 的**打勾動作本身**（`Phase 1 總項` 與 `1.3` 的標題行）。
      **稽核把打勾視為正常的狀態變更、不算「敘述被刪」，本組的工具則一律 flag** ——
      **兩種讀法都說得通，本組不主張自己那個比較對**；兩項都已因修正二而消失。

18. ⭐ **修正輪自己又漏了一類，而且是兩邊的驗證器都看不到的那一類。**
    修正二把 `Phase 1 總項` 退勾之後，**上方那句「→ Phase 1 總項可勾」被留在原地、沒有加線** ——
    同一份檔案於是同時寫著「可勾」與「`- [ ]`」。**由第二輪獨立稽核抓到並就地更正**（見上方該處）。
    ⭐ **為什麼兩邊都沒抓到（這才是要記的）**：**本組的兩階段法與稽核的貪婪切塊法，
    檢查的都是「被刪的行有沒有被保留」** —— 而這一處**沒有任何行被刪**，
    它是**一句原封不動、但因為別處狀態改變而變成假的活句**。
    **「有沒有東西被刪掉」與「留下來的東西還是不是真的」是兩個不同的問題，
    只驗前者的工具，結構上永遠看不到後者。**
    **往後改任何狀態（打勾／退勾／結案）時，除了驗被刪行，還要回頭掃「所有仍然斷言舊狀態的活句」。**
