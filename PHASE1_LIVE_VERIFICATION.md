# 實機驗證紀錄（部署站台）

> 驗證時間：2026-08-14 08:28 TW ｜ 站台：https://my-fund-dashboard-ca8spcltzv6rvgo575fkgp.streamlit.app/
> 方法：實際點擊操作 + 逐段截圖。狀態：未登入 Google、未載入任何基金（部分判定因此無法取得畫面證據，已誠實標註）。

---

## A. 實機確認為真（有畫面證據）

| # | 缺失 | 畫面證據 |
|---|---|---|
| 1 | **總經載入耗時過長** | 畫面自己顯示 `✅ 已抓取 28 個指標！（08:28 TW｜**34259ms**）` — **34.3 秒**。這不是估算，是 app 自己量的 |
| 2 | **Tab④ 分頁名與頁面標題不一致**（🆕 報告未提） | 分頁列寫「📊 配置 & 帳本」，頁面 H1 寫「**組合基金管理**」。麵包屑第 4 站也寫「配置 & 帳本」→ 同一頁三個名字有兩種 |
| 3 | **Tab④ 兩顆「🔐 用 Google 登入」** | 「雲端讀取」面板一顆、下方面板一顆，外觀完全相同 |
| 4 | **Tab④ 兩處「請至左側 sidebar 登入」文案，而按鈕就在正下方** | 「⚠️ 尚未用 Google 登入。請至左側 sidebar 點「🔐 用 Google 登入」。」下一行就是該按鈕 |
| 5 | **健診 slider「吃本金閾值 %」存在且有 help icon** | 值 2.00，右側有 ❓；旁邊同時有「本金（TWD）1000000.00」`number_input` |
| 6 | **健診 caption 寫死「對 100 萬 TWD 為基準」** | 與可調的本金輸入框**同屏並存**，直接矛盾 |
| 7 | **Tab5 完整率是存在性檢查** | ⓪ 表 Tab1 列：狀態 🟢、覆蓋率 **`16/16 指標`**、細項 **`FRED 12 / Yahoo 4 ｜ 缺值:SLOOS 放貸標準（SLOOS）`** → **綠燈與「缺值」並存於同一列** |
| 8 | **Tab5 ① FRED 列三個互斥數字** | 用途欄「美國總經 **14+** 指標」／端點欄「**16 條** series」／狀態欄「已抓 **12 / 12**」 |
| 9 | **Tab5 ① 端點字串** | 畫面顯示 `yp401000.djhtm (wb01) / yp405000.djhtm (wb05) / yp407000.djhtm (wb07)`，而 repo 內真實路徑是 `/w/wb/wb01.djhtm`、`/yp/wb05.djhtm`、`/yp/wb07.djhtm`（這三個 `yp40*` 檔名全 repo 零命中） |
| 10 | **熱錢資料嚴重過期** | 「tw 外資買賣超 × USDTWD 同步判讀（目前資料 **140 天前**）」 |
| 11 | **Tab1 資訊過載** | 一頁含：總表 4 層 + 長期座標 7 卡 + 中期循環 **18 張 Z-Score 卡** + 短線雷達 10 卡 + 拐點警報 3 儀表 + 3 卡 + 即時訊號決策矩陣 + 2 個 ARCHIVED expander |
| 12 | **sector_rotation 量綱錯誤** | 雷達卡「防禦 vs 攻擊」值 **-0.84**（負數）→ 證實是百分點差，不是程式碼裡畫線用的比值 1.00/1.20 |
| 13 | **側邊欄資訊架構倒置** | 由上到下：品牌 → 版本晶片 → **版本 beacon（第 2 次）** → 金鑰 → 測試 Proxy → 強制同步 GitHub → 資料健康 → 全域刷新 →（登入在更下方）。`Fetcher v6.24` 硬編字串也在畫面上 |

---

## B. 實機推翻／需降級的判定

| # | 報告原判 | 實機結果 | 處置 |
|---|---|---|---|
| 1 | **PCR 錯誤紅線（1.5 vs 1.2）真的畫在畫面上，與燈號互相打架** | 雷達卡「Put/Call 比率」顯示 **`無資料` / 值 `—`**，備註「Put/Call 全源失敗｜Yahoo ^CPC: empty(len=0)｜Yahoo ^CPCE: empty(len=0)｜stooq ^cpc: 無法解析」→ **trend 為空，警戒線根本畫不出來** | 🔽 **降級**：從「畫面矛盾」降為「資料源全掛 + 潛在錯線」。真正該修的變成**「PCR 全源失敗」本身** |
| 2 | **VIX 25 vs SSOT 22 是 v19.157 統一時的漏網** | 總表 ② 依據明寫「VIX 恐慌指數 門檻:**≥22 警戒 / ≥30 危機**」；**且 app 自己有一行 caption**：「下方各段圖上的警戒線對齊該段自己的燈號分級，與本表門檻可能略有差異(**例: 🎯 短線雷達的 VIX 線比本表的警戒門檻高一些**)—— 以本表的判讀欄為準」 | 🔽 **大幅降級**：這是**已知且已對使用者揭露**的差異，不是漏網。<br>⚠️ 但 caption 的理由「對齊該段自己的燈號分級」**仍不成立** —— `risk_radar.py:109-111` 判燈也是用 22，所以雷達的「線」(25) 與雷達自己的「燈」(22) 才是真正對不上的那一對 |
| 3 | **Tab5 ① 五列燈號共用 `_fund_n`，MoneyDJ 一次成功就全亮綠燈「已抓 3」** | 未載入基金時，這幾列正確顯示 ⬜「由 fetcher fallback 鏈內部呼叫」/「僅安聯/安達標的觸發」/「尚未使用」 | ⚠️ **本次無法證實**。邏輯上 `_fund_n > 0` 時才會誤綠，需載入基金後複驗 |

---

## C. 重複功能盤點（實機）

### C1 🔴 批次分析 vs 組合健診 —— **官方自承同一張表**

批次分析頁面說明第一句：
> 「上傳或貼上基金代號清單 → 每檔跑與**「組合健診」同一張大表**（評分/報酬/風險/配息/σ位階/買賣點，以 100 萬 TWD 為基準）→ 下載 CSV。」

| | 組合健診 | 批次分析 |
|---|---|---|
| 輸入 | `text_area` 基金代號（最多 10 檔） | `file_uploader` + `text_area`（無上限） |
| 本金 | `number_input` 可調 | **寫死 100 萬，無欄位** |
| 引擎 | `process_one_fund`（4-worker 並行） | `process_one_fund`（**序列**） |
| 輸出 | 14 張表 / ~149 欄 | 1 張表 / **75 欄** |
| 說明 | 「對 100 萬 TWD 為基準」 | 「與組合健診同一張大表」 |

**差別只有規模與並行度，計算引擎完全相同。**

### C2 🟠 總經載入相關按鈕散在 4 處

| # | 位置 | 標籤 | 實際行為 |
|---|---|---|---|
| 1 | 市場定調 頂部 | `📡 更新總經資料` | 主載入 |
| 2 | 市場定調 頂部右 | `🆕 強制重抓最新（清快取）` | 清快取 + 走 #1 |
| 3 | 市場定調 → 長期座標 → Raw data expander | `🔄 強制重抓` | **與 #2 呼叫同一個 `clear_tab1_macro_caches`** |
| 4 | **參考/診斷 → 資料診斷 頂部右** | `🔁 重新載入總經` | 又一個入口 |

另有 `🌊 載入流動性壓力預警引擎`（市場定調 → 短線雷達區）—— 但它旁邊寫明「**為進階觀察，獨立抓取以免拖慢總經主載入**」→ **有正當理由，不算冗餘，建議保留**。

### C3 🟠 Tab④ 登入入口重複
2 顆「🔐 用 Google 登入」按鈕 + 2 處「請至左側 sidebar 登入」文案（而按鈕就在正下方）+ sidebar 本身 1 顆 = **同一件事 5 個觸點**。

### C4 ✅ 已做對的簡化（可作為範本）
市場定調頁面有這段：
> 「原本另外一區的『**總經指南針**』已移除（它三張卡在這裡都有，而且不必再按一次按鈕）；VIX → 本區「VIX 恐慌指數」；美 10Y 殖利率 → 本區「10Y 殖利率衝擊」；S&P 500 vs 60MA → 本區「SPX 均線破口」」

**這正是本次簡化提案該遵循的模式**：移除重複區塊 + 在原地留一句「原本的東西搬到哪去了」的指路。

---

## D. 未能實機驗證的項目（誠實標註）

| 項目 | 原因 | 建議 |
|---|---|---|
| RWD 手機版面 | `resize_window` 對 Streamlit 渲染無效，截圖尺寸固定 1568×698 | 請你本機開 F12 → 裝置模擬（iPhone 390px）自行確認 `tab2:1131` 的 4 欄 grid 與 `tab5` 6 處 CSS grid |
| Tab5 ① 燈號誤綠 | 需先載入基金（`_fund_n > 0`） | 載入 1 檔基金後回頭看 Section ① 的 TDCC/Fundclear/cnyes 三列是否亮綠 |
| 個基深掘、我的管理室、T7 帳本 | 需輸入基金代號 / Google 登入 | 未執行（避免動到你的真實資料） |
| 死控制項（T7 目標單位數） | 需登入 + 建立持倉 | 程式碼證據已足夠（`t7:1091` 無條件覆寫） |
| `nav_history` 寫入、LINE 通報、重置帳本 | **刻意不點** —— 破壞性 / 不可逆 / 會送出訊息 | — |

---

## E. 簡化提案（只動選項與功能，不動資料內容）

### E1 分頁層級

| 提案 | 現況 | 建議 | 理由 |
|---|---|---|---|
| **批次分析併入組合健診** | 2 個獨立分頁，同一引擎同一張表 | 組合健診頁頂加 `st.radio(["深度（≤10 檔）", "批次（大量掃描）"])`，7 分頁 → **6 分頁** | 官方自承同一張表；使用者現在要自己判斷「我該去哪個分頁」。合併後本金輸入框也能一併套用到批次（順手修掉批次寫死 100 萬） |
| **Tab④ 標題正名** | 分頁「配置 & 帳本」vs H1「組合基金管理」 | H1 改吃 `story_nav.tab_label("portfolio")` | 同一頁兩個名字 |

> ⚠️ 這一項牽涉「去重 ≠ 合併資料」的紅線。**合併的是「入口」不是「資料」** —— 兩邊的欄位、計算、輸出全部保留，只是不再是兩個分頁。若你認為批次的「400 檔淺掃」心智模型必須獨立，這條可以只做「在兩頁互相加一行指路 caption」的輕量版。

### E2 控制項瘦身

| # | 現況 | 建議 | 減少 |
|---|---|---|---|
| 1 | 總經載入 4 個入口 | 保留 #1 `更新總經資料` + #2 `強制重抓`；#3 改成 caption 指路、#4 改成 caption 指路 | −2 顆 |
| 2 | Tab④ 3 顆「📡 載入」（同一函式） | 保留「加入與管理基金」區那顆，其餘改 caption | −2 顆 |
| 3 | Tab④ 2 顆登入鈕 + 2 處 sidebar 指路 | 留 1 顆按鈕，刪掉 2 處「請至左側 sidebar」文案 | −1 顆 −2 段文案 |
| 4 | T7 A/B/C 三分頁各一組「落帳目標 + 方案名稱」 | 抽成共用 helper，三處共用（順便補齊只有 A 有的 help） | −4 個重複控制項 |
| 5 | sidebar 版本顯示 2 次 | 合成一行 | −1 區塊 |
| 6 | sidebar `♻️ 強制同步 GitHub` | 雲端實質無效（`PROCESS.md:65` 已記載），改成 caption 顯示版本比對結果 | −1 顆 |
| 7 | sidebar `🔍 測試 Proxy` + 金鑰狀態 | 收進「🔧 開發者工具」expander | 第一屏 −2 區塊 |

**合計：主畫面可減少約 10 個控制項，且不刪任何一項資料。**

### E3 版面收納（第一屏優先）

| Tab | 建議 |
|---|---|
| 市場定調 | 總表 ①②③④ 維持全展開；**詳細區四時域改 `st.tabs`**（長期／中期／短線／拐點）→ 解掉開發者自己記錄的「從頂捲到底需 60+ 次滾輪」 |
| 組合健診 / 批次分析 | 加「精簡（結論）／完整（全欄）」切換；精簡模式 6 欄 |
| 資料診斷 | 依它自己 `st.info` 的建議把 **⓪ 與 ⑥ 移到最上方**，其餘收進「🔧 維運診斷」expander |
| sidebar | 依 E2-#7 重排，登入 + 工作中帳本上移第一屏 |

---

---

# 第二輪深挖：實際跑一次組合健診

**測試條件**：代號 `ACCP138` + `ACUSI23`，**本金刻意設為 500,000**（非預設 100 萬），吃本金閾值維持 2.00。

## F. 實測結果

### F1 🔴 效能實測 — 批次分析的「5s/檔」估算嚴重低估

| 觀測點 | 數值 |
|---|---|
| 2 檔健診總耗時 | **約 90 秒** |
| 進度「已完成 1/2 檔」出現時間 | 約 50 秒 |
| 推得單檔成本 | **約 45 秒/檔** |
| 批次分析頁宣稱 | 「400 檔約 **30~40 分鐘**」（源自 `tab_batch:222` 的 `len(codes)*5/60`） |
| **按實測外推** | 400 × 45s ≈ **5 小時** |

批次頁的估算與實測差了 **7.5 倍**。這也代表報告裡「並行化必須配套限流」的判斷更重要 —— 現況序列跑就是限流器。

### F2 ✅ 抓取失敗的揭露 — 健診端做得非常好

ACUSI23 抓取失敗，畫面顯示：

> ❌ **有 1 / 2 檔抓取失敗，未納入下方所有統計與表格：**
> ACUSI23：⚠️ 淨值歷史抓取失敗（MoneyDJ 可能封鎖伺服器 IP）但已取得部分績效/風險數據，可繼續查看。💡 建議：直接貼 MoneyDJ 完整網址以取得最佳結果

且 KPI「檢查檔數」顯示 **1** 並帶 `↓ -1` delta。

**這正是報告 G1 說的「部分失敗」情境，而健診端處理得完全正確。**
**對照組**：同一個引擎在批次分析端會把這種情況標成 `✅ 成功`（`unified.py:436`）。→ **兩端處理不一致，實證成立。**

### F3 🟠 大表欄位大量 None，但該列無任何標記

ACCP138 實測列：

| 欄位 | 值 |
|---|---|
| 4D Grade / 4D Score | C / 55.0 |
| Sharpe | 0.58 |
| **Sortino / Calmar** | **None / None** |
| 真實收益 % | −8.98 % |
| **費用率 % / Max DD %** | **None / None** |
| **3Y 年化 % / 5Y 年化 %** | **None / None** |
| 1Y 含息 % | 0.97 % |
| 1Y 來源 | 自算含息 |
| 年化配息率 | 10.16 |

**7 個欄位是 None，但該列沒有任何「部分計算失敗」提示** —— 使用者無從分辨「這檔沒有這些資料」與「算失敗了」。

### F4 ⚠️ slider 死控制項 —— 本案例無法用行為證明（誠實標註）

ACCP138 的缺口 = 年化配息率 10.16% − 1Y含息 0.97% = **9.19 pp**。
而 slider 範圍是 **0.50 ~ 5.00** —— **9.19 超出滑桿最大值**，所以無論拉到哪都會判吃本金。

→ **這個標的無法用行為驗證 slider 是否接線。** 程式碼證據（`div_health_light_🧮` production 0 consumer）仍成立，但實機證明需要找一檔缺口落在 0.5~5.0 之間的基金。

### F5 ⚠️ 本金 500,000 —— 部分位置正確使用了

核心/衛星區 caption 實際印出：
> 「（僅 1 檔（**500,000 TWD**），加權方式不影響比例；核心=種穩長線 / 衛星=主題追報酬 / 待定=分類不足）」

→ **這處正確吃了使用者輸入的本金**。報告原本說「checkup 寫死 1M」需要收窄範圍。
**但**頁面頂端 caption 仍寫死「對 **100 萬 TWD** 為基準」，與正下方 `500000.00` 的輸入框同屏矛盾 ✅ 成立。
「換匯資訊 🧮」欄因表格橫向捲動在自動化環境下無法操作，**未能驗證**。

### F6 🟡 同頁兩個結論容易被誤讀

- KPI：**🔴 吃本金 1**
- 換標決策建議：**「目前無 🔴 紅燈(賣出/平轉)檔 —— 沒有急需換掉的標的」**

畫面有解釋（「『策略燈號』紅燈條件 = 嚴重吃本金 或 (1Y含息<0 且 Sharpe<0)；故一般 🔴 吃本金(報酬仍正)不一定是策略紅燈，兩種語意不同」），邏輯上正確（ACCP138 1Y含息 0.97%>0、Sharpe 0.58>0）。
→ **不是 bug，但對新手是明確的困惑點**：「它說我在吃本金，又說沒有急需換掉的」。建議在 KPI 卡加一行連結指向該解釋。

### F7 ✅ 更多正面設計（重構時勿誤刪）

| 位置 | 設計 |
|---|---|
| Sharpe 來源欄自動收合 | 「本批基金的 Sharpe 全部來自同一個地方：『MoneyDJ 官方 1年』—— 整欄逐列都一樣、沒有資訊量，所以先收起來；只要出現期間不一致的情況，這一欄會自動回到表內逐列顯示」 |
| 持股新聞 / 微觀防護盾 | **改為逐基金 expander 按需抓取**，並在 UI 說明理由（「N 檔 × 6 大持股 ≈ 60+ API call」「N 檔 × 10 持股 = 100+ API」避免 timeout） |
| AI 白話總體檢 | **按鈕觸發**，不是預先算 → **與 Tab④ 的 AI snapshot 預先全算形成強烈對比。健診端做對了，Tab④ 沒有** |
| 選基金區的尺一致性 | 「與健診大表『基期』欄同一把尺：同一套算法、同一組門檻（買點 -1.5σ / 賣點 -0.5σ）、同一段回看期間」 |

---

## G. 深挖後的判定更新

| 項目 | 更新 |
|---|---|
| 批次「5s/檔」估算 | 🔺 **升級**：實測 45s/檔，差 7.5 倍。400 檔約 5 小時不是 30-40 分 |
| 批次「部分失敗標✅成功」 | 🔺 **強化**：健診端同一情境有完整揭露，證明批次端是刻意/疏忽的落差，不是引擎限制 |
| 健診 slider 死控制項 | ⚠️ **維持程式碼判定，實機未證**（本測試標的缺口 9.19pp 超出滑桿上限） |
| checkup 本金寫死 1M | 🔽 **收窄**：核心/衛星區已正確使用 500,000。僅頂端 caption 與「換匯資訊」欄仍待確認 |
| Tab④ AI snapshot 預先全算 | 🔺 **強化**：健診端的 AI 是按鈕觸發，證明「lazy 才是本專案的既定作法」，Tab④ 是例外 |

---

---

# 第三輪深挖：個基深掘 + 連線穩定性

## H. 新發現

### H1 🔴 生產環境 WebSocket 中斷 —— 畫面凍結 2.5 分鐘以上

**重現條件**：組合健診跑完（1 檔成功、1 檔失敗）後，切到「📖 參考 / 診斷」分頁。

Console 實錄（08:59:02）：
```
[ERROR] (index.D7cltBCg.js:64:2011)  Client Error: WebSocket onerror
```

**後果**：頁面完全無回應，連續 4 次截圖（每次 30 秒逾時）全部失敗，累計 **超過 150 秒**畫面凍結。Tab 仍存活但 renderer 無回應，最終需重載頁面才恢復。

這與報告 §G5 的預測完全一致 —— Tab5 在每次 rerun 都會跑：
- `backtest_sub_cycle_lights(window=60)` 7 子領域 × 60 月 expanding window（**無 cache**，且在收合的 expander 內）
- `nav_history_gs.status()` + `coverage_status()` 兩次 Google Sheets 網路讀取（**無 cache、無 expander、無條件執行**）
- `_update_data_registry()` 對每個 indicator + 每檔基金全量 `sort_index()` 並把**排序後完整 Series** 塞進 session_state

載入基金後 `_register_fund_subdata` 會再往 registry 加入多筆帶 Series 的條目 → payload 變大 → WebSocket 斷線。

> ⚠️ **誠實標註**：第二次凍結（個基深掘展開 expander，全新 session）console **沒有** WebSocket 錯誤，只有 `INITIAL -> (5, 0, ) -> RUNNING`，代表 script 正在跑。那一次比較可能是截圖機制與渲染競爭，**不能算 app bug**。
> **只有第一次（切參考/診斷）是有 WebSocket onerror 佐證的真實中斷。**

### H2 🔴 搜尋 → 分析斷點 —— 完整證實，且比報告描述更糟

實測：搜「安聯」→ 結果如下

```
✅ 找到 242 檔基金
選擇基金（242 筆）  [下拉選單：安聯印度精選基金-AT累積類股(美元) | AGIF-FF1]
💡 代碼：AGIF-FF1 → 在上方輸入框貼入代碼即可分析
```

**選好基金之後，畫面只給一行文字叫使用者自己讀出代碼、捲回頁首、手動貼上、再按分析。整個區塊沒有任何按鈕。**

附帶問題：242 筆結果塞進單一 `selectbox`。

### H3 🟠 第二處「分頁名 vs 頁面標題」不一致

| 分頁列 | 頁面 H1 |
|---|---|
| 📊 配置 & 帳本 | **組合基金管理** |
| 🔍 個基深掘 | **單一基金深度分析** |

`story_nav.tab_label()` SSOT 只套用在分頁列與麵包屑，**頁面標題各寫各的**。

### H4 🔴 ACCP138 只有 30 筆淨值 / 跨度 42 天，但全站照算

個基深掘實測顯示：
> 「淨值 **30 筆** · 配息 59 筆 · 來源:`tcb_moneydj` · **跨度 42 天(≈0.1 年)**」

**這解釋了組合健診大表為什麼 Sortino / Calmar / 費用率 / Max DD / 3Y年化 / 5Y年化 全部是 `None`** —— 不是計算失敗，是序列真的只有 42 天。

**但這也讓 F3 的判定需要修正為更精確的版本**：

| 頁面 | 有沒有揭露序列長度？ |
|---|---|
| 🔍 個基深掘 | ✅ **有** —— 頂端就寫「淨值 30 筆 · 跨度 42 天(≈0.1 年)」 |
| 💊 組合健診大表 | ❌ **沒有** —— 7 個欄位顯示 `None`，但整列沒有任何「序列僅 42 天」的提示 |

→ **同一檔基金、同一組資料，個基深掘誠實揭露、健診大表沒有。** 使用者在健診大表看到一排 `None`，只會讀成「這檔沒有這些資料」，而不是「這檔的歷史只有 42 天，算不出 3Y/5Y」。

而該檔的 **1Y 含息 0.97%** 與 **年化配息率 10.16%** 都是用這條 42 天序列推出來的 —— 也就是說「吃本金」判定建立在 0.1 年的樣本上。

### H5 ✅ 又一個正面設計

> ⬜ **取不到最新淨值日期** —— 無法判斷下方訊號用的是幾號的價格。資料來源沒有回傳淨值日欄位，請把下方結論當作參考而非即時判斷。

這句話**主動告訴使用者「下面的訊號不能當即時判斷用」**，是 §1 Fail Loud 的正確示範。建議把同樣的揭露複製到健診大表。

### H6 🟡 重載頁面 = 全部重來

重載後：總經「未載入」、組合健診結果全消失、側邊欄「尚未載入任何資料」。
與 T7 帳本自述的「帳本僅存於記憶體，重整頁面即消失」一致。對照 **總經載入要 34 秒、健診 1 檔要 45 秒**，重載成本很高。

---

## I. 判定總更新（累計三輪）

| 項目 | 最終狀態 |
|---|---|
| 批次「5s/檔」估算 | 🔺 **實測 45s/檔**，400 檔約 5 小時（宣稱 30-40 分） |
| 批次「部分失敗標 ✅ 成功」 | 🔺 健診端同情境有完整揭露 → 落差證實 |
| 搜尋 → 分析斷點 | 🔺 **完整證實且更糟**（242 筆 + 零按鈕） |
| Tab5 每 rerun 重運算 | 🔺 **升級為生產事故**：WebSocket 中斷、凍結 150 秒+ |
| 健診大表欄位留白 | 🔄 **修正**：根因是序列僅 42 天（不是算失敗），但**健診大表未揭露序列長度、個基深掘有** |
| 分頁名不一致 | 🔺 從 1 處增為 **2 處** |
| PCR 錯線 | 🔽 全源失敗，線畫不出來 |
| VIX 25 vs 22 | 🔽 app 已有 caption 揭露差異 |
| 健診 slider 死控制項 | ⚠️ 程式碼證據成立，**實機仍未證**（需缺口落在 0.5~5.0 的標的） |
| checkup 本金寫死 1M | 🔽 **收窄**：核心/衛星區正確用 500,000；僅頂端 caption 寫死 |
| Tab5 ① 燈號誤綠 | ⚠️ **仍未證** —— 兩次嘗試都遇上凍結 |

---

---

# 第四輪：載入真實 25 檔持倉後（user 自行登入 + 載入）

## J. 🔴🔴 兩條全新的、等級最高的發現

### J1 🔴🔴 台幣基金被判定為 USD → 單位數與金額差 32 倍

**證據（配置 & 帳本 → 基金體檢表，逐列比對確認代號↔列對應無誤）**

| 代號 | 基金名 | 計價幣別 | NAV(原幣) | 即時匯率(FX) | 可申購單位數 | 原幣本金 |
|---|---|---|---|---|---|---|
| ACDD01 | 安聯台灣大壩基金-A累積型**(台幣)** | **USD** ❌ | 324.8500 | 32.1120 | **95.86** | 9,183.64 |
| ACDD19 | 安聯台灣智慧基金 | **USD** ❌ | 418.0300 | 32.1120 | **74.49** | 4,057.14 |

**這兩檔是台幣計價的台股基金**（基金名自己就寫著「(台幣)」）。NAV 324.85 / 418.03 是合理的**台幣**淨值；當成 USD 等於一單位價值 10,430 台幣。

**量化誤差**：
- ACDD01 本金約 294,904 TWD → 系統算「原幣本金」= 294,904 ÷ 32.112 = **9,183.64**（把台幣本金當美元）
- 可申購單位數 = 9,183.64 ÷ 324.85 = **95.86**
- **正確值應為** 294,904 ÷ 324.85 = **907.8 單位**
- → **低估 32.1 倍**

**汙染範圍**：可申購單位數 / 原幣本金 / 月配息 / 年配息 / 組合市值 / FX 曝險摘要。

**連帶的第二個矛盾**：頁面頂端「FX 曝險摘要（1 種幣別）」寫
> 「**USD 25 檔（100%）**· 1 USD ≈ 32.11 TWD」
> 「⚠️ 組合 **100% 為 USD 計價**，台幣大幅升值時 TWD 績效將明顯縮水。」

—— 但同一頁的表格裡就有兩檔台幣基金。**這個「100% USD」的風險警語本身是錯的。**

> 📌 對照報告 §Tab2 已記載的：「幣別偵測」欄 v19.59 後 fallback 分支不可達、**永遠顯示「自動」**的死欄位。現在證實**自動偵測會判錯**，而那個本該提示的欄位是死的。

### J2 🔴 兩檔台幣基金的 1Y 含息報酬顯示 +201.65% / +190.64%

| 代號 | 基金名 | 1Y 含息報酬 | 年化配息率 | Coverage |
|---|---|---|---|---|
| ACDD01 | 安聯台灣大壩基金-A累積型(台幣) | **201.65%** | None | None |
| ACDD19 | 安聯台灣智慧基金 | **190.64%** | None | None |

台股基金一年 +200% 不合理。這兩檔同時是 J1 幣別誤判的那兩檔 —— 高度懷疑同源（NAV 序列尺度混用，或報酬計算的基期取錯）。

兩檔的燈號都標「⬜ 不適用」，所以**沒有進入吃本金判定**，但**數字仍完整顯示在大表上**，且會被「AI 白話總體檢」讀走。

`CLAUDE.md §3.2` 的合理性檢查表**沒有「基金 1Y 報酬」的上下界**（只有 NAV>0）。建議補一條 sanity gate。

### J3 🔴 第一屏 KPI 自相矛盾：「組合基金數 8 檔」vs「核心 17 檔 / 衛星 8 檔」

配置 & 帳本 頂端「組合健康儀表」六格：

| KPI | 值 |
|---|---|
| 組合基金數 | **8 檔** |
| 核心/衛星檔數 | **核心 17 檔 / 衛星 8 檔**（= 25） |
| 現金流安全 | 4/**8** 檔（↓ 4 吃本金） |
| 撿便宜雷達 | 3 檔 |
| 留校查看 | 3 檔（↓ -3） |
| 停利提醒 | 0 檔 |
| FX 曝險 | USD **25** 檔（100%） |
| sidebar 全局資料健康 | 組合 **25** 檔 |

**同一屏上「幾檔」同時是 8 和 25**，且兩者並排。合理推測：8 = unique `fund_code`、25 = `(policy_id, fund_code)` 複合鍵列數（同一檔基金分佈在多張保單）。

但**畫面上沒有任何一句話解釋這個差異**。新手看到「組合基金數 8 檔」正下方寫「核心 17 檔 / 衛星 8 檔」只會覺得算錯了。

## K. 其他實測數據（真實 25 檔）

| 觀測 | 值 |
|---|---|
| ACTI71 聯博多元資產收益組合基金AI配息 | 🔴 **嚴重吃本金(報酬為負)**：1Y 含息 **−38.18%**、年化配息率 9.73%、Coverage **−3.92**、缺口 47.9pp |
| ACTI71 TER | ⬜ **缺 mgmt_fee 資料** |
| 體檢表整體 | 🏆 0 檔優等生 · ⚠️ 1 檔汰弱候選 · ⬜ **5 檔同類資料不足**（共 8 檔）→ **62.5% 無法 PK** |
| sidebar 資料健康 | 組合 25 檔 · 🟢0 🟠0 🔴0 **⬜25** → **全部未知** |
| 「每月100萬配息(TWD)」欄 | ✅ **是刻意的橫向比較口徑**（caption 明講「用同口徑估現金流量化能力，方便橫向 PK」），與「月配息(本金TWD)」並列。**不是寫死 bug，撤回該項質疑** |

> ⚠️ ACTI71 的 −38% 正是 `STATE.md` 記載的「抓不到官方資料 → 外推 → 假吃本金（ACTI71 −38%）」案例，而它**仍顯示在畫面上**。此標的屬 `CLAUDE.md` 明列的**凍結區**，不可自行開挖，僅記錄現況。

## L. 判定更新

| 項目 | 更新 |
|---|---|
| **幣別誤判 32 倍** | 🆕 **全新，最高優先** |
| **1Y 報酬 +201% 無 sanity gate** | 🆕 **全新** |
| **KPI 8 vs 25 矛盾** | 🆕 **全新** |
| checkup「1M TWD」 | ✅ **撤回** —— 「每月100萬配息(TWD)」是刻意的 PK 口徑，caption 有解釋 |
| 健診 slider | ⚠️ **仍無法測** —— 25 檔的缺口分別是 9.19 / 8.46 / 6.16 / −3.02 pp，**沒有一檔落在滑桿範圍 0.5~5.0 內** |

---

---

# M. 幣別誤判 —— 完整根因鏈（已追到單行）

## M1 守門設在錯的層

專案**確實**有防「矇 USD」的設計，而且寫得很好 —— 但**全部設在下游，防不住上游注入**。

**下游守得很嚴（兩處都正確，不需要改）**：

| 位置 | 程式碼 | 行為 |
|---|---|---|
| `services/fund_row.py:81` | `normalize_ccy(fd.get("currency"), default="")` | 顯式覆寫掉 USD 預設 |
| `services/fund_row.py:90-94` | `if not ccy: return {"ok": False, "error": "幣別未知（MoneyDJ wb05 未提供「計價幣別」欄）"}` | **抓不到直接判失敗，不猜** |
| `ui/helpers/fund/checkup.py:328` | `_norm_ccy(_ccy_raw, default="", mode="yf") if _ccy_raw else ""` | 同樣 `default=""` |
| `ui/helpers/fund/checkup.py:365` | `"計價幣別": _ccy or "—"` | 不知道就顯示 `—` |

`ui/helpers/fund_grp_health/columns.py:42-43` 的 help 因此是**準確的**：
> 「抓不到 → 這一檔整列標失敗，**不會**猜成美元。」

## M2 🔴 但上游把 USD 寫進去了 —— `ui/helpers/v2_editor.py:85`

從 Google Sheet 政策表建 fund dict 的地方，**三行相鄰的程式碼，兩行誠實、一行編值**：

```python
84    "fund_name":  str(r.get("fund_name", "") or ""),      # ← 空就空
85    "currency":   str(r.get("currency", "") or "USD"),    # ← 🔴 空就編一個 USD
86    "tier":       str(r.get("tier", "") or ""),           # ← 空就空
```

**Sheet 的 `currency` 欄一旦空白，這裡就無中生有一個 "USD"。** 之後所有下游守門全部失效 —— 因為它們檢查的是「有沒有值」，而值**已經被填好了**。

## M3 汙染路徑（逐步驗算，ACDD01）

```
Google Sheet: ACDD01 的 currency 欄空白
      ↓  v2_editor.py:85   →  currency = "USD"    ← 🔴 造假發生在這裡
      ↓  存進 portfolio_funds[i]["currency"]
      ↓  checkup.py:327  _ccy_raw = (mj.get("currency") or f.get("currency") or "")
                          MoneyDJ 沒回 → 吃到上游注入的 "USD"
      ↓  checkup.py:328  normalize → "USD"（default="" 守門形同虛設）
      ↓  checkup.py:330  _safe_fx("USD") → 32.1120
      ↓  checkup.py:347  _amt_local_inv = 294,904 ÷ 32.112 = 9,183.64   ← 原幣本金
      ↓                  單位數 = 9,183.64 ÷ 324.85 = 95.86
正確值：              294,904 ÷ 324.85 = 907.8 單位          ← 差 32.1 倍
```

**第二條獨立汙染路徑**（不經 checkup）：

`ui/tab3_portfolio.py:266` —— FX 曝險摘要自己又算一次，且是**雙重** USD fallback：
```python
_ccy = str(_pf_fx.get("currency") or "USD").strip().upper() or "USD"
```
→ 產出「**USD 25 檔（100%）**」與「⚠️ 組合 100% 為 USD 計價」的錯誤風險警語。

> 諷刺的是 `:290-292` 的註解特意寫著「含『USD 佔比過半』警告 → 收起來等於把風險藏起來（原則 1）」—— 開發者很小心地不把警語收合，但**警語本身是用捏造的幣別算出來的**。

## M4 全站 `or "USD"` 盤點（17 處）

| 檔案:行 | 風險 |
|---|---|
| **`ui/helpers/v2_editor.py:85`** | 🔴 **根因** —— Sheet → fund dict 的注入點 |
| `ui/helpers/v2_editor.py:480` | 🔴 同檔第二處 `_ccy or "USD"` |
| **`ui/tab3_portfolio.py:266`** | 🔴 FX 曝險摘要，雙重 fallback |
| `ui/helpers/d_mode.py:51,70,142` | 🟠 D 模式三處 |
| `ui/helpers/cloud_io.py:360,386` | 🟠 雲端讀寫兩處 |
| `ui/tab3_t7_ledger.py:337,370,377,443,624,649,1464,1738,2157,2218,2617` | 🟠 **11 處**（其中 `:649` / `:1464` 另有「中文幣別 `.upper()` 後仍是中文」的問題，已在報告 §Tab④ 記載） |
| `services/ai_service.py:182` | 🟡 餵 AI 的 prompt |
| `services/ledger_service.py:58,191` | 🟡 dataclass 預設 |

## M5 修法建議

**最小修**（單行，立即消除根因）：
```python
# ui/helpers/v2_editor.py:85
"currency":  str(r.get("currency", "") or ""),   # 與 fund_name / tier 一致：空就空
```
配套：`checkup.py` 與 `fund_row.py` 的既有守門會自動接手，該檔顯示 `—` 或整列標失敗（**這正是 columns.py help 承諾的行為**）。

**中期**：把 `normalize_ccy` 的 `default` 參數預設值由 `"USD"` 改為 `""`，強制所有 caller 顯式決定 fallback；17 個 `or "USD"` 逐一檢視。

**必附測試**（依 `PROCESS.md §4`，要能在改回舊行為時變紅）：
```python
def test_v2_editor_does_not_fabricate_currency():
    """Sheet currency 欄空白 → 不可無中生有 USD（否則台幣基金會被 ÷32 匯率）。"""
    df = _rows_to_df(policy_id="P1", rows=[{"fund_code": "ACDD01", "currency": ""}])
    assert df.iloc[0]["currency"] == "", "空白幣別被填成 USD → 台幣基金金額會差 32 倍"
```

## M6 ⚠️ 尚待你確認的前提

我的根因鏈假設「**Sheet 裡 ACDD01/ACDD19 的 `currency` 欄是空白或非 ISO 值**」。
請你打開政策 Sheet 確認那兩列的 `currency` 欄實際內容：

- 空白 → 根因鏈成立，改 `v2_editor.py:85` 即可
- 填「台幣」→ 則問題在 normalize 沒被套用（`v2_editor.py:85` 只做 `str()` 沒有 `normalize_ccy`），修法相同但要補 normalize
- 填「USD」→ 是 Sheet 資料本身錯，程式沒問題，改 Sheet 即可

---

---

# N. 🔴🔴🔴 最高優先：真實 25 檔持倉下，任一互動 17 秒後連線中斷

## N1 兩次獨立重現 + 精確計時

| # | 條件 | Console 證據 | 結果 |
|---|---|---|---|
| 1 | 1 檔基金 → 切到「參考 / 診斷」 | `08:59:02 [ERROR] Client Error: **WebSocket onerror**` | 凍結 150 秒+，需重載 |
| 2 | **25 檔真實持倉** → 「配置 & 帳本」觸發一次 rerun | `09:46:00 [LOG] INITIAL -> (5, 0, ) -> **RUNNING**`<br>`09:46:17 [WARNING] **WebSocket onclose**` | **rerun 開始後 17 秒連線關閉**，此後永久無回應 |

> 第三次凍結（個基深掘展開 expander、全新 session、無資料）**沒有** WebSocket 錯誤，只有 `RUNNING` → 判定為截圖機制與渲染競爭，**不列為 app bug**。

## N2 這是什麼等級的問題

**帶著真實 25 檔持倉，這個儀表板在任何一次觸發完整 rerun 的互動之後 17 秒，就會失去與後端的連線。** 使用者看到的是畫面永久凍結，唯一解法是重新整理 —— 而重載會清空 session（總經 34 秒 + 健診 45 秒/檔 全部重來）。

**這比幣別誤判更嚴重** —— 幣別錯會給錯數字，這個是**根本用不了**。

## N3 為什麼（全部指向報告 §G5 已列的項目）

單次 rerun 在 25 檔規模下要跑：

| 位置 | 25 檔的成本 |
|---|---|
| `tab3_portfolio.py:2694 → :2745-2839` | **AI snapshot 預先全算**（在使用者按 AI 按鈕之前）：`fetch_usdtwd_frame` 網路 + 25× `compute_max_drawdown` + 相關性矩陣 + 每個幣別一次 `get_latest_fx` |
| `tab3_portfolio.py:2559-2626` | 持倉健診 `ThreadPoolExecutor(4)` × **25 檔** `process_one_fund`，唯一守門是 `if _loaded_pf:` |
| `tab5_data_guard.py:793/1371/1407` | 60 月 expanding window 回測（無 cache）+ **2 次 Google Sheets 網路讀取**（無 cache、無 expander） |
| `data_registry._update_data_registry()` | 25 檔 × 全量 `sort_index()`，並把**排序後完整 pandas Series** 塞進 session_state |

而 `app.py:205` 的註解自己寫著「**st.tabs 單次 run 渲染全部分頁**」—— 所以**以上全部在每一次互動都跑一遍**，不管使用者停在哪一頁。

## N4 這條改變了修復順序

原報告把「AI snapshot 預先全算」「持倉健診無守門」「Tab5 無 cache」列在 **P1（效能與體驗）**。
**實機證明它們合起來是 P0 —— 而且是唯一一條會讓 app 完全不能用的。**

建議提升為第一優先，且**必須在動幣別那條之前先修**（否則修完幣別也驗證不了，因為載入 25 檔就當機）。

## N5 ⚠️ 你的 session 已經斷了

第 2 次重現把你剛載入的 25 檔 session 弄斷了，抱歉。剩下這些**還沒驗到**：

- Tab5 Section ① 燈號誤綠（條件成立過，但兩次都撞上凍結）
- T7 帳本：「目標單位數」死碼 / `_FX_FALLBACK` 估算匯率無標記 / `st.stop()` 炸整頁
- 我的管理室：`portfolio_funds` 覆蓋 / 通報預覽 6 項差異 / LINE 燈號假陰性
- 健診大表「換匯資訊 🧮」欄（要橫向捲）
- slider 死控制項（需要缺口落在 0.5~5.0pp 的標的）

---

*實機驗證 · 2026-08-14 · 六輪：未登入盤點 → 實跑健診(本金 500,000) → 個基深掘 + 連線穩定性 → user 登入載入真實 25 檔 → 幣別誤判根因追蹤 → 25 檔 rerun 連線中斷重現 · 破壞性操作（寫 nav_history / 送 LINE / 重置帳本 / 清空紀錄）一律未點擊*
