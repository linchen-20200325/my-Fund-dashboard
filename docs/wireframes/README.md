# UI 線框（Wireframes）—— 客戶拍板原件

本目錄存放**客戶審核並拍板的 UI 線框圖**。它們不是設計參考、不是草圖說明，
而是**規範文件（normative）**：production code 直接引用其中的條文，並以它為實作依據。

---

## ⚠️ 這些是客戶原件，不得改寫、不得「優化」

- **禁止**修改、重排、精簡或「順一下文字」。逐字保留。
- 日後客戶若給新版 → **新增檔案**，並在下方「版本關係」註明它取代／延伸了誰。
  **不要覆蓋舊檔** —— 舊檔是拍板紀錄，覆蓋掉就失去了「當初客戶同意的是什麼」這個事實。

依 `CLAUDE.md §-1.5`（虛擬軟體公司運行規範）v3 `03`-2 ①：
任何版面佈局、欄位增減或分頁動線異動，動工前必須先提線框草稿供客戶拍板。
本目錄即該流程的**產出物與存證**。

---

## 為什麼這些檔案必須進版本控制

在此之前，這三份線框**只存在於 session 的暫存目錄**（gitignored，且容器回收即消失），
但 repo 內的 production code 已經在註解裡逐字引用它們。

**引用一份不在版控裡的檔案，等於引用一份會消失的規範。**

一旦暫存目錄被回收，那些引用就全部變成**指向不存在文件的路徑引用** ——
而這正是本 repo 近期已經用兩個 PR 修過的同一種病。與其日後再修一次，
不如把被引用的原件本身放進版控。

**實測引用範圍（量測日 2026-08-28，`grep -rlE '線框|[Ww]ireframe' --include=*.py`）**：

| 範圍 | 命中 |
|---|---|
| production `.py`（不含 `tests/`） | **26 處，11 檔** |
| 全 repo `.py`（含 `tests/`） | 55 處 |
| 逐字引用**檔名**者 | `ui/helpers/render_state.py`、`tests/test_render_state_color_separation.py`、`tests/test_app_apptest.py` |

⚠️ 上表是**會漂移的量測值**，依 `CLAUDE.md` 慣例標註量測日；需要現值請現場重跑上列指令，
**不要引用本表**。

最強的依賴是 `ui/helpers/render_state.py` —— 其 module docstring 寫明
「線框原文，**本模組存在的唯一理由**」，並逐字引用
`fund-empty-state-wireframe.html` §03「顏色：三態統一規則」。
**該線框若消失，這個模組就失去它自稱的存在依據。**

---

## 各檔用途

| 檔案 | 日期 | 內容 | 狀態 |
|---|---|---|---|
| `fund-wireframe-final.html` | 2026-08-27 ⚠️ | **七分頁重整為五分頁**的結構線框（分頁動線異動） | 客戶已拍板；⚠️ **日期有兩種記載，未定案，見下方「版本關係」末條** |
| `fund-empty-state-wireframe.html` | 2026-08-28 | **空狀態與三態顏色規則**；§03「顏色：三態統一規則」嚴格分離「業務紅燈」與「系統真紅燈」 | 客戶已拍板，**批次 3a 已依此上線**（`ui/helpers/render_state.py` 直接引用其 §03） |
| `fund-redesign-wireframe.html` | 2026-08-27（早期） | 早期改版草稿 | **保留供追溯**，非現行規範 |
| `rotation-components-wireframe.html` | 2026-08-31 | **輪動配對兩元件**：②「🛡️ 持倉互斥避險」（警示對上移 + 同質化分級）＋ ③「🧩 候選標的互補探索」（批次端改型為預設收合 Expander + 卡片 + CSV） | 客戶已拍板（**Q1~Q4 全數照推薦**） |
| `wireframe-macro-health.html` | 2026-08-31 | **① 市場定調／② 組合健診 的頁內去重與重新分配**：逐塊現況盤點 → 8 項重複清單（DUP-1~8，分「真重複／入口碎裂／刻意非重複」三級）→ 重組後版面 → 逐列搬移對照表。① 的「💰 資本防線／🚦 持倉紅綠燈／📋 逐檔決策矩陣」搬去 ②；② 的單檔細節（🎯 選基金低基期、💼 逐檔深度分析、⑪ Bollinger、⑬ 個股新聞、⑭ 三率穿透、🔎 235 各檔理由）搬去 ③。含總管已拍板決定 1（三態顏色要分開）與決定 2（行動摘要口徑須寫出涵蓋範圍） | 客戶已拍板（**「目前這三個草稿ok 請繼續」**） |
| `wireframe-fund-research.html` | 2026-08-31 | **③ 🔍 基金研究合併頁**：原「🔍 個基深掘」與「📦 批次分析」兩個頂層分頁合併成一頁，以單一 radio 模式切換呈現（🔍 單檔深掘／📦 批次掃描）＋ 共用頂部（導覽與代碼搜尋畫一次）＋ 批次延遲載入閘門。含 5 項重複清單（D1~D5）與總管已拍板四項決定（輪動門檻不共用／導覽收成一份／批次 KPI 維持 4 欄／下鑽動線分兩批） | 客戶已拍板（**「目前這三個草稿ok 請繼續」**） |
| `policy-split-wireframe.html` | 2026-08-31 | **保單管理切分**：把原本擠在 ④ 我的配置裡的「保單管理（Google Sheets）」約 800 行收合區切成兩塊 —— ④「📋 保單資料」（3 欄狀態列、保單一覽、每張保單明細、新增／更新保單列）與 ⑤ 設定與診斷「🔌 連線與帳號」（Google 連線狀態、雲端存讀與本機備份、首次設定精靈、格式升級、快取重抓）。含總管已拍板六項決定（A~F）與逐狀況的三態顏色空狀態對照表 | 客戶已拍板（**「版面 OK，動工」**）；**對 `fund-wireframe-final.html` 有一處明確覆蓋，見下方「版本關係」** |
| `ia-wireframe.html` | 2026-09-01 | **五分頁動線重構**：依「功能純粹性」與「金融操作自然動線」把五個分頁重新定義職責並**改名**（① 市場總覽／② 持倉體檢／③ 標的探索／④ 資產配置／⑤ 設定與診斷），含四大鐵則（01 三欄自適應網格／02 Form 封裝防重繪／03 三態顏色分離／04 首屏無冗餘占位）與三項已拍板決定（換股顧問→④、交易帳本拉出巢狀、總經只留一份且舊版本批不拔） | 客戶已拍板；**對 `fund-wireframe-final.html` 有兩處明確覆蓋 —— (a) 分頁命名、(b) ⑤ 的內部區塊切分（2026-09-02 裁決），兩處都見下方「版本關係」**。⚠️ 另對 **②③④ 的頁內架構**亦有規範（四大鐵則＋各 Tab 區塊清單），已陸續落地 |

### 版本關係

- `fund-redesign-wireframe.html` → `fund-wireframe-final.html`：後者是前者定案後的版本。
  **現行結構規範以 `-final` 為準**；`-redesign` 保留是為了追溯「當初考慮過什麼、為什麼沒選它」。
- `fund-empty-state-wireframe.html` 與上述兩者**正交**：前兩者管**版面結構**（哪些分頁、怎麼排），
  它管**狀態呈現**（沒資料時畫什麼、什麼情況用什麼顏色）。**三份都有效，不互相取代。**
- `rotation-components-wireframe.html` **對 `fund-wireframe-final.html` 做了一處明確覆蓋**
  （客戶 2026-08-31 **Q3 裁示**）：`-final` 中「批次端輪動配對**整段移除**、只留 ②」該句
  **自此不再是現行規範** —— ③ 保留輪動能力、改型為「互補配對探索」
  （預設收合 Expander + 最佳配對卡 + 完整表 + CSV）。
  依本 README 開頭規定，**`-final` 檔案本身一字未改**（客戶原件、拍板紀錄）；
  覆蓋關係記在這裡，現行以 `rotation-components-wireframe.html` 第 03／07 節（Q3）為準。
  其餘部分兩份**互不取代**：`-final` 管五分頁整體結構，本份只管輪動配對兩元件。

- **2026-08-31 的三份新線框（`wireframe-macro-health` / `wireframe-fund-research` /
  `policy-split`）與 `fund-wireframe-final.html` 的關係：細部延伸，不是取代**（唯一例外見下一條）。
  `-final` 定的是**五個分頁怎麼分、誰去誰留**（跨分頁的結構）；這三份各自往下鑽一層，
  定的是**某一頁裡面的區塊怎麼排、哪些重複要收**（頁內的資訊架構）。
  三份的守備範圍互不重疊，合起來剛好蓋滿五個分頁：

  | 新線框 | 管哪幾頁 | 它回答的問題 |
  |---|---|---|
  | `wireframe-macro-health.html` | **① 市場定調 · ② 組合健診** | 這兩頁各自該留什麼、哪些區塊該互搬 |
  | `wireframe-fund-research.html` | **③ 基金研究** | `-final` 決定「個基深掘 + 批次分析 → 合併成 ③」之後，**合併頁本身長什麼樣** |
  | `policy-split-wireframe.html` | **④ 我的配置 · ⑤ 設定與診斷** | `-final` 決定「配置留 ④、設定搬 ⑤」之後，**那 800 行保單區實際怎麼切** |

  → 讀的順序是：**先讀 `-final` 知道有哪五頁，再讀對應那一份知道該頁裡面怎麼排。**
  `-final` 本身**除下一條所列的那一處外，其餘一字未被推翻**。

- `wireframe-fund-research.html` **承接** `rotation-components-wireframe.html` 的 Q3 結果，
  兩份**不衝突**：Q3 決定「③ 批次端的輪動配對改型為『🧩 互補配對探索』（預設收合 Expander +
  最佳配對卡 + 完整表 + CSV）」，而 `wireframe-fund-research.html` 的現況盤點（§01 模式 B）
  **就是照這個改型後的樣子畫的**。同理 `wireframe-macro-health.html` 的 DUP-7 判定
  「🛡️ 持倉互斥避險（摘要）留 ② 原位、⑥ 相關性矩陣（明細）不搬家」也與 Q1／Q2 一致。
  **三份併讀，無矛盾。**

- ⚠️ `policy-split-wireframe.html` **對 `fund-wireframe-final.html` 做了一處明確覆蓋**
  —— **有意識的變更，不是漏改**（日期 **2026-08-31**，決策者 **AI 總管**，依客戶當日授權的
  「Tab 內容重分配全權」；客戶其後看過該線框並回覆「版面 OK，動工」）。

  **衝突點**：**保單資料的唯讀檢視（保單一覽 + 每張保單明細）到底住哪一頁。**

  | 線框 | 核准先後 | 該線框怎麼寫 |
  |---|---|---|
  | `fund-wireframe-final.html` | **先** | ⑤ 設定與診斷「B · 🗄️ 資料維護」搬入「🛠️ 進階工具（保單資料表檢視）」，並註明**這裡只留唯讀檢視與批次工具** |
  | `policy-split-wireframe.html` | **後** | 保單一覽與每張保單明細**留在 ④ 我的配置**（該線框的「決定 A」） |

  **兩邊理由並陳（舊寫法沒有寫錯，是被權衡掉的）**：
  - **舊寫法的理由仍然成立** —— 設定性質的東西集中到 ⑤，④ 就能保持乾淨、動線單純；
    而「唯讀檢視」跟著「保單資料表」一起搬，分類上也自洽。
  - **被權衡掉的原因有兩個**：(a) ④ 的「配置總覽 → 保單分組」**本來就是按保單分組呈現的**，
    把明細切到另一頁，等於**把同一件事拆成兩頁對照著看**；(b) 保單明細是**每個月都會看的資料**，
    不屬於 ⑤「出事才來查」的定位。
  - **以後核准者為準**：客戶是在**看過** `policy-split-wireframe.html`（該線框首屏就用紅框
    把這項不一致標示出來，並附「可以直接否決」的說明）**之後**才說「版面 OK，動工」。

  依本 README 開頭規定，**`-final` 檔案本身一字未改**（客戶原件、拍板紀錄）；
  覆蓋關係記在這裡。**現行以 `policy-split-wireframe.html` 的決定 A 為準。**
  ⚠️ **這一條務必讀到** —— 兩張線框對同一塊內容給了相反的位置，
  不知道哪張算數的人，很可能把它當成漏改而「修正」回 ⑤。
  兩份其餘部分**互不取代**：`-final` 管五分頁整體結構，`policy-split` 只管保單區的切分。

- ⚠️ `ia-wireframe.html` **對先前所有線框的「分頁名稱」做了一次全面覆蓋**
  —— **有意識的變更，不是漏改**（日期 **2026-09-01**，決策者 **客戶**）。

  ~~**五個分頁的數量、順序、職責分界一律未動**；被覆蓋的**只有名字**：~~

  ⚠️ **2026-09-04 就地更正：上面那句的射程已經不對了** —— **有意識的更正，不是漏刪**
  （日期 **2026-09-04**，決策者 **AI 總管**，依據：**2026-09-02 總管裁決**，
  記於 commit `a56994e` 的訊息）。
  **舊句在它寫下的當天是對的** —— 2026-09-01 編入 `ia-wireframe.html` 時，
  被覆蓋的確實只有分頁名稱；**被權衡掉的是它的射程**，不是它的判斷。

  **現行讀法 —— `ia-wireframe.html` 覆蓋兩件事，不是一件**：
  1. **五個分頁的名稱**（下表，2026-09-01，決策者客戶）；
  2. **⑤ 設定與診斷的「內部區塊切分」**（2026-09-02，決策者 AI 總管，見下一條）。

  **仍然未被覆蓋的**：**分頁的數量與順序**（仍是五頁、仍是同一個順序）。

  ⚠️ **2026-09-04 二度更正：本條初稿曾寫「②③④ 的頁內架構不在覆蓋射程內」，那句是假的，
  已撤下** —— **有意識的撤回，不是漏刪**（日期 **2026-09-04**，決策者 **AI 總管**，
  依據：**獨立稽核推翻，三方證據如下，全部可自驗**）：
  1. **本 README 自己的目錄表就寫著相反的話** —— `ia-wireframe.html` 那一列列出
     **四大鐵則**（01 三欄自適應網格／02 Form 封裝防重繪／03 三態顏色分離／
     04 首屏無冗餘占位）與「**交易帳本拉出巢狀**」。**四大鐵則就是頁內架構規範，
     「交易帳本拉出巢狀」就是 ④ 的頁內結構決定。**
  2. **線框內文**：Tab 02 訂了診斷條件 Form 與逐檔體檢表等區塊清單；
     Tab 03 訂「**Form ─ 唯一搜尋入口（取代目前三處分散的搜尋框）**」；
     Tab 04 訂 ④ 的區塊與順序，並寫「⚠️ **巢狀 `st.tabs` 一律不留**；分頁只有一層」。
  3. **已合併進 main 的施工**：`adac135`（T5 ④ 交易帳本拉平巢狀分頁）、
     `d673ee5`（T20 ② 5 格結論摘要上移，自陳依「**拍板線框 Tab 02 的順序**」）。
     → **②③④ 早就在依 `ia-wireframe.html` 施工，而且已經 merge。**

  **現行讀法**：`ia-wireframe.html` **對 ②③④ 的頁內架構另有規範**（四大鐵則
  ＋ Tab 02／03／04 的區塊清單與順序 ＋ ④ 交易帳本解巢狀 ＋ ③ 唯一搜尋入口），
  且已由既有批次**陸續落地**；而 **2026-09-02 那次裁決（`a56994e`）處理的只是 ⑤ 的
  NAV 切分**，射程僅止於 ⑤。
  ⚠️ **不要反向讀過頭**：已證實的是「**它對 ②③④ 有頁內規範且正在落地**」，
  **不是**「它取代了 2026-08-31 那三份的每一條」。
  **兩份併存、逐條看；`ia-wireframe.html` 與那三份之間的射程本身仍未釐清**，
  本 README **不替它下結論**。

  **(a) 分頁名稱**（2026-09-01，決策者 **客戶**）：

  | slot | 先前線框用的名字 | `ia-wireframe.html` 起 |
  |---|---|---|
  | ① | 🌐 市場定調 | **🌐 市場總覽** |
  | ② | 💊 組合健診 | **💊 持倉體檢** |
  | ③ | 🔍 基金研究 | **🔍 標的探索** |
  | ④ | 📊 我的配置 | **📊 資產配置** |
  | ⑤ | ⚙️ 設定與診斷 | ⚙️ 設定與診斷（未改） |

  依本 README 開頭規定，**先前那些線框檔案本身一字未改**（客戶原件、拍板紀錄）；
  覆蓋關係記在這裡。**現行分頁名以 `ia-wireframe.html` 為準**，
  程式端唯一真相源是 `ui/helpers/story_nav.py::_TAB_LABELS`。
  ⚠️ **這一條務必讀到**：`wireframe-macro-health.html`／`wireframe-fund-research.html`／
  `policy-split-wireframe.html` 三份的**內文仍用舊名**（它們是拍板紀錄，不改）——
  讀它們時請把名字對照上表，**不要**把舊名當成「現在畫面上找得到的分頁」。

- ⚠️ `ia-wireframe.html` **Tab 05 對 `fund-wireframe-final.html` §03 ⑤ 做了第二處明確覆蓋**
  —— **有意識的變更，不是漏刪**（日期 **2026-09-02**，決策者 **AI 總管**，
  裁決原文記於 commit `a56994e` 的訊息）。
  ⚠️ **執行進度分兩半，不要合併讀**（2026-09-04 稽核指出，就地更正）：
  **NAV 那部分已執行**（產物 `ui/helpers/settings_diag/nav_history_section.py`）；
  **⑤ 重組成五塊尚未落地** —— `a56994e` 自陳「**⑤ 重組成五塊屬 T18 批次，本批不做**」。
  ~~該裁決已被執行~~ 這個寫法把兩半講成一半，已撤（**有意識的更正，不是漏刪**）。

  **衝突點**：**⑤ 設定與診斷「內部」到底切成哪幾塊**（不是它跟別頁的邊界）。

  ⚠️ **⑤ 的切分有三層，不是兩層** —— 本條初稿只拿 `-final` 的 A~E 來比，
  **漏掉了中間那層，而中間那層才是現任**（2026-09-04 稽核指出，就地補上）：

  | 線框 | 時序 | ⑤ 切成什麼 | 狀態 |
  |---|---|---|---|
  | `fund-wireframe-final.html` §03 | 2026-08-27 | **五個分區**：A · 🔌 連線與帳號／B · 🗄️ 資料維護／C · 🔔 通報／D · 🔭 資料診斷／E · 📖 說明書。NAV 在 B 之下是**一塊**（「合一 NAV 歷史 —— **三個功能一個入口**」） | 已被下一列覆蓋 |
  | `policy-split-wireframe.html` | 2026-08-31 | **四個分區**：🔌 連線與帳號 · **🗄️ 資料維護與通報** · 🔭 資料診斷 · 📖 說明書（C 通報併入 B） | ⭐ **現任，production 正在跑的就是它**（`ui/tab_settings_diag.py` 的四個 `safe_section(...)` 逐字對得上） |
  | `ia-wireframe.html` Tab 05 | 2026-09-01 | **五塊**：資料來源健康度／**NAV 累積狀態**／連線與金鑰／**手動補資料**／使用手冊。NAV 是**兩塊** —— 唯讀的「NAV 累積狀態」與寫入類的「手動補資料」（後者全部 Form 封裝） | **NAV 那兩塊已落地**；**五塊重組屬 T18，尚未落地** |

  ⚠️ **讀這張表最容易犯的錯**：以為 ⑤ 現在長得像 `-final` 的 A~E 或像 Tab 05 的五塊。
  **都不是** —— 現任是中間那列的**四分區**（該次覆蓋已登記在上方 `policy-split` 那一條）。

  **兩邊理由並陳（舊寫法沒有寫錯，是被權衡掉的）**：
  - **舊寫法的理由仍然成立** —— 它要解的問題是真的：NAV 匯入當時**分散在管理室與資料診斷
    兩個分頁**、連名字都不一樣，而且「留任一個舊標題都會讓人以為另一個還在別頁」。
    **這件事該收，這個判斷至今沒有被否定。**
  - **被權衡掉的是它的切法**，不是它的目的：把「看狀態」與「寫資料」放進同一塊，
    會讓一個**唯讀**的涵蓋度查詢和三條**寫入**路徑共用一個入口；
    拆成兩塊之後，寫入類才能整批套上鐵則 02（Form 封裝防重繪），
    唯讀那半也才能當成寫入那半的前提來讀（「先看累到哪一天，再決定要不要補」）。
  - ⚠️ **合的是入口，不是實作**：三條寫入路徑實測**行為不等價**
    （多檔／單檔、代號來源、寫本機／只寫雲端、CSV 要不要代號欄），**一條都沒刪**。

  依本 README 開頭規定，**`-final` 檔案本身一字未改**（客戶原件、拍板紀錄）；
  覆蓋關係記在這裡。**現行以 `ia-wireframe.html` Tab 05 為準。**
  ⚠️ **這一條務必讀到** —— `-final` §03 ⑤ B 那句「三個功能一個入口」**已不是現行規範**，
  不知道的人很容易把現在的兩塊當成「拆錯了」而合回去。
  ⚠️ **射程僅止於 ⑤**：`-final` 的其餘部分（五分頁整體結構）**未被本條推翻**；
  ②③④ 的頁內架構同樣不在本條射程內（見上一條）。
  📌 **⑤ 五塊的最終順序尚未落地**：線框的順序是「資料來源健康度 → NAV 累積狀態 →
  **連線與金鑰** → 手動補資料 → 使用手冊」（兩塊 NAV 中間**夾著**連線與金鑰），
  現況兩塊仍相鄰於 B 分區內。**⑤ 重組成那五塊屬 T18 批次**，就地登記，不在本條處理。

- 📌 **登記：ia-wireframe Tab 05「這裡不放什麼」明文排除快取與退避畫面**（2026-09-04 登記）。

  `ia-wireframe.html` Tab 05 的「這裡不放什麼」逐字寫著：

  > **快取與退避狀態不做成畫面** ─ 沿用先前已拍板的裁示：那批不必改任何畫面，本次不推翻

  ⚠️ **2026-09-04 就地更正：本條初稿把「動作」和「狀態」混成一團，寫成「兩項都不實作」，
  那是錯的，而且危險** —— **有意識的更正，不是漏刪**（決策者 **AI 總管**，
  依據：**獨立稽核 + 本組實測**）。**危險在哪**：那兩樣東西**現在就活著、而且有回歸守衛**，
  做 ⑤ 重組的人讀到「不實作」很可能直接把它刪掉。實測：
  `grep -n "清空抓取快取\|快取狀態" ui/helpers/portfolio/policy_admin_section.py`
  → `:747` 的 `"🗑️ 清空抓取快取"` 與 `:788` 的 `f"🔋 快取狀態：…"` **都在**，
  守衛 `tests/test_cache_info_contract.py` 存在。

  **拆成兩件，各自的落點不一樣**：

  | 項目 | 性質 | 落點 |
  |---|---|---|
  | **🔋 快取狀態**（顯示） | **狀態** | ✅ 命中排除句 → **⑤ 不新建這個畫面**。（現存那個 caption 在 ④；它的去留屬 ⑤ 重組時的決定，**不是本條管的**） |
  | **🗑️ 清空抓取快取**（按鈕） | **動作，不是狀態** | ⛔ **不在排除句的字面射程內**。反而 `policy-split-wireframe.html` 逐字寫著「『🗑️ 清空抓取快取』**去 ⑤**」→ **⑤ 重組時要承接，待 T18 處理** |

  ⛔ **`🗑️ 清空抓取快取` 現況活在 ④ 的 `ui/helpers/portfolio/policy_admin_section.py`，
  有 `tests/test_cache_info_contract.py` 守衛，不得逕行刪除。**
  ⚠️ 排除句管的是「**不做成畫面**」，而**按鈕不是狀態畫面** ——
  把一個明文被指定「去 ⑤」的動作，用一句講「狀態」的排除句刪掉，是讀過頭。
  ⚠️ **一處張力，據實登記，本條不解**：同一份 `ia-wireframe.html` 的 **Tab 01**
  「這裡不放什麼」寫的是「**資料源健康度、快取狀態 → 05**」（把它**歸給** ⑤），
  而 Tab 05 自己說**不做成畫面**。兩句可以相容（Tab 01 講的是**歸屬**、
  Tab 05 講的是**本批做不做**），但**只讀 Tab 01 的人會得到相反結論**。
  現行以 **Tab 05 自己的「這裡不放什麼」為準**（同一分頁自己對自己的範圍最具體）。
  ⚠️ **「先前已拍板的裁示」指哪一批，本次未追出確切出處**，只知它自陳「不必改任何畫面」；
  依 `CLAUDE.md §-2` 規則 6，此處**只轉述線框原文，不宣稱已查證**。

- 📌 **登記：ia-wireframe Tab 05 全篇 `通報` 0 命中 —— 依「後版沉默 ≠ 廢除」，🔔 換股通報留在 ⑤ 原位**
  （2026-09-04 登記）。

  **已查證的事實**：`grep -c "通報" docs/wireframes/ia-wireframe.html` → **0**。
  也就是說 `ia-wireframe.html` Tab 05 的五塊裡**沒有**「通報」，
  而 `fund-wireframe-final.html` §03 ⑤ 有「**C · 🔔 通報**」（🔔 換股通報 LINE），
  `policy-split-wireframe.html` ⑤ 的分區列也仍寫著「🗄️ 資料維護與通報〔全部不動〕」。

  **處置：不搬、不拔，🔔 換股通報留在 ⑤ 原位**
  （現位於 `ui/tab_manage.py::_sec_notify`，經 ⑤ 的「🗄️ 資料維護與通報」分區帶入）。

  **依據（2026-09-04 更換，理由見下）**：**根本不必動用「沉默 ≠ 廢除」去救它** ——
  ⑤ 的**現任**分區（`policy-split-wireframe.html`，見上一條的三層表）**逐字就叫
  「🗄️ 資料維護與**通報**」**，production 的 `safe_section("🗄️ 資料維護與通報", ...)`
  也逐字對得上。**通報明文寫在現任規範裡**，它從來不是一個「靠後版沉默才活著」的東西。

  ⚠️ ~~**同時要記一筆：PR #764 撤銷 T24 時用的理由不成立，照實寫，不美化。**~~
  ~~撤銷理由逐字寫在 commit `a56994e`（屬 PR #764）的訊息末段：~~
  ~~「T24（資料維護／通報拆開）依裁決撤銷：`通報` 在 ia-wireframe 的 ⑤ **完全不存在**」——~~
  ~~那是一個**沉默論證**（拿「後版沒提到」直接當成「後版廢除／不必做」），~~
  ~~與上述慣例**正面牴觸**。**結論（不動通報）是對的，理由是錯的**。~~
  → ⛔ **本指控未證立，2026-09-04 撤回**（**有意識的撤回，不是漏刪** ·
  決策者 **AI 總管** · 依據：**獨立稽核指出，本組複核後同意**）。**兩個理由**：
  1. **它與本 README 自己的另一條自相矛盾**：上方那條宣告 `ia-wireframe.html` Tab 05
     **取代**了 `-final` §03 的分區列舉，而 **C · 🔔 通報就在那份被取代的列舉裡** ——
     **取代掉一整份列舉，不等於對列舉裡的個別成員保持沉默。**
     前提既然是「取代」，就套不上「沉默 ≠ 廢除」那條規則。
  2. **通報明文在現任規範裡**（見上），所以「沉默」這個前提**根本不成立**。
  ⚠️ **這一筆本身就是 `CLAUDE.md §-2` 規則 7 的實例**：
  「**指控別人錯，寫進永久記錄之前要先查證**」—— 本條初稿**沒有查證就寫了**。
  **撤的是指控，不是結論**：🔔 換股通報**照樣留在 ⑤ 原位**，一個字都沒改。

- ⚠️ **第二處尚未解決的來源衝突：`fund-wireframe-final.html` 的日期**（2026-09-04 登記）。

  | 出處 | 記的日期 |
  |---|---|
  | 本 README 上方目錄表 | **2026-08-27** |
  | commit `a56994e` 的裁決段 | **2026-08-28** |

  **兩者必有一錯，但 `git` 裁決不了這件事，據實說明為什麼**：
  `git log --format='%ai' -- docs/wireframes/fund-wireframe-final.html`
  → **`2026-08-28 21:29:05 +0800`**，那是**進版控的時間，只是一個上界**，不是拍板日 ——
  本 README 開頭自己就寫著這些線框「**在此之前只存在於 session 的暫存目錄**」。
  **反證就在同一張表裡**：`policy-split-wireframe.html` 表列 **2026-08-31**，
  但它的 commit 是 **2026-09-01** —— **差一天，證明表上的日期不是 commit 日期。**
  → 故 commit 的 2026-08-28 **與 08-27、08-28 兩種說法都相容**，**無法據以裁決**。

  **處置：兩種記載並陳，不擅自挑一個**。表上維持 **2026-08-27**（既有記載，
  本組**沒有**任何證據說它錯），並在此登記 `a56994e` 寫的是 2026-08-28。
  ⚠️ **本組未查證拍板當日的原始出處**（`CLAUDE.md §-2` 規則 6）——
  這一條只能當**待驗事項**，**不得**被引用為「已查證的日期」。
  📌 對讀者的影響**極小**：兩份線框的**先後順序**不受影響（`-final` 無論 27 或 28 都在
  `policy-split`〔08-31〕與 `ia-wireframe`〔09-01〕之前），覆蓋關係的判定不繫於這一天。

- ⚠️ **一處尚未解決的來源衝突，就地登記（2026-09-01）**：① 的名字在
  `ia-wireframe.html` 是「**市場總覽**」，但同批派工單的 IA 表寫的是「**大盤總經**」。
  **實作取線框**（它是客戶實際看過並拍板的那份視覺，且派工單自陳線框為規範性文件）。
  **這是實作組的判斷，未經第二組驗證**（`CLAUDE.md §-2` 規則 6）——
  若總管認定以派工單為準，改 `_TAB_LABELS` 一個字串即可，其餘一律不動。

---

## 怎麼讀

這些是**單檔 HTML**，無外部相依。直接用瀏覽器開啟即可，
或在 repo 根目錄執行 `python3 -m http.server` 後瀏覽 `docs/wireframes/`。
