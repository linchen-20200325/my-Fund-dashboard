## 母法修正提案單 #6

**提案人**：規格生產組（AI 總管派工，2026-09-16 客戶裁示 1）　　**日期**：2026-09-16
**射程**：**基金儀表板（my-Fund-dashboard）** ← 固定值，⛔ 不得填別的
**base commit**：`9cbf0377`（`origin/main`）；母法 base `5569d85c`（`origin/docs/v2-constitution-governance`，**未合併**，PR #837 open + draft）

> **這是什麼**：P0 施工第一批**第 4a 項**的母法修正提案單 —— `services/ai_prompts.py:72-79`（**F4**）。
> **2026-09-16 客戶裁示 1 把原第 4 項拆成 4a／4b**：本單只管 **F4** 那一半；**F1** 那一半（`ui/tab3_t7_ledger.py` 的畫面文案）另見 **`spec/PROPOSAL_4b.md`（提案 #7）**。
> ⛔ **兩張單必須同批核准、同批施工**：只改 prompt ⇒ 畫面變成假記載；只改畫面 ⇒ 違規輸出照印。
> ⚠️ **編號說明**：母法 §8 現有提案 1／2／~~3~~／~~4~~／5，該節逐字寫「**新提案接續編號為 `提案 5`，不填 3／4 的空缺**」，故本單取 **#6**、`PROPOSAL_4b` 取 **#7**。
> **本組未寫入母法檔一個位元組** —— 編號待核發時確認。
> ⚠️ **本單所有引文皆逐行 `git show <ref>:<path> | awk 'NR==N'` 重印核對，非轉述。**
> ⚠️ **本單引述的違規文案一律加引號並標「現況引文」——⛔ 那是轉錄，不是本單的建議。**

---

### 1. 變更條款

**要修改的母法條款（逐字引用原文）**

母法 `docs/v2/CONSTITUTION.md:75`（**§1.1 凍結清單** F4 那一列）：

```
| F4 | **計算引擎 `services/**`** | 業務純函式層 | `CLAUDE.md §8.2` L2 定義 |
```

母法 `:580`（**G1**）：

```
| G1 | **嚴禁提供「一鍵再平衡」按鈕，嚴禁預設「最佳配置推薦」。** |
```

母法 `:582`（**G3**）：

```
| G3 | **所有試算參數均由使用者自行輸入**；**偏離提示僅客觀描述現況與目標差距，不提供具體處置方向**。 |
```

母法 `:1096`（G3 的最嚴重級，逐字）：

```
> (c) 指名**賣哪一檔、買哪一檔、金額多少**（G3 最嚴重的一種）。
```

**改成什麼（完整條文，⛔ 不是「建議放寬 F4」）** —— 本單請求在母法 §6.4.4 之下新增一條，逐字如下：

> 「**§6.4.4-P｜prompt 模板中的處置指令**
> `services/ai_prompts.py` 中**已存在**的 prompt 模板，若其指示形狀為下列任一，一律判為母法 **G1／G3** 所禁：
> **(i)** 要求模型**指名標的**（基金代碼、或穿透到底層個股名稱）的進出；
> **(ii)** 要求模型給出**具體配置百分比**（核心／衛星／現金的 %）；
> **(iii)** 在 prompt 內**寫死判準或水位數字**（例：差距 `> 20pp`、現金 `25%+`），因為那不是使用者輸入的。
> **處置為「prompt ＋ 其畫面描述同批修改」**：把該段依 §6.4.4-T 的三條轉換規則改寫為
> **只要求模型陳述現況與客觀對照**（祈使／動作 → 陳述狀態；移動指令 → 陳述差距；行動問句 → 狀態抬頭），
> **並同批修改 F1 端描述該輸出的畫面文案**（見提案 #7／`spec/PROPOSAL_4b.md`）。
> **依 §6.4.4-T `:1348`（母法逐字：三條轉換都只換「說法」，一個數字都不准變），讀數、燈號與計量資訊全部保留。**
> **本條的射程寫死在 `services/ai_prompts.py:72-79` 這八行**；同一支 `build_mk_advisor_prompt`（`:30-93`）內的
> **`:67-69`** 與 **`:81-89`** ⛔ **不在本條射程內**（客戶未就它們裁示）。
> **在本提案核准前，⛔ 不得動任何一邊。**」

**⛔ 本單不請求放寬 F4 這一格本身** —— 只請求就**這八行**開一次口，並把判準寫成可被下一輪引用的條文。

---

### 2. 阻礙原因（**實測**，附指令與輸出；量測日 2026-09-16 · base `origin/main` ＝ `9cbf0377`）

#### (a) 現況引文 —— 八行逐字，**原樣引用，⛔ 不是本單的建議**

指令：`git show origin/main:services/ai_prompts.py | awk 'NR>=70 && NR<=79'`

| 位置 | 現況引文（原樣引用） | 命中 |
|---|---|---|
| `:72` | `列出 2-3 檔**該減碼**的標的（指明代碼 + 原因；若新聞顯示該標的所在區域/題材有系統性風險，優先點名）+ 2-3 檔**該補進**的類型（不必指名，給類別如「長天期美債基金」「均衡配置型」）。` | **G3 最嚴重的一種**（`:1096`）—— **指名代碼的標的層級進出** |
| `:73` | `**必須引用上方「領先指標排名」前 3 名 driver**（例：「Top 1 SLOOS 反向 -0.62 → 銀行緊縮放貸領先 target 下行 3 個月 → 減碼景氣循環衛星」），對應當前景氣位階「{phase}」+ 新聞事件特性。` | **G3** —— 示例逐字以處置動作收尾 |
| `:74` | `**v18.135 必引用：若上方有「各基金底層前 10 大持股」資料**，必須點名 2-3 檔具體**底層個股**（用持股名稱），分析它們在哪些基金內、被哪條新聞衝擊；評估「若繼續持有這些基金 6-12 個月」，底層個股的最大可能損益。` | **G3** —— **穿透到個股層級的點名**，比 ②-12 的逐檔配對再深一層 |
| `:76` | `### ⚖️ 三、建議配置比例（核心 / 衛星 / 現金）` | **G1** —— **這一行是畫面上真的印出來的章節標題** |
| `:77` | `給出具體 % 比例與每個 bucket 該放什麼類型基金。` | **G1** |
| `:78` | `若使用者組合與建議差距 > 20pp，明確標「需大幅調整」。` | **G1／G3** —— `20pp` 是**寫死的**判準，不是使用者輸入的 |
| `:79` | `**參考上方「子領域燈號歷史回測」**：若多個子領域顯示「✅ 紅燈確實領先衰退」且當前燈號為紅，明確要求提高現金比例至 25%+；若新聞顯示「系統性風險升高」（銀行倒閉/戰爭升級/大規模拋售）同樣加碼現金。` | **G3** —— `25%+` 是**寫死的水位數字** |

⚠️ **`:71`（`### 🔄 二、根據景氣位階「{phase}」+ 領先指標排名 + 近期新聞 + 底層持股的換股建議`）是這一節的章節標題，同樣印上畫面**；
它**不在客戶寫死的 `:72-79` 射程內**，本單**只記位置、不列入要改的清單**，但**施工時若第二節整節改寫，它會連帶要改** —— 就地登記，免得下一輪以為它已被涵蓋。

#### (b) 它確實到得了畫面（鏈路逐行重印）

指令：`git grep -n 'build_mk_advisor_prompt' origin/main -- '*.py'` → **production import 恰好 1 個**（`services/ai_service.py:23`；另 1 個在 `tests/test_ai_prompts.py:8`，非 production）。
指令：`git grep -n 'analyze_portfolio_mk_advisor' origin/main -- '*.py'` → 下游 production import **也只有 1 個**（`ui/tab3_t7_ledger.py:59`）。

```
services/ai_prompts.py:30  def build_mk_advisor_prompt(...)
  → services/ai_service.py:23   from services.ai_prompts import build_mk_advisor_prompt
  → services/ai_service.py:334  prompt = build_mk_advisor_prompt(        （住在 :130 analyze_portfolio_mk_advisor）
  → ui/tab3_t7_ledger.py:59     from services.ai_service import analyze_portfolio_mk_advisor
  → ui/tab3_t7_ledger.py:3290-3296  按鈕 key="btn_mk_advisor"
  → ui/tab3_t7_ledger.py:3355   _mk_txt = analyze_portfolio_mk_advisor(
  → ui/tab3_t7_ledger.py:3401   st.markdown(st.session_state["mk_advisor_txt"])   ← 原樣印出
  ← 入口 ui/tab3_portfolio.py:78 / :2141 render_t7_section()（無條件呼叫）
  ← app.py:673 render_portfolio_tab()（＝舊 ④ 正式格，今天在線上）
```

#### (c) 凍結令擋住了什麼

- `services/ai_prompts.py` 落在 **F4**（母法 `:75`，`services/**`）—— **今天沒有任何豁免涵蓋 F4。**
  實測三張既有豁免的射程：**B8** ＝ `ui/views/page_0*.py` ＋ `app.py` 關舊入口（**F2／F3**）；**B21** ＝ `page_01_macro.py` 僅限 ①-4（**F2**）；
  **B28** ＝ `ui/tab1_macro_midcycle.py` 僅限 ①-8（**F1**）。⇒ **F4 一張都沒有。**
- ⇒ **不提案就動不了。**

#### (d) 「加一句免責」解不掉 —— 這一點要講精確

本鏈**只有一句免責**：`ui/tab3_t7_ledger.py:3402`（現況引文）`st.caption("⚠️ AI 建議僅供參考，最終決策需自行判斷市場與風險承受度")`。
指令：`git show origin/main:services/ai_prompts.py | grep -cE '非投資建議|教學|僅供|免責'` → **0**；同批對 `services/ai_service.py` → **0**。
⛔ **問題不是缺免責，是我們在 prompt 裡直接要求模型產出那些東西** —— 同 ②-12 與 ①-4 已定的同一把尺
（`spec/DECISION_LOG.md §H` 第 2／第 3 項逐字：「**⛔ 不得只加一句免責了事**」）。

#### (e) 六個逐字禁用詞 0 命中 ≠ 合規

母法 §6.2.1 那六個客戶逐字定死的禁用詞，在 `ui/tab3_t7_ledger.py:3284-3402` 這一區段 **0 命中**
（全檔唯一命中在 `:1990` 的 `help="A 推薦：…"`，那是**另一個功能的說明文案，不在本鏈上**）。
⚠️ **同 ②-12 的教訓：字面掃描掃不到「我們要求模型生成什麼」。** 本單的證據來自**渲染端逐行反查**，不是字表。

#### (f) 未能實測的一項，就地標明

⚠️ **本組未實跑 `tests/test_ai_prompts.py`**（沙箱無 `streamlit`／`pandas`，且依安全紅線**未 `pip install`**）。
該檔 `:16 test_build_mk_advisor_prompt_requires_phase4_3b_citation` 從函式名看**可能**鎖著 `:73` 那句「必須引用…前 3 名 driver」——
**「改 `:73` 會不會讓它轉紅」是讀函式名推的，不是跑出來的 —— 未驗，請先驗再用。**
⚠️ 若確認會紅，依母法 **§6.5-P**（守衛牴觸母法時必須與文案同批改），**本單的射程要加上那一條斷言**。

---

### 3. 替代方案（⛔ 「沒有替代方案」不成立，故本欄不空白）

- **(a)【本單推薦】改 prompt 八行 ＋ 同批改 4b 的畫面文案。**
  讀數、燈號、新聞計數、持股快照**全部保留**；只把「該減碼／該補進／給出具體 %／提高現金至 25%+」這些**指示**
  改寫為「請對照並陳述：組合目前的類別分布 vs 各類別在當前位階的歷史區間；差距多少百分點」。
  ⚠️ **依母法 `:1348`，一個數字都不准變。**
- **(b)【唯一一個「不改母法也能做」的】v2 不繼承這條鏈，讓舊 ④ 自然退場。**
  **它成立的部分（實測）**：整條鏈**只活在舊 ④** —— `git grep -n 'mk_advisor\|analyze_portfolio_mk' origin/main -- 'ui/views/**'` → **0 命中**
  （正控同批 `build_structured_summary_prompt` 在 `ui/views/**` → `page_01_macro.py:146`／`:1818`／`:1873` **3 行**，證明那條掃描是活的）。
  ⛔ **不採用**：`app.py:673` 的舊 ④ 正式格**今天還在線上**，違規照樣印在使用者面前。
  這正是母法 **§8 提案 5 替代方案 (b)** 已經否決過的同一個理由（逐字：「**v1 仍在線上，違規照樣印在使用者面前**」）。
- **(c) 在消費端把違規章節濾掉**（`ui/tab3_t7_ledger.py:3401` 印之前先砍掉第二／三節）。
  ⛔ **不採用**：兩個理由 —— (i) 那一端是 **F1**，**同樣凍結、同樣要提案**，省不掉；
  (ii) 模型已經照要求產出了、只是不印，**那是把問題藏起來，不是解決**（`CLAUDE.md §1`：掩蓋 ＝ 違憲）。
- **(d) 正式下架整個「策略3 深度組合建議」功能。**
  ⛔ **不採用**：依 `CLAUDE.md §-1.5.1a 接合 A1`，「**正式下架既有功能**」屬**不可逆操作族，須逐案請示客戶**；
  且它同樣要動 **F1**。**如果客戶想走這條，那是另一個層級的決定，不該由本單夾帶。**
- **(e) 維持現狀。** ⛔ **不採用**：母法未被遵守，且 `:72` 正中 `:1096` 那個「最嚴重的一種」。

---

### 4. 影響評估

- **凍結邊界**：動到 **F4**（`services/ai_prompts.py:72-79` 八行**字串**）。
  ⛔ **不動** F2／F3／F5／F6。
  ⚠️ **F1 由提案 #7 承接**（`ui/tab3_t7_ledger.py` 的畫面文案）—— **兩單同批，但邊界分開寫，不混成一格。**
  ✅ **`services/ai_service.py` 一個位元組都不必動**：`build_mk_advisor_prompt` 的 signature（`:30-35`）**只吃資料參數、沒有 `sections` 清單**，
  改 prompt 文字不影響呼叫端（`:334-340`）。**同一格 F4，但本單不動它。**
  ⚠️ **可能要加進射程的一項**：`tests/test_ai_prompts.py:16` 那條斷言（見 §2 (f)，**未驗**）。
- **合規**：**只減不增** —— 拿掉對模型的處置指令，不新增任何處置方向。
  對照 §6.2 黑名單：改寫後不含 L-B 種子動詞（減碼／補進／換出／提高…至），L-A 六詞本來就 0 命中。
  ⚠️ **本單不放寬任何既有守衛**；⚠️ **不觸及 §6.4 門檻**（讀數與門檻數字全部保留）。
- **機敏資料**：**不動 §7.3 矩陣任何一格。** 本單不碰憑證、不碰 Google Sheets、不碰快取，**不改變任何讀寫權限**。
  ⚠️ 本單全程未讀寫任何 Sheets、未打任何外部 API、未列出任何 token／金鑰／sheet id 的值。
- **回滾**：本單是**純字串**修改，無旗標、無 schema 變更、無資料寫入 ⇒ `git revert` 即可完全復原。
  不適用 §4.2 R2／R4／R5（沒有 v2 旗標介入）。
- **未涵蓋（本評估**沒有**查到什麼，以及為什麼）**：
  1. **`tests/` 會不會轉紅，本組未實跑**（沙箱無依賴、未 `pip install`）—— 見 §2 (f)，**標為待驗**。
  2. **模型實際輸出會變成什麼樣，本組沒有跑過** —— 本單只改「我們要求什麼」，**沒有驗證「改完之後模型會不會自己還是講出處置」**。
     ⚠️ **這是一個真缺口**：prompt 不要求 ≠ 模型不會給。**核准前請總管決定要不要在本單加一條輸出端的驗收。**
  3. **`:67-69` 與 `:81-89` 兩段同型的東西，客戶未裁示，本單不處理**（射程寫死在 `:72-79`）。
  4. **本組未通讀母法全文** —— 只逐字核對了本單引用的幾條（`:72-77`／`:580`／`:582`／`:1096`／`:1348`／§5.1 範本／§8 提案 1／5）。
  5. **本單為單組產出，沒有第二組驗過**（`CLAUDE.md §-2` 規則 6）。

---

### 5. 總管推薦方案（Recommendation）

**推薦採 (a)：改 prompt 八行 ＋ 同批改 4b 的畫面文案（提案 #7），兩單同批核准、同批施工。**

**具體原因三條**：
1. **它是唯一同時解掉「輸出違規」與「畫面說謊」的方案。** (b)(c)(d) 各只解一半或把問題挪個位置：
   (b) 要等舊 ④ 下架（今天還在線上）、(c) 把違規藏起來（違 `CLAUDE.md §1`）、(d) 是不可逆操作、要另外請示客戶。
2. **代價最小、可逆。** 純字串改動、`git revert` 即復原；`services/ai_service.py` 與 `build_mk_advisor_prompt` 的 signature **一個位元組都不動**。
3. **它與母法既有的三次同型裁決一致**（§6.4.4-T 通則：「**保留燈號與理由條列，只把『該做什麼』換成『現在是什麼』**」）——
   **本單不是新開一條路，是把同一把尺套到第四處。**

⚠️ **本推薦為 AI 總管依母法自身原則推導，未經第二組獨立驗證**（`CLAUDE.md §-2` 規則 6）。
⚠️ **§4「未涵蓋」第 2 點（模型輸出端沒有驗收）是本單最大的一個洞，請總管在核准時一併裁定要不要補。**
