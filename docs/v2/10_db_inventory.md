# 現有資料庫全盤點（my-Fund-dashboard）

**基準**：`origin/main = 9cbf03776f2a0ee6bdb5a649efb93352c40baba6`（全部以 `git show origin/main:<path>` 讀取；
工作樹 HEAD 為 `d0c2a8d`，**未使用**）
**盤點日**：2026-09-15~16　**產出者**：盤點生產組（**非稽核組**）
**零程式碼變動**：本輪未寫入 repo 任何檔案、未 commit、未 push、未連任何外部 API、未讀寫任何 Google Sheets。

---

> ## 📌 2026-09-16 修訂輪（矛盾處理，**非重新盤點**）
>
> **做了什麼**：把盤點稽核組列出的 7 條矛盾 ＋ 客戶點名的另外 2 項（說明書 6 vs 9、PPI／銅價）逐條實測後，
> **能就地解的就地改，不能解的不自行裁決**。逐條的「A 立場／B 立場／我的實測／已解或不解」
> 寫在同目錄的 **`CONTRADICTIONS_RESOLUTION.md`**。
>
> | 項 | 主題 | 本輪處置 | 改在哪 |
> |---|---|---|---|
> | 1 | `cache/nav/*` 8 列 | **已解** → 只讀不寫 | §2.1 |
> | 2 | `accumulate_nav_tw.py` 的「CI 排程」 | **已解** → 移出，改記 NAS crontab | §1.6 |
> | 3 | `_Ledgers` 的「活的」 | ~~**不解**（爭點是「讀取」的定義）→ 只補事實、**狀態未動**~~ → **已由客戶 2026-09-16 裁定，見下一個修訂輪** | §1.4 |
> | 4 | `fund.db` 8 列 | **已解** → 只寫不讀（**本 repo 射程**，下游 repo 仍在用） | §2.18 |
> | 5 | `env.ANTHROPIC_API_KEY`／`OPENAI_API_KEY` | **已解** → 活的（兩端都在 production） | §4.2 |
> | 6 | §3 節標題自我矛盾 | **已解** → 三處同改；**實數 32 列有落地，不是 18** | §0（`未落地` 定義）／§3 標題／§5.1 |
> | 7 | `templates/` 4 列 | **已解** → 查不到（§0 沒有「人讀」這個狀態） | §2.20 |
> | 8 | PPI／銅價「沒有資料可畫」 | **已解** → 拆成「即時值可畫／歷史值不可畫」兩列 | §2.8 備註／§6 發現 9 |
> | ＋ | 說明書「6 種分頁」 | **已解（文件端）** → 補成 9 種、4 本 | §6 發現 6／§8 額外抓到表 |
>
> **本輪同樣零程式碼變動**：未改 repo 任何檔案、未 commit／push、未碰 `pr829`~`pr834`、
> 未連任何外部 API、**未讀寫任何 Google Sheets（連唯讀都沒有）**、未觸發任何 workflow。
> git 只用了唯讀動詞（`show` / `grep` / `ls-tree` / `rev-parse`）。
> ⛔ **`ui/tab6_manual.py` 一個 byte 都沒有動**（客戶凍結令：草稿 OK 前不得改 `.py` / UI）——
> 說明書那張表「應該改成什麼樣子」寫在 `CONTRADICTIONS_RESOLUTION.md` 的「待施工的更正」區。
>
> **本輪是單組產出，沒有第二組看過。**

---

> ## 📌 2026-09-16 客戶裁定輪（**把客戶的裁示套進表**，非重新盤點）
>
> **客戶裁示（逐字）**：
> > 1.「有 production 讀取」= 資料到得了消費者。盤點表加一欄「位元組讀回？」（是／否）。
> > 2. 說明書 .py 維持凍結，替換全文留在待施工區。
>
> **本輪做了三件事，只有三件**：
>
> | # | 做了什麼 | 改在哪 |
> |---|---|---|
> | 1 | **把「有 production 讀取」的定義釘死**成客戶裁定的版本（＝**資料到得了消費者**）；舊的「未界定」狀態加刪除線保留＋兩邊理由並陳 | **§0.1**（新增小節） |
> | 2 | **新增一欄「位元組讀回？」（是／否）**，與「狀態」是**兩個不同的軸**；只填在「狀態 ＝ 只寫不讀」的 **5 張表 43 列**，其餘 221 列刻意留白（理由寫在 §0.2） | **§0.2**（新增小節）＋ §1.4／§1.5／§2.3／§2.9／§2.18 五張表 |
> | 3 | **依新定義重新歸類並重算**：**只有 §1.4 `_Ledgers` 9 欄 活的 → 只寫不讀**；反向檢查 §2.3 後**維持只寫不讀**（實測 `get_history_df` 仍零 caller）。六個狀態與逐節數全部重算，**合計仍是 264** | **§1.4**／**§2.3**／**§5.2**／**§5.3**（新增小節） |
>
> **數字變化（與上一輪比，逐項對得起來）**：
> **活的 168 → 159**（−9）、**只寫不讀 32 → 41**（+9），其餘四個狀態**一列未動**；
> 逐節 **89／82／60／33 ＝ 264 不變**（§1 內部由 活的 72／只寫不讀 13 變成 活的 63／只寫不讀 22）。
> **差的就是那 9 列，而且差的原因是定義換了，不是重數出來的誤差。** 數法與正控見 §5.3。
>
> ⛔ **客戶裁示第 2 點：說明書 `.py` 維持凍結** —— 本輪**沒有動 `ui/tab6_manual.py` 一個 byte**，
> 也沒有動 repo 內任何檔案。可直接套用的替換表格全文**仍然留在**
> `CONTRADICTIONS_RESOLUTION.md` 的「待施工的更正」區，**位置不變、內容不變**。
>
> **本輪同樣零程式碼變動**：未改 repo 任何檔案、未 commit／push／開 PR、未碰 `pr829`~`pr834`、
> 未連任何外部 API、**未讀寫任何 Google Sheets（連唯讀都沒有）**、未觸發任何 workflow、
> 未動 `inv/CONTRADICTIONS_RESOLUTION.md` 與 `audit/` 底下任何檔案。
> git 只用了唯讀動詞（`show` / `grep` / `rev-parse`）；基準仍是 `origin/main = 9cbf0377`。
>
> **本輪是單組產出，沒有第二組看過。**

---

> ## 📌 2026-09-16 顆粒度輪（**把客戶的顆粒度裁示套進表**，非重新盤點）
>
> **客戶裁示（逐字）**：
> > 顆粒度：**看欄，不看表**。盤點表加一欄「**誰讀它**」（UI／內部／無）。
>
> **本輪做了四件事，只有四件**：
>
> | # | 做了什麼 | 改在哪 |
> |---|---|---|
> | 1 | **把判準顆粒度釘死**成客戶裁定的版本（＝**看欄，不看表**）；舊的表／檔層判準加刪除線保留＋兩邊理由並陳 | **§0**（狀態表下的 bullet）＋ **§0.3**（新增小節） |
> | 2 | **新增一欄「誰讀它」（UI／內部／無／查不到）**，與「狀態」是**兩個不同的軸**；填 §1～§3 全部 **231 列**，§4 的 33 列刻意留白（理由寫在 §0.3） | **§0.3** ＋ §1／§2／§3 每一張表 |
> | 3 | **依欄層重判狀態**：**48 列由「活的」翻成「只寫不讀」**，橫跨 **11 張表**；每一列就地加刪除線＋一句實測依據 | §1.3／§1.6／§1.7／§1.8／§2.2／§2.7／§2.8／§2.10／§2.19／§3.1／§3.2 |
> | 4 | **六個狀態與逐節數全部重算**，並新增第三支計數程式（含正控／負控）；**合計仍是 264** | **§5.2**／**§5.4**（新增小節） |
>
> **數字變化（與上一輪比，逐項對得起來）**：
> **活的 159 → 111**（−48）、**只寫不讀 41 → 89**（+48），其餘四個狀態**一列未動**；
> 逐節 **89／82／60／33 ＝ 264 不變**。
> **差的就是那 48 列，而且差的原因是顆粒度換了，不是重數出來的誤差。** 逐列清單與數法見 §5.2／§5.4。
>
> ⛔ **本輪零程式碼變動**：未改 repo 任何檔案、未 commit／push／開 PR、未碰 `pr829`~`pr834`、
> 未連任何外部 API、**未讀寫任何 Google Sheets（連唯讀都沒有）**、未觸發任何 workflow、
> 未動 `inv/CONTRADICTIONS_RESOLUTION.md` 與 `audit/` 底下任何檔案、未動 `spec/`。
> git 只用了唯讀動詞（`show` / `grep` / `rev-parse` / `ls-tree`）；基準仍是 `origin/main = 9cbf0377`。
>
> **本輪是單組產出，沒有第二組看過。**

---

> ## 📌 2026-09-16 讀法 A 輪（**把客戶的「選 A」套進表 ＋ 同輪補改 §0 定義**，非重新盤點）
>
> **客戶裁示（逐字）**：
> > 盤點表裁示 4：**選 A**。**照字面翻，同輪補改 §0 定義。**
>
> **這是「兩件一起做」，缺一不可** —— 上一輪之所以停手，正是因為只做其中一件會讓表自我矛盾：
> 目標狀態「只寫不讀」在舊 §0 是「**有 production 寫入**，查不到讀取端」，而那 13 列**0／13 有 production 寫入**，
> 照字面填等於在表裡寫進 13 個不存在的寫入端。**客戶選 A ＝ 接受「改定義」這個代價。**
>
> **本輪做了四件事，只有四件**：
>
> | # | 做了什麼 | 改在哪 |
> |---|---|---|
> | 1 | **先改定義，再翻列**（順序刻意）。「只寫不讀」由 ~~「有 **production** 寫入，查不到讀取端」~~ 改為「**有寫入端（不限 production），但查不到消費端**」；舊定義加刪除線保留＋兩邊理由並陳 | **§0** 六狀態表 ＋ **§0.4**（新增小節，正式條文） |
> | 2 | **把那 13 列翻成「只寫不讀」**，每列就地加刪除線＋一句實測依據（**那個值實際存在哪裡**、**為什麼沒有畫面消費者**） | §1.9 ×2／§2.1 ×5／§2.12 ×1／§2.14 ×2／§2.16 ×1／§3.4 ×2 |
> | 3 | **六個狀態與逐節數全部重算**，並**自寫第四支 parser**（刪除線解法）與既有三支交叉驗證；**合計仍是 264** | **§5.2**／**§5.3**／**§5.4** |
> | 4 | **回頭掃現行「只寫不讀」有沒有列不合新定義**（答案：沒有），並把新定義**會外溢到 `查不到` 桶的 10 列候選**逐列登記、**不自行裁決** | **§0.4 (3)(4)** |
>
> **數字變化（與上一輪比，逐項對得起來）**：
> **只讀不寫 20 → 9**（−11）、**未落地 28 → 26**（−2）、**只寫不讀 89 → 102**（+13）；
> **活的 111／查不到 14／已停用 2 一列未動**；逐節 **89／82／60／33 ＝ 264 不變**；
> **「誰讀它」三個數（UI 113／內部 63／無 55 ＝ 231）一格未動** —— 裁示改的是「狀態」欄。
> **差的就是那 13 列，而且差的原因是定義換了，不是重數出來的誤差。** 數法、正控與負控見 §5.4。
>
> ⚠️ **本輪據實登記的三件事，不要略過**：
> **(a)** 新定義**只在「沒有消費端」那一側放寬** ——`活的`／`只讀不寫` 的寫入端仍用嚴格 production 尺。
> **這是刻意的分流**（若全域放寬，`只讀不寫` 9 列會全變成 `活的`，與客戶裁定的 A 分布不符），理由與實測見 **§0.4 (2)**。
> **(b)** 「只寫不讀」這個桶子**自本輪起同時裝兩種東西**（89 列有 production 寫入 ＋ 13 列沒有），
> **就地註明寫入端種類是硬要求**，不是建議。
> **(c)** `查不到` 14 列中，**10 列**在新定義下是「該不該一起翻」的候選（值確實在 repo 檔案裡），
> **本輪一格未改、兩個方向都沒裁決**，逐列登記在 **§0.4 (3)**。
>
> ⛔ **本輪零程式碼變動**：未改 repo 任何檔案、未 commit／push／開 PR／建分支／merge、未碰 `pr829`~`pr834`、
> 未連任何外部 API、**未讀寫任何 Google Sheets（連唯讀都沒有）**、未觸發任何 workflow、
> 未動 `inv/CONTRADICTIONS_RESOLUTION.md` 與 `audit/` 底下任何檔案、未動 `spec/`。
> git 只用了唯讀動詞（`show` / `grep` / `ls-tree` / `rev-parse`）；基準仍是 `origin/main = 9cbf0377`
> （工作樹 HEAD 為 `d0c2a8d`，**未使用**）。
>
> **本輪是單組產出，沒有第二組看過。**

---

## 0. 怎麼讀這張表

| 狀態 | 定義 |
|---|---|
| **活的** | 有 production 寫入 **且** 有 production 讀取 |
| **只寫不讀** | ~~有 production 寫入，查不到讀取端~~ → **有寫入端（不限 production），但查不到消費端**。**2026-09-16 客戶裁定讀法 A；有意識的政策變更，不是漏刪；決策者：客戶**。完整條文、兩邊理由、以及「為什麼 `活的`／`只讀不寫` 的寫入端仍用嚴格尺」→ **§0.4** |
| **只讀不寫** | 有 production 讀取，查不到寫入端。⚠️ **2026-09-16 補指路（定義一字未改）**：本列的「寫入端」**仍是嚴格的 production 尺**，**沒有**跟著 `只寫不讀` 一起放寬 —— **這是刻意的，不是漏改**，理由見 **§0.4**（放寬會讓本桶 9 列全部變成 `活的`，而客戶裁定的讀法 A 分布是 `活的 111`／`只讀不寫 9`） |
| **查不到** | 兩端都查不到 |
| **已停用** | 程式碼中明文標記停用／退役 |
| **未落地** | 算得出來但沒有持久化（只活在 session）**且有消費端**。⚠️ **2026-09-16 補限定**：這是**單一欄位**的狀態，**不是 §3 整節的性質** —— §3 共 60 列，其中 **32 列不是本狀態**（數法見 §3 標題下的更正註）。⚠️ **2026-09-16 讀法 A 輪補「且有消費端」五個字**（**有意識的更正，不是漏刪**；決策者：**客戶**）：沒有消費端的 session 欄位自本輪起歸 `只寫不讀`（§3.4 兩列已翻），否則同一個欄位會同時符合兩個狀態的定義。**「只活在 session」這半句一字未改**，§3 標題「28 列重繪即消失」在**持久化**的意義上仍然成立，只是**不再等於**標本狀態的列數（自本輪起是 **26**）|

- 「production」＝ 從 `app.py` 可達的 UI 路徑，或 `.github/workflows/` 有排程的 script。
  **手動 CLI script**（沒有 workflow 掛它）另行註明，不當 production。
- **行號產生方式**：一律 `git show origin/main:<path> | awk 'NR==N'` 直接印，**不經任何 `grep -v` 過濾**。
- **負控字串不得寫進本文件**（**客戶 2026-09-16 裁示**）。掃描時的負控（那個「應該 0 命中」的假符號）
  **只准留在跑指令的終端機裡，不准寫進任何文件**；文件裡一律寫成 `<負控串>` 這個佔位符，
  並註明「**負控為該輪自訂的隨機串，未列出**」。
  **理由（很實際，不是潔癖）**：負控一旦被寫進文件，**它自己就成了命中**——
  下一個人拿同一個字串去掃 `scratchpad/`、掃文件，會得到非 0，**那條負控當場失效**。
  ⚠️ **本文件自己就犯過**：前幾輪把負控字串當「範例」寫進正文，**同一個字串在本檔出現 10 處**，
  2026-09-16 客戶裁示後全部清除（改為 `<負控串>`）。
  ⛔ **不要換一個新字串寫進來** —— 那只是把同一個問題再製造一次。
  **掃描時另須排除自身檔案**（`-- . ':(exclude)*DB_INVENTORY.md'` 或等效），
  否則文件裡引用的符號名會讓**正控**也跟著假性命中。
- Google Sheets 的表分散在 **4 本不同的試算表**（＋1 個外部發佈 CSV），別混在一起看（見 §1 開頭）。
- **顆粒度：看欄，不看表**（客戶 2026-09-16 裁定，完整條文與兩邊理由見 **§0.3**）。
  ~~上面六個狀態原本是以「**一張表／一個檔**」為單位判的 —— 整張表只要有人讀，這張表的每一欄都標「活的」。~~
  **有意識的政策變更，不是漏刪**（日期 **2026-09-16** · **決策者：客戶**）。

### 0.1 「有 production 讀取」是什麼意思 —— **客戶 2026-09-16 裁定**

> **客戶原話（逐字）**：「『有 production 讀取』＝ 資料到得了消費者。」

| | 舊定義 | **現行定義（客戶裁定）** |
|---|---|---|
| 「讀取」指的是 | ~~沒有界定~~ —— 上一輪自己寫著「§0 只寫『有 production 讀取』，**沒有界定**『讀取』是指『位元組被讀回來』還是『資料到得了消費者』」，於是同一把尺在 §1.4 與 §2.3 給出相反結果 | **資料到得了消費者**。位元組有沒有被讀回記憶體**不算**；要問的是「**讀回來之後，有沒有任何東西真的用到它的內容**」 |

**有意識的政策變更，不是漏刪**（日期 **2026-09-16** · **決策者：客戶**）。
**舊定義（不界定）為什麼當時是那樣**：上一輪（矛盾 3）刻意**不自行裁決** —— 它判斷這是治理決定、不在盤點組權限內，
所以只補事實、狀態不動，並把爭點送上去。**那個處置本身是對的**，被取代的只是「懸而未決」這個狀態，不是它的理由。
**新定義為什麼勝出**：這張表是拿來**重新設計 UI** 用的。對設計者而言，
「Sheet 上有一張表、程式也真的把它讀進 DataFrame」如果**沒有任何一個欄位上得了畫面**，
那跟「沒有讀取端」在規劃上是同一件事 —— 都得先做出消費端才看得到東西。

⚠️ **這條定義一經釘死，「只寫不讀」就變成兩種完全不同的東西**，而**狀態欄分不出來** ——
所以另立一欄。

### 0.2 新增欄位：「位元組讀回？」（是／否）

**「位元組讀回？」與「狀態」是兩個不同的軸，不要當成同一件事的兩種寫法。**

| | 問的是什麼 | `_Ledgers` | `_持倉總覽` |
|---|---|---|---|
| **狀態** | 資料**到得了消費者**嗎？（客戶裁定的那把尺） | 只寫不讀 | 只寫不讀 |
| **位元組讀回？** | production 有沒有任何路徑，把存進去的**位元組真的讀回記憶體**？（不管讀回來之後有沒有人用） | **是** | **否** |

**這一欄的價值就在於能把這兩種「只寫不讀」分開**：
- **「只寫不讀 ＋ 是」** → 資料已經完整讀回記憶體了，只是沒人用它 →
  **改個消費端就能用**（`_Ledgers` 的 9 欄已經整張進了 DataFrame；
  `cache/fund_history.json` 連現成的 `get_history_df()` 都寫好了，只是零 caller）。
- **「只寫不讀 ＋ 否」** → 連讀取路徑都不存在 → **要從零寫一條讀取路徑**
  （`_持倉總覽` 全 repo 沒有任何 `load_*`；`data_cache/metadata.json` 沒有任何讀取端；
  `fund.db` 的讀取端在**另一個 repo**）。

**這一欄填在哪幾節（只填這些，其餘留白是刻意的）**：
**只填「狀態 ＝ 只寫不讀」的那幾張表** —— §1.4 `_Ledgers`、§1.5 `_持倉總覽`、
§2.3 `cache/fund_history.json`、§2.9 `data_cache/metadata.json`、§2.18 `fund.db`，
**共 5 張表 43 列**（其中 41 列狀態為「只寫不讀」，另 2 列為 §2.18 的「已停用」——
同一張表的欄位要對齊，故一併填）。
**為什麼只填這些**：這一欄是為了把「只寫不讀」拆成兩種而存在的；
對其他四個狀態，**這一欄的值可以直接從定義推出來，不帶新資訊**：
- **活的／只讀不寫** → 依 §0.1，這兩個狀態的成立前提就是「資料到得了消費者」，
  而資料要到得了消費者，位元組**必然**先被讀回記憶體 ⇒ 恆為「是」。
- **未落地** → 根本沒有持久化，**沒有位元組可讀** ⇒ 恆為「否」。
- **已停用／查不到** → 沒有活的讀取路徑 ⇒ 「否」。
⚠️ **上面四行是「從 §0.1 的定義推出來的」，不是本輪逐列量出來的** —— 本輪只實測了那 43 列。
**硬填 264 列，等於多出 221 格從定義抄來、沒有任何一格被實際查證過的值**；
**而且它們只會複述狀態欄已經說過的事。** 那不是資訊，是噪音。
⚠️ 查不到答案的一律寫「**查不到**」，不猜。**本輪 43 列全部查得到，沒有用到這個值。**
⚠️ **一個已知的例外方向，據實寫明**：上面「活的 ⇒ 恆為是」的推論是**表／檔層**的；
**若改用逐欄層**（見 §9.1 登記的那個未裁決的顆粒度問題），推論就不成立 ——
**現成的例子是 §1.8 `_portfolio_perf_history`：15 欄全標「活的」，但實測只有 2 欄上得了畫面。**

### 0.3 顆粒度：看欄，不看表 —— **客戶 2026-09-16 裁定** ＋ 新增欄位「誰讀它」

> **客戶原話（逐字）**：「顆粒度：**看欄，不看表**。盤點表加一欄「**誰讀它**」（UI／內部／無）。」

| | 舊判準 | **現行判準（客戶裁定）** |
|---|---|---|
| 判「活的／只寫不讀」的單位是 | ~~**一張表／一個檔**~~ —— 整張表只要有一個讀取端，這張表的**每一欄**都標「活的」 | **一個欄位**。要逐欄問：「**這一欄的值，有沒有任何東西真的用到？**」 |

**有意識的政策變更，不是漏刪**（日期 **2026-09-16** · **決策者：客戶**）。
**舊判準當時為什麼是那樣**：表層好維護、不容易漏 —— 找到一個讀取端就能收工，
不必逐欄追消費鏈；而且對**欄位用途齊一**的表（例如 `cache/nav/*.history[]`）結論其實一樣。
**這個理由今天仍然成立**，被權衡掉的是它的**精確度**。
**新判準為什麼勝出**：表層會把「**寫了 15 欄、只有 2 欄上得了畫面**」說成「15 欄都是活的」。
這張表是拿來**重新設計 UI** 的，設計者照表層結論去排版，會以為那 13 欄現成可用 —— **實際上要先做消費端**。
⚠️ **現成的實例就在本表裡**：§1.8 `_portfolio_perf_history` 15 欄原本全標「活的」，
逐欄查後**只有 `date` 與 `period_return_pct` 到得了畫面**，另外 13 欄只經
`getattr(self, h) for h in _HEADERS`（`repositories/portfolio_perf_repository.py:71`）
與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，再由 `append_snapshot` 以 `asdict(s)`（`:192`）寫回。

#### 新增欄位：「誰讀它」（UI／內部／無／查不到）

**「誰讀它」與「狀態」是兩個不同的軸**（同 §0.2 的「位元組讀回？」，三個軸互不取代）。

| 值 | 定義（客戶指定） | 本表的實例 |
|---|---|---|
| **UI** | 這一欄的值**到得了畫面** —— 被渲染，**或**被拿去算一個會渲染的值 | `Policies.*` 14 欄（`ui/helpers/portfolio/policy_admin_section.py:882` 的 `st.dataframe(_pdf_cached, …)` 整張渲染） |
| **內部** | 有程式讀它，但**只回流到儲存／計算中繼**，不上畫面 | `_portfolio_perf_history` 的 13 欄（`asdict()` 原樣寫回）；`cache/fred_release/*.cached_at`（只拿來算 30 天 TTL） |
| **無** | **沒有任何程式讀它** | `_T7_State.fund_code`／`.currency`／`.policy_id`（`load_all_ledgers_snapshot` 只取 `pk_str` 與 `ledger_json`） |
| **查不到** | 查了但答不出來 | **本輪 231 列全部查得到，沒有用到這個值** |

**一條界線寫死，免得下一輪各判各的 —— 「畫面」是什麼**：
指**使用者看得到的輸出**（Streamlit 畫面；`watchlist.代號` 這種走 LINE 推播的也算，該列已就地註明）。
⛔ **`print()` / stderr log 不算畫面。**
理由是本表自己的前例：§1.4 `_Ledgers` 那 9 欄被讀進 DataFrame、只餵給一個 `len()`，
結果進了 `_sheet_stats` 這個**沒有讀取端**的 session key —— 客戶 2026-09-16 已裁定那叫「只寫不讀」。
**開發者在 log 裡看得到，不等於資料到得了消費者。**

#### 這一欄填在哪幾節（只填這些，其餘留白是刻意的）

**填 §1（89 列）＋ §2（82 列）＋ §3（60 列）＝ 231 列，覆蓋率 231/231。**
**§4 的 33 列（`st.secrets` ／環境變數／`config.toml`）刻意不填**，兩個理由：
1. **這一欄對它們不帶新資訊。** 密鑰與設定值**依定義就不該出現在畫面上**
   （真出現在畫面上是資安事故），所以逐欄問「誰讀它」，答案恆為「內部」——
   那不是查出來的結果，是從欄位性質推出來的。**硬填 33 格等於多出 33 格沒有被查證過的值。**
2. **填了會誘發一次錯誤的機械翻轉。** 客戶的規則是「誰讀它＝內部或無 → 狀態不該叫活的」；
   §4 若照填，33 列會全部翻成「只寫不讀」—— 而 `secrets.FRED_API_KEY` 這種**兩端都在 production、
   而且天天在用**的東西被標成「只寫不讀」，是把一張要拿來做設計決策的表弄成假的。
   **§4 盤的是「設定從哪來」，不是「資料到不到得了畫面」，本來就不在這把尺的射程內。**

⚠️ **群組列的已知限制，據實寫明**：本表有幾列**一列代表一整組欄位**（§5.1 已列出清單）。
**群組列的「誰讀它」只能回答「這一組裡有沒有任何一欄到得了畫面」，回答不了逐欄的問題。**
本輪對這種列一律**取該組最強的值並在備註寫出內部組成**，**不翻狀態**：
- §2.4 `pool.json[].{PoolEntry 10 欄}` → **UI**（10 欄中 8 欄 UI；`note` / `added_at` 為內部，見 §1.7）
- §2.5 `perf_history.json[].{PerfSnapshot 15 欄}` → **UI**（15 欄中**只有 2 欄** UI，13 欄為內部，見 §1.8）
- §2.16 最後一列（9 個 key）→ **UI**（只有 `verdict_cutoffs` / `phase_thresholds` 到得了畫面）
⛔ **不得**把這幾列的「UI」讀成「這一組每一欄都到得了畫面」——**要逐欄的話，得先把群組列拆開**，
那會動到 264 這個總數，**不在本輪射程內**。

#### 本輪翻了哪些、刻意沒翻哪些

**翻了（48 列，全部是「活的 → 只寫不讀」）**：逐列清單見 §5.2。
依客戶規則「誰讀它＝內部或無 → 狀態不該是『活的』」，且**本輪只對原本標「活的」的列套用這條規則**。

~~**刻意沒翻（13 列）—— 這裡登記，不自行裁決**~~ → ~~**客戶已於 2026-09-16 裁示重判；本輪仍未改狀態格，卡在一個事實前提**~~
→ ✅ **2026-09-16 讀法 A 輪：這 13 列已全部翻成「只寫不讀」，本表已結案**（**狀態變更，不是漏刪** · 決策者：**客戶**）。
**客戶原話：「盤點表裁示 4：選 A。照字面翻，同輪補改 §0 定義。」** —— §0 定義已同輪改完，見 **§0.4**；
**下表 13 列的「現狀態」欄自本輪起全部過期**（它記的是翻轉前的值），**逐列的新狀態與實測依據寫在該列自己的節裡**
（§1.9／§2.1／§2.12／§2.14／§2.16／§3.4）。**下表原文一字未改** —— 它是當時那道張力的紀錄，保留供追溯：
下面這些列**狀態不是「活的」，但逐欄查完之後，它們的狀態值與「誰讀它」之間有張力**。
~~客戶這次裁的是**顆粒度**與**新增欄位**，沒有裁「非『活的』的列要不要跟著重判」，**本組不代為決定**。~~
**狀態變更，不是漏刪**（日期 **2026-09-16** · 決策者：**客戶**）—— 客戶已就這 13 列裁示（原話與處置見下方
「**2026-09-16 客戶裁示重判**」段）。**被劃掉的只有「客戶還沒裁」這個事實描述；「本組不代為決定」的立場沒有改變，
理由換了**：不再是「客戶沒裁」，而是**裁示的目標狀態與本表既有事實衝突**，見下。

| 節 | 欄 | 現狀態 | 誰讀它 | 張力在哪 |
|---|---|---|---|---|
| §1.9 | `_macro_weights.A(slot)`、`.C(updated_at)` | 只讀不寫 | **無** | `_gs_load`（`services/macro/weights_store.py:213-217`）的 row 由程式常數 `_GS_SLOT_ROWS` 決定、**只讀 B 欄**；A／C 兩欄從未被讀回 |
| §2.1 | `cache/nav/*.code`、`.fund_name`、`.source`、`.count` | 只讀不寫 | **無** | `_src_cache_files`（`repositories/fund/sources.py:1235-1253`）只取 `history` 與 `updated_at` |
| §2.1 | `cache/nav/*.updated_at` | 只讀不寫 | **內部** | 讀回後只進一個 `print()` 與 `assess_nav_cache_quality` 的 `file_age_days`（`shared/data_quality.py:197`），而 `file_age_days` **全 repo 無讀取端**；它**不影響** `usable` 判定 |
| §2.12 | `macro_thresholds_global.status` | 只讀不寫 | **無** | 唯一讀取端 `scripts/calibrate_macro_score.py:582` 是**手動 CLI**（§0 定義下不算 production） |
| §2.14 | `preset_funds.funds[].code`、`.name` | 只讀不寫 | **內部** | 兩條讀取鏈都不上畫面：(a) `_DEFAULT_FUNDS` → `_load_with_defaults`（`services/fund_history.py:120`）→ `get_history_df`，**該鏈零 caller**；(b) `scripts/export_fund_db.py:80` → `fund.db`，消費端在下游 repo |
| §2.16 | `.version` | 只讀不寫 | **內部** | 只做 `_validate` 的必要欄檢查（`weights_store.py:232`） |
| §3.4 | `t7_scenarios[].id` | 未落地 | **內部** | 只當 `st.button` 的 widget key（`ui/tab3_t7_ledger.py:2932`），值本身不顯示 |
| §3.4 | `t7_scenarios[].nav_fx_cache` | 未落地 | **內部** | `:1185` 存進方案「供之後重算」（該行註解自陳），但 `_sc["nav_fx_cache"]` **全 repo 無讀取端** |

⚠️ **「本表沒有第 14 處張力」本組不宣稱** —— 上表是逐欄查完 231 列後**目前看得到的**，
不是窮舉；群組列（見上）依定義就藏得住這種張力。

##### 2026-09-16 客戶裁示重判 —— ~~**裁示已收到，狀態格未改，原因寫在這裡**~~ → ✅ **已結案：客戶選 A，13 列本輪翻完**
（**狀態變更，不是漏刪** · 日期 **2026-09-16** · 決策者：**客戶**）。**本段以下原文一字未改**，
它記的是「當時為什麼不敢硬填」；**現行條文在 §0.4，現行狀態在各該節的列上。**

> **客戶原話（逐字）**：「**盤點表 13 列重判：誰讀它＝內部/無 → 只寫不讀。**」

**先講結論**：這 13 列的**「讀」那一半，客戶已經裁定為假**，本段照收、不打折。
**但「只寫不讀」這個目標狀態寫不下去** —— 依 §0 的定義它是「**有 production 寫入**，查不到讀取端」，
而**這 13 列一列都沒有 production 寫入**（逐列實測見下）。照字面填，等於在表裡**寫進 13 個不存在的寫入端**，
而**反證就躺在同一節、離它兩三行**（例：§2.1 的更正註自己寫著「寫入端不是 production」；
§1.9／§2.16 的「寫：**查不到**」）。**一份要拿來做設計決策的表，不能自己打自己。**
故本輪**依派工指示「不硬套、列出來、讓客戶決定」**，狀態格一格未改，把選擇題做成一句話送回。

**(1) 「13」是機器數出來的，不是抄的**（本輪重數，兩支獨立 parser 同時同意）：
交叉表（§5.4）中「**狀態 ≠ 只寫不讀／查不到／已停用，但誰讀它 ∈（內部, 無）**」的格子
＝ 只讀不寫〔內部 4 ＋ 無 7〕**11** ＋ 未落地〔內部 2〕**2** ＝ **13** ✅，
逐列傾印後與上表**逐列相符**（§1.9 ×2、§2.1 ×5、§2.12 ×1、§2.14 ×2、§2.16 ×1、§3.4 ×2）。

**(2) ~~卡住的事實~~ → 已不再卡住的事實：0／13 有 production 寫入**（當輪逐列實測，指令照抄，正控／負控同批跑）。
⚠️ **2026-09-16 讀法 A 輪：這個事實一字未變，變的是它的後果** —— 依 §0.4 的新定義，
「只寫不讀」**不再要求寫入端是 production**，所以下表五種寫入端（無寫入函式／手動 CLI／人手編輯 ＋ git commit／session）
**全部算數**。⛔ **但註明義務是硬要求**：§0.4 (1) 明訂非 production 的寫入端要就地寫出是哪一種，
**下表就是那個「就地寫出」的出處**，不得刪。**下表原文一字未改。**
⚠️ **唯一要補一句的是 §1.9／§2.16 那一列**：它們連「無寫入函式」都成立，寫入端是**人工在 Google Sheets／repo 檔案上編輯**，
屬 §0.4 (1) 的 ③／④ 兩種，已就地註明在該節的列上。

| 節（列數） | 寫入端是什麼 | 為什麼不是 production（§0 定義） |
|---|---|---|
| §1.9（2）、§2.16（1） | **沒有寫入函式** | `git show origin/main:services/macro/weights_store.py \| grep -n "^def "` → 11 個 def，**無 `save_active`**；`for_write=True` 的呼叫點 `git grep -n "for_write" origin/main -- '*.py'` → production 端**只有 `def` 那一行**，實際帶 `True` 呼叫的**只有 `tests/test_weights_store_read_no_writes.py:191`** |
| §2.1（5） | `scripts/fetch_nav_cache.py::save_cache` | 該 script **沒有任何 workflow 掛它**：`git ls-tree -r --name-only origin/main \| grep '^.github/workflows/'` → **8 個**，無 `fetch_nav_cache.yml`；`git grep -n "fetch_nav_cache" origin/main -- '.github/workflows/*'` → **1 行，而且是註解**（`update_macro_history.yml:4`）|
| §2.12（1） | `scripts/calibrate_macro_score.py`（`--emit-json`）| 同上 8 個 workflow 逐一看過，**無 calibrate** |
| §2.14（2） | `services/fund_history.py::promote_to_preset` | `git grep -n "promote_to_preset" origin/main -- '*.py'` → **只有 `:281` 的 `def`**＋2 行測試清單字串，**production 零 caller**；它 docstring 指的 `export_preset_funds_json` 同樣零 caller |
| §3.4（2） | `st.session_state`（`ui/tab3_t7_ledger.py:1099-1100`）| **根本沒有持久化** —— 這正是「未落地」的定義；唯一出口是使用者手動下載 JSON（`ui/helpers/io/json_backup.py:49`）|

**正控**（同批、同型指令，證明指令不是在空掃）：`git grep -n "export_fund_db" origin/main -- '.github/workflows/*'`
→ 命中 `export_db.yml:39`。**負控**：`git grep -n "<負控串>" origin/main -- .` → **0 命中（exit=1）**。
（**負控為本輪自訂的隨機串，依 §0 方法規則不列出。**）

**(3) ~~兩種讀法，請客戶擇一~~ → ✅ 客戶已擇定「讀法 A」（2026-09-16），下表原文保留供追溯**
（**狀態變更，不是漏刪** · 決策者：**客戶**）。**A 欄那一列的數字已於本輪落地並實跑驗證**：
**活的 111／未落地 26／只寫不讀 102／只讀不寫 9／查不到 14／已停用 2 ＝ 264** —— 與下表 A 欄**逐格相符**（見 §5.2）。
**B 欄整欄自本輪起只是備查，不再是待選項。**

| | **讀法 A（照字面）**：13 列 → **只寫不讀** | **讀法 B（照 §0 定義推）**：11 列 → **查不到**，2 列「未落地」不動 |
|---|---|---|
| 「寫」那一半怎麼認 | **放寬**：手動 CLI／人手編輯 Google Sheets／`git commit` 進 repo 的資料檔／寫進 session，**都算「寫入」** | **維持現行 §0**：只有「`app.py` 可達的 UI 路徑」或「有排程的 workflow script」算 production 寫入 |
| 好處 | 對**重新設計 UI** 更有用 —— 這些值**檔案裡真的有**（`cache/nav/TLZF9.json` 的 `fund_name`／`source`／`count` 實際存在），標「查不到」會被誤讀成「這東西不存在」 | 表**內部一致**：不會出現「說有 production 寫入、但同一節自己寫著沒有」 |
| 代價 | **等於改 §0「只寫不讀」的定義**，而且會外溢：現有 89 列「只寫不讀」全都是**有** production 寫入的，混進 13 列沒有的，這個桶子就同時代表兩件事（正是 §0.1／§0.2 當初拆欄要避免的） | 「查不到」由 14 → **25**，桶子變大；且「檔案裡真的有值」這個對設計有用的資訊，**只剩備註欄承載** |
| 狀態分布 | 活的 111／未落地 **26**／只寫不讀 **102**／只讀不寫 **9**／查不到 14／已停用 2 ＝ **264** | 活的 111／未落地 28／只寫不讀 89／只讀不寫 **9**／查不到 **25**／已停用 2 ＝ **264** |
| 逐節 | §1 89（活的41／只寫不讀**46**／只讀不寫**2**）、§2 82（活的16／只寫不讀**43**／只讀不寫**7**／查不到14／已停用2）、§3 60（活的21／只寫不讀**13**／未落地**26**）、§4 33 | §1 89（活的41／只寫不讀44／只讀不寫**2**／查不到**2**）、§2 82（活的16／只寫不讀34／只讀不寫**7**／查不到**23**／已停用2）、§3 60（**不變**）、§4 33 |

**本組推薦：讀法 A ＋ 同時把 §0 的「只寫不讀」定義補一句**，理由三條：
1. **客戶的字面就是 A**，而客戶是這張表的使用者，他要的是「**沒人用**」這件事被標出來，不是形式學。
2. **這張表的用途是重新設計 UI**（§0.3 自己寫的）。對設計者，「值在檔案裡、沒人用」（A）
   跟「連東西都沒有」（B）是**完全不同的施工量**，B 會誤導。
3. A 的代價（桶子混兩種寫入）**可以用一句定義補掉**，B 的代價（資訊遺失）補不掉。
   建議補法：§0「只寫不讀 ＝ **有寫入端**（production 或人工／CLI 皆可，**非 production 者就地註明**），查不到讀取端」。
⛔ ~~**本推薦不是裁決，本輪未據以改動任何一格。** 客戶回一句「A」或「B」即可落地；~~
~~**落地那一輪必須連 §0 的定義一起改**（A 的情況），否則定義與資料又會打架。~~
→ ✅ **2026-09-16：客戶選 A，本輪落地，且「連 §0 定義一起改」這個條件已照做**（**§0.4**，同輪）。
**這句話當時是對的，而且它是對的這件事被證明了** —— 若只翻列不改定義，表裡會出現 13 個查不到出處的 production 寫入端。

⚠️ **不論 A 或 B，都不影響下列三件事**（當輪已實測；**2026-09-16 讀法 A 落地後全部重跑，三件事逐項仍然成立**）：
**(a)** 總列數 **264** 不變；**(b)** 「誰讀它」三個數（UI 113／內部 63／無 55 ＝ 231）**一格不動** ——
裁示改的是「狀態」欄，不是「誰讀它」欄；**(c)** §5.4 交叉表第一列的不變量
（**「活的」沒有任何一列的誰讀它是「內部」或「無」**）**已經成立、且不受這 13 列影響**。


### 0.4 「只寫不讀」換定義 —— **客戶 2026-09-16 裁定讀法 A**

> **客戶原話（逐字）**：「盤點表裁示 4：**選 A**。**照字面翻，同輪補改 §0 定義。**」

**這一節取代 §0.3「2026-09-16 客戶裁示重判」段留下的那道選擇題。** 那 13 列**本輪已經翻完**（逐列就地註明）。

#### (1) 新舊定義並陳

| | ~~舊定義~~ | **現行定義（客戶裁定讀法 A）** |
|---|---|---|
| **只寫不讀** ＝ | ~~有 **production** 寫入，查不到讀取端~~ | **有寫入端，但查不到消費端。** |
| 「寫入端」算到哪裡 | ~~只有兩種：`app.py` 可達的 UI 路徑、`.github/workflows/` 有排程的 script~~ | **不限 production。** 下列**都算**：① production 寫入路徑；② **手動 CLI script**（沒有 workflow 掛它）；③ **人工在 Google Sheets 上手填**；④ **人工編輯檔案 ＋ `git commit` 進 repo 的資料檔**；⑤ **寫進 `st.session_state`**。⚠️ **②~⑤ 一律在該列就地註明是哪一種** |
| 「查不到消費端」＝ | 查不到讀取端 | **依 §0.1 未改一字**：**資料到不了消費者**。位元組有沒有被讀回**不算**（那是 §0.2 另一欄在答的問題） |

**有意識的政策變更，不是漏刪**（日期 **2026-09-16** · **決策者：客戶**）。

**舊定義為什麼當時是對的**：它讓「只寫不讀」**精確指向一條寫入路徑** —— 讀到這個狀態的人可以直接問
「那條 production 寫入在哪」，而且問得到答案。**這個好處今天依然成立**，被權衡掉的是它的**涵蓋面**。

**新定義為什麼勝出（客戶的理由，不是本組的）**：這張表是拿來**重新設計 UI** 的。
舊定義會逼那 13 列改標「**查不到**」，而「查不到」會被設計者讀成「**這東西不存在**」——
但那些值**檔案裡是真的有**（實測：`cache/nav/TLZF9.json` 的 `code`／`fund_name`／`source`／`count` 四個鍵
確實存在且已 commit 進 repo；`config/preset_funds.json`、`services/config/macro_weights_active.json` 同理）。
**「值在那裡、沒人用」與「連東西都沒有」是完全不同的施工量**，把前者標成後者會直接誤導排版決策。

**代價，據實寫明（不粉飾）**：「只寫不讀」這個桶子**自本輪起同時裝兩種東西** ——
**有 production 寫入的 89 列** ＋ **沒有 production 寫入的 13 列**。
**就地註明是唯一能把它們分開的手段**，所以上表 ②~⑤ 的註明義務是硬要求，不是建議。

#### (2) ⚠️ 放寬只發生在「沒有消費端」那一側 —— 這是刻意的，不是漏改

**一句話規則（本輪的分流尺，逐桶可驗）**：

| 這一欄**有沒有**消費端 | 寫入端用哪把尺 | 可能落在哪幾個狀態 |
|---|---|---|
| **有**（誰讀它 ＝ UI） | **嚴格**：只認 production 寫入 | `活的`（有 production 寫入且已持久化）／`只讀不寫`（查不到 production 寫入）／`未落地`（只活在 session） |
| **沒有**（誰讀它 ＝ 內部／無） | **寬鬆**：①~⑤ 任一即可 | `只寫不讀`（值進得來）／`查不到`（值也進不來）／`已停用`（另依明文標記判，與兩端無關） |

**為什麼同一個「寫入端」在兩側用不同的尺** —— 因為它在兩側**回答的不是同一個問題**：
- **有消費端**的列，寫入端回答的是「**這個值會不會自動更新**」。
  `只讀不寫` 存在的全部價值，就是警告設計者「畫面已經在用它，但**沒有 production 寫入端**，
  值只會靠人工／CLI 維護、會慢慢過期」。**把寫入端放寬，這個警告當場消失。**
- **沒有消費端**的列，寫入端只是在區分「**值在不在**」（`只寫不讀` vs `查不到`），
  **值是怎麼進去的完全不影響設計者要做的事** —— 他要做的都是「接一個消費端」。

**不這樣分流的後果，已實測**：若把寬鬆尺**全域**套用，現行 `只讀不寫` 的 **9 列會全部變成 `活的`**
（`_macro_weights.B(payload_json)` 1、`watchlist.代號` 1、`cache/nav/*.history[]` 系列 3、
`macro_thresholds_global.VIX_CRISIS/WARNING_THRESHOLD` 2、`services/config/macro_weights_active.json` 的
`.indicators{}` 與 9-key 群組列 2）→ 變成 `活的 120`／`只讀不寫 0`，
**與客戶裁定的讀法 A 分布（`活的 111`／`只讀不寫 9`）不符**。**客戶要的是前者，所以分流。**

#### (3) ⛔ 射程：本輪只翻客戶點名的那 13 列，`查不到` 桶**沒有**重新掃過

客戶的裁示原話是「**盤點表 13 列重判**」，本輪就只重判那 13 列。
**`查不到` 的 14 列、`活的` 的 111 列、`未落地` 的 26 列，本輪一列都沒有拿新尺重掃。**

⚠️ **但有一件事必須先講，不能等下一輪才發現**：新定義的寬鬆尺**同樣落在 `查不到` 桶的判準上**
（兩者都是「沒有消費端」那一側，只差「值在不在」）。本組把 14 列逐列看過，
**其中 10 列的值實測「檔案裡真的有」**，也就是**照新定義會變成 `只寫不讀` 的候選**。
**本輪一格未改，逐列登記如下，等總管／客戶裁定**：

| 節 | 幾列 | 哪些 | 值實測在哪（本輪 `git ls-tree` / `git show` 實測） |
|---|---|---|---|
| §2.14 | **1** | `preset_funds._comment` | `config/preset_funds.json` **在 repo**，頂層鍵實測為 `_comment`／`funds` —— **`_comment` 真的有值** |
| §2.15 | **1** | `config/macro_weights_active.json`（12-key 群組列） | 該檔 **在 repo**（`git ls-tree -r --name-only origin/main` 命中）；本節自陳「**沒有任何程式讀它或寫它**」—— 值在、但連讀寫路徑都沒有 |
| §2.17 | **4** | `snap.dumped_at` 等 4 列 | `snap.json` **在 repo**（本節自陳「22,235 bytes，已 commit」）；寫入端是**兩支手動 CLI 診斷 script** |
| §2.20 | **4** | `templates/` 4 列 | 5 個範本檔 **全部在 repo**（`templates/nav_history_sample.csv` 等）；但**程式完全不讀它們**，是給使用者複製用的 |
| **合計** | **10** | | |

**另 4 列本組判定「新尺也碰不到」，一併寫下理由**（同樣未改）：
§2.6 `cache/fundclear/*`（1 列）與 §2.13 `/tmp/fund_cache/*`（3 列）——
**那兩處的寫入端本身就是死碼**（§2.6 整條 FundClear 回填鏈零入口、§2.13 三個 `_cache_save_*` 全 repo 零 CALL），
**死碼不是寫入路徑**，所以「值進不來」在新定義下依然成立。

⛔ **不得**把本節讀成「那 10 列已經判定要翻」，**也不得**讀成「已經判定不翻」——**本輪兩個方向都沒有裁決。**
**本組的建議是：這 10 列該不該一起翻，屬同一個治理問題，值得一次問完，不要一輪翻 13、下一輪再翻 10。**

#### (4) 本輪回頭掃的結果：現行 `只寫不讀` 有沒有列不合新定義

**答案：沒有。** 兩半分別實跑，逐項可驗：

**(a) 讀那一半 —— 機器驗，0 例外。**
新定義的讀那一半（「查不到消費端」）與 §0.1 **一字未改**。翻完後跑交叉表：
`只寫不讀` **102 列**的「誰讀它」＝ **內部 63 ／ 無 39 ／ UI 0** —— **沒有任何一列的值到得了畫面，全部滿足新定義**（見 §5.4）。

**(b) 寫那一半 —— 先講邏輯，再講實跑。**
新定義是舊定義的**超集**（production 寫入 ⊂ ①~⑤），**放寬在邏輯上不可能把原本合格的排除掉**。
但邏輯不等於實測，所以本組另外**逐表跑了一次**：現行 `只寫不讀` **102 列橫跨 22 張表**，逐表查「有沒有已登記的寫入端」：

| 結果 | 幾張表 | 是哪些 |
|---|---|---|
| ✅ **節層有寫入端登記行** | **16** | §1.3／§1.4／§1.5／§1.6／§1.7／§1.8／§2.1／§2.2／§2.3／§2.7／§2.8／§2.10／§2.12／§2.14／§2.18／§2.19 |
| ⚠️ **節層沒有，但寫入端登記在該列自己的「出處」欄** | **4** | §2.9（`scripts/update_macro_history.py:367`）／§3.1（`services/ledger_service.py:161,165-167`）／§3.2（`repositories/policy/v1.py:254`）／§3.4（`ui/tab3_t7_ledger.py:1179,1185`） |
| ❌ **節層明寫「寫：查不到」** | **2** | **§1.9**（`:825`）與 **§2.16**（`:1138`）—— **兩張都是本輪翻進來的 13 列所在的表，不在原有那 89 列裡** |
| **合計** | **22** | 16 ＋ 4 ＋ 2 ＝ **22** ✅ |

⚠️ **這張表的三個數字，本組第一版寫成 18／4→2／2（合計 22 對不起來），自查時抓到並就地更正** ——
**登記在這裡，因為它正是本節在講的那種錯：合計對了不代表分類對了。**

**那 2 張「寫：查不到」為什麼仍然合乎新定義**：它們沒有的是**程式寫入函式**，
而新定義的 ③／④（**人工在 Google Sheets 手填**／**人工編輯 ＋ git commit 進 repo 的資料檔**）**正是為它們寫的**，
且已就地註明在該列（§1.9 的 A3／C3 儲存格、§2.16 的 repo 內 JSON 檔）。
**這不是把不合格的塞進來，是新定義本來就涵蓋這一種寫入端。**

⚠️ **本項檢查器自己的盲點，據實寫明**：它只看**節層的散文行**（`寫：`／`寫 A：`／`寫入端` 那類），
**看不到寫在表格列「出處」欄裡的寫入端** —— 上表第二列那 4 張就是被它誤報成「找不到」、由本組人工回讀補上的。
**所以上表的 18／2／2 是「機器掃 ＋ 人工判讀」的合成結果，不是純機器輸出。**

⚠️ **另一個盲點（比上一個重要）**：本項查的是「**本表現有文字裡有沒有登記寫入端**」，
**不是**回頭重追每一條寫入鏈。**若某一張表當初登記的寫入端本身就寫錯了，本項查不出來。**
⚠️ **本項是本組單組實測，沒有第二組看過。**

#### (5) 本節未涵蓋的範圍（分類敘述，不寫全稱句）

- **只重判了客戶點名的 13 列**；`查不到`／`活的`／`未落地` 三桶**未重掃**（候選見 (3)）。
- **沒有補「位元組讀回？」欄**：翻進來的 13 列**沒有填**那一欄，覆蓋率因此由 41/89 變成 41/102（見 §5.3）。
  理由同顆粒度輪：那一欄問的是另一個量測，拿本輪的證據去填等於拿 A 的證據填 B 的欄位。
- **沒有動「誰讀它」欄**：本輪 231 格一格未改（實跑對照，見 §5.4）。
- **沒有重新盤點**：264 列的組成一列未增未減未合併。
- **本節與這 13 列的翻轉，是單組產出，沒有第二組看過。**

---

## 1. Google Sheets（11 張表，分屬 4 本 Sheet ＋ 1 個外部發佈 CSV）

**哪一本是哪一本**（這件事 `ui/tab6_manual.py` 的說明書沒講清楚，見 §6 發現 6）：

| Sheet 本 | 由誰決定 ID | 裡面有哪些分頁 |
|---|---|---|
| **持倉本** | `st.session_state["policy_sheet_id"]`／secret `POLICY_SHEET_ID` | `Policies`、各保單分頁、`_T7_State`、`_Ledgers`、`_持倉總覽` |
| **淨值本** | secret `NAV_SHEET_ID` → 程式內 baked 預設（`services/nav_history_gs.py:73`，**值不列出**） | `nav_history` |
| **選股池本** | secret `POOL_SHEET_ID` → baked 預設（`repositories/pool_repository.py:39`，**值不列出**） | `_fund_pool` |
| **總經本** | secret `macro_weights_sheet_id` | `_macro_weights`、`_portfolio_perf_history` |

### 1.1 `Policies`（v1 平面 schema・持倉本）
分頁名常數：`repositories/policy/_helpers.py:52` `DEFAULT_WORKSHEET = "Policies"`
欄位 SSOT：`ALL_COLS = REQUIRED_COLS + OPTIONAL_COLS`（`_helpers.py:50`）＝ **14 欄**

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `Policies.policy_id` | 活的 | **UI** | `repositories/policy/_helpers.py:26` | REQUIRED；複合主鍵之一 |
| `Policies.policy_name` | 活的 | **UI** | `repositories/policy/_helpers.py:27` | REQUIRED |
| `Policies.fund_url` | 活的 | **UI** | `repositories/policy/_helpers.py:28` | REQUIRED；**實際存的是基金代號**（`ui/helpers/cloud_io.py:244` 寫入 `"fund_url": _code`）；複合主鍵之一 |
| `Policies.invest_twd` | 活的 | **UI** | `repositories/policy/_helpers.py:29` | REQUIRED；TWD **金額**（非權重）；同 `(policy_id, fund_url)` 多筆 → 加總（`repositories/policy/v1.py:243`） |
| `Policies.invest_date` | 活的 | **UI** | `repositories/policy/_helpers.py:30` | REQUIRED；寫入端固定填 `""`（`ui/helpers/cloud_io.py:247`） |
| `Policies.currency` | 活的 | **UI** | `repositories/policy/_helpers.py:31` | REQUIRED |
| `Policies.fx_at_buy` | 活的 | **UI** | `repositories/policy/_helpers.py:32` | REQUIRED；寫入端固定填 `0.0`（`ui/helpers/cloud_io.py:249`） |
| `Policies.notes` | 活的 | **UI** | `repositories/policy/_helpers.py:33` | REQUIRED |
| `Policies.policy_tier` | 活的 | **UI** | `repositories/policy/_helpers.py:40` | OPTIONAL；`core`／`satellite`／`""` |
| `Policies.div_cash_pct` | 活的 | **UI** | `repositories/policy/_helpers.py:41` | OPTIONAL；配息現金給付 %（0~100） |
| `Policies.avg_nav_with_div` | 活的 | **UI** | `repositories/policy/_helpers.py:42` | OPTIONAL；平均買入含息單位成本 |
| `Policies.avg_nav` | 活的 | **UI** | `repositories/policy/_helpers.py:45` | OPTIONAL；平均買入淨值 |
| `Policies.fx_avg` | 活的 | **UI** | `repositories/policy/_helpers.py:46` | OPTIONAL；平均買入匯率 |
| `Policies.units` | 活的 | **UI** | `repositories/policy/_helpers.py:47` | OPTIONAL；持有單位數 |

讀：`repositories/policy/v1.py:39`（`load_policies`）→ `sync_policies_to_portfolio_funds`（`v1.py:201`）
寫：`repositories/policy/v1.py:139,155,157`
📌 **「誰讀它」＝ UI ×14 的出處（2026-09-16）**：`load_policies` 回的整張 DataFrame 進
`ss["policies_df"]`（`ui/helpers/cloud_io.py:459`），再由
`ui/helpers/portfolio/policy_admin_section.py:882` 的 **`st.dataframe(_pdf_cached, …)`** 整張渲染；
`:884-891` 的 `column_order` **逐字列出 v1 的 `policy_name` / `fund_url` / `invest_date` / `fx_at_buy` /
`notes` / `policy_tier`**，`:892-911` 的 `column_config` 給它們中文表頭。
⚠️ **`notes` 特別點名**：它是 14 欄中**唯一沒有被 `sync_policies_to_portfolio_funds` 帶進
`portfolio_funds` 的**（`v1.py:247-256` 的 dict 沒有它），但它**確實在這張表上看得到** ——
**這正是「看欄不看表」會抓到、而「看表」會漏掉的那種差異，方向剛好相反（表層會低估它）。**

### 1.2 各保單分頁（per-policy worksheet）— **同一張分頁有兩套 schema，看寫入端是誰**

⚠️ **這是本次盤點最重要的結構發現**：`repositories/policy/v2.py` 裡住著**兩套完全不同的欄位定義**，
兩套都在 production 寫同一種分頁（分頁名 = `policy_id`）：

#### 1.2a 舊路徑（v1 的 14 欄，主鍵 `fund_url`）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `<保單分頁>.(v1 14 欄同 §1.1)` | 活的 | **UI** | `repositories/policy/v2.py:282`（`cols = ALL_COLS`） | `upsert_fund_in_policy`（`v2.py:240`）以 **`fund_url` 為主鍵** upsert；表頭缺欄會自動升級（`v2.py:275`） |

production 寫入點：`ui/helpers/cloud_io.py:243`、`ui/tab3_portfolio.py:1575`、`ui/tab3_t7_ledger.py:1043`
production 讀取點：`ui/helpers/cloud_io.py:448`（`load_all_policy_worksheets`，`v2.py:179`）

#### 1.2b 新路徑（v2 的 10 欄，主鍵 `fund_code`）

欄位 SSOT：`ALL_COLS_V2`（`repositories/policy/v2.py:506`）＝ **10 欄**；中文表頭 `ZH_HEADERS_V2`（`v2.py:527`）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `<保單分頁v2>.policy_id` | 活的 | **UI** | `repositories/policy/v2.py:507` | 中文表頭「保單編號」；USER_INPUT |
| `<保單分頁v2>.fund_code` | 活的 | **UI** | `repositories/policy/v2.py:508` | 「基金代號」；USER_INPUT；**v2 偵測鍵**（`v2.py:499` 註） |
| `<保單分頁v2>.fund_name` | 活的 | **UI** | `repositories/policy/v2.py:509` | 「基金名稱」；AUTO（`v2.py:557`） |
| `<保單分頁v2>.currency` | 活的 | **UI** | `repositories/policy/v2.py:510` | 「幣別」；AUTO |
| `<保單分頁v2>.tier` | 活的 | **UI** | `repositories/policy/v2.py:511` | 「級別」；`core`／`satellite`／`""`；AUTO |
| `<保單分頁v2>.invest_twd` | 活的 | **UI** | `repositories/policy/v2.py:512` | 「淨投資金額」；USER_INPUT |
| `<保單分頁v2>.div_cash_pct` | 活的 | **UI** | `repositories/policy/v2.py:513` | 「現金給付%」0~100；USER_INPUT |
| `<保單分頁v2>.units` | 活的 | **UI** | `repositories/policy/v2.py:514` | 「持有單位數」；AUTO |
| `<保單分頁v2>.avg_nav` | 活的 | **UI** | `repositories/policy/v2.py:515` | 「平均買入單位成本」；USER_INPUT |
| `<保單分頁v2>.avg_fx` | 活的 | **UI** | `repositories/policy/v2.py:516` | 「平均買入匯率」；USER_INPUT |

寫：`ui/helpers/cloud_io.py:88,186`、`ui/helpers/v2_editor.py:455,529`（→ `write_policy_v2`，`v2.py:794`）
讀：`ui/helpers/cloud_io.py:156,325`、`ui/helpers/v2_editor.py:262`、`ui/helpers/portfolio/policy_admin_section.py:719`
退役但常數仍在：`item_type`／`avg_nav_with_div`／`amount`（中文別名 `_LEGACY_ZH_ALIASES_V2`，`v2.py:542`）→ **已停用**（`v2.py:501` 明文「v19.436 精簡：13 → 10 欄」；讀回時自動丟棄）

### 1.3 `_T7_State`（持倉本）
分頁名：`repositories/snapshot_repository.py:34`；欄位 `SNAPSHOT_COLS`（`:51`）＝ **6 欄**

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `_T7_State.pk_str` | 活的 | **UI** | `repositories/snapshot_repository.py:52` | 主鍵，格式 `"{policy_id}::{fund_code}"`（`repositories/policy/v1.py:221`） |
| `_T7_State.fund_code` | ~~活的~~ → **只寫不讀** | **無** | `repositories/snapshot_repository.py:53` | **2026-09-16 欄層翻轉**：`load_all_ledgers_snapshot`（`repositories/snapshot_repository.py:320-321`）只取 `pk_str` 與 `ledger_json`，本欄無任何讀取端 |
| `_T7_State.currency` | ~~活的~~ → **只寫不讀** | **無** | `repositories/snapshot_repository.py:54` | **2026-09-16 欄層翻轉**：同上，`load_all_ledgers_snapshot` 不取本欄 |
| `_T7_State.policy_id` | ~~活的~~ → **只寫不讀** | **無** | `repositories/snapshot_repository.py:55` | **2026-09-16 欄層翻轉**：同上，`load_all_ledgers_snapshot` 不取本欄 |
| `_T7_State.ledger_json` | 活的 | **UI** | `repositories/snapshot_repository.py:56` | **整本帳本的 JSON blob**（`snapshot_repository.py:152` = `json.dumps(Ledger.to_dict())`）。內含 `fund_code`／`currency`／`transactions[]`（8 欄）／`position`（5 欄）—— 見 §3.1 |
| `_T7_State.updated_at` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/snapshot_repository.py:57` | ISO ts（本機時區，`snapshot_repository.py:131`）　**2026-09-16 欄層翻轉**：`get_state_metadata`（`:350-355`）讀本欄 → `_sheet_stats["last_sync"]`（`ui/helpers/portfolio/policy_admin_section.py:833`），而 `_sheet_stats` **全 repo 無讀取端**（該檔 `:843-845` 自陳「只有這一行寫、無人讀」） |

寫：`ui/helpers/cloud_io.py:282`、`ui/tab3_t7_ledger.py:457`（`save_all_ledgers_snapshot`，`:113`；**先 `ws.clear()` 再整批覆寫**，`:158`）
讀：`ui/helpers/cloud_io.py:477`、`ui/tab3_t7_ledger.py:476`（`load_all_ledgers_snapshot`，`:296`）
📌 **「誰讀它」的逐欄依據（2026-09-16）**：`load_all_ledgers_snapshot` 的迴圈
（`repositories/snapshot_repository.py:319-328`）**只碰兩個 key** ——
`rec.get("pk_str")`（`:320`）與 `rec.get("ledger_json")`（`:321`）。
`pk_str` 成為 `st.session_state["t7_ledgers"]` 的 key，被
`ui/tab3_t7_ledger.py:1937` 這種 `t7_ledgers[fund_pk_str(f)]` 的比對用來挑出要顯示的帳本 → **UI**；
`ledger_json` 反序列化成 `Ledger` 後直接上 T7 面板 → **UI**。
**其餘三欄（`fund_code` / `currency` / `policy_id`）只是給人看的冗餘欄，程式一次都沒讀。**
另一個讀取端 `get_state_metadata`（`:332`）只用 `updated_at`（`:350-352`）與 `len(df)`。

### 1.4 `_Ledgers`（持倉本）
分頁名：`repositories/ledger_repository.py:32`；欄位 `LEDGER_COLS`（`:34`）＝ **9 欄**

> 📌 **本表有「位元組讀回？」欄**（§0.2）：9 欄全部是「**是**」——
> `load_all_ledgers` 真的把整張表讀進 DataFrame、9 欄全部做過型別正規化，**只是結果沒人用**。

| 表 . 欄位 | 狀態 | 位元組讀回？ | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|---|
| `_Ledgers.policy_id` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:35` | 複合主鍵之一 |
| `_Ledgers.date` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:36` | `YYYY-MM-DD`（`:65` 正規化；讀回端 `:136` 再正規化一次） |
| `_Ledgers.code` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:37` | 讀回端 `:137-138` `fillna("").astype(str).str.strip()` |
| `_Ledgers.action` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:38` | 已知值 `buy`/`sell`/`dividend`/`fee`/`fx`（`:46`）；非列舉值不擋 |
| `_Ledgers.units` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:39` | 讀回端 `:132` `_norm_float` |
| `_Ledgers.nav_at_action` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:40` | 讀回端 `:133` `_norm_float` |
| `_Ledgers.twd` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:41` | 讀回端 `:134` `_norm_float` |
| `_Ledgers.fee` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:42` | 讀回端 `:135` `_norm_float` |
| `_Ledgers.note` | 只寫不讀 | **是** | **內部** | `repositories/ledger_repository.py:43` | |

寫：`ui/tab3_t7_ledger.py:699`（`append_ledger_row`）、`ui/tab3_t7_ledger.py:1030`（`replace_ledgers_for_policy`）
讀：`ui/helpers/portfolio/policy_admin_section.py:825`（`load_all_ledgers`，`ledger_repository.py:111`）
⚠️ 兩個寫入端都在 **OAuth 登入分支**內（`ui/tab3_t7_ledger.py:693,1027` 先建 OAuth client）

> ⚠️ **2026-09-16 狀態更正：上表 9 列由 ~~「活的」~~ 改為「只寫不讀」；同批新增「位元組讀回？」＝ 是**
> （**有意識的政策變更，不是漏刪** · 日期 **2026-09-16** · **決策者：客戶** ·
> 起因：盤點稽核組矛盾 3 · 執行者：盤點生產組）
>
> **依據是客戶的裁定，不是新發現的事實** —— 下面三個事實在上一輪就已經實測完畢、**一字未改**；
> 上一輪唯一缺的是「該用哪一把尺」，客戶已於 2026-09-16 裁定：
> **「有 production 讀取」＝ 資料到得了消費者**（§0.1）。依此尺，本節 9 欄 ＝ **只寫不讀**。
>
> **事實 1：上面那個「讀」，取回的資料到不了任何畫面。** 逐行 `awk 'NR==N'` 實測：
> `ui/helpers/portfolio/policy_admin_section.py:825` `_led_df = load_all_ledgers(_cli, _sheet_id)`
> → `:826` `_led_ct = len(_led_df)` → `:829-834` 只把 `len()` 連同另外三個數字塞進
> `st.session_state["_sheet_stats"]`。**9 個欄位的內容一個都沒有被使用。**
> **事實 2：`_sheet_stats` 全 repo 零讀取端。** 本組實測 `git grep -n "_sheet_stats" origin/main -- .`
> → `.py` 內僅 `policy_admin_section.py` 的 `:819`（註解）／`:820`（`def _refresh_sheet_stats`）／
> `:829`（**唯一寫入**）／`:843`（註解）／`:871`（呼叫 `_refresh_sheet_stats`），**沒有任何讀取**；
> 另 3 筆在 `ARCHITECTURE.md:1046`、`SPEC.md:1312,1321`，屬文件。
> 該檔 `:843-848` 的註解**自己就寫著**：「本處寫入的 `_sheet_stats` **全 repo 沒有任何讀取端**
> （AST + grep 窮舉：只有這一行寫、無人讀）—— 它的 3 個 `st.metric` 消費者已於 v18.169 隨
> 「📋 保單清單」區塊移除」。
> **事實 3：`ui/tab3_portfolio.py:42` 只 import、從未呼叫。** 本組實測該檔內 `load_all_ledgers`
> 這個 token **只出現 1 次**，就是 `:41-43` 那個 import 區塊本身。
>
> ⛔ **上一輪的舊表述（原文加刪除線保留，不是漏刪）**：
> ~~「為什麼狀態欄不改：這一條的爭點是『定義』，不是『事實』。§0 只寫『有 production 讀取』，
> 沒有界定『讀取』是指『位元組被讀回來』還是『資料到得了消費者』：
> 讀法甲（位元組）⇒ 活的（現行值）；讀法乙（到得了消費者）⇒ 只寫不讀。
> ……該用哪一把尺是治理決定，不在本組權限內 → 已列入 `CONTRADICTIONS_RESOLUTION.md`「不解」區（矛盾 3），
> 待客戶／總管裁定。」~~
>
> **兩邊理由並陳**：**舊處置仍然成立** —— 那確實是治理決定，盤點組自行拍板才是錯的，
> 它把爭點如實送上去、狀態不動，**做法正確**。**被取代的只是「懸而未決」這個狀態**：
> 客戶已經裁了（§0.1），讀法乙勝出，於是同一段事實直接推出「只寫不讀」。
>
> ✅ **本輪同時解掉了那個尺不一致的問題**：§1.4 與 §2.3 現在用的是**同一把尺（讀法乙）**，
> 兩節都是「只寫不讀」。差別由新欄呈現 —— **兩節的「位元組讀回？」也都是「是」**
> （§2.3 的位元組是被 `record_fund` 自己的 read-modify-write 讀回來的，見該節）。
>
> ⚠️ **本更正**只改「有沒有人把它讀回來用」，不改「有沒有寫進去」 ——
> 逐筆交易確實一列一列被 `append_ledger_row` 寫上雲（`repositories/ledger_repository.py:165`），
> **9 欄的資料在 Sheet 上是完整的**。
> 🔧 **對新 UI 的意思（這才是這一節現在的用處）**：**「只寫不讀 ＋ 是」是最便宜的那一種** ——
> `load_all_ledgers` 已經把 9 欄整張讀成 DataFrame 並做完型別正規化（`:127-139`），
> 現在只被 `len()` 用掉。**要做「交易流水」畫面，不必新寫任何取數，接上那個 DataFrame 即可。**
> ⚠️ 但要注意它的**兩個寫入端都在 OAuth 登入分支內**（見上），**未登入時這張表不會累積**。

### 1.5 `_持倉總覽`（持倉本）— **只寫不讀**
分頁名：`repositories/snapshot_repository.py:39`；欄位 `HOLDINGS_COLS`（`:41`）＝ **13 欄**（中文表頭）

> 📌 **本表有「位元組讀回？」欄**（§0.2）：13 欄全部是「**否**」——
> **連讀取函式都不存在**（`repositories/snapshot_repository.py` 的 `def` 全列裡沒有任何 holdings 的 `load_*`）。
> **這就是它與 §1.4 `_Ledgers` 的差別**：兩者狀態同為「只寫不讀」，但這一張要**從零寫一條讀取路徑**。

| 表 . 欄位 | 狀態 | 位元組讀回？ | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|---|
| `_持倉總覽.保單號碼` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:42` | 來源 `funds_lookup[pk].policy_id` 或 pk 前綴（`:247`） |
| `_持倉總覽.基金代碼` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:42` | |
| `_持倉總覽.基金名稱` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:42` | 來源 `portfolio_funds[].name` |
| `_持倉總覽.幣別` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:42` | |
| `_持倉總覽.級別` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:42` | 值為中文「核心」／「衛星」／`""`（`:250`）**與別處的 `core`/`satellite` 不同字面** |
| `_持倉總覽.持有單位數` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:43` | round 4（`:258`） |
| `_持倉總覽.平均成本淨值` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:43` | round 4 |
| `_持倉總覽.平均含息成本` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:43` | round 4 |
| `_持倉總覽.平均匯率` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:43` | round 4 |
| `_持倉總覽.投資金額(TWD)` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:44` | int |
| `_持倉總覽.現金給付%` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:44` | |
| `_持倉總覽.累積已領配息(TWD)` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:44` | round 0 |
| `_持倉總覽.更新時間` | 只寫不讀 | 否 | **無** | `repositories/snapshot_repository.py:44` | `%Y-%m-%d %H:%M`（UTC+8，`:235`） |

寫：`ui/helpers/cloud_io.py:289`、`ui/tab3_t7_ledger.py:461`（`save_holdings_overview`，`:222`；**clear + 整批覆寫**）
讀：**查不到**。查法：`git grep -n '持倉總覽\|HOLDINGS_TAB\|HOLDINGS_COLS' origin/main -- '*.py'`，
命中 27 行逐一判讀，**沒有任何 `load_*` 函式**（對照組 `T7_STATE_TAB` 同一條指令有 `load_all_ledgers_snapshot`）。
這是 by design——`ui/tab6_manual.py:641` 自陳「給**人看**的完整成本帳本」。

> 📌 **2026-09-16 補：「位元組讀回？＝ 否」的實測（本輪重跑，不是沿用上一輪的敘述）**
> `git show origin/main:repositories/snapshot_repository.py | grep -n '^def '` → 全部 7 個 def：
> `_with_quota_retry`(`:67`) / `ensure_state_worksheet`(`:75`) / `save_all_ledgers_snapshot`(`:113`) /
> `_ensure_overview_worksheet`(`:192`) / **`save_holdings_overview`(`:222`)** /
> `load_all_ledgers_snapshot`(`:296`) / `get_state_metadata`(`:332`)。
> **holdings 只有 `save_*`，沒有 `load_*`；`_T7_State` 才有 `load_all_ledgers_snapshot`（同檔內的正對照）。**
> 負控：`<負控串>` 同型指令 → 0 命中（exit=1）。
> **狀態欄一字未動**（本來就是「只寫不讀」，與客戶裁定同向）；本輪只新增一欄。

### 1.6 `nav_history`（淨值本）
分頁名：`services/nav_history_gs.py:65`；欄位 `_NAV_HEADERS`（`:66`）＝ **7 欄**

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `nav_history.code` | 活的 | **UI** | `services/nav_history_gs.py:66` | 複合主鍵之一 |
| `nav_history.date` | 活的 | **UI** | `services/nav_history_gs.py:66` | 複合主鍵之一；去重靠 `(code, date)` |
| `nav_history.nav` | 活的 | **UI** | `services/nav_history_gs.py:66` | |
| `nav_history.fund_name` | ~~活的~~ → **只寫不讀** | **內部** | `services/nav_history_gs.py:66` | 可留空，系統補（`ui/tab6_manual.py:638`）　**2026-09-16 欄層翻轉**：`load_points`（`services/nav_history_gs.py:808`）把本欄讀進 dict，但 4 個消費端（`load_series` / `coverage_status` / `fundclear_backfill:188-190` / `nav_history_store:715`）**沒有一個取用它** |
| `nav_history.source` | ~~活的~~ → **只寫不讀** | **內部** | `services/nav_history_gs.py:66` | **2026-09-16 欄層翻轉**：同上（`:809` 讀進 dict，無消費端取用） |
| `nav_history.recorded_at` | ~~活的~~ → **只寫不讀** | **內部** | `services/nav_history_gs.py:66` | **2026-09-16 欄層翻轉**：同上（`:810` 讀進 dict，無消費端取用） |
| `nav_history.currency` | 活的 | **UI** | `services/nav_history_gs.py:67` | 第 7 欄，後加；舊分頁靠 `_ensure_grid_width`（`:316`）補欄寬 |

寫（production）：`ui/helpers/nav_history_hook.py:138`、`ui/tab_manage.py:615`（**別名匯入 `_gs_append`**，`:614`）、
`ui/tab5_data_guard.py:1851`（`import_csv_text`）
⚠️ `services/fundclear_backfill.py:303` 也呼叫 `append_points`，但它住在 `download_and_store`（`:237`）裡，
**那支零 caller**（見 §2.6）→ **這條寫入路徑目前打不開**
寫（CI 排程）：`scripts/weekly_nav_backfill.py`（`.github/workflows/weekly_nav_backfill.yml`）
寫（**使用者自己的 NAS／本機 crontab —— 不在本 repo 的 CI 內**）：`scripts/accumulate_nav_tw.py`
（`scripts/accumulate_nav_tw.py:73-74` `from services.nav_history_gs import append_points` / `append_fn = append_points`）

> ⚠️ **2026-09-16 事實更正：`scripts/accumulate_nav_tw.py` 從「寫（CI 排程）」移出**
> （**有意識的更正，不是漏刪** · 決策者：盤點生產組 · 起因：盤點稽核組矛盾 2）
> **本組實測（指令與真實輸出照抄）**：
> `git grep -n "accumulate_nav_tw" origin/main -- '.github/workflows/*'` → **無輸出，exit=1（0 命中）**；
> **正控** `git grep -n "export_fund_db" origin/main -- '.github/workflows/*'` → `export_db.yml:39: run: python scripts/export_fund_db.py --no-live --output fund.db`；
> **負控** `git grep -n "<負控串>" origin/main -- '.github/workflows/*'` → **0 命中（exit=1）**。
> repo 內三處都說它跑在**使用者的 NAS／本機 crontab**，逐行 `awk 'NR==N'` 原文（未經任何 `grep -v`）：
> `docs/NAV_NAS_CRON_SETUP.md:89` → `30 18 * * 1-5  cd /path/to/my-Fund-dashboard && python scripts/accumulate_nav_tw.py >> /var/log/nav_accumulate.log 2>&1`
> `STATE.md:1404` → ``  `30 18 * * 1-5 cd <repo> && python scripts/accumulate_nav_tw.py`(台灣時間傍晚,NAV T+1 多傍晚更新)。 ``
> `scripts/accumulate_nav_tw.py:18` → 同一條 cron 行（在 module docstring 的「4. cron」段內）。
> **舊表述在寫下當時錯在哪**：它把「有 cron」讀成「有 CI 排程」。**兩者的差別是誰在跑** ——
> GitHub Actions 在 repo 裡看得到、查得到上次執行時間；NAS crontab **不在 repo、也不在 Actions**。
> ⚠️ **這一條的份量**：`nav_history` 是本表標為「**唯一不可再生**」的那張表（`ui/tab6_manual.py:652`）。
> 它每天有沒有在累積，**取決於使用者 NAS 上那條 crontab 有沒有在跑** —— **本盤點查不到它現在的狀態。**
> ⚠️ **本節 7 欄的「活的」不因此更動**：`nav_history` 另有 3 個 production UI 寫入端
> （`ui/helpers/nav_history_hook.py:138`、`ui/tab_manage.py:615`、`ui/tab5_data_guard.py:1851`）
> ＋ 1 個真正掛 workflow 的 CI 寫入端（`scripts/weekly_nav_backfill.py`）。**改的是那一行的分類，不是狀態。**

讀：`services/fund_service.py:1095`（`load_series`）、`ui/tab5_data_guard.py:76`（**別名 `_c`**）、
`ui/views/page_05_settings.py:1313`（**別名 `fetch_nav_coverage`**）、`scripts/watchlist_push.py:167`
⚠️ **本表是唯一不可再生的資料**（`ui/tab6_manual.py:652` 明文）
📌 **「誰讀它」的逐欄依據（2026-09-16）**：`load_points`（`services/nav_history_gs.py:803-814`）
把 7 欄全部讀進 dict，但**四個消費端只取其中四欄** ——
`load_series`（`:832-833`）用 `date` / `nav`，`:848` 用 `currency`（→ `reconcile_row_currencies`
→ `s.attrs["currency"]` → `services/fund_service.py:1158` 決定 merged 序列的幣別宣告，**影響畫面上的
metrics**）；`coverage_status`（`:885-888`）用 `code` / `date`；
`services/fundclear_backfill.py:188-190` 用 `date` / `nav`；
`services/nav_history_store.py:717-718` 用 `code`（分組後整包傳給 `analyze_backfill_conflict`，
而那支也只讀 `date` / `nav`）。
→ **`fund_name` / `source` / `recorded_at` 讀得回來，但沒有一個消費端取用。**

### 1.7 `_fund_pool`（選股池本）
分頁名：`repositories/pool_repository.py:26`；欄位 `_HEADERS`（`:32`）＝ **10 欄**（＝ `PoolEntry` 欄位，`:46`）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `_fund_pool.code` | 活的 | **UI** | `repositories/pool_repository.py:47` | 主鍵 |
| `_fund_pool.name` | 活的 | **UI** | `repositories/pool_repository.py:48` | |
| `_fund_pool.category` | 活的 | **UI** | `repositories/pool_repository.py:49` | |
| `_fund_pool.type_override` | 活的 | **UI** | `repositories/pool_repository.py:50` | 合法值 `震盪`／`成長`／`""`（`:40`） |
| `_fund_pool.note` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/pool_repository.py:51` | **2026-09-16 欄層翻轉**：只在 `to_row`（`:70`）序列化與 `switch_advisor_section.py:591` 原值保留時出現；選股池表格（`:547-550`）**不含本欄**，新增表單（`:575-580`）也沒有輸入框 |
| `_fund_pool.added_at` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/pool_repository.py:52` | ISO date（UTC+8，`:89`）　**2026-09-16 欄層翻轉**：只在 `to_row`（`:70`）、`upsert` 補預設（`:156-157`／`:434-435`）、`switch_advisor_section.py:592` 原值保留；表格不顯示 |
| `_fund_pool.status` | 活的 | **UI** | `repositories/pool_repository.py:53` | 狀態機 `WATCHING`/`TRIGGERED`/`HOLDING`/`CLOSED`（`:42`）；非法值強制回 `WATCHING`（`:63`） |
| `_fund_pool.isin` | 活的 | **UI** | `repositories/pool_repository.py:55` | v19.472 併入「代號對照表」；`resolve_isin`（`:665`）供補淨值用 |
| `_fund_pool.currency` | 活的 | **UI** | `repositories/pool_repository.py:56` | `resolve_currency`（`:671`） |
| `_fund_pool.morningstar_secid` | 活的 | **UI** | `repositories/pool_repository.py:57` | `resolve_secid`（`:656`）／`set_secid`（`:677`）回存 |

寫：`ui/helpers/fund_grp_health/switch_advisor_section.py:599,616,623,651`、`scripts/fill_pool_currency.py:88`（workflow `fill_pool_currency.yml`）
讀：`ui/tab_manage.py:152,227,346`、`ui/tab_fund_grp_health.py:489`、`ui/tab_batch_analysis.py:241`、
`ui/tab3_portfolio.py:1985`（**別名 `_lp_h`**）、`repositories/fund/sources.py:964,972,974,1696,1716,1718,1958,1971`（多處**別名匯入**）、
`repositories/fund/fund_orchestration.py:77,80,109`、`services/nav_history_store.py:551,790`、
`scripts/weekly_nav_backfill.py:73`、`scripts/weekly_switch_notify.py:305`
⚠️ **`_fund_pool` 是全站「code → ISIN／晨星 secId／幣別」的唯一對照表**（`repositories/pool_repository.py:31` 明文「退役獨立 id_map_repository」）
📌 **「誰讀它」的逐欄依據（2026-09-16）**：選股池表格
（`ui/helpers/fund_grp_health/switch_advisor_section.py:547-550` 的
`pd.DataFrame([{...} for e in pool])` ＋ `:551` `st.dataframe`）顯示
**代號／名稱(自動)／ISIN／幣別(自動)／補淨值／手動型態／狀態** 七格；
其中「補淨值」由 `_navfill(e)`（`:539-544`）依 `morningstar_secid` → `isin` 判讀，
`category` 則走 `_assemble_rows` 的 `基金類別`（`:78`／`:173`）。
→ **8 欄 UI。`note` / `added_at` 兩欄不在表格裡，新增表單（`:575-580`）也只收「基金代號」與「ISIN」，
它們只在 `to_row`（`:69-71`）序列化與 upsert 時原值保留（`:591-592`）。**

### 1.8 `_portfolio_perf_history`（總經本）
分頁名：`repositories/portfolio_perf_repository.py:38`；欄位 `_HEADERS`（`:39`）＝ **15 欄**
（`PerfSnapshot` 欄位順序由 `:365` 的 `assert` 鎖死）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `_portfolio_perf_history.date` | 活的 | **UI** | `repositories/portfolio_perf_repository.py:51` | ISO date（TW），唯一鍵 |
| `.period_return_pct` | 活的 | **UI** | `repositories/portfolio_perf_repository.py:52` | 缺值存空字串、讀回 `None`（`:30` 明文） |
| `.cagr_pct` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:53` | **2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.cagr_pct\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.ann_vol_pct` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:54` | **2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.ann_vol_pct\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.sharpe` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:55` | **2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。⚠️ **本欄的 grep 不是 0 行，是 5 行，逐行判讀後仍成立**：`git grep -n "\.sharpe\b" origin/main -- '*.py' ':!tests/*'` → **5 行**，全部是 `services/health/grade.py:201`／`ui/helpers/fund_grp_health/ai.py:126`／`quality.py:8`／`unified.py:36`／`ui/views/page_02_health.py:1396` 的**註解或 docstring**，講的是**單一基金的 `metrics.sharpe`**（`calc_metrics` 算的），**不是 `PerfSnapshot.sharpe`（投組層快照）**。**同名不同物，典型的子字串陷阱。****本表其餘 12 欄該指令確實回 0 行。** |
| `.max_drawdown_pct` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:56` | **2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.max_drawdown_pct\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.n_funds` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:57` | int　**2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.n_funds\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.total_cost_twd` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:58` | 成本基礎 Σ `invest_twd`（**非市值**）　**2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.total_cost_twd\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.is_equal_weight` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:59` | Σ invest_twd ≤ 0 時退等權的旗標　**2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.is_equal_weight\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.weights_hash` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:60` | **2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.weights_hash\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.weights_json` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:61` | 歸一化權重 JSON　**2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.weights_json\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.coverage_start` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:62` | **2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.coverage_start\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.coverage_end` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:63` | **2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.coverage_end\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.n_days` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:64` | int　**2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.n_days\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |
| `.recorded_at` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/portfolio_perf_repository.py:65` | ISO ts（UTC）　**2026-09-16 欄層翻轉**：唯一消費端 `ui/helpers/fund_grp_health/switch_advisor_section.py:474,480-481` 只用 `date` 與 `period_return_pct`；本欄只經 `getattr(self, h) for h in _HEADERS`（`:71`）與 `dict(zip(_HEADERS, row))`（`:78`）原樣往返，`append_snapshot` 再以 `asdict(s)`（`:192`）寫回。實測 `git grep -n "\.recorded_at\b" origin/main -- '*.py' ':!tests/*'` → **0 行** |

寫：`ui/helpers/fund_grp_health/switch_advisor_section.py:343`（`append_snapshot`，`:350`）
讀：`ui/helpers/fund_grp_health/switch_advisor_section.py:460`（`load_snapshots`，`:341`）
⚠️ 與 `_macro_weights` **同一本 Sheet**（`repositories/portfolio_perf_repository.py:213` 用 `macro_weights_sheet_id`）

> 📌 **2026-09-16 顆粒度輪：本表 15 欄中 13 欄由 ~~「活的」~~ 翻成「只寫不讀」**
> （**有意識的政策變更，不是漏刪** · 日期 **2026-09-16** · **決策者：客戶** ·
> 依據：客戶裁示「顆粒度：看欄，不看表」，§0.3 · 執行者：盤點生產組）
>
> **這一張就是客戶那句裁示的示範案例，所以把鏈路完整寫出來**：
> **唯一的讀取端**是 `ui/helpers/fund_grp_health/switch_advisor_section.py:460` `load_snapshots()`，
> 回來之後**只有三處用到欄位值**（逐行 `awk 'NR==N'` 實測）：
> `:474` `f"…（最新 {_last.date}）"`、`:480` `[s.period_return_pct for s in _snaps]`、
> `:481` `index=[s.date for s in _snaps]`。**其餘 13 欄一次都沒有被取用。**
> 它們的來回路徑是**泛型的**、不碰個別欄名：
> 寫 `[_cell(getattr(self, h)) for h in _HEADERS]`（`repositories/portfolio_perf_repository.py:71`）、
> 讀 `dict(zip(_HEADERS, row))`（`:78`）、再寫回 `asdict(s)`（`:192`）。
> **在表層它們是「活的」；在欄層，13 欄是「只寫不讀」。**
>
> ⚠️ **這不代表那 13 欄沒有價值** —— 它們**真的被存下來了、也真的讀得回來**，
> 缺的只是一個用它們的畫面。對重新設計 UI 而言，這是**最便宜的一類**：
> 資料已經在了（每按一次快照鈕就多一列），**接個消費端就能畫**。
> ⚠️ **同一組欄位在 §2.5 `cache/portfolio_perf/perf_history.json`（本地後端）是「一列代表 15 欄」的群組列，
> 依 §0.3 的群組列規則標「UI」、狀態未翻** —— **兩節不是互相矛盾，是顆粒度不同。**

### 1.9 `_macro_weights`（總經本）— **只讀不寫**
分頁名：`services/macro/weights_store.py:34`；格線 `A1:C3`

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `_macro_weights.A(slot)` | ~~只讀不寫~~ → **只寫不讀** | **無** | `services/macro/weights_store.py:29`（schema 註解） | 只有 `active` 一列（row 3，`:35`）　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`_macro_weights` 分頁的 **A3 儲存格**，由**使用者自己在 Google Sheets 上手填**（本節「寫：**查不到**」＝ 沒有程式寫入端；實測 `git grep -n "save_active" origin/main -- '*.py'` → **0 命中（exit=1）**，正控 `load_active` → 11 命中）。**為什麼沒有畫面消費者**：唯一讀取端 `_gs_load`（`:217`）只讀 `ws.acell(f"B{row}")` —— **只碰 B 欄**；`row` 由程式常數 `_GS_SLOT_ROWS`（`:213`）決定，**不是**從 A 欄查出來的，A 欄因此從未被讀回。 |
| `_macro_weights.B(payload_json)` | 只讀不寫 | **UI** | `services/macro/weights_store.py:217`（`ws.acell(f"B{row}")`） | JSON；必要鍵 `version`+`indicators`（`:25`、`:232`） |
| `_macro_weights.C(updated_at)` | ~~只讀不寫~~ → **只寫不讀** | **無** | `services/macro/weights_store.py:29` | **2026-09-16 客戶裁定讀法 A**：**值在哪**：`_macro_weights` 分頁的 **C3 儲存格**，同 A 欄 —— 人工在 Google Sheets 上手填，程式沒有寫入端。**為什麼沒有畫面消費者**：`_gs_load`（`:217`）只讀 B 欄，C 欄從未被讀回；模組內也沒有任何地方取用它。 |

讀：`services/macro/weights_store.py:206`（`_gs_load`）← `load_active`（`:239`）
← `apply_weight_overrides`（`services/macro/composite_score.py:124`、`services/macro/causal_sankey.py:606`、`services/realtime_signal.py:75`）、
`get_verdict_cutoffs`（`composite_score.py:270,300`、`services/macro/explain.py:242`）、
`get_phase_thresholds`（`services/calibration/macro_score.py:303`）
寫：**查不到。** 查法：`grep -rn '_gs_get_worksheet' --include=*.py .`（排除 tests）→
只有 `:124` 定義、`:209` 註解、`:214` 用 **`for_write` 預設 False** 呼叫。
`for_write=True` 的分支（`:197` `add_worksheet`）**零 call site**；模組也**沒有 `save_active`**
（`grep -n '^def ' services/macro/weights_store.py` 全列無）。
→ **payload 要使用者自己在 Google Sheets 上手填。**

### 1.10 追蹤清單 CSV（`WATCH_CSV_URL`，外部發佈 CSV，非本 repo 的 Sheet）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `watchlist.代號`（或 `code`/`ticker`…） | 只讀不寫 | **UI** | `scripts/watchlist_push.py:79`（表頭精確比對）、`:81`（substring 退路） | **只讀這一欄**；無表頭時退回全表 token 掃描（`:89`）。消費者：`scripts/weekly_switch_notify.py:314`、`scripts/dividend_calendar_notify.py:225`。workflow `weekly_switch_notify.yml`／`dividend_calendar_notify.yml`／`watchlist_verify.yml` |

---

## 2. 本地檔案（20 組）

### 2.1 `cache/nav/{CODE}.json` — GitHub Actions 預抓的 NAV 種子檔

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `cache/nav/*.code` | ~~只讀不寫~~ → **只寫不讀** | **無** | `scripts/fetch_nav_cache.py:542` | **2026-09-16 客戶裁定讀法 A**：**值在哪**：`cache/nav/TLZF9.json` —— **已 commit 進 repo**（實測頂層鍵恰為 `code`／`fund_name`／`updated_at`／`source`／`count`／`history` 六個，本欄真的在檔案裡）；寫入端 `scripts/fetch_nav_cache.py::save_cache` 是**手動 CLI**（8 個 workflow 無 `fetch_nav_cache.yml`）。**為什麼沒有畫面消費者**：唯一讀取端 `_src_cache_files`（`repositories/fund/sources.py:1221`）只取 `data.get("history", [])`（`:1236`）與 `data.get("updated_at", "")`（`:1246`）——**本欄從未被讀回**。 |
| `cache/nav/*.fund_name` | ~~只讀不寫~~ → **只寫不讀** | **無** | `scripts/fetch_nav_cache.py:543` | 樣本檔實測為空字串　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`cache/nav/TLZF9.json` —— **已 commit 進 repo**（實測頂層鍵恰為 `code`／`fund_name`／`updated_at`／`source`／`count`／`history` 六個，本欄真的在檔案裡）；寫入端 `scripts/fetch_nav_cache.py::save_cache` 是**手動 CLI**（8 個 workflow 無 `fetch_nav_cache.yml`）。**為什麼沒有畫面消費者**：唯一讀取端 `_src_cache_files`（`repositories/fund/sources.py:1221`）只取 `data.get("history", [])`（`:1236`）與 `data.get("updated_at", "")`（`:1246`）——**本欄從未被讀回**。 |
| `cache/nav/*.updated_at` | ~~只讀不寫~~ → **只寫不讀** | **內部** | `scripts/fetch_nav_cache.py:544` | UTC ISO；讀取端只取前 10 碼（`repositories/fund/sources.py:1247`）並掛進 `attrs["cache_updated_at"]`（`:1253`）　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`cache/nav/TLZF9.json` —— **已 commit 進 repo**（實測頂層鍵恰為 `code`／`fund_name`／`updated_at`／`source`／`count`／`history` 六個，本欄真的在檔案裡）；寫入端 `scripts/fetch_nav_cache.py::save_cache` 是**手動 CLI**（8 個 workflow 無 `fetch_nav_cache.yml`）。**為什麼沒有畫面消費者**：`_src_cache_files` 讀回它之後只做兩件事 —— `:1247` 把前 10 碼塞進一個 `print()`，`:1253` 掛進 `Series.attrs["cache_updated_at"]`。依 §0.3 的界線，**`print()`／stderr log 不算畫面**；`attrs` 那一條本組也查不到畫面消費端。**這是 §2.1 五列裡唯一被讀回記憶體的一欄**，故「誰讀它」是「內部」而非「無」。 |
| `cache/nav/*.source` | ~~只讀不寫~~ → **只寫不讀** | **無** | `scripts/fetch_nav_cache.py:545` | **2026-09-16 客戶裁定讀法 A**：**值在哪**：`cache/nav/TLZF9.json` —— **已 commit 進 repo**（實測頂層鍵恰為 `code`／`fund_name`／`updated_at`／`source`／`count`／`history` 六個，本欄真的在檔案裡）；寫入端 `scripts/fetch_nav_cache.py::save_cache` 是**手動 CLI**（8 個 workflow 無 `fetch_nav_cache.yml`）。**為什麼沒有畫面消費者**：唯一讀取端 `_src_cache_files`（`repositories/fund/sources.py:1221`）只取 `data.get("history", [])`（`:1236`）與 `data.get("updated_at", "")`（`:1246`）——**本欄從未被讀回**。 |
| `cache/nav/*.count` | ~~只讀不寫~~ → **只寫不讀** | **無** | `scripts/fetch_nav_cache.py:546` | `len(history)`　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`cache/nav/TLZF9.json` —— **已 commit 進 repo**（實測頂層鍵恰為 `code`／`fund_name`／`updated_at`／`source`／`count`／`history` 六個，本欄真的在檔案裡）；寫入端 `scripts/fetch_nav_cache.py::save_cache` 是**手動 CLI**（8 個 workflow 無 `fetch_nav_cache.yml`）。**為什麼沒有畫面消費者**：唯一讀取端 `_src_cache_files`（`repositories/fund/sources.py:1221`）只取 `data.get("history", [])`（`:1236`）與 `data.get("updated_at", "")`（`:1246`）——**本欄從未被讀回**。 |
| `cache/nav/*.history[]` | 只讀不寫 | **UI** | `scripts/fetch_nav_cache.py:547` | 最多 750 筆、日期新→舊（`:562,563`） |
| `cache/nav/*.history[].date` | 只讀不寫 | **UI** | `scripts/fetch_nav_cache.py:562` | |
| `cache/nav/*.history[].nav` | 只讀不寫 | **UI** | `scripts/fetch_nav_cache.py:562` | |

寫：`scripts/fetch_nav_cache.py:539`（`save_cache`）—— ⚠️ **`.github/workflows/fetch_nav_cache.yml` 不存在**
（`git ls-tree -r --name-only origin/main | grep '^.github/workflows/'` 共 8 個，無此檔）→ **只剩手動觸發**
讀：`repositories/fund/sources.py:1221`（`_src_cache_files`）—— `fetch_nav` 全敗後的最終降級
repo 內實體檔：**只有 1 個**（`cache/nav/TLZF9.json`，`.gitignore` 白名單唯一例外）
品質閘：`shared/data_quality.assess_nav_cache_quality`（`sources.py:1273`）＋ `shared/schemas.validate_fund_nav`（`:1281`）

> ⚠️ **2026-09-16 狀態更正：上表 8 列由 ~~「活的」~~ 改為「只讀不寫」**
> （**有意識的更正，不是漏刪** · 決策者：盤點生產組 · 起因：盤點稽核組矛盾 1）
>
> **依據是本表 §0 自己的定義，不是新標準**：§0 寫「**活的** ＝ 有 production 寫入 **且** 有 production 讀取」，
> 且「production ＝ 從 `app.py` 可達的 UI 路徑，或 `.github/workflows/` 有排程的 script；
> **手動 CLI script**（沒有 workflow 掛它）**不當 production**」。
>
> **本組實測（指令與真實輸出照抄）**：
> `git ls-tree -r --name-only origin/main | grep '^.github/workflows/'` → **8 個**：
> `dividend_calendar_notify.yml` / `export_db.yml` / `fill_pool_currency.yml` / `pr-check.yml` /
> `update_macro_history.yml` / `watchlist_verify.yml` / `weekly_nav_backfill.yml` / `weekly_switch_notify.yml`
> —— **無 `fetch_nav_cache.yml`**。
> `git grep -n "fetch_nav_cache" origin/main -- '.github/workflows/*'` → **1 行，而且是註解**：
> `update_macro_history.yml:4:  # 每週日 UTC 00:00（台灣時間 週日 08:00）— 與 fetch_nav_cache 每日 cron 錯開`。
> **正控** `git grep -n "export_fund_db" origin/main -- '.github/workflows/*'` → 命中 `export_db.yml:39`；
> **負控** `git grep -n "<負控串>" origin/main -- '.github/workflows/*'` → **0 命中（exit=1）**。
> → **寫入端不是 production；讀取端 `repositories/fund/sources.py:1221` 是 production** ⇒ 依 §0 ＝ **只讀不寫**。
>
> **本表自己的兩個同型前例本來就是這樣處理的，§2.1 是唯一沒套到的地方**：
> §2.12 `data_cache/macro_thresholds_global.json`（唯一寫入端是手動 CLI、production 有讀）→ **只讀不寫**；
> §2.14 `config/preset_funds.json`（只能人手改檔 ＋ git commit、production 有讀）→ **只讀不寫**。
>
> ⚠️ **這不代表這份快取沒在用。** `cache/nav/TLZF9.json` **已 commit 進 repo**，
> production 的 `fetch_nav` 全敗降級鏈**真的讀得到它**。改掉的是「它怎麼被寫進來」那一半。
> ⛔ **若客戶／總管決定把「已 commit 進 repo 的資料檔」也算 production 寫入**，那是 **§0 定義的修改**：
> 本列會翻回「活的」，**而且 §2.12 / §2.14 必須一起翻**。
> **本組不代為裁決該定義，只據現行 §0 一致套用。**

### 2.2 `cache/nav_history/{CODE}.json` — ⚠️ **同一個路徑有兩個寫入端**

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `cache/nav_history/*.timestamp` | ~~活的~~ → **只寫不讀** | **內部** | 寫 A `repositories/fund/nav_metrics.py:192`／寫 B `services/nav_history_store.py:76` | **TTL 語意不同**：A 判 24h 過期（`nav_metrics.py:159,173`），B **無 TTL**（`nav_history_store.py:69` 明文「manual import 視為永久有效」）　**2026-09-16 欄層翻轉**：`_nav_history_cache_load`（`repositories/fund/nav_metrics.py:173`）只拿它做 24h TTL 判斷，值本身不上畫面 |
| `cache/nav_history/*.dates[]` | 活的 | **UI** | `repositories/fund/nav_metrics.py:193`／`services/nav_history_store.py:77` | `str(d.date())` |
| `cache/nav_history/*.values[]` | 活的 | **UI** | `repositories/fund/nav_metrics.py:194`／`services/nav_history_store.py:78` | float |

寫 A：`repositories/fund/nav_metrics.py:191`（`_nav_history_cache_save`，目錄 `:155`）
寫 B：`services/nav_history_store.py:75`（`_save_cache_series`，目錄 `:34`）
讀 A：`repositories/fund/nav_metrics.py:162`（`_nav_history_cache_load`）；讀 B：`services/nav_history_store.py:50`
⚠️ **`_NAV_HISTORY_CACHE_TTL_SEC = 86400` 是寫死的**（`nav_metrics.py:159`），而 `shared/ttls.py` 最大只到 `TTL_1HOUR`

### 2.3 `cache/fund_history.json` — 「查過哪些基金」歷史

> 📌 **本表有「位元組讀回？」欄**（§0.2）：6 欄全部是「**是**」——
> `record_fund` 自己的 read-modify-write 每次都把整份 JSON 讀回記憶體（`:94` `data = _load()`
> → `:67` `_HIST_FILE.read_text(...)`），而且**讀回來的值真的被用到**
> （`:98` `count` 累加、`:99-101` `sources` 併集、`:103` 名稱保護）——
> **只是這些都只回流到檔案本身，沒有任何一個欄位上得了畫面。**

| 表 . 欄位 | 狀態 | 位元組讀回？ | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|---|
| `fund_history.{CODE}.code` | 只寫不讀 | **是** | **內部** | `services/fund_history.py:107` | 頂層 key 也是 code |
| `fund_history.{CODE}.name` | 只寫不讀 | **是** | **內部** | `services/fund_history.py:108` | 只在原本為空或等於 code 時才覆蓋（`:103`，**要先讀回舊值才判得出來**） |
| `fund_history.{CODE}.first_seen` | 只寫不讀 | **是** | **內部** | `services/fund_history.py:109` | ISO（秒）；已存在時**沿用讀回來的舊值**（`:95-104` 不覆寫） |
| `fund_history.{CODE}.last_seen` | 只寫不讀 | **是** | **內部** | `services/fund_history.py:110` | |
| `fund_history.{CODE}.count` | 只寫不讀 | **是** | **內部** | `services/fund_history.py:111` | 累計查詢次數；`:98` `int(entry.get("count", 0)) + 1` **靠讀回來的舊值累加** |
| `fund_history.{CODE}.sources[]` | 只寫不讀 | **是** | **內部** | `services/fund_history.py:112` | 集合，排序後存；`:99-101` **與讀回來的舊集合取併集** |

檔案常數 `services/fund_history.py:26`；寫 `:76`（`_save`）。
**唯一的寫入觸發點**：`record_fund`（`:82`）—— production 只有 2 處，**兩處都是別名匯入**：
`ui/tab2_single_fund.py:384`（`_rec_fh`，匯入於 `:383`）、`ui/tab3_portfolio.py:1566`（`_rec_fh3`，匯入於 `:1565`）。

**為什麼算「只寫不讀」**：唯一讀到檔案內容的是 `record_fund` 自己的 read-modify-write（`:94` `data = _load()`），
**沒有任何下游消費者**。本模組的顯示端 API `get_history_df`（`:158`）、`clear_history`（`:183`）、
`import_from_csv`（`:198`）、`promote_to_preset`（`:281`）、`export_preset_funds_json`（`:340`）、
`is_preset`（`:352`）**全部零 caller**。
**查法（兩種交叉）**：(a) AST refmap 7 個符號 → 只有 `record_fund` 有 CALL；
(b) `git grep -n 'export_preset_funds_json\|import_from_csv\|clear_history\|get_history_df\|is_preset\|from services.fund_history' origin/main -- '*.py'`（排除 tests）→ 9 行，
7 行是**定義或 docstring**，只有 2 行是真 import（都是 `record_fund`）。
⚠️ 模組 docstring `:10` 還寫著「Tab6 顯示時呼叫 `get_history_df()`」—— **那條線已經斷了**。
`_load_with_defaults`（`:120`）唯一 caller 是 `get_history_df`（`:161`），一併是死碼。

> 📌 **2026-09-16 反向檢查：本節 6 列在客戶新定義下**維持「只寫不讀」**，狀態欄一字未動**
> （執行者：盤點生產組 · 起因：客戶 2026-09-16 裁定「有 production 讀取 ＝ 資料到得了消費者」，§0.1）
>
> **為什麼要反向查**：客戶的裁定是把尺**收緊**（位元組 → 消費者），照理只會讓「活的」往「只寫不讀」掉；
> 但本節上一輪用的**本來就是收緊的那把尺**（原文逐字：「唯一讀到檔案內容的是 `record_fund` 自己的
> read-modify-write，**沒有任何下游消費者**」）。所以要反過來問一次：
> **這半年有沒有人把消費端接回去了？有的話它要翻回「活的」。**
>
> **本組 2026-09-16 實測（`origin/main = 9cbf0377`；指令與真實輸出照抄，未經任何 `grep -v`）**：
> 1. **先列別名 import**（字面掃 `get_history_df(` 會被 `from … import … as _x` 結構性繞過）：
>    `git grep -nE "^[[:space:]]*(from|import).*fund_history" origin/main -- '*.py'`
>    → **只有 2 行**，而且**兩行都是 `record_fund`**：
>    `ui/tab2_single_fund.py:383: from services.fund_history import record_fund as _rec_fh`
>    `ui/tab3_portfolio.py:1565: from services.fund_history import record_fund as _rec_fh3`
>    → **沒有任何檔案 import 過 `get_history_df`，所以它不可能有別名。**
> 2. **再掃符號本身（全檔案類型，不排除任何東西）**：`git grep -n "get_history_df" origin/main -- .`
>    → **29 行**：`services/fund_history.py:10`（docstring）、`:158`（`def`）、
>    其餘 **27 行全在 `tests/test_fund_history.py`**。**production `.py` 0 行。**
> 3. **正控**（證明這條指令會命中）：`git grep -n "record_fund\|_rec_fh" origin/main -- '*.py'`
>    → 命中 `ui/tab2_single_fund.py:383,384`、`ui/tab3_portfolio.py:1565,1566`（**import ＋ 真呼叫各 2 處**）。
>    **負控**：`git grep -n "<負控串>" origin/main -- .` → **0 命中（exit=1）**。
> → **消費端沒有被接回去，`get_history_df` 仍是零 caller ⇒ 維持「只寫不讀」。**
>
> 🔧 **對新 UI 的意思**：本節與 §1.4 `_Ledgers` 是**同一類**（只寫不讀 ＋ 位元組讀回「是」）——
> **改個消費端就能用**，而且這一個更便宜：**現成的 `get_history_df()`（`:158`）已經寫好、回傳排序好的 DataFrame**，
> 缺的只是一個呼叫它的畫面。**模組 docstring `:10` 講的那條線（「Tab6 顯示時呼叫」）是斷掉、不是不存在。**

### 2.4 `cache/fund_pool/pool.json` — `_fund_pool` 的本地後端

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `pool.json[].{PoolEntry 10 欄}` | 活的 | **UI** | `repositories/pool_repository.py:135`（寫）；欄位同 §1.7 | 路徑 `:34`＋`:35`；`get_pool_store`（`:460`）在 GS 未啟用時回本地後端。**二擇一，不是同時寫** |

### 2.5 `cache/portfolio_perf/perf_history.json` — `_portfolio_perf_history` 的本地後端

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `perf_history.json[].{PerfSnapshot 15 欄}` | 活的 | **UI** | `repositories/portfolio_perf_repository.py:174`（寫）；欄位同 §1.8 | 路徑 `:45`＋`:46`；`get_perf_store`（`:333`）二擇一 |

### 2.6 `cache/fundclear/{md5}.json` — FundClear API 回應快取

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `cache/fundclear/*`（整份 API 回應，**無固定 schema**） | 查不到 | **無** | 寫 `repositories/fundclear_offshore.py:105`；讀 `:90`；目錄 `:46` | key ＝ `md5(path+body)[:16]`（`:85`）。**不是表格，是 opaque blob** |

**為什麼是「查不到」**：這層 cache 只在 `repositories/fundclear_offshore.py` 的 `list_organizes` /
`list_funds` / `list_classes`（`:277` `__all__`）被呼叫時才會動，而**那三支的唯一 caller**
是 `services/fundclear_backfill.py:83,90,101` 的 `find_fund_candidates`（`:68`）/ `list_classes_for`（`:98`），
**而那兩支本身零 caller**。
查法（兩種交叉）：(a) AST refmap 5 個符號 → 只有 `rank_candidates` 被同檔 `:95` 呼叫；
(b) `git grep -n 'download_and_store\|find_fund_candidates' origin/main -- '*.py'`（排除 tests）→ 5 行，
**全部是定義、`__all__` 或註解**，沒有一行是呼叫。
→ **FundClear 回填整條鏈（含 `download_and_store` `:237` 那條會寫 `nav_history` 的路）目前沒有入口。**
唯一還活著的是 `analyze_backfill_conflict`（`:104`），被 `services/nav_history_store.py:636,895` 用來做衝突偵測
—— **它不碰網路、也不碰這層 cache**。

### 2.7 `cache/fred_release/{SERIES_ID}.json` — FRED 下次發布日

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `fred_release/*.release_id` | ~~活的~~ → **只寫不讀** | **無** | `repositories/macro/fred.py:136` | **2026-09-16 欄層翻轉**：只在寫入時存（`repositories/macro/fred.py:136`）；讀取路徑（`:82-91`）**只取 `next_release_date`**，本欄從未被讀回 |
| `fred_release/*.next_release_date` | 活的 | **UI** | `repositories/macro/fred.py:137` | ISO date；過期（< today）視同失效（`:88`） |
| `fred_release/*.cached_at` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/macro/fred.py:59` | 30 天 TTL（`:33,48`）　**2026-09-16 欄層翻轉**：`_fred_release_cache_load`（`:47-49`）只拿它算 30 天 TTL |

目錄 `:32`；寫 `:55`；讀 `:40`；消費端 `fred_get_next_release_date`（`:66`）
→ `ui/helpers/io/data_registry.py:208`（函式內 lazy import，**別名 `_fred_next_rel`**）、
`ui/tab5_data_guard.py:1512`（lazy，**別名 `_diag_next_rel`**）
⚠️ **`CLAUDE.md` / `EXCEPTIONS.md` 的 `EX-PASSTHRU-1` 把這兩處記成 `data_registry.py:139` 與
`tab5_data_guard.py:1237` —— 兩個行號在 `origin/main` 上都已漂移**（實測 `:139` 是一個 `}`，
`:1237` 是 `where=_key_where)`）。本表用的是實測後的 `:208` / `:1512`。

### 2.8 `data_cache/*.parquet`（4 個）— cron 累積的歷史資料倉

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `fred_indicators.parquet.date` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:220` | parquet footer 實測 `date/series_id/value`　**2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `fred_indicators.parquet.series_id` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:220` | **實測只有 9 種值**：`BAMLH0A0HYM2/CPIAUCSL/DGS10/DGS2/DGS3MO/DTWEXBGS/M2SL/UNRATE/WALCL`。程式宣告 11 種（`:65-74`）→ **`PPIACO`／`PCOPPUSDM` 兩支查不到任何一列**。⚠️ **2026-09-16 補射程（有意識的更正，不是漏刪）**：這句只描述**這個離線倉**。**production 畫面上的 PPI 與銅價不從這裡來** —— PPI 走 FRED 即時 API （`services/macro/us_indicators.py:907`），銅價走 **Yahoo `HG=F`**（同檔 `:925`，**不是** `PCOPPUSDM`，單位也不同）。**兩支缺席擋住的是離線回測／校準，不是畫面。**完整拆解見 §6 發現 9。⚠️ 另補一筆：即使把這兩支補進 parquet，`services/macro/validation.py::load_indicators_from_parquet` 的對映也**只有 8 個 key，沒有 `PPI` / `COPPER` 分支**（本組逐行讀 `:170-231` 實測），**補了也讀不到**　**2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `fred_indicators.parquet.value` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:220` | **2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `vix_history.parquet.date` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:245` | parquet pandas metadata 實測 `date`(date)/`close`(float64)　**2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `vix_history.parquet.close` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:246` | `^VIX`（`:260`）　**2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `spx_history.parquet.date` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:245` | **2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `spx_history.parquet.close` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:246` | `^GSPC`（`:267`）　**2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `twii_history.parquet.date` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:245` | **2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |
| `twii_history.parquet.close` | ~~活的~~ → **只寫不讀** | **內部** | `scripts/update_macro_history.py:246` | `^TWII`（`:274`）　**2026-09-16 欄層翻轉**：parquet 的 production 讀取端只有兩支 workflow script —— `scripts/export_fund_db.py:61,70`（寫進 `fund.db`，其消費端在下游 repo，§0 射程外）與 `scripts/update_macro_history.py:87`（讀回舊檔做 append 去重）。`services/macro/validation.py::load_indicators_from_parquet` 的唯一上游是`scripts/calibrate_macro_score.py`（**手動 CLI，無 workflow**）。實測 `git grep -nE "data_cache\|parquet" origin/main -- 'ui/*'`（管線符在 md 表格內須跳脫）→ **1 行，且是註解** |

寫：`scripts/update_macro_history.py:93`（workflow `update_macro_history.yml`，有 cron）
讀：`services/macro/validation.py:33`（`DEFAULT_PARQUET_CACHE_DIR`）→ `load_indicators_from_parquet`（`:239`）；
`scripts/export_fund_db.py:61,70`；`scripts/calibrate_macro_score.py:97`
**已 commit 進 repo**（4 檔皆在 `git ls-tree origin/main`）

### 2.9 `data_cache/metadata.json` — parquet 的 last_updated / row_count

> 📌 **本表有「位元組讀回？」欄**（§0.2）：5 欄全部是「**否**」—— 與 §1.5 `_持倉總覽` 同一類，
> **連讀取路徑都不存在**，要做「資料倉最後更新時間」畫面得**從零寫一條讀取路徑**。

| 表 . 欄位 | 狀態 | 位元組讀回？ | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|---|
| `metadata.updated_at` | 只寫不讀 | 否 | **無** | `scripts/update_macro_history.py:367` | repo 實測值 `2026-09-13T02:52:31` |
| `metadata.datasets.{name}.name` | 只寫不讀 | 否 | **無** | `scripts/update_macro_history.py:367` | 4 個 dataset（`:76`） |
| `metadata.datasets.{name}.last_updated` | 只寫不讀 | 否 | **無** | 同上 | |
| `metadata.datasets.{name}.row_count` | 只寫不讀 | 否 | **無** | 同上 | |
| `metadata.datasets.{name}.last_error` | 只寫不讀 | 否 | **無** | 同上 | |

讀：**查不到。** 查法：`git grep -n 'metadata.json\|META_PATH' origin/main -- '*.py'` → 只有 `update_macro_history.py:58,367,369` 與
`repositories/fundclear_offshore.py` 無關命中；對照組同一條指令對 `spx_history` 有 3 個外部讀取端。**已 commit 進 repo**（供人看）

> 📌 **2026-09-16「位元組讀回？＝ 否」的實測（本輪重跑）**：
> `git grep -nE "metadata\.json|META_PATH|META_FILE" origin/main -- '*.py'`（排除 `tests/`）
> → **5 行，全在生產者自己檔內**：`scripts/update_macro_history.py:14`（docstring）／`:58`（`META_PATH` 定義）／
> `:362`（註解）／`:367`（**唯一寫入**）／`:369`（`print`）。**沒有任何一行是讀。**
> **正控**（同型指令）：`git grep -n "spx_history" origin/main -- '*.py'` → 命中
> `scripts/calibrate_macro_score.py:97`、`scripts/export_fund_db.py:60`、`scripts/update_macro_history.py:263,281` 等
> —— **同一條指令找得到真讀取端**。**負控** `<負控串>` → **0 命中（exit=1）**。

### 2.10 `data_cache/ai_cache.json` — AI 白話總結落地

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `ai_cache.{key}.text` | 活的 | **UI** | `repositories/ai_cache.py:59` | AI markdown 字串；**明禁從中萃取數字**（`:9`） |
| `ai_cache.{key}.updated_at` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/ai_cache.py:59` | `%Y-%m-%d %H:%M`（UTC+8）　**2026-09-16 欄層翻轉**：`load()`（`repositories/ai_cache.py:47-53`）只回 `text`；本欄僅供 `save()` 的 LRU 汰舊排序（`:63-65`） |

key ＝ `f"{tab_key}:{sha1(snapshot)[:16]}"`（`:31,32`）；路徑 `:24`；上限 50 筆汰最舊（`:25,60`）
寫 `:56`；讀 `:47`。`.gitignore` 明列不入 repo

### 2.11 `data_cache/batch/batch_{run_id}.json` — 批次分析續存

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `batch_*.run_id` | 活的 | **UI** | `repositories/batch_checkpoint.py:61` | `sha1(sorted(codes))[:12]`（`:49,50`） |
| `batch_*.codes[]` | 活的 | **UI** | `repositories/batch_checkpoint.py:62` | |
| `batch_*.rows{}` | 活的 | **UI** | `repositories/batch_checkpoint.py:63` | `{code: row}`；row 的欄位見 §3.3 |
| `batch_*.updated_at` | 活的 | **UI** | `repositories/batch_checkpoint.py:64` | |
| `batch_*.n_done` | 活的 | **UI** | `repositories/batch_checkpoint.py:65` | |

目錄 `:29`；寫 `:57`；讀 `:81`；列表 `:105`（另產 `n_codes`／`mtime` 兩個**衍生欄，非落地欄**）；刪 `:97`
消費端：`ui/tab_batch_analysis.py`（EX-CRUD-1）、`scripts/audit_6d_coverage.py:143`

### 2.12 `data_cache/macro_thresholds_global.json` — VIX 校準門檻

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `macro_thresholds_global.VIX_CRISIS_THRESHOLD` | 只讀不寫 | **UI** | 讀 `services/macro/validation.py:95` | 越界守門 `[25,35]`（`:97`） |
| `macro_thresholds_global.VIX_WARNING_THRESHOLD` | 只讀不寫 | **UI** | 讀 `services/macro/validation.py:96` | 越界守門 `[18,26]`（`:97`） |
| `macro_thresholds_global.status` | ~~只讀不寫~~ → **只寫不讀** | **無** | `scripts/calibrate_macro_score.py:582`（比對用） | **2026-09-16 客戶裁定讀法 A**：**值在哪**：⚠️ **這是 13 列裡唯一「值不在 repo 檔案裡」的一列** —— `data_cache/macro_thresholds_global.json` **不在 repo**（實測 `git ls-tree -r --name-only origin/main -- data_cache/` 只有 4 個 parquet ＋ `metadata.json`），**要有人跑過那支手動 CLI 之後本機才會有值**（寫入端 `scripts/calibrate_macro_score.py:589`；`git grep -n "calibrate" origin/main -- '.github/workflows/*'` → **0 命中（exit=1）**，正控 `export_fund_db` → 命中 `export_db.yml:39`）。**為什麼沒有畫面消費者**：唯一讀它的是**同一支 CLI 自己**（`:582`，拿舊值比對決定要不要重寫），而 production 讀取端 `services/macro/validation.py:95,96` **只取兩個 VIX 門檻**，不取本欄。 |

**檔案不在 repo**（`git ls-tree origin/main -- data_cache/` 只有 4 parquet ＋ metadata.json）。
讀取在 **module import 時就執行**（`services/macro/validation.py:104`）→ 實務上永遠 fallback 到
`DEFAULT_VIX_CRISIS`／`DEFAULT_VIX_WARNING`。
唯一寫入端 `scripts/calibrate_macro_score.py:589`（`--emit-json` 預設值 `:685`）是**手動 CLI，沒有任何 workflow 掛它**
（8 個 workflow 逐一看過，無 calibrate）

### 2.13 `/tmp/fund_cache/{CODE}_{nav|div|meta}.csv` — **查不到**（死碼）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `{CODE}_nav.csv`（單欄 `nav`，index=date） | 查不到 | **無** | 寫 `infra/cache.py:888`；讀 `:870` | |
| `{CODE}_div.csv`（配息 list JSON，**副檔名是 .csv 但內容是 JSON**） | 查不到 | **無** | 寫 `infra/cache.py:919`；讀 `:903` | |
| `{CODE}_meta.csv`（13 個 key：`fund_name/currency/risk_level/dividend_freq/fund_scale/category/fund_region/nav_latest/nav_date/year_high_nav/year_low_nav/moneydj_div_yield/mgmt_fee`） | 查不到 | **無** | `infra/cache.py:947-950` | |

路徑 `infra/cache.py:845`（Colab → `/content/fund_cache`，否則 `/tmp/fund_cache`）＋ `:851`
**查法（兩種方法交叉）**：
(a) AST refmap（`_cache_save_nav` 等 7 個符號，含別名追蹤）→ `infra/cache.py` 內部以外**零 CALL**；
(b) `git grep -n '_cache_save_nav\|_cache_load_nav\|_cache_save_meta\|_cache_load_meta\|_cache_save_div\|_cache_load_div' origin/main -- .`
（**全檔案類型，不只 .py**）→ 19 行命中，全部是 **定義**（`infra/cache.py`）或 **`noqa: F401` re-export 匯入**
（`fund_fetcher.py:340-345`、`repositories/fund/{sources,nav_metrics,fx_and_main,fund_orchestration}.py`）。
對照組（同型指令）`save_holdings_overview` 在 `ui/` 有 2 處真呼叫。→ **這三種檔案從來沒被寫過、也沒被讀過。**

### 2.14 `config/preset_funds.json` — 預設基金清單

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `preset_funds._comment` | 查不到 | **無** | 檔內 `config/preset_funds.json:2`；寫入端 `services/fund_history.py:320` | 寫入端零 caller（見下）；讀取端 `_load_default_funds` 也只取 `funds`（`:43`），**兩邊都不碰它** |
| `preset_funds.funds[].code` | ~~只讀不寫~~ → **只寫不讀** | **內部** | 讀 `services/fund_history.py:46`、`scripts/export_fund_db.py:80` | 強制大寫；空則跳過　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`config/preset_funds.json` —— **已 commit 進 repo**（實測頂層鍵為 `_comment`／`funds`）；**只能人手編輯 ＋ git commit** —— 實測 `git grep -n "promote_to_preset" origin/main -- '*.py'` → **只有 `services/fund_history.py:281` 的 `def` ＋ 2 行測試清單字串，production 零 caller**；它 docstring 指的 `export_preset_funds_json`（`:340`）同樣零 caller。**為什麼沒有畫面消費者**：兩條讀取鏈都不上畫面 —— (a) `_load_default_funds`（`:37-50`）→ `_DEFAULT_FUNDS`（`:59`）→ `_load_with_defaults`（`:120`）→ `get_history_df`（`:158`），而 `get_history_df` 實測 **production 零 caller**（`git grep -n "get_history_df" origin/main -- '*.py'` 的非 `tests/` 命中只有 `:158` 的 `def` 與 `:10` 的 docstring）；(b) `scripts/export_fund_db.py:80` 把它寫進 `fund.db::fund_universe`，而 `fund.db` 的消費端在**下游 repo**（同 §2.18 的判法）。 |
| `preset_funds.funds[].name` | ~~只讀不寫~~ → **只寫不讀** | **內部** | 讀 `services/fund_history.py:49`、`scripts/export_fund_db.py:80` | 可空　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`config/preset_funds.json` —— **已 commit 進 repo**（實測頂層鍵為 `_comment`／`funds`）；**只能人手編輯 ＋ git commit** —— 實測 `git grep -n "promote_to_preset" origin/main -- '*.py'` → **只有 `services/fund_history.py:281` 的 `def` ＋ 2 行測試清單字串，production 零 caller**；它 docstring 指的 `export_preset_funds_json`（`:340`）同樣零 caller。**為什麼沒有畫面消費者**：兩條讀取鏈都不上畫面 —— (a) `_load_default_funds`（`:37-50`）→ `_DEFAULT_FUNDS`（`:59`）→ `_load_with_defaults`（`:120`）→ `get_history_df`（`:158`），而 `get_history_df` 實測 **production 零 caller**（`git grep -n "get_history_df" origin/main -- '*.py'` 的非 `tests/` 命中只有 `:158` 的 `def` 與 `:10` 的 docstring）；(b) `scripts/export_fund_db.py:80` 把它寫進 `fund.db::fund_universe`，而 `fund.db` 的消費端在**下游 repo**（同 §2.18 的判法）。 |

檔案常數：`services/fund_history.py:31`。**repo 內實測 8 檔基金**（`config/preset_funds.json:4-11`）。
**讀取端（兩個，都是 production）**：
(a) `_load_default_funds`（`:37`），在 **module import 時**就跑（`:59` `_DEFAULT_FUNDS = _load_default_funds()`）；
(b) `scripts/export_fund_db.py:79`（workflow `export_db.yml` 每日 cron）→ 產出 `fund.db::fund_universe`（§2.18）。
**寫入端：`promote_to_preset`（`services/fund_history.py:281`，`write_text` 在 `:326`）—— 零 caller。**
查法（兩種交叉）：(a) AST refmap → 無 CALL；
(b) `git grep -n 'promote_to_preset' origin/main -- '*.py'`（排除 tests）→ **只有 `:281` 的 `def` 一行**。
→ **這個檔目前只能靠人手動編輯 + git commit**，而這正是它自己的 docstring 寫的
（`services/fund_history.py:285`：「`export_preset_funds_json()` 下載新 JSON、git commit 回 repo 才真正持久化」），
但**那支下載函式同樣零 caller**（`:340`）—— UI 上按不到。

### 2.15 `config/macro_weights_active.json`（repo 根）— **查不到**

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `config/macro_weights_active.json`（12 個 key：`version/calibrated_at/calibration_method/horizon_months/drawdown_threshold/indicators/verdict_cutoffs/phase_thresholds/oos_metrics/ai_explanation/notes`） | 查不到 | **無** | 檔案本身 `config/macro_weights_active.json:1-13` | **沒有任何程式讀它或寫它** |

**查法**：`services/macro/weights_store.py:21` `_CONFIG_DIR = Path(__file__).resolve().parent.parent / "config"`
→ `__file__` 在 `services/macro/`，`parent.parent` ＝ **`services/`** → 實際讀的是 **`services/config/macro_weights_active.json`**（見 §2.16）。
`git grep -n 'macro_weights_active' origin/main -- .`（全檔案類型）共 5 行：`ARCHITECTURE.md:104`（文件）、
`weights_store.py:4,22,240`、`tests/test_macro_weights_store.py:26`（monkeypatch）。**沒有一行指向 repo 根的那一份。**

### 2.16 `services/config/macro_weights_active.json` — **只讀不寫**

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `.version` | ~~只讀不寫~~ → **只寫不讀** | **內部** | `services/macro/weights_store.py:25`（`_REQUIRED_KEYS`） | 必填，缺則 `_validate` raise（`:232`）　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`services/config/macro_weights_active.json` —— **已 commit 進 repo**（實測該檔頂層鍵恰為 `version`／`indicators` 兩個）；模組**沒有 `save_active`**（實測 0 命中），所以它**只能人手編輯 ＋ git commit**。**為什麼沒有畫面消費者**：`_validate`（`:232`）只拿它做 `_REQUIRED_KEYS` 的**鍵存在檢查**（`missing = _REQUIRED_KEYS - set(payload.keys())`）—— **讀的是「這個 key 在不在」，值本身從未被取用**。 |
| `.indicators{}` | 只讀不寫 | **UI** | `services/macro/weights_store.py:25` | 必填 dict；repo 內實測只有一項 `VIX_DELTA_5D.weight = 0.8` |
| `.calibrated_at` / `.calibration_method` / `.horizon_months` / `.drawdown_threshold` / `.verdict_cutoffs` / `.phase_thresholds` / `.oos_metrics` / `.ai_explanation` / `.notes` | 只讀不寫 | **UI** | `services/macro/weights_store.py:41-53`（`_empty_active` 的同名 key） | 選填；repo 內那份**只有 version + indicators 兩個 key** |

讀：`services/macro/weights_store.py:254`（**只在 `_gs_enabled()` 為假時才走這條**，`:248`）
寫：**查不到**（模組無 `save_active`；`grep -n '^def ' services/macro/weights_store.py` 全列已看過）

### 2.17 `snap.json`（repo 根）— 離線重跑快照

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `snap.dumped_at` | 查不到（production） | **無** | `snap.json:2` | 22,235 bytes，已 commit |
| `snap.funds.{CODE}.inception_date` | 查不到（production） | **無** | `snap.json:5` | |
| `snap.funds.{CODE}.metrics.{inception_date,ret_3y_ann,ret_3y_cum,ret_3y,sharpe,std_1y,max_drawdown,ret_5y_ann,ret_5y_cum,ret_5y,nav}` | 查不到（production） | **無** | `snap.json:6-18` | 11 個 metric key |
| `snap.funds.{CODE}.perf` / `.perf_source` / `.fund_name` / `.category` | 查不到（production） | **無** | `snap.json:19-22` | |

寫／讀：**只有兩支手動 CLI 診斷 script**——`scripts/compare_inception_years.py:48,49`（`--live --dump` / `--snapshot`）、
`scripts/diagnose_ret_3y_fallback.py:61`。**沒有任何 workflow、沒有任何 UI 路徑碰它。**
查法：`git grep -n 'snap\.json' origin/main -- .` → 6 行，全在那兩支 script 的 docstring／argparse。

### 2.18 `fund.db`（SQLite，在 `origin/data` 分支上）— **本 repo 唯一真正的關聯式資料庫**

分支 `origin/data`，單一檔案 `fund.db`（**實測 806,912 bytes**，`git ls-tree -r --long origin/data`）
產生：`scripts/export_fund_db.py`；發佈：`.github/workflows/export_db.yml`（cron `0 21 * * *`，force-push 到 `data` 分支）

> 📌 **本表有「位元組讀回？」欄**（§0.2），**而且這一張的欄義要特別看清楚**：
> 「否」指的是「**本 repo 內**沒有任何路徑把它讀回記憶體」——
> **不是**「這個檔沒人讀」。它真正的讀取端在**下游 repo `2026_strategy_0719`**，
> 那超出 §0 對 `production` 的界定（本 repo 之內），本盤點**射程外、查不到**。
> ⚠️ 所以這一張**不適用**「改個消費端就能用」那句話 —— 它的消費端本來就在別人家，**而且已經在用了**。

| 表 . 欄位 | 狀態 | 位元組讀回？ | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|---|
| `fund.db::global_index.date` (DATE) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:63` | **實測 11,406 列**，`2011-06-07`~`2026-09-11` |
| `fund.db::global_index.symbol` (TEXT) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:62,63` | **實測 3 種**：`SPX`／`VIX`／`TWII`（`:60`） |
| `fund.db::global_index.close` (REAL) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:61,63` | |
| `fund.db::fred_macro.date` (DATE) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:70` | **實測 13,654 列**，`2011-06-01`~`2026-09-10` |
| `fund.db::fred_macro.series_id` (TEXT) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:70` | **實測 9 種**（同 §2.8） |
| `fund.db::fred_macro.value` (REAL) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:70` | |
| `fund.db::fund_universe.code` (TEXT) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:80` | **實測 8 列**（＝ `config/preset_funds.json`） |
| `fund.db::fund_universe.name` (TEXT) | 只寫不讀 | 否（本 repo 內） | **無** | `scripts/export_fund_db.py:80` | |
| `fund.db::us_market.{date,us_stock_id,close}` | 已停用 | 否 | **無** | `scripts/export_fund_db.py:96-98` | workflow 用 `--no-live`（`export_db.yml` 的 `run:` 行）→ **不產生**；`sqlite_master` 實測**無此表**。**這兩列的「否」比上面 8 列更絕對：表根本沒被建出來，沒有位元組可讀** |
| `fund.db::fx.{date,usdtwd}` | 已停用 | 否 | **無** | `scripts/export_fund_db.py:134` | 同上，實測無此表 |

⚠️ 消費者是**下游另一個 repo**（`export_fund_db.py:4` 明文「供下游 2026_strategy_0719 多智能體系統讀取」），
本 repo 的 UI **不讀它**。輸出路徑由 env `FUND_DB` 決定（`:184`）。

> ⚠️ **2026-09-16 狀態更正：上表 8 列由 ~~「活的」~~ 改為「只寫不讀」**
> （**有意識的更正，不是漏刪** · 決策者：盤點生產組 · 起因：盤點稽核組矛盾 4）
>
> ⛔⛔ **先講清楚，免得有人拿這三個字去刪東西：「只寫不讀」＝「本 repo 內沒有讀取端」，
> 不是「沒人用」。它真正的消費者是下游 repo `2026_strategy_0719`，那個消費者是真的、而且是這支 script 的存在理由。**
>
> **為什麼還是要改**：§0 把「production」界定在**本 repo 之內**（「從 `app.py` 可達的 UI 路徑，
> 或 `.github/workflows/` 有排程的 script」），而 §0 的「活的」要求**兩端都是 production**。
> 寫入端 ✅（`.github/workflows/export_db.yml`，cron `0 21 * * *`）；讀取端 ❌（在別的 repo）。
> **本節第 2 段與 §6 發現 8 本來就自己寫著「本 repo 的 UI 不讀它」** —— 舊狀態值與同一節的散文互相打架。
>
> **本組實測**：`git grep -nE "fund\.db|FUND_DB|sqlite3|sqlite" origin/main -- '*.py'`（排除 `tests/`）
> → 命中 **13 行**，全部在 `scripts/export_fund_db.py`（生產者，`:2,19,31,58,69,75,102,137,164,179,180,184,188`）
> 與 `infra/asset_publish.py:15`（**一行註解**，講 force-push 慣例，不是讀取）。
> **正控**：同型指令對 `parquet` 在 `.py` 有 8 檔命中（含 `services/macro/validation.py`、`services/us_liquidity_engine.py` 等真讀取端）。
> **負控** `<負控串>` → 0 命中。
> ⛔ **若客戶決定「production」要涵蓋下游 repo**，那是 **§0 定義的修改**，本列會翻回「活的」。
> **本組不代為裁決，只據現行 §0 套用。**
>
> 📌 **2026-09-16 續記：客戶當日裁定的是「讀取」的定義（§0.1），不是「production」的邊界** ——
> 本節的狀態因此**一字未動**。客戶裁定「讀取 ＝ 資料到得了消費者」，
> 而本節卡住的是**更前面那一關**：那個消費者不在 §0 界定的 production 範圍（本 repo）內。
> **兩個問題各自獨立，上面那句「⛔ 若客戶決定…」依然有效、依然待裁。**
>
> 📌 **「位元組讀回？＝ 否（本 repo 內）」的實測（本輪重跑）**：
> `git grep -nE "fund\.db|FUND_DB|sqlite3|sqlite" origin/main -- '*.py'`（排除 `tests/`）→ **13 行**，
> **全部在生產者 `scripts/export_fund_db.py`**（`:2,19,31,58,69,75,102,137,164,179,180,184,188`）
> ＋ `infra/asset_publish.py:15`（**一行 docstring 註解**，講 force-push 慣例，不是讀取）。**沒有一行是讀。**
> **正控**（同型指令）：`git grep -ln "parquet" origin/main -- '*.py'` → **8 檔**，含
> `services/macro/validation.py`／`services/us_liquidity_engine.py`／`scripts/calibrate_macro_score.py` 等**真讀取端**。
> **負控** `<負控串>` → **0 命中（exit=1）**。

### 2.19 使用者下載／上傳的 JSON 備份（`portfolio_backup`）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `backup.schema_version` | ~~活的~~ → **只寫不讀** | **無** | `ui/helpers/io/json_backup.py:45`（值 `"1.0"`，`:14`） | **2026-09-16 欄層翻轉**：`restore_from_json_bytes`（`ui/helpers/io/json_backup.py:61-106`）**沒有讀本欄**（`:66` 只檢查 `portfolio_funds` 在不在）；全 repo grep `schema_version` 的其餘命中都是 `detect_sheet_schema_version`（**子字串陷阱，不是本欄**） |
| `backup.exported_at` | ~~活的~~ → **只寫不讀** | **無** | `ui/helpers/io/json_backup.py:46` | TW ISO（秒）　**2026-09-16 欄層翻轉**：同上，restore 不讀本欄 |
| `backup.portfolio_funds[].{code,name,invest_twd,policy_id,policy_name,policy_tier,currency,is_core,invest_date,fx_at_buy,avg_nav_with_div,div_cash_pct}` | 活的 | **UI** | `ui/helpers/io/json_backup.py:28-39` | **12 欄**；刻意剝掉 `series`／`moneydj_raw`（`:22`） |
| `backup.t7_ledgers{}` | 活的 | **UI** | `ui/helpers/io/json_backup.py:48` | `{pk_str: Ledger.to_dict()}` |
| `backup.t7_scenarios[]` | 活的 | **UI** | `ui/helpers/io/json_backup.py:49` | 見 §3.4 |
| `backup.active_policy_id` | 活的 | **UI** | `ui/helpers/io/json_backup.py:50` | |
| `backup.policy_sheet_id` | 活的 | **UI** | `ui/helpers/io/json_backup.py:51` | ⚠️ 備份檔內含 Sheet ID |

寫 `build_export_payload`（`:17`）；讀回 `restore_from_json_bytes`（`:55`）
範本：`templates/portfolio_backup_sample.json`
⚠️ **不是自動落地**——靠使用者按下載／上傳。`ui/helpers/portfolio/policy_admin_section.py:490` 呼叫

### 2.20 範本 CSV／JSON（`templates/`，供使用者複製，程式不讀）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `templates/nav_history_sample.csv.{date,nav}` | 查不到 | **無** | `templates/nav_history_sample.csv:1` | 西元日期 |
| `templates/nav_history_sample_roc.csv.{日期,單位淨值}` | 查不到 | **無** | `templates/nav_history_sample_roc.csv:1` | 民國年；解析器 `services/nav_history_store.py:82` |
| `templates/fund_history_sample.csv.{代號,名稱,來源,查詢次數,首次查詢,最近查詢}` | 查不到 | **無** | `templates/fund_history_sample.csv:1` | ＝ §2.3 的匯出版（`services/nav_history_store.py:387` 產 CSV bytes） |
| `templates/preset_funds_sample.json` / `templates/portfolio_backup_sample.json` | 查不到 | **無** | 檔案本身 | 同 §2.14／§2.19 schema |

> ⚠️ **2026-09-16 狀態更正：上表 4 列由 ~~「只讀不寫（人讀）」~~ 改為「查不到」**
> （**有意識的更正，不是漏刪** · 決策者：盤點生產組 · 起因：盤點稽核組矛盾 7）
>
> **兩個理由，缺一不可**：
> **(1) `只讀不寫（人讀）` 不在 §0 的封閉集合裡。** §0 列的六個狀態是
> 活的／只寫不讀／只讀不寫／查不到／已停用／未落地，**沒有「人讀」這個變體**。
> **(2) `只讀不寫` 的定義要求「有 production 讀取」，而這四個檔沒有。**
> 本組實測：`git grep -nE "nav_history_sample|preset_funds_sample|portfolio_backup_sample|fund_history_sample" origin/main -- .`
> → 命中 **7 行**：`templates/README.md:10,11,12,13,14`（**文件表格**）
> ＋ `tests/test_preset_funds_drift.py:13,30`（**測試**）。**`.py` production 端 0 行。**
> **正控**：同型指令對 `preset_funds.json` 在 `.py` 命中 **15 行**（含 `services/fund_history.py:31,38`、
> `scripts/export_fund_db.py:76` 等真讀取端）。**負控** `<負控串>` → 0 命中。
>
> ⚠️ **這四個檔沒有變沒用** —— 節標題寫的「**供使用者複製，程式不讀**」本來就是對的，
> `templates/README.md:10-14` 逐檔標明它對應 App 裡的哪個上傳入口。**狀態欄描述的是「程式端」，不是「有沒有價值」。**
> ⛔ **若客戶／總管要為「給人複製的樣板」另立一個狀態值**（例如 `人讀`），那是 **§0 的修改**，
> 屆時這 4 列會改用新狀態。**本組不自行擴充 §0 的封閉集合。**

---

## 3. 走 `st.session_state` 的表格狀結構（60 列中 **32 列另有落地**、28 列重繪即消失）

> ⚠️ **2026-09-16 標題更正（有意識的更正，不是漏刪 · 決策者：盤點生產組 · 起因：盤點稽核組矛盾 6）**
> 舊標題：~~「只活在記憶體、沒有落地的（`st.session_state`）」~~
>
> **舊標題在寫下的當天就被本節自己的內容推翻**：§3.1 的小標逐字寫著
> 「⚠️ **這一項要更正：它其實有落地**」，§3.3 的小標寫著「（**有落地**，見 §2.11）」。
> **被推翻的是標題的全稱性，不是任何一列的狀態值** —— 本輪**沒有改動 §3 任何一列的狀態欄**。
>
> **本組自己重數的結果（數法在下面，可複跑）**：§3 共 **60** 列，
> 標「活的／活的（有落地）」**32 列（53%）**、標「未落地」**28 列（47%）**。
>
> | 小節 | 列數 | 活的 | 未落地 |
> |---|---|---|---|
> | §3.1 `t7_ledgers` | 15 | **15** | 0 |
> | §3.2 `portfolio_funds` | 22 | **14** | 8 |
> | §3.3 `batch_codes` / `batch_rows` | 6 | **3** | 3 |
> | §3.4 `t7_scenarios` | 8 | 0 | 8 |
> | §3.5 其他 session 表 | 9 | 0 | 9 |
> | **合計** | **60** | **32** | **28** |
>
> 15 ＋ 14 ＋ 3 ＝ **32**；8 ＋ 3 ＋ 8 ＋ 9 ＝ **28**；32 ＋ 28 ＝ **60** ✅
>
> ⚠️ **2026-09-16 顆粒度輪：上面這張表的「活的」欄已被客戶的欄層裁示拆開，狀態更新如下**
> （**有意識的政策變更，不是漏刪** · 日期 **2026-09-16** · **決策者：客戶**）。
> **上面整段原文一字未改** —— 它當時數的是對的，被推翻的是它賴以成立的**表層顆粒度**。
>
> | 小節 | 列數 | 活的 | **只寫不讀（本輪新增）** | 未落地 |
> |---|---|---|---|---|
> | §3.1 `t7_ledgers` | 15 | ~~15~~ → **5** | **10** | 0 |
> | §3.2 `portfolio_funds` | 22 | ~~14~~ → **13** | **1** | 8 |
> | §3.3 `batch_codes` / `batch_rows` | 6 | **3** | 0 | 3 |
> | §3.4 `t7_scenarios` | 8 | 0 | 0 | 8 |
> | §3.5 其他 session 表 | 9 | 0 | 0 | 9 |
> | **合計** | **60** | **21** | **11** | **28** |
>
> 5 ＋ 13 ＋ 3 ＝ **21**；10 ＋ 1 ＝ **11**；21 ＋ 11 ＋ 28 ＝ **60** ✅
>
> ⚠️ **2026-09-16 讀法 A 輪：上表再更新一次，`§3.4` 兩列離開「未落地」**
> （**有意識的政策變更，不是漏刪** · 日期 **2026-09-16** · **決策者：客戶**）。
> **上面兩張表原文一字未改**；改的是 §0 對「未落地」與「只寫不讀」的分界（見 **§0.4**）——
> 沒有消費端的 session 欄位自本輪起歸「只寫不讀」。
>
> | 小節 | 列數 | 活的 | 只寫不讀 | 未落地 |
> |---|---|---|---|---|
> | §3.1 `t7_ledgers` | 15 | **5** | **10** | 0 |
> | §3.2 `portfolio_funds` | 22 | **13** | **1** | 8 |
> | §3.3 `batch_codes` / `batch_rows` | 6 | **3** | 0 | 3 |
> | §3.4 `t7_scenarios` | 8 | 0 | ~~0~~ → **2** | ~~8~~ → **6** |
> | §3.5 其他 session 表 | 9 | 0 | 0 | 9 |
> | **合計** | **60** | **21** | ~~11~~ → **13** | ~~28~~ → **26** |
>
> 5 ＋ 13 ＋ 3 ＝ **21**；10 ＋ 1 ＋ 2 ＝ **13**；21 ＋ 13 ＋ 26 ＝ **60** ✅
>
> ⚠️ **「重繪即消失」這件事完全沒有變** —— `t7_scenarios[].id` 與 `.nav_fx_cache` **仍然只活在 session**，
> 它們離開「未落地」**不是因為變得持久了**，是因為它們**沒有畫面消費端**（逐列依據見 §3.4 該列）。
> **所以本節標題的「28 列重繪即消失」在「不持久化」的意義上仍然成立**，
> 但**不能再用「數幾列標未落地」去導出它** —— 那個數法自本輪起會回 **26**。
> **這與上一輪對「32 列另有落地」的處置是同一種**：句子還成立，導出它的數法換了。
>
> ⚠️ **「有落地」這件事沒有變，變的是「落地之後有沒有人用」**：
> §3.1 那 10 列**仍然真的落到 `_T7_State.ledger_json`**、也真的被 `from_dict` 讀回來 ——
> 只是讀回來之後**沒有任何畫面用到它們**（`transactions[]` 在 `ui/**` 零消費端，逐列依據見該節）。
> **所以本節標題的「32 列另有落地」在「落地」這個意義上仍然成立**，
> 但**不能再用「數幾列標活的」去導出它** —— 那個數法自本輪起會回 21。
>
> ⚠️ **「32」與稽核報告矛盾 6 所寫的「18（15 ＋ 3）」不同，差在 §3.2 的 14 列。**
> 稽核組（以及本輪派工單）都只數了 §3.1 與 §3.3，**漏掉 §3.2 `portfolio_funds` 那 14 列** ——
> 它們的狀態欄逐列寫「活的」，出處指向 `repositories/policy/v1.py:248-255,262,265`
> 與 `ui/helpers/portfolio/load.py:126,128,130,242`，**全是真的落到 Sheet 的欄位**
> （`code` / `invest_twd` / `policy_id` / `policy_name` / `currency` / `invest_date` / `fx_at_buy` /
> `policy_tier` / `div_cash_pct` / `avg_nav_with_div` / `avg_nav` / `fx_avg` / `units` / `is_core`）。
> **§3.2 的小標本來就寫著「未落地：只有部分欄位落地」** —— 它從一開始就是混合的，只是沒有人把數字數出來。
>
> **數法（可複跑，不經任何 `grep -v`）**：以 markdown 表格列 parser 逐列取第 2 欄，
> 去掉 `**` 與全形括號補語後，只認 §0 那六個封閉狀態值；跳過表頭列（第一欄 ＝「表 . 欄位」）與分隔列。
> ⚠️ **這個數字是「本表現在寫了幾列」，不是「repo 裡實際有幾個 session 結構」** ——
> 後者本節開頭已自陳只列「表格狀」的那幾個，且稽核報告的漏盤 O1／O2 另指出至少 2 個表格狀 session key
> （`tdcc_results`／`phase_history`）與屬性式寫法（`st.session_state.prev_phase`）**不在本表內**。
> **本輪沒有補盤那些，狀態不變。**


實測 **82 個**不同的 session key（AST 掃 `st.session_state[...]` / `.get` / `.setdefault` / `.pop`，
排除 `tests/`）。以下只列**表格狀**的那幾個 —— 它們才是畫 UI 會用到的資料。

### 3.1 `t7_ledgers` — ⚠️ **這一項要更正：它其實有落地**

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `t7_ledgers{pk_str}.fund_code` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:161` | **2026-09-16 欄層翻轉**：只出現在 `to_dict`（`services/ledger_service.py:161`）、`FundPosition(fund_code=…)`（`:63`）與換股 note 字串（`:418`／`:444`）；畫面上的「代碼」來自 `portfolio_funds`，不是本欄 |
| `t7_ledgers{pk_str}.currency` | 活的（有落地） | **UI** | `services/ledger_service.py:162` | |
| `t7_ledgers{pk_str}.transactions[].txn_type` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:165`；型別 `models/ledger.py:44` | 合法值 `_TXN_TYPES`（`models/ledger.py:8`）　**2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.transactions[].txn_date` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:166`；`models/ledger.py:45` | ISO　**2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.transactions[].amount_twd` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:167`；`models/ledger.py:46` | **2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.transactions[].fx_rate` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:168`；`models/ledger.py:47` | **2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.transactions[].nav` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:169`；`models/ledger.py:48` | **2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.transactions[].div_per_unit` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:170`；`models/ledger.py:49` | **2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.transactions[].new_units` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:171`；`models/ledger.py:50` | **2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.transactions[].note` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:172`；`models/ledger.py:51` | **2026-09-16 欄層翻轉**：`transactions[]` 在 `ui/**` **零消費端** —— `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` 命中 15 行，全在 `services/ledger_service.py`（`to_dict`／`from_dict`／append）、`scripts/migrate_v149_schema.py` 與 `ui/helpers/cloud_io.py:316`（**註解**）。唯一會逐筆讀它的 `calculate_xirr`（`:465`）**零 caller**；`position` 是由 `subscribe()` 等方法**當場更新**，不靠回讀 transactions |
| `.position.units` | 活的（有落地） | **UI** | `services/ledger_service.py:177`；`models/ledger.py:64` | |
| `.position.cost_unit` | 活的（有落地） | **UI** | `services/ledger_service.py:178`；`models/ledger.py:65` | |
| `.position.fx_avg` | 活的（有落地） | **UI** | `services/ledger_service.py:179`；`models/ledger.py:66` | |
| `.position.cost_unit_with_div` | 活的（有落地） | **UI** | `services/ledger_service.py:180`；`models/ledger.py:67` | |
| `.position.dividends_received_twd` | ~~活的~~ → **只寫不讀** | **內部** | `services/ledger_service.py:181`；`models/ledger.py:68` | **2026-09-16 欄層翻轉**：三個用途都到不了畫面 —— 寫進 `_持倉總覽.累積已領配息(TWD)`（該表本身只寫不讀，§1.5）、`Ledger.compare()`（`:227-228`，**零 caller**）、`FundPosition.roi_total_cashflow()`（`models/ledger.py:106`，**零 caller**） |

**落地在哪**：`Ledger.to_dict()`（`services/ledger_service.py:158`）**含完整 `transactions[]`**，
被 `json.dumps` 整串寫進 `_T7_State.ledger_json`（`repositories/snapshot_repository.py:152`），
再由 `load_all_ledgers_snapshot`（`:296`）→ `Ledger.from_dict`（`services/ledger_service.py:186`）**原樣還原**（`:192-193`）。
另一份落地在 `_Ledgers` 分頁（每筆一列，§1.4）。
⚠️ **2026-09-16 補一句防誤讀（本節 15 列的狀態一字未動）**：**§3.1 的「活的（有落地）」靠的是
`_T7_State` 那條路**（寫進去 → `load_all_ledgers_snapshot` 讀回來 → `st.session_state["t7_ledgers"]`
→ T7 面板顯示，實測 `ui/helpers/cloud_io.py:477-490`、`ui/tab3_t7_ledger.py:476-494`）。
**`_Ledgers` 那一份是「寫得進去、讀不回來用」**（客戶 2026-09-16 裁定後為「只寫不讀」，見 §1.4）——
**兩份落地不是同一個性質，做畫面時要從 `_T7_State` 那條取，不要從 `_Ledgers`**（除非你自己接消費端，
那也很便宜，因為它的「位元組讀回？」是「是」）。
⚠️ **`FundPosition` 有 7 個欄位（`models/ledger.py:62-68`），`to_dict()["position"]` 只帶 5 個** ——
少的是 `fund_code`（`:62`）與 `currency`（`:63`），但那兩個在 **Ledger 層**另存
（`services/ledger_service.py:161,162`），**所以來回一趟不掉資料**。
`Transaction.txn_type` 的合法值鎖在 `models/ledger.py:24` `_TXN_TYPES`，非法值在 `:55` 直接 raise。

> 📌 **2026-09-16 顆粒度輪：本節 15 列中 10 列由 ~~「活的（有落地）」~~ 翻成「只寫不讀」**
> （**有意識的政策變更，不是漏刪** · 日期 **2026-09-16** · **決策者：客戶**，依 §0.3 · 執行者：盤點生產組）
>
> **落地那件事沒有被推翻，一個字都沒改** —— `transactions[]` **確實**整串進了
> `_T7_State.ledger_json`，也**確實**被 `Ledger.from_dict`（`services/ledger_service.py:186-193`）原樣還原。
> **被推翻的是「還原回來之後有人用」這個隱含前提。**
>
> **實測（指令與輸出照抄）**：
> `git grep -n "transactions" origin/main -- '*.py' ':!tests/*'` → **15 行**，
> 全在 `services/ledger_service.py`（`:59` 欄位定義、`:91`／`:127`／`:152`／`:414`／`:440` append、
> `:163,174` `to_dict`、`:192-193` `from_dict`、`:466,485` `calculate_xirr`、`:291` 註解）、
> `scripts/migrate_v149_schema.py:91`、以及 `ui/helpers/cloud_io.py:316`（**一行註解**）。
> **`ui/**` 沒有任何一行是真的讀它。**
> **唯一會逐筆走訪 `transactions` 的 `calculate_xirr`（`:465`）本身零 caller**
> （`git grep -n "calculate_xirr" origin/main -- '*.py' ':!tests/*'` → 2 行：`:465` 的 `def`
> ＋ `models/ledger.py:4` 的 docstring）。
> **正控**：同型指令對 `position.cost_unit` → `ui/tab3_t7_ledger.py:3038,3081` 真的畫在「平均買入淨值 NAV」。
> **負控**：`git grep -n "<負控串>" origin/main -- '*.py'` → **0 命中（exit=1）**。
>
> **為什麼 `position` 那 4 欄還是 UI，`transactions` 卻不是**：`position` 是被
> `subscribe()` / `dividend_*()` **當場就地更新**的（`:74-90`、`:113-125`），
> **不是靠回讀 `transactions` 重算出來的** —— 所以交易明細少一筆，畫面也不會有任何反應。
> **這條帳等於一本沒有人翻的流水帳。**

### 3.2 `portfolio_funds` — 中央持倉清單（**未落地：只有部分欄位落地**）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `portfolio_funds[].code` | 活的 | **UI** | `repositories/policy/v1.py:248`；新增骨架 `ui/helpers/portfolio/add_entry.py:142` | |
| `portfolio_funds[].invest_twd` | 活的 | **UI** | `repositories/policy/v1.py:249` | |
| `portfolio_funds[].policy_id` | 活的 | **UI** | `repositories/policy/v1.py:250` | |
| `portfolio_funds[].policy_name` | 活的 | **UI** | `repositories/policy/v1.py:251` | |
| `portfolio_funds[].currency` | 活的 | **UI** | `repositories/policy/v1.py:252` | |
| `portfolio_funds[].invest_date` | 活的 | **UI** | `repositories/policy/v1.py:253` | |
| `portfolio_funds[].fx_at_buy` | ~~活的~~ → **只寫不讀** | **內部** | `repositories/policy/v1.py:254` | **2026-09-16 欄層翻轉**：`portfolio_funds` 這一份只被 `ui/helpers/io/json_backup.py:34` 讀去寫備份檔；三個寫入端（`cloud_io.py:249`／`tab3_portfolio.py:1581`／`tab3_t7_ledger.py:1049`）**固定填 `0.0`**。⚠️ 畫面上看得到的「買入匯率」是 §1.1 `Policies.fx_at_buy` 經 `policies_df` 渲染，**不是本欄** |
| `portfolio_funds[].policy_tier` | 活的 | **UI** | `repositories/policy/v1.py:255` | |
| `portfolio_funds[].div_cash_pct` | 活的 | **UI** | `repositories/policy/v1.py:262` | 只在 Sheet 欄非空時才帶回（`:261`） |
| `portfolio_funds[].avg_nav_with_div` | 活的 | **UI** | `repositories/policy/v1.py:265` | 同上（`:264`） |
| `portfolio_funds[].avg_nav` | 活的 | **UI** | `ui/helpers/portfolio/load.py:126` | 缺值時從 ledger position 補（`:106,125`） |
| `portfolio_funds[].fx_avg` | 活的 | **UI** | `ui/helpers/portfolio/load.py:128` | 同上 |
| `portfolio_funds[].units` | 活的 | **UI** | `ui/helpers/portfolio/load.py:130` | 同上 |
| `portfolio_funds[].name` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:236` | 抓回來的基金名；只在 JSON 備份會被帶走（§2.19） |
| `portfolio_funds[].series` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:237` | NAV 序列；`json_backup.py:22` 明文剝掉 |
| `portfolio_funds[].dividends` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:238` | |
| `portfolio_funds[].metrics` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:239` | |
| `portfolio_funds[].moneydj_raw` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:240` | 整包原始回應；`json_backup.py:22` 明文剝掉 |
| `portfolio_funds[].risk_metrics` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:241` | |
| `portfolio_funds[].is_core` | 活的 | **UI** | `ui/helpers/portfolio/load.py:242` | 由基金名啟發式推（`_is_core`）；落地時轉成 `_持倉總覽.級別` 中文（`repositories/snapshot_repository.py:250`） |
| `portfolio_funds[].loaded` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:245`；`ui/helpers/portfolio/add_entry.py:146` | |
| `portfolio_funds[].load_error` | **未落地** | **UI** | `ui/helpers/portfolio/load.py:232,245`；`add_entry.py:147` | |

### 3.3 `batch_codes` / `batch_rows` — 批次分析（**有落地**，見 §2.11）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `batch_codes[]` | 活的 | **UI** | `ui/tab_batch_analysis.py:55` | 落地成 `batch_*.codes`（`repositories/batch_checkpoint.py:62`） |
| `batch_rows{code}` | 活的 | **UI** | `ui/tab_batch_analysis.py:56` | 落地成 `batch_*.rows`；row 欄位 SSOT ＝ `BATCH_UNIFIED_COLUMNS`（`ui/helpers/fund_grp_health/unified.py`，由 `ui/tab_batch_analysis.py:49,50` 匯入） |
| `batch_run_at` | **未落地** | **UI** | `ui/tab_batch_analysis.py:57` | |
| `batch_run_id` | 活的 | **UI** | `ui/tab_batch_analysis.py:58` | 落地成 `batch_*.run_id` |
| `batch_disk_off` | **未落地** | **UI** | `ui/tab_batch_analysis.py:59` | 磁碟續存失敗的降級旗標 |
| `v03_research_batch_rows` | **未落地** | **UI** | `ui/views/page_03_research.py:669` | 新 ③ 的批次列；**與舊 `batch_rows` 是兩個鍵** |

### 3.4 `t7_scenarios` — 再平衡方案快照（**未落地到 Sheet，只進 JSON 備份**）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `t7_scenarios[].id` | ~~未落地~~ → **只寫不讀** | **內部** | `ui/tab3_t7_ledger.py:1179` | `sc_{epoch_ms}`　**2026-09-16 客戶裁定讀法 A**：**值在哪**：`st.session_state.t7_scenarios[]`（寫入 `ui/tab3_t7_ledger.py:1179`、append `:1188`）—— **這一條是 production UI 路徑**（不是 CLI、不是人工），只是**不持久化**。**為什麼沒有畫面消費者**：唯一用到它的是 `:2932` 的 `key=f"t7_commit_sc_{_sc['id']}"` —— `st.button` 的 **widget key**，**值本身不顯示**（畫面上顯示的是 `:2926` 的 `name`／`action_type`／`created_at`）。⚠️ **這一列與另 11 列不同族**：它離開的是「未落地」而不是「只讀不寫」，理由見 §0.4 的分流規則。 |
| `t7_scenarios[].name` | 未落地 | **UI** | `ui/tab3_t7_ledger.py:1180` | |
| `t7_scenarios[].action_type` | 未落地 | **UI** | `ui/tab3_t7_ledger.py:1181` | |
| `t7_scenarios[].details` | 未落地 | **UI** | `ui/tab3_t7_ledger.py:1182` | 截斷 200 字 |
| `t7_scenarios[].snapshot` | 未落地 | **UI** | `ui/tab3_t7_ledger.py:1183` | 當下 `t7_ledgers` 的 dict 快照 |
| `t7_scenarios[].summary` | 未落地 | **UI** | `ui/tab3_t7_ledger.py:1184` | |
| `t7_scenarios[].nav_fx_cache` | ~~未落地~~ → **只寫不讀** | **內部** | `ui/tab3_t7_ledger.py:1185` | **2026-09-16 客戶裁定讀法 A**：**值在哪**：同上，`st.session_state.t7_scenarios[]`（`:1185` `dict(nav_fx_cache or {})`），production UI 路徑、不持久化。**為什麼沒有畫面消費者**：實測 `git grep -n "nav_fx_cache" origin/main -- '*.py'` → **11 行**，逐行判讀後**全部是函式參數宣告、docstring／註解，或呼叫時的傳參**，**沒有任何一行從 scenario dict 取回 `_sc["nav_fx_cache"]`**；`:1172` 的註解自陳「存進 scenario 供之後重算」，但那個「之後」在本 repo 不存在。⚠️ **同上一列，它離開的是「未落地」**，理由見 §0.4。 |
| `t7_scenarios[].created_at` | 未落地 | **UI** | `ui/tab3_t7_ledger.py:1186` | `%H:%M:%S`（**只有時間，沒有日期**） |

⚠️ 上限 `_T7_SCENARIO_LIMIT`，超過砍最舊（`ui/tab3_t7_ledger.py:1189-1191`）。
唯一的持久化管道是使用者手動下載 JSON 備份（`ui/helpers/io/json_backup.py:49`）。

### 3.5 其他未落地的 session 表（重繪即消失）

| 表 . 欄位 | 狀態 | **誰讀它** | 出處（`file:line`） | 備註 |
|---|---|---|---|---|
| `indicators` | 未落地 | **UI** | `app.py:378` 等 14 處 | 總經指標全集，Tab ① 的資料源 |
| `news_items` | 未落地 | **UI** | `ui/tab1_macro.py:2270` 等 8 處 | RSS 新聞列 |
| `fund_data` / `current_fund` | 未落地 | **UI** | `ui/tab5_data_guard.py:247` | 單檔深掘結果 |
| `data_registry` | 未落地 | **UI** | `ui/tab5_data_guard.py:520` | 資料新鮮度註冊表 |
| `phase_info` / `composite_score` / `systemic_risk_data` / `macro_last_update` | 未落地 | **UI** | `ui/tab_fund_grp_health.py:165` / `ui/tab1_macro.py:2282,2400,1805` | |
| `api_latency_log` | 未落地 | **UI** | `ui/tab1_macro.py:2118` | |
| `_hm_card_fetch_tried` / `_hm_card_frames` | 未落地 | **UI** | `shared/session_keys.py:31,34`（SSOT，元組 `:38`） | 熱錢卡 stash |
| `gsheet_tokens` / `gsheet_email` / `_oauth_state` / `custom_oauth_cfg` | 未落地 | **UI** | `ui/sidebar.py:235,238` / `ui/helpers/io/oauth_state.py:90` | OAuth 狀態 |
| `policy_sheet_id` / `active_policy_id` / `policy_tabs` / `_schema_ver` | 未落地 | **UI** | `ui/tab_manage.py:66` / `ui/tab3_portfolio.py:1427,1012` | 有進 JSON 備份（`json_backup.py:50,51`） |

（完整 82 鍵清單可用同一支 AST 腳本重跑；本表只列會影響 UI 資料呈現的那些。）

---

## 4. 設定／密鑰來源（**只列欄位名，一律不列任何實際值**）

### 4.1 `st.secrets`（`.streamlit/secrets.toml`）

| 表 . 欄位 | 狀態 | 出處（`file:line`） | 備註 |
|---|---|---|---|
| `secrets.FRED_API_KEY` | 活的 | `app.py:316`；`ui/helpers/io/data_registry.py:214` | |
| `secrets.GEMINI_API_KEY` | 活的 | `app.py:317`；`ui/helpers/fund_grp_health/ai.py:222` | |
| `secrets.FINMIND_TOKEN` | 活的 | `ui/helpers/macro/ndc.py:65`；`ui/tab1_macro.py:893` | |
| `secrets.POLICY_SHEET_ID` | 活的 | `scripts/weekly_nav_backfill.py:103` | 持倉本 |
| `secrets.NAV_SHEET_ID` | 活的 | `services/nav_history_gs.py:84` | 淨值本；未設 → baked 預設（`:73`） |
| `secrets.POOL_SHEET_ID` | 活的 | `repositories/pool_repository.py:179` | 選股池本；未設 → baked 預設（`:39`） |
| `secrets.macro_weights_sheet_id` | 活的 | `services/macro/weights_store.py:67`；`repositories/portfolio_perf_repository.py:213` | 總經本 |
| `secrets.google_service_account.*`（`type/project_id/private_key_id/private_key/client_email/client_id/auth_uri/token_uri/auth_provider_x509_cert_url/client_x509_cert_url`，**10 個子欄**） | 活的 | `.streamlit/secrets.toml.example`；`repositories/pool_repository.py:186` | |
| `secrets.NAV_GATE0_MODE` | 活的 | `services/nav_history_store.py:494` | 合法值 `enforce`/`observe`/`off`（`:473`） |
| `secrets.PROXY_URL` | 活的 | `infra/proxy.py:56` | NAS Squid |
| `secrets.proxy.{username,password,endpoint}` | 活的 | `infra/proxy.py:58`；`secrets.toml.example` | |
| `secrets.client_id` / `secrets.client_secret` / `secrets.redirect_uri` | 活的 | `ui/helpers/io/oauth_state.py:46,47,48` | Google OAuth |
| `secrets.ALPHAVANTAGE_API_KEY` | 活的 | `repositories/fund/sources.py:2068` | |

### 4.2 環境變數（CI／腳本）

| 表 . 欄位 | 狀態 | 出處（`file:line`） | 備註 |
|---|---|---|---|
| `env.GOOGLE_SERVICE_ACCOUNT_JSON` / `env.GSPREAD_SA_JSON` | 活的 | `scripts/fetch_nav_cache.py:49` | 二擇一 |
| `env.GOOGLE_APPLICATION_CREDENTIALS` | 活的 | `scripts/migrate_v149_schema.py:276` | |
| `env.POLICY_SHEET_ID` / `env.SHEET_ID` | 活的 | `scripts/fetch_nav_cache.py:50` | |
| `env.NAV_SHEET_ID` / `env.POOL_SHEET_ID` / `env.MACRO_WEIGHTS_SHEET_ID` | 活的 | `weekly_nav_backfill.yml` / `fill_pool_currency.yml` / `weekly_switch_notify.yml` | |
| `env.FRED_API_KEY` | 活的 | `ui/tab5_data_guard.py:1110`；`update_macro_history.yml` | |
| `env.FINMIND_TOKEN` | 活的 | `ui/views/page_01_macro.py:328` | |
| `env.GEMINI_API_KEY` / `env.GEMINI_API_KEYS` | 活的 | `ui/tab3_t7_ledger.py:315`；`services/ai_service.py:369` | 後者為多把輪替 |
| `env.PROXY_URL` | 活的 | `scripts/fetch_nav_cache.py:109` | |
| `env.WATCH_CSV_URL` | 活的 | `scripts/weekly_switch_notify.py:129` | §1.10 的來源 |
| `env.LINE_CHANNEL_TOKEN`（別名 `LINE_CHANNEL_ACCESS_TOKEN`） | 活的 | `infra/line_push.py:52` | |
| `env.LINE_USER_ID` | 活的 | `infra/line_push.py:57` | |
| `env.NAV_CODES` | 活的 | `scripts/accumulate_nav_tw.py:48` | |
| `env.FUND_DB` / `env.US_STOCK_IDS` | 活的 | `scripts/export_fund_db.py:184,185` | |
| `env.CHROMIUM_EXECUTABLE_PATH` | 活的 | `infra/html_to_png.py:31` | |
| `env.ALPHAVANTAGE_API_KEY` | 活的 | `repositories/fund/sources.py:2072` | |
| `env.GITHUB_STEP_SUMMARY` / `env.GITHUB_TOKEN` | 活的 | `scripts/weekly_nav_backfill.py:156`；各 workflow | CI 內建 |
| `env.ANTHROPIC_API_KEY` / `env.OPENAI_API_KEY` | ~~查不到（本 repo 無消費路徑）~~ → **活的** | 寫 `app.py:335-338`；讀 `infra/llm.py:71,72` | **2026-09-16 狀態更正**（有意識的更正，不是漏刪 · 決策者：盤點生產組 · 起因：盤點稽核組矛盾 5）。**兩端都在 production，本組逐段實測**：寫入端 `app.py:335-338` `for _llm_key in ("ANTHROPIC_API_KEY", "OPENAI_API_KEY"): _v = _secret(_llm_key); if _v: os.environ[_llm_key] = _v`，而 `app.py:341` `FRED_KEY, GEMINI_KEY = _load_keys()` 在 **module top-level 無條件執行**；讀取端 `infra/llm.py:69-73` 在 `call_llm` 內**每次呼叫都執行**（`:71` anthropic、`:72` openai）。**UI 可達鏈（逐段實測）**：`app.py:673 render_portfolio_tab()` → `ui/tab3_portfolio.py`（`app.py:157` import）→ `ui/tab3_t7_ledger.py:3355 analyze_portfolio_mk_advisor(...)`（`:59` import）→ `services/ai_service.py:341 return call_llm(prompt, max_tokens=5000, gemini_key=api_key)`（def ＠`:130`；`:22` `from infra.llm import _call_gemini, call_llm`）→ `infra/llm.py:71,72`。`_DEFAULT_CHAIN = ["gemini", "anthropic", "openai"]`（`infra/llm.py:33`）——gemini 失敗且該 key 有值時**真的會輪到** anthropic／openai（`:76-93` 的 chain 迴圈）。⚠️ **「key 沒設定」與「沒有消費路徑」是兩件事**，舊表述把前者寫成後者。⚠️ **附帶發現（本批不加列，據實登記）**：§4.1 `st.secrets` 沒有列 `secrets.ANTHROPIC_API_KEY` / `secrets.OPENAI_API_KEY` —— `app.py:336` 的 `_secret()` 讀的就是它們，屬**漏盤**。本批任務是處理矛盾、不是重新盤點，故**不新增表格列**（加列會動到 264 這個已被稽核複核過的總數），登記於此供下一輪補 |

### 4.3 `.streamlit/config.toml`（非機密，值可列）

| 表 . 欄位 | 狀態 | 出處（`file:line`） | 備註 |
|---|---|---|---|
| `config.theme.{base,primaryColor,backgroundColor,secondaryBackgroundColor,textColor}` | 活的 | `.streamlit/config.toml:1-6` | `base = "dark"`，底色 `#0e1117` |
| `config.server.{headless,enableCORS}` | 活的 | `.streamlit/config.toml:8-10` | |
| `config.browser.gatherUsageStats` | 活的 | `.streamlit/config.toml:11-12` | |

---

## 5. 統計

### 5.1 盤點單位

| 分類 | 單位數 | 資料列數 |
|---|---|---|
| §1 Google Sheets 分頁（4 本 Sheet ＋ 1 個外部發佈 CSV） | **11** | 89 |
| §2 本地檔案 / SQLite / 使用者備份 | **20 組** | 82 |
| ~~§3 只活在 session 的表格狀結構~~ → **§3 走 `session_state` 的表格狀結構**（60 列中 **32 列另有落地**；2026-09-16 更正，數法見 §3 標題下。⚠️ **2026-09-16 顆粒度輪補**：「32」是**落地**的列數，**不再等於標「活的」的列數** ——後者自本輪起是 **21**，另 11 列落了地但沒人用，改標「只寫不讀」，見 §3 標題下的狀態更新表） | **6** | 60 |
| §4 設定／密鑰欄位名 | **3 組** | 33 |
| **合計** | **40** | **264** |

**§1 的 11 張**：`Policies`、保單分頁(v1 14 欄)、保單分頁(v2 10 欄)、`_T7_State`、`_Ledgers`、
`_持倉總覽`、`nav_history`、`_fund_pool`、`_portfolio_perf_history`、`_macro_weights`、watchlist CSV。
**§2 的 20 組**＝ §2.1~§2.20（其中 §2.8 一組含 4 個 parquet 檔、§2.18 一組含 3 張 SQLite 表、
§2.20 一組含 5 個範本檔）。
**§3 的 6**＝ `t7_ledgers`／`portfolio_funds`／`batch_codes`＋`batch_rows`／`t7_scenarios`／
`v03_research_batch_rows`／其他 session 鍵（§3.5 彙整）。

⚠️ **「資料列數」不等於「欄位數」**：有些列刻意用一列代表一整組
（例：§1.2a 那一列 ＝ v1 的 14 欄；§2.4 那一列 ＝ `PoolEntry` 10 欄；
§2.13 的 `_meta.csv` 那一列 ＝ 13 個 key；§2.15 那一列 ＝ 11 個 key；
§2.16 最後一列 ＝ 9 個 key；§2.19 的 `portfolio_funds[]` 那一列 ＝ 12 欄）。
**把這些展開後，實際欄位數約 340。** 本表不宣稱精確欄位總數 —— 那要看「一個 JSON blob 算幾欄」怎麼定義。

### 5.2 狀態分布（264 列）

> ⚠️ **2026-09-16 矛盾輪重算（有意識的更正，不是漏刪 · 決策者：盤點生產組）**
> 該輪依 §0 現行定義一致套用後，**21 列換了狀態**（8 ＋ 8 ＋ 4 ＋ 1），故整表重算。
> **總列數 264 一列未增未減**，六個狀態的加總仍為 264（下表末行自驗）。
> **每一列的變動理由都寫在該節就地的更正註**：§2.1（8 列 活的→只讀不寫）、
> §2.18（8 列 活的→只寫不讀）、§2.20（4 列 只讀不寫（人讀）→查不到）、§4.2（1 列 查不到→活的）。
>
> ⚠️ **2026-09-16 客戶裁定輪再重算（有意識的政策變更，不是漏刪 · 日期 2026-09-16 · 決策者：客戶）**
> 客戶裁定「有 production 讀取 ＝ 資料到得了消費者」（§0.1）後，**再有 9 列換狀態**：
> **§1.4 `_Ledgers` 9 欄 活的 → 只寫不讀**。**總列數仍是 264，一列未增未減。**
> **反向也查過**：依舊定義被判「只寫不讀」、但可能其實到得了消費者的候選 ——
> **§2.3 `cache/fund_history.json` 6 欄，實測後維持「只寫不讀」**（`get_history_df` 仍零 caller，
> 指令、正控、負控見該節）。**沒有任何一列翻回「活的」。**

> ⚠️ **2026-09-16 顆粒度輪再重算（有意識的政策變更，不是漏刪 · 日期 2026-09-16 · 決策者：客戶）**
> 客戶裁定「顆粒度：**看欄，不看表**」（§0.3）後，**再有 48 列換狀態，全部是「活的 → 只寫不讀」**，
> 橫跨 **11 張表**（⚠️ 本段初稿寫「9 張表」，**是本組自己數錯**：
> §1.3／§1.6／§1.7／§1.8／§2.2／§2.7／§2.8／§2.10／§2.19／§3.1／§3.2 **共 11 張**，
> 下方翻轉表逐列列得出來，**表列自己就能推翻那個數字**；**48 這個列數一直是對的**）。**總列數仍是 264，一列未增未減；其餘五個狀態一列未動。**
> **逐列清單見下方「本輪翻了哪 48 列」。**
> **反向也查過**：本輪**沒有任何一列翻回「活的」** —— 原本標「只寫不讀」「只讀不寫」「查不到」的列，
> 逐欄查完後**沒有出現「其實到得了畫面」的**（最接近的 13 列反而是**反方向**的張力，
> 已登記在 §0.3「刻意沒翻」表，本輪**不自行裁決**；
⚠️ **2026-09-16 客戶已就那 13 列裁示，但本輪仍未改狀態格 —— 見 §0.3「2026-09-16 客戶裁示重判」段**）。

> ⚠️ ~~**2026-09-16 客戶裁示輪（第四輪）：六個數字一格未動，這是刻意的，不是漏算**~~
> → ✅ **已被 2026-09-16 讀法 A 輪（第五輪）取代：客戶選 A，那 13 列已翻完，六個數字因此動了**
> （**狀態變更，不是漏刪** · 決策者：**客戶**）。**本段以下原文一字未改**，它記的是第四輪為什麼停手。
> 客戶裁示「**盤點表 13 列重判：誰讀它＝內部/無 → 只寫不讀**」。
> **那 13 列的「讀」那一半照收**（客戶已裁定為假），**但狀態格本輪未改** ——
> 目標狀態「只寫不讀」依 §0 是「**有 production 寫入**，查不到讀取端」，
> 而這 13 列**逐列實測後 0／13 有 production 寫入**（weights_store 無 save 函式；
> `fetch_nav_cache.py`／`calibrate_macro_score.py` 兩支 script 都沒有 workflow 掛；
> `promote_to_preset` 零 caller；`t7_scenarios` 只活在 session）。
> **照字面填會在表裡寫進 13 個不存在的寫入端，而反證就在同一節裡。**
> **兩種讀法、各自的完整分布、本組推薦 → §0.3「2026-09-16 客戶裁示重判」段**，客戶回「A」或「B」即可落地。
> **落地後的數字（兩種讀法都仍是 264）**：
> **讀法 A** 活的 111／未落地 26／只寫不讀 102／只讀不寫 9／查不到 14／已停用 2；
> **讀法 B** 活的 111／未落地 28／只寫不讀 89／只讀不寫 9／查不到 25／已停用 2。

> ⚠️ **2026-09-16 讀法 A 輪（第五輪）重算（有意識的政策變更，不是漏刪 · 日期 2026-09-16 · 決策者：客戶）**
> 客戶裁示「**盤點表裁示 4：選 A。照字面翻，同輪補改 §0 定義。**」後，**13 列換狀態**：
> **11 列 只讀不寫 → 只寫不讀**（§1.9 ×2、§2.1 ×5、§2.12 ×1、§2.14 ×2、§2.16 ×1）
> ＋ **2 列 未落地 → 只寫不讀**（§3.4 ×2）。**總列數仍是 264，一列未增未減未合併。**
> **§0 的定義已同輪改完** —— 「只寫不讀」自本輪起是「**有寫入端（不限 production），但查不到消費端**」，
> 完整條文、分流規則與射程見 **§0.4**。
> **活的／查不到／已停用三個狀態本輪一列未動**；「誰讀它」三個數（UI 113／內部 63／無 55）**一格未動**。
> **逐列清單見下方「本輪翻了哪 13 列」；數法與正控見 §5.4。**

| 狀態 | ~~初版~~ → ~~09-16 矛盾輪~~ → ~~09-16 客戶裁定輪~~ → ~~09-16 顆粒度輪~~ → **09-16 讀法 A 輪（現行）** | 佔比 |
|---|---|---|
| **活的** | ~~183~~ → ~~168~~ → ~~159~~ → **111**（含「活的（有落地）」5；**讀法 A 輪未動**） | 42.0% |
| **未落地** | ~~28（四輪未變）~~ → **26**（讀法 A 輪 −2：§3.4 兩列改歸「只寫不讀」） | 9.8% |
| **只寫不讀** | ~~24~~ → ~~32~~ → ~~41~~ → ~~89~~ → **102**（讀法 A 輪 ＋13 ＝ 11 ＋ 2） | 38.6% |
| **只讀不寫** | ~~16（含「只讀不寫（人讀）」4）~~ → ~~20（**已無「（人讀）」變體**）~~ → **9**（讀法 A 輪 −11） | 3.4% |
| **查不到** | ~~11~~ → **14**（含「查不到（production）」4；**讀法 A 輪未動** —— ⚠️ 但其中 **10 列**在新定義下是「該不該一起翻」的候選，已逐列登記於 **§0.4 (3)**，**本輪一格未改、兩個方向都沒裁決**） | 5.3% |
| **已停用** | 2（五輪未變） | 0.8% |
| **合計** | **264** ✅（~~111＋28＋89＋20＋14＋2~~ → **111＋26＋102＋9＋14＋2**） | 100.0% |

~~**逐節數（顆粒度輪實跑，`count_status.py` 輸出照抄）**：~~
~~§1 **89**（活的 41／只寫不讀 44／只讀不寫 4）、~~
~~§2 **82**（活的 16／只寫不讀 34／只讀不寫 16／查不到 14／已停用 2）、~~
~~§3 **60**（活的 21／只寫不讀 11／未落地 28）、~~
~~§4 **33**（活的 33）。89＋82＋60＋33 ＝ **264** ✅~~
→ **逐節數（2026-09-16 讀法 A 輪實跑，`count_status.py` 輸出照抄；該程式本輪一個字都沒改）**：
§1 **89**（活的 41／只寫不讀 **46**／只讀不寫 **2**）、
§2 **82**（活的 16／只寫不讀 **43**／只讀不寫 **7**／查不到 14／已停用 2）、
§3 **60**（活的 21／只寫不讀 **13**／未落地 **26**）、
§4 **33**（活的 33）。89＋82＋60＋33 ＝ **264** ✅
**逐節總數 89／82／60／33 五輪未變**，動的只有各節內部的分布。

**本輪翻了哪 48 列（活的 → 只寫不讀）**

| 節 | 表 | 幾列 | 哪幾欄 | 一句話理由（逐列完整依據寫在該列備註） |
|---|---|---|---|---|
| §1.3 | `_T7_State` | **4** | `fund_code`／`currency`／`policy_id`／`updated_at` | 前三欄 `load_all_ledgers_snapshot` 根本不取；`updated_at` 只進 `_sheet_stats`（**無讀取端**） |
| §1.6 | `nav_history` | **3** | `fund_name`／`source`／`recorded_at` | `load_points` 讀進 dict，4 個消費端**沒有一個取用** |
| §1.7 | `_fund_pool` | **2** | `note`／`added_at` | 選股池表格不含這兩欄，新增表單也沒有輸入框 |
| §1.8 | `_portfolio_perf_history` | **13** | `date`／`period_return_pct` 以外全部 | 只經 `getattr`／`zip`／`asdict` 原樣往返 |
| §2.2 | `cache/nav_history/*` | **1** | `timestamp` | 只拿來算 24h TTL |
| §2.7 | `cache/fred_release/*` | **2** | `release_id`／`cached_at` | 前者從未被讀回；後者只算 30 天 TTL |
| §2.8 | `data_cache/*.parquet` | **9** | 全部 | 兩個 production 讀取端都是 workflow script，**沒有一條通到畫面** |
| §2.10 | `data_cache/ai_cache.json` | **1** | `updated_at` | 只供 `save()` 的 LRU 汰舊排序 |
| §2.19 | `portfolio_backup` | **2** | `schema_version`／`exported_at` | `restore_from_json_bytes` **沒有讀這兩欄** |
| §3.1 | `t7_ledgers` | **10** | `fund_code` ＋ `transactions[]` 8 欄 ＋ `position.dividends_received_twd` | `transactions[]` 在 `ui/**` 零消費端；唯一逐筆讀它的 `calculate_xirr` **零 caller** |
| §3.2 | `portfolio_funds` | **1** | `fx_at_buy` | 只被備份匯出讀走；三個寫入端固定填 `0.0` |
| | **合計** | **48** | | |

4＋3＋2＋13 ＝ **22**（§1）；1＋2＋9＋1＋2 ＝ **15**（§2）；10＋1 ＝ **11**（§3）；22＋15＋11 ＝ **48** ✅

⚠️ **與總管給的基準（活的 159／只寫不讀 41；逐節 89／82／60／33）差在哪**：
**只差這 48 列，而且差的原因就是客戶那句裁示本身**（§0.3「看欄，不看表」）——
基準值是**裁示前**的數字，那 48 欄當時掛在「活的」是因為**判準單位是整張表**。
**不是重數出來的誤差，是顆粒度換了。** 本輪**沒有**重新盤點、**沒有**新增或刪除任何一列，
`count_status.py` **一個字都沒改**（正控見 §5.4）。

**~~本輪~~（客戶裁定輪，＝第三輪）只動了 9 列，全部是 §1.4 `_Ledgers`**：活的 → 只寫不讀。
**活的 168 − 9 ＝ 159；只寫不讀 32 ＋ 9 ＝ 41；其餘四個狀態一列未動；總數 264 不變。**
⚠️ **與總管給的基準（活的 168／只寫不讀 32）差在哪**：
**只差這 9 列，而且差的原因就是客戶那句裁定本身**（§0.1「資料到得了消費者」）——
基準值是**裁定前**的數字，`_Ledgers` 當時掛在「活的」是因為「讀取」沒有被界定（見 §1.4 的刪除線原文）。
**不是重數出來的誤差，是定義換了。** 本輪**沒有**重新盤點、**沒有**新增或刪除任何一列。

**本輪（2026-09-16 讀法 A 輪，＝第五輪）翻了哪 13 列**

| 節 | 表／檔 | 幾列 | 哪幾欄 | 舊 → 新 | 一句話理由（逐列完整實測依據寫在該列備註） |
|---|---|---|---|---|---|
| §1.9 | `_macro_weights` | **2** | `A(slot)`／`C(updated_at)` | 只讀不寫 → 只寫不讀 | 值在 A3／C3 儲存格、**人工在 Google Sheets 手填**；`_gs_load` 只讀 B 欄 |
| §2.1 | `cache/nav/{CODE}.json` | **5** | `code`／`fund_name`／`updated_at`／`source`／`count` | 只讀不寫 → 只寫不讀 | 值在 **已 commit 進 repo 的 `TLZF9.json`**；`_src_cache_files` 只取 `history` 與 `updated_at`（後者只進 `print()`／`attrs`） |
| §2.12 | `macro_thresholds_global.json` | **1** | `status` | 只讀不寫 → 只寫不讀 | 寫入端是**手動 CLI**；讀它的也只有同一支 CLI 自己。⚠️ **13 列裡唯一「值不在 repo 檔案裡」的一列** |
| §2.14 | `config/preset_funds.json` | **2** | `funds[].code`／`.name` | 只讀不寫 → 只寫不讀 | 值在 **已 commit 進 repo 的檔案**、只能人手編輯（`promote_to_preset` 零 caller）；兩條讀取鏈都不上畫面 |
| §2.16 | `services/config/macro_weights_active.json` | **1** | `.version` | 只讀不寫 → 只寫不讀 | 值在 **已 commit 進 repo 的檔案**（實測只有 `version`／`indicators` 兩個鍵）；`_validate` 只檢查「這個 key 在不在」 |
| §3.4 | `t7_scenarios` | **2** | `id`／`nav_fx_cache` | **未落地** → 只寫不讀 | 寫入是 **production UI 路徑**但不持久化；`id` 只當 `st.button` 的 widget key，`nav_fx_cache` 全 repo 無人取回 |
| | **合計** | **13** | | | |

2＋5＋1＋2＋1 ＝ **11**（只讀不寫 → 只寫不讀）；**2**（未落地 → 只寫不讀）；11＋2 ＝ **13** ✅
節別小計：§1 **2**、§2 **9**、§3 **2**。

⚠️ **與總管給的基準（活的 111／只寫不讀 89／只讀不寫 20／未落地 28）差在哪**：
**只差這 13 列，而且差的原因就是客戶那句裁示本身**（「選 A」＋ §0.4 的新定義）——
基準值是**裁示落地前**的數字。**不是重數出來的誤差，是定義換了。**
本輪**沒有**重新盤點、**沒有**新增或刪除任何一列，`count_status.py` **一個字都沒改**（正控見 §5.4）。
⚠️ **§3.4 那 2 列與另 11 列不同族，不要混為一談**：另 11 列離開的是「只讀不寫」（它們的「讀」被客戶裁定為假），
**這 2 列離開的是「未落地」**（它們的「讀」本來就沒被當成 production 讀取，動的是 §0.4 新補的分界）。

~~**「只寫不讀」41 列的來源**：**`_Ledgers` 9 欄（客戶裁定輪新增，§1.4）**、`_持倉總覽` 13 欄、~~
~~`cache/fund_history.json` 6 欄、`data_cache/metadata.json` 5 欄、`fund.db` 8 列（§2.18）。~~
→ ~~**2026-09-16 顆粒度輪更新（狀態變更，不是漏刪）：「只寫不讀」現為 89 列**~~
~~＝ 上列 **41 列**（一列未動）＋ **本輪新增的 48 列**（清單見上方翻轉表）。~~
→ **2026-09-16 讀法 A 輪再更新（狀態變更，不是漏刪 · 決策者：客戶）：「只寫不讀」現為 102 列**
＝ **41**（矛盾輪／客戶裁定輪）＋ **48**（顆粒度輪）＋ **13**（本輪，清單見上方 13 列翻轉表）。
⚠️ **這個桶子自本輪起同時裝兩種東西**：前 89 列**有 production 寫入**，新進來的 13 列**沒有**
（寫入端是手動 CLI／人工編輯 ＋ git commit／人工填 Google Sheets／session）。
**分辨方式：看該列備註裡就地註明的寫入端種類**（§0.4 (1) 的硬要求）。
~~**「只讀不寫」20 列的來源**：`_macro_weights` 3 欄、watchlist CSV 1 欄、~~
~~`data_cache/macro_thresholds_global.json` 3 欄、`config/preset_funds.json` 的 `funds[].code`／`.name` 2 欄、~~
~~`services/config/macro_weights_active.json` 3 列、**`cache/nav/*` 8 列（矛盾輪新增，§2.1）**。~~
→ **2026-09-16 讀法 A 輪更新（狀態變更，不是漏刪）：「只讀不寫」現為 9 列**，全部是**「誰讀它 ＝ UI」**的那些（11 列「內部／無」已翻走）：
`_macro_weights.B(payload_json)` 1、watchlist CSV `代號` 1、`cache/nav/*.history[]` 系列 **3**、
`macro_thresholds_global` 的 `VIX_CRISIS_THRESHOLD`／`VIX_WARNING_THRESHOLD` **2**、
`services/config/macro_weights_active.json` 的 `.indicators{}` 與 9-key 群組列 **2**。1＋1＋3＋2＋2 ＝ **9** ✅
⚠️ **這 9 列為什麼沒有一起翻成「活的」**：它們的寫入端也不是 production，但本狀態的寫入端**仍用嚴格尺**——
**這是刻意的分流，理由與實測見 §0.4 (2)。**
**「查不到」14 列的來源**（**讀法 A 輪一列未動**）：`/tmp/fund_cache/*` 3 組、`snap.json` 4 欄、
`config/macro_weights_active.json`（根）1 組、`cache/fundclear/*` 1 組、
`preset_funds._comment` 1 欄、**`templates/` 4 列（矛盾輪新增，§2.20）**。
⚠️ **其中 10 列在 §0.4 的新定義下是「該不該一起翻成只寫不讀」的候選**
（`preset_funds._comment` 1、`config/macro_weights_active.json`（根）1、`snap.json` 4、`templates/` 4）——
**本輪一格未改、兩個方向都沒裁決**，逐列登記與實測見 **§0.4 (3)**。
另 4 列（`cache/fundclear/*` 1、`/tmp/fund_cache/*` 3）本組判定**新尺也碰不到**，理由同處。
~~`env.ANTHROPIC_API_KEY`／`env.OPENAI_API_KEY` 1 列~~ → **已移出「查不到」**（矛盾輪改為「活的」，§4.2）。

### 5.3 「位元組讀回？」的分布（本輪新增欄，只填 5 張表 43 列）

| 狀態 ／ 位元組讀回？ | 列數 | 是哪幾張 | 對新 UI 的意思 |
|---|---|---|---|
| **只寫不讀 ／ 是** | **15** | §1.4 `_Ledgers` 9 ＋ §2.3 `cache/fund_history.json` 6 | **最便宜的一類**：資料已經完整讀回記憶體了，**只是沒人用** → 接個消費端即可 |
| **只寫不讀 ／ 否** | **26** | §1.5 `_持倉總覽` 13 ＋ §2.9 `metadata.json` 5 ＋ §2.18 `fund.db` 8 | 要**從零寫一條讀取路徑**（`fund.db` 例外：它的讀取端在下游 repo，見該節） |
| **已停用 ／ 否** | **2** | §2.18 `fund.db::us_market`／`::fx` | 表根本沒被建出來，沒有位元組可讀 |
| **合計** | **43** | **5 張表** | 41 列狀態為「只寫不讀」（＝本表全部的只寫不讀）＋ 2 列「已停用」 |

**「查不到」0 列** —— 本輪 43 列全部查得到答案，沒有用到這個值。
⚠️ **其餘 221 列刻意留白**，理由見 §0.2（對其他四個狀態這一欄不帶新資訊）。
~~**「只寫不讀」共 41 列，本欄填了 41 列 —— 覆蓋率 41/41。**~~
→ ⚠️ **2026-09-16 顆粒度輪補正（狀態變更，不是漏刪 · 決策者：盤點生產組）**：
**這句自本輪起不成立** —— 顆粒度輪把 **48 列**由「活的」翻成「只寫不讀」，
「只寫不讀」從 41 列變成 **89 列**，而**本欄一格都沒有新增**（上表 43 列原封不動，
`count_bytes.py` 編輯前後輸出逐字相同）。
~~**現行覆蓋率：41/89。** 未填的 **48 列** 就是本輪新翻的那批。~~
**為什麼不順手補**：「位元組讀回？」要逐列實測「有沒有任何路徑把位元組讀回記憶體」，
與本輪查的「有沒有人用它的值」**是兩個不同的量測**；沿用本輪的查證結果去推它，
等於拿 A 的證據去填 B 的欄位。**本輪授權是套用顆粒度裁示，不是補測另一欄**，故據實留白並登記於此。

→ ⚠️ **2026-09-16 讀法 A 輪再補正（狀態變更，不是漏刪 · 決策者：客戶）**：
讀法 A 輪再把 **13 列**翻進「只寫不讀」（11 列來自「只讀不寫」、2 列來自「未落地」），
「只寫不讀」由 89 列變成 **102 列**，而**本欄一格都沒有新增**
（上表 43 列原封不動；`count_bytes.py` 編輯前後**每一個數字都相同**，見 §5.4 的程式對照表 ——
⚠️ **不是「`diff` 回 0 行」**：該程式最後一行用 set comprehension 印 dict，順序吃 hash 隨機化，
**同一個檔案連跑兩次就可能不同順序**，比的時候要比數字）。
**現行覆蓋率：41/102。** 未填的 **61 列** ＝ 顆粒度輪的 48 ＋ 本輪的 13。
**不順手補的理由與上一輪一模一樣**，一個字都不必改：那是另一個量測。
⚠️ **本輪這 13 列的方向同樣不能整批推定**：`cache/nav/*.updated_at` 明顯是「**是**」
（`_src_cache_files:1246` 真的把它讀回記憶體），而同一張表的 `code`／`fund_name`／`source`／`count`
**連讀都沒讀**，方向相反。**同一張表內就兩種都有 —— 要補就得逐列量。**
⚠️ **可以先講的方向（登記，非結論）**：新翻的 48 列多數屬「**位元組讀回＝是**」那一類
（資料真的被讀回記憶體，只是沒人用），例如 §1.8 的 13 欄走 `dict(zip(_HEADERS, row))` 整列還原；
但 §1.3 的 `fund_code`／`currency`／`policy_id` 連讀都沒讀，方向相反。
**兩種都有，所以不能整批推定 —— 要補就得逐列量。**

**重算方法（兩支小程式，可複跑，不經任何 `grep -v`）**

**(a) 狀態計數** —— 以 markdown 表格列 parser 逐列取**第 2 欄**，
去掉 `**` 與全形括號補語、取 `~~舊值~~ →` 之後的新值，**只認 §0 那六個封閉狀態值**
（不在集合內的一律跳過，因此 §1 開頭的「4 本 Sheet」表、§5.1 統計表本身、§3 標題下的分節計數表、
本節的 §5.3 分布表都不會被誤計）；只掃 §1~§4 四節。
⚠️ **新增的「位元組讀回？」欄刻意插在第 3 欄，不是第 2 欄** —— 就是為了讓這支既有的數法**一字不改**還能跑。
**輸出（本輪實跑）**：§1 **89** 列（活的 63／只寫不讀 22／只讀不寫 4）、
§2 **82** 列（活的 31／只寫不讀 19／只讀不寫 16／查不到 14／已停用 2）、
§3 **60** 列（活的 32／未落地 28）、§4 **33** 列（活的 33）。
89＋82＋60＋33 ＝ **264** ✅；159＋28＋41＋20＋14＋2 ＝ **264** ✅
**正控**：同一支程式對**編輯前**的檔案跑，回 §1 89（活的 72／只寫不讀 13／只讀不寫 4）、
活的 **168**／只寫不讀 **32**，**與總管給的基準逐項相符** —— 證明數法本身沒換，換的只是被數的內容。

**(b) 新欄計數** —— 同一個 parser，但只認**表頭第 3 欄為「位元組讀回？」**的那幾張表，
取其資料列第 3 欄，只認 `{是, 否, 查不到}`。
**輸出（本輪實跑）**：§1.4 只寫不讀／是 9；§1.5 只寫不讀／否 13；§2.3 只寫不讀／是 6；
§2.9 只寫不讀／否 5；§2.18 只寫不讀／否 8 ＋ 已停用／否 2 —— **5 張表 43 列**（是 15／否 28／查不到 0）。

~~**兩支程式放在**~~ → **三支程式放在** `scratchpad/inv_count/`：`count_status.py`、`count_bytes.py`、
**`count_who.py`（2026-09-16 顆粒度輪新增）**，
**用法**：`python3 count_status.py DB_INVENTORY.md` / `count_bytes.py` / `count_who.py`
（**純 stdlib，不需要 `pandas`**）。

### 5.4 「誰讀它」的分布（2026-09-16 顆粒度輪新增欄，填 §1～§3 全部 231 列）

**覆蓋率 231/231**（§1 89 ＋ §2 82 ＋ §3 60）；**§4 的 33 列刻意留白**，理由見 §0.3。
**「查不到」0 列 —— 本輪 231 列全部查得到答案，沒有用到這個值。**

| 誰讀它 | 列數 | 佔 231 | 對新 UI 的意思 |
|---|---|---|---|
| **UI** | **113** | 48.9% | 這一欄現在就看得到，重新設計時可以直接排版 |
| **內部** | **63** | 27.3% | 資料讀得回來，**但沒有任何畫面用它** → 要用就得接一個消費端（多半便宜） |
| **無** | **55** | 23.8% | **連讀取端都沒有** → 要用得從零寫一條讀取路徑 |
| **查不到** | **0** | 0% | — |
| **合計** | **231** | 100% | |

**逐節（本輪實跑，`count_who.py` 輸出照抄）**：
§1 **89**（UI 43／內部 28／無 18）、§2 **82**（UI 23／內部 22／無 37）、§3 **60**（UI 47／內部 13／無 0）。
43＋23＋47 ＝ **113**；28＋22＋13 ＝ **63**；18＋37＋0 ＝ **55**；113＋63＋55 ＝ **231** ✅

**交叉表（狀態 × 誰讀它）—— 這張才是本輪真正的產出**
**（⚠️ 2026-09-16 讀法 A 輪更新：三列換了值，右邊加一欄「讀法 A 輪前」供對照；`count_who.py` 一個字都沒改）**

| 狀態 | UI | 內部 | 無 | 小計 | ~~讀法 A 輪前的小計~~ |
|---|---|---|---|---|---|
| **活的** | **78** | 0 | 0 | 78 | 78（未動） |
| **只寫不讀** | 0 | **63** | **39** | **102** | ~~89~~（0／57／32） |
| **只讀不寫** | **9** | **0** | **0** | **9** | ~~20~~（9／4／7） |
| **查不到** | 0 | 0 | 14 | 14 | 14（未動） |
| **已停用** | 0 | 0 | 2 | 2 | 2（未動） |
| **未落地** | **26** | **0** | **0** | **26** | ~~28~~（26／2／0） |
| **合計** | **113** | **63** | **55** | **231** | **231**（三個直欄一格未動） |

⚠️ **「活的」那一列小計是 78 不是 111** —— 差的 33 列是 **§4**（刻意留白，§0.3）。
✅ **本輪要達成的不變量就是第一列**：套用客戶的規則之後，
**「活的」不再有任何一列的「誰讀它」是「內部」或「無」**（該列後兩格皆為 0）。
⚠️ ~~**「只讀不寫」與「未落地」兩列還留著 11 格與 2 格的張力** —— 那 13 列**刻意沒翻**，~~
~~逐列登記在 **§0.3「刻意沒翻」表**，本輪**不自行裁決**。~~
~~→ **2026-09-16 客戶裁示輪更新**：客戶已裁示「13 列重判」，**本輪仍未改這 13 格**……~~
→ ✅ **2026-09-16 讀法 A 輪：那 11 格與 2 格已全部清零**（**狀態變更，不是漏刪** · 決策者：**客戶**）。
客戶裁示「**選 A。照字面翻，同輪補改 §0 定義。**」→ 13 列翻完、§0 定義同輪改完（**§0.4**）。

✅ **本輪要達成的不變量（實跑驗證，三項全過）**：
1. **「活的」那一列的「內部」「無」兩格仍是 0**（上一輪已達成，本輪未破壞）。
2. **「只讀不寫」與「未落地」兩列的「內部」「無」四格全部歸 0** —— 這是本輪新達成的。
3. → 合起來：**現在只有「只寫不讀」「查不到」「已停用」三個狀態底下會出現「內部」或「無」**，
   而那三個狀態的定義本來就是「沒有消費端」。**交叉表自本輪起與 §0／§0.4 的定義完全對齊，零例外。**
**驗法**：交叉格「狀態 ∉（只寫不讀, 查不到, 已停用）且 誰讀它 ∈（內部, 無）」→ 本輪實跑 **0 列**
（同一支程式對編輯前的檔跑 → **13 列**，逐列與 §0.3「刻意沒翻」表相符 —— 這就是本輪的正控）。
⚠️ **本輪六個狀態數字動了 13 格**（264 不變；111／**26**／**102**／**9**／14／2），
**「誰讀它」三個數一格未動**（UI 113／內部 63／無 55 ＝ 231）—— 裁示改的是「狀態」欄，不碰「誰讀它」欄。
⚠️ **「本表沒有第 14 處張力」本組同樣不宣稱**：上面那個交叉格只看得到**有填「誰讀它」的 231 列**，
**§4 的 33 列沒填**（§0.3 刻意留白），而且**群組列**依定義就藏得住逐欄層的張力（§0.3 已列出 3 列群組列）。

**重算方法（第三支小程式，可複跑，不經任何 `grep -v`）**

**(c) 「誰讀它」計數** —— 同一套 markdown 表格列 parser，但**欄位位置由表頭決定、不寫死**：
逐表找出表頭列裡哪一格是「誰讀它」，資料列就取那一格，只認 `{UI, 內部, 無, 查不到}`；
同時取第 2 欄的狀態做交叉表。
⚠️ **為什麼不寫死欄序**：這一欄在**有**「位元組讀回？」的 5 張表是**第 4 欄**、在其餘表是**第 3 欄** ——
寫死任何一個都會漏掉另一半。**這正是 `count_bytes.py` 當初用「表頭是不是『位元組讀回？』」
當條件的同一個理由。**

**三支程式的正控／負控（顆粒度輪實跑，輸出照抄；下方另有讀法 A 輪的同型表）**

| 程式 | 對**編輯後**的檔 | **正控**（對**編輯前**的檔跑） | **負控** |
|---|---|---|---|
| `count_status.py` | 活的 111／只寫不讀 89／只讀不寫 20／查不到 14／已停用 2／未落地 28 ＝ **264** | 活的 **159**／只寫不讀 **41**／只讀不寫 20／查不到 14／已停用 2／未落地 28 ＝ **264** —— **與總管給的基準逐項相符** | — |
| `count_bytes.py` | 5 張表 43 列（是 15／否 28） | **同左，逐字相同** —— 證明本輪**沒有動到「位元組讀回？」欄一格** | — |
| `count_who.py` | 231 列（UI 113／內部 63／無 55） | — | **對編輯前的檔跑 → 四節皆「本節未填『誰讀它』」、合計 0** —— 證明它真的在讀那一欄，不是在數別的東西 |

**編輯前的檔**留在 `scratchpad/inv_count/DB_INVENTORY.before_granularity.md`（本輪起點），
另有 `DB_INVENTORY.before.md`（更早一輪的起點，**未動**）。

**2026-09-16 讀法 A 輪的正控／負控（本輪實跑，輸出照抄）**

**本輪另外自寫了第四支 parser 做交叉驗證**（`scratchpad/work_pt/p2.py`），
**刻意與 `count_status.py` 不同寫法**：狀態正規化是**先整段丟掉 `~~…~~`（刪除線內容）**、再去 `**`／反引號／
全形括號補語；既有那支用的是 `s.split("→")[-1]`。**兩種寫法對同一格必須給同一個答案，不同就是有人錯。**
⚠️ **為什麼要換寫法**：本輪大量新增的狀態格長成 `~~只讀不寫~~ → **只寫不讀**`，**一格裡同時含兩個狀態詞**——
**先丟刪除線**與**取箭頭之後**是兩條獨立的路徑，兩條都走到同一個答案，這個數字才算被驗過。

| 程式 | 對**編輯後**的檔 | **正控**（對**編輯前**的檔跑） | **負控** |
|---|---|---|---|
| `count_status.py`（既有，本輪一個字未改） | 活的 111／只寫不讀 **102**／只讀不寫 **9**／查不到 14／已停用 2／未落地 **26** ＝ **264** | 活的 111／只寫不讀 **89**／只讀不寫 **20**／查不到 14／已停用 2／未落地 **28** ＝ **264** —— **與總管給的基準逐項相符** | — |
| `count_bytes.py`（既有，本輪一個字未改） | 5 張表 43 列（是 15／否 28／查不到 0）；§1.4 是 9／§1.5 否 13／§2.3 是 6／§2.9 否 5／§2.18 否 8 ＋ 已停用否 2 | **每一個數字都相同** —— 證明本輪**沒有動到「位元組讀回？」欄一格**。⚠️ **不要寫成「`diff` 會回 0 行」，那句話會被照跑推翻**：最後一行「依狀態：{…}」是用 **set comprehension** 印的（`count_bytes.py:68`），**key 的順序吃 Python 的字串 hash 隨機化，逐次執行都可能不同** —— 本組拿**同一個檔案**連跑 5 次，就跑出兩種順序。**那是執行期噪音，不是內容差異；要比就比數字，不要比 `diff` 的行數。** | — |
| `count_who.py`（既有，本輪一個字未改） | 231 列（UI 113／內部 63／無 55／查不到 0）；逐節 §1 89／§2 82／§3 60／§4 0 | **「誰讀它」那一半逐字相同** —— 逐節四行、四個總數、合計 231 **完全一樣**，證明本輪**沒有動到「誰讀它」欄一格**。⚠️ **但不要說成「整支輸出逐字相同」**：這支程式同時印**交叉表**，而交叉表吃的是「狀態」欄，本輪改了 13 格狀態 → **交叉表那一段本來就會變，而且必須變**（編輯前 `只讀不寫 無7／UI9／內部4`、`未落地 UI26／內部2`；編輯後 `只讀不寫 UI9`、`未落地 UI26`）。**照跑會看到差異，那是對的。** | 對更早一輪的起點（`DB_INVENTORY.before.md`，那時還沒有這一欄）跑 → 四節皆「本節未填『誰讀它』」、UI/內部/無/查不到 全 0、合計 **0** —— 證明它真的在讀那一欄，不是在數別的東西 |
| **`p2.py`（本輪自寫的第二支，刪除線解法）** | 逐節 §1 89／§2 82／§3 60／§4 33；狀態 111／102／9／14／2／26 ＝ **264**；誰讀它 113／63／55 ＝ **231** —— **與上面三支逐項相符** | 對編輯前的檔跑 → 111／89／20／14／2／28 ＝ **264**，**同樣與基準逐項相符** | 交叉格傾印：編輯後 **0 列**、編輯前 **13 列**（§1.9 ×2／§2.1 ×5／§2.12 ×1／§2.14 ×2／§2.16 ×1／§3.4 ×2） |

**逐節數五輪未變：§1 89／§2 82／§3 60／§4 33 ＝ 264。**
**負控**：本輪另跑了一條「應該 0 命中」的假符號掃描（`git grep -n "<負控串>" origin/main -- .` → **0 命中，exit=1**），
正控同批跑（`git grep -n "load_active" origin/main -- '*.py'` → 11 命中；
`git grep -n "export_fund_db" origin/main -- '.github/workflows/*'` → 命中 `export_db.yml:39`）。
**負控為本輪自訂的隨機串，依 §0 方法規則不列出**（寫進文件它自己就成了命中，當場失效）。
⚠️ **`count_status.py` / `count_bytes.py` 本輪一個字都沒改** ——
新欄插在「位元組讀回？」旁邊（有該欄的表插第 4 欄、其餘插第 3 欄），
**狀態欄始終是第 2 欄**，兩支既有程式因此不必改就還能跑。**這是刻意的欄序安排，不是巧合。**

#### 本輪自查抓到的兩個自製錯誤（誠實登記，不是別人抓的）

本輪產出的**第一版**有兩處錯，都是在自己回頭跑檢查時抓到的，就地記下來免得下一輪重犯：

1. **一個假的 grep 宣稱（比欄位值錯更嚴重）**：§1.8 那 13 列的備註原本套同一句模板，
   宣稱 `git grep -n "\.<欄名>\b" … → **0 行**`。**其中 `.sharpe` 那一列是假的 —— 實測 5 行。**
   那 5 行全是講**單一基金的 `metrics.sharpe`** 的註解／docstring，
   與 `PerfSnapshot.sharpe`（投組層快照）**同名不同物**。
   **結論沒有錯（本欄確實沒有消費端），錯的是那句可以被照跑、而且照跑會得到 5 的宣稱。**
   已就地改成實測值 ＋ 逐行判讀理由。
   ⚠️ **這正是派工單方法紀律第 5 條點名的子字串陷阱**（`TER` 命中 `MATERIAL_RED` 那一類），
   只是這次是**同名不同物件**的版本 —— **套用模板批次產生備註時最容易長出這種東西。**
2. **備註裡的裸 `|` 把 markdown 表格切多了一欄**：§2.8 那 9 列的備註引了
   `git grep -nE "data_cache|parquet" …`，**那個管線符被當成欄位分隔符**，9 列各多出一欄。
   已改成跳脫寫法 `data_cache\|parquet`。
   ⚠️ **連帶修了檢查器本身**：`check_tables2.py` 原本直接 `split("|")`，**看不懂 `\|`** ——
   修好之後才驗得出「跳脫過的那 9 列是對的」。**檢查器自己的負控（故意插一列少一欄）也補上了。**

⚠️ **這兩個都是本組自己回頭檢查抓到的，沒有第二組看過**；
**「本輪沒有第三個自製錯誤」本組不宣稱。**

## 6. 給 UI 重新設計用的重點發現

**發現 1｜同一種分頁有兩套 schema，而且主鍵不同。**
保單分頁被 `upsert_fund_in_policy`（`repositories/policy/v2.py:240`）以 **`fund_url` 為主鍵**寫 14 欄，
也被 `write_policy_v2`（`:794`）以 **`fund_code` 為主鍵**寫 10 欄。
`ALL_COLS_V2`（`:506`）**沒有 `fund_url`**，`ALL_COLS`（`_helpers.py:50`）**沒有 `fund_code`**。
`detect_sheet_schema_version`（`v2.py:627`）決定走哪條；兩條路徑在 production 都活著。

**發現 2｜`_持倉總覽` 寫了沒人讀。** 13 欄成本資料每次 T7 落帳都整張覆寫，但 repo 內沒有任何讀取端。
要做「持倉總覽」畫面的話，資料要從 `_T7_State.ledger_json` 或 session 的 `t7_ledgers` 取，不是從這張表。

**發現 2b｜「寫了沒人讀」有兩種，成本差很多 —— 2026-09-16 客戶裁定後新增的「位元組讀回？」欄就是在分這個。**
（詳見 §0.2 與 §5.3；下表是給設計用的摘要）

| 這一類 | 是哪幾張 | 列數 | 要做畫面的話 |
|---|---|---|---|
| **只寫不讀 ＋ 位元組讀回「是」** | §1.4 `_Ledgers`（9）、§2.3 `cache/fund_history.json`（6） | **15** | **最便宜**：資料已經完整讀回記憶體了，只是沒人用。`_Ledgers` 已經是整張 DataFrame（9 欄型別都正規化好）；`fund_history` 連 `get_history_df()` 都寫好了、只是零 caller。**接個消費端即可，不必新寫取數。** |
| **只寫不讀 ＋ 位元組讀回「否」** | §1.5 `_持倉總覽`（13）、§2.9 `metadata.json`（5）、§2.18 `fund.db`（8） | **26** | **要從零寫一條讀取路徑。**（`fund.db` 是例外：它的讀取端在**下游 repo**，本 repo 射程外） |

⚠️ **這一欄只填了這 5 張表 43 列**，其餘 221 列刻意留白（理由見 §0.2）。

**發現 3｜`_fund_pool` 同時是選股池 ＋ 全站唯一的「code → ISIN / 晨星 secId / 幣別」對照表。**
（`repositories/pool_repository.py:31`）。它被 `repositories/fund/sources.py` 8 處呼叫、多半是**別名匯入**。
任何「找不到淨值」的畫面，缺的很可能是這張表的 `isin` / `morningstar_secid`。

**發現 4｜`_macro_weights` 與 `services/config/macro_weights_active.json` 都是唯讀。**
總經權重校準要生效，使用者得**自己去 Google Sheet 手填 B3 那一格**——UI 上沒有任何寫入入口。
（`_gs_get_worksheet(for_write=True)` 零 call site；模組無 `save_active`。）

**發現 5｜`nav_history` 是唯一不可再生的。** 其餘分頁都能從 App 重生（`ui/tab6_manual.py:652`）。

**發現 6｜說明書寫「6 種分頁、同一本 Sheet」，實際是「9 種分頁、4 本 Sheet」。**

`ui/tab6_manual.py:631`（`awk 'NR==631'` 原文）：
「系統目前會讀寫**這 6 種分頁**，平時各動作（批次加入、T7 套用、CSV 匯入）會自動同步到對應分頁。」
下方 `:634-641` 的表格列出 6 列：📋 保單分頁 ／ 📄 `Policies` ／ 🗂️ `nav_history` ／
📸 `_T7_State` ／ 📜 `_Ledgers` ／ 📊 `_持倉總覽`。

**實際是 9 種、分屬 4 本**（本組逐檔實測，Sheet ID 一律**值不列出**）：

| # | 分頁 | 哪一本 | 本子的 ID 由誰決定（出處） | 說明書有列嗎 |
|---|---|---|---|---|
| 1 | **保單分頁**（分頁名 ＝ `policy_id`） | 持倉本 | `st.session_state["policy_sheet_id"]`／secret `POLICY_SHEET_ID` | ✅ |
| 2 | **`Policies`** | 持倉本 | 同上 | ✅ |
| 3 | **`_T7_State`** | 持倉本 | 同上 | ✅ |
| 4 | **`_Ledgers`** | 持倉本 | 同上 | ✅ |
| 5 | **`_持倉總覽`** | 持倉本 | 同上 | ✅ |
| 6 | **`nav_history`** | **淨值本（另一本）** | secret `NAV_SHEET_ID` → baked 預設（`services/nav_history_gs.py:76-85` `_nav_sheet_id()`；`:68-72` 註解自陳「v19.472：NAV 淨值存進**獨立一本**」） | ✅（但被寫成同一本） |
| 7 | **`_fund_pool`** | **選股池本（第三本）** | secret `POOL_SHEET_ID` → baked 預設（`repositories/pool_repository.py:178-180`；`:36-38` 註解自陳「與持倉 `POLICY_SHEET_ID` **不同本**」） | ❌ **漏列** |
| 8 | **`_portfolio_perf_history`** | **總經本（第四本）** | secret `macro_weights_sheet_id`（`repositories/portfolio_perf_repository.py:213` `require_secret("macro_weights_sheet_id")`） | ❌ **漏列** |
| 9 | **`_macro_weights`** | **總經本（第四本）** | secret `macro_weights_sheet_id`（`services/macro/weights_store.py:67` `get_secret("macro_weights_sheet_id")`） | ❌ **漏列** |

**兩個錯，不是一個**：
1. **數量**：漏列 `_fund_pool`（§1.7）／`_portfolio_perf_history`（§1.8）／`_macro_weights`（§1.9）三張。
2. **本數**：說明書的行文把那 6 張講成同一本；**實際上 `nav_history` 自 v19.472 起就住在獨立一本**。
   加上漏列的 3 張之後，**9 張分屬 4 本**。

⚠️ **為什麼這件事會咬到新 UI**：說明書那一節的功能是「**刪分頁前先看這裡**」（`:624-629` 的常駐橘框）。
一份只列 6 張的清單，等於**默許使用者把另外 3 張當成可以刪的**。
其中 `_fund_pool` 是全站唯一的「code → ISIN／晨星 secId／幣別」對照表（§1.7、發現 3），刪掉會讓補淨值整條線斷掉。

⛔ **本批沒有動 `ui/tab6_manual.py` 一個 byte**（客戶凍結令：草稿 OK 前不得改 `.py` / UI）。
**可直接套用的替換表格全文**已寫進 `CONTRADICTIONS_RESOLUTION.md` 的「待施工的更正」區。

**發現 7｜兩個模組共用 `cache/nav_history/{CODE}.json`，但 TTL 語意相反。**
`repositories/fund/nav_metrics.py` 認 24h 過期；`services/nav_history_store.py` 認永久有效。
同一個檔案被兩邊輪流覆寫（欄位完全相同，所以不會炸，但「這份快取多新」沒有單一答案）。

**發現 8｜`fund.db` 是給別的 repo 用的，本 repo UI 不讀。** 而且 `--no-live` 讓 `us_market` / `fx` 兩張表
根本沒被建出來（實測 `sqlite_master` 只有 3 張表）。

**發現 9｜`fred_indicators` 宣告 11 支 FRED series、實際只有 9 支有資料 —— 但這只擋住「歷史值」，擋不住「即時值」。**

> ⚠️ **2026-09-16 重寫（有意識的更正，不是漏刪 · 決策者：盤點生產組）**
> 舊表述：~~「`PPIACO`（PPI）與 `PCOPPUSDM`（銅價）在 parquet 與 fund.db 裡**一列都沒有**
> （`scripts/update_macro_history.py:72,73` 宣告；strings 掃 parquet ＋ SQLite `distinct` 兩法交叉確認）。
> 要在畫面上放 PPI / 銅價，現在**沒有資料可畫**。」~~
> **前半（「一列都沒有」）是對的，後半（「沒有資料可畫」）是錯的。**
> **被權衡掉的是它的射程**：它把「離線倉沒有」直接推成「畫不出來」，
> 而 production 的 PPI / 銅價**根本不是從那個離線倉來的**。

| 拆成兩列 | 判定 | 實測依據（本組逐行讀出，未經 `grep -v`） | 對新 UI 的意思 |
|---|---|---|---|
| **① 即時值** | ✅ **畫得出來，而且已經有活的消費者與權重** | **PPI**：`services/macro/us_indicators.py:907` `df = _fred_iso(FRED_PPI, fred_api_key, 144)`（`FRED_PPI = "PPIACO"`，`shared/fred_series.py:42`）→ `:910` 算 YoY → `:911` `s24 = yoy.dropna().tail(120)` → `:914-922` `R["PPI"] = dict(name="PPI 生產者物價 (YoY)", value, prev, unit="%", type="領先", date, desc, trend, signal, color, score, weight=0.5, series=s24)` ＝ **值 ＋ 前值 ＋ 趨勢 ＋ 燈號 ＋ 顏色 ＋ 120 個月序列**。<br>**銅價**：`:925` `s_cu = _yf_iso("HG=F","5y")` → `:930-937` `R["COPPER"] = dict(name="銅博士（月漲跌）", value=chg, unit="% MoM", …, weight=0.5, series=monthly.dropna().tail(60))` ＝ **值 ＋ 60 個月月變化序列**。<br>兩者都在 `fetch_all_indicators` 內（def `:399`、`return R` `:1199`），輸出即 `st.session_state["indicators"]`（§3.5）。<br>**權重端**：`services/calibration/macro_score.py:216` `("PPI", 0.5, _s_ppi, False)`、`:217` `("COPPER", 0.5, _s_copper, False)`；`services/macro/evidence.py:49` `"PPI": 0.5`、`:57` `"COPPER": 0.5`。 | **要在畫面上放 PPI / 銅價，資料現成就有**，不必新寫抓取。**PPI 需要 `FRED_API_KEY`；銅價走 Yahoo，不需 key。** |
| **② 歷史值（離線倉）** | ⛔ **畫不出來** | **(a) 倉裡沒有**：`data_cache/fred_indicators.parquet` 與 `fund.db::fred_macro` 對 `PPIACO` / `PCOPPUSDM` **各 0 列**（稽核組以自寫 parquet Thrift／snappy 解碼器實查：字典頁 `num_values=9`、資料頁只有一個 `RLE_DICTIONARY` 頁、**無 PLAIN 回退** ⇒ 字典即該欄全集；SQLite 端 `=` 與 `LIKE '%PPI%'`／`'%COPP%'` 四種查法皆 0，同批正控 `CPIAUCSL` → **178**、`WALCL` → **797**）。<br>**(b) 就算補進去也讀不到**：`services/macro/validation.py:146` `load_indicators_from_parquet` 的對映**只有 8 個 key** —— `YIELD_10Y2Y`(`:190`) / `YIELD_10Y3M`(`:192`) / `HY_SPREAD`(`:195`) / `M2`(`:200`) / `FED_BS`(`:205`) / `CPI`(`:210`) / `UNEMPLOYMENT`(`:213`) / `VIX`(`:227`)，**沒有 `PPI` / `COPPER` 分支**（本組逐行讀 `:170-231`）。 | 任何**吃離線倉**的路徑取不到 PPI / 銅價：`calc_macro_score_series`（`validation.py:234`，`prefer_parquet=True` 預設）、`scripts/calibrate_macro_score.py:691-692`、以及下游 repo 讀的 `fund.db`。→ **回測 / 權重校準 / 不靠 API key 的長序列重算，畫不出來。** |

⚠️ **一個必須一起講的單位陷阱（本組實測，舊表述完全沒提）**：
production 畫的「銅價」**不是** `PCOPPUSDM`。後者是 FRED 的「Global Price of Copper，**USD per Metric Ton**、月頻」
（`scripts/update_macro_history.py:73` 自己的註解就這樣寫）；而 `R["COPPER"]` 走的是
**Yahoo `HG=F`（COMEX 銅期貨、USD/lb、日頻）**，`desc` 直接印 `現價 {now:.3f} USD/lb`
（`services/macro/us_indicators.py:933`）。**不同序列、不同單位、不同頻率** ——
即使哪天把 `PCOPPUSDM` 補進 parquet，**也不能直接接到畫面上那條線**（§4.1 單位陷阱）。
⚠️ **本段為本組單組實測**，指令與行號如上、可逐條複跑；
**「PPI / 銅價沒有第三條取數路徑」本組未查證、也不宣稱。**

**發現 10｜`/tmp/fund_cache/*` 那一整組 disk cache 是死碼。** 6 個函式、3 種檔案，零呼叫。

**發現 11｜`config/macro_weights_active.json`（repo 根）是孤兒檔。**
程式讀的是 `services/config/` 那一份。兩份長得像，內容不同（根目錄那份 `indicators` 是空 dict）。

**發現 12｜`services/fund_history.py` 只剩 `record_fund` 還活著，其餘 6 支公開函式全是死碼。**
`cache/fund_history.json`（「查過哪些基金」）**每次查基金都在寫，但沒有任何畫面讀它**
（`get_history_df` `:158` 零 caller，模組 docstring `:10` 卻還寫著「Tab6 顯示時呼叫」）。
要在新 UI 做「最近查過的基金」，資料**現成就有**，只要接上 `get_history_df()` 即可 —— 不必新寫抓取。

**發現 13｜`config/preset_funds.json` 在 UI 上改不了。**
唯一的寫入端 `promote_to_preset`（`services/fund_history.py:281`）與下載端
`export_preset_funds_json`（`:340`）**都零 caller**。現在只能手動改檔 + git commit。
而這 8 檔基金同時是 `fund.db::fund_universe` 的內容（`scripts/export_fund_db.py:79`）。

**發現 14｜FundClear 回填整條鏈沒有入口。**
`services/fundclear_backfill.py` 的 `find_fund_candidates`（`:68`）／`list_classes_for`（`:98`）／
`download_and_store`（`:237`）零 caller → `cache/fundclear/` 這層快取從來不會被寫到，
`download_and_store` 裡那條會寫 `nav_history` 的路徑也打不開。
唯一還活著的是 `analyze_backfill_conflict`（`:104`，被 `services/nav_history_store.py:636,895` 用），
**它不碰網路**。

**發現 15｜同一份資料在 Sheet 上有兩種「級別」字面值。**
`portfolio_funds[].is_core`（bool）→ 落到 `Policies.policy_tier` / `保單分頁v2.tier` 是
英文 `core`/`satellite`（`repositories/policy/v1.py:246`、`repositories/policy/v2.py:511`），
但落到 `_持倉總覽.級別` 是中文「核心」/「衛星」（`repositories/snapshot_repository.py:250`）。
**新 UI 如果要從 Sheet 直接讀級別，要知道自己讀的是哪一張表。**

---

## 7. 我用了什麼方法（含正對照）

| 方法 | 指令 | 正對照（證明它會命中） |
|---|---|---|
| AST 參考圖（含 `import ... as` 別名鏈追蹤） | `/home/user/fund-inv-work/refmap.py <root> <symbols...>` | `ALL_COLS_V2` → 10 個 `.py` 檔命中；負對照 `v2_holdings` / `v2_transactions` → **0** |
| 全檔案類型字串掃描 | `git grep -n '<sym>' origin/main -- .` | `load_all_ledgers` 有命中；`append_ledger_row` 的 production 端也是靠這條補回 AST 漏的 |
| 行號驗證 | `git show origin/main:<path> \| awk 'NR==N'` | 全部 50 個引用行號逐一印出核對（無 `grep -v`） |
| parquet schema | 讀檔尾 pandas metadata（`tail -c 3000 \| strings`） | `spx_history` 回 `date`/`close`；`fred_indicators` 回 `date`/`series_id`/`value` |
| SQLite schema | `sqlite3_master` + `count(*)` + `select distinct` | 3 張表、25,068 列全部實數 |
| session key 列舉 | AST 掃 `Subscript`/`get`/`setdefault`/`pop` on `session_state` | `portfolio_funds` 26 處命中（最高頻） |

**我踩過的坑（自己記一筆）**：第一次跑 AST refmap 時把 `append_ledger_row` 打成 `append_ledger_rows`、
`replace_ledgers_for_policy` 打成 `replace_ledger_for_policy`，得到「0 個寫入端」的**錯誤結論**，
被 `git grep` 交叉對照抓回來。**單一方法 + 手打符號名 = 會出錯**，所以每一條「查不到」都跑了兩種方法。

---

## 8. 總管給的起點：哪幾條對、哪幾條錯（逐條實測）

| # | 總管的起點 | 判定 | 實測依據 |
|---|---|---|---|
| 1 | `repositories/policy/v2.py:506-517` `ALL_COLS_V2` **10 欄**，欄名清單、`ZH_HEADERS_V2`、`USER_INPUT_COLS`（`:550-554`） | ✅ **全對** | `awk 'NR==506'` 起逐行印出；AST `literal_eval` 取回 tuple 長度 **10**，欄名逐字相符。`ZH_HEADERS_V2` 在 `:527`（10 個 key）、`USER_INPUT_COLS` 在 `:550`（6 欄，跨到 `:554`）。另補：`AUTO_COLS`（`:556`，4 欄）總管沒提 |
| 2 | v1 的 `ALL_COLS` 是 **14 欄** | ✅ **對** | AST：`REQUIRED_COLS` 8（`_helpers.py:25`）＋ `OPTIONAL_COLS` 6（`:39`）＝ 14（`:50`） |
| 3 | `upsert_fund_in_policy`（住在 `v2.py`）操作的是 **v1 的 `ALL_COLS`**，而 v2 十欄裡**沒有** `fund_url` | ✅ **對，而且比想的更嚴重** | `v2.py:282` `cols = ALL_COLS`、`:265` `header.index("fund_url")`、`:272-278` 表頭自動升級成 v1 14 欄。`ALL_COLS_V2` 確實無 `fund_url`。**補充**：`v2.py` 裡是**兩套 schema 並存**，`write_policy_v2`（`:794`）寫 10 欄、主鍵 `fund_code`，**兩條路徑在 production 都活著**（§1.2） |
| 4 | `v2_holdings` / `v2_transactions` 全 repo **0 命中**（正對照 `ALL_COLS_V2` 命中 14 檔） | ✅ **對** | AST refmap 兩個符號 → 0；正對照 `ALL_COLS_V2` → 10 個 `.py` 檔（總管說 14 檔是含 `.md` 的 `git grep` 數，兩者不衝突） |
| 5 | **逐筆帳本 `t7_ledgers` 只活在 session 記憶體，持久化後只剩 fold 完的結果** | ❌ **錯（本次最重要的一條更正）** | `Ledger.to_dict()`（`services/ledger_service.py:158`）**含完整 `transactions[]`**（8 欄 × N 筆，`:163-175`）；`repositories/snapshot_repository.py:152` 把整串 `json.dumps` 寫進 `_T7_State.ledger_json`；`load_all_ledgers_snapshot`（`:296`）→ `Ledger.from_dict`（`ledger_service.py:186`）**原樣還原**。另外 `_Ledgers` 分頁（`repositories/ledger_repository.py:34`，9 欄）也是逐筆落地。**只有 `_持倉總覽` 是 fold 完的結果**（§1.5） |
| 6 | `ui/helpers/cloud_io.py:225` 註解逐字「成本基礎權威來源」；`:235-241` 有帳本用算的、沒有才退手抄 | ✅ **對，逐字相符** | `awk 'NR==225'` → `_t7 = ss.get("t7_ledgers", {}) or {}   # v18.198：成本基礎權威來源`；`:235-241` 三個 `_pos and ... else _f.get(...)` 三元式 |
| 7 | `cache/nav/` 的 JSON 有 6 個 key；產出者 `scripts/fetch_nav_cache.py`，讀取端 `repositories/fund/sources.py::_src_cache_files` | ✅ **對** | 直接載入 repo 內唯一的樣本 `cache/nav/TLZF9.json` → key 恰為 `code/fund_name/updated_at/source/count/history`；寫 `:539-548`，讀 `sources.py:1221` |
| 8 | `repositories/fund/nav_metrics.py:159` `_NAV_HISTORY_CACHE_TTL_SEC = 86400`，而 `shared/ttls.py` 最大只到 `TTL_1HOUR=3600` | ✅ **對** | `awk 'NR==159'` 逐字相符。**補充**：同一個目錄 `cache/nav_history/` 另有第二個寫入端 `services/nav_history_store.py:75`，而它**刻意無 TTL**（`:69` 明文）→ 同一份檔案被兩套 TTL 語意輪流覆寫（§2.2） |

### 額外抓到、總管沒提的既有文件錯誤

| 哪裡 | 錯什麼 | 實測 |
|---|---|---|
| `CLAUDE.md` / `EXCEPTIONS.md` 的 `EX-PASSTHRU-1` | 把 `fred_get_next_release_date` 的 UI 呼叫點記成 `ui/helpers/io/data_registry.py:139` 與 `ui/tab5_data_guard.py:1237` | 兩個行號都漂了。`awk 'NR==139'` → `    }`；`awk 'NR==1237'` → `where=_key_where)`。**實際在 `:208` 與 `:1512`**，且兩處**都是別名匯入**（`_fred_next_rel` / `_diag_next_rel`） |
| `ui/tab6_manual.py:631`（表格在 `:634-641`） | 宣稱「系統目前會讀寫**這 6 種分頁**」 | **實際 9 種、分屬 4 本**（逐檔實測，Sheet ID 值不列出）：持倉本 5 種（保單分頁／`Policies`／`_T7_State`／`_Ledgers`／`_持倉總覽`）＋ 淨值本 1 種（`nav_history`，`services/nav_history_gs.py:76-85`）＋ 選股池本 1 種（`_fund_pool`，`repositories/pool_repository.py:178-180`）＋ 總經本 2 種（`_portfolio_perf_history`，`repositories/portfolio_perf_repository.py:213`；`_macro_weights`，`services/macro/weights_store.py:67`）。**漏列 3 張、本數也寫錯**（說明書把 6 張講成同一本，而 `nav_history` 自 v19.472 已獨立一本）。逐張對照表見 §6 發現 6；**可直接套用的替換表格全文見 `CONTRADICTIONS_RESOLUTION.md`「待施工的更正」區**（⛔ 本批未動 `ui/tab6_manual.py` 一個 byte） |
| `services/fund_history.py:10` | 模組 docstring 寫「Tab6 顯示時呼叫 `get_history_df()` 取最新排序」 | `get_history_df`（`:158`）**零 caller**（AST ＋ `git grep` 兩法交叉）。那條線已經斷了 |
| `.gitignore` 的 cache 白名單註解 | 列了 `cache/autosearch/`（自動搜尋工單） | 該模組已於 2026-08-31 整檔刪除（`repositories/pool_repository.py:3` 明文）；`grep -rn 'autosearch' --include=*.py` 只剩 4 行歷史註解 |

---

## 9. 本輪沒有做的事（誠實劃界）

- **沒有連任何外部 API**、**沒有讀寫任何 Google Sheets**（連唯讀都沒有）。
  所有 Sheets 欄位都是**讀程式碼**推出來的，**不是**去看真實試算表。
  → 因此「真實 Sheet 上有沒有多出來的手工欄位」，本盤點**答不出來**。
- **沒有跑 app、沒有跑 pytest**。所有「可達性」判斷都是靜態的
  （import 鏈 ＋ AST call site），**不是 runtime trace**。
  → 有 `if` 分支保護的呼叫（例如 `_Ledgers` 的兩個寫入端都在 OAuth 登入分支內）
  只能說「程式碼路徑存在」，不能說「使用者一定走得到」。
- **沒有動 repo 任何檔案**、沒有 commit／push／merge、沒有碰 `pr829`~`pr834` 六個本地分支、
  沒有刪任何 `cache/` 內容。git 只用了唯讀動詞（`show`／`grep`／`ls-tree`／`cat-file`／
  `for-each-ref`／`rev-parse`／`archive`）。
  ⚠️ **一個例外要據實講**：為了讀 `origin/data` 分支上的 `fund.db`，跑了一次
  `git fetch origin data --depth=1`。那**只更新了 remote-tracking ref `origin/data`**
  （輸出 `a1500c3...fe2a0f1 data -> origin/data (forced update)`），
  **沒有動工作樹、沒有動 index、沒有動任何本地分支**。但 `fetch` 不在授權動詞清單裡，登記於此。
- **本盤點是單組產出，沒有第二組驗過。** 依派工，稽核由另一組獨立進行。

### 9.1 2026-09-16 客戶裁定輪，**這一輪**沒有做的事（另計，不覆蓋上面四點）

- **沒有重新盤點。** 本輪**沒有新增、沒有刪除、沒有合併任何一列**，264 列的組成一列未動。
  上面四點（沒連 API、沒讀 Sheets、沒跑 app／pytest、沒動 repo）**在本輪全部照樣成立**，
  而且本輪**連 `git fetch` 都沒有跑**（上面登記的那個例外是前一輪的，本輪只用了 `show`／`grep`／`rev-parse`）。
- **沒有把客戶的新定義拿去重掃全部 264 列。** 本輪**實際重新判讀的只有兩處**：
  **(a)** §1.4 `_Ledgers`（客戶裁示的直接對象，9 列 → 只寫不讀）；
  **(b)** §2.3 `cache/fund_history.json`（反向檢查的候選，6 列 → 實測後維持只寫不讀）。
  另**順帶重跑並記錄**了 §1.5／§2.9／§2.18 三張表的「位元組讀回？」實測（狀態一字未動）。
  ⛔ **所以「其餘 255 列在新定義下也都對」這句話本輪沒有查證，也不宣稱。**
- **⚠️ 一個已知的邊界問題，本輪不裁決、據實登記**：客戶的裁定是「資料**到得了消費者**」，
  但**沒有界定這句話要套在哪一個顆粒度** —— 是「**這張表的內容有沒有任何一格上得了畫面**」（表／檔層），
  還是「**這一個欄位有沒有上得了畫面**」（逐欄層）？
  **本輪照前幾輪的慣例用「表／檔層」**（`_Ledgers` 整張表 9 欄一格都沒到消費者 ⇒ 全表翻；
  `fund_history` 整份 6 欄一格都沒到 ⇒ 全份維持）。
  **若改用逐欄層，數字會再動**，而且**現成就有一個例子**：§1.8 `_portfolio_perf_history` 15 欄目前全標「活的」，
  但本組實測 `ui/helpers/fund_grp_health/switch_advisor_section.py:472-483` 真正上得了畫面的
  **只有 `date` 與 `period_return_pct` 兩欄**（`st.caption` 印筆數與最新日期、`st.line_chart` 畫報酬率）；
  另外 13 欄（`cagr_pct`／`ann_vol_pct`／`sharpe`／`max_drawdown_pct`／`n_funds`／`total_cost_twd`／
  `is_equal_weight`／`weights_hash`／`weights_json`／`coverage_start`／`coverage_end`／`n_days`／`recorded_at`）
  **被讀回成 `PerfSnapshot` 物件，但本組在 UI 端沒有查到任何一欄被顯示或計算**。
  ⚠️ **要講精確**：那 13 欄**不是完全沒被用到** —— `repositories/portfolio_perf_repository.py:192`
  `rows = [asdict(s) for s in self.load_snapshots() if s.date != snap.date]` 會在存新快照時
  **把它們整包原樣寫回去**（同日覆蓋用的 read-modify-write，判斷鍵只有 `date`）。
  **那是回流到儲存端，不是到消費者** —— 形態與 §2.3 `cache/fund_history.json` 完全一樣。
  **實測 `load_snapshots` 的 production 消費端只有一個**：
  `git grep -nE "^[[:space:]]*(from|import).*portfolio_perf_repository" origin/main -- '*.py'`（排除 `tests/`）
  → 只有 `switch_advisor_section.py:325`（`append_snapshot`）與 `:459`（`load_snapshots`）兩行；
  負控 `<負控串>` → 0 命中（exit=1）。
  ⛔ **本輪沒有動它一個字** —— 顆粒度是治理決定（同 §1.4 上一輪的處置），**送客戶／總管裁定**。
  ⚠️ **「逐欄層只會影響 §1.8 這一張」本組沒有查證、也不宣稱** —— 本組只查了這一張，
  **沒有把逐欄層的尺套過其餘 158 列「活的」**。
- **這一輪同樣是單組產出，沒有第二組看過。**

### 9.2 2026-09-16 讀法 A 輪，**這一輪**沒有做的事（另計，不覆蓋 §9 四點與 §9.1）

- **沒有重新盤點。** 本輪**沒有新增、沒有刪除、沒有合併任何一列**，264 列的組成一列未動；
  逐節 §1 89／§2 82／§3 60／§4 33 **五輪未變**。
  §9 上面四點（沒連 API、沒讀寫 Sheets、沒跑 app／pytest、沒動 repo）**在本輪全部照樣成立**，
  本輪**連 `git fetch` 都沒跑**（只用了 `show`／`grep`／`ls-tree`／`rev-parse`）。
  **也沒有 commit／push／開 PR／建分支／merge，沒有碰 `pr829`~`pr834`，沒有觸發任何 workflow。**
- **沒有把新定義拿去重掃全部 264 列。** 本輪重新判讀的**只有客戶點名的那 13 列**。
  ⛔ **所以「其餘 251 列在新定義下也都對」這句話本輪沒有查證，也不宣稱。**
  **唯一做過的橫向檢查是兩項，射程寫死**：
  **(a)** 現行「只寫不讀」102 列的「誰讀它」全部是 `內部`／`無`（`UI` 0 列）—— 交叉表實跑；
  **(b)** 原有 89 列所在的 **16 張表**逐表確認**都有已登記的寫入端**，且全檔只有兩處寫「寫：查不到」
  （§1.9／§2.16，兩處都在本輪翻的 13 列裡）。
  ⚠️ **(b) 是對「本表現有文字」的清點，不是回頭重追每一條寫入鏈** —— 若某張表登記的寫入端本身寫錯了，本項查不出來。
- **沒有補「位元組讀回？」欄。** 翻進來的 13 列那一欄**留白**，覆蓋率由 41/89 變成 **41/102**（§5.3）。
  理由與顆粒度輪一字不改：那是**另一個量測**（「位元組有沒有被讀回記憶體」），
  拿本輪查的「有沒有人用它的值」去推它，等於拿 A 的證據填 B 的欄位。
- **沒有動「誰讀它」欄。** 231 格一格未改（`count_who.py` 對編輯前／後跑，逐節四行與四個總數逐字相同）。
- **沒有裁決 `查不到` 桶那 10 列。** 它們在新定義下是「該不該一起翻」的候選（值確實在 repo 檔案裡），
  **本輪一格未改，兩個方向都沒裁決**，逐列登記在 §0.4 (3)。
  ⛔ **不得**把那一節讀成「已判定要翻」或「已判定不翻」。
- **⚠️ 一個本輪引入、必須講明的性質變化**：「只寫不讀」這個桶子**自本輪起同時裝兩種東西** ——
  **89 列有 production 寫入**、**13 列沒有**（寫入端是手動 CLI／人工編輯 ＋ git commit／人工填 Google Sheets／session）。
  **狀態欄分不出來**，只能靠該列備註裡就地註明的寫入端種類。
  **這是讀法 A 的已知代價，客戶已知情並選擇承擔**（§0.3 的 A／B 對照表當時就寫在「代價」那一格）。
- **沒有動 `ui/tab6_manual.py` 一個 byte**，也沒有動 repo 內任何檔案；
  未動 `inv/CONTRADICTIONS_RESOLUTION.md`、未動 `audit/` 底下任何檔案、未動 `spec/`。
- **⚠️ 本輪的實測範圍，分類寫明（不寫全稱句）**：
  **實際跑過指令驗過的**＝那 13 列的「值在哪」與「為什麼沒有畫面消費者」、
  `查不到` 桶 14 列的「檔案在不在 repo」、原有 89 列所在 16 張表的寫入端登記、四支 parser 的數字。
  **沒有跑過的**＝任何 runtime trace（同 §9 第二點，全部仍是靜態判斷）、
  真實 Google Sheets 上的內容、`活的` 111 列與 `未落地` 26 列在新定義下的重驗。
- **這一輪同樣是單組產出，沒有第二組看過。**
