## 母法修正提案單 #7

**提案人**：規格生產組（AI 總管派工，2026-09-16 客戶裁示 1）　　**日期**：2026-09-16
**射程**：**基金儀表板（my-Fund-dashboard）** ← 固定值，⛔ 不得填別的
**base commit**：`9cbf0377`（`origin/main`）；母法 base `5569d85c`（`origin/docs/v2-constitution-governance`，**未合併**，PR #837 open + draft）

> **這是什麼**：P0 施工第一批**第 4b 項**的母法修正提案單 —— `ui/tab3_t7_ledger.py` 的畫面文案（**F1**）。
> **2026-09-16 客戶裁示 1 把原第 4 項拆成 4a／4b**：本單只管 **F1** 那一半；**F4** 那一半（`services/ai_prompts.py:72-79`）另見 **`spec/PROPOSAL_4a.md`（提案 #6）**。
> ⛔ **兩張單必須同批核准、同批施工**：只改 prompt ⇒ 畫面變成假記載（說會給換股建議、實際不再給）；只改畫面 ⇒ 違規輸出照印。
> ⚠️ **編號說明**：母法 §8 現有提案 1／2／~~3~~／~~4~~／5，該節逐字寫「**新提案接續編號為 `提案 5`，不填 3／4 的空缺**」，故 `PROPOSAL_4a` 取 **#6**、本單取 **#7**。
> **本組未寫入母法檔一個位元組** —— 編號待核發時確認。
> ⚠️ **本單所有引文皆逐行 `git show origin/main:<path> | awk 'NR==N'` 重印核對，非轉述。**
> ⚠️ **本單引述的違規文案一律加引號並標「現況引文」——⛔ 那是轉錄，不是本單的建議。**

---

### 1. 變更條款

**要修改的母法條款（逐字引用原文）**

母法 `docs/v2/CONSTITUTION.md:72`（**§1.1 凍結清單** F1 那一列）：

```
| F1 | **16 個 `ui/tab*.py`** | **16 檔**，一個位元組都不准動 | `git ls-tree -r --name-only 9cbf0377 \| grep -cE '^ui/tab[^/]*\.py$'` → `16` |
```

母法 `:580`（**G1**）與 `:582`（**G3**）（逐字引文見 `spec/PROPOSAL_4a.md` §1，本單不重印一份，避免兩份引文日後分岔）。

**改成什麼（完整條文，⛔ 不是「建議放寬 F1」）** —— 本單請求在母法 §1.1 之下新增一格豁免，逐字如下：

> 「**F1 豁免（第二格）｜`ui/tab3_t7_ledger.py`，僅限『策略3 深度組合建議』那一區描述 AI 輸出型別的畫面文案。**
> **射程寫死為四行**：`:3237`（面板抬頭）／`:3239`（四節結構說明）／`:3263`（灰態文案）／`:3291`（按鈕字）。
> **豁免的內容**：把這四行改寫成與**提案 #6 改寫後的 prompt 輸出**一致的說法 —— ⛔ **不得順手改該區任何其他東西**。
> **⛔ `:3402` 的免責 caption 不在豁免射程內，且必須原封保留**（那是本鏈唯一一句緩解語）。
> **⛔ 本豁免不涵蓋該檔其餘 3,398 行，也不涵蓋其他 15 個 `ui/tab*.py`** —— 那 15 檔維持『一個位元組都不准動』。
> **⛔ 本豁免不得與既有的 B8（F2／F3）、B21（F2）、B28（F1，`ui/tab1_macro_midcycle.py` 僅限 ①-8）互相借用射程。**
> **⛔ 本豁免依附於提案 #6**：#6 未核准 ⇒ 本豁免不生效（單獨改畫面會讓違規輸出繼續印，卻少掉描述它的字，更難被發現）。」

---

### 2. 阻礙原因（**實測**，附指令與輸出；量測日 2026-09-16 · base `origin/main` ＝ `9cbf0377`）

#### (a) 現況引文 —— 四行逐字，**原樣引用，⛔ 不是本單的建議**

指令：`git show origin/main:ui/tab3_t7_ledger.py | awk 'NR==3237 || NR==3239 || NR==3263 || NR==3291'`

| 位置 | 現況引文（原樣引用） | 強弱與命中 |
|---|---|---|
| `:3239` | `"AI 4 節結構：3 大缺點 / 換股建議 / 配置比例 / 高賣低買 vs 跌就買</span>"` | **最強** —— 逐字點名「**換股建議**」與「**配置比例**」，正是 #6 要拿掉的那兩節（**G3** ＋ **G1**）。**客戶點名的兩行之一** |
| `:3263` | `"AI 給不出景氣位階對應的換股建議;"` | **最強** —— 灰態文案同樣逐字承諾「換股建議」（**G3**）。**客戶點名的兩行之一**（⚠️ 句尾原文是**半形分號**，照抄） |
| `:3237` | `"📜 策略3 深度組合建議</span>"` | **較弱** —— 面板抬頭，只以「建議」命名功能、沒有點名輸出型別；**但 #6 改完後同樣變成假記載** |
| `:3291` | `"🤖 生成 策略3 深度建議",` | **較弱** —— **按鈕字**（`key="btn_mk_advisor"`，`:3292`），同上 |

⚠️ **`:3237`／`:3291` 是本組依派工指示「查還有沒有第三行同型」實測補上的，客戶只點名了 `:3239`／`:3263`。**
**本組據實分強弱、不拉平** —— 是否把較弱的兩行一併納入，**請總管／客戶裁定**（⛔ 本單不代為擴張客戶給的射程；若裁定只改兩行，本單 §1 的「四行」改為「兩行」即可，其餘不變）。

#### (b) 怎麼查的（兩種方法，正負控同批，本組實跑）

- **寬字表掃描**（45 詞：加碼／減碼／換股／停利／配置比例／下一步／水位／降槓桿／抄底／接刀／布局／再平衡…）掃全檔 → **63 行命中**，
  **逐行人工判讀**分出「畫面字」與「原始碼註解」。
  **正控** `換股建議` → **2 行**（`:3239`／`:3263`）；**負控**（本輪現編隨機串）→ **0**。
- **渲染端逐行讀**：從 `:3220` 那一區的區塊註解往下讀到 `:3402`，逐一確認每個字串是不是進了 `st.markdown` / `st.button` / `not_ready` / `st.caption`。
- ⚠️ **字表看不到動態組出的字串，也看不到本區以外的同型文案。本表是「已知分類」，⛔ 不宣稱窮舉。**

#### (c) 它們全部到得了畫面（逐行重印）

```
:3232-3242  st.markdown(  …  unsafe_allow_html=True )   ← :3237 與 :3239 都在這一顆呼叫裡
:3261-3268  not_ready( … )                              ← :3263 在這裡（灰態）
:3290-3296  _mk_btn = _mk_cols[0].button( :3291 , key="btn_mk_advisor", … )
:3401       st.markdown(st.session_state["mk_advisor_txt"])   ← 模型輸出原樣印出
:3402       st.caption("⚠️ AI 建議僅供參考，最終決策需自行判斷市場與風險承受度")  ← 本鏈唯一免責
```

#### (d) 凍結令擋住了什麼

指令：`git ls-tree -r --name-only origin/main | grep -E '^ui/tab[^/]*\.py$'` → **16 檔**，`ui/tab3_t7_ledger.py` 是其中之一（字典序第 9）。
⇒ 它落在 **F1**，母法 `:72` 現況欄逐字「**16 檔**，一個位元組都不准動」。
**今天涵蓋 F1 的豁免只有一張**：**B28**（2026-09-16 客戶裁示 5），**射程寫死為 `ui/tab1_macro_midcycle.py` 的 ①-8 那一處** ——
**射不到本檔。** **B8** ＝ F2／F3、**B21** ＝ F2，兩張都不是 F1。
⇒ **不提案就動不了。**

#### (e) 為什麼不能只改 #6 那一邊 —— 這是本單存在的全部理由

`:3239` 那一行**是對 prompt 輸出結構的描述**（「AI 4 節結構」），而 `services/ai_prompts.py:65` 逐字寫著
`═══ 輸出格式（繁體中文，4 節必出，每節用 ### 開頭）═══` —— **兩邊是同一件事的兩份記載。**
#6 把第二／三節改掉之後，`:3239` 會變成一句**畫面上的假記載**：說會給「換股建議 / 配置比例」，實際不再給。
⚠️ **對照本專案已經發作過的同型事故**：`CLAUDE.md §2.1` 記載「**已被查證為假、卻沒被撤下的宣稱，比沒查證的更危險，因為它看起來已經有出處**」。
⇒ **兩單必須同批。**

#### (f) 未能實測的一項，就地標明

⚠️ **本組未實跑任何測試**（沙箱無 `streamlit`／`pandas`，依安全紅線**未 `pip install`**）。
**是否有守衛鎖著這四行的字面（例如以 `btn_mk_advisor` 或「策略3」為錨點），本組查不到** ——
`git grep -n 'btn_mk_advisor\|策略3' origin/main -- 'tests/**'` 本組**未跑**（它不在本輪的必跑清單裡）。
**⛔ 施工前必須先跑這一條**；若有守衛命中，依母法 **§6.5-P**（守衛牴觸母法時必須與文案同批改），本單射程要加上那條斷言。

---

### 3. 替代方案（⛔ 「沒有替代方案」不成立，故本欄不空白）

- **(a)【本單推薦】四行同批改寫，與 #6 的新輸出對齊。**
  例：`:3239` 由「4 節結構：… / 換股建議 / 配置比例 / …」改為描述改寫後實際會出的那幾節（**⛔ 不得再點名處置型輸出**）；
  `:3263` 的灰態理由改為「總經完整率不足，AI 這一輪給不出對應的**位階判讀**」；`:3237`／`:3291` 的「建議」改為不承諾處置的說法。
  ⚠️ **`:3402` 的免責原封不動。**
- **(b)【唯一一個「不改母法也能做」的】不改畫面，改為在 #6 那一側把章節名也一併改掉，讓畫面描述「碰巧」仍然成立。**
  ⛔ **不採用，而且它其實做不到**：`:3239` 逐字寫的是「**換股建議 / 配置比例**」這兩個**輸出型別**，
  而 #6 的整個目的就是**不再產出那兩個型別** —— 沒有任何一種 prompt 改寫能讓這句話繼續為真。
  **這一條寫在這裡，是為了讓下一輪不必再想一次。**
- **(c) 只改 `:3239`／`:3263` 兩行（客戶原本點名的），放掉 `:3237`／`:3291`。**
  ⚠️ **這是一個真的選項，不是陪襯**：那兩行較弱（只承諾「建議」、沒點名輸出型別）。
  ⛔ **本單不推薦**：`:3291` 是**按鈕字**，是使用者點下去之前看到的最後一句承諾；留著它，
  等於把「這顆鈕會給你建議」留在畫面上。**但要不要收窄成兩行，是客戶的射程決定，不是本組的。**
- **(d) 正式下架整個「策略3 深度組合建議」功能。**
  ⛔ **不採用**：依 `CLAUDE.md §-1.5.1a 接合 A1`，「**正式下架既有功能**」屬**不可逆操作族，須逐案請示客戶**。
  **如果客戶想走這條，那是另一個層級的決定，⛔ 不該由本單夾帶。**
- **(e) 維持現狀。** ⛔ **不採用**：#6 核准後現狀就是一句假記載；不核准則違規輸出照印。**兩條路都不行。**

---

### 4. 影響評估

- **凍結邊界**：動到 **F1**（`ui/tab3_t7_ledger.py` 的**四行畫面字串**）。
  ⛔ **不動** F2／F3／F5／F6；**F4 由提案 #6 承接**。
  ⚠️ **本單請求的是 F1 的第二格豁免** —— **第一格是 B28**（`ui/tab1_macro_midcycle.py` 僅限 ①-8）。
  ⛔ **兩格互不涵蓋，射程不得互相借用**（同 `spec/DECISION_LOG.md` B21-a 已釘死的紀律）。
  ⚠️ **可能要加進射程的一項**：鎖著這四行字面的守衛（見 §2 (f)，**本組查不到，施工前必查**）。
- **合規**：**只減不增** —— 拿掉畫面上對處置型輸出的承諾，不新增任何處置方向。
  ⚠️ **`:3402` 的免責保留 ⇒ 緩解語不減少。**
  ⚠️ 母法 §6.2.1 那六個逐字禁用詞在本區段（`:3284-3402`）**0 命中**（全檔唯一命中在 `:1990`，屬另一個功能，不在本鏈上）——
  **⛔ 0 命中不等於合規**，本單的證據來自渲染端逐行讀，不是字表。
- **機敏資料**：**不動 §7.3 矩陣任何一格。** 本單只改畫面字串，不碰憑證、不碰 Google Sheets、不碰快取。
  ⚠️ 本單全程未讀寫任何 Sheets、未打任何外部 API、未列出任何 token／金鑰／sheet id 的值。
- **回滾**：**純字串**修改，無旗標、無 schema 變更、無資料寫入 ⇒ `git revert` 即可完全復原。
  不適用 §4.2 R2／R4／R5（沒有 v2 旗標介入）。
  ⚠️ **但回滾要與 #6 一起回** —— 只回一邊就回到「畫面與輸出不一致」的狀態。
- **未涵蓋（本評估**沒有**查到什麼，以及為什麼）**：
  1. **有沒有守衛鎖著這四行，本組查不到**（未跑 `tests/` 掃描，見 §2 (f)）——**施工前必查。**
  2. **本區以外是否另有同型文案，本組不宣稱窮舉**（字表看不到動態組出的字串）。
  3. **`:3237`／`:3291` 該不該納入，本組不代客戶決定** —— 據實分強弱後送裁（見 §3 (c)）。
  4. **本組未通讀母法全文** —— 只逐字核對了本單引用的幾條（`:72`／`:580`／`:582`／§5.1 範本）。
  5. **本單為單組產出，沒有第二組驗過**（`CLAUDE.md §-2` 規則 6）。

---

### 5. 總管推薦方案（Recommendation）

**推薦採 (a)：四行同批改寫，並與提案 #6 一起核准、一起施工、一起回滾。**

**具體原因三條**：
1. **本單的存在理由就是「不讓畫面說謊」。** #6 一旦核准，`:3239` 立刻變成假記載；
   而本專案憲法對這件事的判詞已經寫死（`CLAUDE.md §2.1`：「**已被查證為假、卻沒被撤下的宣稱，比沒查證的更危險**」）。
2. **代價最小、可逆。** 純字串、`git revert` 即復原；**豁免射程只有四行，其餘 3,398 行與另外 15 個 `ui/tab*.py` 一個位元組都不動。**
3. **把 `:3291` 一起收進來，是因為它是使用者按下去之前看到的最後一句承諾。**
   留著它，等於在最關鍵的那一格保留「這顆鈕會給你建議」。
   ⚠️ **但若客戶裁定只改 `:3239`／`:3263` 兩行，本單照辦** —— **那是射程決定，屬客戶；本組只負責把強弱講清楚。**

⚠️ **本推薦為 AI 總管依母法自身原則推導，未經第二組獨立驗證**（`CLAUDE.md §-2` 規則 6）。
⚠️ **§4「未涵蓋」第 1 點（守衛是否鎖著這四行）尚未查，施工前必查** —— ⛔ **不得把本單當成「已經查過沒有守衛」的證據。**
