# 交接：2026-09-17 收工存檔

> **給下週一開工的人。你沒有上一段對話，這份檔就是全部。**
> 本檔是**狀態快照 ＋ 待裁示清單**，**不是**結論、**不是**動工授權。
>
> **位階**：本檔受 `CLAUDE.md` `§-2`（總管派工）＋ `§-1.5`（虛擬公司運行規範）＋ `§-1`（停手準則）管轄。
> 本檔任何一行都**不**凌駕上述三節。特別是 **`§-1`：客戶沒明確指派 → 停手等指令**——
> 下面列的每一件「待辦」都**不是**叫你去做，是叫你**知道它在那裡**。
>
> **怎麼用這份檔**：先讀〈一〉知道站在哪 → 讀〈二〉知道在等什麼 →
> 〈四〉是唯一一件「客戶一開口就可以立刻動」的事 → 其餘按需查。

---

## 〇、三行版

1. 分支 `claude/fund-handover-verification-89o6ys`、**v2 內容 head `2e2803c`**（CI 三條全綠）、已 push、工作樹乾淨。
   （⚠️ `git rev-parse HEAD` 會**比 `2e2803c` 新一至數筆** —— 那些全是**本交接檔自己**的存檔與就地更正，
   純 docs、0 個 `.py`，**不承載任何 v2 產出**。見 1.1。）
2. **卡在客戶**：六件事等裁示（見〈二〉），母法 PR #837 未合併，HTML 原型的前置門檻未解鎖。
3. **下週一第一件事**：`2e2803c` 那 11 檔**從來沒跑過秘密／負控稽核**（見〈四〉）。

---

## 一、現在的狀態

### 1.1 分支與工作樹

| 項目 | 值 |
|---|---|
| 分支 | `claude/fund-handover-verification-89o6ys` |
| **v2 內容 head** | `2e2803c`（`2e2803ca4c745202af81a3770fc3eb4529ff979f`）—— 承載全部 v2 產出，**本檔各節談的都是它** |
| **實際 head** | **比 `2e2803c` 新一至數筆**，全部是**本交接檔自己**的存檔與就地更正（純 docs、0 個 `.py`）。<br>**現值請跑 `git rev-parse --short HEAD`。**<br>⚠️ **本欄刻意不寫死 SHA**：寫的當下無法知道自己的 SHA，寫上去**下一次更正就過期** —— 本檔已經因此更正過一輪。 |
| 遠端 | 已 push，`origin/claude/fund-handover-verification-89o6ys` ＝ head（同一個 SHA） |
| 工作樹 | 乾淨（`nothing to commit, working tree clean`） |
| 線性歷史 | `origin/main` → `588e3ae` → `18c0df8` → `2e2803c` → （本交接檔的一至數筆），**線性、無 force、無 rebase** |

### 1.2 PR #845（本分支）

| 項目 | 值 |
|---|---|
| 狀態 | **open** / **draft** / 未合併 |
| CI | **三條全綠** —— Fast checks `success`／Schema gate `success`／Slow tests `success` |
| 衝突 | 無（`mergeable_state: clean`） |
| 規模 | 至 `2e2803c` 為 **24 檔、+15,126、−2**；本交接檔另加 **1 檔**（`docs/v2/40_handover_2026-09-17.md`）。<br>**現值請跑 `git diff --shortstat origin/main..HEAD`**（同上，不寫死會漂移的數字）。 |
| `.py` | **0 行**（`git diff --numstat origin/main..HEAD -- '*.py'` 無輸出） |

> ⛔ **客戶指令：維持 draft，不轉 ready、不 merge。** 這條沒有期限，直到客戶另外說。

> 📌 那 **−2** 行是 `CLAUDE.md` `§-2.A` 兩處「七款→八款」計數行的改寫，
> **沒有任何既有條文被刪除**。它與 `2e2803c` 自身 `git show --stat` 的 **−187** 行**是兩回事**：
> 那 187 行全部落在 4 個改寫檔裡、蓋掉的是 `18c0df8` 放上遠端的舊版，
> 而那 4 個檔在 `origin/main` 上根本不存在。**引用數字前先分清楚你在量哪一個。**

### 1.3 PR #837（母法，另一條分支）

| 項目 | 值 |
|---|---|
| 內容 | `docs/v2/CONSTITUTION.md`（**母法**），單檔新增 2,419 行 |
| 分支 | `docs/v2-constitution-governance`，head `5569d85` |
| 狀態 | **open / draft / 未合併** |
| 為什麼沒併 | 客戶裁示「**通過再併**」，而**稽核未通過**（第四輪後仍有未結項） |

> ⚠️ **下面〈二〉多處引用「母法 `CONSTITUTION.md:897`」之類的行號，指的就是這份未合併的 PR #837 的內容。**
> 它**不在 `origin/main` 上**，也不在本分支上。要看它得去 `docs/v2-constitution-governance` 分支。

### 1.4 凍結面

**本輪全程未動**，實測 `git diff --stat origin/main..HEAD -- 'ui/tab*.py' 'ui/views/page_0*.py' app.py` → **輸出為空**。

- `ui/tab*.py`：**16 檔**
- `ui/views/page_0*.py`：**5 檔**
- `app.py`：**1 檔**

---

## 二、⛔ 六件等客戶裁示的事

> **這一段是整份交接最重要的部分。**
>
> ⚠️ **六條的「總管推薦」全部是推薦，不是已定案。**
> 客戶未裁示前，**不得**把任何一條當成前提去推下一步、
> **不得**寫進任何會被後人讀的記錄當成事實（`CLAUDE.md §-2` 規則 6）。
> 六條目前**沒有任何一條有客戶的答覆**。

### ①　`us_indicators.py` 兩處文案要不要納入母法紅線清單

| | |
|---|---|
| **問題** | `services/macro/us_indicators.py:601` 與 `:844` 兩處文案**到得了 ①（市場總覽）**，要不要納入母法紅線清單？ |
| **背景** | 目前紅線計數為 **9**（8 處補標 ＋ 已標的 ①-4 卡 1 處）。**納入則總數由 9 變 11。**<br>這兩處是第七輪稽核（X-1）才抓到的，前幾輪的模組清單**結構上看不見它們**——原因見〈七〉之 1。 |
| **總管推薦** | **納入**（⇒ 11）。 |

**存檔組唯讀實測（2026-09-17，僅確認位置存在，不對「該不該納入」表示意見）**：
兩行都在界內（該檔共 2,012 行），內容為：

- `:601` → `desc="倒掛(<0)=衰退 | 由負翻正=黃金買點",`
- `:844` → `desc=f"<{_VIX_SNAPSHOT_CALM:.0f}平靜 | >{_MB_VIX_RED:.0f}恐慌=逢低加碼時機",`

### ②　母法 `CONSTITUTION.md:897` 的射程要不要裁

| | |
|---|---|
| **問題** | 母法 `:897` 那條**明寫「對門檻型文案不作裁決」**。這條的射程要不要裁？ |
| **背景** | `ui/views/page_01_macro.py:801` 與 `ui/helpers/macro/beginner_view.py:779` **兩處都掛在它身上**。<br>只要這條不裁，那兩處就懸著。 |
| **總管推薦** | **先裁這一條**——因為**另外兩處的答案都取決於它**。 |

> ⚠️ **這一條是六條裡的關鍵路徑。** 先裁它，②、以及〈三〉之 3 的 `page_01_macro.py:801`
> 「同一處兩個答案」才有辦法收斂。**建議客戶優先處理這一條。**

**存檔組唯讀實測（2026-09-17，只查行在不在、內容是什麼）**：

- `ui/views/page_01_macro.py:801`（該檔共 2,192 行，在界內）
  → `_gate = (f"停利 Z ≥ {_al['stop_gain_z']:+.2f}、加碼 Z ≤ {_al['add_z']:+.2f}"`
- `ui/helpers/macro/beginner_view.py:779`（該檔共 1,110 行，在界內）
  → `f"🟡≥{_HOLD_SCORE_10:.1f}（加碼門檻較嚴）—— 兩者不同不是錯,"`

`CONSTITUTION.md:897` **本組沒有查**——該檔在未合併的 PR #837 分支上，不在本分支。

### ③　`turning_points.py` 的「歷史經驗」句要不要納入

| | |
|---|---|
| **問題** | `services/macro/turning_points.py:217-218`（「**歷史經驗：解除後 12 月內為股市底部布局期**」）要不要納入紅線？ |
| **背景** | 這是一句**帶時間窗的方向性陳述**，形態上接近買賣指引。 |
| **總管推薦** | **納入。** |

**存檔組唯讀實測（2026-09-17）**：兩行都在界內（該檔共 536 行），`:218` 逐字為
`f"｜歷史經驗：解除後 12 月內為股市底部布局期")`，與引述相符。

### ④　`page_01_macro.py:1869` 的 AI prompt 句要不要納入

| | |
|---|---|
| **問題** | `ui/views/page_01_macro.py:1869`（AI prompt 內「**下一步怎麼做**」那句）要不要納入？ |
| **背景** | 它與 **②-13** 的 `ai_summary.py:69` **是同一句話**。分開處理會變成同一句話兩個答案。 |
| **總管推薦** | **納入，但併進 P0 第 12 項**（理由：與 `ai_summary.py:69` 同源，應同批處理）。 |

> ⚠️ **存檔組唯讀實測發現一處路徑錯誤，就地更正（2026-09-17）**：
> 原敘述寫 ~~`services/ai_summary.py:69`~~ —— **該檔不存在**（`services/ai_summary.py` 查不到）。
> **實際位置是 `ui/helpers/ai_summary.py:69`**，**行號正確、檔案路徑錯了**。
> 逐字內容：
> `st.caption("把這個 Tab 的所有資料交給 AI，逐段用白話講「現在是好是壞、跟最近新聞有沒有關係、下一步怎麼做」。")`
>
> **「是同一句話」這個判斷大致成立，但不是逐字相同**，據實寫明差異：
> `page_01_macro.py:1869` 是 `"逐段用白話講「現在是好是壞、下一步怎麼做」。"` ——
> **少了「跟最近新聞有沒有關係」這一段**。兩者共用「逐段用白話講」框架與「下一步怎麼做」結尾。
> 全 repo `下一步怎麼做` 實測**只有這 2 處命中**。
> ⛔ **本組只更正路徑與指出字面差異，不對「該不該納入」「該不該併進 P0 第 12 項」表示意見。**

### ⑤　B8 豁免要開通格還是窄格

| | |
|---|---|
| **問題** | B8 豁免要**通格**（涵蓋 `ui/views/page_0*.py` ＋ `app.py`，即凍結表 **F2／F3**）還是**窄格**？ |
| **背景** | 客戶**另外單開了 B21**，而 B21 只限 `page_01_macro.py` ①-4 那**一處**。 |
| **總管推薦** | **窄格**。理由：**若 B8 是通格，就不必再開 B21** —— 客戶既然另開了 B21，反推 B8 的本意應是窄格。 |

### ⑥　`25_proposal_4b.md` 要改兩行還是四行

| | |
|---|---|
| **問題** | `docs/v2/25_proposal_4b.md` 要改**兩行**還是**四行**？ |
| **背景** | 推薦四行的理由：面板標題「📜 策略3 深度組合建議」與按鈕「🤖 生成 策略3 深度建議」兩處，**在 4a 落地後會變成假記錄**。 |
| **總管推薦** | **四行。** |

> ⚠️ **存檔組唯讀實測發現兩處路徑錯誤，就地更正（2026-09-17）**：
> 原敘述把這兩行寫成 ~~`page_01_macro.py:3237`~~ 與 ~~`page_01_macro.py:3291`~~ ——
> **`ui/views/page_01_macro.py` 只有 2,192 行，這兩個行號直接越界。**
> **實際位置是 `ui/tab3_t7_ledger.py`，行號 `3237` 與 `3291` 完全正確**：
>
> - `ui/tab3_t7_ledger.py:3237` → `"📜 策略3 深度組合建議</span>"`
> - `ui/tab3_t7_ledger.py:3291` → `"🤖 生成 策略3 深度建議",`
>
> **錯的只有檔名，行號與文案逐字相符。** 這與 `CLAUDE.md §-2.A` 實證表的 **#11／#8 同型**
> （派工單指錯檔）。
>
> 📌 **一個順帶的事實，寫給下週一的人，不構成建議**：
> `ui/tab3_t7_ledger.py` 符合 `ui/tab*.py`，**屬於〈一〉1.4 的凍結面（16 檔之一）**。
> ⛔ 本組**不主張**這代表什麼、**不主張**要不要解凍，只是把這個事實放在你看得到的地方。

---

## 三、已寫進 `2e2803c` commit message 的三項未結案

> 這三項**已經在 commit message 裡了**（可用 `git log -1 --format=%B 2e2803c` 取回原文），
> 也已寫進 PR #845 說明 §2.3。**這裡重述是為了讓這份交接自己站得住，不是新資訊。**
> **三項都是待裁示，不是結論。**

1. **母法紅線計數 9 vs 11 的 package 再匯出盲點**
   8 處補標 ＋ 已標的 ①-4 卡 1 ＝ **9**；第七輪稽核另指出
   `services/macro/us_indicators.py:601`／`:844` 兩處到得了 ①，**若納入則為 11**。
   該模組經由 `services/macro` 這個 **package 再匯出**，在 ① 的 import 面上**不出現檔名**，
   **前幾輪的模組清單結構上掃不到它。**（＝〈二〉① 的同一件事；成因見〈七〉之 1。）

2. **`25_proposal_4b.md` §2(b) 宣稱的「63 行命中」複跑不出來**
   未達母法 `§5.1`「**附指令與輸出**」的要求，**待退回重寫**。

3. **`page_01_macro.py:801` 同一處兩個答案**
   母法 `:897` 判「**待判讀不裁決**」、`20_ui_spec.md` 判「**G3**」，**待裁**。
   （＝〈二〉② 的下游；② 不裁，這一項就收斂不了。）

---

## 四、⚠️ 下週一第一件事：那 11 檔的秘密／負控正式稽核

> **這是唯一一件「客戶一開口就可以立刻動」的事，而且它是個真缺口，不是儀式。**

### 4.1 缺口是什麼

`2e2803c` 動到的 **11 檔**：

- **改寫 4 檔**：`10_db_inventory.md`、`20_ui_spec.md`、`21_decision_log.md`、`22_ui_page_today.md`
- **新增 7 檔**：`23_ui_draft_01_macro.md`、`24_proposal_4a.md`、`25_proposal_4b.md`、
  `36_audit_spec_r4.md`、`37_audit_spec_r5.md`、`38_audit_spec_r6.md`、`39_audit_spec_r7.md`

**這 11 檔從未納入 `18c0df8` 那一輪的秘密掃描與負控掃描。**
那一輪的射程只有**當時的 17 檔**（`CLAUDE.md` ＋ 當時存在的 16 個 `docs/v2/*.md`）——
7 個新增檔那時**還不存在**，4 個改寫檔**掃過的是舊版內容**。

**此缺口已寫進 PR #845 說明 §1.3 的就地註記**，不是隱藏狀態。

### 4.2 已經做過的事（不能當成稽核結論）

**2026-09-17 總管做過一次唯讀風險分流。**

> ⛔ **這是一個人做的分流，不是稽核結論。**
> 依 `CLAUDE.md §-2` 規則 6 就地揭露：**沒有第二組看過**。
> **不得**被引用為「已查證的事實」，**不得**拿它當「所以不用再掃了」的理由。

分流結果：

- 正控 **14 命中**（＝掃描器本身是活的）
- 負控 **0 命中**（**本次現編隨機串、未列出**——依 `§-2.A` 第 8 款，負控不落文件）
- `AIza`／`gh[pousr]_`／`github_pat_`／`sk-`／`AKIA`／`xox*`／PEM／email／Bearer
  **九種形狀全部 0 命中**
- 40–60 字元長 token 去重後 **8 個**，逐一判讀**全為 commit SHA 與測試函式名**

### 4.3 要做的是

**派一組獨立稽核正式重跑。** 三條硬要求：

1. **兩種方法，缺一不可**——(a) 已知字串直掃　(b) **上下文反查**
   （從「負控／negative control／應該 0 命中／假符號」鄰近行抽出所有隨機樣態 token 人工判讀）。
   ⚠️ 只用字串直掃**結構上必然漏**：拿已知的串去 grep，**永遠找不到下一輪新編的那個串**。
2. **負控現編，不落文件**（`CLAUDE.md §-2.A` 第 8 款）。正確寫法是
   「**負控為本組自訂隨機串，未列出 → 0 命中**」——資訊留著，字串不留。
3. **掃描時排除自身檔案**（同上款）。

⚠️ **依 `§-2` 規則 4，這件事不可由做過 4.2 那次分流的人自己查自己。**

---

## 五、第二部分（HTML 原型）的門檻狀態 —— **還沒解鎖**

### 5.1 門檻

**客戶原話**：「**【前提】第一部分兩份回來並經我同意後，才做這部分。**」

**目前狀態**：第一部分兩份**已回**、**客戶尚未同意**。

⇒ `docs/v2/prototype/ui_prototype_today.html` **一個字都還沒寫，也不准寫。**
（實測：`docs/v2/prototype/` 目錄**不存在**。）

### 5.2 客戶對它的要求（門檻解鎖後才適用；寫在這裡是因為下週一一定會漏）

**做什麼**：

- 只做「**今天**」**一頁**
- **四層結構**：結論燈 **1 顆** ／ **3 張並排卡** ／ 操作列**主 CTA** ／ **可收合佐證**
- 用 `UI_TOKENS.md` 的**實際 hex**（**該檔不存在就用佔位色並註明「待補」**）
- 用 `UI_COMPONENTS.md` 的**卡片／徽章／按鈕**規格
- **狀態覆蓋至少 5 種**：正常／載入中／缺漏／未接線／錯誤
- **響應式 375／768／1280**
- **深色模式底色 `#0e1117`**
- **母法衝突逐處標 ⛔**
- **按鈕字逐顆標 ✓／⚠**

**不准做什麼**：

- ⛔ 不准接**真實資料**（示意數字要標「**示意**」）
- ⛔ 不准做**圖表**
- ⛔ 不准做**其他四頁**
- ⛔ 不准引入**外部框架**
- ⛔ 不准發明 `UI_COMPONENTS.md` **以外**的元件

**完成後停在本地，客戶同意才 commit。**

### 5.3 ⚠️ 存檔組實測發現的一個前置問題（請在動工前先問客戶）

**`UI_TOKENS.md` 與 `UI_COMPONENTS.md` 兩個檔在整個 repo 裡都查不到。**

實測：`git ls-tree -r --name-only HEAD | grep -iE 'UI_TOKENS|UI_COMPONENTS'` → **0 命中**。

- **`UI_TOKENS.md` 不存在有解**——客戶已經給了 fallback：「**用佔位色並註明「待補」**」。
- **`UI_COMPONENTS.md` 不存在沒有解**，而且它同時卡住兩條要求：
  1. 「用 `UI_COMPONENTS.md` 的卡片徽章按鈕規格」——**沒有檔可以用**；
  2. 「不准發明 `UI_COMPONENTS.md` 以外的元件」——**在該檔不存在時，這條字面上禁止任何元件**。

⇒ **建議動工前先向客戶確認這一點**，不要自行發明一份 `UI_COMPONENTS.md` 再拿它當依據——
那會變成「自己立規則、自己遵守」。

⛔ **本組只陳述「這兩個檔查不到」這個事實，不代客戶決定怎麼辦。**
⛔ **本行不構成動工授權**（`CLAUDE.md §-1`）——門檻仍未解鎖。

---

## 六、客戶的常設限制（每輪都適用）

| # | 限制 |
|---|---|
| 1 | **回報不超過一頁** |
| 2 | **不准寫「未複驗」**——查不到就寫「**查不到**」 |
| 3 | **不准動豁免範圍外的任何程式碼** |
| 4 | **不准自動做 ②** |
| 5 | **PR #845 維持 draft** |
| 6 | **不准中途 merge 回主線** |
| 7 | **回覆客戶一律用繁體中文** |

> 📌 **第 2 條與 `CLAUDE.md §-2` 規則 6 怎麼併讀**：規則 6 要求揭露「有沒有第二雙眼睛」，
> 客戶禁的是把「未複驗」當成萬用免責的**罐頭字**。**兩者不衝突**——
> 照實寫「**這是我自己查的，沒有第二組看過**」或「**本組沒有查**」或「**查不到**」，
> 兩邊都滿足。**本檔全篇照這個寫法。**

> 📌 **第 1 條的射程是「給客戶的回報」，不是這份交接檔。**
> 交接檔要冷讀能接手，長度另計。

---

## 七、本輪總管自己犯的錯（據實記錄，不美化）

> 依 `CLAUDE.md §-2.A` 體例：**錯誤清單只記總管自己的錯**，不混入別組的，
> 否則「總管自身錯誤」這個欄位本身會變成假的。

### 7.1 總管的模組清單漏掉 `services/macro/us_indicators.py`

- **怎麼錯的**：**字表沒問題**。錯在**列模組清單的方法**——
  是照 `page_01_macro.py` 的 `from services.macro import (` 這一行去列檔案，
  而 **`services.macro` 是一個 package**，`us_indicators` 這個名字**在 ① 的 import 面上不出現**。
- **誰抓到**：**第七輪稽核（X-1）**。
- **教訓**：**從 import 字面列模組清單，結構上看不見被 package 再匯出的子模組。**

**存檔組唯讀實測，把機制釘死（2026-09-17）**：

- `ui/views/page_01_macro.py:156` 寫的是
  `from services.macro import (calc_macro_phase, detect_turning_points, fetch_all_indicators, macro_action_light,)`
  —— **整段沒有 `us_indicators` 這個字**。
- 而 `services/macro/__init__.py:39` 是
  `from services.macro.us_indicators import (  # noqa: F401`，
  再匯出 `calc_macro_phase`、`fetch_all_indicators`、`identify_regime`、`detect_systemic_risk` 等。
- ⇒ ① 用的 `calc_macro_phase` / `fetch_all_indicators` **實際住在 `us_indicators.py`**，
  但**從 import 那一行完全看不出來**。
- 補充：`us_indicators` 在 `page_01_macro.py` 內確實有 **2 次**字面命中，
  但**兩次都在註解裡**（`:78`、`:639`），**不在任何 import 上**——
  也就是說，**連「grep 這個檔有沒有提到它」都會給出誤導性的答案**。

### 7.2 總管在回報裡宣稱「已經派一組去掃秘密」，實際沒有派

- **怎麼錯的**：**打了字沒送出**。
- **誰抓到**：**下一輪總管自己發現並當場更正。**
- **教訓**：同 `CLAUDE.md §-2.A` 第 1 款——**說做過的事要真的做過。**

---

## 八、這份交接檔是誰寫的、查了什麼、沒查什麼

**本檔由存檔與交接組單組產出，沒有第二組看過。**

### 8.1 本組**實跑查證**過的（指令與輸出都跑過，可自驗）

- 分支／head／工作樹乾淨／遠端 SHA 與 head 相同（`git status`、`git rev-parse`）
- `git diff --numstat origin/main..HEAD -- '*.py'` → **無輸出**
- 整支 PR 對 `origin/main`：**24 檔 +15,126 −2**（`git diff --shortstat`）
- 凍結面 diff 為空；`ui/tab*.py` **16**、`ui/views/page_0*.py` **5**、`app.py` **1**
- PR #845：open／draft／未合併／`mergeable_state: clean`／**三條 check run 全 `success`**
- PR #837：open／draft／**未合併**／head `5569d85`／分支 `docs/v2-constitution-governance`
- `2e2803c` 的 11 檔，逐檔確認**哪些在 `18c0df8` 已存在**（4 改寫）、**哪些是新增**（7 新增）
- `18c0df8` 當時 `docs/v2/` 底下**共 16 檔** ⇒ 加 `CLAUDE.md` ＝ 該輪射程 **17 檔**，與 PR 說明 §1.3 相符
- 〈二〉各處引用的行號**逐條 bounds-check ＋ `sed -n 'Np'` 讀出該行實際內容**
- `docs/v2/prototype/` **不存在**；`UI_TOKENS.md`／`UI_COMPONENTS.md` 全 repo **0 命中**
- `page_01_macro.py:156` 的 import 內容、`services/macro/__init__.py:39` 的再匯出清單
- 全 repo `下一步怎麼做` 僅 **2 處**命中

**本組因此就地更正了三處引用路徑**（**只更正路徑，不動任何裁決、不動任何推薦**）：

| 原敘述 | 實際位置 | 錯在哪 |
|---|---|---|
| ~~`services/ai_summary.py:69`~~ | **`ui/helpers/ai_summary.py:69`** | 檔案路徑錯，行號與內容正確 |
| ~~`page_01_macro.py:3237`~~ | **`ui/tab3_t7_ledger.py:3237`** | 檔案路徑錯（原路徑越界），行號與文案逐字正確 |
| ~~`page_01_macro.py:3291`~~ | **`ui/tab3_t7_ledger.py:3291`** | 同上 |

### 8.2 本組**沒有查**的（請下週一的人不要當成已查過）

- **11 份產出的內容本身，一句都沒有讀過。** 本組職責是交接存檔，不是稽核。
- **〈二〉六條的實質對錯完全沒有判斷**——只查了「被引用的那一行在不在、寫的是什麼」。
  **六條的「總管推薦」本組沒有複核，也不背書。**
- **〈三〉三項未結案沒有重新查證**，是照 `2e2803c` commit message 的語意轉寫。
- **母法 `CONSTITUTION.md` 的任何一行都沒有讀過**——該檔在未合併的 PR #837 分支上。
  `:897`、`§5.1` 這些引用**本組查不到**，原樣轉錄。
- **〈四〉4.2 那次風險分流本組沒有重跑**，原樣轉錄並標明它不是稽核結論。
- **沒有跑過測試、沒有開過畫面、沒有碰過任何 `.py`。**
- 「B21 只限 `page_01_macro.py` ①-4 那一處」「P0 第 12 項」「②-13」「凍結表 F2／F3」
  這些編號**本組沒有回去對照原始定義**，照客戶／總管的用法原樣轉錄。

### 8.3 本輪做了什麼

**只新增這一個檔。** 未動 `CLAUDE.md`、未動 `docs/v2/` 既有任何一檔、未動任何 `.py`、
未 force push、未 rebase、未改動既有 commit、未把 PR 轉 ready、未 merge。
