# 基金儀表板改版・完整畫面草稿 —— 第五～八節（決策・缺口・三層對照・待您裁示）

> **這四節不畫線框。** 第一～四節（版面與畫面句子）由另一組同時產出，本組沒有讀過他們的稿子，
> 也沒有和他們對過答案 —— 這是刻意的，兩邊獨立寫完再由總管合稿，才看得出哪裡對不起來。
>
> **本節所有「現在畫面是…」的敘述，都是讀程式碼推論出來的，不是看著畫面抄的。**
> ⛔ **本組從頭到尾沒有打開過任何一個畫面** —— 沙箱不允許憑證、不允許連外部 API，
> 所以沒有任何一句話是「我看到它長這樣」。這一點在第六節展開。
>
> **每一句關於現況的斷言，後面都附「出處」（檔名 + 行號）。** 沒有出處的，本組一律標「未複驗」。
> 行號以 `origin/main` = `9cbf0377` 為準（量測日 2026-09-15）。**行號會在下一次重構後失效。**

---

## 🔭 讀這四節之前，先看懂一件事：現在的網站是「九格分頁」，而且新舊不對稱

**這件事會改變您對底下好幾題的理解，所以放在最前面。**

分頁列現在有 **9 格**：**5 格正式**（①②③④⑤）＋ **4 格標著 `[新]` 的預覽**（⑥⑦⑧⑨）。
出處：`app.py:348`（程式自己寫著「現在是 9 格(5 正式 + 4 [新] 預覽)」）、`app.py:571-577`（分頁建立處）。

**但五格正式分頁裡，只有 ① 已經換成新版，其餘四格都還是舊的**：

| 分頁 | 現在跑的是哪一份 | 出處 |
|---|---|---|
| **① 市場總覽** | ⭐ **已經是新版** | `app.py:156` 匯入 `ui/views/page_01_macro.py`；`app.py:619,637` 呼叫它 |
| ② 持倉體檢 | 舊版 | `app.py:646,648` 呼叫 `render_fund_grp_health_tab()` |
| ③ 標的探索 | 舊版 | `app.py:660,662` 呼叫 `render_fund_research_tab()` |
| ④ 資產配置 | 舊版 | `app.py:671,673` 呼叫 `render_portfolio_tab()`（＝`ui/tab3_portfolio.py`） |
| ⑤ 設定與診斷 | 舊版 | `app.py:686,688` 呼叫 `render_settings_diag_tab()` |
| ⑥⑦⑧⑨ `[新]` | ②③⑤④ 的新版預覽 | `app.py:718,726,750,774` |

⇒ **所以分頁列上沒有「`[新]` 市場總覽」這一格 —— 因為 ① 的新版已經佔了正式的位置。**

**為什麼這件事重要（請務必記住這三句）**：

1. **① 上的任何一句話，今天就印在您眼前的正式畫面上**（問題九、問題五的第一列都屬於這種）。
2. **④ 上的新版句子，今天只印在 `[新] ⑨` 那一格裡**（問題六屬於這種）——
   而**正式的 ④ 上印的是另一句完全不同的話**，那一句本組這一輪才查出來，見「新發現」。
3. **② 上的新版欄位，今天只印在 `[新] ⑥` 那一格裡**（問題二屬於這種）。

⚠️ **本組單組實測，沒有第二組驗過。** 這張表取決於「本組有沒有看漏 `app.py` 裡的第十個掛載點」。

---

# 五、九題決策

> ⛔ **這一節分成兩大塊，刻意不合併成一張表。**
> **5-A 是「已經定案的七題」** —— 本組**沒有改動任何一題的結論**，只做一件事：
> 把每一題的「現況」重新去程式碼裡對一次，附上出處。
> **5-B 是「還沒定案的兩題」** —— 需要您回答才能往下走。
>
> **兩塊不要混著讀。** 5-A 的東西可以直接施工；5-B 的東西一動都還不能動。

---

## 5-A｜已定案的七題（問題一、二、三、四、六、七、八）

> **這七題的結論本組一個字都沒有改。**
> 每一題六欄：**現況｜選項｜已定案（原為「建議」欄）｜理由｜代價｜是否需重拍**。
> 「已定案」那一欄記的是**您的決定**，不是本組的推薦 —— 兩者在本節嚴格分開。

---

### 問題一｜保單那一塊，要多大？

| 欄 | 內容 |
|---|---|
| **現況** | **正式的 ④ 上，保單資料是擠在多欄卡片裡的**；而 `[新] ⑨` 已經照決定做成**全寬區塊 ＋ 四格狀態列**（第四格是「💰 總投入」）。<br>⚠️ **兩邊狀態不同，這是雙軌的正常現象，不是有一邊壞了。**<br>**出處**：`ui/views/page_04_portfolio.py:653`（`STATUS_INVESTED_LABEL = "💰 總投入"`）、`:665`（`STATUS_COLS = 4`）、`:2070-2071`（全寬區塊與理由）、`:2081`（保單一覽走全寬表格）。<br>⚠️ **正式 ④ 那一邊「擠成幾分之一寬」的具體欄數，本組未逐欄數過 → 未複驗。** |
| **選項** | **A** 全寬區塊 ＋ 狀態列補「總投入」　\|　**B** 維持多欄卡片　\|　**C** 摘要卡留在網格、明細另外全寬 |
| **已定案** | ✅ **案 A**（決策者：客戶．日期 **2026-09-14**．**總管轉述**）<br>⭐ **而且您 2026-09-07 就拍過同一件事**，那一次的紀錄**直接寫在程式裡**：<br>`ui/views/page_04_portfolio.py:49`（決定 ①：全寬區塊）、`:53`（決定 ②：狀態列補一格「💰 總投入」，變四格）。<br>⇒ **兩次結論一致。** |
| **理由** | **不選 C 的理由是您自己說的，不是本組判斷的** —— 逐字記在 `ui/views/page_04_portfolio.py:2071-2072`：<br>「**1/3 卡放不下保單一覽表；兩者並存會讓同一份資料在同頁出現兩次，不一致時使用者不知道信哪個。**」 |
| **代價** | **狀態列從三格變四格，是全站唯一一處偏離「固定三欄」的地方**（`page_04_portfolio.py:53-55`）。<br>⚠️ **四格在窄螢幕會不會塌成兩列，到今天沒有任何人實測過** —— 程式裡就地記著三條「試過但不算數」的理由（`:103-136`）：沙箱沒有瀏覽器、AppTest 沒有寬度、唯一能測的那套只跑 ①。<br>⇒ **這是一個已知的、沒被關掉的風險，不是已解決事項。** |
| **是否需重拍** | **否。** |

---

### 問題二｜持倉體檢那個「總分」，要不要換掉？

| 欄 | 內容 |
|---|---|
| **現況** | **「組合健康總分」是灰的，而且逐檔體檢表的「五桶評等」整欄恆為 ⬜ —— 兩者都是刻意的，不是還沒接。**<br>**出處**：`ui/views/page_02_health.py:291-292`（總分灰態的理由字串：「五桶評等加權」的評等定義未定）、`:1458`（`"五桶評等": NOT_READY_MARK` —— **每一列都寫死 ⬜**）、`:1402-1406`（就地說明為什麼恆為 ⬜：本站的「五桶」是總經概念，逐檔評等是另一套，維度／級距／名字都不同）。<br>⚠️ **這裡講的是 `[新] ⑥` 那一格。正式的 ② 上有沒有對應的總分格，本組未查 → 未複驗。** |
| **選項** | **A** 換成「本次體檢涵蓋範圍」　\|　**B** 先定義一套評等再做總分　\|　**C** 整格拿掉 |
| **已定案** | ✅ **案 A ＋ 欄名改「體質評等」**（決策者：客戶．日期 **2026-09-14**．**總管轉述**） |
| **理由** | 一個永遠算不出來的分數留在最顯眼的位置，等於每天提醒使用者它壞了；「涵蓋範圍」是現在就算得出來的真數字。<br>**（這一段是上一版草稿的理由，本組未改。）** |
| **代價** | ⭐ **改欄名會讓一道自動檢查當場變紅，就得同一批改掉 —— 本組實測確認。**<br>**出處**：`tests/test_wf02_health_skeleton.py:569-571` —— 它把九個欄名**逐字**釘死：<br>`"代碼", "名稱", "幣別", "近 1 年", "Sharpe", "最大回撤", "配息覆蓋", "五桶評等", "資料日期"`，<br>並且在 `:573` 額外斷言**恰好九欄**。<br>⇒ 把「五桶評等」改成「體質評等」，這道檢查**必然**變紅。<br>另一處要同步改的是欄名的來源：`ui/views/page_02_health.py:278-281`（`HEALTH_TABLE_COLUMNS`）。<br>⚠️ **本組沒有去數「除了這兩處還有沒有第三處手抄同一組欄名」 → 未複驗。** |
| **是否需重拍** | **否。** |

---

### 問題三｜舊版資產配置頁的三個小區塊，要不要留？

| 欄 | 內容 |
|---|---|
| **現況** | **三塊都已經在 `[新] ⑨` 升成具名區塊；但其中一塊「只做完一半」，而且程式自己就地承認了。**<br>**出處**：`ui/views/page_04_portfolio.py:57-63`（2026-09-07 決定 ③ 逐條落地：組合績效／效率前緣、穿透式持股／產業集中度、總經曝險聯動）。<br>⭐ **就地自陳的缺口**（`:64-69` 逐字）：「**三塊裡有一塊只做到一半…組合績效的『具名區塊』與『只留一份尺』都做到了，但委派本身沒有做** —— 它一渲染就打一次匯率 API 並落盤，被本批的零寫入量測擋下。」<br>⇒ **這一塊今天是「有名字、沒內容」。** |
| **選項** | 三塊都留（升成具名區塊）　\|　留部分　\|　都不留 |
| **已定案** | ✅ **三塊都留、升成具名區塊**（決策者：客戶．日期 **2026-09-14**．**總管轉述**）<br>⭐ **您 2026-09-07 拍過同一件事**，逐字記在 `ui/views/page_04_portfolio.py:57-58`。**兩次結論一致。** |
| **理由** | 第二塊是風險示警（穿透後才知道分散是不是假的）；第三塊是整個網站唯一把大盤和您的組合連起來的地方。**（上一版草稿的理由，本組未改。）** |
| **代價** | **(a)** 組合績效那一塊與同頁另一張卡有重複的數字 —— **上一版草稿標的代價，本組這一輪未重新量測 → 未複驗。**<br>**(b)** ⭐ **本組實測補上的代價**：組合績效的**委派本身還沒做**（見上「現況」），所以「升成具名區塊」目前拿到的是名字，不是內容。這一項已在程式裡回報給總管裁決（`page_04_portfolio.py:69`）。 |
| **是否需重拍** | **否。**（結論兩次一致；變的是「它做到哪裡」，不是「要不要做」。） |

---

### 問題四｜選股池要住哪一頁？

| 欄 | 內容 |
|---|---|
| **現況** | **選股池的編輯器與用它配對的換股顧問，確實住在兩個不同的地方 —— 本組實測確認。**<br>**選股池編輯器** → 管理室（`ui/tab_manage.py:75,85-86`，它 import 並呼叫 `_render_pool_editor`），而管理室現在是**⑤ 設定與診斷頁內的一個分區**（`ui/helpers/fund_grp_health/switch_advisor_section.py:764-768` 就地記著「七→五之後『📋 我的管理室』不再是分頁，降級成⑤頁內的分區」）。<br>**換股顧問** → **④ 資產配置**（`ui/tab3_portfolio.py:2238-2241` 呼叫 `render_switch_advisor_section`；`ui/helpers/story_nav.py:211` 也記著「渲染於 ④ 資產配置」）。<br>**畫面上自己就在指路**：`switch_advisor_section.py:770-771` 印「💡 選股池的新增/刪除已搬到〈管理室〉集中管理；本區直接讀你的選股池做換股配對。」 |
| **選項** | 搬到 ④ 緊鄰換股顧問　\|　維持住在 ⑤　\|　兩邊各放一份 |
| **已定案** | ✅ **搬到 ④ 資產配置，緊鄰換股顧問**（決策者：客戶．日期 **2026-09-14**．**總管轉述**）<br>⚠️ **這一項推翻了您 2026-09-07 的明示** —— 那一句逐字記在 `ui/views/page_04_portfolio.py:73`：<br>「**⛔ 客戶同批明示「不做的」：選股池 ＋ 除息行事曆維持留在 ⑤，本批不碰。**」<br>⇒ **推翻的事實已在上一版草稿內文寫明後才送出，您是在知情下改變決定的。本組照規矩保留這筆紀錄，不刪。** |
| **理由** | 選股池唯一的用途是餵給換股顧問，分在兩頁要來回跑。**（上一版草稿的理由，本組未改。）** |
| **代價** | ⭐ **本組實測補上兩項代價，上一版只寫「會動到幾道自動檢查」，講得太輕**：<br>**(a) 「緊鄰換股顧問」這句話，以今天的程式結構做不到。** 換股顧問**被一道全域檢查釘死只能在一個地方畫**，而那個地方是**舊的 ④**（`ui/views/page_04_portfolio.py:919-921` 逐字記著：`tests/test_ia_switch_advisor_moved_to_portfolio.py::test_switch_advisor_renders_only_from_the_portfolio_tab` 釘住渲染點**恰好一個**，`EXPECTED_RENDER_SITE == "ui/tab3_portfolio.py::render_portfolio_tab"`）。<br>⇒ 要「緊鄰」，就得**搬進舊 ④** —— 而那正是您 2026-09-14 另一道指示說「不改內容」的那一批（同問題五，見 5-B）。<br>⇒ 若改搬到 `[新] ⑨`，**那裡的換股顧問是灰的、根本沒畫**（`page_04_portfolio.py:938-942` 記著它缺兩份輸入：雲端選股池與逐檔基準線）。**「緊鄰」會變成緊鄰一塊灰色。**<br>**(b) 當初把它們分開，是為了修一個真的發生過的錯。** `switch_advisor_section.py:757-758` 逐字：「選股池的加/刪/改已移到管理室集中管理（**避免同一次執行兩處都渲染 → 元件 ID 相撞**）」。<br>⇒ 搬回去時若**兩邊各留一份**，那個錯會重新發生。**只能留一份。** |
| **是否需重拍** | **否（結論不變）**，但 **(a) 這個代價是新的**，它把問題四和問題五綁在同一個裁示上 —— 已列入第八節。 |

---

### 問題六｜資產配置的差距那一行，要不要講「往哪邊搬」？

| 欄 | 內容 |
|---|---|
| **現況** | ⭐ **本組這一輪查出：這一行有兩份，住在兩個不同的分頁，而且句子不一樣。上一版草稿只講了其中一份。**<br>**(1) `[新] ⑨` 那一份**（上一版草稿引用的就是它）：`ui/views/page_04_portfolio.py:1581`（「核心比目標少 X%」）＋ `:1452-1453`（「**從衛星移 NT$… 到核心**」／「**從核心移 NT$… 到衛星**」）＋ `:1311`（收尾「**就到目標**」）＋ `:1458`（沒填本金時補「（只算已填本金的 N 檔）」）。<br>**(2) ⭐ 正式 ④ 那一份**（**上一版草稿沒有提到**）：`ui/tab3_portfolio.py:1404-1406` 逐字 ——<br>「⚠️ 配置偏離 X%（核心 Y% vs 目標 Z%）— **核心過重，可贖回轉衛星**」／「— **衛星過重，可獲利轉核心**」<br>它只在偏離超過 10% 時出現（`:1403` 的條件）。另在 `:1359` 有一格「目標偏差（目標核心 Z%）」只印百分點、不印金額也不印方向。<br>⇒ **「往哪邊搬」的句子，正式畫面上今天就有一句，而且它在舊分頁裡。** |
| **選項** | **A** 金額 ＋ 方向都講（維持現狀）　\|　**B** 只講金額、不講方向　\|　**C** 只講百分點、不講金額 |
| **已定案** | ✅ **案 B ＋ 區塊名改「現況 vs 你設定的目標」**（決策者：客戶．日期 **2026-09-14**．**總管轉述**） |
| **理由** | 「從衛星移到核心」是方向與動作；「差多少錢」是事實。**（上一版草稿的理由，本組未改。）** |
| **代價** | **(a)** 有一道自動檢查明文要求這一行要講「所需動作」，改句子要同一批改掉 —— **上一版草稿標的代價；本組看到相關檢查檔存在（`tests/test_wf04_portfolio_skeleton.py:1642,1803`），但沒有逐條讀完它到底斷言什麼 → 這一項未複驗。**<br>**(b) ⭐ 本組實測補上的代價**：**要改的地方是兩處不是一處**，而第二處（正式 ④）**住在舊分頁**。<br>⇒ **問題六因此和問題五撞上同一道指示**（「舊分頁不改內容」）。已列入第八節。 |
| **是否需重拍** | **否（結論不變）**，但**射程變了**：原本以為改一處，實際是兩處，而第二處要先解鎖。 |

---

### 問題七｜兩件卡在資料源上的事

| 欄 | 內容 |
|---|---|
| **現況** | **(1) MoneyDJ 的「同類排名」確實已經做好而且在畫 —— 本組實測確認。**<br>**出處**：`ui/helpers/fund/checkup.py:557,579`（把 `同類排名` 這一欄放進表格）、`:610-611`（欄名逐字就是「同類排名」）、`:513`（就地註解：從 MoneyDJ `peer_compare` 萃取百分位，缺則顯示「—」）。<br>**(2) 我們自己算的四分位，卡在兩道關卡上。**<br>**出處**：`services/peer_rank.py:8-10` 就地寫著自己的誠實規則 —— 同類少於 2 檔不給名次；2~3 檔只給 X/n **不分四分位**；要達到門檻檔數才給真四分位。<br>⚠️ **上一版草稿那張「0 / 8 檔」的分組表，是上一組自己跑的；本組這一輪沒有重跑 → 未複驗。**<br>**(3) 台灣的無風險利率確實沒有取得管道，但「注入口」是現成的。**<br>**出處**：`services/fund_service.py:34,39`（`set_risk_free_rate()` —— 可由外部注入年化無風險利率）。<br>⚠️ **「本站沒有任何取得台灣利率的管道」這句是上一組的結論，本組沒有重查 → 未複驗。** |
| **選項** | (1) 同類排名：**保留** \| **一律關成 ⬜**　　(2) 自算四分位：**保留** \| **關成 ⬜**　　(3) 無風險利率：**用替代利率並標註** \| **不給** |
| **已定案** | ✅ **重新拍板（決策者：客戶．日期 2026-09-15．總管轉述）**：<br>**(1-a) MoneyDJ 同類排名 —— 保留，不關掉。**<br>**(1-b) 我們自己算的四分位 —— 關閉，一律 ⬜。**<br>**(2) 無風險利率 —— 先用可取得的替代利率，畫面上標註「非央行定存牌告利率」。** |
| **理由** | (1-a) 它的「同類」是 MoneyDJ 自己分的，和我們那把分類無關，關掉等於把一個能用的東西換成空白。<br>(1-b) 分組錯誤的排名比沒有排名更危險。<br>(2) 沒有它，台股標的的夏普值整條算不出來，代價更大。**（以上為上一版草稿的理由，本組未改。）** |
| **代價** | ⭐ **本組實測補上一項上一版沒講的代價 —— 而且它是一句畫面上的假話**：<br>**(1-a) 保留同類排名，就同時保留了「那 3 成／7 成」這個沒有人量過的數字。**<br>`ui/helpers/fund/checkup.py:613` 那句「約 3 成基金 MoneyDJ 無同類排名資料 → 顯示 —」**是一段滑鼠提示文案，不是量測結果** —— 程式裡沒有任何一行在數它。<br>⇒ 依您的規矩，**「約 7 成有值」在畫面上與文件上都要標「未複驗」，不能當事實講。**<br>另：(1-b) 關閉四分位之後，**③ 的「同類位置」那一格會變成長期空白**，畫面上要說清楚它為什麼空。 |
| **是否需重拍** | **否** —— 您 2026-09-15 已在更正過的前提上重新拍過了。**這一題的「重拍」已經完成，不是待辦。** |

---

### 問題八｜核心／衛星是「看基金名字猜」的，而它猜錯了

| 欄 | 內容 |
|---|---|
| **現況** | **本組逐條實測，四件事全部確認成立：**<br>**(a) 它真的是拿名字比對關鍵字。** `services/ai_service.py:26-28` —— 核心關鍵字清單裡有「**配息**」，衛星關鍵字清單裡有「科技」「半導體」。<br>**(b) 核心那組先比，比中就不再看衛星那組。** `services/ai_service.py:40-42`（先 `if 核心關鍵字 → 回核心`，再才輪到衛星）。<br>⇒ 名字尾巴帶法定揭露句「（基金之配息來源可能為本金）」的基金，**必然**先被「配息」攔截判成核心。<br>**(c) 全站不只一套，本組實測到三套，判法各不相同**：① `services/ai_service.py:26-28`（看基金名字）；② `ui/helpers/session.py:101-103`（看基金名字，但關鍵字清單**不一樣**，另有一份白名單）；③ `services/health/asset_class.py:137`（**看基金類別，不看名字** —— 逐字「本函式分類依據完全不同(MoneyDJ 基金類別關鍵字 + 3-3-3)」）。<br>**(d) 它真的會寫進您的資料表，而且是三顆您按得到的按鈕。** 本組實測到三個寫入點，每一個都把猜出來的值寫成「級別」：<br>　`ui/helpers/cloud_io.py:251-254` ← 由 `ui/helpers/portfolio/policy_admin_section.py:385` 的「**📦 立即全部寫入**」觸發<br>　`ui/tab3_portfolio.py:1583` ← 批次加入<br>　`ui/tab3_t7_ledger.py:1053` ← 套用為起始部位<br>⚠️ **本組的掃法只認「寫進去那一行本身出現 `is_core` 字樣」的寫法** —— **先把值算進一個變數、再寫出去的寫法，本組結構上看不到。⛔ 請不要把「掃到三處」讀成「只有三處」。** |
| **選項** | (1) 一律以您自己填的那一欄為準 ＋ (2) 沒填的顯示 ⬜ ＋ (3) 寫死「系統永不自動寫入」　\|　折衷版：顯示空白但提供一鍵帶入供您逐檔確認 |
| **已定案** | ✅ **(1)(2)(3) 三件一起**（決策者：客戶．日期 **2026-09-14**．**總管轉述**）<br>⚠️ **折衷版（一鍵帶入）您沒有選** —— 您回的是「都照建議」，沒有提那條路。**照三件辦。**<br>⭐ **而且 (1) 不是新規定，是把您 2026-08-07 說過的話落實。** 您的原話逐字記在 `services/health/asset_class.py:135`（拍板註記在同檔 `:133`，模組開頭 `:52` 另記一份）：<br>「**『核心佔比該不該調整』的唯一真相是使用者在 Google Sheet 標的 `policy_tier`**」<br>⚠️ **「該不該調整」這五個字是您原話的一部分，不能拿掉** —— 同一段（`:136-139`）明寫：判斷「這檔基金體質偏穩健還是偏主題」是**另一個問題**，走另一套。 |
| **理由** | 您自己填的值不會被法定揭露句干擾；猜錯比空白更糟（空白看得出來，猜錯看不出來）。**（上一版草稿的理由，本組未改。）** |
| **代價** | **(a)** 您沒填過的基金會從「有分類」變成 ⬜「尚未指定核心／衛星」。**（上一版草稿已標，本組未改。）**<br>**(b) ⭐ 本組實測補上的代價**：**那三個寫入點全部住在舊分頁**（`ui/tab3_portfolio.py`、`ui/tab3_t7_ledger.py`、`ui/helpers/portfolio/policy_admin_section.py`）。<br>⇒ 第 (3) 件（寫死「永不自動寫入」）**必然要動舊分頁的程式**。**這和問題五、問題六撞上同一道指示**，已列入第八節（本組在那裡也給了一個「這一題可能不算同一種衝突」的讀法）。 |
| **是否需重拍** | **否。** ⚠️ **但急迫性請記住**：這不是「以後要防的」，是**現在每按一次那三顆鈕就在發生**。<br>⚠️ **您的資料表目前有幾筆已經被蓋掉，本組不知道** —— 那要讀您的線上資料，這一輪不允許。**未複驗。** |

---

## 5-B｜還沒定案的兩題（問題五、問題九）

> ⛔ **這兩題在您回答之前，本組一個字都不動。**
> 它們不是「還沒做完」，是**還沒決定**。

---

### 問題五｜另外兩處「該買該賣」的句子

| 欄 | 內容 |
|---|---|
| **現況** | ⭐ **本組把上一版草稿那三列全部重查了一次，三列都要更正位置。**<br>**第 1 處｜市場總覽的那句行動話 —— 它不是一個獨立區塊，它是「② 依據」那張表底下的一行註記。**<br>**出處**：`ui/views/page_01_macro.py:736`（算出那句行動話）→ `:748,751`（把它傳進表格與註記）→ `ui/helpers/macro/beginner_view.py:974`（印出來的形狀逐字是「**；白話行動 —— ⟨那句話⟩**」）。<br>那句話本身的五種寫法在 `services/macro/composite_score.py:305,314,316`，逐字含「**可滿倉持有，衛星部位積極佈局成長題材**」「**拉高現金水位至 15-25%，衛星部位設停利**」「**現金 30%+，核心轉防守型**」。<br>⭐ **這一處住在 ① —— 而 ① 已經是正式分頁上的新版**（見本節開頭那張表）⇒ **它今天就印在您眼前，而且它屬於「v2 新頁」那一批，不受「舊分頁不改內容」拘束。**<br>✅ **另補一件好消息**：證據撐不住的時候，程式**已經**會把那句行動話清空（`page_01_macro.py:739-740`），所以它不是無條件印出來的。<br>**第 2、3 處｜說明書的兩個章節 —— 上一版說「兩處」，本組實測確認，而且三個具體句子都在。**<br>**出處**：`ui/tab6_manual.py:89`（章節標題逐字「⚖️ 再平衡公式（**One-Click Rebalance**）」）、`:474-475`（表格逐字「5~10% → ⚠️ **建議再平衡**（下次配息時執行）」「> 10% → 🚨 **必須執行再平衡**」）、`:478-480`（逐字「從「**最大衛星基金**」贖回 ΔNT$，轉入「**最小核心基金**」」）；<br>另一章 `:90`（章節「🛡️⚡ 核心/衛星分類邏輯」）、`:534`（逐字「偏離 >5% → ⚠️ **建議再平衡**　\|　偏離 >10% → 🚨 **必須執行**」）。<br>⭐ **本組更正上一版一句話：說明書在新舊兩邊的「要不要先勾選」是相反的。**<br>・**正式 ⑤（舊）：沒有任何勾選框，打開分頁就直接印出來。** 出處 `ui/tab_settings_diag.py:379,383-384`。<br>・**`[新] ⑦`：有一個勾選框「📖 載入使用手冊」，預設不勾。** 出處 `ui/views/page_05_settings.py:472`（標籤）、`:1676-1677`（真的是一個預設關閉的勾選框）。<br>⇒ **上一版寫「⑤ 的線框裡那個☐說明書勾選框」只對新的那一邊成立；正式畫面上那兩句話，今天是無條件印出來的。** |
| **選項** | 見下方「⛔ 待您裁示」——**甲／乙／丙**。 |
| **建議** | ⛔ **本組不給推薦，理由與上一版相同且本組認同**：甲乙丙的差別不在技術，在於**您要哪一邊讓步**，而**兩邊都是您自己的指示**。本組沒有立場替您排先後。<br>**本組能做的是把代價量出來**（下一欄）。 |
| **理由** | 「該做什麼」換成「現在是什麼」，是您當初定的原則的字面要求。**（上一版草稿的理由，本組未改；這個「改成什麼」的結論本身沒有爭議。）** |
| **代價** | ⭐ **本組把甲乙丙的代價重新量了一次，並且更正了一項**：<br>**甲（以本題為準，改說明書）**：要把說明書**單獨**從「舊分頁不改內容」的名單裡放出來。<br>**乙（以 9/14 那道裁示為準，一個字不改）**：那兩句話**會繼續無條件印在正式 ⑤ 上**（⚠️ **更正**：上一版說「印在 ⑤ 的畫面上，只是旁邊多一句免責」——實際上正式 ⑤ 連勾選框都沒有，是一打開就印）。<br>**丙（兩者都守，⑤ 不再叫出說明書）**：使用者在新舊兩邊**都查不到說明書**（「偏離多少算多」無處可查）。<br>⚠️ **上一版說那道 9/14 裁示「在程式庫裡找不到原始記錄」** —— **本組這一輪也沒有找到，狀態未變 → 未複驗。** |
| **是否需重拍** | ⛔ **結論（兩處都改）沒有爭議；卡住的是「什麼時候能動、要不要先解禁那個檔」。**<br>**在您裁示甲乙丙之前，說明書那兩處本組不動。** |

---

### 問題九｜市場總覽那張直接給您配置比例的卡

| 欄 | 內容 |
|---|---|
| **現況** | ⭐ **本組把那張卡的每一個數字都回到程式裡對過，上一版草稿更正後的版本是對的 —— 本組逐條複驗通過。**<br>**它住在哪**：`ui/views/page_01_macro.py:767-814`（卡名 `📐 建議資產水位`，見 `:780,788,808`）。<br>⭐ **它在正式的 ① 分頁上**（① 已換新版），**不是預覽格** ⇒ **今天就印在您眼前。**<br>**(1) 三個百分比只有五組可能，`55／30／15` 是其中之一。** 出處 `shared/signal_thresholds.py:394-400`，逐字五組：`85/10/5`、`70/20/10`、**`55/30/15`**、`40/35/25`、`25/40/35`。<br>⇒ **上一版最早抄的 `55／35／10` 那一組，在程式裡根本不存在。更正成立。**<br>**(2) 兩個門檻的真值是 `+1.75` 與 `-1.00`。** 出處 `shared/signal_thresholds.py:409-410`（無景氣燈號時的預設值：停利 `1.75`、加碼 `-1.0`）。<br>**(3) `+1.00` 在任何景氣燈號下都不可能出現。** 出處 `shared/signal_thresholds.py:406`，五個燈號對應的停利門檻逐字是 `1.25 / 1.5 / 2.0 / 1.75 / 1.5` —— **沒有一個是 1.00。更正成立。**<br>**(4) 那句「不會跟著景氣燈號變動」是真的，而且是必然的。** 出處 `ui/views/page_01_macro.py:785` —— 它呼叫時**把景氣分數那一格寫死成「沒有」**；`services/allocation_ladder.py:46-48` 收到「沒有」就退回預設值並把燈號設成空；`page_01_macro.py:804-806` 再依那個空值選文案 ⇒ **永遠走「這兩個門檻是固定的預設值，不會跟著台灣景氣燈號變動」那一句。**<br>⇒ **上一版最早寫的「已依台灣景氣黃燈調整」意思剛好相反。更正成立。**<br>✅ **一件值得肯定的設計**：`page_01_macro.py:774-775` 就地寫著「文案讀旗標、不寫死『預設』兩個字，日後真的接上景氣燈號時這張卡會自己改口」。**它現在說的是實話。** |
| **選項** | **A** 整張拿掉　\|　**B** 只把標題「建議」改成「參考」　\|　**C** 上半（三個百分比）拿掉、下半留下並改詞 |
| **建議** | ⭐ **本組推薦 C，與上一版一致** —— 但**這是本組的推薦，不是已定案**，因為您先前是在抄錯的數字上點頭的。<br>**若選 C，畫面上要寫的是**：卡名改「**📐 估值位階門檻**」；內容改「**偏貴線 Z ≥ +1.75、偏便宜線 Z ≤ -1.00**」；**原本那段白話解釋逐字照留**；**「不會跟著台灣景氣燈號變動」那一句照原句留著**（⛔ **不要留成反過來的那一句**）。 |
| **理由** | 上半是一份配置推薦，沒有「事實版」可言；下半的 Z 門檻是客觀事實（這檔基金離它自己近三年平均多遠），而且可以拿去對照每一檔基金自己的 Z 值。<br>**C 拿掉的正好是唯一沒有事實版的那一半。**<br>**不選 B**：換兩個字不會讓那三個百分比不是推薦。**不選 A**：連有用的下半一起拿掉，還留一個洞。<br>**（以上為上一版草稿的理由，本組複驗後認為仍然成立，未改。）** |
| **代價** | **那一列會少掉一塊內容的一半**（三欄網格中的一欄變短）。<br>⚠️ **本組沒有查「這一頁還有沒有第三處同型的東西」，也不宣稱只有這些。** |
| **是否需重拍** | ⛔ **要 —— 請您重看一次再點頭。**<br>**理由**：**選 C 的切法與數字無關**（上半是推薦、下半是事實，這個切法不受數字影響），**所以本組判斷您的結論很可能不變**；<br>**但您當時看到的那張卡，有兩個數字和一整句話是錯的**，而**要寫進畫面的字也跟著變了**（是 `+1.75` 不是 `+1.00`）。<br>**若您看完真實的現況仍然選 C，這一題就結案；若改變主意，請直接說。** |

---

### 📌 問題九順帶的一處同型指路（一起回答就好）

**同一頁底下有一句指路，逐字是**：`📋 **逐檔的加減碼建議**請到〈持倉體檢〉`
**出處**：`ui/views/page_01_macro.py:2031`。

**問題不在指路本身，在它指向的那個東西的名字** —— ② 之後沒有「加減碼建議」，它有的是逐檔體檢。
**本組推薦改成**：「📋 **逐檔的體質診斷**請到〈持倉體檢〉」。

⚠️ **代價（本組實測）**：這句話**被一道自動檢查盯著**，那道檢查把「加減碼建議」這五個字當成不得消失的記號
（`tests/test_wf01_detail_zone_order.py:135` 把它存成一個常數、`:414` 斷言這句指路不得消失）。
⇒ **改文字要同一批改那道檢查。**
⚠️ **這是本組推薦，不是已定案。**

---

# 六、未做與不確定（照實說）

> ⛔ **這一節是整份草稿最重要的部分之一。**
> 一份草稿的價值不在它宣稱查到了什麼，在它**誠實標出哪裡沒查**。
> 底下每一條都是「本組知道自己不知道」的東西 —— **沒被列到的，不代表查過了。**

## 6-1｜⛔⛔ 最重要的一條：**沒有人打開過畫面**

**本組從頭到尾沒有渲染過任何一次真實畫面，一次都沒有。**

- 沙箱**不允許憑證**（Google Sheets、FRED、Gemini 一律連不上）、**不允許打外部 API**。
- 因此**這份草稿裡每一句「現在畫面是…」，都是讀渲染程式碼推論出來的**，
  不是「本組看著畫面抄下來的」。
- ⚠️ **這兩者的差別很實在**：程式碼告訴您「在什麼條件下會印什麼」，
  **但它不會告訴您那個條件今天成不成立** —— 例如某個區塊可能因為資料抓不到而整塊是灰的，
  而讀程式碼的人會以為它有內容。
- ⛔ **所以：本節所有「畫面上會出現 ⟨某句話⟩」，正確的讀法是「在程式判斷成立時會出現」，不是「今天一定看得到」。**

**這不是本組偷懶，是環境限制** —— 但**限制本身不會讓推論變成事實**，所以逐字寫在這裡。

## 6-2｜⛔ 您的缺口清單約 46 項，這一輪只碰了 10 項 ＋ 2 項附帶，**其餘約 34 項完全沒碰**

- 已測的是 **P0／P1 共 10 項**，另有 **2 項**是查別的東西時順手撞到的。
- **其餘約 34 項：一次都沒有打開過。**
- ⛔ **請不要把第七節那張三層對照表讀成「缺口盤點完成」** —— 它涵蓋的是那 12 項，不是 46 項。
- ⚠️ **「46」「10」「2」「34」這幾個數字是總管交辦時給本組的，本組沒有自己去數那份清單 → 未複驗。**

## 6-3｜上一版草稿自陳「只讀過 2 個檔，其餘線框沿用舊規格、未回程式碼複驗」

- 也就是說：**您先前看過的那份線框，大部分沒有回到程式碼對過**。
- 本組這一輪查出的**三處位置錯誤**（問題五第 1 處的區塊歸屬、問題六漏掉正式 ④ 那一句、
  問題五說明書勾選框的新舊相反），**都是這個沒複驗的後果**。
- ⚠️ **「只讀過 2 個檔」是上一組自己寫的，本組沒有回頭數它到底讀了幾個 → 未複驗。**

## 6-4｜上一版草稿自己承認過的事（本組照錄，一條都沒刪）

| 項目 | 上一組自陳 |
|---|---|
| **九題是一輪一輪冒出來的** | 「前幾輪我每次都以為找完了。**所以我不敢跟您保證第十個不存在。**」 |
| **修錯的那一次又生出三個新錯** | 換股顧問的欄位、配息月曆的順序、保單明細表的兩欄 ⇒「**新寫的句子比舊句子更值得您懷疑**」 |
| **九題裡四題的現況本身是錯的** | 問題七、九、三、八 —— **也就是說那四題是在錯的資訊上做的決定** |
| **灰字那張表是已知清單，不是全部** | 「之後一定還會冒出沒列到的位置」 |
| **問題八沒有清點「還有哪些地方在用這個分類」** | 只查了怎麼判、以及寫入路徑 |
| **問題八不知道資料表被蓋掉幾筆** | 要讀線上資料，不允許 |
| **問題七的三項轉述** | 「約 3 成抓不到」（程式註解，沒量過）、無風險利率（資料組查的，沒重查）、「分類根因已修好」（別人說的，沒複驗） |

## 6-5｜⛔ 本組這一輪自己的未驗事項（逐條列出，不美化）

| 沒查的 | 為什麼 / 狀態 |
|---|---|
| **上一版那張「0 / 8 檔」分組表** | 上一組自己跑的；本組**沒有重跑** |
| **「30 個淨值點、42~43 天、至少 50 點、至少三年」** | 同上，**本組沒有重量** |
| **「本站沒有任何取得台灣利率的管道」** | 上一組結論，**本組沒有重查** |
| **「約 3 成 MoneyDJ 沒有同類排名」** | 本組**確認了它是一段滑鼠提示文案**（`checkup.py:613`），但**沒有自己去量真實比例** |
| **問題六那道自動檢查到底斷言什麼** | 本組**只確認相關檔案與行號存在**（`tests/test_wf04_portfolio_skeleton.py:1642,1803`），**沒有逐條讀完** |
| **問題二「除了那兩處還有沒有第三處手抄九個欄名」** | **沒查** |
| **正式 ② 上有沒有對應的「總分」格** | **沒查**（本組只查了 `[新] ⑥`） |
| **正式 ④ 的保單資料實際擠成幾欄** | **沒數** |
| **問題三「兩張卡重複三個數字」** | 上一組量的，**本組沒有重量** |
| **問題八的第四、第五個寫入點** | 本組的掃法**結構上看不到**「先算進變數再寫出去」的寫法 |
| **9/14 那道「舊分頁不改內容」裁示的原始出處** | 本組**也沒有找到**，狀態與上一版相同 |
| **「46 項缺口／10 項已測」這些數字** | 總管交辦時給的，**本組沒有自己數** |
| **`app.py` 有沒有第十個掛載點** | **沒有窮舉**；本節開頭那張九格表取決於「本組有沒有看漏」 |

## 6-6｜⚠️ 三句本組刻意**不說**的話（它們取決於「有沒有漏看」）

本組在這份草稿裡**沒有**、也**不會**講下面這三種話：

1. ❌「**全站只有 N 處**」—— 本組每一次掃描都可能被「換個寫法」躲過。
   **實例**：問題八那三個寫入點，本組的掃法**看不到**先存變數再寫出去的寫法。
2. ❌「**全部都是**」/「**都查過了**」—— 本組查的是被點名的那幾項。
3. ❌「**這一頁只有這兩處**」—— 上一組在問題九末尾就明講過不敢這樣說，本組同意。

⚠️ **凡是本節出現的計數（三套分類器、三個寫入點、兩個章節、五組配置、三處位置更正），
一律是「本組單組實測到的下界」，不是母體數。**

---

# 七、資／算／畫三層對照

> ⛔ **本組沒有照抄總管給的材料，每一列都自己回程式碼重驗過一次。**
> **三欄的定義**：
> **資料在不在** ＝ 這個數字的原始素材，現在抓不抓得到。
> **算式在不在** ＝ 從素材變成那個數字的算法，寫好了沒。
> **畫面在不在** ＝ 使用者按得到的地方，畫不畫得出來。
>
> ⚠️ **「畫面在」一律是讀渲染碼推論的**（見 6-1），不是本組看到的。

## 7-A｜三層都在（已經在畫）

| 項目 | 資料 | 算式 | 畫面 | 出處（本組實測） |
|---|---|---|---|---|
| **相關性矩陣** | ✅ | ✅ | ✅ **新舊兩邊都掛** | 算式 `ui/helpers/fund_grp_health/correlation.py:113,164`；舊 ② 掛在 `ui/helpers/fund_grp_health/__init__.py:98`（`render_fund_grp_health_extras`）；新 ⑥ 掛在 `ui/views/page_02_health.py:1739,1758` |
| **配置回測五策略** | ✅ | ✅ | ✅ **新舊兩邊都掛** | `ui/helpers/fund_grp_health/backtest_section.py:22`；舊 ② `ui/tab_fund_grp_health.py:412-413`；新 ⑥ `ui/views/page_02_health.py:1737,1763` |
| **追蹤誤差** | ✅ | ✅ | ✅ **只在一個檔** | `ui/tab2_single_fund.py:216`（風險指標列）、`:257`（長版「追蹤誤差 TE(1Y)」）、`:261`（短版「追蹤誤差(1Y)」）。**可達路徑**：③ → `ui/tab_fund_research.py:129,133` 呼叫 `render_single_fund_tab()`。<br>**正對照**：全 repo 只有這一個production 檔含「追蹤誤差」字樣（另一處在測試檔）；同一把掃描對「Sharpe」在 `ui/**` 命中 **25 個檔**，證明掃描本身有效 |
| **基金 Z 值** | ✅ | ✅ | ✅ | 算式 `services/zscore_engine.py:44`（`nav_zscore`）；畫在 `ui/tab2_single_fund.py:1540,1543` |
| **操盤評分／上下檔捕捉率** | ✅ | ✅ | ✅ | 算式 `services/capture_ratio.py:1-3`；欄位 `ui/helpers/fund_grp_health/columns.py:250`（「上檔捕捉%」，含「各至少 N 個月才算得出來」的誠實說明） |
| **同類排名（MoneyDJ 給的）** | ✅ | ✅ | ✅ | `ui/helpers/fund/checkup.py:513,557,579,610-611` |

## 7-B｜⛔ **畫面在說謊** —— 這一列請單獨看

| 項目 | 資料 | 算式 | 畫面 | 出處（本組實測，逐條可自驗） |
|---|---|---|---|---|
| **費用（TER）** | ✅ **兩份都抓得到** | 不需算式 | ⛔ **在畫，但畫的是另一個東西** | **(1) 卡片的標題**寫「**💰 TER 費用率**」：`ui/helpers/fund/checkup.py:280`（區塊起點 `:272`）。<br>**(2) 卡片的數字**來自 `mgmt_fee`：`ui/helpers/fund/checkup.py:196`（`_ter_val = _safe_num(mj.get("mgmt_fee"))`）→ `:205`（存成 `ter_val`）→ `:273-274`（卡片讀它）。<br>**(3) 而 `mgmt_fee` 是「最高經理費(%)」，是一個收費上限**：`repositories/fund/fund_orchestration.py:1082`。<br>**(4) 真正的總費用率有抓，但沒有被這張卡用**：`repositories/fund/fund_orchestration.py:1088`（`total_expense_ratio` ← 「總費用率(%)」）。<br>**(5) ⭐ 連「沒資料」的那句話都露餡了**：抓不到時畫面印的是「💰 TER 費用率 — ⬜ ⟨代碼⟩ 缺 **mgmt_fee** 資料」（`ui/helpers/fund/checkup.py:297`）—— **標題說 TER，錯誤訊息說缺的是經理費。** |

⭐ **這一列有一個本組實測到的、非常強的自證**：
**同一個數字，在隔壁的表格裡欄名是對的** —— `ui/helpers/fund/checkup.py:577` 把 `ter_val` 這個值
放在一欄叫「**最高經理費%**」的欄位底下。
⇒ **同一份程式，同一個數字，表格裡叫「最高經理費」，卡片上叫「TER 費用率」。**
**這個網站自己已經知道正確的名字是什麼了。**

⚠️ **另補一件與本列相關、上一版沒提的事**：費用與規模**先前已經有反對納入評分的決議**
（`services/health/grade.py:20` 自陳「官方揭露率實測 0，有值的都是拿經理費當費用率」）。
**這與「把卡片標題改對」不衝突** —— 一個是「要不要拿它評分」，一個是「有沒有叫錯名字」。
⚠️ **本組只讀了 `grade.py:20` 那一句，沒有重量「揭露率 0」這個數字 → 未複驗。**

## 7-C｜算式在、資料在，只差把它接起來（不需要新資料源）

| 項目 | 資料 | 算式 | 畫面 | 出處（本組實測） |
|---|---|---|---|---|
| **危機回撤** | ✅ | ✅ **完整** | ❌ **沒有任何畫面在用** | 引擎 `services/crisis_backtest.py`（`detect_crisis_events`）仍在；`app.py:158-169` 記著 v19.314 已把危機回測室整功能拔掉、且**沒有任何 UI 再 import 它**；`STATE.md:2108` 逐字記著那個 UI「**import 自 v19.31 起即註解停用、進不去**」 ⇒ **當初拔掉的是一個已經死掉的畫面，引擎本身沒有被動到。** |
| **風險貢獻** | ✅ **共變異數矩陣算得出來** | ⚠️ **只到共變異數，沒有拆成逐檔風險貢獻** | ❌ | `services/portfolio_frontier.py:59-62`（`annualized_moments` 回年化共變異數 `Sigma`） |
| **資訊比率** | ✅ 分子分母都在手 | ❌ **沒有把兩者相除的地方** | ❌ | 分母＝追蹤誤差（見 7-A）；分子＝超額報酬。⚠️ **本組沒有找到現成的合成函式，也沒有窮舉搜尋 → 這一列標「未複驗」** |
| **12 個月現金流** | ✅ | ✅ **已經可以指定任意年月** | ⚠️ **未確認有「連續跑 12 個月」的畫面** | `services/dividend_calendar.py:882`（`predict_ex_for_month(schedule, year, month, ...)` —— 年、月都是參數）。⚠️ **本組沒有查有沒有畫面在跑 12 期迴圈 → 未複驗** |
| **匯率曝險改按金額** | ✅ 金額與幣別都在 | ⚠️ **現在的算法是數檔數** | ⚠️ **在畫，但畫的是檔數** | `ui/tab3_portfolio.py:379`（`_fx_counts[_ccy] = _fx_counts.get(_ccy, 0) + 1` —— **一檔加一，不是把金額加起來**）、`:380`（分母也是檔數總和） |
| **前瞻衝擊** | — | — | — | ⚠️ **本組這一輪沒有查這一項 → 完全未複驗** |

## 7-D｜⛔ 抓不到（要新資料源，或根本沒有來源）

| 項目 | 資料 | 算式 | 畫面 | 出處（本組實測） |
|---|---|---|---|---|
| **債券久期／信評分佈／YTM** | ❌ | ❌ | ❌ | 把 `duration` / `久期` / `信評` / `信用評等` / `YTM` 一起掃過整個資料層（`repositories/**`），**唯一的命中是 `repositories/news_repository.py:410` 的一份新聞關鍵字清單**（`"bond": ["bond","treasury","yield","credit","duration","coupon",…]`）——**那是用來分類新聞的，不是基金欄位。**<br>⚠️ **單組實測，取決於有沒有漏看；若這些欄位以別的名字存在，本組這一掃看不到。** |
| **未對沖比例** | ❌ | ❌ | ❌ | ⚠️ **本組這一輪沒有查 → 未複驗**（上一組說沒有 hedge 欄位） |
| **同類使用者比較** | ❌ | ❌ | ❌ | 沒有多租戶基建。⚠️ **本組沒有查證這一句 → 未複驗** |
| **經理人姓名／任期** | ❌ | — | ❌ | 把 `經理人` / `manager_name` / `fund_manager` / `基金經理` 一起掃過 `repositories/**`、`services/**`、`ui/**`，**命中全部不是人名**：兩處是「採購經理人指數」（`repositories/tw_pmi_repository.py:141,312`），其餘是「經理人**操作能力評分**」（`services/capture_ratio.py:1,3,12,41`）。**沒有任何一處存基金經理人的姓名或到任日。** |

## 7-E｜性質不同，不是資料問題

| 項目 | 說明 | 出處 |
|---|---|---|
| **稅務** | 這是一個**規則引擎**要不要做的問題，不是資料抓不抓得到。<br>而且**現在的畫面已經在誠實揭露它沒算稅**：`services/policy_fee_optimizer.py:45-51` 的免責句逐字寫「⚠️ 教學參考,非賣出建議…**未計入**:海外所得/最低稅負、基金贖回/轉換費與短線交易罰則、以及贖回後放棄的複利。」 | `services/policy_fee_optimizer.py:45-51` |
| **自算四分位** | 算式寫好了而且**自帶誠實護欄**（樣本不足就不給名次）；卡住的是分類與樣本量，不是算式。 | `services/peer_rank.py:1,8-10` |
| **無風險利率（台灣）** | **注入口是現成的**（外部可以把一個年化利率餵進來），缺的是台灣那個數字的來源。 | `services/fund_service.py:34,39`（`set_risk_free_rate`） |

## 7-F｜⭐ 同名陷阱（一個都不准寫成「已完成」）

> **這一張表的用途是擋一種特定的錯**：看到一個名字很像的東西，就以為那件事已經做好了。
> **本組逐條打開看過，每一條都是「名字像、東西不同」。**

| 看起來像 | 實際是什麼 | 出處（本組實測） |
|---|---|---|
| `ER` 像 Expense Ratio（費用率） | **Kaufman 效率比** —— 量的是「趨勢 vs 來回震盪」，和費用完全無關 | `services/fund_type_classifier.py:3,27` |
| `contribution_by_fund` 像風險貢獻 | **報酬**貢獻 —— 逐字定義是「各檔對組合**報酬**的貢獻 ＝ 權重 × 該檔於共同期間報酬」 | `services/portfolio_performance.py:193-195` |
| `peer_rank` 像同類**使用者**比較 | 同類**基金**四分位排名 | `services/peer_rank.py:1` |
| 「TER 費用率」卡 | **最高經理費**（收費上限），不是總費用率 | 見 7-B |
| ⭐ **「經理人操作評分」像經理人資料** | **是一個分數，不是一個人** —— 全站沒有存任何基金經理人的姓名或任期 | `services/capture_ratio.py:1,3` vs 7-D 那一列 |
| 「約 7 成有值」像一個量測結果 | **是一段滑鼠提示文案**，程式裡沒有任何一行在數它 | `ui/helpers/fund/checkup.py:613` |
| `v2_holdings` / `v2_transactions` 像現有的資料表 | **這兩個名字在整個程式庫 0 命中**。現行的是另一份 **10 欄**的結構：`policy_id, fund_code, fund_name, currency, tier, invest_twd, div_cash_pct, units, avg_nav, avg_fx` | `repositories/policy/v2.py:506-517`（**正對照**：同一次掃描對 `ALL_COLS_V2` 有命中，證明掃描有效） |

⚠️ **最後一列有一個與問題八直接相關的細節**：您自己填的那個級別欄，
**在這份結構裡叫 `tier`，不叫 `policy_tier`**（`repositories/policy/v2.py:511`；
中文欄名「級別」的對照在同檔 `:130` 與 `:532`）。**問題八第 (3) 件要寫死保護的就是這一欄。**

---

# 八、待您裁示清單

> ⛔ **這一節只放「只有您能決定」的事。**
> 每一項都寫清楚**為什麼只有您能決定** —— 如果一件事本組判斷得出來，它就不該出現在這裡。
> **本組能給推薦的就給推薦；不能給的，明講為什麼不給。**

---

## ⛔ 裁示 1｜問題五的甲、乙、丙 —— **您自己的兩條指示互相牴觸**

**牴觸的兩條，都是您說的**：
- **(i)** 問題五的決定：「該買該賣」的句子要改掉 —— 其中兩處住在**說明書**。
- **(ii)** 您 2026-09-14 另一道裁示：**16 個舊分頁不改內容**，只在入口加一句總體免責。
  而**說明書正是那 16 個之一**（`ui/tab6_manual.py`）。

**為什麼繞不過去（本組實測）**：不能用「反正是舊分頁、新版看不到」搪塞 ——
說明書的內容**在正式 ⑤ 上是無條件印出來的**，連勾選框都沒有（`ui/tab_settings_diag.py:379,383-384`）。
⇒ **那兩句話今天就印在您眼前。**

| | 怎麼做 | 代價 |
|---|---|---|
| **甲** | 以問題五為準 —— 改說明書的內容 | 要把說明書**單獨**從「舊分頁不改內容」名單裡放出來 |
| **乙** | 以 9/14 那道裁示為準 —— 一個字不改 | 「建議再平衡／必須執行」**會繼續無條件印在正式 ⑤ 上**，只是旁邊多一句免責 |
| **丙** | 兩者都守 —— ⑤ 不再叫出說明書 | 使用者**查不到說明書**（「偏離多少算多」無處可查）|

⛔ **本組不給推薦，理由請看清楚**：
甲乙丙的差別**不在技術，在於您要哪一邊讓步** —— **兩邊都是您自己的指示**，
本組沒有立場替您排先後。**本組能做的就是把代價量出來擺在上面。**

⚠️ **在您裁示之前，說明書那兩處本組不動。**
⚠️ **那道 9/14 裁示的原始記錄，本組在程式庫裡找不到 → 未複驗。若您記得的不是這樣，以您為準。**

---

## ⛔ 裁示 2｜問題九 —— **請在正確的數字上重新點一次頭**

**為什麼只有您能決定**：這是您已經拍過板的決定，**而您是在抄錯的資訊上拍的**。
依規矩，**本組不會自己把您拍過的板改掉**。

**您當時看到的 vs 真正印出來的（本組逐條複驗，出處見 5-B 問題九）**：

| 您當時看到的 | 真正印出來的 |
|---|---|
| `股票 55％ ・債券 35％ ・現金 10％` | **這一組不存在**。只有五組：`85/10/5`、`70/20/10`、**`55/30/15`**、`40/35/25`、`25/40/35` |
| `停利 Z ≥ +1.00` | **`+1.75`**（而 `+1.00` 在任何景氣燈號下都不可能出現） |
| `這兩個門檻已依台灣景氣黃燈調整。` | **`這兩個門檻是固定的預設值，不會跟著台灣景氣燈號變動。`**（**意思整個相反**） |

**本組推薦：仍然選 C（上半拿掉、下半留下並改詞）** ——
理由是**切法與數字無關**（上半是推薦、下半是事實）。
**但要寫進畫面的字跟著變了**：是「**偏貴線 Z ≥ +1.75、偏便宜線 Z ≤ -1.00**」，不是 `+1.00`；
而且那句「不會跟著景氣燈號變動」**要照原句留著，不要留一句反過來的**。

⚠️ **這張卡在正式的 ① 分頁上，不是預覽格** —— 改或不改，效果都是立刻的。

---

## ⛔ 裁示 3｜SPY / QQQ 的基準爭議 —— ⚠️ **本組要先更正總管對這一題的描述**

**三份文件的原文（本組逐字實測，三處都親自打開看過）**：

| 出處 | 逐字內容 |
|---|---|
| `SPEC.md:1640` | 「**基準線禁用 ETF**：**資產曲線**對比基準必須使用「無風險利率年化複利」，**不得引入 0050.TW / SPY / QQQ 等 ETF**（§0 全面排除）」 |
| `docs/wireframes/ia-wireframe.html:470`（**2026-09-01 您親自拍板的線框**） | 「連兩季落後對比基準（**SPY / QQQ**）。」 |
| `ui/components/mk_dashboard.py:696-700`（程式已照線框做） | 一個下拉選單，標題「**衛星對比基準**」，選項 `["SPY", "QQQ"]`，說明文字「SPY=S&P 500、QQQ=Nasdaq 100。連續兩季落後此基準會在表格顯示黃燈。」 |

### ⚠️ 本組的更正：**這兩者可能根本不是同一個東西，所以「矛盾」還沒有成立**

總管交辦時的描述是「兩份文件互相牴觸，推薦改 `SPEC.md:1640`」。
**本組回去讀完三處原文之後，認為這個描述講得太快了**：

- **`SPEC.md:1640` 管的是「資產曲線」的對比基準** —— 那是**整個組合**的那一條績效曲線，
  它要求拿「無風險利率」當比較對象。**這條規則有配套的東西真的存在**：
  `services/fund_service.py:34,39`（無風險利率的注入口）、
  `services/allocation_backtest.py:277`（回測吃 `rf_annual`）。
- **`mk_dashboard` 用 SPY/QQQ 的地方，是「單一檔衛星基金跟不跟得上」的判定** ——
  它產出的是一個逐檔的黃燈旗標，不是一條組合資產曲線
  （`ui/components/mk_dashboard.py:8`、`:174-175`、`:260`、`:281`：`Benchmark_Lag` 三態 `Lag / OK / N/A`）。

⇒ **兩件事的對象不同：一個是「整個組合的績效曲線」，一個是「這一檔衛星有沒有落後」。**
**如果它們是兩個不同的東西，那就沒有矛盾，什麼都不用改。**

### 所以要請您裁示的，其實是一個**範圍問題**

**`SPEC.md:1640` 那句「資產曲線對比基準」，涵不涵蓋「單檔基金跟基準的對比圖」？**

| | 讀法 | 後果 |
|---|---|---|
| **甲** | **只管整個組合的資產曲線** | **沒有矛盾，三份文件都不用動。** 這是本組認為比較站得住的讀法 |
| **乙** | **任何拿 ETF 當比較對象的地方都算** | 那就真的牴觸了，要改 `SPEC.md:1640`，承認「ETF 可以當量尺、不可以當標的」 |

**⭐ 本組推薦：先按甲處理（不動任何東西），因為「資產曲線」四個字在原文裡是具體的、有配套實作的。**
⛔ **但這是本組的推薦，不是已定案**，而且**本組沒有窮舉全站還有哪些地方拿 ETF 當比較對象 → 未複驗。**

⚠️ **為什麼仍然要送到您面前**：因為 `ia-wireframe.html:470` 那一份是**您 2026-09-01 親自拍板的**。
**動它要您點頭；不動它、改 SPEC 也要您點頭。** 本組不會自己決定哪一份文件說了算。

---

## ⛔ 裁示 4｜總經 → 基金 → 組合的傳導，要哪一種？ **兩種是不同的東西**

**您舉過的例子是**：「這檔債券基金久期 5.2 年，升息 1 碼大概會掉 X%」。

**本組實測的結果是：那個例子做不到，因為它需要久期，而久期抓不到。**
**出處**：把 `duration` / `久期` / `信評` / `YTM` 一起掃過整個資料層，
**唯一命中是一份新聞關鍵字清單**（`repositories/news_repository.py:410`），**不是基金欄位**（見 7-D）。

| | 哪一種 | 做得到嗎 | 它會回答什麼 | 它答不了什麼 |
|---|---|---|---|---|
| **甲** | **久期版**（您舉的那個例子） | ❌ **做不到** | 「升息 1 碼 → 這檔大概掉 X%」 | — |
| **乙** | **Beta 版**（不用久期） | ✅ **做得到** | 「這檔基金過去對某個總經變數的連動大概有多強」 | **它不會給您「升息一碼掉幾趴」這種數字** |

**為什麼只有您能決定**：這**不是技術選型，是「您要的那個數字是什麼」** ——
甲和乙回答的是**兩個不同的問題**，不是同一件事的兩種做法。
**只有您知道您真正想看到的是哪一個。**

⚠️ **如果您要的是甲**，那要先解一個資料問題（久期從哪來），**那是一件獨立的事，不是這次改版的一部分**。
⚠️ **「久期抓不到」是本組單組實測，取決於有沒有漏看** —— 若它以別的名字存在，本組這一掃看不到。

---

## ⛔ 裁示 5｜⭐ **本組新發現：問題六與問題八，也撞上同一道「舊分頁不改內容」**

**這一項總管的清單上沒有，是本組這一輪查出來的。**

| 題 | 要改的東西住在哪 | 出處 |
|---|---|---|
| **問題六** | **正式 ④ 上有第二句「往哪邊搬」的話，而它在舊分頁裡** —— 逐字「核心過重，**可贖回轉衛星**」／「衛星過重，**可獲利轉核心**」 | `ui/tab3_portfolio.py:1404-1406` |
| **問題八** | **三個寫入點全部在舊分頁裡** | `ui/tab3_portfolio.py:1583`、`ui/tab3_t7_ledger.py:1053`、`ui/helpers/portfolio/policy_admin_section.py:385` → `ui/helpers/cloud_io.py:251-254` |
| **問題四** | **「緊鄰換股顧問」要求搬進舊 ④**（換股顧問被釘死只能在那裡畫） | `ui/views/page_04_portfolio.py:919-921` |

### ⭐ 本組推薦：**這三題不要用同一把尺，理由如下**

**本組認為「改內容」和「修錯誤」是兩件事，而這三題分屬不同的一邊**：

- **問題六 → 和問題五同一種。** 它改的是**使用者看到的字**，那就是「內容」。
  ⇒ **建議併進裁示 1 一起回答**（甲／乙／丙怎麼答，這一句就怎麼處理）。
- **問題八 → 本組認為不同種。** 它修的是**一個把您填的值蓋掉的錯**，
  使用者看到的字不會變、版面不會動。**把「不改內容」讀成「連錯都不准修」，
  會讓一個正在吃掉您資料的錯繼續吃下去。**
  ⇒ **本組推薦：問題八照辦，不受那道裁示拘束。**
  ⛔ **但這是本組的推薦，不是已定案** —— 因為那道裁示是您下的，**它的射程只有您能定**。
- **問題四 → 是一個「搬家」，兩邊都說得通。**
  ⇒ **本組推薦：等裁示 1 有結果再處理**，因為它的落點取決於舊 ④ 能不能動。

⚠️ **本組沒有窮舉「還有沒有第四題也撞上」 → 未複驗。**
⚠️ **那道 9/14 裁示的原始記錄本組找不到**，所以上面所有關於它射程的討論，
**都建立在總管的轉述上**（同裁示 1 的免責）。

---

## 📋 這五項裁示的相依關係（一張圖看完，免得您答了 A 才發現 B 卡著）

```
裁示 1（甲／乙／丙）
   ├─→ 決定了「說明書那兩句能不能動」      ← 問題五
   ├─→ 決定了「正式 ④ 那一句能不能動」      ← 問題六（裁示 5）
   └─→ 決定了「選股池能不能搬進舊 ④」      ← 問題四（裁示 5）

裁示 5（問題八算不算「改內容」）
   └─→ 決定了「那三顆鈕還要不要繼續蓋掉您填的值」  ← 問題八・急迫

裁示 2（問題九重確認）      ── 獨立，不卡別人，可以單獨回
裁示 3（SPY／QQQ 範圍）     ── 獨立，不卡別人，可以單獨回
裁示 4（傳導要哪一種）      ── 獨立，但它決定的是「要不要做這件事」，範圍最大
```

⭐ **若您時間有限，本組建議的回答順序是：裁示 5 → 裁示 1 → 裁示 2 → 裁示 3 → 裁示 4。**
**理由**：裁示 5 的問題八**現在每按一次那三顆鈕就在發生**，其他都可以等；
裁示 1 卡著三題，解開它才動得了最多東西。

---

**第五～八節到此結束。**

⚠️ **最後一句，照規矩講清楚**：
**這四節由本組單獨產出，沒有第二組複驗過。**
本組這一輪推翻了上一版草稿的**三處位置敘述**與**一處機制敘述**，也更正了**總管交辦材料裡的一項描述**
（裁示 3）—— **這個比例本身就是一個訊號：請不要假設沒被本組點到的地方都是對的。**
