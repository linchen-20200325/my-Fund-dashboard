# 規格稽核 R5 — 客戶裁示 1~5 落實的獨立驗證

**性質**：純唯讀稽核。**未改任何檔案、未 commit、未開 PR、未建分支、未 merge。** PR #845 未動。
**基準**：`origin/main` ＝ `9cbf03776f2a0ee6bdb5a649efb93352c40baba6`（工作樹 HEAD 為 `d0c2a8d`，**一律未用**）。
**取檔方式**：`git show origin/main:<path>`；五個 page 檔另抽出到本組自己的目錄後以 `git hash-object` 比對 blob，**5/5 MATCH**，確認與 `origin/main` 逐位元相同。
**本組不裁決。** 矛盾一律 A/B 並陳。

---

## 方法

1. **AST，不是字面 grep**：五種掃描全部自行以 `ast.walk` 重寫，不沿用生產組的腳本。
2. **每一批掃描同批跑正控＋負控**。負控為本輪現編隨機串，**依規定不寫進本文件**。
   - 正控（掃描 1~5）：在 `page_03_research.py` 副本尾端注入 1 個 `safe_section` / 1 個 `render_cards` /
     1 個 `card_row` / 1 個 `st.tabs` / 1 個 `### ` 標題 / 1 個 `##### ` 標題 →
     前四種由 7 升為 **11**（+4，全中）、H2~H4 由 0 升為 **1**（H5 **正確未計入**）。
   - 正控（母法字表）：`抄底` 在 `risk.py` → **2**；`推薦` 在 `rotation.py` → **1**。兩者皆非 0，掃描是活的。
   - 負控：隨機串在各目標檔 → 全 **0**。
3. **行號一律 `git show origin/main:<path> | awk 'NR==N'` 重印原文再比對**，未經 `grep -v` 數序號。
4. **子字串陷阱已就地處理**：`承接`（「由所屬區塊承接」＝結構詞，非買進動作）、
   `建議`（「建議 10」＝稽核項次編號；「📐 建議資產水位」＝卡片自己的標題）逐處人工判讀。
5. **盤點表解析的兩個陷阱都處理了**：`(?<!\\)\|` 切欄（救回 §2.8 那 9 列）、
   `查不到（production）` 變體正規化（救回 §2.17 那 4 列）。

### 射程外（誠實揭露）

- 本組**只驗**十件事所列的範圍。規格四件內容的**業務正確性**不在射程內。
- 「五種掃描是否窮舉」本組**不宣稱** —— 動態組出的渲染物 AST 看不到（規格自己已在 §1.6 末段聲明）。
- 「本稽核沒有第四條矛盾」本組**不宣稱**。

---

## 十件事逐項結論

### (1) M5 → ④-8 ✅ 補成區塊、四件齊、標籤「沿用」站得住、四格都到得了畫面；`t3_last_load_at` 0 命中確已就地標示

- ④-8 在 `UI_SPEC.md:554-558`，**四件齊**（`- 來源`／`- 規則`／`- 空狀態`／`- 回答` 各恰好一次，由本組 parser 驗）。
- **四格都到得了畫面**（實測）：`_status_tiles()` 的 `return` 在 `:1903-2032`（本組量測起訖**完全吻合**），
  四個 dict 的 label 依序為 `STATUS_BOOK_LABEL`／`STATUS_POLICY_LABEL`／`STATUS_LOADED_AT_LABEL`／`STATUS_INVESTED_LABEL`；
  呼叫端 `:2176-2191` 以 `zip(_cells, _tiles)`、`cols=STATUS_COLS`（`:665` 實測 `STATUS_COLS: int = 4`）逐格渲染。
- **`t3_last_load_at` 確實就地標了**：`UI_SPEC.md:555` 逐字「**盤點表 0 命中**，依 §1 的暫行判法就地標示」。
  本組實測權威盤點表 `grep -c 't3_last_load_at'` → **0**。✅
- 其餘引用逐條實測相符：`:649-653` 四個標籤常數、`:673 UNKNOWN_VALUE = "—"`、`:1879-1881`、`:1882-1883`、
  `:1891-1892 invested_hint`、`:2030-2031`、`:2170-2173`、`:2179-2186`、`:2190-2191`、`:2197-2200`、`:2762-2763`、`:2781`。
- ⚠️ **一處矛盾見 M1**（tile ① 的來源漏了 `_t3_cur_sheet_title`）。

### (2) 交叉掃描 ✅ 五種全部獨立重跑，逐檔數字**完全吻合**；「0 個對不回去」**成立**

本組自寫 AST scanner，結果與生產組宣稱**逐格相同**：

| 檔 | 1 `safe_section` | 2 `render_cards` | 3 `card_row`／`card_grid` | 4 `st.tabs` | 5 H2~H4 |
|---|---|---|---|---|---|
| `page_01_macro.py` | 1 | 8 | 0 | 0 | 8 |
| `page_02_health.py` | 13 | 1 | 0 | 0 | 5 |
| `page_03_research.py` | 5 | 1 | 1 | 0 | 4 |
| `page_04_portfolio.py` | 10 | 0 | 2 | 0 | 9 |
| `page_05_settings.py` | 7 | 1 | 0 | 0 | 8 |

- 前四種合計 **50**（9＋14＋7＋12＋8）✅；第 5 種 **34**（8＋5＋4＋9＋8）✅；**共 84** ✅。
- **逐一對回**：本組把 §1.6 `:145-158` 的落點清單與自己掃出的 84 個行號做集合比對 ——
  ①9＋8、②14＋5、③7＋4、④12＋9、⑤8＋8，**每一個命中都在清單裡，清單裡也沒有多出來的行號**。
  → **「83 個對得回、1 個對不回（＝`page_04_portfolio.py:2176`）、補進 ④-8 後 0 個」獨立成立。** ✅
- **`safe_section` 自己不畫標題的理由：實測成立。** `ui/helpers/render_state.py:186-213` 全函式為
  `try: fn(...) / except: system_error(f"「{label}」區塊渲染失敗", _sec_e)` ——
  `label` **只**出現在 except 分支，正常渲染時完全不產生標題。故掃描 1 與掃描 5 確實量兩件事，第 5 種不是湊數。✅
- **§1.6 的 grep 灌水實證也成立**：`grep -nE 'with card_row\(|card_grid\('` 在 `page_03_research.py` → **4 行**
  （`:238`／`:533`／`:1075`／`:1078`），AST 真呼叫 **1**；`page_05_settings.py` → **1 行**（`:239`），真呼叫 **0**。
  規格點名的三個非呼叫行號與本組實測**完全一致**。✅
- **規則本身寫明了硬性**：`UI_SPEC.md:97` 標題逐字「**交付前必跑，任一命中對不回區塊或排除清單即不得交付**」，
  `:99-101` 再寫「規則（硬性，不是建議）」「**對不回去的就是漏掉的區塊，補進規格；一個都不准留著**」。✅

### (3) F-13 ✅ 全部成立

- `UI_SPEC.md:194` 標題帶 **⛔【母法衝突・待修】**。✅
- **五列引文逐行重印比對**：`:809-810`／`:811`／`:801`／`:804-806`／`:785` —— **內容全部相符**
  （`:811`、`:801`、`:785`、`:806` 為**逐字**；`:809-810` 是把原文兩段隱式串接合併成一式，內容等值；
  `:804` 有一處縮寫，見「觀察 O2」）。
- **「免責一句都沒有」屬實，且本組用比規格更寬的字表重驗**：以 19 個詞
  （`非投資建議|投資建議|不構成|僅供|僅為|參考|教學|免責|風險自負|自行判斷|不保證|示意|試算|非建議|請自行|盈虧|自負|非推薦|不代表`）
  掃 `_card_allocation` 全函式 `:767-814` → **唯一命中在 `:800`，是原始碼註解**，而規格已就地寫明
  「`:794-800` 那幾行是**原始碼註解**，不在畫面上」。✅ **畫面上確實一句免責都沒有。**
- **`DECISION_LOG` §H 列 P0 第一批第 3 項**：`DECISION_LOG.md:253` 該列第一格逐字為 `| **3** |`。✅
- **豁免射程寫死**：B21（`:50`）、B21-a（`:51`）、§H（`:253`／`:258`）四處皆寫
  「`ui/views/page_01_macro.py` **僅限 ①-4 那一處**，其餘維持 F2 凍結、**不得外推**」。✅
- **通道差別三條全對**（對照母法原文實測）：
  - ②-12 ＝ `ui/helpers/fund_grp_health/backtest_section.py`，而 F1~F6 逐列為
    F1 `ui/tab*.py`／F2 `ui/views/page_0*.py`／F3 `app.py`／F4 `services/**`／F5 Schema／F6 遷移鏈 ——
    **`ui/helpers/**` 不在其中**（母法 `:72-77`，且 `:1860` 另列「不在 F1~F6 的消費者」欄）。✅
  - ①-4 ＝ `ui/views/page_01_macro.py` → **F2**（母法 `:73` 逐字「五個線上頁 `ui/views/page_0*.py`」）。✅
  - 門檻 ＝ `services/allocation_ladder.py:57`（實測該行正是 `def allocation_from_composite(`）→ **F4**（母法 `:75`）。✅
  - 附帶實測：`shared/signal_thresholds.py:409-410` 確為 `ZSCORE_STOP_GAIN_DEFAULT: float = 1.75` / `ZSCORE_ADD_DEFAULT: float = -1.0`。✅
- ⚠️ **母法檔本身的所在地見 M3。**

### (4) F-14 ✅ 兩項都有同型問題 —— 本組獨立重查，**與生產組同一結論**

**②-11：成立。** `risk.py:203-204` 逐字重印：

```
203	            f"💡 深度超跌:若基本面(評分/吃本金/Sharpe)仍健康 → 可考慮抄底;"
204	            f"基本面也轉差 → 不要接刀</span>"
```

- **真的到得了畫面、且真的每張指名卡都印**（實測）：`_render_oversold_badges` ＝ `:152-207`；
  `:192 for _o in _oversold:` 是**逐檔迴圈**；`:193 st.markdown(` 起、`:206 unsafe_allow_html=True`、`:207 )` ——
  `:203-204` 是這**同一個** `st.markdown` f-string 的最後一個 span，`:196` 是同卡的指名標題。
  生產路徑：`page_02_health.py:1759 safe_section("−2σ 超跌警示", lambda: _render_oversold_badges(_funds))`。✅
- **總管那次 0 命中的原因確認**：`抄底`／`接刀` **不在**舊字表 `加碼|減碼|買進|賣出|停利|停損|換出|逢低|建議` 內。
  本組以 33 詞加寬字表重掃 `risk.py` → 命中 `:156`（docstring）／`:203`／`:204`（畫面字串），與規格 `:337` 記載一致。

**②-13：成立，四跳鏈逐行重印全部相符。**

| 跳 | 實測原文 |
|---|---|
| `ai.py:248` | `                "換手與調整建議",` —— 確為 `sections`（`:243-249`）的**第 5 項** |
| `ai_prompts.py:137` | `請用繁體中文，**依照下面的章節順序逐節輸出**，每一節都用 \`### \` 開頭當標題，` |
| `ai_prompts.py:143-144` | `需要逐節輸出的章節：` ＋ `{_sec_block}`（`:112-113` 確為 `enumerate` 逐項編號展開） |
| `ai_prompts.py:148` | `### ✅ 一句話總結 & 下一步` —— 在 `最後再加一節：` 之後，**無條件附加** |
| `ai_prompts.py:150` | `- 給 1-2 個很具體、新手也做得到的下一步（例：這個月繼續定期定額、先別急著加碼、某檔可以考慮換掉）。` |
| `ai_summary.py:69` | `st.caption("把這個 Tab 的所有資料交給 AI，逐段用白話講「現在是好是壞、跟最近新聞有沒有關係、下一步怎麼做」。")` |
| `ai_summary.py:138` | `        st.markdown(_cached)` —— 模型輸出原樣印 |

⚠️ 派工單把該檔寫成 `ui/helpers/fund_grp_health/ai_summary.py`，**該路徑在 `origin/main` 不存在**；
**規格寫的 `ui/helpers/ai_summary.py` 才是對的**（本組實測）。規格無誤。

- **免責 0 命中：本組用 19 詞加寬字表重掃 `risk.py`／`ai.py`／`ai_summary.py` → 三檔仍然全部 0 命中。** ✅
  （規格只用了 4 個詞；加寬後結論不變，**反而更強**。）
- **六個逐字禁用詞**（`建議買進|立即出清|應該加碼|推薦|必漲|目標價`，母法 `:588` 實測）掃
  `ai.py`／`ai_summary.py`／`ai_prompts.py`／`risk.py` → **四檔皆 0**。✅ 正控同批非 0，負控同批 0。
- **「逐檔簡表最後一欄是動作導向」的更正：成立。** 實測 `ai.py:29` docstring 確有
  `- 逐檔簡表(代號 / 名稱 / 配息率 / 覆蓋率燈號 / σ rank / 操作建議)`，
  但真正組表的 `:154-167` `_bits` 只有 名稱(代號)／配息／1Y 含息／覆蓋率／`div_status`／σ／Sharpe，
  `_per_fund` 的 dict（`:132-138`）鍵為 `code/name/div_pct/ret1y/div_status/coverage/sigma_label/sigma_rank/sharpe`
  —— **沒有任何「操作建議」鍵**。→ `:29` 確為過期 docstring。✅
- **F4→F2 連鎖：成立。** `git grep build_structured_summary_prompt origin/main -- '*.py'` 實測，
  production import 端**恰好兩個**：`ui/helpers/ai_summary.py:37`（②-13）與 `ui/views/page_01_macro.py:146`（①-11，F2），
  使用點 `:1873`。→ 動 `ai_prompts.py:148-150`（F4）**會同時改到 ①-11**，「要兩格通道」成立。✅

### (5) ②-12 引文 ✅ 六處逐處比對，**全部改對了**

| # | 位置 | 本組重印原文 | 判定 |
|---|---|---|---|
| 1 | `:168` | `st.markdown(f"#### 📋 勝出策略「{STRATEGY_LABELS[_res['winner']]}」建議")` | ✅ 已補回原式，無省略號 |
| 2 | `:191` | `help="勝出策略在**最後一次再平衡日**判定要減碼/換出的檔。"` | ✅ 原文**確有** `最後一次再平衡日` 的粗體、**確無** `減碼/換出` 的粗體；規格已拿掉 |
| 3 | `:195` | `help="配對的進場標的;無合適者誠實留「無合適標的」。"` | ✅ 半形 `;`，相符 |
| 4 | `:208` | `help="勝出策略在最後一次再平衡日的目標配置(總和 100%);**教學示意,非投資建議**。"` | ✅ 整句、半形 `,`，相符 |
| 5 | `:138-139` | `st.success(f"🏆 效益最高:**{…}**" "(依 Sharpe→Calmar→CAGR 排名;in-sample,勿當未來保證)")` | ✅ 整句、半形 `,`，相符 |
| 6 | `:176` | `st.caption("本策略不擇時(等權/靜態最佳)→ 無買賣切換動作;下方為目標配置權重。"` | ✅ 半形括號／半形 `;`／無粗體，相符 |

另**同批順帶驗過且無問題**的既有列：`:172-174`、`:181`／`:183`、`:186`、`:200-202`、`:104`、`:177-178` —— 全部逐字相符。
**`:202` 在 `if/else` 之外**（實測縮排：`:170` `if` / `:175` `else` 區塊在 `:178` 結束，`:180` 起為同層新 `if`）→
規格「兩條分支都會印」的敘述成立。✅

### (6) 計數 ✅ 本組自寫 parser 獨立重算，**五個數字全中**

| 項目 | 生產組宣稱 | 本組獨立實測 | |
|---|---|---|---|
| 區塊 | 42 | **42** | ✅ |
| 逐頁 | ①10 ②12 ③4 ④8 ⑤8 | **①10 ②12 ③4 ④8 ⑤8** | ✅ |
| 標籤 | 沿用 27／部分 4／新建 10／不標 9／查不到 0 ＝ 50 | **27／4／10／9／0 ＝ 50** | ✅ |
| 四件 | 168，缺漏 0、重複 0 | **168**，**不是 `[1,1,1,1]` 的區塊：0 個** | ✅ |
| `grep -c '^### '` | 50 | **50** | ✅ |

- **多標籤的六塊也對得起來**：①-1(2)／①-4(3)／②-1(3)／②-13(2)／④-2(2)／④-5(2) → **多出 8**，
  50 ＝ 42 ＋ 8，內部自洽。✅
- **`48→50` 的 +2 不是漏帳**：規格 `:36` 自己就寫明「＋1 ＝ `### ④-8` 本身、＋1 ＝ 排除清單新增的 `### 己、…` 小標」。
  本組驗算：42 活躍 ＋ 6 個排除清單小標（甲乙丙丁戊己）＋ 2 個退役標題（~~①-5~~／~~②-2~~）＝ **50**。✅
  區塊數差額**確實只有 ④-8 一筆**。
- 本組 parser **獨立於**生產組那一支（本組另行排除刪除線標題、另行檢查四件前綴各恰一次），結論相同。

### (7) F-15 ✅ 歧義**真的存在**（本組不選邊）

§1.5 第 1 條逐字（`UI_SPEC.md:72`）：

> 1. **一個區塊 ＝ 一個 H2~H4 標題，或一次頂層 `safe_section()` 呼叫，或一次卡片網格呼叫**（`render_cards([...])`／`card_row()`）。

- **「頂層」這個限定詞只掛在 `safe_section` 那一句上** —— 前一句（H2~H4 標題）與後一句（卡片網格呼叫）都沒有。**屬實。**
- **兩個讀法在 `:2176` 這一格真的都成立**（本組 AST 實測）：
  `:2176` 住在 `def _render_policy`（`:2065-2227`）內，該函式由 `:2781 safe_section(BLOCK_POLICY, _render_policy)` 帶進來，
  頭上是 `:2780 st.markdown(f"#### {BLOCK_POLICY}")`；`:2100-2300` 之間**沒有**任何自己的 `safe_section`。
  → 第三句指向「它是一塊」，前兩句指向 ④-3。**§1.5 未寫三句衝突時以哪一句為準。**
- 已登記為 `UI_SPEC.md` 〈待確認〉**11**、`DECISION_LOG` **F-15**（`:181`）與 **B24**（`:55`），
  兩種讀法並陳、採 A、並寫明改判 B 的後果（區塊回 41、標籤回 49、四件內容不受影響）。
  **生產組沒有自行拍板。** ✅ **本組亦不裁決。**

### (8) 裁示 4／5 ✅ 全部成立

**裁示 4（§2.15 標題 12 → 11）**
- 標題已改：`10_db_inventory.md:1325` 逐字 `（~~12~~ → **11** 個 key：…）`，舊值加刪除線保留。✅
- **三條出處本組逐條獨立重跑**：
  1. `git show origin/main:config/macro_weights_active.json` → `json.load` → **11** 個頂層 key，
     逐字為 `version/calibrated_at/calibration_method/horizon_months/drawdown_threshold/indicators/verdict_cutoffs/phase_thresholds/oos_metrics/ai_explanation/notes` —— **與規格列出的名稱完全一致**。✅
  2. 標題那串以 `/` 切開計數 ＝ **11**。✅
  3. §5.1（`:1766`，確在 §5.1 標題 `:1747` 與 §5.2 標題 `:1770` 之間）逐字「§2.15 那一列 ＝ 11 個 key」。✅
  - 對照組：`services/config/macro_weights_active.json`（§2.16 那一份）頂層只有 **2** 個 key，兩份確實不同檔。
- ⚠️ **一處矛盾見 M2。**

**裁示 5（收斂成一份 ＋ 路徑統一 ＋ 舊的標已過期）**
- **權威路徑宣告在**：`10_db_inventory.md:8-12`，逐字「**本檔的權威路徑 ＝ `docs/v2/10_db_inventory.md`。**」
  並附客戶原話與「為什麼會有兩份」（派工單漏寫路徑）。✅
- **舊檔只在檔頭加標記：BYTE-IDENTICAL 實測通過。** 舊檔 `scratchpad/inv/DB_INVENTORY.md` 檔頭 `:1-17` 為
  「⛔ 本檔已過期（2026-09-16）」區塊；`tail -n +18` 與 before 快照 `/home/user/fund-doc-inv/base/docs/v2/10_db_inventory.md`
  **`cmp` 通過、`md5sum` 相同（`c4d77df48166d36fb8392d7cea3edf5f`）**。行數差 2171−2154 ＝ **17**，與檔頭長度吻合。✅
  正控（自己比自己）identical、負控（比權威版）differs，兩控皆如預期。
- **六個狀態數與逐節數一格未動 —— 本組自寫 trap-safe parser 獨立重算全中**：

| | 宣稱 | 本組實測 |
|---|---|---|
| 活的／未落地／只寫不讀／只讀不寫／查不到／已停用 | 111／26／112／9／4／2 ＝ 264 | **111／26／112／9／4／2 ＝ 264**，未分類 0 列 ✅ |
| 逐節 | §1 89／§2 82／§3 60／§4 33 ＝ 264 | **89／82／60／33 ＝ 264** ✅ |
| 誰讀它 | UI 113／內部 63／無 55 ＝ 231 | **113／63／55 ＝ 231**，未分類 0 列 ✅ |
| 不變量（狀態∉(只寫不讀,查不到,已停用) 且 誰讀它∈(內部,無)） | 0 列 | **0 列** ✅ |

  - **兩個陷阱都實測到並處理**：全檔含跳脫豎線的行 **34** 行，`(?<!\\)\|` 切欄後 §2.8 那 **9** 列全部救回；
    `查不到（production）` 變體正規化後 §2.17 那 4 列不再漏算。
  - ⚠️ 本組第一版 parser 因把含「誰讀它」字樣的**內文列**誤判為表頭，曾算出 227/231；
    改為「只有第一格是『表 . 欄位』才算表頭」後得 231。**這是本組的 parser bug，不是文件的問題**，就地記錄以免誤導。

### (9) 母法禁令 ✅ 三檔獨立重掃，**0 處以規格口吻給出處置建議**

- **六個逐字禁用詞**：`UI_SPEC.md` 4 行、`DECISION_LOG.md` 5 行、`UI_PAGE_TODAY.md` 2 行。
  逐行以 `grep -o` 取出**實際命中的詞**後人工判讀：除了三檔各有一行**列舉那六個詞本身**（禁用詞清單／掃描字表），
  其餘命中**全部是 `推薦`，且全部在「最佳配置推薦」這個母法禁令片語裡被引用**。**無一處是規格自己的口吻。** ✅
- **加寬字表（33 詞）逐行語意閱讀**：`UI_SPEC.md` 21 行、`DECISION_LOG.md` 9 行、`UI_PAGE_TODAY.md` 5 行，逐行判讀：
  - **引文**（在四張現況引文表或以「」／反引號包住）：`UI_SPEC.md` `:215`／`:347`／`:348`／`:381`／`:383`／`:384`／`:385`／`:441`／`:399`／`:390`
  - **母法引用**：`:206`
  - **掃描方法描述**：`:336`／`:337`／`:718`、`DECISION_LOG.md` `:411`／`:412`
  - **規格自己的禁令**：`UI_SPEC.md:503`「⛔ **不出買賣動作、無一鍵再平衡鈕**」、
    `UI_PAGE_TODAY.md` `:10`／`:11`／`:12`／`:79`（`:14` 且明標「⛔ 反例」）
  - **區塊名稱**：`:507 ### ④-2 再平衡試算`
  - **子字串誤擊**：`:231`／`:280`／`:531`／`:625`／`:644` 命中的是「**承接**」，
    語境為「由所屬區塊**承接**」（結構詞），**不是買進動作**。
  → **沒有任何一處是規格在建議使用者做什麼。**
- **現況引文的形式要求：四張表全部合格。** `UI_SPEC.md` `:211`（①-4）／`:344`（②-11）／`:378`（②-12）／`:435`（②-13）
  四張表的表頭**逐字相同**：`| 位置 | 現況引文・待修（原樣引用，**不是本規格的建議**） | 命中 |`，
  且四張表都掛在標題帶 **⛔【母法衝突・待修】** 的區塊底下，表內每一格引文都加了反引號或「」。✅
- ⚠️ **一項射程外的觀察見 O4。**

### (10) 尺寸 ✅ 四檔全部與宣稱相符

| 檔 | `wc -l` | `LC_ALL=C.UTF-8 wc -m` | python `len()` | `wc -c` | 宣稱 |
|---|---|---|---|---|---|
| `spec/UI_SPEC.md` | **728** | **61,838** | 61,838 | 117,324 | 728 行／61,838 字元 ✅ |
| `spec/DECISION_LOG.md` | **439** | **45,848** | 45,848 | 84,105 | 439 行／45,848 字元 ✅ |
| `docs/v2/10_db_inventory.md` | **2,626** | 179,150 | 179,150 | **287,544** | 2,626 行／287,544 bytes ✅ |
| `spec/UI_PAGE_TODAY.md` | **149** | 8,455 | 8,455 | 16,359 | 149 行 ✅ |

⚠️ **本機 `LANG` 與 `LC_ALL` 皆為空（`LC_CTYPE="POSIX"`）**，實測 bare `wc -m` **確實退化成位元組數**
（四檔的 bare `wc -m` 與 `wc -c` 逐檔相同）。上表已一律加 `LC_ALL=C.UTF-8`，並與 python `len()` **四檔全部吻合**。
（舊檔 `scratchpad/inv/DB_INVENTORY.md` ＝ 2,171 行／232,909 bytes，供對照。）

---

## 矛盾清單（A／B 並陳，**本組不裁決**）

### M1｜④-8 tile ① 的來源只寫了一個 session 鍵，而被略去的那個與 tile ③ 同樣是「盤點表 0 命中」

- **A 說**：`spec/UI_SPEC.md:555` ——「①**📒 目前帳本** `_book_title()` 讀 session `policy_sheet_id`（**盤點表有列**，⛔ **值一律不列出**）」。
- **B 說**：`ui/views/page_04_portfolio.py:1697-1700` —— `_book_title()` **先**讀 `_t3_cur_sheet_title`，
  有值就直接 `return`，**讀不到才**退到 `policy_sheet_id`；同一支函式的 docstring `:1873` 自己寫的來源是
  「`_t3_cur_sheet_title` → `policy_sheet_id`」（兩個鍵）。
  本組實測：`_t3_cur_sheet_title` 在權威盤點表 `grep -c` → **0**（與 `t3_last_load_at` 同級），
  而 `UI_SPEC.md` 全檔提到 `_t3_cur_sheet_title` → **0 次**。
- **不能同時為真**：tile ① 的來源若只有 `policy_sheet_id`，`:1697` 與 `:1873` 就是錯的；
  若是兩個鍵，A 的「盤點表有列」就漏掉了一個**和 tile ③ 完全同級、規格卻沒有就地標示**的 0 命中鍵。
- 出處：`spec/UI_SPEC.md:555`；`ui/views/page_04_portfolio.py:1697-1700,1873`；`docs/v2/10_db_inventory.md`（0 命中）。

### M2｜§2.15 自稱「`12` 是全檔孤例、其餘三處一個字都沒動」，但同一檔還有兩處 `12`

- **A 說**：`docs/v2/10_db_inventory.md:1325` ——「→ **三處一致，`12` 是全檔孤例**，故**只改標題那一個數字**，其餘三處一個字都沒動。」
- **B 說**：同一檔另有兩處：
  - `:567`（在 `## 0. 怎麼讀這張表` 底下）——「`config/macro_weights_active.json`（**12-key 群組列**）」，
    **無刪除線、無更正註，是一個活的 `12`**。
  - `:1914`（在 `## 5. 統計` 底下）——「群組列（**本列標題寫 12 key**，**實測 11 個且全部有值**）」，
    這句描述的是**標題現在寫什麼**；標題已於本輪改成 11，**這句因此過期**。
- **不能同時為真**：若 `12` 是孤例，`:567` 與 `:1914` 不會存在；若它們存在，「孤例」與「其餘三處一個字都沒動」就不成立。
- ⚠️ **標題本身改對了**（本組 `json.load` 實測 11 個 key、名稱逐字一致），**M2 只打到那句「孤例／只改一處」的自述**。
- 出處：`docs/v2/10_db_inventory.md:1325`／`:567`／`:1914`。
- （另：`:195`／`:2575` 也含 `12`，但兩處**都**在其後緊接的 `:198`／`:2578` 附了「已於 2026-09-16 收斂輪改完」的狀態更新註，**不計入本條**。）

### M3｜母法 `docs/v2/CONSTITUTION.md` 不在宣告的基準 `origin/main` 上

- **A 說**：`UI_SPEC.md:28` 宣告「**基準 `origin/main`**」，而 `UI_SPEC.md` **7 行**
  （`:206`／`:224`／`:356`／`:361`／`:374`／`:407`／`:460`）與 `DECISION_LOG.md` **10 行**
  以 `docs/v2/CONSTITUTION.md:580`／`:582`／`:73`／`:72-77`／`:1096`／`:1860`
  當「**母法逐字**」出處，且規格通篇的重印指令寫成 `git show origin/main:<path>`。
- **B 說**：本組實測 `git ls-tree -r --name-only origin/main | grep -i constitution` → 只有 `tests/test_constitution_file_refs.py`；
  `origin/main` 的 `docs/` 底下**沒有 `v2/` 這一層**。該檔實際住在**未合併分支**
  `origin/docs/v2-constitution-governance`（tip `5569d85`，`git merge-base --is-ancestor` → **NOT MERGED**，領先 `origin/main` **11** 個 commit）。
- **不能同時為真**：讀者照規格的基準與指令跑 `git show origin/main:docs/v2/CONSTITUTION.md`，得到的是空的。
- ⚠️ **引文本身沒有錯**：本組在該分支上逐條比對，`:580` G1、`:582` G3、`:73` F2、`:1096`「G3 最嚴重的一種」、
  `:1860`「不在 F1~F6 的消費者」欄 —— **五條全部逐字相符**。
  該分支的 `:73` 還獨立列出五頁行數 2192／1858／2755／2789／1763，與本組實測**完全一致**。
  **M3 打到的是「母法在哪個 ref 上」沒有被寫明，不是引文的正確性。**
- 出處：`UI_SPEC.md:28,206,224,356,361,374,407,460`；`DECISION_LOG.md`（10 行）；`git ls-tree origin/main`；`git rev-parse origin/docs/v2-constitution-governance` ＝ `5569d85`。

---

## 觀察（不構成矛盾，但據實登記）

- **O1｜`UI_SPEC.md:556` 稱 `:1863` 為「逐字」，實際有標點改寫。**
  原文：`狀態列四格。**每一格要嘛有真值，要嘛明講「不知道」——沒有第三種。**`
  規格：`每一格要嘛有真值、要嘛明講「不知道」，沒有第三種`（`，`→`、`，`——`→`，`，句號略）。語意不變。
- **O2｜`UI_SPEC.md:216`（表頭寫「原樣引用」）把 `:804` 的 `{_al['light']}` 縮寫成 `{light}`。** 語意不變。
- **O3｜`UI_SPEC.md:454` 引 `ai_summary.py:141-143` 的 caption 止於「結果已暫存」，原文續有「，重整頁面不會重打 API」。**
  該引文前段已用「…」示意省略，尾端未示意。
  （O1~O3 三處與 ②-12 那一輪「引文逐字化」是**同一型**；②-12 的六處本輪已全部改對，見 (5)。）
- **O4｜射程外：`services/ai_prompts.py` 另有一支比 ②-13 露骨得多的 prompt builder。**
  `build_mk_advisor_prompt`（`:30-94`）逐字含 `:72`「列出 2-3 檔**該減碼**的標的（指明代碼 + 原因）+ 2-3 檔**該補進**的類型」、
  `:76`「### ⚖️ 三、**建議配置比例**（核心 / 衛星 / 現金）」、`:79`「明確要求提高現金比例至 25%+」、`:69`「**建議行動**：…」。
  **本組實測其消費鏈**：→ `services/ai_service.py:334`（`analyze_portfolio_mk_advisor`，`:130`）→ **`ui/tab3_t7_ledger.py:3355`**。
  落在**舊分頁（F1）**，**不在本規格五頁的射程內**，故**不是規格的矛盾**。登記供總管判斷是否屬同族。
- **O5｜盤點表尚有第三份副本**：`/home/user/fund-doc-inv/base/docs/v2/10_db_inventory.md`（before 快照）。
  權威版 `:29-31` 已就地登記它、並說明未動。本組實測它與舊那份 `tail -n +18` **md5 相同**，與該登記一致。

---

## 查了但沒問題的（逐項列出，避免「沒講＝沒查」）

1. 五個 page 檔與 `origin/main` 的 blob hash **5/5 MATCH**（本組是在真的那一版上查的）。
2. 掃描 1~4 逐檔數字（1/8/0/0、13/1/0/0、5/1/1/0、10/0/2/0、7/1/0/0）—— 獨立重跑**全中**。
3. 掃描 5 逐檔數字（8/5/4/9/8）與**逐一行號**（如 ①：676/733/1053/1143/1969/2064/2123/2131）—— 獨立重跑**全中**。
4. H5（`#####`）在掃描 5 中**正確未被計入**（正控注入一個 H5，未計）。
5. 五頁頁面大標 `:2064`／`:1827`／`:2730`／`:2729`／`:1710` 五個，規格確實一個都沒列為區塊。
6. `safe_section` 只在 except 分支用 `label`（`render_state.py:186-213` 全函式逐行讀過）。
7. §1.6 的 grep 灌水實例（`page_03` 4 行/1 真、`page_05` 1 行/0 真）與點名的三個非呼叫行號。
8. ④-8 的 13 處原始碼引用（`:649-653`／`:665`／`:673`／`:1863`／`:1879-1883`／`:1891-1892`／`:1903-2032`／`:2030-2031`／`:2170-2173`／`:2175-2176`／`:2179-2191`／`:2197-2200`／`:2762-2763`／`:2781`）。
9. `STATUS_COLS: int = 4` 且 `_tiles` 恰 4 個 → `zip` 不會靜默吃掉任何一格。
10. ①-4 五列引文＋`:770-772`／`:778`／`:794-800`＋`allocation_ladder.py:57`＋`signal_thresholds.py:409-410`。
11. ①-4「免責一句都沒有」以 19 詞加寬字表重驗 —— 結論不變。
12. ②-11 的 `:196`／`:203`／`:204` 引文、`:152-207` 函式界、`:192` 逐檔迴圈、`:193-207` 單一 `st.markdown`、`:188` 空狀態文案、呼叫端 `page_02_health.py:1759`。
13. ②-13 七列引文＋`ai.py:243-249` sections 序位＋`ai_prompts.py:102-152`／`:112-113`＋`ai_summary.py:37`／`:69`／`:109`／`:138`／`:141-143`。
14. `build_structured_summary_prompt` 的 production import 端**恰好兩個**（②-13 與 ①-11）。
15. ②-12 六處更正**全部改對**，另同批複驗 `:104`／`:172-174`／`:177-178`／`:181`／`:183`／`:186`／`:200-202` 皆相符。
16. `:202`（建議配置權重表）確實在 `if/else` 之外，兩條分支都會印。
17. 母法六個逐字禁用詞在 `ai.py`／`ai_summary.py`／`ai_prompts.py`／`risk.py` **四檔皆 0**（正控非 0、負控 0）。
18. 計數五項全中，且六個多標籤區塊的 +8 逐塊對得起來。
19. `48→50` 的 +2 已被規格自己拆解（④-8 本身 ＋ 己段小標），非漏帳。
20. 盤點表六個狀態、四個逐節數、誰讀它三個數、不變量 0 —— 四組全中，未分類 0 列。
21. 盤點表兩個解析陷阱（跳脫豎線 34 行、`查不到（production）` 變體）實測存在且已處理。
22. 舊盤點表 `tail -n +18` 與 before 快照 **cmp 通過、md5 相同**，只多了 17 行檔頭。
23. 權威路徑宣告存在且含客戶原話逐字。
24. §2.15 三條出處逐條獨立重跑；對照組 `services/config/…json` 只有 2 個 key，兩份確為不同檔。
25. 四張現況引文表的表頭逐字相同、且都掛在 ⛔【母法衝突・待修】 區塊下。
26. `UI_PAGE_TODAY.md` 本輪未動，其 `:10`／`:11`／`:12`／`:14`／`:79` 五處全部是禁令或明標的反例。
27. F-15 的兩個讀法在 `:2176` 這一格**都真的成立**（`_render_policy` 2065-2227；`:2100-2300` 無自有 `safe_section`）。
28. ④-3 的 `:2771`／`:2781` 兩次確為互斥分支（規格 §1.5 `:94` 的敘述成立）。
