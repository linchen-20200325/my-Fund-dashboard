# ① 市場總覽 — **目標**文字線框草稿（to-be）

> **階段**：母法階段 1（**只出文字草稿**）。本檔**沒有**高保真視覺稿、**沒有**任何前端程式碼，
> `.py` 一個位元組都沒有動。
> **基準**：工作分支 `claude/fund-handover-verification-89o6ys`，commit **`0b08201`**（工作樹乾淨）。
> 本檔所有實測一律釘這個 SHA；**不用會移動的 ref 當基準**。
> **它是什麼**：把客戶 2026-09-21 三條裁示 ＋ 追加三條裁示 ＋ **同日第三批裁示（B／A／C／硬約束，見 §0.2a）**，
> 套到 ① 現有版面上之後，
> **這一頁會長成什麼樣子**的文字線框，供客戶依母法 `§-1.5.4`「UI 草稿先行」拍板。
> **它不是什麼**：不是施工單、不是動工授權（`CLAUDE.md §-1`）、不是範圍決定。
> **產出者**：U 組（架構與前端組），**單組，未經第二組獨立驗證**（`CLAUDE.md §-2` 規則 6）。
>
> ⚠️ **2026-09-21 第二版（本輪修訂，決策者：AI 總管 ＋ 客戶第三批裁示）**：
> 本輪把**客戶第三批裁示**（§0.2a）落地，並依**獨立稽核 V 組**的發現逐條更正。
> **修訂體例一律是「舊表述加刪除線保留 ＋ 就地註明理由、日期、決策者」**，不重寫、不裸刪；
> **唯一的例外是「描述內容的計數」** —— 依 `CLAUDE.md §-2.A` 的既有慣例，
> 計數**就地改成與內容一致的數字、不加刪除線**（表已經是 13 列，旁邊留一個劃掉的「14」只會讓記錄自相矛盾）。
> **本輪修訂同樣是單組產出，未經第二組獨立驗證。**

## 與現況線框 `docs/v2/23_ui_draft_01_macro.md` 的關係 —— **兩份並存，本檔不取代它**

| | `docs/v2/23_ui_draft_01_macro.md` | 本檔 |
|---|---|---|
| 畫的是 | **現況（as-is）** —— 今天線上那一頁，13 塊逐字轉錄 | **目標（to-be）** —— 裁示落地之後的樣子 |
| 逐字文案 | 從程式碼逐行重印，一個標點都沒改 | **既有塊一律回指那一份**，本檔不重抄一遍 |
| 狀態 | **歷史紀錄，本檔一個字都沒動** | 草稿，等客戶拍板 |

⛔ **本檔不修改、不覆蓋、不「更新」那一份。** 現況線框記的是「2026-09-16 那天畫面長什麼樣」，
那是**當時為真的紀錄**；把它改成目標的樣子，等於把紀錄改成願望。

⚠️ **本檔的塊結構以現況線框 §0.3 那張表為底**，層名同樣不是發明的 ——
`ui/views/page_01_macro.py` 自己把這一頁切成「層 1 結論／卡片網格／層 2 依據／層 3 三欄／層 4 詳細區」。

---

## §0 讀這份草稿之前（五條，缺一條就會讀錯）

### 0.1 `†` 是什麼 —— 定義寫在這裡一次，標記逐處都在

**`†` ＝「引自子法 1 `:580-582`，母法原文查不到」。**

法源是客戶 2026-09-21 裁示 3（逐字見 §0.2 碼塊第 3 條）：
「母法原文引不到：所有 G1～G3 標「引自子法 1 :580-582，母法原文查不到」。不假裝引到母法。」

**本檔的機械寫法（與 `docs/v2/20_ui_spec.md` §①-Z.0 同一套，不另立規矩）**：
~~碼塊內的客戶原話**除外**~~ → **客戶原話（不論在不在碼塊內）除外**，
本檔正文**每一處** `G1`／`G2`／`G3` 裸 token 之後一律緊接一個 `†`。

⚠️ **2026-09-21 就地更正（稽核 V 組 低-6 指出；有意識的更正，不是漏刪 · 決策者 AI 總管）**：
舊表述把例外限縮成「**碼塊內**」，**沒有涵蓋正文中的逐字引文** ——
而**上一段（§0.1 引裁示 3 的那一句客戶原話）正是那一筆**：它在正文、不在碼塊，
裡面的 `G1～G3` 依舊表述應該要標 `†`，**標了就是在客戶原話裡插字**。
**舊表述的用意完全成立**（不得竄改客戶原話），**被權衡掉的是它的射程** ——
它用「介質」（在不在碼塊裡）去畫界線，而真正該畫的界線是「**這句話是不是客戶的原話**」。
⚠️ **本檔其餘各處的 `†` 標記一個都沒有動**，改的只有這條規則的射程敘述。

⚠️ **碼塊內刻意不標** —— 在逐字引文裡加註記就是竄改客戶原話。

**三條的內容（引自子法 1，非母法原文）**：

| 代號 | 子法 1 轉述的內容（原樣引用，不是本草稿的主張） |
|---|---|
| **G1†** | 嚴禁提供「一鍵再平衡」按鈕，嚴禁預設「最佳配置推薦」 |
| **G2†** | ~~子法 1 逐字 ——「所有標的調整與再平衡一律採『偏離提示、客觀對照、情境試算』三種形式之一」~~ → **子法 1 逐字（2026-09-21 校正引號與句尾）**：所有標的調整與再平衡一律採「偏離提示、客觀對照、情境試算」三種形式之一。 |
| **G3†** | ~~子法 1 逐字 ——「所有試算參數均由使用者自行輸入；偏離提示僅客觀描述現況與目標差距，不提供具體處置方向」~~ → **子法 1 逐字（2026-09-21 補回句尾句號）**：所有試算參數均由使用者自行輸入；偏離提示僅客觀描述現況與目標差距，不提供具體處置方向。 |

⚠️ **2026-09-21 就地更正（稽核 V 組 低-4 指出；有意識的更正，不是漏刪 · 決策者 AI 總管）**：
上表 **G2†** 標「子法 1 逐字」，但與轉述來源**有兩處出入**：
內層引號由「」改成了『』、句尾的句號被吃掉。**G3† 同樣被吃掉句尾句號**（本輪一併補回；⚠️ **稽核只點名 G2†，G3† 是本組順手對讀時發現的**）。
**來源逐字**（`docs/v2/20_ui_spec.md` 的 G1†／G2†／G3† 轉述表，釘 `0b08201`）：

```
git grep -n '^| \*\*G[123]†\*\* |' -- 'docs/v2/20_ui_spec.md'
docs/v2/20_ui_spec.md:1339:| **G1†** | **嚴禁提供「一鍵再平衡」按鈕，嚴禁預設「最佳配置推薦」。** |
docs/v2/20_ui_spec.md:1340:| **G2†** | 所有標的調整與再平衡一律採「**偏離提示、客觀對照、情境試算**」三種形式之一。 |
docs/v2/20_ui_spec.md:1341:| **G3†** | **所有試算參數均由使用者自行輸入**；**偏離提示僅客觀描述現況與目標差距，不提供具體處置方向**。 |
exit=0
```

⚠️ **上表 G1† 未加刪除線，理由據實寫明**：它**沒有**自稱「逐字」（欄名只寫「原樣引用」），
而它與來源的差別**只有句尾句號與粗體標記**；本輪**不動它**，把這件事寫下來讓後人知道這是**選擇**、不是漏看。
⚠️ **本檔的 `†` 定義（§0.1）已寫明：這三條是「轉述的轉述」** ——
`docs/v2/CONSTITUTION.md` 不在本工作分支上（§8.3 第 5 條），
**本輪校正的是「本檔 vs `docs/v2/20_ui_spec.md`」，不是「本檔 vs 子法 1 原文」。**

### 0.2 客戶 2026-09-21 裁示（逐字照抄，不得改寫）

```
1. 13 塊上限 = 預設展開上限，不是總塊數上限。
   - 預設展開：結論燈 + 3 張核心卡（4 塊）
   - 預設收合：其餘 13 塊以內
   - 7 項合併：
     P0-1 四象限 → 併入 ①-3
     P0-3 實質利率 → 併入 P0-2
     P0-5a/5b → 合成「跨資產相對價值」
     P0-6 配置信號表 → 併入 ①-4 或 ①-6
   - 新增塊數 3～4，總塊數 13～14
2. P0-6「方向」改「客觀對照」：
   「宏觀象限 → 各資產類別歷史表現中位數」
   符合 G2，不撞 G3。
3. 母法原文引不到：所有 G1～G3 標
   「引自子法 1 :580-582，母法原文查不到」。
   不假裝引到母法。
```

**追加三條裡與本檔直接相關的那一條（逐字）**：

```
1. G1 补判：
   - 歷史中位數本身不撞 G1（是事實，不是推薦）。
   - 但呈現方式不得附帶任何「該買哪個」暗示。
   - 文件三處（:1431／:1540／:1677）補判 G1，
     寫「符合 G1 的前提是：只顯示歷史中位數，不附暗示」。
   - 規格加一行硬規則：呈現層不得出現方向詞、箭頭、顏色暗示。
```

⛔ **呈現層硬規則（本檔逐塊套用）**：**呈現層不得出現方向詞、箭頭、顏色暗示。**
射程與正反例見 `docs/v2/20_ui_spec.md` 的 〈①-Z.0a〉，本檔不重寫一份。

### 0.2a 客戶 2026-09-21 **第三批**裁示（逐字照抄，不得改寫）—— 本輪修訂的法源

```
B：P0-7 情境分析「不進 ①」。
理由：現狀未定（母法衝突、凍結面、豁免討論中），
性質與 ①「今天狀態」不同，不同頁更清楚；
① 現有 10 塊加 3 塊已達上限 13，加第 4 塊超載；
不進就不用現在決定那兩處 caption 去留。

A：B 不進 → 新增 3 → 總數 13。不用選併法。

C：接受「①-1 + ①-4 ＝ 結論燈 + 3 張核心卡」。

硬約束：接受，四塊新塊排在 ①-4 之後、①-6 之前。
```

**本檔的落地（四項逐條）**：

| 裁示 | 落地在哪 | 本檔做了什麼 |
|---|---|---|
| **B** P0-7 不進 ① | §2.8／§1.2／§1.4／§3／§4／§5／§6 | §2.8 **整節加刪除線保留、查證內容一字未刪**（P0-7 日後另開一頁還用得到）；各表的 新-4 那一列退場 |
| **A** 新增 3、總數 13、不用選併法 | §1.2／§1.3 | 主表改為 **13**；§1.3 那五種併法**整節加刪除線保留** —— **裁示 A 已由裁示 B 決定，五種做法全部不再適用** |
| **C** 接受 ①-1 ＋ ①-4 的對映 | §0.6／§0.5／§9 | 主張改標「**客戶 2026-09-21 已接受**」；⚠️ 其理由 (a) 另因稽核發現而改寫（見 §0.6 就地更正） |
| **硬約束** 新塊排在 ①-4 之後、①-6 之前 | §1.2 | 主表順序照此排；⚠️ **「四塊」與實際的 3 塊有一個數字落差，見下方就地說明** |

⚠️ **「四塊新塊」這個數字的來由，就地寫明（不改客戶原文一個字）**
（**有意識的說明，不是竄改** · 日期 **2026-09-21** · 決策者 **AI 總管**）：
客戶硬約束那一句逐字寫的是「**四塊**新塊排在 ①-4 之後、①-6 之前」，
而在**他自己同一批的裁示 B 之後，新塊只剩 3 塊**（新-1／新-2／新-3）——
「**四**」是 **B 裁之前那條 A-14 路線**的數字（新-1／新-2／新-3／新-4）。
**總管已當面向客戶指出這個落差**；本檔**照 3 塊落地**，
客戶原文**逐字保留在上方碼塊、一個字都沒有改**（改了就是竄改客戶原話，同 §0.1 的理由）。
⛔ **位置約束本身完全不受影響** —— 「排在 ①-4 之後、①-6 之前」對 3 塊與 4 塊一樣成立。

### 0.3 逐字標記法（沿用現況線框 §0.2，**本檔不另立一套**）

| 標記 | 意思 |
|---|---|
| **沿用** | 這一行**今天就在 `0b08201` 的畫面上**，逐字在現況線框 `docs/v2/23_ui_draft_01_macro.md` 可查 |
| **這是我編的** | 這一行**畫面上沒有**，是本草稿為了把目標講清楚而寫的草稿註記 |
| `現況引文・待修` | 已判定 ⛔ 命中 G1†／G3†，等客戶裁決後重寫 —— **本草稿不提供替代文案** |
| **需新增** | 這個東西今天不存在，要做出來才畫得出這一塊 |
| **查不到** | 本草稿查了，沒查到；**不是**「未複驗」，**不是**「還沒找」 |

⚠️ **框線、`┌┬┐`、以及框內為了對齊而做的換行，全部是草稿記號，不是畫面元素。**
⚠️ **框內的「」是本草稿加的引號，畫面上沒有。**

### 0.4 按鈕字判準（**沿用現況線框 §0.1，那條判準是前一組自訂的，不是客戶給的**）

> 一顆按鈕字 ＝ 可選前綴 emoji ＋ **1~3 個中文字的動作動詞** ＋ 受詞名詞片語；
> 去 emoji／空白後**全長 ≤ 8**（中文字 1、英數詞 1）；**不含括號補述**。四項全過 ✓，任一不過 ⚠。

**出處**：`docs/v2/23_ui_draft_01_macro.md` §0.1，該節自陳「客戶沒有給定義，下面這條判準是我定的」。
**本檔照用、不改一個字**，並在 §6 逐顆重標。**客戶一句話就能改判準**；改完只要重標 ✓／⚠，不必重畫任何一塊。

### 0.5 本草稿**不**替客戶決定的兩件事，以及**一個**它自己提的對映主張

> ✅ **2026-09-21 狀態更新：三件全部已由客戶裁定**（第三批裁示，§0.2a；**狀態變更，不是漏刪**）。
> **下表原文一字未改** —— 它記的是**送審當時**本草稿的處置，那個處置在當時為真；
> 變的是每一列的**狀態**，就地補在最右欄。

| 代號 | 是什麼 | 本草稿的處置 | **客戶裁定（2026-09-21）** |
|---|---|---|---|
| **決定 A** | 總塊數 14 還是 13 | **兩條路並列畫出來**（§1.2／§1.3）。⛔ 不代選、不寫「建議併 X」 | ✅ **已裁：13**（「B 不進 → 新增 3 → 總數 13。不用選併法。」）⇒ **§1.3 五種併法整節退場** |
| **決定 B** | P0-7 情境分析進不進 ① | **兩條路並列畫出來**（§1.4）。⛔ 不代選 | ✅ **已裁：不進 ①** ⇒ **§2.8 整節加刪除線保留**（查證內容不刪） |
| **對映主張 C** | 裁示 1 說的「結論燈 ＋ 3 張核心卡」對映到 ① 現有哪兩塊 | **本草稿提一個主張 ＋ 理由 ＋ 替代路**（§0.6），客戶一句話可改 | ✅ **已接受**（「接受「①-1 + ①-4 ＝ 結論燈 + 3 張核心卡」。」）⚠️ 其**理由 (a) 另因稽核發現而改寫**，見 §0.6 |

**為什麼 A 與 B 不代選（法源）**：`CLAUDE.md §8.4` 步驟 4 ——「要不要順手做一次大重構」屬**範圍（scope）**，
範圍是客戶的決定，總管沒有能力代為判斷（只有客戶知道時間與優先序）。
**列選項不等於替客戶選**：本草稿把「技術上哪幾種做法可行、各自代價是什麼」列成對照表，
**那是總管的本職**（`CLAUDE.md §-1.5.1b` 裁決二強制條件 3：「哪些屬必須改」是技術判斷）。

**為什麼 C 要提主張**：它是**版面對映**不是範圍 —— 而版面正是本草稿要送審的東西。
依 `CLAUDE.md §-1.5.1b` 裁決二強制條件 1／2：**附推薦方案、不得只丟選項不表態**。

### 0.6 對映主張 C：「結論燈 ＋ 3 張核心卡」對到 ① 現有哪兩塊 —— ✅ **客戶 2026-09-21 已接受**

> ✅ **2026-09-21 狀態更新**：客戶第三批裁示逐字「**C：接受「①-1 + ①-4 ＝ 結論燈 + 3 張核心卡」。**」
> ⇒ **本節的對映已從「主張」變成「已拍板」**（狀態變更，不是漏刪）。
> ⚠️ **但它的理由 (a) 在同一輪被獨立稽核推翻，就地改寫於下** ——
> **結論（對映到 ①-4）不受影響、客戶也已接受**，被推翻的是那條理由的**鑑別力**。

**裁示 1 逐字**：「預設展開：結論燈 + 3 張核心卡（4 塊）」。
① 今天有兩個卡片網格：**①-2 六張指標卡**與 **①-4 一列三欄卡**。

**本草稿的主張（這是我編的）—— ✅ 客戶 2026-09-21 已接受**：

| 裁示的字 | 本草稿對映到 | 理由（三條） |
|---|---|---|
| **結論燈** | **①-1 總經結論**（層 1，全寬） | (a) 它是 ① 唯一一塊**只印一盞燈 ＋ 理由條列**的全寬塊；(b) 它自己的渲染器叫 `_render_layer_conclusion`；(c) `docs/v2/22_ui_page_today.md` §3 的「結論燈」就是照它的形狀寫的（該節逐字：「對照 `ui/views/page_01_macro.py` 既有的四層」） |
| **3 張核心卡** | **①-4 層 3 三欄卡** | (a) ~~它是 ① 唯一一次**正好三張**的 `render_cards([...])`~~ → **2026-09-21 就地更正，改寫為可自驗的敘述，見下方「更正 (a)」**；(b) `docs/v2/20_ui_spec.md` §1.5 的顆粒度尺已把那三張判為同一塊；(c) ①-2 是**六張**，與「3 張核心卡」對不上 ~~，也與客戶「一頁最多 3 張核心卡（第二層）」的上限對不上~~ → **2026-09-21 就地更正，見下方「更正 (c)」** |

#### ⚠️ 更正 (a)：「唯一一次正好三張」是假的（稽核 V 組 高-2；**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

**舊表述錯在哪**：`ui/views/page_01_macro.py` 有**另一處**字面同樣正好三張的 `render_cards`，
而且產品碼在它上面一行的註解**逐字**寫著同一條鐵則。**它不住在 ①-4，住在 ①-10 拐點警報。**

**改寫後的敘述（可自驗，不需要全檔盤點）—— 釘 `0b08201`**：
① 這一頁有**兩處**字面正好三張的 `render_cards`，**①-4 是其中之一**；另一處在 ①-10 拐點警報的「全域導航塔」。

```
git show 0b08201:ui/views/page_01_macro.py | grep -n '鐵則 01：正好 3 張 → 一列 3 欄'
1538:    # 沒有額外取數。鐵則 01：正好 3 張 → 一列 3 欄。
2184:    # 鐵則 01：正好 3 張 → 一列 3 欄。**三張都吃層 2 算好的結果，不重算。**
exit=0
```

`:2184` 的下一個 `render_cards`（`:2185`）是 **①-4 層 3 三欄卡**（`_card_allocation`／`_card_exceptions`／`_card_credibility`）；
`:1538` 的下一個（`:1540`）是 **①-10 的全域導航塔**（薩姆／SLOOS／市場廣度），它住在 `:1512` 的 `def _detail_inflection()` 裡。

⇒ **「正好三張」這個特徵不具鑑別力，(a) 不能拿來單獨支撐對映。**
✅ **(b) 仍然成立、也仍然是這條對映真正的依據** —— 稽核組查過 `docs/v2/20_ui_spec.md` §1.5 的合併表為真。
⛔ **結論不變**：「3 張核心卡」仍對映到 **①-4**，客戶亦已接受。
⚠️ **本節刻意不寫「① 只有這兩處正好三張」** —— 那句話取決於「有沒有漏看」，
而 §8.3 第 4 條已自陳**沒有逐行通讀 `ui/views/page_01_macro.py` 全檔**。
**上面那條指令的輸出是本節唯一宣稱的東西。**

#### ⚠️ 更正 (c)：後半句引的是一條已加刪除線、射程待裁示的舊算式（稽核 V 組 低-1；**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

**(c) 前半（「①-2 是六張，與『3 張核心卡』對不上」）獨立成立、未被動到。**
**被劃掉的是後半句** —— 它把「一頁最多 3 張核心卡（第二層）」當成**現行上限**引用，
而該句在來源處是**已加刪除線的舊算式**，且其射程客戶本輪**沒有重講**：

```
sed -n '1360,1362p;1366p' docs/v2/20_ui_spec.md
- ⇒ **頁面總塊數的上限改由客戶另外給：「總塊數 13～14」。**
- ⚠️ **「超過就砍，不是折疊」這句在新口徑下管不管、管哪一區，客戶本輪沒有重講** ——
  **本節不替客戶解釋它的射程，登記待裁示。**
- ~~**客戶 2026-09-21 新規則**：一頁最多 3 張核心卡（第二層）、**一頁不超過 13 塊**、**超過就砍，不是折疊**。~~
exit=0
```

⇒ **那條上限今天是「待裁示」狀態，不是現行規則**；拿它當理由，等於用一條退役條文替對映背書。
⛔ **結論不變**（客戶已接受本對映），改的只有這半句理由。

**替代路（客戶若要改，改這一句就夠）**：把「3 張核心卡」對映到 **①-2 六張指標卡挑三張**。
**代價**（本草稿只列代價，不評價）：
- ①-2 會從**一次** `render_cards` 拆成兩組 ⇒ 依 `docs/v2/20_ui_spec.md` §1.5 第 1／2 條，**它會從 1 塊變成 2 塊**；
- 總塊數的算式因此要重算，**決定 A 的兩條路都會跟著位移**；
- 被留下的三張（熱錢動向／極端風險警語／新聞情緒）要重新決定去處。

⚠️ **本對映主張未經第二組驗證**（`CLAUDE.md §-2` 規則 6），**不得**被引用為已查證的事實。

### 0.7 ⚠️ 一個把 A 與 C 綁在一起的顆粒度張力 —— **只登記，不裁決**（詳見 §7 `N-1`）

**裁示 1 的兩半用了兩把不同的尺**：
- 「預設展開：結論燈 + 3 張核心卡（**4 塊**）」⇒ 1 ＋ 3 ＝ 4，**一張卡算一塊**。
- 「新增塊數 3～4，總塊數 13～14」的被加數「① 現有 10 塊」⇒ 出自 `docs/v2/20_ui_spec.md` §1.5，
  而該節第 2 條逐字：「**同一次卡片網格呼叫產出的多張卡，是同一塊裡的多張卡，不是多個區塊**」
  ⇒ ①-4 那三張卡算 **1 塊**。

**同一份裁示裡，前半把三張卡數成 3、後半把三張卡數成 1。**
⛔ **本草稿不裁決哪一把尺對**，也**不替客戶調整任何一個數字。**
**本草稿的處置**：§1 的總表**一律用 §1.5 那把尺**（因為「10 ＋ 3 ＝ 13」這個算式是用它算出來的，
換尺會讓客戶自己給的「總塊數 13」失去被加數），並在**預設展開欄**照裁示標「展開」。
**兩種讀法下，畫面長得一模一樣；差的只有怎麼數。**

⚠️ **2026-09-21 狀態更新：決定 A 與 C 都已裁定，但本張力沒有被裁掉**
（狀態變更，不是漏刪 · 決策者 **AI 總管**）：
客戶裁示 **A** 給了「新增 3、總數 13」、裁示 **C** 接受了 ①-1 ＋ ①-4 的對映 ——
**兩個數字都定了，但它們仍然是用兩把不同的尺數出來的**：
「13」用 §1.5 那把尺，「結論燈 ＋ 3 張核心卡（**4 塊**）」用一張卡算一塊那把。
⇒ **`N-1` 仍然有效、仍然只登記不裁決。**
⚠️ **本輪另外發現這把尺的落差比原本寫的更大**：
「≤13」這個約束**只在 §1.5 那把尺下成立**，換成裁示 1 那把尺**光收合側就超過 13** ——
**指令、輸出與下界算式見 §1.2 的就地更正（稽核 V 組 中-6）。**
⚠️ **上一行的算式也順手更新了**：舊表述寫「10 ＋ 4 ＝ 14」，
那是**新增 4 塊時**的算式；裁示 A 之後是 **10 ＋ 3 ＝ 13**。
**這是計數同步，不是政策變更**（`CLAUDE.md §-2.A`：描述內容的計數不加刪除線）。

---

## §1 四層閱讀順序總表（目標）

### 1.1 兩個口徑先講清楚，否則下面的數字會對不上

| 口徑 | 誰在用 | ① 今天是幾塊 |
|---|---|---|
| **規格塊**（`docs/v2/20_ui_spec.md` §1.5 那把尺） | 客戶裁示 1 的「總塊數」被加數、本檔 §1 各表 | **10**（`①-1/2/3/4/6/7/8/9/10/11`） |
| **渲染物**（畫面上真的印出來的東西） | `docs/v2/23_ui_draft_01_macro.md` §0.3 | **13**（10 塊 ＋ 2 塊規格未收 ＋ 1 塊排除項但有渲染） |

**兩個口徑差 3 個渲染物**：**載入閘門 Form**（規格未收，現況線框 `M-1`）、
**決策矩陣指路句**（規格未收，現況線框 `M-2`）、**總經燈號全表**（排除清單 甲-8，但畫面上有渲染）。

⛔ **這三個渲染物在客戶的塊數帳裡是看不見的** —— 客戶說「總塊數 13～14」
（**2026-09-21 裁示 A 已收斂為 13**），
用規格塊尺是 **13**，用渲染物尺是 **13 ＋ 3 ＝ 16**。**本草稿只登記這個落差**（§7 `N-2`），
**不替客戶選口徑、也不主張要把那三個收進規格。**
⚠️ **這兩個數字是計數同步，不加刪除線**（舊值為 14／`13 ＋ 4 ＝ 17`，
那是**新增 4 塊**時的數字；依 `CLAUDE.md §-2.A`，描述內容的計數就地改）。
⚠️ **落差本身一格都沒有變**：規格塊尺與渲染物尺仍然差那 3 個渲染物。

### 1.2 主表 —— ~~**路線 A-14**（照裁示直接落地，新增 4 塊、總 14）~~ → **新增 3 塊、總 13**（客戶 2026-09-21 裁示 A＋B）

> ✅ **2026-09-21 就地更新（狀態變更，不是漏刪 · 決策者 客戶第三批裁示，§0.2a）**：
> 客戶裁示 **B**「P0-7 情境分析不進 ①」＋ 裁示 **A**「B 不進 → 新增 3 → 總數 13。不用選併法。」
> ⇒ **路線分歧已經消失，本表不再是「兩條路之一」，它就是那一張表。**
> **新-4 那一列加刪除線保留在原位**（退場的是它進 ① 這件事，不是它的查證內容 —— 見 §2.8）。
> ⚠️ **計數（13／3／10／…）一律就地改成與表一致的數字、不加刪除線** ——
> 依 `CLAUDE.md §-2.A` 的既有慣例：**描述內容的計數不適用「劃掉保留」**，
> 表已經是 13 列，旁邊留一個劃掉的「14」只會讓記錄自相矛盾。
> ✅ **客戶硬約束已落地**：新塊（`#5`／`#6`／`#7`）排在 **①-4 之後、①-6 之前**，
> 即本表畫面順序 6～8；⚠️ 客戶原句寫「**四塊**新塊」，落差來由見 **§0.2a** 末段。

**預設展開 ＝ 4 塊（結論燈 ＋ 3 張核心卡）；其餘 9 塊預設收合（`#8` ①-6 區頭恆現，不計入收合）。**
⚠️ **「4 塊」是客戶裁示 1 那把尺數出來的；依 `docs/v2/20_ui_spec.md` §1.5 那把尺是 2 塊**（§0.7、§7 `N-1`）。

| # | 畫面順序 | 塊名 | 層 | 版面 | 預設 | 新增／既有 | 落點來源 |
|---|---|---|---|---|---|---|---|
| — | 0 | （頁首，**不是區塊**） | — | 全寬 | 恆現 | 既有 | `ui/views/page_01_macro.py` 頁面大標 ＋ 麵包屑 ＋ 一句話職責 |
| — | 1 | （**載入閘門 Form**，規格未收） | — | 全寬 form | 恆現 | 既有 | 現況線框 `M-1` |
| **1** | 2 | **①-1 總經結論**（＝**結論燈**） | 層 1 | 全寬 | **展開** | 既有 | 對映主張 C |
| **2** | 3 | **①-2 六張指標卡** | 層 1↔2 | 3 欄網格（6 張 → 2 列） | **收合** | 既有 | — |
| **3** | 4 | **①-3 五桶證據表　＋ P0-1 四象限** | 層 2 | 全寬表 | **收合** | 既有（**接新內容**） | 裁示 1：「P0-1 四象限 → 併入 ①-3」 |
| **4** | 5 | **①-4 層 3 三欄卡**（＝**3 張核心卡**） | 層 3 | 一列 3 欄 | **展開** | 既有（卡 1 待修） | 對映主張 C |
| **5** | 6 | **新-1 政策利率路徑 ＋ 實質利率** | 層 3 之後 | 待定（見 §2.5） | **收合** | **新增** | 裁示 1：「P0-3 實質利率 → 併入 P0-2」 |
| **6** | 7 | **新-2 美元週期** | 層 3 之後 | 待定 | **收合** | **新增** | 裁示 1：P0-4 **未點名合併** |
| **7** | 8 | **新-3 跨資產相對價值** | 層 3 之後 | 待定 | **收合** | **新增** | 裁示 1：「P0-5a/5b → 合成「跨資產相對價值」」 |
| ~~**8**~~ | ~~9~~ | ~~**新-4 情境分析**~~　⛔ **客戶 2026-09-21 裁示 B：不進 ①，本列退場**（§2.8） | ~~層 3 之後~~ | ~~待定~~ | ~~**收合**~~ | ~~**新增**~~ | ~~裁示 1：P0-7 **未點名合併**　⚠️ **僅在路線 B-進① 存在**（§1.4）~~ → **裁示 B 已選定「不進 ①」** |
| **8** | 9 | **①-6 🔎 詳細資料與說明**（層 4 區頭）　※ P0-6 的去處二 | 層 4 | 分隔線 ＋ `###` 區頭 | 恆現 | 既有 | 裁示 1：「P0-6 → 併入 ①-4 **或 ①-6**」 |
| **9** | 10 | **①-7 🌳 長期座標** | 層 4 | `####` ＋ 3 欄網格 ＋ 展開器 | **收合** | 既有 | — |
| **10** | 11 | **①-8 📈 中期循環** | 層 4 | 委派 | **收合** | 既有 | — |
| **11** | 12 | **①-9 🎯 短線雷達** | 層 4 | `#####` ×2 ＋ 全寬大表 ＋ 3 欄網格 | **收合** | 既有 | — |
| **12** | 13 | **①-10 ⚠️ 拐點警報** | 層 4 | `#####` ×2 ＋ 3 欄網格 ×2 | **收合** | 既有 | — |
| **13** | 14 | **①-11 🤖 AI 景氣判斷總結** | 層 4 | Form ＋ markdown 全文 | **收合** | 既有 | — |
| — | 15 | （**決策矩陣指路句**，規格未收） | — | 一則 `st.caption` | 恆現 | 既有 | 現況線框 `M-2` |
| — | 16 | （**總經燈號全表 —— 已拍板不做**） | — | `empty_state` 三要素 | 恆現 | 既有 | 排除清單 甲-8 |

**計數**：規格塊 **13**（既有 10 ＋ 新增 3）—— 與客戶裁示 A 逐字「新增 3 → 總數 13」一致。
**預設展開**：`#1` 與 `#4` —— **依 `docs/v2/20_ui_spec.md` §1.5 那把尺是 2 塊，依客戶裁示 1 那把尺是 1 ＋ 3 ＝ 4 塊**
（兩把尺之差見 §0.7；**兩種讀法下畫面長得一模一樣，差的只有怎麼數**）。
**預設收合**：**依 §1.5 那把尺是 10 塊**（13 − 展開 2 − `#8` 區頭恆現 1），落在客戶給的「其餘 13 塊以內」。
⚠️ **本行刻意把兩個數字都寫出來** —— 只寫一個，讀者無法判斷它用的是哪一把尺，而這正是 `N-1` 登記的那個問題。

#### ⚠️ 就地更正：「≤13」只在 §1.5 那把尺下成立（稽核 V 組 中-6；**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

**舊表述錯在哪**：上一行的「**兩把尺都寫出來**」**只套用在「預設展開」**，
而**帶 `≤13` 約束的是「預設收合」那一半** —— 那一半當時只寫了一個數字，還寫成無條件肯定句。

**兩把尺各自的收合側數字（釘 `0b08201`）**：

- **§1.5 那把尺（一次卡片網格呼叫 ＝ 一塊）** → 收合 **10 塊** ⇒ **`≤13` 成立。**
- **裁示 1 那把尺（一張卡算一塊）** → **光收合側就已經超過 13**。
  ⚠️ 以下四組是**字面可數的下界**，**不是全頁窮舉**（§8.3 第 4 條：沒有逐行通讀該檔全檔）：

```
sed -n '2157,2165p' ui/views/page_01_macro.py | grep -c '^        _card_'          # ①-2 六張指標卡
6
sed -n '1224,1232p' ui/views/page_01_macro.py | grep -c '^    ("'                  # ①-7 _US_LIQ_ROWS
7
sed -n '1339,1345p' ui/views/page_01_macro.py | grep -c '^    ("'                  # ①-9 _LIQ_FACTOR_ROWS
4
sed -n '1540,1548p' ui/views/page_01_macro.py | grep -cE '^        (_nav_tower_card|_breadth_card)\('   # ①-10 全域導航塔
3
```

  ⇒ **6 ＋ 7 ＋ 4 ＋ 3 ＝ 20**，四組全在**預設收合**的塊裡，**已經 > 13**，
  而這還沒算 ①-3／①-8／①-11／新-1／新-2／新-3 與 ①-10 的拐點偵測中心網格。

⛔ **本草稿不裁決哪一把尺對**（同 §0.7、§7 `N-1`），
**只把「`≤13` 繫於哪一把尺」這件事講明白** —— 它**不是**一句無條件成立的話。
⚠️ **本段刻意只給下界、不給總數** —— 給總數需要全檔盤點，而本草稿沒有做（§8.3 第 4 條）。

⛔ **新塊的畫面位置為什麼一律排在 ①-4 之後、①-6 之前 —— 這不是美感，是守衛逼的（~~實測~~ → **讀守衛原始碼**）**：
`tests/test_wf01_detail_zone_order.py::test_detail_zone_renders_its_five_sections_in_order_and_contiguous`
把「連續」一路判到**畫面結尾**，該檔 `_zone_openers()` 的 docstring 逐字寫著
「從詳細區的區頭那一格起，取到最後 —— **區內與區後不得再冒出別的一級區塊**」。
⇒ ~~**任何 H2~H4 的新區塊，只要落在 `🔎 詳細資料與說明` 那一格之後（含最後面），這條守衛當場轉紅。**~~
→ **受限版本（2026-09-21 就地更正，見下）**：**凡用 `_RECORDED_APIS` 那 11 個渲染 API 之一畫出、
且文字長得像 `## ` 或 `<h2>` 的新一級區塊**，只要落在 `🔎 詳細資料與說明` 那一格之後（含最後面），這條守衛會轉紅。
**排在區頭之前則不在它的切片內，守衛照綠。**

#### ⚠️ 就地更正：那條守衛沒有「任何寫法都擋得住」那麼強，而且 `expander` 正好是它的盲點（稽核 V 組 中-1）

（**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

**舊表述錯在哪 —— 兩處**：
1. **「任何 H2~H4」把守衛講得比它實際強。** 那支測試**自己的 docstring 用刪除線就地撤回過這個讀法**，
   改寫成「受限版本」：它只擋得住 `_RECORDED_APIS` 字表涵蓋的渲染 API。
   **草稿引的是 `_zone_openers()` 那一句（逐字為真），卻沒有引到同一檔內撤回它的那一段。**
2. **「（實測）」這兩個字不成立。** §8.3 第 2 條自陳**沒有跑任何測試** ——
   本項是**讀守衛原始碼**推出來的，不是量出來的行為。**兩者不得混寫。**

**字表實際涵蓋哪些（讀 `tests/test_wf01_detail_zone_order.py` 原始碼，釘 `0b08201`）**：

```
sed -n '107,111p' tests/test_wf01_detail_zone_order.py
_RECORDED_APIS: tuple[str, ...] = (
    "markdown", "write", "subheader", "header", "title",
    # 2026-09-05 A779-b 擴充：這六個原本整組漏掉（各 10 passed 全綠）。
    "info", "success", "warning", "error", "caption", "html",
)
exit=0
```

```
sed -n '179,180p' tests/test_wf01_detail_zone_order.py
      - `st.expander("…")` **刻意不算**一級區塊 —— 📈 中期循環自己就有一個
        （Z-Score 完整矩陣），算進去會讓「連續」永遠不成立。**這是取捨，不是疏漏。**
exit=0
```

```
sed -n '275,277p' tests/test_wf01_detail_zone_order.py
      (b) 五塊之間**沒有夾別的一級區塊** —— ⚠️ **這句要照「受限版本」讀**：
          本條只擋得住 :data:`_RECORDED_APIS` 字表涵蓋的那些渲染 API
          （逐一實測過的清單見 :func:`_render_and_record` 的兩份 bullet）。
exit=0
```

⛔ **這一條直接打到本草稿自己的設計，必須寫在這裡而不是藏在 §7**：
**§6 主張「收合列 ＝ `st.expander`」，而 `expander` 正是這條守衛看不見的那一類。**
⇒ **若收合列真的用 `st.expander` 做，它不會被這條守衛擋住** ——
**位置約束對「用 expander 包起來的新塊」可能根本不觸發。**
⚠️ **但本草稿不把這讀成「所以位置可以隨便排」**：
(1) 收合列裡面的**塊標題**仍可能是 `st.markdown("#### …")`，那就**在**字表射程內；
(2) `N-6` 另有一條字面值錨要處理（見 §7）；
(3) 客戶硬約束已經逐字指定「排在 ①-4 之後、①-6 之前」—— **那是裁示，不是守衛的副產品。**
⇒ **位置照客戶裁示排；守衛強度的敘述照上面的受限版本讀。兩件事分開。**
⚠️ **本段是讀原始碼得到的，沒有跑過那支測試**（§8.3 第 2 條）——
**「用 expander 就不會轉紅」是讀碼推論，不是量測**，**不得**被引用為已驗證的事實。

### ~~1.3 差異表 —— **路線 A-13**（再併掉一項，新增 3 塊、總 13）~~ → ⛔ **整節退場**（客戶 2026-09-21 裁示 A）

> ⛔ **客戶裁示 A 逐字**：「**A：B 不進 → 新增 3 → 總數 13。不用選併法。**」
> ⇒ **本節五種做法（甲／乙／丙／丁／戊）全部不再適用** ——
> 裁示 B 選了「P0-7 不進 ①」，新增塊**自然就是 3**、總數**自然就是 13**，
> **沒有一項需要再併**，也就沒有併法要選。
> **本節整節加刪除線保留、一個字都沒有刪**（**有意識的政策變更，不是漏刪** ·
> 日期 **2026-09-21** · 決策者 **客戶**）。
>
> **兩邊理由並陳**：**舊表述在寫下當時完全成立** —— 當時決定 A 與決定 B 都還沒裁，
> 「要落到下緣得讓新增塊少一塊」這個算術是對的，把可行做法連同代價列出來也正是
> `CLAUDE.md §-1.5.1b` 裁決二要求的做法。**被權衡掉的是它的前提**：
> 決定 B 一旦選了「不進 ①」，決定 A 就**沒有東西可選** —— 這一點**舊表述自己在末段就寫明了**
> （「決定 B 一旦選了『不進①』，決定 A 就沒有東西可選了」），本輪只是那個條件成真了。
>
> ⚠️ **同輪一併記一筆稽核發現（V 組 低-2），不因整節退場而略過** ——
> **本節表頭自稱「代價（本草稿只列，**不評價**）」，但表內有兩處不符**：
> (1) **丙**的「技術上可行嗎」欄填「**可行但形狀不合**」，而其餘四列都是乾淨的「可行」
> —— 「形狀不合」是**評價**，不是可行性判定，它該落在代價欄；
> (2) **丁**的代價欄**一項代價都沒有**（整格寫的是好處與釐清）。
> ⇒ **那張表的「只列不評價」在這兩格沒有做到。**
> **記下來的理由**：本節雖然退場，但它是「怎麼把選項列給客戶」的體例樣本，
> 後人會照抄；**照抄一份自己沒做到宣稱的表，會把同一個病帶到下一頁。**

~~裁示 1 給的區間是「新增塊數 **3～4**，總塊數 **13～14**」。~~
~~**A-14 落在上緣。要落到下緣，得讓新增塊少一塊。**~~
~~**而 P0-4 與 P0-7 客戶沒有點名要併。**~~

~~⛔ **本草稿不代選要併哪一項，也不寫「建議併 X」。**~~
~~✅ **本草稿做的是：把技術上可行的幾種做法，連同各自的代價列出來，讓客戶一眼能選。**~~

| ~~做法~~ | ~~動到哪一項~~ | ~~技術上可行嗎~~ | ~~代價（本草稿只列，不評價）~~ |
|---|---|---|---|
| ~~**甲** P0-4 併進 新-1~~ | ~~美元週期 → 併進「政策利率路徑 ＋ 實質利率」那一塊~~ | ~~**可行**~~ | ~~一塊裡放三個主題（政策利率／實質利率／美元）。三者的阻塞點不同：P0-2 原料整條不存在、P0-3 缺 1 條 FRED series、P0-4 資料源現有 ⇒ **整塊的可上線時間被最硬的 P0-2 綁住**，而 P0-4 本來今天就畫得出一半~~ |
| ~~**乙** P0-4 併進 新-3~~ | ~~美元週期 → 併進「跨資產相對價值」~~ | ~~**可行**~~ | ~~兩者共用同一支歷史分位函式 `services/macro/us_indicators.py::calc_macro_phase_zpct`（`DXY` 與 `HY_SPREAD` 同在 `_ZPCT_REVERSE_KEYS`）⇒ **接線一次、兩項一起解鎖**。⚠️ 但該函式在 `docs/v2/42_live_dead.md` 登記為 **dead**（可達 production 的 import 只有套件 re-export、call 為 0）⇒ 這條路要先把一支今天沒有呼叫點的函式接回線上~~ |
| ~~**丙** P0-7 併進 新-3~~ | ~~情境分析 → 併進「跨資產相對價值」~~ | ~~**可行但形狀不合**~~ | ~~P0-7 依 G3† 硬條件**帶有使用者輸入控制項**（見 §2.8），而 新-3 是純讀數塊 ⇒ 併了之後那一塊同時是「唯讀對照」與「互動試算」，兩種契約混在一塊~~ |
| ~~**丁** P0-7 不進 ①（＝路線 **B-不進①**）~~ | ~~情境分析整項不落在 ①~~ | ~~**可行**~~ | ~~**不動任何既有塊，新增自然變成 3、總數自然變成 13。** ⚠️ 這**不是**「併掉一項」，是「不進這一頁」—— 兩件事的性質不同，見 §1.4~~ |
| ~~**戊** P0-1 不併進 ①-3，改自成一塊、再把別的兩項合併~~ | ~~重排裁示 1 已指定的去處~~ | ~~**可行**~~ | ~~⛔ 它會**推翻客戶已經逐字指定的去處**。本草稿列它只是為了讓「可行清單」完整，**不主張**走這條~~ |

~~⚠️ **一個純算術的事實，寫出來讓客戶少算一次（不是推薦）**：~~
~~**路線 B-不進① 的新增塊數就是 3、總塊數就是 13** —— 決定 B 一旦選了「不進①」，決定 A 就沒有東西可選了。~~
~~**反過來不成立**：選了 B-進①，A 仍然是 14 或 13 兩條路。~~

✅ **上面那句劃掉的末段，正是本輪成真的那一句** —— 客戶選了「不進①」，於是 A 沒有東西可選。
⛔ **本節保留在原位、不搬走**，因為 §1.4／§2.8／§3 都還指著它；**指過來的人要看得到它為什麼退場。**

### 1.4 決定 B —— P0-7 進不進 ①：**這是比塊數更前面的阻塞點**　✅ **已裁示：不進 ①**

> ✅ **2026-09-21 客戶裁定（第三批裁示，§0.2a；狀態變更，不是漏刪）**：
> **P0-7 情境分析「不進 ①」。** 客戶給的四條理由**逐字**：
> 1. 「現狀未定（母法衝突、凍結面、豁免討論中）」
> 2. 「性質與 ①「今天狀態」不同，不同頁更清楚」
> 3. 「① 現有 10 塊加 3 塊已達上限 13，加第 4 塊超載」
> 4. 「不進就不用現在決定那兩處 caption 去留」
>
> ⛔ **本節整節保留、一個字都沒有刪** —— 下方那張 `B-進①` vs `B-不進①` 對照表
> **記的是裁決當時擺在客戶面前的兩條路**，那是**為什麼會裁成這樣**的證據。
> **現行狀態一律讀右欄（`B-不進①`）**。
>
> ✅ **連帶結案：那兩處 caption 本輪不必決定去留**（客戶理由 4 逐字）——
> 對照表「那句版面定位怎麼辦」那一列的右欄逐字寫的是「**一個字都不必動**」，
> **裁示選的正是那一欄**。⇒ **`:2032` 與 `:2068` 兩處本輪都不動**，
> 連帶 `_SIGNPOST_MARK` 那條守衛**本輪也不必動**（對照表右欄「不適用」）。
> ⚠️ **這不是說那兩句 caption 沒有問題** —— 它們的去留**只是不再由本輪決定**；
> 若 P0-7 日後另開一頁，**那一頁不受 ① 這兩句 caption 拘束**（它們講的是 ① 這一頁）。

**阻塞點不是塊數，是 ① 頁自己的版面定位。**
`ui/views/page_01_macro.py::_render_matrix_signpost` 的 caption 逐字寫著
「這一頁只講市場（大盤與總經），不出現任何一檔你持有的基金」——
而 **P0-7「情境分析：利率、匯率、信用衝擊下，組合預估影響」的「組合」就是使用者的持倉。**

⚠️ **實測補一件事：那句話在 ① 頁出現兩次，不是一次**（釘 `0b08201`）：

```
grep -n '不出現任何一檔你持有的基金' ui/views/page_01_macro.py
2032:        f"　這一頁只講市場（大盤與總經），不出現任何一檔你持有的基金；"
2068:               "這裡只有大盤與總經，不出現任何一檔你持有的基金。")
```

`:2032` 在 `_render_matrix_signpost()` 裡（頁尾指路句）；**`:2068` 在頁首 caption 裡**。
⇒ **要處理那句版面定位，兩處都要處理，不是一處。**
**正控**（刻意不是受測目標）：同一把尺掃 `where_to_find` → 該檔多行命中，`exit=0`；
**負控為本組自訂隨機串，未列出 → 0 命中**。

| | **B-進①** | **B-不進①** |
|---|---|---|
| ① 的塊數 | 新-4 存在 ⇒ 新增 4、總 14（或依 §1.3 再併成 13） | 新-4 不存在 ⇒ **新增 3、總 13** |
| 那句版面定位怎麼辦 | **兩處 caption 的去留要同批決定** —— 留著就是畫面自打嘴巴；改掉就是改 ① 的頁面定位 | **一個字都不必動** |
| 撞哪些凍結面 | `ui/views/page_01_macro.py` ＝ **F2**（渲染）＋ 持倉來源在 `ui/tab3_portfolio.py`／`ui/tab3_t7_ledger.py` ＝ **F1** ＋ 試算邏輯 ＝ **F4**　⇒ **三格全撞** | ① 這一側**零新增凍結面撞擊** |
| 豁免射得到嗎 | **一格都射不到**（§5） | 不適用 |
| 另一個連帶 | `tests/test_wf01_detail_zone_order.py` 的 `_SIGNPOST_MARK` 守衛正盯著頁尾那句指路**恰好一句** ⇒ 動 `:2032` 要同批動守衛 | 不適用 |
| P0-7 去哪 | — | **本草稿不判**（那是「它該落在哪一頁」，屬客戶的範圍決定）。⚠️ 見下方實測 |

**⚠️ P0-6 不在本決定的射程內 —— 這是本草稿發現的一處派工前提誤差，詳見 §7 `N-3`：**
裁示 2 已把 P0-6 的定義換成「**宏觀象限 → 各資產類別歷史表現中位數**」，
它的兩個輸入是「(a) 歷史象限序列」與「(b) **各資產類別**歷史報酬序列」——
**兩個都是市場層資料，沒有一個是使用者持倉。**
「P0-6 需要使用者持倉」那句話出自 `docs/v2/20_ui_spec.md`〈①-Z.3〉，
而該句寫在**裁示 2 換定義之前**，換定義那一輪**沒有回頭改它**。
⇒ **本草稿把 P0-6 一律當市場層處理；決定 B 只管 P0-7。**

**本草稿實測：P0-6／P0-7 是不是已經部分存在於 ② 或 ④？**
（`docs/v2/20_ui_spec.md`〈①-Z.3〉自陳「本組沒有掃…查不到」；**本草稿掃了，指令與輸出照抄，釘 `0b08201`**）

```
# 負向 A：P0-6（象限 × 資產類別中位數）
git grep -nE 'quadrant|quad_alloc|quad_desc|growth_inflation|象限|asset_class.*(return|median)|類別.*中位數' \
    -- 'ui/views/page_02_health.py' 'ui/views/page_04_portfolio.py'
（無輸出）    exit=1

# 負向 B：P0-7（情境／衝擊試算）
git grep -nE 'scenario|情境|stress_test|壓力測試|衝擊|shock|sensitivit|敏感度|what_if' \
    -- 'ui/views/page_02_health.py' 'ui/views/page_04_portfolio.py'
ui/views/page_02_health.py:794:    於是在「四檔、每桶各一」的情境下，它印出來的三個數字加起來是 **3**，
ui/views/page_04_portfolio.py:1305:#: 這三個字是**無條件語氣**（「搬完就到了」），而在 `n_missing_amount > 0` 的情境下
ui/views/page_04_portfolio.py:1400:       就整句消失 —— 而那正是客戶抱怨「看不出要不要調整」的那個情境。
exit=0

# 正控（刻意不是受測目標）：同一把尺掃一個一定在的字
git grep -cE 'safe_section' -- 'ui/views/page_02_health.py' 'ui/views/page_04_portfolio.py'
ui/views/page_02_health.py:19
ui/views/page_04_portfolio.py:19      exit=0
```

**負向 B 那 3 行逐行讀過，沒有一行是情境分析實作** —— 三行都是原始碼註解裡的中文「情境」
（`ui/views/page_02_health.py:794` 講「四檔、每桶各一」的舉例；`ui/views/page_04_portfolio.py:1305`／`:1400` 講文案語氣）。
⚠️ **2026-09-21 就地更正（本輪自查，稽核未點名；有意識的更正，不是漏刪 · 決策者 AI 總管）**：
上面「負向 B」那三行的輸出，**第一版只抄了 `檔名:行號`、把命中的內容整段截掉** ——
**那不是逐字輸出，是摘要**（與稽核 V 組 低-3 是**同一類**缺陷，只是它沒被點名）。
**本輪把三行補成逐字。** ⚠️ **這件事的意義**：正因為有內容才看得出「命中的是註解裡的中文『情境』」，
**截掉內容之後，下一段那句「三行都是註解」就變成一句無從複核的斷言。**
⚠️ **本輪是照 `CLAUDE.md §-2.A` 第 1 款把本檔碼塊內的每一條指令實跑一次才抓到的** ——
**同一次全掃另外抓到 §8.2 (g) 一處（見該處）。**
⇒ **P0-6 與 P0-7 的成品，在 ② 與 ④ 都查不到。**

⚠️ **但 ④ 有一塊形狀最接近的東西，而且它是 live 的**（已登記於 `docs/v2/20_ui_spec.md`〈①-Z.10〉發現 1）：
`ui/views/page_04_portfolio.py` 的 `BLOCK_MACRO_LINK`（「總經曝險聯動」），
經 `safe_section` 接線、委派 `ui/helpers/macro/linkage.py::render_macro_exposure_link`。
**本草稿實測它在 `0b08201` 上仍然接著線**：

~~```~~
~~git grep -n 'BLOCK_MACRO_LINK|render_macro_exposure_link' -- 'ui/views/page_04_portfolio.py'~~
~~→ 命中 8 行（含 :624 常數、:2553 import、:2567 呼叫、:2787 safe_section）    exit=0~~
~~```~~

⛔ **上面那段整段加刪除線 —— 它是一條跑不出宣稱輸出的指令**
（稽核 V 組 高-1；**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）。

**錯在哪（機制，不是筆誤）**：`git grep` **預設走 BRE**，`|` 在 BRE 裡是**字面字元**，
不是交替符號 —— 那條指令等於在找「`BLOCK_MACRO_LINK|render_macro_exposure_link`」這個**含豎線的字串**。
**照它重跑得到的是零輸出、`exit=1`**，不是宣稱的那 8 行。**要加 `-E` 才是 ERE。**

**兩條並排實跑（本組自己重跑，輸出逐字照抄；釘 `0b08201`）**：

```
git grep -n 'BLOCK_MACRO_LINK|render_macro_exposure_link' -- 'ui/views/page_04_portfolio.py'
（無輸出）    exit=1
```

```
git grep -nE 'BLOCK_MACRO_LINK|render_macro_exposure_link' -- 'ui/views/page_04_portfolio.py'
ui/views/page_04_portfolio.py:624:BLOCK_MACRO_LINK: str = "總經曝險聯動"
ui/views/page_04_portfolio.py:875:    ("ui.helpers.macro.linkage", "render_macro_exposure_link"),
ui/views/page_04_portfolio.py:2461:#      · `render_macro_exposure_link`    → 走 `st.caption("🧭 載入…")`（灰字，但**沒有 ⬜ 記號**）
ui/views/page_04_portfolio.py:2544:    `render_macro_exposure_link()` 在 ① 沒載入時**會**畫一句灰字，
ui/views/page_04_portfolio.py:2553:    from ui.helpers.macro.linkage import render_macro_exposure_link
ui/views/page_04_portfolio.py:2567:    render_macro_exposure_link(st.session_state, core_pct=_summary.get("core_pct"))
ui/views/page_04_portfolio.py:2786:    st.markdown(f"#### {BLOCK_MACRO_LINK}")
ui/views/page_04_portfolio.py:2787:    safe_section(BLOCK_MACRO_LINK, _render_macro_link)
exit=0
```

✅ **結論不變、而且現在真的可以照跑**：`BLOCK_MACRO_LINK` 那一塊在 `0b08201` 上仍然接著線
（`:624` 常數、`:875` 委派註冊、`:2553` import、`:2567` 呼叫、`:2786` 標題、`:2787` `safe_section`；
`:2461`／`:2544` 是同檔的說明註解）。

⚠️ **這一筆為什麼要就地寫明，而不是默默改掉**（`CLAUDE.md §2.1` 記載的**偽造可驗證性**）：
舊表述**長得像證據** —— 它有指令、有輸出、有 `exit=0`，後人會照抄、也會照跑；
**而照跑會得到 `exit=1`、零輸出**，然後合理懷疑整段結論。
**一條沒有真的跑過就宣稱輸出的指令，比沒有指令更危險** ——
沒有指令時讀者知道自己在讀一句未驗證的話，有假指令時讀者以為自己在讀證據。
⚠️ **舊表述還有第二層問題**：它的「輸出」那一行**不是輸出，是摘要**
（`→ 命中 8 行（含 …）`）—— 真正的 8 行從來沒有被抄下來，
所以**就算 `-E` 當初有加，也沒有人能從那段文字複核那 8 行是哪 8 行。**
✅ **本輪改成逐行照抄，並把兩種寫法的輸出並排**，讓「為什麼會錯」本身可被複核。

⛔ **本草稿只陳述它存在，不裁決它與 P0-6／P0-7 的關係，也不提任何處置** ——
客戶對發現 1 的裁示逐字是「**不動手，等①完成後另開一題**」。

---

## §2 逐塊文字線框

> **既有塊的逐字文案一律回指 `docs/v2/23_ui_draft_01_macro.md`，本檔不重抄一遍。**
> 本節對既有塊只畫**目標狀態下改變的那一部分**（預設展開／收合、有沒有接新內容），
> 對新增塊則畫完整的框。
> **每一塊都給六件**：逐字文案／標記／⛔ 母法衝突／凍結面 ＋ 豁免／live-dead／按鈕字。

### 2.1 ①-1 總經結論（＝**結論燈**）　層 1 · 全寬 · **預設展開**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ### 🧾 ① 結論 — 現在該加碼還是防禦                                   │
│        〔沿用 · 現況引文・待修 —— 母法登記為「行動問句抬頭」〕       │
│                                                                      │
│ 〔一盞燈 ＋ 一句結論 ＋ 逐條理由；四條路徑（證據撐不住／🔴／🟢🟡／   │
│   尚未載入）逐字見現況線框 §1.2，本檔不重抄〕         〔沿用〕       │
│                                                                      │
│ 〔⛔ 抬頭與本文五句 action：母法衝突・待修。                         │
│    本草稿**不提供**替代文案〕                    〔這是我編的〕      │
│                                                                      │
│ 〔目標狀態下唯一的改變：本塊是**預設展開區**的第一塊，               │
│   其上除頁首與載入閘門外不再有別的內容〕         〔這是我編的〕      │
└──────────────────────────────────────────────────────────────────────┘
```

- **逐字出處**：`docs/v2/23_ui_draft_01_macro.md` §1.2（四條路徑逐字、空狀態逐字、規則逐字）。
- **⛔ 母法衝突**：**兩處，都不是本草稿新編的** —— 抬頭「現在該加碼還是防禦」命中 **G3†**
  （子法 1 已登記為「行動問句抬頭」）；本文五句 `action` 命中 **G3† ＋ B-2**（「核心／衛星／現金」是資產桶名）。
  現況線框標為 `H-3`／`H-4`。⚠️ **抬頭字串在該檔有兩份（已載入路徑與灰態路徑），改的時候兩處都要改。**
- **凍結面**：渲染端 **F2**（`ui/views/page_01_macro.py`）＋ 文案產出端 **F4**（`services/macro/action_light.py`）。
  **豁免**：`B21` 那格 F2 豁免**只限 ①-4**，射不到本塊；`B8` 經客戶 2026-09-21 裁定為「**窄格，不通格**」。
  ⇒ **本塊無豁免；F4 那一半今天也沒有任何豁免。**
- **live/dead**：`services/macro/us_indicators.py::fetch_all_indicators` → **live**（`docs/v2/42_live_dead.md` 主表）。
  `services/macro/action_light.py::macro_action_light` → **`docs/v2/42_live_dead.md` 查不到**（該表自陳是「子集的子集」，非母體）。
- **按鈕字**：本塊無按鈕；灰態指路指向載入閘門那一顆。

### 2.2 ①-2 六張指標卡　層 1↔2 · 3 欄網格（6 張 → 2 列）· **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕六張指標卡                                〔這是我編的〕 │
│   （展開後內容逐字見現況線框 §1.3，本檔不重抄）        〔沿用〕      │
│   景氣位階／波動與信用／通膨與利率／熱錢動向／極端風險警語／新聞情緒 │
│                                                       〔沿用〕       │
└──────────────────────────────────────────────────────────────────────┘
```

- **目標狀態下的唯一改變**：**由恆現改為預設收合**。內容、燈號規則、六支空狀態一字未改。
- ⚠️ **收合之後有一件事要一起決定，本草稿只登記不裁決**：
  現況線框 §1.3 記載新聞卡**恆灰**、且極端風險警語卡在 `red = 1` 時與 ①-4 卡 2 **顏色相反**（現況線框 `M-6`）。
  **這兩件事在「恆現」時使用者一眼看得到，收合之後就看不到了。**
- **⛔ 母法衝突**：**無**（現況線框已逐卡逐行讀過，六張卡只報讀數與讀法）。
- **凍結面**：**F2** ＋ **F4**。**無豁免。**
- **live/dead**：`fetch_all_indicators` → **live**。
- **按鈕字**：本塊無按鈕。

### 2.3 ①-3 五桶證據表　＋ **P0-1 四象限**　層 2 · 全寬表 · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕### 🧾 ② 依據 — 憑什麼這樣說              〔沿用〕      │
│                                                                      │
│ 〔展開後：五桶證據表（5 欄 × 6 列）＋ 表下註記 ＋ 展開器             │
│   逐字見現況線框 §1.4，本檔不重抄〕                    〔沿用〕      │
│                                                                      │
│ 〔⛔ 表下註記那句「白話行動 —— …」：母法衝突・待修。                 │
│    本草稿**不提供**替代文案〕                    〔這是我編的〕      │
│                                                                      │
│ ┌─ 新接的內容：P0-1 四象限定位 ─────────────────────────────────┐ │
│ │ 成長動能 × 通膨動能 → 復甦／過熱／滯脹／衰退                      │ │
│ │                                            〔這是我編的〕        │ │
│ │ 〔可印的欄位（已存在於計算層；**live 渲染端 0 命中**，見框下更正）：│ │
│ │   `quadrant`（四值列舉）／`growth_dir`／`inflation_dir`〕        │ │
│ │                                            〔這是我編的〕        │ │
│ │ ⛔ **不得一併印出 `quad_desc`／`quad_alloc`／`quad_color`／      │ │
│ │    `quad_icon`** —— 前兩個是配置方向字串（G1† ＋ G3†）；        │ │
│ │    後兩個是象限色與象限圖示，命中追加裁示 1 的                   │ │
│ │    「顏色暗示」與「方向詞／箭頭」。                              │ │
│ │                                            〔這是我編的〕        │ │
│ └──────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

#### ⚠️ 就地更正 A：「渲染端 0 命中」為假（稽核 V 組 中-2）

（**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

**舊表述**寫「**全部已存在於計算層，渲染端 0 命中**」。**「渲染端 0 命中」實測為假。**
**實跑（釘 `0b08201`，輸出逐字照抄）**：

```
git grep -nE "quadrant|quad_desc|quad_alloc|quad_color|quad_icon|growth_dir|inflation_dir" -- 'ui/**'
ui/components/health_quadrant_scatter.py:1:"""ui/components/health_quadrant_scatter.py — 4D 健診四象限散佈圖（UI3b）。
ui/components/health_quadrant_scatter.py:29:- **缺 X 或 Y 的點不畫，但必須列出來。** `build_health_quadrant_figure()` 回傳
ui/components/health_quadrant_scatter.py:41:`split_placeable()` / `build_health_quadrant_figure()` 只依賴 plotly + `shared.colors`
ui/components/health_quadrant_scatter.py:141:def build_health_quadrant_figure(
ui/components/health_quadrant_scatter.py:167:    _quadrants = (
ui/components/health_quadrant_scatter.py:173:    for qx0, qx1, qy0, qy1, color in _quadrants:
ui/components/health_quadrant_scatter.py:230:def render_health_quadrant_scatter(
ui/components/health_quadrant_scatter.py:238:    fig, unplaceable, n_missing_amount = build_health_quadrant_figure(
ui/components/mk_clock.py:143:    quadrants = [
ui/components/mk_clock.py:153:    for key, theta_center, label in quadrants:
ui/components/mk_clock.py:172:        cur_theta = next(t for k, t, _ in quadrants if k == phase)
ui/tab1_macro.py:864:                signal=f"{_gi.get('quad_icon', '')} {_gi.get('quadrant', '—')}".strip(),
ui/tab1_macro.py:865:                color=_gi.get("quad_color") or _MACRO_CARD_LIGHT_COLOR["gray"],
ui/tab1_macro.py:868:                note=_gi.get("quad_desc") or "—",
exit=0
```

**改寫後的敘述**：**live 渲染端 0 命中**；
**唯一既有的「成長×通膨象限」渲染在 `ui/tab1_macro.py:864-868`，而它住在已判 dead 的 `render_macro_tab` 內。**
**可達性鏈（實跑，釘 `0b08201`）**：

```
git grep -nE '^\s+_render_top_card_grid\(' -- 'ui/tab1_macro.py'
ui/tab1_macro.py:2241:            _render_top_card_grid(ind, phase)
exit=0
git grep -n 'ui/tab1_macro.py::render_macro_tab' -- 'docs/v2/42_live_dead.md'
docs/v2/42_live_dead.md:457:| `ui/tab1_macro.py::render_macro_tab` | **dead** | U 組判 dead；5A 組 AST 覆核：可達 production 的 import 與 call 皆為 0，repo 內唯一 importer 是一個 `tests/` 檔 | 2026-09-21 | — |
exit=0
```

`:864-868` 住在 `ui/tab1_macro.py:528` 的 `def _render_top_card_grid(...)` 裡；
它在該檔的呼叫點是 `:2241`，而 `:2241` 住在 `:1774` 的 `def render_macro_tab()` 裡 —— **登記簿判該入口 dead。**

⛔ **這一筆是本草稿自己漏標的那一處**：
本檔在 §2.3（`calc_growth_inflation_axis` vs `mk_clock`）、§2.6（`calc_macro_phase_zpct`）、
§2.8（`risk_alert`）、§2.11（`render_macro_tab`）**四次**小心區分過「**符號 dead ≠ 模組 dead**」，
**唯獨這一格把「住在 dead 入口底下」直接寫成了「0 命中」。**
⚠️ **兩者的差別不是措辭**：「0 命中」意味著**沒有人寫過**，
而實際是**寫過、而且還在原始碼裡，只是今天到不了畫面** ——
後人若照「0 命中」動工，會以為要從零做起，
**而真正該先回答的是「那 3 行要不要搬過來、還是另寫一份」。**
⚠️ **上面那條指令的其餘命中不是同一個東西**：`ui/components/health_quadrant_scatter.py`
是**健診 4D 散佈圖**的象限、`ui/components/mk_clock.py` 是**美林時鐘**的象限，
**兩者都不吃 `calc_growth_inflation_axis` 的回傳鍵**（該函式的鍵名一個都沒有出現在那兩檔的命中行裡）。
⚠️ **本段刻意不寫「`ui/**` 只有這幾處」** —— 那取決於「有沒有漏看」；
**本段宣稱的只有上面那條指令在 `0b08201` 上的輸出。**

#### ⚠️ 就地更正 B：「不得印」清單漏掉 `quad_color` 與 `quad_icon`（稽核 V 組 中-3）

（**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

**舊表述的 ⛔ 清單只有 `quad_desc` 與 `quad_alloc`**，於是 `quad_color`／`quad_icon`
**既不在可印側、也不在不得印側 —— 等於留白**，而留白會被讀成「沒說不行就是可以」。

**本檔 §0.2 已逐字引了追加裁示 1 的硬規則：「呈現層不得出現方向詞、箭頭、顏色暗示。」**
§2.6（新-2）與 §4 對這條規則套用正確，**唯獨 P0-1 這一塊沒有套。**

**那兩個欄位實際長什麼樣（讀 `services/macro/us_indicators.py::calc_growth_inflation_axis` 的四個象限分支，釘 `0b08201`）**：

```
git grep -nE 'quad_color  = |quad_icon   = ' -- 'services/macro/us_indicators.py'
services/macro/us_indicators.py:1364:        quad_color  = MATERIAL_GREEN;   quad_icon   = "🌱"
services/macro/us_indicators.py:1369:        quad_color  = MATERIAL_ORANGE;   quad_icon   = "🔥"
services/macro/us_indicators.py:1374:        quad_color  = MATERIAL_RED;   quad_icon   = "⚠️"
services/macro/us_indicators.py:1379:        quad_color  = MATERIAL_ORANGE;   quad_icon   = "🌧️"
exit=0
```

⇒ **`quad_color` 是按象限給綠／橘／紅／橘 ＝ 逐字命中「顏色暗示」**；
**`quad_icon` 是 🌱／🔥／⚠️／🌧️ ＝ 一組把象限好壞畫成圖示的符號**，
同屬追加裁示 1 要擋的那一類（它與「箭頭」是同一種東西：**不印數字、只印一個好壞暗示**）。
⇒ **兩者一律列入 ⛔ 側**（框內已改）。
⚠️ **與 `quad_desc`／`quad_alloc` 的理由不同，不要合併讀**：
後兩者命中的是 **G1† ＋ G3†**（它們的內容是配置方向字串，`quad_alloc` 還帶 `↑`／`↓`）；
**`quad_color`／`quad_icon` 命中的是追加裁示 1 的呈現層硬規則**，不是 G1†／G3† 的字面。
⚠️ **本草稿不替客戶決定「是否可以用一個中性色」** —— 那是呈現層設計，
**本更正只做一件事：把這兩個欄位從「留白」移進「⛔ 側」，讓它不再是沒說到的東西。**

- **落點依據**：裁示 1 逐字「P0-1 四象限 → 併入 ①-3」。**不新增塊。**
- **逐字出處**：既有部分見現況線框 §1.4；新接部分的欄位名取自
  `services/macro/us_indicators.py::calc_growth_inflation_axis`（**本草稿實測該符號存在於該檔**）。
- **⛔ 母法衝突**：
  - **既有**：表下註記那句「白話行動 —— …」命中 **G3† ＋ B-2**（現況線框 `H-5`，子法 1 已登記為提案 1）。
  - **新接**：**條件性衝突** —— 只印 `quadrant`／`growth_dir`／`inflation_dir` ⇒ **不命中任何一條**；
    一併印 `quad_desc`／`quad_alloc` ⇒ **G1† ＋ G3†**（那兩欄的內容是帶 `↑`／`↓` 的資產方向字串）。
  - ⛔ **2026-09-21 補（稽核 V 組 中-3）**：一併印 `quad_color`／`quad_icon` ⇒ **命中追加裁示 1 的呈現層硬規則**
    （象限色綠／橘／紅／橘 ＝ 顏色暗示；🌱／🔥／⚠️／🌧️ ＝ 好壞圖示）。**理由與實測見上方「就地更正 B」。**
  - ⛔ **呈現層硬規則直接適用**：`↑`／`↓` 正是追加裁示 1 點名的「箭頭」。
- **凍結面**：渲染端 **F2**；既有文案產出端 **F4**（`services/macro/composite_score.py`）
  ＋ **不在 F1~F6**（`ui/helpers/macro/beginner_view.py`，由它把那句話串進 caption）。
  **豁免**：`B21` 只限 ①-4 ⇒ **併進 ①-3 沒有豁免可用。**
- **live/dead**：`services/macro/us_indicators.py::calc_growth_inflation_axis` → **live**（`docs/v2/42_live_dead.md` 主表：
  由同檔 `calc_macro_phase` 呼叫，有一個可達 production 呼叫點）。
  ⚠️ **另一套同名主題的東西是 dead 的，不要接錯**：`ui/components/mk_clock.py`（模組）與
  同檔 `render_mk_clock_section` 在 `docs/v2/42_live_dead.md` **皆判 dead**（該模組的 importer 只有測試檔）。
- **按鈕字**：本塊無按鈕；既有展開器 `🔍 各列判讀門檻全文(上表「說明」欄的完整版)` **⚠**（見 §6）。

### 2.4 ①-4 層 3 三欄卡（＝**3 張核心卡**）　層 3 · 一列 3 欄 · **預設展開**

```
┌─ 一列 3 欄（一次 render_cards，三張都吃層 2 算好的 _ev）──────────────┐
│ 📐 建議資產水位          │ ⚡ ③ 例外            │ 🔍 ④ 可信度         │
│                          │                      │                      │
│ 〔⛔ 卡 1 母法衝突・待修  │ 〔逐字見現況線框     │ 〔逐字見現況線框     │
│   —— 客戶已排 P0 第一批   │   §1.5〕   〔沿用〕  │   §1.5〕   〔沿用〕  │
│   第 3 項，且已另開一格   │                      │                      │
│   F2 豁免。本草稿**不提供**│                     │                      │
│   替代文案〕             │                      │                      │
│        〔這是我編的〕    │                      │                      │
├──────────────────────────┴──────────────────────┴──────────────────────┤
│ 〔P0-6 去處一（客戶二選一）：把「宏觀象限 → 各資產類別歷史表現中位數」│
│   接在本塊〕                                     〔這是我編的〕      │
│ ⛔ 呈現層硬規則：只顯示歷史中位數，不附暗示；                        │
│    不得出現方向詞、箭頭、顏色暗示。            〔這是我編的〕        │
└──────────────────────────────────────────────────────────────────────┘
```

- **逐字出處**：`docs/v2/23_ui_draft_01_macro.md` §1.5（三張卡的 `value`／`note`、兩層空狀態逐字）。
- **⛔ 母法衝突**：**卡 1** 命中 **G1†**（直接給出股／債／現金三個百分比）＋ **G3†**
  （停利／加碼各給一個觸發門檻，且那兩個門檻**不是使用者輸入的**）。
  **卡 2、卡 3 無衝突。** 新接的 P0-6 依客戶裁示 2「符合 G2†，不撞 G3†」，
  追加裁示 1 另補判「**符合 G1† 的前提是：只顯示歷史中位數，不附暗示**」。
- **凍結面**：渲染端 **F2** —— ⭐ **這是 ① 唯一一塊有豁免的**（`B21`：`ui/views/page_01_macro.py` **僅限 ①-4 這一處**）。
  ⚠️ **豁免的方向是「拿掉」不是「再加一張」**：`docs/v2/21_decision_log.md` §H 第 3 項逐字寫的是
  「把那張卡改成**不給資產配置百分比、不給停利／加碼門檻**的形式」。
  ⚠️ **另一半不在豁免裡**：門檻與比例由 `services/allocation_ladder.py::allocation_from_composite` 算出，
  那一檔是 **F4**，**要動必須走提案單**。
- **live/dead**：`services/allocation_ladder.py::allocation_from_composite` → **`docs/v2/42_live_dead.md` 查不到**
  （該表未收錄此符號）。P0-6 兩側的歷史序列 → **資料源本身查不到**，無符號可判 ⇒ ~~**未確認**~~ → **不適用**
  （**2026-09-21 就地更正，稽核 V 組 低-5；有意識的更正，不是漏刪 · 決策者 AI 總管**：
  「未確認」是 `docs/v2/42_live_dead.md` 的**三值判定之一**，寫上它等於本草稿替那張表**補了一個判**，
  與 §8.3 第 6 條自陳的「**該表查不到的就寫「查不到」，本草稿不替它補判**」**自相矛盾**。
  **沒有符號可判 ≠ 判它未確認** —— 前者是「這個問題問不出來」，後者是一個結論。**改寫成「無符號可判／不適用」。**）
- **按鈕字**：本塊無按鈕；三張灰卡的指路都指向載入閘門那一顆。

### 2.5 新-1「政策利率路徑 ＋ 實質利率」（P0-2 ＋ P0-3）　**新增** · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 政策利率路徑 ＋ 實質利率          〔這是我編的〕    │
│                                                                      │
│ 〔展開後 —— **今天這一塊只畫得出空狀態，畫不出任何讀數**〕           │
│                                                                      │
│ ⬜ 市場隱含的升息／降息路徑還沒有資料來源。                          │
│    缺什麼：Fed Funds futures／OIS 曲線／CME FedWatch 之一            │
│            （**需新增**：1 個全新來源 ＋ 1 個全新 L1 fetcher）       │
│    （去哪補：本頁上方「📡 載入總經資料」）        〔這是我編的〕     │
│                                                                      │
│ ⬜ 實質利率（名目 − 通膨預期）還沒有定義上正確的序列。               │
│    缺什麼：1 條 FRED series（`DFII10` 直接給 10Y TIPS 實質殖利率，   │
│            或 `T10YIE` 配 `DGS10` 做天期對齊）        **需新增**     │
│    現有的兩腿：`shared/fred_series.py::FRED_DGS10`（10Y 名目）       │
│                `shared/fred_series.py::FRED_T5YIE`（5Y 通膨預期）    │
│    ⚠️ 兩者相減是**混期**數字，定義上不是實質利率。                   │
│    （去哪補：本頁上方「📡 載入總經資料」）        〔這是我編的〕     │
└──────────────────────────────────────────────────────────────────────┘
```

- **落點依據**：裁示 1 逐字「P0-3 實質利率 → 併入 P0-2」⇒ 兩項合成一塊，**計 ＋1 塊**。
- **⚠️ 合併的是版面，不是判定** —— 兩項的資料源／計算／畫面三欄各自獨立
  （`docs/v2/20_ui_spec.md`〈①-Z.5〉：P0-2 ＝ 查不到／查不到／查不到；P0-3 ＝ 需新增 1 條 FRED series／查不到／需新增）。
  ⇒ **這一塊的阻塞點是兩者中較硬的那個：P0-2 的原料整條不存在。**
- **空狀態三要素必填**（鐵則 04，體例照 `docs/v2/22_ui_page_today.md` §6）：標題用使用者的話／缺什麼要具體／去哪補。
  ⚠️ 「去哪補」**不得手抄鈕字**，要回讀 `ui/views/page_01_macro.py::_where_to_load()`
  （它讀的是本輪畫面上實際印出來的送出鈕字）。
- **⛔ 母法衝突**：**無**（以陳述市場隱含機率與實質利率讀數的形式呈現時，不含動作動詞）。
  ⚠️ **邊界**：若以「市場預期降息 ⇒ 宜加碼債券」這種形式收尾，**落回 G3†**。
- **凍結面**：渲染端 **F2**（**無豁免**）；取數端新 fetcher 落在 `repositories/**` ⇒ **不在 F1~F6**；
  L2 若要加編排 ⇒ **F4**（**無豁免**）。
- **live/dead**（查 `docs/v2/42_live_dead.md`）：`shared/fred_series.py::FRED_DGS10` → **live**；
  同檔 `FRED_T5YIE` → **live**；`repositories/macro/fred.py::fetch_fred` → **live**；
  P0-2 的市場隱含路徑 → ~~**未確認**~~ → **不適用**（資料源本身查不到，沒有符號可判）
  —— **2026-09-21 就地更正，同上（稽核 V 組 低-5）**：理由與 §2.4 那一處逐字相同，不重寫一份。
- **按鈕字**：**本塊不新增控制項** —— 沿用 ① 的唯一載入閘門（鐵則 02）。
  ⚠️ 自己裸寫一個 `st.form(` 會讓 `tests/test_ui_rerun_contract.py` 的 `FORM_SITE_TOTAL` 由 7 變 8 轉紅。

### 2.6 新-2「美元週期」（P0-4）　**新增** · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 美元週期                          〔這是我編的〕    │
│                                                                      │
│ 〔展開後 —— 依客戶對「名目 vs 實質」的選擇，長成兩種之一〕           │
│                                                                      │
│ 〔讀法甲：接受**名目**廣義貿易加權美元〕                             │
│ 美元指數（廣義貿易加權，名目）  ──.──   歷史分位 ──%                 │
│ 取自 `shared/fred_series.py::FRED_DXY`，實際 series 是 `DTWEXBGS`。  │
│ ⚠️ 它**不是** ICE DXY，**也不是**實質有效匯率。   〔這是我編的〕     │
│ ⛔ 「歷史分位」今天畫不出來：演算法已寫好在                          │
│    `services/macro/us_indicators.py::calc_macro_phase_zpct`，        │
│    但該符號在登記簿判 **dead**（Streamlit 側 0 個呼叫點）。          │
│                                                   〔這是我編的〕     │
│                                                                      │
│ 〔讀法乙：要**實質**有效匯率〕                                       │
│ ⬜ 美元實質有效匯率還沒有資料來源。                                  │
│    缺什麼：1 條 FRED series（`RBUSBIS` 類）        **需新增**        │
│    （去哪補：本頁上方「📡 載入總經資料」）        〔這是我編的〕     │
└──────────────────────────────────────────────────────────────────────┘
```

- **落點依據**：裁示 1 **未點名 P0-4 合併** ⇒ **自成一塊，計 ＋1 塊。**
  ⛔ **本草稿不替客戶把它併掉**（要併的話，技術上可行的做法見 §1.3 甲／乙）。
- **⚠️「名目 vs 實質」是客戶的定義選擇，不是技術細節 —— 本草稿不代選**，兩種讀法都畫。
- **⛔ 母法衝突**：**無**（方向與分位都是客觀讀數）。
  ⚠️ 呈現層硬規則仍然適用：**不得**在美元旁邊掛 `↑`／`↓`，也不得用紅綠把它標成該買該賣。
- **凍結面**：渲染端 **F2**（無豁免）。
  ⚠️ **今天唯一畫得到美元的地方是 ①-8 的 Z-Score 矩陣那一列，字面住在 `ui/tab1_macro_midcycle.py` ＝ F1**；
  `B28` 那格 F1 豁免**只涵蓋 ①-8 的兩行**，**加一列指標不在射程內**。
- **live/dead**（查 `docs/v2/42_live_dead.md`）：`shared/fred_series.py::FRED_DXY` → **live**；
  `services/macro/us_indicators.py::fetch_all_indicators` → **live**；
  ⛔ `services/macro/us_indicators.py::calc_macro_phase_zpct` → **dead**（⚠️ 模組本身是 live，**符號 dead ≠ 模組 dead**）。
  ⇒ **這一塊最便宜的那條路，前提是先把一支今天沒有呼叫點的函式接回線上。**
- **按鈕字**：**本塊不新增控制項。**

### 2.7 新-3「跨資產相對價值」（P0-5a ERP ＋ P0-5b 信用利差 vs 歷史）　**新增** · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 跨資產相對價值                    〔這是我編的〕    │
│                                                                      │
│ 〔展開後 —— **一塊裡兩半，一半畫得出水準、一半整條畫不出來**〕       │
│                                                                      │
│ ┌ 5b 信用利差 vs 歷史 ──────────────────────────────────────────┐ │
│ │ 高收益信用利差（OAS）  ─.──%     歷史分位  ──%                  │ │
│ │ 取自 `shared/fred_series.py::FRED_HY_SPREAD`（`BAMLH0A0HYM2`）  │ │
│ │ ⚠️ **水準今天畫得出來**（①-2「波動與信用」卡已經在印）；         │ │
│ │    **「vs 歷史」畫不出來** —— 同 新-2，演算法在那支 dead 的      │ │
│ │    `calc_macro_phase_zpct` 裡。            〔這是我編的〕       │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ ┌ 5a 股票風險溢酬（ERP）────────────────────────────────────────┐ │
│ │ ⬜ 股票風險溢酬算不出來：被減數整條不存在。                     │ │
│ │    缺什麼：S&P 500 forward P/E 或盈餘殖利率的取數               │ │
│ │            （**需新增**：1 個新來源 ＋ 1 個新 L1 fetcher）      │ │
│ │    ⚠️ 原本那兩支取數 `fetch_yf_forward_pe`／`fetch_multpl_pe`   │ │
│ │       在登記簿是 **已退役**（2026-08-28 經客戶授權整段刪除），  │ │
│ │       **不是 dead code —— 兩者處置方式完全不同。**              │ │
│ │    （去哪補：本頁上方「📡 載入總經資料」）  〔這是我編的〕      │ │
│ └──────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

- **落點依據**：裁示 1 逐字「P0-5a/5b → 合成「跨資產相對價值」」⇒ **計 ＋1 塊。**
- ⚠️ **`docs/v2/20_ui_spec.md`〈P0-5〉自己寫過「本項分兩半答，兩半結論不同，合成一個字會失真」，
  那句話沒有被裁示推翻** —— 裁示合併的是**版面上的塊**，不是五欄判定。
  ⇒ **框內刻意分兩個子框，就是為了不把兩個不同的結論寫成一句。**
- **⛔ 母法衝突**：**無** —— 兩者都是 **G2†** 明列允許的「客觀對照」。
  ⚠️ **邊界**：不得以「⇒ 股票相對便宜，宜加碼」這種形式收尾（**落回 G3†**）。
- **凍結面**：渲染端 **F2**（無豁免）；計算下沉 ⇒ **F4**（無豁免）；
  新 series／新 fetcher 落在 `repositories/**`／`shared/fred_series.py` ⇒ **不在 F1~F6**。
- **live/dead**（查 `docs/v2/42_live_dead.md`）：
  `shared/fred_series.py::FRED_HY_SPREAD` → **live**；
  `services/macro/us_indicators.py::calc_macro_phase_zpct` → **dead**；
  `repositories/external_market_repository.py::fetch_yf_forward_pe` → **已退役**；
  同檔 `fetch_multpl_pe` → **已退役**；
  ⚠️ 同檔**模組本身** → **live**（其下 `fetch_stooq_csv`／`fetch_cboe_csv`／`_validate_market_series` 有實際 importer，
  **但那三個符號不是 ERP 的原料**）。
- **按鈕字**：**本塊不新增控制項。**

### ~~2.8 新-4「情境分析」（P0-7）　**新增** · **預設收合** · ⚠️ **僅在路線 B-進① 存在**~~ → ⛔ **整節退場：不進 ①**（客戶 2026-09-21 裁示 B）

> ⛔ **客戶 2026-09-21 裁示 B 逐字**：「**B：P0-7 情境分析「不進 ①」。**」
> **客戶給的四條理由（逐字，不改寫）**：
> 1. 「現狀未定（母法衝突、凍結面、豁免討論中）」
> 2. 「性質與 ①「今天狀態」不同，不同頁更清楚」
> 3. 「① 現有 10 塊加 3 塊已達上限 13，加第 4 塊超載」
> 4. 「不進就不用現在決定那兩處 caption 去留」
>
> **本節整節退場、但一個字都沒有刪**（**有意識的政策變更，不是漏刪** ·
> 日期 **2026-09-21** · 決策者 **客戶**）。
>
> ✅ **為什麼刻意整節保留而不是刪掉 —— 這一段是 P0-7 的查證成果，日後另開一頁還用得到**：
> 框內的 **G3† 硬條件**（三個衝擊幅度一律由使用者輸入、寫死預設值就從合規的情境試算變成違規）、
> **⬜ 空狀態三要素**、以及下方六件裡的 **live/dead 逐符號查證**
> （`FRED_DGS10`／`FRED_HY_SPREAD`／`fetch_usdtwd_series`／`annualized_moments`／
> `to_twd_total_return_series`／`compute_capture` 皆 live；`risk_alert` **dead**），
> **這幾項不會因為「它不進 ① 這一頁」而失效** —— 它們講的是 **P0-7 這個項目本身**，不是它的容器。
> ⛔ **刪掉等於把一整輪查證扔掉，而那是不可逆的。**
>
> ⚠️ **下方被劃掉的部分，射程僅限「它進不進 ①」，不是「這些事實不成立」**：
> - **「凍結面三格全撞」** —— 那是**針對 ①** 的撞擊分析（渲染端是 `ui/views/page_01_macro.py`）。
>   P0-7 若另開一頁，**渲染端換成哪一格要重算**，不得照抄。
> - **「① 頁自己那兩處 caption」** —— 那是 **① 這一頁**的版面定位（§1.4），
>   **另一頁不受它拘束**。
> - **live/dead 那一段與 G3† 硬條件那一段，換頁之後照樣成立**（它們與容器無關）。
>
> ⚠️ **框內的 ASCII 線框刻意沒有逐行加刪除線，理由是機械的、寫在這裡不讓它看起來像漏掉**：
> **刪除線符號在圍籬碼塊內不會被渲染成刪除線，只會多印兩個字元**，
> 逐行插入會把框線對齊整個打散 —— **那等於為了標記而毀掉被保留的內容本身**。
> **本節的退場由標題的刪除線 ＋ 本段 ⛔ 宣告，框內內容原樣保留。**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 情境分析                          〔這是我編的〕    │
│                                                                      │
│ 〔展開後 —— 這是 ① 唯一一塊會有**使用者輸入控制項**的〕              │
│                                                                      │
│ ┌─ st.form（鐵則 02：互動也走 form，不放裸按鈕）──────────────────┐ │
│ │ 利率衝擊　[  ] bp      匯率衝擊　[  ] %     信用衝擊　[  ] bp    │ │
│ │                                            〔這是我編的〕       │ │
│ │ ⛔ **三個幅度一律由使用者自行輸入** —— G3† 的硬條件。            │ │
│ │    一旦寫死一組預設衝擊幅度，就從合規的情境試算變成 G3† 違規。   │ │
│ │                                            〔這是我編的〕       │ │
│ │                                                                  │ │
│ │   [ 鈕字待定 ]   ← **需新增**，字未定                            │ │
│ │                                            〔這是我編的〕       │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ ⬜ 組合預估影響算不出來：衝擊變數與持倉之間的**係數**不存在。        │
│    缺什麼：久期／beta／金額加權的幣別曝險，三者都查不到              │
│            （**需新增**：2～3 個新純函式；久期法另需新 fetcher）     │
│                                            〔這是我編的〕           │
│                                                                      │
│ ⛔ **輸出不得以處置動作收尾**（否則落回 G3† 後半）。                 │
│                                            〔這是我編的〕           │
└──────────────────────────────────────────────────────────────────────┘
```

- ~~**落點依據**：裁示 1 **未點名 P0-7 合併** ⇒ **自成一塊，計 ＋1 塊。**~~
  ~~⛔ **本草稿不替客戶把它併掉，也不替客戶決定它進不進 ①**（決定 B，§1.4）。~~
- ~~**⛔ 母法衝突**：**無 —— 而且它是七項裡唯一被點名允許的**：~~
  ~~**G2†** 逐字把「**情境試算**」列為三種合規形式之一。~~
  ~~⚠️ **但 G3† 附帶硬條件**：「所有試算參數均由使用者自行輸入」⇒ 三個衝擊幅度是輸入欄，不是預設值。~~
  ~~⚠️ **這正是 ①-4 卡 1 被判違規的同一個理由**（那張卡的兩個門檻恆是寫死的預設值）。~~
- ~~**凍結面**：**三格全撞，一格豁免都沒有** ——~~
  ~~渲染端 `ui/views/page_01_macro.py` ＝ **F2**（`B21` 只限 ①-4）；~~
  ~~持倉來源 `ui/tab3_portfolio.py`／`ui/tab3_t7_ledger.py` ＝ **F1**（`B28` 只限 `ui/tab1_macro_midcycle.py` 的 ①-8 兩行）；~~
  ~~試算邏輯 ＝ **F4**（無豁免）。~~
  ~~⚠️ **還有一個先於凍結令的阻擋**：① 頁自己那兩處 caption（§1.4 實測）。~~
- ~~**live/dead**（查 `docs/v2/42_live_dead.md`）：~~
  ~~衝擊側 `shared/fred_series.py::FRED_DGS10` → **live**；同檔 `FRED_HY_SPREAD` → **live**；~~
  ~~`repositories/hot_money_repository.py::fetch_usdtwd_series` → **live**；~~
  ~~組合側 `services/portfolio_frontier.py::annualized_moments` → **live**；~~
  ~~`services/allocation_backtest.py::to_twd_total_return_series` → **live**；~~
  ~~`services/capture_ratio.py::compute_capture` → **live**；~~
  ~~⛔ `services/portfolio_service.py::risk_alert` → **dead**（import 與 call 在 production／`tests/`／`scripts/` 三區皆為 0）~~
  ~~⇒ **它那兩句文案今天印不出來，不能拿來當「這一側已經有東西」的憑據。**~~
  ~~⚠️ 模組 `services/portfolio_service.py` 本身 → **live**。~~
- ~~**按鈕字**：**需新增 1 顆送出鈕，字未定** —— 本草稿**不編一個鈕字**（§0.3「不准發明不存在的按鈕」）。~~
  ~~**定字時要套的三條（體例照 `docs/v2/22_ui_page_today.md` §5.1，本草稿不另立規矩）**：~~
  1. ~~鈕字收進 L0 SSOT `shared/ui_control_labels.py`（**實測現有 6 個常數**，釘 `0b08201`）；~~
     ~~⛔ **不得借用 `MACRO_LOAD_BTN_FIRST`／`MACRO_LOAD_BTN_AGAIN`** —— 那是載入閘門的鈕。~~
  2. ~~灰態指路**回讀 session 鍵**，⛔ 不手抄字面。~~
  3. ~~定完字之後照 §0.4 的判準標 ✓／⚠。~~

### 2.9 ①-6 🔎 詳細資料與說明（層 4 區頭）　※ **P0-6 的去處二**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ───────────────────────────── st.divider() ───────────────  〔沿用〕 │
│ ### 🔎 詳細資料與說明                                       〔沿用〕 │
│                                                                      │
│ 〔P0-6 去處二（客戶二選一）：把「宏觀象限 → 各資產類別歷史表現       │
│   中位數」接在本區頭底下〕                        〔這是我編的〕     │
│                                                                      │
│ ⛔ **這條路有一個 去處一 沒有的代價，客戶選之前要知道**：             │
│    (a) `B21` 那格 F2 豁免**只限 ①-4** ⇒ **併進本塊連字面都涵蓋不到**；│
│    (b) 本區頭底下**恰好五塊**，而守衛把「連續」一路判到畫面結尾      │
│        ⇒ 在這裡多一個一級區塊，守衛當場轉紅（見 §1.2 末段）。        │
│                                                   〔這是我編的〕     │
└──────────────────────────────────────────────────────────────────────┘
```

⚠️ **框內 (b) 那句「守衛當場轉紅」要照受限版本讀（2026-09-21，稽核 V 組 中-1 連帶）**：
那條守衛**只看得見 `_RECORDED_APIS` 那 11 個渲染 API**，`st.expander` **不在裡面**
（指令與輸出見 §1.2 末段的就地更正）。
⇒ **「多一個一級區塊」要成立，前提是那個區塊用字表內的 API 印出一個長得像 `## ` 或 `<h2>` 的標題。**
⛔ **(b) 的代價本身沒有被推翻** —— 去處二仍然落在守衛的切片內、去處一不在；
**被收窄的只有「一定會紅」這個強度**。
⚠️ **框內文字刻意未改**（刪除線符號在圍籬碼塊內不會被渲染成刪除線，逐行插入會把框線打散 ——
同 §2.8 的理由）；**本註即為該句的就地更正。**

- **目標狀態下的改變**：**區頭本身一字未改、恆現**；改變的是**它底下五塊由恆現改為預設收合**。
- **⛔ 母法衝突**：**無**（純版面分界）。
- **凍結面**：**F2**。**無豁免。**
- **live/dead**：本塊吃的是模組常數，無外部符號可判 ⇒ **不適用。**
- **按鈕字**：本塊無按鈕。

### 2.10 ①-7 🌳 長期座標　層 4 · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 🌳 長期座標                          〔沿用〕      │
│   （展開後內容、四支空狀態、兩支紅框逐字見現況線框 §1.7）  〔沿用〕  │
└──────────────────────────────────────────────────────────────────────┘
```

- **目標狀態下的唯一改變**：**由恆現改為預設收合。**
- **⛔ 母法衝突**：**無 ⛔；一處 ⚠️ 待判讀**（現況線框 `H-8`：`➖ 觀望 / 中性` 是燈號分級的名字，不是祈使句）。
  **本草稿不裁決，沿用現況線框的待判讀狀態。**
- **凍結面**：**F2** ＋ **F4**（`services/us_liquidity_engine.py`）。**無豁免。**
- **live/dead**：`services/us_liquidity_engine.py::fetch_us_liquidity_snapshot` → **`docs/v2/42_live_dead.md` 查不到**。
- **按鈕字**：既有展開器 `📋 原始讀數與來源` **⚠**（見 §6）。
  ⚠️ **收合之後這裡會變成「展開器裡的展開器」** —— 本草稿只登記這個結構，不裁決要不要攤平（§7 `N-4`）。

### 2.11 ①-8 📈 中期循環　層 4 · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 📈 中期循環                          〔沿用〕      │
│   （展開後由 `ui/tab1_macro_midcycle.py` 自印標題與內容）  〔沿用〕  │
│                                                                      │
│ 〔⛔ L3 情境判斷卡那兩句「策略：…」：母法衝突・待修。                │
│    本草稿**不提供**替代文案〕                    〔這是我編的〕      │
└──────────────────────────────────────────────────────────────────────┘
```

- **目標狀態下的唯一改變**：**由恆現改為預設收合。**
- **⛔ 母法衝突**：**兩句**（現況線框 `H-6`）—— 命中 **G3†** ＋ **B-2**（「衛星」是資產桶名）。
- **凍結面**：**F1**（`ui/tab1_macro_midcycle.py`）＋ **F2**（委派點）。
  ⭐ **`B28` 那格 F1 豁免涵蓋得到這兩句**（客戶裁示 5，**僅限 ①-8 那一處，`:401` 與 `:409` 兩行**）。
  ⛔ **射程不得外推**到該檔其餘部分、也不得外推到其他 15 個 `ui/tab*.py`。
- **live/dead**：`ui/tab1_macro.py::render_macro_tab` → **dead**（`docs/v2/42_live_dead.md`）。
  ⚠️ **這件事與本塊直接相關**：現況線框 §1.8 已登記 `render_mid_cycle_section` 內部 lazy import
  兩個**舊頁私有符號**（住在 `ui/tab1_macro.py`）⇒ **① 的新 View 仍在執行期相依一個入口已 dead 的模組**。
  ⚠️ 模組 `ui/tab1_macro.py` 本身 → **live**（`docs/v2/42_live_dead.md`：`tab6_manual`／`tab1_macro_*` 取用其 helper）。
- **按鈕字**：本塊無按鈕（被委派模組內部控制項不在本草稿射程內）。

### 2.12 ①-9 🎯 短線雷達　層 4 · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 🎯 短線雷達                          〔沿用〕      │
│   （10 燈全寬大表 ＋ 4 因子 3 欄網格 ＋ 五支空狀態逐字見現況線框 §1.9）│
│                                                       〔沿用〕       │
│ 〔⛔ 壓力研判那兩句尾句：母法衝突・待修。                            │
│    本草稿**不提供**替代文案〕                    〔這是我編的〕      │
└──────────────────────────────────────────────────────────────────────┘
```

- **目標狀態下的唯一改變**：**由恆現改為預設收合。**
- ⚠️ **收合帶來一個要一起決定的事，本草稿只登記**：10 燈走**全寬大表**（`wide_table`），
  而 `ui/helpers/ia/layout.py` 明文「`wide_table()` 不得放進 `card_row()` 的欄位裡」。
  **收合容器不是 `card_row`，所以這條不直接適用** —— 但「大表塞進收合列」的行為本草稿**沒有實測過**（§8）。
- **⛔ 母法衝突**：**兩句**（現況線框 `H-7`）—— 命中 **G3†**。
- **凍結面**：**F2** ＋ **F4**（`services/liquidity_engine.py`）。**無豁免。**
- **live/dead**：`services/risk_radar.py::_signal_yield_10y_shock` → **live**（`docs/v2/42_live_dead.md`）；
  `services/liquidity_engine.py::liquidity_verdict` → **`docs/v2/42_live_dead.md` 查不到**。
- **按鈕字**：本塊無按鈕。

### 2.13 ①-10 ⚠️ 拐點警報　層 4 · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### ⚠️ 拐點警報                          〔沿用〕      │
│   （導航塔 3 欄 ＋ 拐點中心 3 欄網格 ＋ 四支空狀態逐字見現況線框 §1.10）│
│                                                       〔沿用〕       │
│ 〔⛔ 拐點卡那兩處「布局」：一處母法衝突・待修、一處待判讀。          │
│    本草稿**不提供**替代文案〕                    〔這是我編的〕      │
└──────────────────────────────────────────────────────────────────────┘
```

- **目標狀態下的唯一改變**：**由恆現改為預設收合。**
- **⛔ 母法衝突**：**一處 ⛔ ＋ 一處 ⚠️ 待判讀**（現況線框 `H-9`）。
- **凍結面**：**F2** ＋ **F4**（`services/macro/turning_points.py`）。**無豁免。**
- **live/dead**：`services/macro/turning_points.py::detect_turning_points` → **`docs/v2/42_live_dead.md` 查不到**。
  ⚠️ **一筆與 P0-6 直接相關的「查了、沒問題」**：中位數的 machinery 就在這一塊的服務層裡
  （該檔已用 `np.median`，回出 `median_6m`／`median_12m`／`median_18m`）
  ⇒ **P0-6 缺的不是「中位數怎麼算」，是它的兩個輸入序列。**
- **按鈕字**：本塊無按鈕。

### 2.14 ①-11 🤖 AI 景氣判斷總結　層 4 · **預設收合**

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▸ 〔收合列〕#### 🤖 AI 景氣判斷總結                   〔沿用〕      │
│   （Form ＋ 完整率警語 ＋ 四支空狀態逐字見現況線框 §1.11） 〔沿用〕  │
│                                                                      │
│ 〔⛔ prompt 無條件要求模型輸出「下一步」那一節：母法衝突・待修。      │
│    本草稿**不提供**替代文案〕                    〔這是我編的〕      │
└──────────────────────────────────────────────────────────────────────┘
```

- **目標狀態下的唯一改變**：**由恆現改為預設收合。**
  ⚠️ **這一塊收合有一個別塊沒有的副作用**：它裡面有一顆**會產生內容的送出鈕**。
  收合之後使用者要先展開才看得到那顆鈕。**本草稿只登記，不裁決**（§7 `N-5`）。
- **⛔ 母法衝突**：**兩處**（現況線框 `H-10`）—— 命中 **G3†**（prompt 的例子本身就是三個買賣動作）。
  ⚠️ **動那支 prompt builder 會同時改到 ②-13**（~~同一個 builder、**兩個**消費端~~
  → **2026-09-21 就地更正：直接 importer 是 2 個，但其中一個是共用 widget，其下還有 5 個渲染站點**，見下方）。

  #### ⚠️ 就地更正：低估了 blast radius（稽核 V 組 中-7）

  （**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

  ⚠️ **先講清楚責任歸屬**：這個低估**不是本草稿新造的錯** ——
  `docs/v2/20_ui_spec.md`〈①-Z.4〉那一側就已經是「兩個消費端」的口徑，
  **本草稿忠實繼承了它**。**繼承一個低估仍然是低估**，所以就地改，並把來源講明。

  **實跑（釘 `0b08201`，輸出逐字照抄）**：

```
git grep -nE 'build_structured_summary_prompt' -- '*.py'
services/ai_prompts.py:16:- build_structured_summary_prompt — v18.214 通用：吃該 Tab 全章節快照,逐章節
services/ai_prompts.py:102:def build_structured_summary_prompt(*, tab_label: str, snapshot: str,
tests/test_ai_prompts.py:9:    build_structured_summary_prompt,
tests/test_ai_prompts.py:48:# v18.214 build_structured_summary_prompt — 逐章節白話結論 + 時事
tests/test_ai_prompts.py:50:def test_build_structured_summary_prompt_lists_sections_and_news():
tests/test_ai_prompts.py:51:    out = build_structured_summary_prompt(
tests/test_ai_prompts.py:73:def test_build_structured_summary_prompt_no_news_placeholder():
tests/test_ai_prompts.py:74:    out = build_structured_summary_prompt(
tests/test_ai_prompts.py:82:def test_build_structured_summary_prompt_empty_sections_safe():
tests/test_ai_prompts.py:83:    out = build_structured_summary_prompt(
ui/helpers/ai_summary.py:4:- 純 UI 層：呼叫 services/ai_prompts.build_structured_summary_prompt
ui/helpers/ai_summary.py:37:from services.ai_prompts import build_structured_summary_prompt
ui/helpers/ai_summary.py:109:            prompt = build_structured_summary_prompt(
ui/views/page_01_macro.py:146:from services.ai_prompts import build_structured_summary_prompt
ui/views/page_01_macro.py:1818:    本塊因此直接呼叫**服務層**的 `build_structured_summary_prompt` ／
ui/views/page_01_macro.py:1873:        _prompt = build_structured_summary_prompt(
exit=0
```

```
git grep -nE '^\s*render_ai_summary_widget\(' -- '*.py'
ui/helpers/ai_summary.py:21:    render_ai_summary_widget(
ui/helpers/fund_grp_health/ai.py:239:        render_ai_summary_widget(
ui/tab1_macro_ai.py:85:    render_ai_summary_widget(
ui/tab2_single_fund.py:2698:                    render_ai_summary_widget(
ui/tab3_portfolio.py:2306:        render_ai_summary_widget(
ui/tab3_portfolio.py:2558:    render_ai_summary_widget(
exit=0
```

  **逐行讀過之後的分類**：
  - **production 端真正 `import` 它的檔有 2 個**：`ui/helpers/ai_summary.py:37` 與 `ui/views/page_01_macro.py:146`
    （`services/ai_prompts.py` 是它**自己的定義檔**；`tests/test_ai_prompts.py` 是測試；
    `ui/helpers/ai_summary.py:4` 與 `ui/views/page_01_macro.py:1818` 是**註解／docstring**，不是呼叫）。
  - **第二個 importer 是共用 widget，不是一個畫面**：`ui/helpers/ai_summary.py` 匯出的
    `render_ai_summary_widget`，其下**有 5 個真實呼叫站點**
    （`ui/helpers/fund_grp_health/ai.py:239`／`ui/tab1_macro_ai.py:85`／`ui/tab2_single_fund.py:2698`／
    `ui/tab3_portfolio.py:2306`／`ui/tab3_portfolio.py:2558`）。
    ⚠️ **上面那條指令回 6 行，但其中 `ui/helpers/ai_summary.py:21` 在該模組的 docstring「使用範例」裡，不是呼叫** ——
    **6 行 ≠ 6 個站點，差在這一行。**

  ⇒ **改 `services/ai_prompts.py` 的「下一步」那一節（`:148-150`，經實測住在
  `:102` 的 `def build_structured_summary_prompt` 內），會一次改到
  ①-11 這個直呼站點 ＋ 上列 5 個 widget 站點，合計 6 個渲染站點。**
  ⚠️ **本段刻意寫「渲染站點」而不是「畫面」** —— `ui/tab3_portfolio.py` 自己就佔 2 個站點，
  **站點數與畫面數不是同一個東西**，本草稿沒有去數它們落在幾個畫面上。
  ⚠️ **本段刻意不寫「只有這 6 個」** —— 那取決於「有沒有漏看」（動態 import／間接包裝都掃不到）；
  **本段宣稱的只有上面兩條指令在 `0b08201` 上的輸出，以及逐行判讀的結果。**
- **凍結面**：**F2** ＋ **F4**（`services/ai_prompts.py`）。
  ⚠️ **這裡有一個兩種讀法並存的爭點，本草稿不選邊**：`docs/v2/20_ui_spec.md`〈①-Z.4〉把
  「`services/ai_prompts.py` 限 `:72-79`」列為客戶給的四張豁免之一；
  而 `docs/v2/21_decision_log.md` §H 第 4a 項逐字寫「**同時撞 F4 與 F1，而且這兩格今天都沒有豁免 → 兩格都要走提案單**」。
  ⇒ **「豁免是為了拆掉它」還是「豁免是為了在那裡動工」，兩種讀法都成立**（已登記於〈①-Z.7〉第 5 條）。
  ⚠️ **而且那個射程是 `:72-79`，本塊的衝突點不在那個區間**（現況線框 `H-10` 指的是同檔另一段）。
- **live/dead**：`services/ai_prompts.py` 相關符號 → **`docs/v2/42_live_dead.md` 查不到**。
- **按鈕字**：`▶️ 生成 AI 總結` **✓**／`🔄 重新生成 AI 總結` **⚠**（見 §6）。

---

## §3 P0 七項的落點 —— 逐項交代

| P0 項 | 落在哪一塊 | 新增一塊嗎 | 依據（客戶逐字） | 為什麼是這裡 |
|---|---|---|---|---|
| **P0-1** 四象限定位 | **①-3**（§2.3） | **否** | 「P0-1 四象限 → 併入 ①-3」 | ①-3 是「憑什麼這樣說」的依據表，象限是同一種「這一輪處在什麼狀態」的陳述 |
| **P0-2** 政策利率路徑 | **新-1**（§2.5） | **＋1**（與 P0-3 合成） | 「P0-3 實質利率 → 併入 P0-2」 | 兩項都是利率主題；合成的是版面，**判定各自獨立** |
| **P0-3** 實質利率 | **新-1**（§2.5） | 同上 | 同上 | 同上 |
| **P0-4** 美元週期 | **新-2**（§2.6） | **＋1** | **客戶未點名合併** | ⛔ **本草稿不替客戶併**；~~可行併法與代價見 §1.3~~ → **§1.3 整節已因裁示 A 退場（不用選併法）** |
| **P0-5a** ERP | **新-3**（§2.7） | **＋1**（與 5b 合成） | 「P0-5a/5b → 合成「跨資產相對價值」」 | 合成的是版面；**兩半結論不同，框內刻意分兩個子框** |
| **P0-5b** 信用利差 vs 歷史 | **新-3**（§2.7） | 同上 | 同上 | 同上 |
| **P0-6** 配置信號表（新定義） | **①-4**（§2.4）**或** **①-6**（§2.9） | **否** | 「P0-6 配置信號表 → 併入 ①-4 或 ①-6」 | ⛔ **客戶二選一，本草稿兩個去處都畫、不代選**；兩者的代價差異見 §2.9 框內 |
| **P0-7** 情境分析 | ~~**新-4**（§2.8）~~ → ⛔ **不落在 ①**（客戶 2026-09-21 裁示 B） | ~~**＋1**（僅路線 B-進①）~~ → **0** | ~~**客戶未點名合併**~~ → **「B：P0-7 情境分析「不進 ①」。」** | ⛔ ~~**先要決定它進不進 ①**（決定 B，§1.4）；進了才談塊數~~ → **已裁：不進**。**它落在哪一頁仍屬客戶的範圍決定，本草稿不判**（§1.4） |

**合計**：新增 **3**、總 **13** —— 與客戶裁示 A 逐字「新增 3 → 總數 13」一致。
~~**合計**：路線 **A-14 ＋ B-進①** ⇒ 新增 **4**、總 **14**。~~
~~路線 **B-不進①** ⇒ 新增 **3**、總 **13**（§1.3 丁）。~~
⚠️ **上面兩行劃掉、新的一行不劃** —— 依 `CLAUDE.md §-2.A` 的既有慣例，
**「路線 A-14／B-不進①」是被權衡掉的政策敘述（劃掉保留），而「新增 3、總 13」是描述內容的計數（就地改、不劃掉）。**

⚠️ **「併入」是版面歸屬，不改五欄判定** —— 上表每一項的資料源／計算／畫面／撞凍結面／母法衝突
**一格都沒有因為換容器而改變**。五欄的內容以 `docs/v2/20_ui_spec.md`〈①-Z.5〉為準，本檔不重抄、也不改一格。

⚠️ **①-3／①-4／①-6 三塊都住在同一個檔** ⇒ 同一格 **F2**；
而 `B21` 那格豁免**只限 ①-4** ⇒ **併進 ①-3 或 ①-6 都沒有豁免可用。**

---

## §4 母法衝突總表

> **本表只登記，不改規格、不改任何 `.py`、不提供任何替代文案。**
> ⚠️ **`G1†`／`G2†`／`G3†` 的 `†` 定義見 §0.1** —— 引自子法 1 `:580-582`，**母法原文查不到**。

| 落點 | 現況引文・待修（**原樣轉錄，不是本草稿的建議**） | 命中 | 現況線框代號 | 目標狀態下的狀態 |
|---|---|---|---|---|
| **①-1 抬頭** | 「現在該加碼還是防禦」 | ⛔ **G3†** | `H-3` | **待修，且進了預設展開區** —— 它是使用者第一眼 |
| **①-1 本文** | `services/macro/action_light.py` 五句 `action` | ⛔ **G3† ＋ B-2** | `H-4` | 同上 |
| **①-3 表下註記** | `services/macro/composite_score.py` 五句 `action_text` | ⛔ **G3† ＋ B-2** | `H-5` | 待修；本塊改為**預設收合** |
| **①-4 卡 1** | 股／債／現金三個百分比 ＋ 停利／加碼兩個門檻 | ⛔ **G1† ＋ G3†** | （現況線框已標） | **待修，且進了預設展開區**；⭐ **唯一有豁免的一塊** |
| **①-8 情境判斷卡** | 兩句「策略：…」 | ⛔ **G3† ＋ B-2** | `H-6` | 待修；⭐ **有 `B28` F1 豁免**（僅限那兩行） |
| **①-9 壓力研判** | `liquidity_verdict()` 兩支的尾句 | ⛔ **G3†** | `H-7` | 待修；本塊改為**預設收合** |
| **①-10 拐點卡** | 「⚠️ 衰退末期，布局反彈」 | ⛔ **G3†** | `H-9` | 同上 |
| **①-11 AI 總結** | prompt 無條件要求「下一步」那一節 | ⛔ **G3†** | `H-10` | 待修；⚠️ ~~**同一支 builder 也餵 ②-13**~~ → **那支 builder 下游共 6 個渲染站點**（§2.14 就地更正） |
| **決策矩陣指路句** | 「📋 **逐檔的加減碼建議請到 …**」 | ⛔ **G3†**，且**守衛正在強制它存在** | `H-11` | 待修；⚠️ **文案與守衛必須同一批改** |
| 頁首麵包屑／頁首 caption | 「看懂景氣位階,決定加碼或防禦」／「現在市場環境該進攻還是防守？」 | ⚠️ **待判讀** | `H-1`／`H-2` | **本草稿不裁決，沿用待判讀** |
| ①-7 子指標判讀 | 「➖ 觀望 / 中性」 | ⚠️ **待判讀** | `H-8` | 同上 |
| ①-10 拐點卡（第二半） | 「…為股市底部布局期」 | ⚠️ **待判讀** | `H-9` 後半 | 同上 |

**新增塊的母法衝突**（本草稿新判，**未經第二組驗證**）：

| 新增塊 | 命中 | 判定依據 | 邊界（踩到就落回 G3†） |
|---|---|---|---|
| **新-1** 政策利率路徑 ＋ 實質利率 | **無** | 市場隱含機率與實質利率都是客觀讀數 | 不得以「市場預期降息 ⇒ 宜加碼債券」收尾 |
| **新-2** 美元週期 | **無** | 方向與歷史分位都是客觀讀數 | 不得在資產類別旁掛 `↑`／`↓`，不得用紅綠標成該買該賣 |
| **新-3** 跨資產相對價值 | **無** | **G2†** 明列允許的「客觀對照」 | 不得以「⇒ 股票相對便宜，宜加碼」收尾 |
| ~~**新-4** 情境分析~~　⛔ **不進 ①**（裁示 B） | ~~**無 —— 七項裡唯一被點名允許的**~~ | ~~**G2†** 明列允許的「情境試算」~~ | ~~⛔ **三個衝擊幅度由使用者自行輸入**（G3† 硬條件逐字）；輸出不得以處置動作收尾~~　⚠️ **本列的判定換頁之後照樣成立，見 §2.8** |
| **P0-1 接進 ①-3** | **條件性** | 只印 `quadrant`／`growth_dir`／`inflation_dir` ⇒ 無 | 一併印 `quad_desc`／`quad_alloc` ⇒ **G1† ＋ G3†**；**2026-09-21 補**：一併印 `quad_color`／`quad_icon` ⇒ **命中追加裁示 1 的呈現層硬規則**（§2.3 就地更正 B） |
| **P0-6 接進 ①-4 或 ①-6** | **無（依客戶裁示 2）** | 「符合 G2†，不撞 G3†」 | **符合 G1† 的前提是：只顯示歷史中位數，不附暗示**（追加裁示 1） |

⛔ **本草稿沒有替任何一處寫替代文案。** 替代文案要等客戶對上表裁決之後才寫。

---

## §5 凍結面總表

**凍結格定義（引自子法 1 §1.1）**：**F1** 16 個 `ui/tab*.py`／**F2** 5 個 `ui/views/page_0*.py`／
**F3** `app.py`／**F4** `services/**`／**F5** 資料表 Schema／**F6** 遷移歷史鏈。
⚠️ **`repositories/**` 與 `shared/**` 不在 F1~F6 任一格**；⚠️ **`ui/helpers/**` 與 `tests/` 也不在**。

**現有豁免（四張，逐張寫明射程）**：

| 豁免 | 射程 | 出處 |
|---|---|---|
| `B21` | **F2**，`ui/views/page_01_macro.py` **僅限 ①-4 那一處** | `docs/v2/21_decision_log.md` B21／B21-a |
| `B8` | **F2／F3**，經客戶 2026-09-21 裁定為「**窄格，不通格**」，僅限規格已登記的改動 | `docs/v2/21_decision_log.md` §H-2 |
| `B28` | **F1**，`ui/tab1_macro_midcycle.py` **僅限 ①-8 那一處（兩行）** | `docs/v2/21_decision_log.md` B28 |
| （第四張） | `services/ai_prompts.py` 限 `:72-79` —— ⚠️ **客戶沒有標 F 編號，且與 §H-4a 有兩種讀法** | `docs/v2/20_ui_spec.md`〈①-Z.4〉／〈①-Z.7〉第 5 條 |

⛔ **三張已編號的豁免互不涵蓋，射程不得互相借用**（`B21-a` 已釘死的紀律）。

| 塊 | 渲染端 | 文案／計算產出端 | 豁免射得到嗎 |
|---|---|---|---|
| ①-1 | **F2** | **F4**（`services/macro/action_light.py`） | **射不到** |
| ①-2 | **F2** | **F4** | **射不到** |
| ①-3（＋P0-1） | **F2** | **F4** ＋ **不在 F1~F6**（`ui/helpers/macro/beginner_view.py`） | **射不到** —— `B21` 只限 ①-4 |
| **①-4（＋P0-6 去處一）** | **F2** | **F4**（`services/allocation_ladder.py`） | ⭐ **渲染端射得到（`B21`）**；⚠️ **F4 那一半不在豁免裡，要動必須提案** |
| **新-1** | **F2** | 取數端 `repositories/**` **不在 F1~F6**；L2 編排 ⇒ **F4** | **射不到** |
| **新-2** | **F2**；⚠️ 若改在既有那一列 ⇒ **F1** | **F4** | **射不到** —— `B28` 只限 ①-8 兩行 |
| **新-3** | **F2** | **F4**；新 series／fetcher **不在 F1~F6** | **射不到** |
| ~~**新-4**~~　⛔ **不進 ①**（裁示 B） | ~~**F2** ＋ 持倉來源 **F1** ＋ 試算邏輯 **F4**~~ | ~~同左~~ | ~~**三格全撞，一格都射不到**~~　⚠️ **那是針對 ① 的撞擊分析，換頁要重算**（§2.8） |
| ①-6（＋P0-6 去處二） | **F2** | 本檔常數 | **射不到** —— `B21` 只限 ①-4 |
| ①-7 | **F2** | **F4**（`services/us_liquidity_engine.py`） | **射不到** |
| **①-8** | **F1** ＋ **F2**（委派點） | **F1** | ⭐ **射得到（`B28`，僅限兩行）** |
| ①-9 | **F2** | **F4**（`services/liquidity_engine.py`） | **射不到** |
| ①-10 | **F2** | **F4**（`services/macro/turning_points.py`） | **射不到** |
| ①-11 | **F2** | **F4**（`services/ai_prompts.py`） | ⚠️ **兩種讀法並存，本草稿不選邊**（§2.14） |
| 決策矩陣指路句 | **F2** | 本檔文案 ＋ **`tests/` 不在 F1~F6** | **射不到**；⚠️ **文案與守衛必須同一批改** |
| 總經燈號全表 | **F2** | 本檔文案 | **射不到** |

⚠️ **一個目標狀態才會出現的凍結面事實，寫在這裡**：
「把既有塊改成預設收合」這個動作**本身**就要改 `ui/views/page_01_macro.py` ＝ **F2**，
而**那不是 ①-4 那一處** ⇒ **`B21` 射不到它**。
⇒ **裁示 1 的「預設收合」要落地，① 這一頁需要一張今天還不存在的豁免，或走提案單。**
⛔ **本草稿只指出這件事，不主張要開豁免**（開不開是客戶的決定）。

⚠️ **F5／F6 兩格本表從頭到尾沒有評估過 —— 就地揭露（稽核 V 組 中-5）**
（**有意識的補充，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）：
本表對 **新-1／新-2／新-3** 三列都寫了「新 series／新 fetcher 落在 `repositories/**` ⇒ **不在 F1~F6**」，
**那個結論抄自 `docs/v2/20_ui_spec.md`，但抄的時候把它的但書丟掉了**。
**來源的但書逐字（釘 `0b08201`）**：

```
git grep -c '但 F5「資料表 Schema」會不會涵蓋' -- 'docs/v2/20_ui_spec.md'
docs/v2/20_ui_spec.md:1
exit=0
```

```
sed -n '1946p' docs/v2/20_ui_spec.md
9. **沒有做 F5（資料表 Schema）與 F6（遷移歷史鏈）的撞擊分析。** 七項若需新欄位／新快取檔，可能吃到 **F5**，**本節完全沒查**。
exit=0
```

（`:1673` 是 P0-6 那格「撞凍結面」，末句逐字是：
「⚠️ **但 F5「資料表 Schema」會不會涵蓋 `data_cache/*.parquet` 的欄位，本節沒查**（見〈①-Z.8〉第 9 條）。」
**該行整行過長，此處只引末句；上面那條 `-c` 指令供複核它確實在該檔且只出現一處。**）

⇒ **新-1／新-2／新-3 三塊全都要新 FRED series ＋ 新 L1 fetcher，正是「可能吃到 F5」的那一類**
（新 series 要不要落進 `data_cache/*.parquet`、落進去算不算改 Schema —— **本草稿同樣沒查**）。
⛔ **「不在 F1~F6」這句話在本表的正確讀法是「不在 **F1~F4**」** ——
**F5／F6 沒有被評估過，不是被評估後排除。**
⚠️ **本草稿不替它補判**（同 §8.3 第 6 條的立場），**只把來源的但書補回來**，
以免下一個人照本表的「不在 F1~F6」動工，以為那兩格已經查過。
（**同一筆已補進 §8.3 第 13 條**，那裡是本草稿「沒做什麼」的正式清單。）

---

## §6 按鈕字總表

> **判準見 §0.4（沿用現況線框 §0.1，那條判準是前一組自訂的，不是客戶給的）。**

| # | 落點 | 類型 | 逐字 | 動詞 | 全長 | 括號 | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 載入閘門（首次） | **送出鈕** | `📡 載入總經資料` | 載入（2）✓ | 6 ✓ | 無 ✓ | **✓** |
| 2 | 載入閘門（已載入） | **送出鈕** | `🔄 更新總經資料` | 更新（2）✓ | 6 ✓ | 無 ✓ | **✓** |
| 3 | ①-11 AI（首次） | **送出鈕** | `▶️ 生成 AI 總結` | 生成（2）✓ | 5 ✓ | 無 ✓ | **✓** |
| 4 | ①-11 AI（已生成） | **送出鈕** | `🔄 重新生成 AI 總結` | **重新生成（4）⚠** | 7 ✓ | 無 ✓ | **⚠**（動詞 4 字 > 3） |
| 5 | ①-7 原始讀數 | 展開器 | `📋 原始讀數與來源` | **無動詞 ⚠** | 7 ✓ | 無 ✓ | **⚠**（名詞片語開頭） |
| 6 | ①-3 註記全文 | 展開器 | `🔍 各列判讀門檻全文(上表「說明」欄的完整版)` | **無動詞 ⚠** | **20 ⚠** | **有 ⚠** | **⚠**（三項全不過） |
| ~~**7**~~ | ~~**新-4 情境分析送出鈕**~~　⛔ **不進 ①**（裁示 B） | ~~**送出鈕**~~ | ~~**需新增，字未定**~~ | ~~—~~ | ~~—~~ | ~~—~~ | ~~**不判** —— 定字時照 §0.4 套~~　⚠️ **那三條定字規矩換頁之後照樣適用**（§2.8） |

**計數**：送出鈕 **4 顆**（**全部既有**，本輪不新增）／展開器 **2 個**。
**既有 6 顆的判定：✓ 3／⚠ 3**（與現況線框 §3 一字未改）。
⚠️ **客戶 2026-09-21 裁示 B 之後，第 7 列退場 ⇒ ① 這一頁的控制項計數與現況線框相同。**
~~**計數（路線 B-進①）**：送出鈕 **5 顆**（4 顆既有 ＋ 1 顆需新增）／展開器 **2 個**。~~
~~**路線 B-不進①**：第 7 列不存在，計數與現況線框相同。~~

⚠️ **目標狀態新增一類控制項：收合列本身。**
「預設收合」在 Streamlit 上最接近的元件是 `st.expander`，**而它的標籤也是使用者會讀的字**。
⛔ **本草稿不替每一塊編收合列的字** —— 那些字今天不存在，編了就是發明按鈕（§0.3）。
**本草稿的主張**：收合列的字**直接沿用該塊的既有標題**（例：`🌳 長期座標`、`📈 中期循環`），
**不另編一套** —— 另編就會出現「收合時叫 A、展開後標題叫 B」的兩份名字。
⚠️ **這個主張會撞到一條既有守衛**，本草稿只登記（§7 `N-6`）。
⚠️ **2026-09-21 補一件相關、但方向相反的事（稽核 V 組 中-1）**：
`st.expander` **正是** `tests/test_wf01_detail_zone_order.py` 那條「連續」守衛**看不見**的元件
（該檔的 `_RECORDED_APIS` 字表**不含** `expander`，且同檔明文「`st.expander("…")` **刻意不算**一級區塊」——
指令與輸出見 §1.2 末段的就地更正）。
⇒ **「收合列用 expander」不會被那條守衛擋住，但 `N-6` 的字面值錨是另一支守衛，仍然要處理。**
⛔ **兩支守衛不要混為一談** —— 哪一支會轉紅，見 §7 `N-6` 的 2026-09-21 就地更正。

---

## §7 本草稿新登記的矛盾（**只登記，不回頭改規格**）

> ⛔ **依客戶止損規則：草稿階段遇到新矛盾只登記。** 例外是母法衝突與凍結面衝突 ——
> 那兩類已分別集中於 §4 與 §5，並在回報中點名。

| # | 矛盾 | 證據 | 為什麼是矛盾 |
|---|---|---|---|
| **N-1** | **裁示 1 的兩半用了兩把不同的顆粒度尺** | 前半「結論燈 + 3 張核心卡（**4 塊**）」⇒ 一張卡算一塊；後半的被加數「① 現有 10 塊」出自 `docs/v2/20_ui_spec.md` §1.5，而該節第 2 條逐字「同一次卡片網格呼叫產出的多張卡，是**同一塊**裡的多張卡，不是多個區塊」⇒ ①-4 那三張算 1 塊 | 同一份裁示裡，**三張卡前半數成 3、後半數成 1**。「總塊數 13～14」的算式是用後半那把尺算的；若改用前半那把尺，被加數與總數都要重算 |
| **N-2** | **客戶的塊數帳看不見三個渲染物** | 規格塊尺 ⇒ ① 今天 10 塊；渲染物尺 ⇒ 13 個（多出**載入閘門 Form**、**決策矩陣指路句**、**總經燈號全表**）。兩者差 3 | 客戶說「總塊數 13～14」。**2026-09-21 裁示 A／B 之後**：用規格塊尺是 **13**，用渲染物尺是 **16**（~~14~~／~~17~~ 是新增 4 塊時的數字，**計數就地改、不劃掉政策**）。⛔ 本草稿不選口徑、也不主張把那三個收進規格 |
| **N-3** | **「P0-6 需要使用者持倉」這句話已經被裁示 2 推翻，但沒有人回頭改它** | `docs/v2/20_ui_spec.md`〈①-Z.3〉末段逐字「其中 **P0-6／P0-7** 需要使用者持倉」；而**裁示 2** 已把 P0-6 換成「宏觀象限 → **各資產類別**歷史表現中位數」，其兩個輸入是「歷史象限序列」與「各資產類別歷史報酬序列」，**兩個都是市場層資料** | 那句話寫在**換定義之前**，換定義那一輪只把旁邊「不是 13 塊上限」加了刪除線，**沒有回頭檢查這半句**。⇒ 後人照它派工，會把一個市場層的東西當成持倉層的擋掉。**對照 `CLAUDE.md §2.1`「TW 出口 YoY」那條記載過的同一個病：已被推翻、卻沒被撤下的記載，比沒查證的更危險** |
| **N-4** | **「預設收合」會製造展開器裡的展開器** | ①-7 內有既有展開器 `📋 原始讀數與來源`；①-3 內有既有展開器 `🔍 各列判讀門檻全文(…)`。兩塊在目標狀態下都是預設收合 | 使用者要點兩層才看得到原始讀數。**這是版面決定，本草稿不裁決要不要攤平** |
| **N-5** | **①-11 收合之後，那顆會產生內容的送出鈕也一起被收起來** | ①-11 內有 `▶️ 生成 AI 總結`／`🔄 重新生成 AI 總結` 一顆送出鈕（依 session 擇一） | 其餘收合塊裡只有唯讀內容，**只有這一塊收的是一個互動入口**。⛔ 本草稿不裁決 |
| **N-6** | **收合列沿用既有標題的主張，會撞到一條既有守衛** | `tests/test_wf01_detail_zone_order.py` 有一條「五塊標題必須逐字等於線框名字」的字面值錨，另一條把「連續」一路判到畫面結尾 | 若收合列**也是**一個 H2~H4 標題，五塊的標題序列會多出東西；若收合列**不是**標題，~~那條字面值錨就對不到標題~~ → **2026-09-21 就地更正：歸因指錯了支，見表下**。**兩種做法都要動守衛**，而 `tests/` 不在 F1~F6 ⇒ 不必開豁免，**但必須同一批改** |
| **N-7** | **「預設收合」本身就是一次 F2 改動，而今天沒有一張豁免射得到它** | §5 末段 | 裁示 1 是版面裁示，但落地要動 `ui/views/page_01_macro.py` 的**非 ①-4 部分**，`B21` 只限 ①-4 ⇒ **裁示與凍結令在這裡對上了**。⛔ 本草稿只指出，不主張開豁免 |
| **N-8** | **新塊的位置被守衛鎖死在區頭之前，而裁示 1 沒有講位置** | `tests/test_wf01_detail_zone_order.py::test_detail_zone_renders_its_five_sections_in_order_and_contiguous` 的「連續」判到畫面結尾；`_zone_openers()` docstring 逐字「**區內與區後不得再冒出別的一級區塊**」 | 裁示 1 只講「新增幾塊」與「併到哪一塊」，**沒有講新塊畫在哪個位置**。而守衛已經把「區頭之後」整段鎖住 ⇒ **位置不是自由的** |
| **N-9** | **P0-6 的兩個去處，豁免狀態不對稱，而裁示寫成二選一** | 「併入 ①-4 **或** ①-6」；`B21` 只限 ①-4 ⇒ 去處一在豁免內、去處二完全在豁免外，且去處二還會撞 `N-8` 那條守衛 | 一個二選一的選項，兩邊的**技術代價差一個數量級**。⛔ 本草稿兩個都畫、不代選，只把代價寫在 §2.9 框內 |
| **N-10**<br>（**2026-09-21 新增**，稽核 V 組 中-4） | **裁示 1 的去處安排，會讓同一個宏觀象限值同時出現在兩塊 —— 而那一組重複第一次真的並排出現，就是在本草稿的圖上** | `docs/v2/20_ui_spec.md:2232` 已登記此事，出處是客戶禁令「**不准重複的數字出現在兩塊**」；而本草稿 §2.3 把 **P0-1（印 `quadrant`）** 併進 **①-3**、§2.4／§2.9 把 **P0-6（左鍵就是「宏觀象限」）** 併進 **①-4 或 ①-6** | 兩處印的是**同一個象限值**，只是一邊當定位、一邊當對照表的左鍵。**規格端只登記了「會發生」，本草稿是第一份把它畫成並排版面的文件** —— 在圖上它不再是抽象風險。⛔ **只登記，不裁決**（要不要去重、去哪一邊，是客戶的版面決定）；體例同上列九條 |

#### ⚠️ `N-6` 就地更正：歸因指錯了守衛（稽核 V 組 中-8）

（**有意識的更正，不是漏刪** · 日期 **2026-09-21** · 決策者 **AI 總管**）

**`N-6` 的結論（兩種做法都要動守衛）方向仍然成立，被更正的是「哪一支會轉紅」。**

**舊表述**說：收合列若**不是**標題，「那條**字面值錨**就對不到標題」。
**實際不會** —— 那條字面值錨比對的是**模組常數**，不是畫面輸出：

```
sed -n '475,486p' tests/test_wf01_detail_zone_order.py
def test_detail_section_titles_match_the_wireframe_literals():
    """五塊標題必須逐字等於線框 section 03 的名字。

    **突變會紅**：把 `_DETAIL_TITLES` 任一個值改名 → 本條紅
    （前一版的守衛對這個突變 8 passed 全綠）。

    ⚠️ 本條是**唯一**不從產品碼推導期望值的一條 —— 它就是那根樁。
    其餘各條刻意從 `_DETAIL_ZONE` / `BUCKET_ORDER` 推導（不抄第二份順序），
    但**全部推導 ＝ 全部同源 ＝ 一起漂移**，所以名字這一項必須有字面值。
    """
    _got = tuple(_t for _k, _t, _r in _page._DETAIL_ZONE)
    assert _got == _WIREFRAME_TITLES, (
exit=0
```

⇒ 它讀的是 `_page._DETAIL_ZONE` 這個**模組常數**。
**換渲染方式（把標題包進 `st.expander`）不會動到那個常數 ⇒ 這一支不會紅。**

**真正會紅的是讀實際輸出的那一支**：

```
sed -n '291,292p' tests/test_wf01_detail_zone_order.py
    _zone = _zone_openers(_render_and_record(loaded=loaded))
    _want = [_page._DETAIL_HEADING] + [_t for _k, _t, _r in _page._DETAIL_ZONE]
exit=0
```

⇒ `test_detail_zone_renders_its_five_sections_in_order_and_contiguous` 走
`_render_and_record()`（`:148` 的 `def _render_and_record(*, loaded: bool)`），**讀的是渲染時真的被呼叫的那些 API**。
**收合列若讓五塊標題不再以字表內的 API 印出來（或多印一個一級標題），紅的是這一支。**

⚠️ **但這一支同樣有受限版本的問題**（§1.2 末段）：
它只看得到 `_RECORDED_APIS` 那 11 個 API，**`expander` 不在裡面**。
⇒ **`N-6` 的正確讀法**：**兩種做法都要動守衛這個結論不變**，
但**要動的是哪一支、會不會真的轉紅，取決於收合列用什麼元件做、塊標題還印不印** ——
**那是實作決定，本草稿不預判。**
⚠️ **本段是讀守衛原始碼得到的，沒有跑過那兩支測試**（§8.3 第 2 條）。

#### 📌 `N-10` 的補充：這一筆是**本草稿漏登的第十條**，不是新發現的事實

**規格端早就登記過了**（`docs/v2/20_ui_spec.md` 盲點清單第 12 條），**實跑照抄（釘 `0b08201`）**：

```
sed -n '2231,2232p' docs/v2/20_ui_spec.md
12. **裁示 1 的去處安排會讓同一個宏觀象限值同時出現在兩塊，本節沒有登記。**
    **出處**：`F_audit_compliance.md`〈C. 客戶禁令〉→「「不准重複的數字出現在兩塊」：裁示 1 的去處安排產生一組未登記的重複」。**嚴重度：低**。
exit=0
```

⛔ **為什麼它非登不可**：`N-1`～`N-9` 九條裡，**唯獨這一條在規格端已經有出處，而本草稿沒有接住它** ——
而本草稿正是**第一份把 P0-1 與 P0-6 畫在同一張圖上**的文件，
**那組重複在這裡第一次從「會發生」變成「畫面上真的並排」**。
⛔ **本條只登記、不裁決**（體例同其餘九條）：要不要去重、去掉哪一邊，
是**版面決定**，屬客戶（`CLAUDE.md §-1.5.4`：版面異動一律草稿先行、客戶拍板）。
⚠️ **本草稿也不宣稱「① 上只有這一組重複」** —— 那取決於「有沒有漏看」；
**本條宣稱的只有上面那條指令的輸出，以及 §2.3／§2.4／§2.9 三處圖上的並排事實。**

---

## §8 本草稿的方法與射程（誠實揭露）

### 8.1 方法

1. **先逐份讀完素材，再落筆。** 讀過：`docs/v2/23_ui_draft_01_macro.md`（現況線框全檔）、
   `docs/v2/20_ui_spec.md` 的 `## ① 市場總覽` 一節與 `## ①宏觀配置鏈缺項清單（2026-09-21）` 全節、
   `docs/v2/42_live_dead.md`（§0～§6）、`docs/v2/22_ui_page_today.md`（全檔）、
   `docs/v2/prototype/ui_prototype_today.html`（結構與 ⛔ 標記法）。
2. **落筆前把本檔引用的 `檔案::符號` 逐一實測一次**，確認它指得到東西。
   做法：以 Python 逐一開檔、用正規式找該符號的定義（`def`／`class`／模組級賦值），
   **不抽驗**。結果與指令見 §8.2 (a)。
3. **live/dead 一律查 `docs/v2/42_live_dead.md`，不自行重算。**
   客戶裁示逐字：「遇到死碼先查本檔，不重新確認」。**查不到就寫「查不到」**（該表自陳是「子集的子集」，不是母體）。
4. **客戶原話逐字碼塊照抄，碼塊內的 `G1`~`G3` 刻意不標 `†`**（在逐字引文裡加註記就是竄改原話）。
5. **兩個範圍決定（A／B）一律並列畫出來，不代選。** 列出「技術上哪幾種可行、各自代價是什麼」，
   **列選項不等於替客戶選**。
6. **掃描一律附正控與負控**；負控為本組自訂隨機串，未列出 → 0 命中。

### 8.2 實測記錄（指令與真實輸出，**全部釘 `0b08201`**）

**(a) 路徑與符號全查，不抽驗** —— 本檔正文裡凡是反引號內「像檔案路徑」的 token，逐一開檔驗證三件事：
檔案在不在、行號有沒有超出檔尾、`::符號` 是不是真的定義在那個檔裡。

```
refs: 57  OK: 55  problems: 2
  !! ('docs/v2/CONSTITUTION.md', '', '', 'FILE MISSING')
  !! ('repositories/external_market_repository.py', 'fetch_yf_forward_pe', '', 'present but NOT a definition')
```

**兩個不成立的，兩個都是本檔自己已經寫明的狀態，不是錯字**：

- `docs/v2/CONSTITUTION.md` **不在本工作分支上**（§8.3 第 5 條已寫）。實測：

```
git ls-tree -r --name-only 0b08201 | grep -c 'docs/v2/CONSTITUTION.md'
0        exit=1
git ls-tree -r --name-only 0b08201 | grep -c 'docs/v2/20_ui_spec.md'     # 正控，刻意不是受測目標
1        exit=0
```

- `fetch_yf_forward_pe` 在 `docs/v2/42_live_dead.md` 是 **已退役**（2026-08-28 經客戶授權整段刪除），
  該檔今天只留一則退役註記、**沒有定義** —— **這正是「已退役」與「dead」的差別**。實測：

```
grep -n 'fetch_yf_forward_pe' repositories/external_market_repository.py
13:⚠️ 2026-08-28 退役(**有意識的移除,不是漏刪**):`fetch_yf_forward_pe` /
```

**⚠️ 這一查抓到一處本草稿自己的真錯，已就地改正**：初稿把 `build_evidence_rows`
寫成住在 `services/macro/evidence.py`，實測它定義在 `ui/helpers/macro/beginner_view.py`：

```
git grep -n 'def build_evidence_rows' -- '*.py'
ui/helpers/macro/beginner_view.py:830:def build_evidence_rows(
```

⚠️ **本檢查驗的是「指得到東西」，不驗「那個東西是不是它自稱的那個」** ——
同 `CLAUDE.md §2.1` 記載的那個射程外缺口（守衛只驗檔案在不在、行號界不界內）。

**(b) ① 現有規格塊數（嚴寫法，刻意帶 `[0-9]`）**：

```
grep -cE '^### ①-[0-9]+' docs/v2/20_ui_spec.md
10
```

⚠️ **刻意不用鬆寫法 `'^### ①-'`** —— 那會把 `①-Z.*` 那些小節一起數進去。

**(c) ① 頁那句版面定位出現幾處**：見 §1.4 的指令與輸出（**2 處**，不是 1 處）。

**(d) P0-6／P0-7 在 ② 與 ④ 的掃描**：見 §1.4 的三條指令與輸出（含正控）。

**(e) 五個線上頁的數量**：

```
ls ui/views/page_0*.py | wc -l
5
```

**(f) 按鈕字 SSOT 的常數數量**：

```
grep -cE '^[A-Z_]+: str' shared/ui_control_labels.py
6
```

**(g) 守衛把「連續」判到畫面結尾** —— `tests/test_wf01_detail_zone_order.py` 的 `_zone_openers()`：

```
sed -n '223,228p' tests/test_wf01_detail_zone_order.py
def _zone_openers(openers: list[str]) -> list[str]:
    """從詳細區的區頭那一格起，取到最後 —— 區內與區後不得再冒出別的一級區塊。"""
    assert _page._DETAIL_HEADING in openers, (
        f"畫面上找不到詳細區的區頭「{_page._DETAIL_HEADING}」；"
        f"實際印出的一級標題：{openers}")
    return openers[openers.index(_page._DETAIL_HEADING):]
exit=0
```

⚠️ **2026-09-21 就地更正（本輪自查，稽核未點名；有意識的更正，不是漏刪 · 決策者 AI 總管）**：
舊表述在指令底下寫的是 ~~`→ docstring 逐字「…」`~~ —— **那是摘要，不是那條指令的輸出**
（與稽核 V 組 低-3、以及 §1.4 負向 B 那一處是**同一類**缺陷）。
**本輪改成逐字照抄六行並補 `exit=`。**
⚠️ **摘要裡引的那一句 docstring 本身逐字為真**（它就在輸出的第 2 行），
**被更正的是「這是輸出」這個宣稱**，不是那句話的內容。

**(h) 表單站點總數鎖**：

```
grep -n 'FORM_SITE_TOTAL = ' tests/test_ui_rerun_contract.py
327:FORM_SITE_TOTAL = 7   # 2026-08-31：5 → 6（元件 B 門檻列包 form）；
exit=0
```

⚠️ **2026-09-21 就地更正（稽核 V 組 低-3；有意識的更正，不是漏刪 · 決策者 AI 總管）**：
舊表述把輸出截在 `FORM_SITE_TOTAL = 7` 就停了，**把同一行尾端的註解吃掉**，
於是它**不是逐字輸出，是一個被修剪過的版本**。
**數值 7 沒有錯**，錯的是「這是那條指令印出來的東西」這個宣稱 ——
照跑的人會拿到比文件多一截的字串，然後不確定文件是不是還漏了別的。
**本輪改成逐字照抄並補上 `exit=`**（同本檔其餘實測區塊的體例）。

### 8.3 射程 —— 本草稿**沒有**做什麼

1. **沒有開過瀏覽器，一次都沒有。** 「收合之後長什麼樣」「大表塞進收合列會不會壞」
   **這兩件一律是讀渲染碼與元件契約推出來的**，**沒有任何一項是量出來的**。
2. **沒有執行任何本 repo 的程式碼、沒有跑任何測試、沒有啟動 Streamlit。**
   本輪執行過的 Python，只有 §8.2 (a) 那支自寫的符號檢查腳本。
3. **沒有量過三斷點。** 本檔的「版面」欄寫的是 `cols` 值與**預期**塌法，不是量測值
   （同 `docs/v2/22_ui_page_today.md` §2 記載的同一個限制：本 repo 目前沒有任何由自己控制的寬度斷點）。
4. **沒有逐行通讀 `ui/views/page_01_macro.py` 全檔。** 既有塊的逐字文案**一律引用現況線框**，
   本輪只逐行讀了與本檔判斷相關的那幾段（頁首 caption、指路句 caption、兩個展開器標籤）。
5. **沒有讀子法 1 的原文。** `docs/v2/CONSTITUTION.md` **不在本工作分支上**，
   本檔的 G1†／G2†／G3† 內容轉引自 `docs/v2/20_ui_spec.md`〈①-Z.2〉那張轉述表。
   ⇒ **本檔的母法引用是「轉述的轉述」，比 `docs/v2/20_ui_spec.md` 又遠一層。**
6. **沒有重算任何 live/dead。** 一律引 `docs/v2/42_live_dead.md`；該表查不到的就寫「查不到」，
   **本草稿不替它補判**。
7. **沒有驗證 `docs/v2/42_live_dead.md` 的判定本身**（該表自陳它是「把既有判定抄成表，沒有重跑任何一次掃描」）。
8. **沒有掃 ③ 與 ⑤。** §1.4 那三條掃描只涵蓋 `ui/views/page_02_health.py` 與 `ui/views/page_04_portfolio.py`
   ⇒ **P0-6／P0-7 會不會以本草稿沒想到的詞彙出現在 ③ 或 ⑤，本檔查不到。**
9. **沒有查 `tests/` 內還有哪些守衛會因為這些改動轉紅。**
   本輪只讀了 `tests/test_wf01_detail_zone_order.py` 與 `tests/test_ui_rerun_contract.py` 的相關段落
   —— **那是兩支，不是全部。**
10. **沒有判斷「值不值得做」、沒有估工時、沒有排優先序。** 那是範圍決定（`CLAUDE.md §8.4` 步驟 4）。
11. **沒有替任何一處母法衝突寫替代文案。**
12. **本檔單組產出，未經第二組獨立驗證**（`CLAUDE.md §-2` 規則 6）。
13. **沒有做 F5（資料表 Schema）與 F6（遷移歷史鏈）的撞擊分析**
    （**2026-09-21 新增，稽核 V 組 中-5**）。本檔 §2.5／§2.7／§5 多處寫「新 series／新 fetcher
    落在 `repositories/**` ⇒ **不在 F1~F6**」，**那個結論抄自 `docs/v2/20_ui_spec.md`，
    但抄的時候把來源的但書丟掉了** —— 來源明文「**但 F5「資料表 Schema」會不會涵蓋
    `data_cache/*.parquet` 的欄位，本節沒查**」，另一處逐字「**沒有做 F5…與 F6…的撞擊分析**」。
    **新-1／新-2／新-3 三塊全部要新 FRED series ＋ 新 L1 fetcher，正是可能吃到 F5 的那一類。**
    ⇒ **本檔那句「不在 F1~F6」的正確讀法是「不在 F1~F4」；F5／F6 沒有被評估過，不是被評估後排除。**
    **指令、輸出與完整理由見 §5 末段。**
    ⚠️ **這一筆的性質是「做了沒做的事而沒有承認」，不是「抄錯字」** ——
    **抄結論、丟但書，會讓一句有條件的話在下一份文件裡變成無條件的。**
14. **本輪（2026-09-21 第二版）的修訂同樣沒有跑任何測試、沒有開瀏覽器。**
    本輪的更正一律是**讀原始碼 ＋ 重跑掃描指令**得到的；
    「收合列用 `st.expander` 不會被那條連續守衛擋住」「`_DETAIL_ZONE` 那支字面值錨不會因換渲染方式轉紅」
    **兩句都是讀碼推論，不是量測**（§1.2 末段、§7 `N-6` 就地更正皆已就地標註）。
15. **本輪沒有回頭重驗第一版那些沒有被稽核點名的判定。**
    本輪只處理**客戶第三批裁示**與**稽核 V 組逐條發現**；
    **第一版其餘部分只在被動到的段落順手對讀，沒有系統性重跑。**
    ⇒ **不得**把「本檔已經過一輪稽核」讀成「本檔每一句都被查過」。

### 8.4 本草稿刻意不宣稱的話

- **「① 沒有第 15 塊渲染物」本草稿不宣稱** —— 本檔的完整性靠有人回頭讀，不靠任何一次掃描。
- **「新增塊只會撞到上面列的那幾格凍結面」本草稿不宣稱** —— §8.3 第 9 條已寫明守衛只讀了兩支。
- **「P0-6／P0-7 在 ② 與 ④ 完全不存在」本草稿不宣稱** —— 只宣稱**上面那三條指令**在 `0b08201` 上的輸出。
- **對映主張 C（§0.6）是本草稿自己提的，不是客戶指定的** —— ~~客戶一句話就能推翻它。~~
  → **2026-09-21 狀態更新（不是漏刪）：客戶已接受它**（§0.2a 裁示 C）。
  **「是本草稿自己提的」這半句仍然為真、仍然要讀** —— 它記的是這條對映的**來歷**；
  被更新的只有它的**狀態**（從「待客戶裁決」變成「已接受」）。
  ⚠️ **而它的理由 (a) 在同一輪被稽核推翻**（§0.6 更正 (a)）——
  **客戶接受的是結論，不是那條理由**；**不得**拿「客戶已接受」回頭替那條理由背書。
- **「① 上只有一組重複的象限值」本草稿不宣稱**（§7 `N-10`）—— 只宣稱那一組在圖上的並排事實。
- **「本輪改完之後這份草稿每一句都查過了」本草稿不宣稱**（§8.3 第 15 條）——
  本輪只處理客戶第三批裁示與稽核 V 組逐條發現。

---

## §9 給客戶的三個問題 —— ✅ **三題已於 2026-09-21 全部裁定**（依 `CLAUDE.md §-1.5.1b` 裁決二：附推薦、不丟純選擇題）

> ⛔ **決定 A 與決定 B 屬範圍問題，本草稿依 `CLAUDE.md §8.4` 步驟 4 不附推薦、只附代價。**
> ✅ **對映主張 C 屬版面問題，本草稿附了推薦與理由（§0.6）。**
> ✅ **2026-09-21 狀態更新（狀態變更，不是漏刪）**：三題**全部已由客戶裁定**（§0.2a）。
> **下表左兩欄原文一字未改** —— 它記的是**送審當時問的是什麼**；裁定結果補在最右欄。

| 問題 | 選項 | 本草稿的立場 | **客戶裁定（2026-09-21）** |
|---|---|---|---|
| **A** 總塊數 14 還是 13？若要 13，從 §1.3 的甲／乙／丙／丁／戊 裡挑一條 | 見 §1.3 五行對照 | ⛔ **不代選。** 範圍屬客戶 | ✅ **13**；逐字「**不用選併法**」⇒ **§1.3 五行整節退場** |
| **B** P0-7 情境分析進不進 ①？進了就要同批決定那兩處 caption 的去留 | 見 §1.4 對照表 | ⛔ **不代選。** 範圍屬客戶 | ✅ **不進 ①**；連帶「**那兩處 caption 本輪不必決定去留**」（客戶理由 4 逐字） |
| **C** 「結論燈 ＋ 3 張核心卡」對映到 ①-1 ＋ ①-4，可以嗎？ | 見 §0.6 主張與替代路 | ✅ **本草稿推薦這個對映，理由三條寫在 §0.6**；客戶一句話可改 | ✅ **接受**；⚠️ 理由 (a)／(c) 另因稽核發現而改寫（§0.6） |

**另有三件需要客戶知道、但本草稿不請示的**（它們是登記，不是選擇題）：
`N-1` 顆粒度兩把尺／`N-3` 一句已被推翻卻沒撤下的記載／`N-7` 裁示 1 落地需要一張今天不存在的豁免。
**2026-09-21 增為五件**：`N-8` 新塊位置被守衛鎖住（⚠️ 那條守衛的強度已就地改為受限版本，§1.2 末段）／
**`N-10` 同一個宏觀象限值會同時出現在兩塊**（規格端早有登記，本草稿是第一份把它畫成並排版面的文件）。

---

## §10 本輪（2026-09-21 第二版）改了什麼 —— 逐條對帳

> **本節是本輪修訂的索引，方便客戶與下一組只讀改動處。**
> **本輪只動本檔一個檔**（刻意不把本檔的檔名寫進正文 ——
> 寫進去之後，任何拿檔名去掃 `docs/v2/` 的指令都會被本檔自己命中，
> 那條掃描當場失效；體例同 `CLAUDE.md §-2.A` 第 8 款「負控不落文件」的理由）。
> **沒有動任何 `.py`、沒有動任何其他 `docs/v2/*.md`。**

### 10.1 客戶第三批裁示的落地（四項）

| 裁示 | 改在哪 |
|---|---|
| **B** P0-7 不進 ① | §0.2a（收錄逐字）／§1.4（標已裁示 ＋ 四條理由逐字 ＋ caption 連帶結案）／**§2.8 整節加刪除線保留**（查證內容一字未刪）／§1.2 表列退場／§3／§4／§5／§6 各表對應列退場 |
| **A** 新增 3、總數 13、不用選併法 | §1.2（主表計數改 13／新增 3／收合 10）／**§1.3 整節加刪除線保留**（五種併法全部不再適用）／§3 合計／§7 `N-2` 計數 |
| **C** 接受 ①-1 ＋ ①-4 的對映 | §0.5／§0.6（標題與主張改標「已接受」）／§8.4／§9 |
| **硬約束** 新塊排在 ①-4 之後、①-6 之前 | §1.2 主表順序（畫面順序 6～8）；**「四塊」與實際 3 塊的落差就地寫明於 §0.2a 末段**（客戶原文逐字未改） |

### 10.2 獨立稽核 V 組發現的逐條處置

| 稽核編號 | 改在哪 | 結果 |
|---|---|---|
| **高-1** 指令跑不出宣稱的輸出 | §1.4 末段 | 舊指令整段劃掉 ＋ 兩種寫法並排實跑 ＋ 8 行逐字照抄 ＋ 偽造可驗證性的就地說明 |
| **高-2** 「唯一一次正好三張」為假 | §0.6 更正 (a) | 刪「唯一」，改寫成附指令與輸出的可自驗敘述；**結論與客戶裁定不受影響** |
| **中-1** 把守衛講得比它實際強 | §1.2 末段 ＋ §6 末段 | 改為受限版本（`_RECORDED_APIS` 11 個 API）；「（實測）」改「讀守衛原始碼」；並指出 `expander` 正是盲點 |
| **中-2** 「渲染端 0 命中」為假 | §2.3 更正 A | 改為「**live 渲染端 0 命中**」＋ 指出唯一既有渲染住在已判 dead 的入口底下 |
| **中-3** 「不得印」漏 `quad_color`／`quad_icon` | §2.3 框內 ＋ 更正 B ＋ §2.3 母法衝突 bullet ＋ §4 表 | 兩欄明確列入 ⛔ 側，理由引追加裁示 1 的「顏色暗示」 |
| **中-4** 漏登一條客戶禁令命中 | §7 新增 `N-10` ＋ 表下補充 | 只登記、不裁決（體例同其餘九條） |
| **中-5** 抄走結論、丟掉但書 | §5 末段 ＋ §8.3 第 13 條 | 補回 F5／F6 未評估的揭露；「不在 F1~F6」正確讀法改為「不在 F1~F4」 |
| **中-6** `≤13` 繫於未定的尺 | §1.2 就地更正 | 兩把尺的收合側數字都寫出來（§1.5 尺 10 塊；裁示 1 尺下界 20 > 13） |
| **中-7** 低估 blast radius | §2.14 就地更正 ＋ §4 表 | 改為 6 個渲染站點並附兩條指令；**就地寫明這是繼承自 spec 的低估，不是草稿新造** |
| **中-8** 指錯會轉紅的那支守衛 | §7 `N-6` 就地更正 | 歸因改到 `test_detail_zone_renders_its_five_sections_in_order_and_contiguous`；結論方向不變 |
| **低-1** 引用已退役的舊算式 | §0.6 更正 (c) | 後半句劃掉，附指令顯示來源已加刪除線、射程待裁示 |
| **低-2** §1.3 表頭自稱「只列不評價」但兩格不符 | §1.3 退場註 | 隨整節退場一併記下（丙的評價、丁的空代價欄） |
| **低-3** §8.2(h) 輸出截斷 | §8.2 (h) | 改逐字照抄 ＋ 補 `exit=` |
| **低-4** G2† 標「逐字」但與來源有出入 | §0.1 三條表 | G2† 與 **G3†** 都劃掉重寫 ＋ 附來源逐字指令；**G1† 未動，理由就地寫明** |
| **低-5** 自行給出「未確認」 | §2.4／§2.5 | 兩處改「不適用」，理由引 §8.3 第 6 條 |
| **低-6** `†` 規則例外不涵蓋正文引文 | §0.1 | 例外改為「客戶原話（不論在不在碼塊內）除外」 |

### 10.3 本輪**沒有**做的事（誠實揭露，不是清單漏了）

- **沒有動任何 `.py`**（客戶明令），**沒有動 `docs/v2/` 底下任何其他檔**。
- **沒有跑任何本 repo 的功能測試、沒有開瀏覽器、沒有啟動 Streamlit**
  —— 本輪跑過的只有 `tests/test_doc_counters.py`（本檔的文件守衛）與各條掃描指令。
- **沒有系統性重驗第一版其餘未被稽核點名的段落**（§8.3 第 15 條）。
- **本輪修訂單組產出，未經第二組獨立驗證**（`CLAUDE.md §-2` 規則 6）。
