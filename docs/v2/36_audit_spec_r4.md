# 規格稽核 R4（第四輪）—— 客戶五條裁示的獨立驗證

> **性質**：純唯讀稽核。**一個 `.py`、一個規格檔都沒有動**；未 commit、未開 PR、未建分支、未 merge。PR #845 維持 draft。
> **不裁決**：矛盾一律 A／B 並陳，**本組不寫「應以 X 為準」**。
> **基準**：`origin/main` ＝ `9cbf0377`。工作樹 HEAD `d0c2a8d` 落後 220 個 commit，故**全程 `git show origin/main:<path>`**，不讀工作區 `.py`。

---

## 0. 受檢檔案的實際路徑（派工單給的四個路徑指不到東西，就地更正）

派工單寫 `scratchpad/spec/…`／`scratchpad/inv/…`，而 **`/home/user/my-Fund-dashboard/scratchpad/` 底下只有 `audit/` 一個子目錄**，
那四個檔**不在那裡**。實際住在**本 session 的 scratchpad**：

| 檔 | 實際路徑 | 行數 | 派工單宣稱 |
|---|---|---|---|
| 盤點表（新） | `/home/user/fund-doc-inv/docs/v2/10_db_inventory.md` | **2,429** | 2,429 ✅ |
| 盤點表（舊） | `<session scratchpad>/inv/DB_INVENTORY.md` | **2,154** | 2,154 ✅ |
| UI 規格 | `<session scratchpad>/spec/UI_SPEC.md` | **482** | 482 ✅ |
| 決策日誌 | `<session scratchpad>/spec/DECISION_LOG.md` | **367** | 367 ✅ |
| 今天頁 | `<session scratchpad>/spec/UI_PAGE_TODAY.md` | **149** | 149 ✅ |

`<session scratchpad>` ＝ `/tmp/claude-0/-home-user-my-Fund-dashboard/c73da973-…/scratchpad`。
**五份的行數與字元數全部對得上派工單**，只有**目錄前綴**寫錯。本報告放在同層 `audit/`（與 R1~R3 同住）。

---

## 1. 方法

**機制**：`git archive origin/main | tar -x` 展一份唯讀樹到 `/home/user/r4audit`（不動共用工作樹）；
行號引用一律 `git show origin/main:<path> | awk 'NR==N'` **直接印原文**，**不經任何 `grep -v`**；
區塊／呼叫點一律 **AST**（`ast.walk` 取 `ast.Call`），不靠字面 grep。

**正控／負控**：每一批掃描**同批**跑正控與負控。
- 正控：四個檔各取一個必然命中的常用詞（`欄位`／`來源`／`客戶`／`層 1`），全部 >0 ✅。
- 負控：**本輪現編的隨機串**（`python3 -c "random…"`），四個檔全部 **0 命中** ✅。
  **依盤點表 §0「負控字串不得寫進本文件」（客戶 2026-09-16 裁示），本報告一律寫成 `<負控串>`，不列出。**
  本輪**未**使用本專案任何舊負控。

**射程外，誠實寫明**：
- 本輪**沒有跑任何測試**（未 `pytest`）。所有「守衛存在」的斷言只驗**函式定義存在**，不驗它會不會轉紅。
- 母法本文 `docs/v2/CONSTITUTION.md` **不在 `origin/main` 上**（`git ls-tree` 查無），故 ②-12 引的 `:582`／`:1096`／`:72-77`／`:1860` 與 F-13 引的 `:580` **本組查不到原文**，只驗了引用格式與內文自洽。
- 「該檔沒有第 N 個 X」這一類**窮舉句本組一律不寫**。

---

## 2. 十件事逐項結論

### (1) 裁示 1：②-12 母法衝突登記 —— **標記與行號全對，但「原樣引用」有 2 處不成立**

**標題** ✅ `UI_SPEC.md:225` 逐字：`### ②-12 配置回測 ⛔【母法衝突・待修】`

**七列行號引文** —— 逐行 `git show origin/main:… | awk 'NR==N'` 重印核對：

| 規格列 | 行號對不對 | 引文逐字對不對 |
|---|---|---|
| `:168` | ✅ | ✅（`{…}` 是標示過的省略） |
| `:172-174` | ✅ | ✅ |
| `:186` | ✅ | ✅ 一字不差 |
| `:181`／`:183` | ✅ | ✅ |
| `:190-191` | ✅ | ❌ **加了原文沒有的粗體**（見 M1） |
| `:194-195` | ✅ | ❌ **標點被換過**（見 M1） |
| `:202` ＋ `:200-201` | ✅ | ✅ |

**七列全部加了引號、且掛在 `現況引文・待修（原樣引用，不是本規格的建議）` 這個表頭下**（`:237`），
逐列實測 col-2 **7/7 含反引號或「」** —— **不是以規格口吻寫成建議**。✅

**決策日誌 §H** ✅ `DECISION_LOG.md:242-243`：第 **1** 項＝**④-6 穿透式持股／產業集中度**，
第 **2** 項＝**②-12 配置回測：母法 G3 衝突**，且 `:245` 寫明「順序是客戶給的，不是本組排的」。

**生產組「來源標籤與母法標記是兩個軸」的主張 —— 本組判定：站得住。**
理由是 `UI_SPEC.md:18` 的標籤定義逐字只量一件事：「**盤點表判活、且本區塊要用的每一欄都到得了畫面**」。
②-12 的來源是 `portfolio_funds[].series/.currency/.code/.name` ＋ USDTWD 序列；
**母法要修的是那三項動作型輸出（`:168-210`）怎麼呈現，不是它讀哪幾欄** ——
排名表（`:107-141`）照樣需要同一組欄位，**修完之後這四欄一欄都不會少**。故 `沿用` 不因母法標記而改。
⚠️ 這是本組依定義文字所作的判斷，**不是查證題**。

---

### (2) 生產組推翻總管兩個事實 —— **本組獨立重跑，兩條都成立**

**(a) 畫面上的免責不只 `:13`／`:208`。**
逐行實測 `ui/helpers/fund_grp_health/backtest_section.py`（全檔 **213** 行）：

- `:13` —— 住在 **module docstring**（`:1-14`）內，**根本不在畫面上**。
- `:104` `st.warning(_res["caveat"])` —— **看得到**。
- `:138-139` `st.success(… "(依 Sharpe→Calmar→CAGR 排名;in-sample,勿當未來保證)")` —— **看得到**。
- `:208` —— `column_config` 的 `help=`，**hover tooltip**。

AST／逐行列出全部 `st.*` 呼叫後，**`:168` 與 `:210` 之間沒有任何一個看得到的免責**：
該區間的輸出是 `:172-174` 三欄動作分組、`:186-198` 賣→買配對表、`:202-210` 建議配置權重表。
→ **「真正的不對等是免責全印在 `:168` 之前」成立。**

**(b)「一條分支乾淨」不成立。**
- `else:` 分支 `:176-178` 的 `st.caption` 後半逐字：「（想看**當前**買/賣訊號 → 見上方健診大表的「淨值×匯率」/「策略燈號」欄，那是即時值…）」
  → **它自己不產生買賣指令，但它指路到另一處買賣訊號。**
- `:202 st.markdown("**⚖️ 建議配置權重**")` 的縮排是 **4 空格**，與 `:170 if _al["buy"] or _al["switch"]:` **同級** ——
  它在 `if/else` **之外**，`:180 if _act["sell_buy_pairs"]:` 也在其外。**兩條分支都會印。**

---

### (3) 裁示 2：`查不到` 桶 10 列 —— **全部落實，數字六項全中**

自寫 parser（**不吃被跳脫的 `\|`**；狀態格先剝 `~~…~~` 再取**最後一個**粗體狀態詞）：

| 量的是什麼 | 本組實測 | 總管給的 |
|---|---|---|
| 逐節 §1/§2/§3/§4 | **89／82／60／33 ＝ 264** | 89/82/60/33＝264 ✅ |
| 狀態分布 | 活的 **111**／未落地 **26**／只寫不讀 **112**／只讀不寫 **9**／查不到 **4**／已停用 **2** ＝ **264** | 全中 ✅ |
| 誰讀它 | UI **113**／內部 **63**／無 **55** ＝ **231**（§4 的 33 列無此欄，264−33＝231） | 全中 ✅ |
| 不變量 | 狀態格解析失敗 **0** 列 | 0 ✅ |

**跳脫陷阱確認**：全檔 **32 行**含 `\|`。用 `split("|")` 會切壞它們，§2.8 那 9 列首當其衝（`:1148-1150` 三行親眼可見）。本組 parser 已避開。

**10 列全翻 ✅**，逐列實測：
- `~~查不到~~ →` **6 列**：`:1244`(§2.14)、`:1263`(§2.15)、`:1373-1376`(§2.20)
- `~~查不到（production）~~ →` **4 列**：`:1285-1288`(§2.17)
- ⚠️ **只 grep `~~查不到~~` 會漏掉 §2.17 那 4 列**（它們帶 `（production）` 括號）。6＋4 ＝ **10** ✅，分布 §2.14×1／§2.15×1／§2.17×4／§2.20×4 與宣稱一致。

**寫入端種類 ✅ 10/10 逐列就地註明**：④ 人工編輯＋git commit **6 列**（`:1244`／`:1263`／`:1373-1376`）、② 手動 CLI＋④ commit **4 列**（`:1285-1288`）。與 `§0.4 (1)` 的五種定義對得上。

**另 4 列（死碼那批）未動 ✅**：`:1116`(§2.6)、`:1228-1230`(§2.13) —— 狀態格**無刪除線**、整列**不含 `2026-09-16`**。

---

### (4) 裁示 3：顆粒度 —— **三處合併全對；但兩個方向的掃描各有一個缺口**（M4／M5）

**§1.5 四條的依據，四處逐字重印，全部命中**：

| 規格引用 | 實測 |
|---|---|
| `page_01_macro.py:1134-1141` | ✅ `:1134`「`####`（H4）是刻意的，而且是唯一允許的一級開頭」；`:1140-1141`「它在告訴你**你多畫了一個與那五塊平起平坐的區塊**」 |
| `tests/test_wf01_detail_zone_order.py:66-70` | ✅ `:67`「**刻意不含 H5**」、`:70 _OPENER_RE = re.compile(r"^#{2,4}\s")` |
| 同檔 `:782-784` | ✅「**刻意只收 H5**……會讓「塊」和「小節」變成同一層」 |
| 同檔 `:798` | ✅ `_grey_units` docstring「小節（`#####`）**再切到卡片**（`**標題**`）」 |

**三處合併的證據，逐行重印，全部命中**：
- ①-4：`page_01_macro.py:2185 render_cards([_card_allocation(_ev), _card_exceptions(_ev), _card_credibility(_ind, _ev)])` ✅；`:764-766` 分隔註解逐字「層 3：📐 建議資產水位 ／ ⚡ ③ 例外 ／ 🔍 ④ 可信度（三欄）」✅
- ②-1：`page_02_health.py:1585 render_cards([_eat_card, _lag_card, _shadow_card])` ✅；`:1852 safe_section("警示卡片", _render_alert_cards)` ✅
- ④-5：`page_04_portfolio.py:2778 safe_section("動作卡", _render_action_cards)` ✅ → `:1653 with card_row(cols=GRID_COLS) as _cells` ✅；`_cells[0]` ＝ `:1655 state_card(switch_block_label(), state=STATE_NOT_READY …)` ✅、`_cells[1]` ＝ `:1668 _render_dividend_calendar_card()` ✅

**三個宣稱數字，本組獨立重算，全中**：
- **區塊 41**：跑規格自己的 parser → `41`；`grep -c '^### '` → **48** ＝ 41 ＋ 5（甲乙丙丁戊）＋ 2（劃掉的 ~~①-5~~／~~②-2~~）✅
- **標籤 49**：沿用 **26**／沿用（部分）**4**／新建 **10**／不標標籤 **9**／查不到 **0** ＝ **49** ✅
- **四件齊不齊**：`- 來源`／`- 規則`／`- 空狀態`／`- 回答` 各 **41**，合計 **164**，**沒有任何一塊某個前綴出現 0 次或 2 次** ✅
- **排除清單 13→10**：甲 9 個編號**劃掉 2**（#2／#9）→ **7**；乙 4 個編號**劃掉 1**（#2）→ **3**；合計 **10** ✅

**⚠️ 本組重掃 `render_cards(` 得 11，不是 9。**（AST，五個 `ui/views/page_0*.py`）

| 檔 | `render_cards` | `card_row` | `safe_section` |
|---|---|---|---|
| `page_01_macro.py` | **8**（`:1283 :1424 :1540 :1560 :2125 :2133 :2157 :2185`） | 0 | 1 |
| `page_02_health.py` | 1（`:1585`） | 0 | 13 |
| `page_03_research.py` | 1（`:1651`） | 0 | 5 |
| `page_04_portfolio.py` | 0 | **2**（`:1653` `:2176`） | 10 |
| `page_05_settings.py` | 1（`:1184`） | 0 | 7 |
| **合計** | **11** | **2** | **36** |

**差在哪，講清楚**：`:2125` 與 `:2133` 住在 `render_market_overview` 的 `if not isinstance(_ind, dict):` **早退分支**（`:2119-2139`，結尾 `return`），與 `:2157`／`:2185` **互斥**；
`:2133` 的三個標題正是 `("📐 建議資產水位", "⚡ ③ 例外", "🔍 ④ 可信度")`，與 `:2185` **同一組卡**。
扣掉這兩個 → **9**，與總管的數字一致。
→ **「只有三處該合併」本組確認為真**：11 個呼叫點裡，`:2125`／`:2133` 是空狀態雙胞胎，其餘 9 個與 41 塊的對映沒有第四處「一次呼叫被拆成多塊」。
**但「9」這個數字是套用了 §1.5 第 4 條之後才有的，而第 4 條的文字管不到 `render_cards`** → 見 **M4**。

**反方向（該拆的沒拆）**：規格宣稱 0 處。本組掃到 **1 個未被任何區塊承接的卡片網格呼叫** → 見 **M5**。
（H2~H4 方向另掃：五頁 markdown 標題共 32 處，未發現任一規格區塊橫跨兩個頂層 `safe_section` 或兩個 H2~H4 標題 —— **這一半的宣稱成立**。）

---

### (5) 裁示 4：今天頁 —— **全部落實，四步配套指到的機制全部真實存在**

**§5 標記** ✅ `UI_PAGE_TODAY.md:83` 逐字：「**取捨：已拍板**（裁示「**接受**」／決策者 **客戶**／日期 **2026-09-16**）。**原脈絡保留，後人要看得懂當初在權衡什麼**」，
`:84-85` 原取捨脈絡（「本頁操作列在層 3，第一次進來時層 1 與層 2 全是灰的，而要按的鈕在它們下面」）**原文保留** ✅。

**§5.1 四步逐一實測，全部指到真東西**：

| 步 | 指到什麼 | 實測 |
|---|---|---|
| 1 | `shared/ui_control_labels.py` **只有 6 個常數** | ✅ 實測 6：`MACRO_LOAD_BTN_FIRST/AGAIN`、`MACRO_FORCE_REFETCH_CHECKBOX`、`SIDEBAR_GLOBAL_REFRESH_BTN`、`DATA_GUARD_HOT_MONEY_BTN`、`DATA_GUARD_RELOAD_MACRO_BTN` |
| 2-3 | `st.session_state[_SK_BTN] = _submit_label` 緊接 `applied_form(…, submit_label=_submit_label)` | ✅ `page_01_macro.py:253 _SK_BTN = "v01_macro_submit_label"`；`:2074-2075` 正是這個形狀；`:2082 with applied_form(` |
| 4 | `_where_to_load()` 只讀那個鍵、不重判 | ✅ `:292 _label = st.session_state.get(_SK_BTN) or MACRO_LOAD_BTN_FIRST`；`:281-285` 逐字寫明「重判會得到 AGAIN，於是又指錯一次，只是方向相反」 |

**背景兩條，本組複跑，全中**：
- `MACRO_LOAD_BTN_FIRST`＝「📡 載入總經資料」、`MACRO_LOAD_BTN_AGAIN`＝「🔄 更新總經資料」，
  該檔 `:27-31` 的註解逐字「**字是動態的**，依 `st.session_state.macro_done`」→ **同一顆鈕的兩個狀態** ✅；
  檔頭 `:11` 的表格第一列正是「文案寫『📡 載入總經資料』／畫面實際『🔄 更新總經資料』」✅。
- `_where_to_load()` docstring **`:270`** 逐字：「⚠️ **2026-09-05 修正：本函式原本寫死 `MACRO_LOAD_BTN_FIRST`，那是錯的。**」✅ **行號精確命中。**

**其餘引用一併實測**：`applied_form(key: str, *, submit_label: str = APPLY_LABEL, …)` 住在 `ui/helpers/ia/gated_form.py:75`（**接受 `submit_label`** ✅）；
`tests/test_ia_kit.py:173 test_gate_is_false_inside_the_block_and_reflects_submit_after_it` ✅；
`tests/test_wf01_detail_zone_order.py:1814 test_the_ai_remedy_names_the_button_that_is_actually_rendered` ✅；
`tests/test_batch2_top_card_grid.py:3985 test_every_where_names_something_that_exists_on_screen` ✅；
`where_to_find("today")` → **會 KeyError**（`ui/helpers/story_nav.py` 的 `_TAB_LABELS` 只有 `macro/health/research/portfolio/settings` 五鍵，§1 Fail Loud 明寫不回退）✅ —— §5.1 `:101` 的推論成立。

**三項沒刪 ✅**：`§0 定位釘死`（`:6-15`，含禁用詞、語意反例、⛔ 無一鍵再平衡／無最佳配置推薦、正例反例、既有前例）、
`§8 反例自檢`（`:136-149`，Q1／Q2 兩題）、「**三斷點是預期不是量測**」（`:39`「本組同樣沒有裝置可量，下面每層的「窄」欄寫的是預期，不是量測值」＋ `:42`「窄 ＝ 預期塌 1 欄（未量測）」）。
**行數 149 ≤ 150 ✅。**

---

### (6) 裁示 5 規則 3：`legacy_ui` 做法 —— **可執行；但 I-1 的聯集數字對不起來**（M2）

**做法的三個前提，本組全部實測**：
- `pytest.ini` 現有 markers：**`slow`、`playwright` 兩個**；`addopts = --strict-markers` ✅
  → §I-2 第 1 步「**註冊是硬要求不是禮貌**」成立：未註冊的 marker 會讓整批 error。
- `tests/` **完全沒有子目錄**（`git ls-tree -r` 下 `tests/` 全部是單層 `.py`）✅
  → §I-2「移目錄」那個被權衡掉的選項，代價敘述（101 個檔的路徑字串會脫鉤）與現況相符。
- `tests/*.py` ＝ **392** ✅（正控命中）

**規模數字重量**：

| 量的是什麼 | 本組實測 | 文件宣稱 |
|---|---|---|
| A：路徑字串引用 `ui/tab` | **101** | 101 ✅ |
| B：直接 import `ui.tab`（含縮排 lazy） | **61** | 61 ✅ |
| **A∪B** | **118** | **114** ❌ **見 M2** |
| A′：收窄成要求 `.py` | **95** | 95 ✅ |
| B′：只認行首（允許縮排） | **54** | 54 ✅ |
| 正控 `tests/*.py` | **392** | 392 ✅ |

⚠️ 順帶一筆：「只認行首」若解成**嚴格第 0 欄**（`^(import|from)`）是 **9**，不是 54 —— 54 對應的是 `^[[:space:]]*`。文件寫 54，本組能重現。

---

### (7) 生產組對總管數字的更正 —— **220／498 成立；465 本組重現不出來**（M3）

| 量的是什麼 | 指令 | 本組實測 | 文件宣稱 |
|---|---|---|---|
| 工作樹落後 | `git rev-list --count d0c2a8d..origin/main` | **220** | 220 ✅ |
| 工作樹超前 | `git rev-list --count origin/main..d0c2a8d` | **0** | 0 ✅ |
| 自 09-01（台北午夜） | `--since=2026-09-01T00:00:00+08:00` | **498** | 498 ✅ |
| 自 09-01（文件稱 UTC 邊界） | `--since=2026-09-01` | **462** | **465** ❌ |

**462 是穩定的**：連跑 3 次皆 462；`TZ=UTC／Asia/Taipei／America/Los_Angeles／Etc/GMT-8／Etc/GMT+8` 五種環境**全部 462**；
`--after` 別名同樣 462。**465 與總管的 466 在本組任何一種叫法下都沒有出現。**
→ 「**該用不依賴日期邊界的 220**」這個結論**本組同意其理由**（220 完全重現、且無分岔），**但支撐它的那個對照數字本身沒被重現**。

---

### (8) 母法禁令 —— **四個檔獨立重掃，無違規**（正控全中、負控全 0）

**六個禁用詞 ＋ 兩條 ⛔**（`建議買進`／`立即出清`／`應該加碼`／`推薦`／`必漲`／`目標價`／`一鍵再平衡`／`最佳配置推薦`）：

| 檔 | 命中 | 逐行判讀 |
|---|---|---|
| 盤點表 | `推薦` ×3（`:412 :418 :1744`） | **全部是「本組推薦讀法 A／B」的治理用語**，與投資建議無關 ✅ |
| `UI_SPEC.md` | `推薦`／`最佳配置推薦` ×1（`:123`）、`一鍵再平衡` ×1（`:298`） | `:123` 是**引述母法紅線**指控 ①-4 卡 1；`:298` 是 ④-2 的 ⛔ **禁令**（「不出買賣動作、無一鍵再平衡鈕」）✅ |
| `DECISION_LOG.md` | `:18`（客戶裁示：母法嚴禁）、`:171`（F-13 待裁示，引述母法 G1） | **都是引述禁令** ✅ |
| `UI_PAGE_TODAY.md` | `:10`／`:12` | **禁用詞清單本身** ✅ |

**反方向（沒踩關鍵字但語意是買賣動作）**：另掃 26 個動作詞（`加碼/減碼/買進/賣出/出清/進場/轉出/停利/停損/抄底/布局/換股/再平衡/建議配置/觀望/提高現金…`）。
逐行判讀後**全部落在三類**：(a) 禁令本身（`UI_PAGE_TODAY.md:10-14`、`:79`「⛔ 裡面不放什麼：任何買進賣出鈕、任何再平衡鈕、任何「套用建議配置」」）；
(b) **②-12 那七列現況引文**（`UI_SPEC.md:240-245`）；(c) 區塊名稱（`換股顧問`＝恆灰卡、`再平衡試算`＝④-2、`t7_scenarios` 再平衡方案快照）。**沒有一句是規格在建議買賣。** ✅

**②-12 七列的呈現方式**（客戶最在意的那一點）：表頭 `:237` 逐字「**現況引文・待修（原樣引用，不是本規格的建議）**」，
七列 col-2 **7/7 帶反引號或「」**，且 `:259` 另寫「⛔ **本輪只登記，不修**：`.py` 一個位元組都沒動」。
→ **是引述，不是建議。** ✅（唯一的問題是「原樣引用」這四個字對其中 2 列不成立 —— M1。）

---

### (9) 四個檔的尺寸（四種量法）

⚠️ **本機 `LANG` 與 `LC_ALL` 都是空的**，預設 `wc -m` **退化成位元組數**（下表最後一欄＝倒數第二欄，親眼可見）。

| 檔 | `wc -l` | `wc -c` | `LC_ALL=C.UTF-8 wc -m` | python `len()` | 預設 `wc -m`（退化） |
|---|---|---|---|---|---|
| `10_db_inventory.md`（新） | **2,429** | 269,188 | **169,943** | **169,943** | 269,188 |
| `DB_INVENTORY.md`（舊） | **2,154** | 231,906 | **150,309** | **150,309** | 231,906 |
| `UI_SPEC.md` | **482** | 86,010 | **44,620** | **44,620** | 86,010 |
| `DECISION_LOG.md` | **367** | 64,930 | **35,384** | **35,384** | 64,930 |
| `UI_PAGE_TODAY.md` | **149** | 16,359 | **8,455** | **8,455** | 16,359 |

`LC_ALL=C.UTF-8 wc -m` 與 python `len()` **五份全部相等**；派工單給的 44,620／35,384／8,455 **全中**。

---

### (10) 兩份盤點表的分歧 —— **新版是舊版的嚴格超集，未發現內容遺失**

`diff`：**僅舊版有 53 行**、**僅新版有 328 行**（2,154 → 2,429）。

把 53 行逐行正規化（剝 `~~…~~`、剝空白與 `*`/`` ` ``/`|`）後比對新版全文：
**23 行的前 40 個正規化字元在新版找不到** —— 逐一讀過，**23 行全部是被取代的上一輪數值**，
新版以「劃掉舊值 → 新值」原地保留了它們的歷史：

| 舊版 | 新版對應 |
|---|---|
| `只寫不讀 … → **102**` (`:1628`) | `:1775` `~~102~~ → **112**（查不到桶輪 ＋10）` |
| `查不到 … → **14**` (`:1630`) | `:1777` `~~14（含「查不到（production）」4）~~ → **4**` |
| `合計 264 ✅（… 111＋26＋102＋9＋14＋2）` (`:1632`) | `:1779` `→ **111＋26＋112＋9＋4＋2**` |
| 誰讀它交叉表 `只寫不讀 0／63／39＝102` (`:1811`) | `:2002` `0／63／**49**＝**112**`，並保留 `~~102~~（0／63／39）` 一欄 |
| `#### (3) ⛔ 射程：…查不到 桶沒有重新掃過` (`:430`) | `:480` 同一標題**加刪除線** ＋「✅ 已於 2026-09-16 由客戶裁定一起翻，本節結案」 |
| `2.15 … — **查不到**` | `2.15 … — ~~**查不到**~~ → **只寫不讀**` |
| `preset_funds._comment ｜ 查不到` (`:1127`)／`snap.*` 4 列 (`:1168-1171`) | `:1244`／`:1285-1288` 同列改為 `~~查不到(（production）)~~ → **只寫不讀**` |

**新版獨有的兩節**：`### 9.3 2026-09-16 查不到 桶輪，這一輪沒有做的事` 與 `#### (6) …翻完之後的全桶重掃`。
**標題層級**：只有 2 個標題「僅舊版有」，兩個都是新版**原地加刪除線改寫**的同一個標題。
→ **交付時收斂成一份，以新版為基底即可；本組未發現舊版有新版沒有的實質內容。**
⚠️ 本比對以「前 40 個正規化字元」為鍵，**是啟發式，不是逐字證明**。

---

## 3. 矛盾清單（A／B 並陳，**本組不裁決**）

### M1｜②-12 表頭寫「原樣引用」，但 7 列裡有 2 列不是逐字

- **A**：`UI_SPEC.md:237` 表頭 —— 「現況引文・待修（**原樣引用**，不是本規格的建議）」
- **B1**：`UI_SPEC.md:243` 引作 `help=`「勝出策略在**最後一次再平衡日**判定要**減碼/換出**的檔。」
  實測 `backtest_section.py:191` 原文為 `help="勝出策略在**最後一次再平衡日**判定要減碼/換出的檔。"`
  —— **`減碼/換出` 兩側的 `**` 是引用時加上去的，原文沒有**（`grep -c '\*\*減碼/換出\*\*'` → `0`）。
- **B2**：`UI_SPEC.md:244` 引作「配對的進場標的**；**無合適者…」
  實測 `:195` 原文的分隔符是 **ASCII `;`（`0x3b`）**，不是全形 `；`。
- **兩者不能同時為真**：一段自稱「原樣引用」的引文，不能同時帶有原文沒有的強調標記與被替換過的標點。
- ⚠️ **射程**：只影響**這兩列的引用精確度**；**七列的行號全部正確**，`:168`／`:172-174`／`:181`／`:183`／`:186`／`:200-202` 五列**逐字無誤**，母法衝突的指控本身不受影響。

### M2｜I-1 的「兩者聯集」＝ 114，但它寫的那條指令在它自己的兩列上跑出 118

- **A**：`DECISION_LOG.md:258-261` 表格 —— 第 1 列 `101`、第 2 列 `61`、第 3 列「**兩者聯集**（真正「碰到舊 UI」的檔數）｜**上兩條輸出** `sort -u | wc -l`｜**114**」
- **B**：實跑「上兩條輸出」的聯集 → **118**（`|A|=101`、`|B|=61`、`|A∩B|=44`、`|B\A|=17`，17 個檔名可列）。
  **114 只在把第 1 列換成收窄版 A′（`ui/tab[A-Za-z0-9_]*\.py` → 95）時才出現** —— 而 A′ 是 `:263` 那條 ⚠️ 註記**後面**才引進的變體。
- **兩者不能同時為真**：第 3 列若真是第 1、2 列的聯集就該是 118；若是 114 則它聯集的不是第 1 列。
- **連帶**：`:265` 推導的「**114/392 ≈ 29%**」隨之改為 **118/392 ≈ 30.1%**。
  ⚠️ 該行的**結論**（「每四個測試檔就有一個以上碰得到舊 UI」）**兩個數字都撐得住**（30.1% > 25%）。

### M3｜J-2 的 465 重現不出來，且「UTC 邊界」這個歸因與實測不符

- **A**：`DECISION_LOG.md:321` —— `git rev-list --count origin/main --since=2026-09-01` → **465（UTC 邊界）**；`:323-324` 稱同一刻改台北午夜 → 498。
- **B1**：同一個固定 ref（`9cbf0377`）跑同一條指令 → **462**。連跑 3 次皆 462；`TZ` 換 5 種皆 462。**465（與總管的 466）本組任何叫法都沒跑出來。**（台北午夜的 **498** 則**完全重現**。）
- **B2**：把 committer date 逐筆全走一遍、取 ≥ `2026-09-01T00:00:00Z` → **493**；`--since=2026-09-01T00:00:00Z` → **489**。
  → **462 與 493 的落差來自 `--since` 的走訪剪枝，不是時區**；而 `:321` 的欄位把差異標成「UTC 邊界」、`:323` 把它整個歸給時區。
- **兩者不能同時為真**：同一條指令在同一個 ref 上不能既是 465 又是 462；「兩個都對，量的不是同一件事」這個解釋涵蓋不了剪枝造成的那一段。
- ⚠️ **不影響** `220`／`0 超前`（兩者完全重現），也不影響「該用 220」這個選擇的理由。

### M4｜§1.5 第 1 條把 `render_cards` 算成區塊，第 4 條的豁免卻只寫給 `safe_section`

- **A**：`UI_SPEC.md:67` 第 1 條 —— 「一個區塊 ＝ 一個 H2~H4 標題，或一次**頂層** `safe_section()` 呼叫，或**一次卡片網格呼叫**（`render_cards([...])`／`card_row()`）」
  —— 「頂層」只修飾 `safe_section`，**沒有修飾卡片網格**。
- **B**：`UI_SPEC.md:70` 第 4 條 —— 「**分支互斥的空狀態 `safe_section`** …不另計一塊」，
  其三個例示 `page_02_health.py:1844`／`page_03_research.py:2747`／`page_04_portfolio.py:2769` 經實測**全部是 `safe_section` 呼叫**，**一個 `render_cards` 都沒有**。
- **命中點**：`page_01_macro.py:2125` 與 `:2133` 是**分支互斥的空狀態 `render_cards`**（住在 `:2119-2139` 的早退分支，結尾 `return`；`:2133` 的三張卡標題與 `:2185` 完全相同）。
  依第 1 條字面**它們是兩個區塊**；依第 4 條字面**豁免碰不到它們**（該條只寫 `safe_section`）。
- **兩者不能同時為真**：`render_cards` 不能既在第 1 條裡算一塊、又被一條只寫給 `safe_section` 的豁免免掉。
- **這正是 9 與 11 的差額**：AST 原始計數 **11**，扣掉這兩個才是 **9**。
  ⚠️ 規格**實務上**已按「不另計」處理（`UI_SPEC.md:120` 把 `:2133-2137` 寫進 ①-4 的**空狀態**欄；①-2 的空狀態則以敘述涵蓋 `:2125`，未附行號）——
  **爭的是條文文字與實務的落差，不是實務本身。**

### M5｜「該拆的沒拆 0 處」宣稱掃了卡片網格，但結論只涵蓋 `safe_section` 與 H2~H4；`page_04_portfolio.py:2176` 無人承接

- **A**：`UI_SPEC.md:89` —— 「以 AST 取五個 `ui/views/page_0*.py` 的全部 `safe_section`／**`render_cards`**／**`card_row`**／H2~H4 `st.markdown` 呼叫點」
- **B**：同一句的結論只說 —— 「**沒有任何一個規格區塊橫跨兩個頂層 `safe_section` 或兩個 H2~H4 標題**」。**它對卡片網格沒有下任何結論。**
- **命中點**：`page_04_portfolio.py:2176 with card_row(cols=STATUS_COLS) as _cells`（實測存在，住在 `_render_policy`，`:2065-2227`）。
  在 `UI_SPEC.md` 全檔搜 `2176`／`STATUS_COLS`／`狀態列`／`_status_tiles` —— **四個關鍵字全部 0 命中**；
  承接它的 ④-3（`:308-312`）四件裡**沒有一個字提到這個卡片網格**。
  → 它**不是**一個區塊、**不在**任何區塊的四件裡、**不在**排除清單裡。
- **兩者不能同時為真**：一份宣稱掃過 `card_row` 呼叫點的檢查，不能同時讓一個 `card_row` 呼叫點在規格裡完全不存在。
- ⚠️ **另一半的宣稱成立**：`safe_section`／H2~H4 方向本組獨立重掃，**確實 0 處**（`④-3 BLOCK_POLICY` 在 `:2771` 與 `:2781` 出現兩次，實測是 `:2772 return` 分隔的**兩條互斥分支**，與規格 `:89` 的說明一致）。
- ⚠️ `page_03_research.py:1080 state_card` 本組**不列為命中** —— 第 1 條的列舉沒有 `state_card`。

---

## 4. 查了但沒問題的（只寫命中不寫清白，看不出這把尺有沒有往內用）

**逐字重印後確認無誤的行號引用（共 28 處）**：
`backtest_section.py` 的 `:13`／`:104`／`:138-139`／`:168`／`:172-174`／`:176-178`／`:181`／`:183`／`:186`／`:190`／`:194`／`:200-202`／`:208`；
`page_01_macro.py` 的 `:764-766`／`:1134-1141`／`:2185`／`:253`／`:270`／`:292`／`:2074-2075`；
`page_02_health.py` 的 `:1585`／`:1844`／`:1851`／`:1852`；
`page_04_portfolio.py` 的 `:1653`／`:1655-1666`／`:1668`／`:2769`／`:2771`／`:2778`／`:2781`／`:2783`；
`page_03_research.py:2747`；
`tests/test_wf01_detail_zone_order.py` 的 `:66-70`／`:782-784`／`:798`／`:1814`；
`tests/test_batch2_top_card_grid.py:3985`；`tests/test_ia_kit.py:173`；`ui/helpers/ia/gated_form.py:75`。
**一處行號錯位都沒有找到。**

**重算後與宣稱完全一致的數字（共 21 個）**：
盤點表 `89／82／60／33／264`、`111／26／112／9／4／2`、`UI 113／內部 63／無 55／231`、`查不到 14→4`、`只寫不讀 102→112`、翻轉 `10` 列（`6 + 4`）；
規格 `區塊 41`、`標籤 26/4/10/9/0 = 49`、`四件 164`、`grep -c '^### ' = 48`、`排除清單 甲7+乙3 = 10`；
決策日誌 `101`／`61`／`95`／`54`／`392`；J-2 `220`／`0`／`498`。

**查過、結論是「沒問題」的機制（共 10 項）**：
`pytest.ini` 只有 `slow`／`playwright` 兩個 marker 且有 `--strict-markers`（§I-2 第 1 步的前提成立）；
`tests/` 無子目錄（§I-2「移目錄」的代價敘述與現況相符）；
`applied_form` 確實接受 `submit_label` 關鍵字；
`where_to_find` 的 `_TAB_LABELS` 確實無 `today` 鍵（§5.1 `:101` 的 KeyError 推論成立）；
`shared/ui_control_labels.py` 確實只有 6 個常數、`FIRST`／`AGAIN` 確實是同一顆鈕的兩態、檔頭確實記著那則手抄錯誤；
§H 第 1 項確實是 ④-6、第 2 項確實是 ②-12；
②-12 七列確實全部加引號、確實掛在「不是本規格的建議」表頭下；
`查不到` 桶剩下的 4 列（`:1116`／`:1228-1230`）確實一格未動；
10 列翻轉確實逐列註明五種寫入端之一；
今天頁確實 149 行、§0／§8／「三斷點是預期不是量測」三項確實都在。

**掃過、確認乾淨的方向（共 3 個）**：
四個檔的八個禁用詞（命中 13 處，**13 處全部是禁令或引述**）；
26 個語意動作詞的反方向掃描（**沒有一句是規格在建議買賣**）；
`safe_section`／H2~H4 的「該拆的沒拆」方向（**確實 0 處**）。

**踩到、但自己先驗掉的兩個陷阱（留給下一輪）**：
1. `~~查不到~~` 這條 grep 只回 **6** 列，會讓人以為「10 列只翻了 6 列」 —— §2.17 那 4 列寫的是 `~~查不到（production）~~`，**括號讓字面 grep 看不見它們**。
2. `split("|")` 會靜默吃掉 §2.8 那 9 列（全檔 **32 行**含跳脫的 `\|`）。本輪 parser 兩個都避開了，數字才對得上。

---

## 5. 本輪的射程（誠實劃界）

- **沒有跑任何測試。** 所有「守衛存在」只驗到函式定義存在，**沒有驗它會不會轉紅**，也沒有做突變驗證。
- **母法本文查不到**：`docs/v2/CONSTITUTION.md` 不在 `origin/main` 上，②-12 與 F-13 引的母法行號本組**查不到原文**。
- **本報告由本組單組產出，未經第二組獨立複驗**（`CLAUDE.md §-2` 規則 6）。
- **M4／M5 是條文文字與掃描射程的問題，不是實務對錯的判定** —— 本組**不裁決**該改條文還是改實務。
- 第 (10) 項的「未發現內容遺失」以**前 40 個正規化字元**為比對鍵，是啟發式。
