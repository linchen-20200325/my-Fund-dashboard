# 基金儀表板 v2「定案包」—— 第 1、2、6、8 節（裁示與盤點組）

> **本組是誰、做了什麼**：裁示與盤點組。本輪**零程式碼變動** —— 沒有改 repo 任何檔案、
> 沒有 commit／push／merge，只用唯讀 git 動詞（`show` / `grep` / `ls-tree` / `rev-parse`）。
>
> **基準**：`origin/main` = `9cbf03776f2a0ee6bdb5a649efb93352c40baba6`（**本組開工時實跑 `git rev-parse origin/main` 確認**）。
> 量測日 **2026-09-15**。**行號會在下一次重構後失效。**
>
> **行號怎麼來的**：一律 `git show origin/main:<path> | awk 'NR>=s && NR<=e'` 直接印**原始**行號，
> **不經任何 `grep -v` / `sed` 過濾**。本組**沒有**從任何一份原料照抄行號 —— 每一個都自己重印了一次。
>
> ⛔ **本組從頭到尾沒有打開過畫面。** 沙箱不允許憑證、不允許打外部 API。
> **所以本文每一句「畫面上是…」，正確的讀法是「在程式判斷成立時會出現」，不是「今天一定看得到」。**
>
> ⚠️ **本文由本組單組產出，未經第二組獨立複驗**（`CLAUDE.md §-2` 規則 6）。
> 凡是「取決於有沒有漏看」的句子，本文一律標「**單組實測到的下界，不是母體數**」。

---

## 📑 目錄

- [第 1 節｜裁示清單](#第-1-節裁示清單)
  - [1-0 ⭐ Q2 —— 第一優先，其他四題卡在它後面](#1-0--q2--第一優先其他四題卡在它後面)
  - [1-A 【待決定】區](#1-a-待決定區客戶要回答的)
  - [1-B 【已定案】區](#1-b-已定案區本區不需回答只更正現況)
- [第 2 節｜資／算／畫三層對照](#第-2-節資算畫三層對照)
- [第 6 節｜依賴關係圖](#第-6-節依賴關係圖)
- [第 8 節｜待我裁示清單](#第-8-節待您裁示清單)
- [附錄 A｜本組推翻總管材料的 8 條](#附錄-a本組推翻總管給的材料的-8-條)
- [附錄 B｜本組沒驗到什麼](#附錄-b本組沒驗到什麼為什麼)

---

# 第 1 節｜裁示清單

> ## 🔭 總判
>
> **這一節共 30 項待您回答**（裁示 1–5、Q1–Q12、C1–C8、問題五、問題九的重拍），
> 但它們**不是 30 個獨立問題** —— **其中 5 項全部卡在 Q2 一題上**。
> **而 Q2 的真正代價，比兩份原料算出來的大一個數量級**：
> 兩份原料合計找到 **4 處**跨線的地方；**本組實測到的下界是「至少 7 個獨立的產出點」，
> 分佈在 ①、③、④、⑤ 四個分頁**，另有 **71 個 production `.py` 檔**含動作詞字樣待人工判讀
> （其中 **29 檔**含「買進／賣出／出清」這三個字面動作詞）。
> **在 Q2 的射程定下來之前，① 的改動範圍無法界定** —— 改窄會漏，改寬會動到您沒想動的東西。

**格式說明（每一項固定四欄）**：**選項 ｜ 本組推薦 ｜ 代價 ｜（不給推薦時）不給的理由**。
**狀態標記**：【已定案】／【待決定】／【未驗】／【示意】。

---

## 1-0 ⭐ Q2 —— 第一優先，其他四題卡在它後面

### Q2｜「禁建議買進／出清」這條紅線，射程到哪？　【待決定】

**這一題不是措辭題，是範圍題。** 您先前指定「① 合規紅線」為第一優先，
而**什麼算跨線，就是 Q2** —— 沒有 Q2 的答案，① 的改動射程無法界定。

#### ⭐ 先看事實：現況實際印出來的動作句（本組逐行實測，全部附 `file:line`）

> ⚠️ **以下每一句都是【逐字引述】程式碼裡的字串**，不是本組的描述。
> ⚠️ **「至少 7 處」是本組單組實測到的下界，不是母體數。**

| # | 在哪一格 | 產出點（`file:line`） | 渲染點（`file:line`） | 逐字內容（節錄） | 兩份原料有找到嗎 |
|---|---|---|---|---|---|
| **A** | **① 市場總覽**（層 1「🧾 ① 結論」） | `services/macro/action_light.py:186-188` | `ui/views/page_01_macro.py:693`（`business_alert`）／`:696`（`st.markdown`） | 「可加碼 —— 核心持有不動 + 衛星積極佈局，定期收息再投」／「持有 —— 分批進場、避免重押單一題材」／「減碼 —— 景氣位階偏弱,拉高現金水位」 | ✅ 線框組找到 |
| **B** | **① 市場總覽**（層 2「🧾 ② 依據」表底註記） | `services/macro/composite_score.py:305/308/311/314/316`（**另有逐字相同的第二份**：`services/macro/explain.py:247/250/253/256/258`） | `ui/views/page_01_macro.py:736` → `:748`（表）／`:751`（註腳） | 「多頭市場強勁：可滿倉持有，衛星部位積極佈局成長題材」／「風險正在集結：拉高現金水位至 15-25%，衛星部位設停利」／「避險情緒高漲：現金 30%+，核心轉防守型（投資等級債/全球均衡）」 | ✅ 兩組都找到 |
| **C** | **④ 資產配置（正式格，舊分頁）** | — | `ui/tab3_portfolio.py:1404-1405`（條件在 `:1402` `elif _alert:`；`_alert = abs(_diff) > 10` 在 `:1372`） | 「⚠️ 配置偏離 X%（核心 Y% vs 目標 Z%）— **核心過重，可贖回轉衛星**」／「— **衛星過重，可獲利轉核心**」 | ✅ 決策組找到 |
| **D** | **⑤ 設定與診斷（正式格，說明書）** | — | `ui/tab6_manual.py:474`／`:475`／`:481`／`:482`／`:534`／**`:686`**／**`:687`**／**`:688`** | 「⚠️ **建議再平衡**（下次配息時執行）」／「🚨 **必須執行再平衡**」／「從「最大衛星基金」**贖回** ΔNT$，**轉入**「最小核心基金」」／「從「最大核心基金」**獲利了結** ΔNT$…」／**「→ 提前**減碼**利率敏感資產（長債、科技股）」／「→ **加碼**利率敏感資產（長債、REITs、成長股）」／「防禦類股（必需消費、公用事業）**優於**成長股」** | ⚠️ **只找到一半** —— 見下方 ⭐ |
| **E** ⭐ | **③ 標的探索 → 🔍 單檔深掘** | `services/health/action.py:69-77`（**九宮格動作矩陣**）＋ `:82-84` | `ui/tab2_single_fund.py:1566-1567`（`combine_action`）→ `:1572` 印出「🎯 **建議動作**：⟨那句話⟩」 | 「🟢 **加碼好時機**」／「🟠 好標的但位階高 → 續抱別追、**可部分停利**」／「🟠 **減碼**」／「🟠 別為低基期留弱基金 → **考慮換**」／「🔴 表現弱 → **考慮換**」／「🔴 弱且位階高 → **優先減碼 / 換**」／「🟡 **逢低小加** / 觀望」／「🔴 品質弱 → **考慮換**」 | ⛔ **兩份原料都沒有找到** |
| **F** ⭐ | **③ 標的探索 → 🔍 單檔深掘** | `services/zscore_engine.py:107`／`:109`／`:111` | `ui/tab2_single_fund.py:1552` 印出「→ **⟨action⟩**」 | 「**停利/減碼**」／「**加碼/逢低**」／「持有」（`:107` 附理由「Z=… ≥ 停利門檻 …(位階偏高)」） | ⛔ **兩份原料都沒有找到** |
| **G** ⭐ | **③ 標的探索 → 🔍 單檔深掘**（「策略3 操作訊號」卡） | `ui/helpers/macro/helpers.py:100-105`（`RECS`，**8 組**） | `ui/tab2_single_fund.py:688` 印出 `sig['label']` | 「🟢 **買進加碼**」／「🟢 **積極買進**」／「⚪ 持有核心」／「🟡 **持有設停利**」／「🟡 **持有減碼**」／「🔴 **賣出獲利**」／「🟢 **逢低買進**」／「⏸️ 觀望等待」 | ⚠️ 線框組在 M3 點到產出點，**沒有追到渲染點** |

#### ⭐ E／F／G 的可達路徑（本組逐段 awk 重印，這是它們成立的前提）

```
app.py:201   from ui.tab_fund_research import render_fund_research_tab
app.py:660   with tab_research:            ← ③ 標的探索（正式格）
app.py:662     render_fund_research_tab()
ui/tab_fund_research.py:159  def render_fund_research_tab()
ui/tab_fund_research.py:186    safe_section(MODE_SINGLE, _render_single_mode)
ui/tab_fund_research.py:127-133  def _render_single_mode() → 從 ui.tab2_single_fund import 並呼叫
                                  render_single_fund_tab()
ui/tab2_single_fund.py:688     ← G（策略3 操作訊號）
ui/tab2_single_fund.py:1552    ← F（→ 停利/減碼・加碼/逢低）
ui/tab2_single_fund.py:1572    ← E（🎯 建議動作：⟨九宮格那句話⟩）
```

⛔ ⭐ **順帶查到一件會誤導下一個人的事**：`ui/tab_fund_research.py:38-42` 的 docstring 逐字寫著
「**⚠️ 尚未接線（刻意）** —— 本檔**還沒有**被 `app.py` 掛上去」。
**那句話今天是假的**（`app.py:201` 匯入它、`:662` 呼叫它，本組 awk 重印確認）。
⚠️ **這與線框組記載的 `ui/views/page_02_health.py:1802-1812` 那次是同一種失效模式**：
**檔案自陳的接線狀態過期了，而讀檔的人會照著它推論。**
⇒ **任何一份依「檔案自陳」寫出來的接線圖，都要回 `app.py` 對一次。**

⭐ **E／F／G 三處是本組新查到的，兩份原料都沒有。它們的嚴重度高於 A～D，理由有三**：

1. **它們用的是紅線的字面動作詞。** A／B／C 用的是「加碼／減碼／拉高現金水位」這類**位階語言**，
   而 **G 直接寫「買進」「賣出」**（`ui/helpers/macro/helpers.py:101/103`），
   **E 直接寫「考慮換」**（＝出清換股，`services/health/action.py:75/76/84`）。
   **如果紅線只禁「買進／出清」這兩個動作詞，A／B／C 可能都不算跨線，但 E 與 G 一定算。**
2. **它們是逐檔的，不是總體的。** A／B 講的是大盤；**E／F／G 講的是「您手上這一檔該怎麼辦」**。
3. **它們的渲染點在 ③，而 ③ 是舊分頁** —— 落在「舊分頁不改內容」那道裁示的射程裡（見裁示 1／裁示 5）。

⭐ **D 的「一半」是什麼意思**：決策組找到 `:474-475`／`:534` 兩章（`ui/tab6_manual.py:89` ⚖️ 再平衡公式、
`:90` 🛡️⚡ 核心/衛星分類邏輯），**漏了第三章** —— `:93` 🗺️ 全局指標關聯地圖，
底下 `:686-688` 是一整段「看到 PMI 強勁 → 提前減碼…／看到 PMI 走弱 → 加碼…」的操作指南。
⚠️ 另外決策組把「贖回／轉入」那兩句記在 `:478-480`，**實際在 `:481-482`**（本組 awk 重印確認）。
⚠️ 順帶一提：同一份說明書還有一章的**標題本身**就是動作句 —— `:91`「🔄 **汰弱留強**（同類 PK）」。
**本組不判定它算不算跨線，那正是 Q2 要定的。**

#### ⭐ 再看範圍：如果紅線讀寬，要動的不是 7 處

**本組實測（量測日 2026-09-15，指令與正對照如下）**：

```
git grep -l -E "買進|賣出|出清|加碼|減碼|停利|停損|逢低|滿倉|現金水位|獲利了結|考慮換|建議動作|積極佈局|分批進場" origin/main -- '*.py' | grep -v ":tests/" | grep -v ":scripts/" | wc -l
```

| 量到什麼 | 數字（**單組實測到的下界**） |
|---|---|
| production `.py` 含上列 15 個動作詞任一者 | **71 檔** |
| 其中在 `ui/**` 底下 | **37 檔** |
| 其中在 `ui/views/**`（＝**新頁**）底下 | **2 檔**（`page_01_macro.py`、`page_04_portfolio.py`） |
| 只取「買進／賣出／出清」三個**字面**動作詞 | **29 檔** |
| **正對照**（證明掃描是活的）：同一把尺對 `NAV` | **115 檔** |

⚠️ **這 71 檔沒有人逐行判讀過** —— 裡面一定混著註解、變數名、docstring。
**本組不宣稱「71 檔全部要改」**；本組宣稱的是：**Q2 若讀寬，要判讀的候選是 71 檔，不是 4 處。**

⚠️ **而且已知有一整個子系統的詞彙就是買賣配對**（本組逐行讀過，**確認是畫面上的欄名與說明，不是註解**）：

| 位置 | 逐字內容 |
|---|---|
| `ui/helpers/fund_grp_health/columns.py:296-298` | 「這檔**該不該換掉**的紅綠燈。🔴 **賣出 / 轉換**(…)/ 🟡 觀望 / 🟢 續抱、**可考慮加碼**…」 |
| `ui/helpers/fund_grp_health/columns.py:448/450/452/454` | 「第 3 段(最深)的**分批買進參考價** —— 跌到這裡才進場的那一段。」／「第 1 段(最早)的**分批賣出參考價** —— 小漲就可以開始減碼的那一段。」／「漲到這裡**建議收手**的那一段。」 |
| `ui/helpers/fund_grp_health/signals.py:142-151` | 表格欄名：「操作訊號」「**買 3 (深跌)**」「**買 1 (小跌)**」「現價」「**賣 1 (小漲)**」「**賣 3 (大漲)**」「現價位階」 |
| `ui/helpers/fund_grp_health/rotation.py:157`／`:160`／`:175` | 表格欄名：「**賣出(高基期)**」／「**建議換進**(別類低基期健康)」 |
| `ui/helpers/fund_grp_health/backtest_section.py:181/183/190/194` | 表格欄名：「**賣出**」／「**→ 買進**」 |
| `ui/helpers/fund_grp_health/switch_advisor_section.py:710` | 「換股 / **賣出轉現金** / 警示 / 續抱 / 資料不足。」 |
| `ui/helpers/fund_grp_health/regime_section.py:58` | 「⚠️ **逆風**(資產屬性與當前景氣相左,可留意 —— **非賣出訊號**):」（⭐ **這一句已經在自我免責了**） |

⭐ **注意 `backtest_section.py` 與 `correlation.py` 這兩塊，新舊兩邊都掛**
（舊 ② `ui/tab_fund_grp_health.py:412`；新 ⑥ `ui/views/page_02_health.py:1758`／`:1763`）——
**所以「賣出／→ 買進」那兩個欄名今天同時出現在舊分頁與新分頁上。**
⚠️ 新 ⑥ 的掛點旁邊 `page_02_health.py:1760` 就地寫著「標註『教學非建議』」——
**也就是這個 repo 內部已經在用「加免責」這條路處理同一個問題了**（同 `regime_section.py:58`）。

#### 選項

| | 讀法 | 要動的範圍（本組實測到的下界） | 代價 |
|---|---|---|---|
| **甲** | **窄讀**：只禁「買進／出清」這**兩個字面動作詞** | **G**（`helpers.py:101/103` 的「買進加碼／積極買進／賣出獲利／逢低買進」）＋ **E** 的「考慮換」三句；`columns.py`／`signals.py`／`rotation.py`／`backtest_section.py` 的欄名（**含新 ⑥**） | **A／B／C／D 全部不動** —— 也就是您第一優先指名的 ① 那兩條鏈**不會被改到**。⛔ 這是甲案最需要先看清楚的後果 |
| **乙** | **中讀**：禁所有**指示使用者改變部位**的祈使句（含加碼／減碼／停利／逢低／滿倉／拉高現金水位／贖回／轉入／換） | **A～G 七處全部**，跨 ①③④⑤ 四個分頁；另 71 檔候選要逐行判讀 | 射程大，且 **C／D／E／F／G 五處住在舊分頁**（③④⑤）→ **直接撞上「舊分頁不改內容」那道裁示**（見裁示 1／5） |
| **丙** | **寬讀**：連**位階描述**（「位階偏高」「高基期」「偏貴」）與**買賣點位表**（買 3／賣 1 價格階梯）一起算 | 乙 ＋ `signals.py` 整張價格階梯表 ＋ `rotation.py` 整個輪動配對 ＋ `switch_advisor` 整個換股顧問 | **這已經不是措辭調整，是功能取捨** —— 換股顧問、輪動配對、買賣點階梯的**存在理由**就是配對買賣 |
| **丁** | **不改字，一律加免責** | 0 處改字；在每一個產出點旁加一句免責 | repo 已有兩個現成前例（`regime_section.py:58`「非賣出訊號」、`page_02_health.py:1760`「教學非建議」）。⚠️ **代價是：畫面上會同時存在「可加碼」與「這不是建議」兩句話** |

#### 本組推薦

⛔ **本組不給推薦，理由請看清楚。**

這一題的差別**不在技術，在於「您要的產品是什麼」**：
- 選甲，① 上那兩條鏈保持原樣 —— 但您指名 ① 是第一優先；
- 選乙，要動五個舊分頁 —— 但您 2026-09-14 說舊分頁不改內容；
- 選丙，等於決定換股顧問／輪動配對這些功能的去留 —— 那是功能取捨，不是措辭；
- 選丁，等於決定「免責句能不能抵銷動作句」——**那是您對自己使用習慣的判斷，本組沒有立場替您決定。**

**本組能做的，是把上面那張表量出來擺在您面前。**

⚠️ **本組刻意不用「這一題很簡單，一句話就能答」這種說法。**
合稿組在相依圖底下寫過「Q2 本身是一個一句話就能答的射程題」——
**本組實測後不同意這個描述**：一句話能寫出射程，但**射程一旦定下來，代價從「4 處」到「71 檔候選」差一個數量級**，
而且**乙／丙兩個讀法會當場把裁示 1 的甲乙丙選項整個改寫**。
⚠️ **這是本組的判斷，不是事實；與合稿組不同調，據實並陳。**

#### Q2 ⇄ ① 的相依（這是您最關心的那一條）

```
Q2（紅線射程）
  ├─ 甲（窄讀）→ ① 的 A、B 兩條鏈【不動】。第一優先的那一頁這一輪沒有改動。
  ├─ 乙（中讀）→ ① 的 A、B 兩條鏈【要動】；同時 C/D/E/F/G 五處落在舊分頁 → 卡裁示 1。
  ├─ 丙（寬讀）→ 同乙，另加換股顧問／輪動配對的功能取捨。
  └─ 丁（加免責）→ ① 不改字，加句子。改動最小，但「① 上的話變兩句」。

Q2 決定之後，下列四題才有答案：
  Q3（📐 建議資產水位 卡去留）……… 它是否跨線，取決於 Q2（線框組已如此標記）
  問題九／裁示 2（那張卡的 A/B/C）… 同上，M2 就是在問「要不要等 Q2」
  M1（「該買該賣」第 1 處涵蓋幾條鏈）… A 與 B 算一條還是兩條，取決於 Q2
  裁示 1（甲乙丙）………………………… Q2 選乙或丙時，要解禁的舊分頁從 1 個變成 4 個
```

⚠️ **本組實測的一項補充，會影響您怎麼答 Q2**：
`ui/views/page_01_macro.py:739-740` 有一道閘門 —— 資料不足（`is_sufficient` 為假）時，
`_action` 會被清成空字串，B 那一組話就不印。**但那是資料品質閘門，不是合規閘門**：
**資料足夠時照印。** A 那一組（`:693`／`:696`）**沒有**這道閘門。

---

## 1-A 【待決定】區（客戶要回答的）

> ⛔ **本區的每一項都還沒有結論。本區與底下 1-B 的【已定案】區嚴格分開，不得混讀（禁止事項 3）。**

### 1-A-1 裁示 1～5

---

#### ⛔ 裁示 1｜問題五的甲／乙／丙 —— 您自己的兩條指示互相牴觸　【待決定】

**牴觸的兩條都是您說的**：(i) 問題五的決定「該買該賣的句子要改掉」；
(ii) 您 2026-09-14「**16 個舊分頁不改內容**，只在入口加一句總體免責」，而說明書正是其中之一。

**為什麼繞不過去（本組實測）**：說明書在**正式 ⑤** 上是**無條件印出來的，連勾選框都沒有** ——
`ui/tab_settings_diag.py:378`（畫區頭）→ `:379`（`from ui.tab6_manual import render_manual_tab`）→ `:383`（直接呼叫）。
⚠️ **對照組**：`[新] ⑦` 那一邊**有**一個預設不勾的勾選框 ——
`ui/views/page_05_settings.py:472`（`MANUAL_GATE_LABEL = "📖 載入使用手冊"`）＋ `:1676-1677`（`st.checkbox(..., value=False)`）。
⇒ **新舊兩邊的行為是相反的：新的要勾才看得到，正式的一打開就印。**

| | 怎麼做 | 代價（本組實測） |
|---|---|---|
| **甲** | 以問題五為準 —— 改說明書 | 要把說明書**單獨**從「舊分頁不改內容」名單放出來。⭐ **本組補的代價**：要改的**不是 3 句，是 7 行、跨 3 章**（`tab6_manual.py:474/475/481/482/534/686/687/688`，章節 SSOT 在 `:89`／`:90`／`:93`）。決策組只圈到 2 章 |
| **乙** | 以 9/14 那道裁示為準 —— 一個字不改 | 那 7 行**會繼續無條件印在正式 ⑤ 上** |
| **丙** | 兩者都守 —— ⑤ 不再叫出說明書 | 使用者在新舊兩邊都查不到說明書（「偏離多少算多」無處可查）。⚠️ 本組補：說明書還有 `:92` 📋 Sheet 資料結構那一章，**那一章沒有任何動作句**，丙案會一起關掉 |

⛔ **本組不給推薦。理由**：甲乙丙的差別**不在技術，在於您要哪一邊讓步，而兩邊都是您自己的指示**。
本組沒有立場替您排先後。

⚠️ **那道 9/14 裁示的原始記錄，本組在程式庫裡找不到 →【未驗】。**
本組實跑過 `git grep` 找「16 個舊分頁」「不改內容」等字樣，**沒有命中任何一份 repo 內文件**。
⚠️ **這一點兩份原料也都說找不到，三組一致** —— 但**三組都找不到，不等於它不存在**（它可能只在對話裡）。
**若您記得的不是這樣，以您為準。**

⭐ **Q2 會改寫本裁示的選項**：若 Q2 選乙或丙，要解禁的舊分頁**不只說明書一個**，
還有 ③（`tab2_single_fund.py`，E／F／G 三處）與 ④（`tab3_portfolio.py:1404-1405`，C）。
**屆時甲案要問的就不是「說明書能不能動」，而是「四個舊分頁能不能動」。**

---

#### ⛔ 裁示 2｜問題九 —— 請在**正確的數字**上重新點一次頭　【待決定】

**為什麼要重問**：這是您已經拍過板的決定，**而您當時看到的三個關鍵事實都是錯的**。

**您當時看到的 vs 程式裡真正的（本組逐條 awk 重印確認）**：

| 您當時看到的 | 程式裡真正的 | 出處（本組實測） |
|---|---|---|
| `股票 55％ ・債券 35％ ・現金 10％` | **這一組不存在。** 只有五組：`85/10/5`、`70/20/10`、**`55/30/15`**、`40/35/25`、`25/40/35` | `shared/signal_thresholds.py:394-400`（`ALLOCATION_LADDER`） |
| `停利 Z ≥ +1.00` | **`+1.75`**；加碼 **`−1.00`** | `shared/signal_thresholds.py:409`（`ZSCORE_STOP_GAIN_DEFAULT = 1.75`）／`:410`（`ZSCORE_ADD_DEFAULT = -1.0`） |
| `這兩個門檻已依台灣景氣黃燈調整。` | **`這兩個門檻是固定的預設值，不會跟著台灣景氣燈號變動。`（意思整個相反）** | `ui/views/page_01_macro.py:785` 呼叫時第二引數傳 **`None`** → `services/allocation_ladder.py:46-48` 收到 `None` 回 `light=None`、`source='default'` → `page_01_macro.py:804-806` 依該空值選到後面那一句 |

⭐ **補一件兩份原料都說對、本組也確認的事**：`+1.00` 在**任何**景氣燈號下都不可能出現 ——
`shared/signal_thresholds.py:406` 五個燈號的停利門檻逐字是 `{"藍": 1.25, "黃藍": 1.5, "綠": 2.0, "黃紅": 1.75, "紅": 1.5}`，**沒有 1.00**。

##### 選項

| | 做法 | 代價 |
|---|---|---|
| **A** | 整張卡拿掉 | 連下半（Z 門檻，是客觀事實）一起拿掉，那一欄會空一塊 |
| **B** | 只把標題「建議」改成「參考」 | 三個百分比仍然是一份配置分配 —— **換兩個字不改變它是什麼**。卡名在 `page_01_macro.py:780`／`:788`／`:808` **三處**，都要改 |
| **C** | 上半（三個百分比）拿掉、下半（Z 門檻）留下並改詞 | 那一欄會變短（三欄網格中的一欄）。要寫進畫面的字是「**偏貴線 Z ≥ +1.75、偏便宜線 Z ≤ −1.00**」 |

##### 本組推薦

⭐ **本組推薦 C，但附一個兩份原料都沒有提出的警告，請連著看完再點頭。**

**推薦 C 的理由**（與決策組一致，本組複驗後同意）：上半是一份配置分配，沒有「事實版」可言；
下半的 Z 門檻是客觀量測（這檔基金離它自己近三年平均多遠）。**C 拿掉的正好是唯一沒有事實版的那一半。**

⛔ ⭐ **本組實測補上的警告：C 案留下的那兩個數字，和同一個網站另一處的同名數字對不起來。**

- ① 那張卡的門檻是**固定的**（`+1.75`／`−1.00`），因為 `page_01_macro.py:785` 傳 `None`。
- **但 ③ → 🔍 單檔深掘那邊的同一組門檻是動態的** ——
  `ui/tab2_single_fund.py:1545` 呼叫 `rebalance_signal(s, category, get_ndc_score())`，
  **第三個引數傳的是真的景氣分數，不是 `None`**；
  `services/zscore_engine.py:92` 收到後走 `dynamic_zscore_gates(ndc_score)` →
  拿到 `shared/signal_thresholds.py:406` 那組**隨燈號變動**的門檻（1.25／1.5／2.0／1.75／1.5）。
- ⇒ **同一個「停利 Z」在 ① 上寫 +1.75（固定），在 ③ 上可能是 +1.25 或 +2.0（隨燈號）。**
- ⚠️ **而且那一段程式自己的註解是錯的**：`ui/tab2_single_fund.py:1537` 逐字寫著
  「NDC 景氣分數 UI 無 live 來源 → **傳 None**,退預設門檻(誠實)」，
  **但 8 行之後的 `:1545` 傳的是 `get_ndc_score()`**（`:1544` 的註解自己也說「NDC 景氣分數**接回**」）。
  **同一個函式裡兩行註解互相矛盾，程式以 `:1545` 為準。**

⇒ **C 案要補一個決定**：① 那張卡上寫的 Z 門檻，要不要註明「這一格是固定值，
和逐檔那邊的動態門檻不是同一把尺」？**本組不代答** —— 那取決於您要不要讓這兩個數字被互相比較。
⚠️ **repo 內部已經有一個現成前例**：`ui/tab2_single_fund.py:1556-1558` 就地印著
「Z-Score 與 σ rank **兩套口徑不同、同一檔可能給不同貴 / 便宜結論,不可直接互比**」——
**同一頁已經為另一組數字寫過這種揭露句。**

⚠️ **這張卡在正式的 ① 上，不是預覽格** —— 改或不改，效果都是立刻的。
⚠️ **本項與 M2 是同一件事**：線框組認為這題要等 Q2 才能答（因為它是否跨線取決於紅線射程）。**見 M2。**

---

#### ⛔ 裁示 3｜SPY / QQQ 的基準爭議 —— 是一個範圍問題　【待決定】

**三處原文（本組逐字重印確認）**：

| 出處 | 逐字內容 |
|---|---|
| `SPEC.md:1640` | 「**基準線禁用 ETF**：**資產曲線**對比基準必須使用「無風險利率年化複利」，不得引入 0050.TW / SPY / QQQ 等 ETF（§0 全面排除）」 |
| `ui/components/mk_dashboard.py:696-700` | 下拉選單「**衛星對比基準**」，選項 `["SPY", "QQQ"]`，說明「SPY=S&P 500、QQQ=Nasdaq 100。連續兩季落後此基準會在表格顯示黃燈。」 |
| `docs/wireframes/ia-wireframe.html:470` | 「連兩季落後對比基準（**SPY / QQQ**）。」 ⚠️ **本組未打開這個 HTML 檔重驗，這一列是轉述決策組的 →【未驗】** |

| | 讀法 | 後果 |
|---|---|---|
| **甲** | `SPEC.md:1640` 只管**整個組合的資產曲線** | 沒有矛盾，三份文件都不用動 |
| **乙** | 任何拿 ETF 當比較對象的地方都算 | 要改 `SPEC.md:1640`，承認「ETF 可以當量尺、不可以當標的」 |

##### 本組推薦

⭐ **本組推薦甲，與決策組同向 —— 但本組的理由與它不同，據實說明。**

**決策組的理由**是「『資產曲線』四個字在原文裡是具體的、有配套實作的」。
**本組的理由多一條、也更硬**：`SPEC.md:1640` 的原文寫的是「**資產曲線**對比基準」，
而 `mk_dashboard` 那個下拉選單的標題逐字是「**衛星對比基準**」（`:697`）——
**兩者連名字都不一樣，一個是「資產曲線」、一個是「衛星」。**
它產出的也不是一條組合曲線，而是一個逐檔黃燈旗標。

⛔ **但本組要更正決策組的一個說法**：它寫「**本組沒有窮舉全站還有哪些地方拿 ETF 當比較對象**」，
然後推薦甲。**本組也沒有窮舉** —— 所以甲案成立的前提（「只有這一處」）**兩組都沒有驗過**。
⇒ **本組推薦甲，但把它標為「在只有這一處的前提下成立」，而那個前提【未驗】。**

**代價**：選甲 = 0 改動；選乙 = 改 `SPEC.md:1640` 一行，**外加把 `mk_dashboard.py:696-700` 那個選單換掉**，
而換掉之後「連續兩季落後基準」這個黃燈**沒有替代基準可用**（無風險利率對單檔股票型基金不是可比基準）。

---

#### ⛔ 裁示 4｜總經 → 基金 → 組合的傳導，要哪一種？　【待決定】

**您舉的例子是**：「這檔債券基金久期 5.2 年，升息 1 碼大概會掉 X%」。

**本組實測：那個例子做不到，因為久期抓不到。**

```
git grep -n -E "duration|久期|信評|信用評等|YTM|存續期" origin/main -- 'repositories/**'
```
→ **唯一命中**是 `repositories/news_repository.py:410` 的一份**新聞分類關鍵字清單**
（`"bond": ["bond","treasury","yield","credit","duration","coupon",…]`）—— **不是基金欄位**。
**正對照**：同一把尺對 `nav` 在 `repositories/**` 命中 **12 檔**，證明掃描是活的。

| | 哪一種 | 做得到嗎 | 它回答什麼 | 它答不了什麼 |
|---|---|---|---|---|
| **甲** | 久期版（您舉的例子） | ❌ **做不到**（缺久期資料） | 「升息 1 碼 → 這檔大概掉 X%」 | — |
| **乙** | Beta 版（不用久期） | ✅ 做得到 | 「這檔基金過去對某個總經變數的連動大概有多強」 | **不會給您「升息一碼掉幾趴」這種數字** |

⛔ **本組不給推薦。理由**：甲和乙回答的是**兩個不同的問題**，不是同一件事的兩種做法。
**只有您知道您想看到的是哪一個。**

⚠️ **代價（本組實測補）**：若選甲，要先解一個**資料源**問題（久期從哪來），
而本 repo **沒有任何一個現成資料源供應商提供這個欄位**（見第 2 節 2-F）。
⚠️ 另外：`ui/tab6_manual.py:686-688` 現在已經有一段**文字版**的傳導說明
（「看到 PMI 強勁 → …央行升息預期升高 → 提前減碼利率敏感資產」）——
**它是靜態教學文字，不是算出來的**（`:690` 就地自陳「此圖為**靜態教學示意**」）。
**若選乙，要先決定它與這段靜態文字的關係。** ⚠️ **這一段同時落在 Q2 的射程內**（見 1-0 表 D）。

---

#### ⛔ 裁示 5｜「舊分頁不改內容」的射程 —— 問題四、六、八的共同卡點　【待決定】

**這是您 2026-09-14 那道裁示的射程問題。** 它現在卡住三題（決策組找到），
**本組實測後要再加兩題**（Q2 若選乙／丙）。

| 題 | 要動的東西住在哪 | 出處（本組實測重印） |
|---|---|---|
| **問題六** | 正式 ④ 上有第二句「往哪邊搬」的話，在舊分頁裡 | `ui/tab3_portfolio.py:1404-1405`（條件 `:1402`，門檻 `:1372` `abs(_diff) > 10`） |
| **問題八** | 三個把「猜出來的核心／衛星」寫進您資料表的寫入點 | `ui/tab3_portfolio.py:1583-1586`／`ui/tab3_t7_ledger.py:1053-1056`／`ui/helpers/portfolio/policy_admin_section.py:385`（鈕）→ `:390-392` → `ui/helpers/cloud_io.py:251-254` |
| **問題四** | 「緊鄰換股顧問」要求搬進舊 ④ | `ui/views/page_04_portfolio.py:919-921`（守衛釘住渲染點**恰好一個**，`EXPECTED_RENDER_SITE == "ui/tab3_portfolio.py::render_portfolio_tab"`） |
| ⭐ **Q2 的 E／F／G** | ③ 單檔深掘的三處動作句，在舊分頁 `ui/tab2_single_fund.py` | `:688`／`:1552`／`:1572` |
| ⭐ **Q2 的 D** | ⑤ 說明書的 7 行，在舊分頁 `ui/tab6_manual.py` | `:474/475/481/482/686/687/688`＋`:534` |

##### ⭐ 本組實測要更正決策組的一處，因為它會改變您怎麼答

決策組寫「問題八的**三個寫入點全部住在舊分頁**（`ui/tab3_portfolio.py`、`ui/tab3_t7_ledger.py`、
`ui/helpers/portfolio/policy_admin_section.py`）」。**這句話只對前兩個成立。**

**本組實測**：
- 前兩個確實在舊分頁檔內（`ui/tab3_portfolio.py`、`ui/tab3_t7_ledger.py`）。
- **第三個不是** —— `ui/helpers/portfolio/policy_admin_section.py` 是 `ui/helpers/**` 底下的**共用模組**，
  它**同時被新舊兩邊接著**：
  - **舊 ④**：`ui/tab3_portfolio.py:757-760` 直接 import 並呼叫（**今天是活的**）。
  - **新 ⑦**：`ui/views/page_05_settings.py:1355` 呼叫 `render_policy_admin_bridge(sheet_client=None)`
    → `ui/helpers/settings_diag/policy_admin_bridge.py:116` 委派到同一支函式。
  - ⚠️ 該橋接層 `:62` 有一道旗標閘門（`if not owned_by_settings_page(POLICY_ADMIN)`），
    而 `:1-10` 的 docstring 逐字自陳「旗標…**預設關閉**」。
    **⚠️ 本組沒有實跑 `owned_by_settings_page` 確認它的預設回傳值 →【未驗】。**

⇒ **正確的說法是**：「三個寫入點裡，**兩個住在舊分頁檔內；第三個住在共用模組，今天的活入口是舊 ④，
但新 ⑦ 已經接好線、只差一個旗標**。」
⭐ **這對您的裁示有實際影響**：若把「不改內容」讀成「舊分頁檔一個字不准動」，
**第三個寫入點在檔案邊界上根本不在舊分頁裡** —— 也就是它可以在不動舊分頁檔的情況下修掉。
⚠️ **但那樣修會同時改變未來 ⑦ 的行為**（同一份程式碼）。**這一點需要您知道，本組不代答。**

##### 「舊分頁解禁範圍」的選項與代價（⛔ 本組不代選，只把四個落點擺出來）

> **您要定的是一句話：「不改內容」這四個字，涵蓋到哪裡為止。**
> **底下四個選項是本組依實測整理的落點，不是本組的推薦。**

| | 解禁範圍 | 打開了什麼 | 代價 | 仍然關著什麼 |
|---|---|---|---|---|
| **甲** | **一格都不解禁**（字面照做） | 無 | 問題六改不了（正式 ④ 那一句留著）；問題四的「緊鄰」做不到；**問題八第 (3) 件只能做掉三分之二** —— `ui/tab3_portfolio.py:1583` 與 `ui/tab3_t7_ledger.py:1053` 動不了。<br>⭐ **但 `ui/helpers/cloud_io.py:251` 那一個可以做**（它不在舊分頁檔內，見上方更正） | 全部 |
| **乙** | **只解禁「修錯」，不解禁「改字」** | 問題八全部三處（它修的是一個把您填的值蓋掉的錯，**使用者看到的字不會變、版面不會動**） | ⚠️ **要先定義「修錯」** —— 否則下一個人會拿它當萬用通行證。<br>⭐ **repo 內有現成的分界範本可以照抄**：`CLAUDE.md §-1.5.4` 的兩步判定（先問「動到的是版面結構還是內容值」，再問「是修回原本該有的樣子，還是改成另一個樣子」）。⚠️ **本組未重印該節 →【未驗】**，轉述自專案憲法 | 問題六（那是改字）、問題四（那是搬家） |
| **丙** | **逐檔解禁**（點名哪幾個檔可以動） | 由您點名 | 最可控，但**每次遇到新的落點就要再問您一次** —— 而本組實測到的落點已經是 **5 個舊分頁檔**（`tab3_portfolio.py`／`tab3_t7_ledger.py`／`tab2_single_fund.py`／`tab6_manual.py`／`tab_fund_grp_health.py`），且 **Q2 選乙／丙時還會再增加** | 沒點名的 |
| **丁** | **全部解禁**（撤銷那道裁示） | 全部 | ⚠️ **那道裁示當初要防的東西會回來** —— 本組**不知道它當初要防什麼**（找不到原始記錄），所以**無法量這一項的代價**。⛔ **本組把這一格標成「代價未知」，不是「代價低」** | 無 |

⛔ **本組不給推薦，理由**：這四個選項的差別在於「您有多信任執行組不會把解禁當通行證」——
**那是您對團隊的信任度判斷，本組沒有立場替您決定。**
⚠️ **但本組要提醒一件事**：**甲案並不等於「什麼都不動」** ——
`ui/helpers/cloud_io.py:251` 那個寫入點在檔案邊界上不在舊分頁裡，
**甲案之下它仍然可以修，而那是三顆鈕裡的一顆。**

---

##### ⭐ 本組推薦（僅就「這三題該不該用同一把尺」）

**本組推薦：不要用同一把尺，而且切法與決策組**略有不同**，據實並陳。**

| 題 | 決策組的分法 | **本組的分法** | 差在哪 |
|---|---|---|---|
| 問題六 | 「和問題五同一種」→ 併進裁示 1 | **同意** | 一致 |
| 問題八 | 「不同種 —— 它修的是一個把您填的值蓋掉的錯，使用者看到的字不會變」→ 推薦照辦 | **同意結論，但代價欄要補一項** | ⭐ 本組補：第 (3) 件（寫死永不自動寫入）**有一個它沒提到的落點** —— `ui/helpers/cloud_io.py:243` 呼叫 `upsert_fund_in_policy` 時用的 key 是 **`policy_tier`**（v1 欄名），而新結構 `repositories/policy/v2.py:511` 叫 **`tier`**。兩者由 `repositories/policy/v2.py:927` 的 `_ren = {"policy_tier": "tier", ...}` 明確互相映射。⇒ **這是一個欄位兩個名字，不是兩個欄位**。決策組寫「叫 `tier`，不叫 `policy_tier`」，**只說了一半，會讓執行組以為要新增欄位** |
| 問題四 | 「是一個搬家，兩邊都說得通」→ 等裁示 1 | **同意** | 一致 |

⛔ **但「問題八算不算改內容」這一題本身，本組不代答** —— 那道裁示是您下的，**它的射程只有您能定**。

⚠️ **急迫性（本組據實寫，不加重也不減輕）**：問題八的三個寫入點，
**每按一次那三顆鈕就會把猜出來的值寫進您的資料表一次**。
猜法是「拿基金名字比對關鍵字」：`services/ai_service.py:26-30` 的核心關鍵字清單裡有「**配息**」，
`:40-42` 先比核心、比中就不再看衛星 ⇒ **名字尾巴帶法定揭露句「（基金之配息來源可能為本金）」的基金，
必然先被「配息」攔截判成核心。**
⚠️ **您的資料表目前有幾筆已經被蓋掉，本組不知道** —— 那要讀您的線上資料，本輪不允許 →【未驗】。

---

### 1-A-2 Q1、Q3～Q12（線框組提的十二題）

> ⚠️⚠️ **您先前只點名 Q5～Q10。落在您點名範圍外的是 Q1／Q2／Q11／Q12** ——
> **本組一題都沒有刪，四題全部標「客戶未點名、但仍未決」。**
> **Q2 已提到本節最前面（1-0）。**

| # | 題目 | 選項 | 本組推薦 | 代價 ／（不給推薦時）理由 | 狀態 |
|---|---|---|---|---|---|
| **Q1** ⚠️ | **① 的表單欄位要哪一組？** 兩份線框給了兩套：(a)「觀察區間 ＋ 資料源」／(b)「四個資料類別勾選框 ＋ 強制重抓」 | a ／ b ／ 兩者合併 | ⛔ **不給** | **不給的理由**：這是「您想怎麼操作這一頁」的問題，本組判斷不出來。<br>⭐ **這題是程式自己掛牌待決的**：`ui/views/page_01_macro.py:2086-2088` 的 caption 逐字寫著「⬜ 表單欄位待客戶裁決：兩份線框一份是『觀察區間 ＋ 資料源』，另一份是『四個資料類別勾選框 ＋ 強制重抓』。本批只做骨架與送出鈕。」——**這行字現在就印在正式 ① 的畫面上**（在程式判斷成立時）。<br>**代價**：不答＝那行「待客戶裁決」的灰字繼續印在正式分頁上 | **【待決定】⚠️ 客戶未點名** |
| **Q3** | **「📐 建議資產水位」這張卡要留、改名、還是拿掉？** | 留 ／ 改名 ／ 拿掉 | ⛔ **不給（卡在 Q2）** | **不給的理由**：它是否落在紅線內**取決於 Q2**。卡名逐字含「建議」，出處 `ui/views/page_01_macro.py:780`／`:788`／`:808`（**三處**），內容是股／債／現金三個百分比（`:809-810`）。<br>⚠️ **本項與裁示 2／問題九、與 M2 是同一張卡的三個問法** —— **請一起回答，不要分開答** | **【待決定】** |
| **Q4** | **舊分頁什麼時候退場？**（順序與第一前置已拍板，本題只問**時間點**） | 立刻 ／ 驗收後 ／ 分批 | ⛔ **不給** | **不給的理由**：這是排程與優先序，只有您知道。<br>**已定案的部分**（本組重印確認）：`ui/views/page_04_portfolio.py:12` 逐字引您 2026-09-08 原話「…退場順序完全同意採「**⑦ → ⑥ → ⑧ → ⑨**」，並**以「⑨ 建立寫入入口」為第一前置**。」<br>**代價**：`ui/views/page_04_portfolio.py:2761` 登記著「走到底時，那句指路會指向不存在的地方」⇒ **每拔一格都會有既有指路失效**。⚠️ **`:2761` 這一行本組未重印 →【未驗】，轉述自線框組** | **【待決定】** |
| **Q5** | **「第四層最多一個展開 ＋ 一個勾選方框」是硬規則還是目標值？**（＋四層要不要補到 ③④？＋② 要不要補「② 依據」區頭？） | 硬規則 ／ 目標值 | ⛔ **不給** | **不給的理由**：這是設計紀律的鬆緊，屬您的產品判斷。<br>⚠️ **本組未重驗線框組的「⑤ 有 6 個勾選方框 ＋ 3 個展開」計數 →【未驗】**。本組只重印確認了「⑤ 的說明書 gate 是一個 checkbox」（`page_05_settings.py:1676`） | **【待決定】** |
| **Q6** | **按鈕字「只准試算／搜尋／套用」，含不含下載鈕與導覽鈕？** | 含 ／ 不含 ／ 另立一類 | ⛔ **不給** | **不給的理由**：同 Q5，屬設計紀律。<br>⚠️ **本組未重驗線框組的「5 個按鈕字不在這三個之內」→【未驗】** | **【待決定】** |
| **Q7** | **「四色」指哪一個四？** | (A) 五態裡帶顏色的四個 ／ (B) `CARD_STATES` 四個 | ⛔ **不給** | **不給的理由**：這是命名的定義權。<br>⚠️ **本組完全沒有碰第四節（顏色狀態），本題全部轉述線框組 →【未驗】。**<br>⭐ 但本組要提醒一件事：總管交辦時自陳「我對『四色 vs 五態』的假設整個錯了」—— **本題有前科，請優先確認** | **【待決定】** |
| **Q8** | **TER 卡要修數字、改卡名，還是整張拿掉？** | 修數字（改餵 `total_expense_ratio`）／ 改卡名（→「最高經理費」）／ 整張拿掉 | ⭐ **改卡名** | ⭐ **本組給推薦，理由是這個 repo 自己已經給了答案 4 次**（詳見第 2 節 2-B）：同一張卡的**副標**（`checkup.py:282`）、**隔壁表格的欄名**（`:577`）、**缺料訊息**（`:297`）、**排名欄的 help**（`:608`）**四處都已經寫「最高經理費」**，只有卡片大標（`:280`）寫 TER。<br>**代價（改卡名）**：最小 —— 但「費用率排名」那一欄（`:578`）也要一起改，否則同一張表裡仍有一個叫「費用率」的欄位餵著經理費。<br>**代價（修數字）**：`repositories/fund/fund_orchestration.py:1088-1091` 的 `total_expense_ratio` **有抓但目前 0 個消費者**，改餵它等於新增一條資料鏈；而 `services/health/grade.py:20` 就地記著「費用率：**官方揭露率實測 0**」⇒ **改餵真 TER 很可能大量變成空白**。<br>**代價（拿掉）**：`:578` 的「費用率排名」欄會失去來源 | **【待決定】** |
| **Q9** | **顯示表的「金額」與 Sheet schema 的「淨投資金額」要不要統一？** | 統一 ／ 不統一 | ⭐ **統一** | **理由**：`repositories/policy/v2.py:533` 的中文表頭逐字是「**淨投資金額**」，英文欄名 `:512` 是 `invest_twd`。**同一個欄位兩個顯示名，正是第 2 節 2-H「同名陷阱」那張表在防的形狀。**<br>**代價**：要找出所有手抄「金額」二字的顯示點。⚠️ **本組沒有數過有幾處 →【未驗】** | **【待決定】** |
| **Q10** | **主 CTA「更新淨值與配息」要不要做？落在哪一頁？** | 做（落 ①／②／④／⑤）／ 不做 | ⛔ **不給** | **不給的理由**：這是功能取捨 ＋ 動線決定。<br>⭐ **本組重跑了線框組那句「repo 0 命中」，結論一致**：`git grep "更新淨值與配息"` 與放寬後的 `git grep "更新淨值"` **皆 0 命中**（正對照：「淨值」在 `fund_fetcher.py` 單檔 8 行命中）。<br>⚠️ **線框組自己也標了**：它沒有查證「它應該存在」這件事 —— **也就是這個 CTA 可能從來就不是既有規格，而是某一份草稿寫出來的** | **【待決定】** |
| **Q11** ⚠️ | **「持久化失敗」要不要有自己的顏色？**（現行借用 🔴，repo 自標「是判斷不是事實」） | 給自己的顏色 ／ 維持借用 🔴 | ⛔ **不給** | **不給的理由**：顏色語意的定義權在您。<br>⚠️ **本組完全沒有碰第四節，本題全部轉述線框組 →【未驗】** | **【待決定】⚠️ 客戶未點名** |
| **Q12** ⚠️ | **🟠 的三個意思要不要拆？** | 拆 ／ 不拆 | ⛔ **不給** | **不給的理由**：同 Q11。<br>⭐ 線框組記載「repo 已標明它是**客戶 gate**，不是內部自決」—— ⚠️ **本組未重印該處 →【未驗】** | **【待決定】⚠️ 客戶未點名** |

---

### 1-A-3 C1～C8（合稿組找到的兩組不同調）＋ M1／M2／M3

> ⚠️ **M1／M2／M3 是合稿組從 C5／C6／C1 升上來的** —— **不要重複計數**。
> 下表在對應的 C 項下已標明「**已升為 M?**」。
> **C2／C3／C4／C8 是「誰的行號對」，不需要您裁示；本組把它們重驗了一遍，結論列在最右欄。**

| # | 不同調在哪 | 本組實測結論 | 需要您裁示嗎 |
|---|---|---|---|
| **C1** | **① 這一格該怎麼標？** 線框組標 `[兩邊都有]`；決策組標「已經是新版」 | **兩邊在事實上一致，本組複驗成立**：`app.py:156` `from ui.views.page_01_macro import render_market_overview`；`app.py:619` `with tab_macro:` → `:636` 呼叫它；`ui/helpers/story_nav.py:130-135` 的 `PREVIEW_TAB_LABELS` 只有 `health`／`settings`／`research`／`portfolio` **四個 key，沒有 `macro`**。⇒ **① 沒有預覽雙胞胎。** 不同調的只有**標籤怎麼寫** | ⭐ **是 → 已升為 M3** |
| **C2** | 畫面上那句話裡的數字是「3 成」還是「7 成」？ | ⭐ **本組第三次獨立重跑，與線框組一致**：`git grep "約 3 成"` → **6 行 / 4 檔**（`SPEC.md:712`、`ui/helpers/fund/checkup.py:7`／`:407`／`:474`／`:613`、`ui/tab6_manual.py:588`），其中**只有 `checkup.py:613` 講的是「同類排名」，另外 5 處講的是「同類平均」**。「7 成／七成」作為覆蓋率的說法 **0 命中**（正對照：「約」命中 **219 檔**）。<br>⇒ **畫面上印的是「約 3 成**沒有**」，不是「約 7 成有」。兩組的實質結論一致（那個比例沒有人量過）** | ❌ 否（事實已釐清） |
| **C3** | 九格分頁的建立處在哪幾行？ | **本組 awk 重印**：`app.py:581-583` 是九個變數的 tuple 拆包（`:583` 尾端是 `st.tabs(`），`:584-587` 是九個標籤。**線框組寫 `:583-588`（`:588` 是空行、漏了 `:581-582`）；決策組寫 `:571-577`（兩端都不是分頁建立處）。合稿組寫的 `:581-587` 正確。** 三組的**結論**（九格、5 正式 + 4 預覽）都成立 | ❌ 否 |
| **C4** | ① 的呼叫點在哪一行？ | **本組 awk 重印**：`app.py:636` 是 `render_market_overview()`，`:637` 是 `except Exception as _macro_tab_e:`。**線框組對；決策組差一行。** 結論相同 | ❌ 否 |
| **C5** | **「該買該賣」的第 1 處涵蓋幾條鏈？** 決策組只圈 `composite_score` 那一條；線框組另外查到 `action_light` 那一條 | **本組逐行複驗：兩條鏈都成立**（見 1-0 表 A、B）。⭐ **而且本組又查到第三、四、五組**（表 E／F／G）—— **「第 1 處」的射程比兩組加起來還大** | ⭐ **是 → 已升為 M1（而且射程要擴大，見下）** |
| **C6** | **「📐 建議資產水位」現在該不該給推薦？** 決策組給推薦 C；線框組認為要等 Q2 | 兩邊都有道理：決策組的切法（上半是分配、下半是事實）**確實與數字無關**；線框組的顧慮（它是否跨線取決於 Q2）**也確實成立**。**本組的立場見裁示 2：給推薦 C，但附一個新警告（① 固定門檻 vs ③ 動態門檻）** | ⭐ **是 → 已升為 M2** |
| **C7** | **「逐檔的加減碼建議請到 ②」那句指路** 線框組列為現況不提議改；決策組推薦改成「逐檔的體質診斷」 | **本組重印確認兩件事**：(a) 那句在 `ui/views/page_01_macro.py:2031`，**而同一個 caption 的 `:2033` 還有第二句同型話**「「我手上這幾檔**該加該減**」是 ⟨②⟩ 的題目」—— **決策組只圈了一句，要改是兩句**；(b) 守衛確實存在：`tests/test_wf01_detail_zone_order.py:135` 逐字 `_SIGNPOST_MARK: str = "加減碼建議"`，而 `:134` 就地記著「2026-09-05 實測：把『已搬到』改成中性時態『請到』時，本標記就擋下來過一次」 | ⚠️ **是，但併進 Q2 一起答** —— 它用的詞（加減碼）正是 Q2 在問的那組詞 |
| **C8** | TER 卡的取值那一行 | **本組 awk 重印**：`checkup.py:194` 是空行、`:195` 是註解（「5) TER 費用率 — 只取本檔真實 mgmt_fee + category」）、**`:196` 才是 `_ter_val = _safe_num(mj.get("mgmt_fee"))`**。**決策組對；線框組整段偏 1 行。** 兩組對這張卡的結論完全相同 | ❌ 否 |

#### M1／M2／M3（合稿組升上來的三項，本組逐項重驗並補充）

| # | 要您定的界 | 為什麼只有您能決定 | 本組補充 |
|---|---|---|---|
| **M1**（源自 C5） | **「該買該賣」要改的第 1 處，涵蓋幾條鏈？** | 兩條鏈都在正式 ① 上、都在您眼前。**這是射程問題，不是事實問題，而射程由您定。** | ⛔ ⭐ **本組實測：這一題的問法本身要改。** 合稿組把它寫成「一條還是兩條」，**但本組另外查到第三、四、五組動作句**（表 E／F／G，住在 ③）。<br>⇒ **正確的問法是「第 1 處」指的是「① 這一頁」還是「所有逐檔動作句」** —— **後者的射程是前者的兩倍以上**。<br>⇒ ⭐ **本組推薦：M1 不要單獨答，直接併進 Q2** —— 它是 Q2 的一個子問題，分開答會得到互相矛盾的射程 |
| **M2**（源自 C6） | **「📐 建議資產水位」現在就拍，還是等 Q2 先答？** | 這是決策順序問題 —— 兩組對「該不該先等上游」看法不同 | ⭐ **本組同意線框組：等 Q2。** 理由不是「決策組錯了」，而是**本組在裁示 2 裡查到的那個新事實**（① 固定門檻 vs ③ 動態門檻）**本身就取決於 Q2 的射程** —— 若 Q2 選乙／丙，③ 那三處要改，① 這張卡要不要改口徑就變成另一個答案 |
| **M3**（源自 C1） | **① 這一格在往後的文件裡，統一標成什麼？** | 這直接關係到您的第 9 條禁止事項。**兩邊在事實上一致，不同調的是標籤 —— 但標籤會被後續每一份文件引用** | ⭐ **本組推薦一個兩組都沒提的第三種寫法**：**「① ＝ 正式格 ＋ 新 View，無預覽緩衝」**。<br>**理由**：線框組的 `[兩邊都有]` 會被誤讀成「新舊各有一份同時渲染」（它自己的圖例就是這樣定義的）；決策組的「已經是新版」則漏掉了「**所以改它就是改正式畫面**」這半句 —— 而那半句才是您第 9 條禁令真正要防的。<br>⚠️ **這是本組的推薦，不是事實。** |

---

### 1-A-4 問題五、問題九（待重拍的兩題）

> ⛔ **這兩題與 1-B 的【已定案】七題嚴格分開。**

#### 問題五｜另外兩處「該買該賣」的句子　【待決定】

**結論（兩處都改）本身沒有爭議；卡住的是「什麼時候能動、要不要先解禁那個檔」** → **走裁示 1**。

⭐ **本組實測要更正兩處**（兩份原料都沒抓到）：
1. **不是「兩處」，本組實測到的下界是「三章 7 行」**（見 1-0 表 D 與裁示 1）。
2. **決策組把「贖回／轉入」那兩句記在 `ui/tab6_manual.py:478-480`，實際在 `:481-482`。**

**甲／乙／丙的代價已量在裁示 1。⛔ 本組不給推薦（理由同裁示 1）。**

#### 問題九｜市場總覽那張直接給您配置比例的卡　【待決定・要重拍】

**⛔ 本節在正確的數字上重給選項**（派工要求）—— 三個關鍵事實已在裁示 2 逐條更正並附出處：
**停利 Z ≥ +1.75、加碼 Z ≤ −1.00、固定不隨景氣燈**（`shared/signal_thresholds.py:409-410`；
`ui/views/page_01_macro.py:785` 傳 `None` ⇒ 固定，機制在 `services/allocation_ladder.py:46-48`）。
**本組已逐條 awk 重印複驗，三條全部成立。**

**選項 A／B／C、本組推薦 C、以及本組新查到的警告（① 固定 vs ③ 動態），全部寫在裁示 2。**

---

## 1-B 【已定案】區（本區不需回答，只更正現況）

> ⛔ **本區七題（問題一、二、三、四、六、七、八）的結論一字未改**（禁止事項 8）。
> **本組只做一件事：把每一題的「現況」重新去程式碼裡對一次，附上 `file:line`。**
> ⛔ **本區與 1-A 的【待決定】區不得混讀。**
> ⚠️ **「已定案」的來源**：以下七題的決策者、日期、轉述來源**全部出自決策組的轉錄**
> （客戶．2026-09-14／2026-09-15．**總管轉述**）。**本組沒有看到原始記錄 →【未驗】。**

| 題 | 已定案結論（**一字未改**） | 決策者／日期／來源 | 本組對「現況」的更正或補充（附 `file:line`） |
|---|---|---|---|
| **一**｜保單那一塊要多大 | ✅ **案 A：全寬區塊 ＋ 狀態列補「總投入」（四格）** | 客戶／2026-09-14／總管轉述。⭐ 另有 2026-09-07 同結論的就地紀錄：`ui/views/page_04_portfolio.py:49`／`:53` | ⚠️ **本組未重印 `page_04_portfolio.py` 的 `:49`／`:53`／`:653`／`:665`／`:2070-2081` →【未驗】，此列全部轉述決策組。** 本組本輪把查證額度用在 Q2 的射程上 |
| **二**｜持倉體檢那個「總分」 | ✅ **案 A ＋ 欄名改「體質評等」** | 客戶／2026-09-14／總管轉述 | ⭐ **本組重印確認代價成立**：`ui/views/page_02_health.py:278-281` 的 `HEALTH_TABLE_COLUMNS` 九欄逐字含「五桶評等」；`tests/test_wf02_health_skeleton.py:569-571` 把同九欄**逐字**釘死、`:573` 再斷言 `len(...) == 9`。⇒ **改欄名必然讓那道檢查轉紅，要同一批改掉。**<br>⭐ **本組另補**：總分灰態的理由字串在 `ui/views/page_02_health.py:291-293`，逐字「「五桶評等加權」的評等定義未定（本站的「五桶」是總經概念，逐檔評等是另一套 4D Grade），**已送客戶確認，不先湊一個分數出來**」 —— **這一格已經在等您了**<br>⚠️ **「除了這兩處還有沒有第三處手抄同一組欄名」本組沒查 →【未驗】** |
| **三**｜舊版資產配置頁的三個小區塊 | ✅ **三塊都留、升成具名區塊** | 客戶／2026-09-14／總管轉述。⭐ 2026-09-07 同結論：`ui/views/page_04_portfolio.py:57-58` | ⚠️ **前提要更正（決策組已指出，本組未重印該處 →【未驗】）**：三塊裡有一塊「**只做到一半**」——「組合績效」的具名區塊做了，**委派本身沒做**（`page_04_portfolio.py:64-69` 就地自陳）。⇒ **今天它是「有名字、沒內容」。結論不變，變的是「它做到哪裡」** |
| **四**｜選股池要住哪一頁 | ✅ **搬到 ④ 資產配置，緊鄰換股顧問** | 客戶／2026-09-14／總管轉述 | ⭐ **本組重印確認代價 (a) 成立**：`ui/views/page_04_portfolio.py:919-921` 逐字記著守衛 `tests/test_ia_switch_advisor_moved_to_portfolio.py::test_switch_advisor_renders_only_from_the_portfolio_tab` 釘住渲染點**恰好一個**，且 `EXPECTED_RENDER_SITE == "ui/tab3_portfolio.py::render_portfolio_tab"`；`:922` 補記「本頁再畫一次會讓那條全域守衛當場轉紅，線上還會撞 widget key `switch_advise_btn`」。⇒ **「緊鄰」要搬進舊 ④ → 卡裁示 1／5** |
| **六**｜資產配置的差距那一行 | ✅ **案 B：只講金額、不講方向 ＋ 區塊名改「現況 vs 你設定的目標」** | 客戶／2026-09-14／總管轉述 | ⭐ **前提要更正（射程是兩處，不是一處）**，本組重印確認第二處：`ui/tab3_portfolio.py:1404-1405` 逐字「⚠️ 配置偏離 X%（核心 Y% vs 目標 Z%）— 核心過重，**可贖回轉衛星**／衛星過重，**可獲利轉核心**」。<br>⭐ **本組補一格決策組沒講的**：觸發條件不是 `:1403` 而是 **`:1402` `elif _alert:`**，而 `_alert` 定義在 **`:1372`**：`_alert = abs(_diff) > 10`。**「偏離 >10% 才印」成立。**<br>⭐ **再補**：同頁 `:1359` 另有一格「目標偏差（目標核心 Z%）」，**只印百分點、不印金額也不印方向** —— 案 B 落地後要確認這兩處的口徑一致 |
| **七**｜兩件卡在資料源上的事 | ✅ **(1-a) MoneyDJ 同類排名保留、(1-b) 自算四分位關閉一律 ⬜、(2) 無風險利率用替代利率並標註「非央行定存牌告利率」** | 客戶／**2026-09-15**／總管轉述 | ⭐ **客戶 2026-09-15 明示：本題已於當日重拍完成，不是待辦。**<br>⭐ **本組重印確認代價成立**：`ui/helpers/fund/checkup.py:612-613` 那句「約 3 成基金 MoneyDJ 無同類排名資料 → 顯示 —」**是 `st.column_config.TextColumn` 的 `help=` 文案，不是量測結果**；全 repo 沒有任何一行在數它（見 C2 的六行清單）。⇒ **保留同類排名 ＝ 同時保留一個沒有人量過的比例。**<br>⭐ **本組補**：`services/peer_rank.py:8-10` 的誠實護欄逐字確認 —— 同類 < 2 檔不給名次；2~3 檔只給 X/n 不分四分位；≥ 門檻才給真四分位 |
| **八**｜核心／衛星是「看基金名字猜」的 | ✅ **(1) 一律以您自己填的那一欄為準 ＋ (2) 沒填的顯示 ⬜ ＋ (3) 寫死「系統永不自動寫入」三件一起** | 客戶／2026-09-14／總管轉述 | ⭐ **本組逐行重印確認全部四件事，並更正一項、補上兩項** —— 見下方獨立區塊 |

### 問題八的現況更正（前提要改，結論不動）

**✅ 本組逐行 awk 重印確認成立的**：

| 事實 | 出處（本組實測） |
|---|---|
| 它真的是拿名字比對關鍵字，而且核心關鍵字裡有「配息」 | `services/ai_service.py:26-27`（`_CORE_KW` 含「配息」）／`:28-30`（`_SAT_KW` 含「科技」「半導體」） |
| 核心那組先比，比中就不再看衛星 | `services/ai_service.py:40-41`（`if any(kw in name_lower for kw in _CORE_KW): return "core"`）→ `:42-43` 才輪到衛星 |
| 全站不只一套 | ① `services/ai_service.py:26-30`（看名字）／② `ui/helpers/session.py:101-103`（也看名字，**但關鍵字清單不一樣，另有一份 12 檔白名單 `_CORE_WHITELIST`**）／③ `services/health/asset_class.py`（**看類別不看名字** —— ⚠️ **本組未重印此檔 →【未驗】，轉述決策組**） |
| 三個寫入點 | `ui/tab3_portfolio.py:1583-1586`／`ui/tab3_t7_ledger.py:1053-1056`／`ui/helpers/cloud_io.py:251-254`（觸發鈕 `ui/helpers/portfolio/policy_admin_section.py:385`「📦 立即全部寫入」→ `:390-392` 呼叫 `dump_all_to_sheet`）。**三處寫的都是同一個運算式**：`("core" if ….get("is_core") else "satellite" if ….get("is_core") is False else "")` |

**⛔ 本組要更正的兩項**：

1. **「三個寫入點全部住在舊分頁」只對兩個成立。** 第三個（`policy_admin_section.py:385` → `cloud_io.py:251`）
   住在 `ui/helpers/**` 的**共用模組**，今天的活入口是舊 ④（`ui/tab3_portfolio.py:757-760`），
   但新 ⑦ 已經接好線、只差旗標（`ui/views/page_05_settings.py:1355` →
   `ui/helpers/settings_diag/policy_admin_bridge.py:62` 旗標閘門 → `:116` 委派）。
   **詳見裁示 5，它會影響您怎麼答。**
2. **「級別欄叫 `tier` 不叫 `policy_tier`」只說了一半。**
   本組實測：**兩個名字指同一個欄位，而且程式裡有明確的互相映射** ——
   `repositories/policy/v2.py:927` 逐字 `_ren = {"policy_tier": "tier", "fx_avg": "avg_fx"}`；
   同檔 `:130` 另有 `_zh_to_v1 = {**EN_HEADERS_V2, "基金代號": "fund_url", "級別": "policy_tier"}`。
   **v2 新結構的欄名是 `tier`（`:511`，中文表頭「級別」在 `:532`）；v1 舊路徑的欄名是 `policy_tier`，
   而三個寫入點走的正是 v1 路徑**（`cloud_io.py:243-244` 傳的 key 是 `fund_url`，那是 v1 的欄名）。
   ⇒ **第 (3) 件「寫死永不自動寫入」要保護的是同一個欄位的兩個名字，不是新增欄位。**

**⭐ 本組補的一項代價（兩份原料都沒有）**：第 (2) 件（沒填的顯示 ⬜）落地後，
`ui/tab3_portfolio.py:1397-1401` 已經有一句現成的同型誠實文案可以參考 ——
逐字「⬜ 全部 %d 檔都沒填投入本金 → 無法算金額比例，**上方 0%% 不代表真的沒有核心資產**。」
**這是同一頁對「沒填」已經寫過的處理方式。**

⚠️ **您的資料表目前有幾筆已經被蓋掉，本組不知道 →【未驗】**（要讀線上資料，本輪不允許）。
⚠️ **本組的掃法只認「寫進去那一行本身出現 `is_core` 字樣」的寫法** ——
**先把值算進一個變數、再寫出去的寫法，本組結構上看不到。**
⛔ **請不要把「掃到三處」讀成「只有三處」**（**單組實測到的下界**）。

---

# 第 2 節｜資／算／畫三層對照

> ## 🔭 總判
>
> **這張表最重要的不是「哪些還沒做」，是「哪些已經在畫、但畫的名字是錯的」。**
> 本組逐項回程式碼重驗後：**「已在畫」的項目裡至少有 1 項的卡片標題與它的數字不是同一個東西**
> （TER 卡），**而同一份程式在另外四個地方已經把正確的名字寫出來了** ——
> **這個網站自己早就知道正解，只有最顯眼的那一格是錯的。**
> 另外，**兩份原料標為「接線」的「危機回撤」，它的模組其實不是死的**（見 2-C），
> 而**標為「補邏輯」的「12 個月現金流再投資」，它在另一個檔案裡**（見 2-D）——
> **這兩項的難度都被估錯了，一個估太難、一個估太容易。**

**三欄定義**：
**資料在不在** ＝ 原始素材現在抓不抓得到。
**算式在不在** ＝ 從素材變成那個數字的算法寫好了沒。
**畫面在不在** ＝ 使用者按得到的地方，畫不畫得出來。

⚠️ **「畫面在」一律是讀渲染碼推論的，不是本組看到的**（本組沒有打開過畫面）。

---

## 2-A｜三層都在（已經在畫）　狀態：**已在畫**

| 項目 | 資 | 算 | 畫 | 依賴 | 需新資料源 | 出處（本組 awk 重印確認） |
|---|---|---|---|---|---|---|
| **相關性矩陣** | ✅ | ✅ | ✅ **新舊兩邊都掛** | 每檔 NAV ≥30 點 ＋ 共同交易日 ≥2 | ❌ | 舊 ②：`ui/tab_fund_grp_health.py` → `ui/helpers/fund_grp_health_extras.py:26`（`_render_correlation_matrix`）；新 ⑥：`ui/views/page_02_health.py:1739`（import）→ `:1758`（`safe_section("持股相關性矩陣", …)`）。<br>⭐ **門檻實測**：`services/portfolio_service.py:609-610`（`len(f["series"]) >= 30`）＋ `:611-612`（`if len(valid) < 2: return None`）；畫面上的空狀態文案逐字在 `ui/helpers/fund_grp_health/correlation.py:174`：「⬜ 漲跌幅相關係數計算失敗(NAV 資料不足,需 ≥30 筆且共同交易日 ≥2)」 |
| **配置回測五策略** | ✅ | ✅ | ✅ **新舊兩邊都掛** | 需 ≥2 檔有序列 | ❌ | 舊 ②：`ui/tab_fund_grp_health.py:412`（import `render_allocation_backtest_section`）；新 ⑥：`ui/views/page_02_health.py:1736-1737`（import）→ `:1763`（`safe_section("配置回測", …)`）。<br>⚠️ **它的表格欄名含「賣出」「→ 買進」**（`ui/helpers/fund_grp_health/backtest_section.py:181/183/190/194`）⇒ **落在 Q2 射程內，而且新舊兩邊都有** |
| **追蹤誤差** | ✅ | ✅ | ✅ **只在一個檔** | MoneyDJ wb07 風險表 | ❌ | `ui/tab2_single_fund.py:216`（風險指標列 docstring）／`:257`（長版「追蹤誤差 TE(1Y)」）／`:261`（短版「追蹤誤差(1Y)」）。<br>**正對照（本組實跑）**：全 repo `git grep "追蹤誤差" -- '*.py'` → production **只有這一個檔**（另 4 行在 `tests/test_review_fixes_v19_347.py`）；同一把尺對 `Sharpe` 在 `ui/**` 命中 **25 檔** ⇒ 掃描是活的 |
| **基金 Z 值** | ✅ | ✅ | ✅ | NAV 序列（窗 756 交易日、最少 252 有效點） | ❌ | 算式 `services/zscore_engine.py:44`（`def nav_zscore`）；畫在 `ui/tab2_single_fund.py:1539-1541`（lazy import）→ `:1543`（`_zi = nav_zscore(s)`）→ `:1551-1552`（印出）。**路徑**：③ → 🔍 單檔深掘 |
| **操盤評分／上下檔捕捉率** | ✅ | ✅ | ✅ | 月對齊 ＋ 大盤基準序列 | ❌ | 算式 `services/capture_ratio.py`；欄位 `ui/helpers/fund_grp_health/columns.py:251-255`（「上檔捕捉%」，help 含「要漲的月份和跌的月份**各**至少 N 個月才算得出來」）／`:256`（「下檔捕捉%」） |
| **同類排名（MoneyDJ 給的）** | ✅ | 不需算式 | ✅ | MoneyDJ `peer_compare` | ❌ | `ui/helpers/fund/checkup.py:579`（放進表格）／`:610-613`（欄名「同類排名」＋ help）。⚠️ **help 裡那句「約 3 成」沒有量測背書**（見 2-H） |
| **費用（數字本身）** | ✅ **兩份都抓得到** | 不需算式 | ⛔ **在畫，但畫的是另一個東西** | — | ❌ | **見 2-B，本列請單獨看** |

---

## 2-B｜⛔ 畫面在說謊 —— TER 卡　狀態：**已在畫，但名字錯**

| 項目 | 資 | 算 | 畫 | 出處（本組逐行 awk 重印，可自驗） |
|---|---|---|---|---|
| **TER 費用率** | ✅ 兩份都抓得到 | 不需算式 | ⛔ **卡名與數字不是同一個東西** | **(1) 卡片大標**：`ui/helpers/fund/checkup.py:280` 逐字 `💰 TER 費用率{_ter_lbl}`<br>**(2) 卡片的數字**：`:196` `_ter_val = _safe_num(mj.get("mgmt_fee"))` → `:205` 存成 `ter_val` → `:273-274` 卡片讀它<br>**(3) `mgmt_fee` 是什麼**：`repositories/fund/fund_orchestration.py:1082` 逐字 `result["mgmt_fee"] = rows_map.get("最高經理費(%)", "")` —— **那是收費上限**<br>**(4) 真 TER 有抓、沒被這張卡用**：同檔 `:1088-1091` `result["total_expense_ratio"] = (rows_map.get("總費用率(%)") or … )`；另 `:1083-1086` `custody_fee`，就地註解 `:1083` 自陳是「TER 估計第 2 主成分」 |

### ⭐ 本組新查到的：這個 repo 在**同一個檔案裡**已經把正確名字寫出來 **4 次**

> **兩份原料找到 2 處（缺料訊息 `:297`、隔壁表格欄名 `:577`）。本組實測到 4 處。**

| # | 位置 | 逐字內容 | 性質 |
|---|---|---|---|
| 1 ⭐ | `ui/helpers/fund/checkup.py:282` | 「**最高經理費（MoneyDJ 基本資料頁）**」 | ⭐ **這是 TER 卡自己的副標** —— 也就是**同一張卡，大標寫 TER、副標寫最高經理費**。**兩份原料都沒抓到這一處，而它是最強的一項** |
| 2 | `ui/helpers/fund/checkup.py:297` | 「💰 TER 費用率 — ⬜ ⟨代碼⟩ 缺 **mgmt_fee** 資料」 | 抓不到時的文案 |
| 3 | `ui/helpers/fund/checkup.py:577` | 表格欄名「**最高經理費%**」（值就是 `ter_val`） | 隔壁表格 |
| 4 ⭐ | `ui/helpers/fund/checkup.py:608` | 排名欄的 help：「在你載入的這幾檔之間，按**最高經理費**由低到高排名（第1名=最便宜）」 | ⭐ **本組新查到** |

⛔ ⭐ **而且本組查到第 5 處，方向相反 —— 它讓問題更大**：
`ui/helpers/fund/checkup.py:578` 的**欄名是「費用率排名」**，
但 `:608` 它自己的 help 說那是「按**最高經理費**排名」。
⇒ **同一張表裡，值一樣的兩欄，一欄叫「最高經理費%」（對）、一欄叫「費用率排名」（錯）。**

⚠️ **另一項必須一起看的既有決議（本組重印確認）**：
`services/health/grade.py:19-21` 逐字記著「**為什麼是 5D 而不是原訂的 6D**（2026-08-14 實測後改案）—— 費用率：
**官方揭露率實測 0** —— 有值的都是「拿經理費當費用率」，境外基金常差一倍以上，不足以拿來分 A/B。維持現況（大表有欄可看，不進總分）。」
同一份決議也在 `PHASE1_FINAL_ISSUE_LIST.md:304`。
⇒ **這與「把卡片標題改對」不衝突**（一個是「要不要拿它評分」，一個是「有沒有叫錯名字」），
**但它預告了 Q8 選「修數字」的後果：真 TER 的官方揭露率實測為 0。**
⚠️ **本組沒有重量「揭露率 0」這個數字 →【未驗】**（那要打外部 API）。

⚠️ **同型卡在另一處也有**：`ui/helpers/fund_grp_health/investment.py:146`／`:166`。
⚠️ **這兩個 TER 卡掛在哪幾格，本組沒有追呼叫鏈 →【未驗】。**
⛔ **不得據本段主張「新 ② 上有這張卡」或「新 ② 上沒有」。**

---

## 2-C｜接線（算式與資料都在，只差把它接到畫面上）　狀態：**接線**

| 項目 | 資 | 算 | 畫 | 依賴 | 需新資料源 | 出處（本組實測） |
|---|---|---|---|---|---|---|
| **總經匯率面板** | ✅ | ✅ | ⚠️ **部分已在畫** | FinMind 外資 ＋ Yahoo USDTWD | ❌ | ⛔ ⭐ **本組實測要更正總管材料**：材料把它列為「接線（還沒接）」，**但 USDTWD 的最新值今天已經印在正式 ① 上** —— `ui/views/page_01_macro.py:561-566` 算出 `_fx_txt = f"USDTWD {_fx_last:.3f}"`，`:600` 把它放進熱錢卡的註腳（「近 N 天累計，單位為億元新臺幣；⟨USDTWD …⟩。」）。<br>⇒ **正確說法是：一個 USDTWD 數字已經在畫；一個獨立的「匯率面板」本組沒有找到。**<br>⚠️ **「面板」是什麼，需要您定義才知道要接什麼** →【待決定】 |
| **危機回撤** | ✅ | ✅ **完整** | ❌ **沒有畫面在用那兩支函式** | 大盤序列 ＋ 基金 NAV | ❌ | ⛔ ⭐ **本組實測要更正兩份原料**：它們都寫「`services/crisis_backtest.py` **production 0 caller**」。**那是錯的。**<br>**(a) 模組是活的**：`ui/helpers/fund_grp_health/capture.py:60` 逐字 `from services.crisis_backtest import fetch_market_series`（上/下檔捕捉率的基準抓取），而 `capture_by_code` 被 `ui/helpers/fund_grp_health/unified.py:110`／`switch_advisor_section.py:232` 呼叫、`ui/tab2_single_fund.py:923` 也用它的 `_benchmark_nav`。<br>**(b) `app.py:174-181` 就地寫著同一件事**，逐字「它**漏掉了真正讓 crisis_backtest.py 活著的那個消費者**…所以 `services/crisis_backtest.py` …**依然要保留,不是孤兒**」，`:182` 還就地警告「消費者清單是**會漂移的量測值**…**不要引用本行**」。<br>**(c) 真正沒有畫面的是那兩支函式**：`detect_crisis_events`（`:95`）與 `attach_fund_drawdown`（`:189`）—— 本組實測 **production 端除了同檔 `:249`／`:252` 的一條龍包裝之外，沒有任何呼叫**。<br>⇒ **接線難度被高估了**：模組已經在 production import 鏈上，不是要復活一個死模組 |
| **基金 Z 值接進健診大表** | ✅ | ✅ | ⚠️ 只在單檔頁 | — | ❌ | 算式 `services/zscore_engine.py:44`；目前唯一 production 渲染點 `ui/tab2_single_fund.py:1543`。<br>⛔ ⭐ **本組實測補一個關鍵前提**：大表用的**不是同一把尺** —— `ui/tab2_single_fund.py:1556-1558` 就地印著「**此區的 Z-Score 位階**＝近 3 年常態分布；健診/批次大表的「σ rank / 基期」是**距 252 日高點的 σ**…**兩套口徑不同、同一檔可能給不同貴/便宜結論，不可直接互比**」。⇒ **「接進大表」不是接線，是要先決定用哪一把尺** →【待決定】 |

---

## 2-D｜補邏輯（不需新資料源）　狀態：**補邏輯**

| 項目 | 資 | 算 | 畫 | 依賴 | 出處（本組實測） |
|---|---|---|---|---|---|
| **資訊比率（＝超額 ÷ 追蹤誤差）** | ✅ 分母在（2-A 追蹤誤差）、分子在 | ❌ **沒有把兩者相除的地方** | ❌ | 追蹤誤差 ＋ 基準報酬 | ⭐ **本組實跑（決策組標【未驗】，本組補量）**：`git grep -n -E "資訊比率|information_ratio|info_ratio" -- '*.py'` → **0 命中**。⚠️ **若它以別的名字存在，本組這一掃看不到 → 單組實測到的下界** |
| **風險貢獻** | ✅ 共變異數算得出來 | ⚠️ **只到共變異數，沒有拆成逐檔風險貢獻** | ❌ | 權重 ＋ 共變異數矩陣 | `services/portfolio_frontier.py:59-63`（`annualized_moments` 回 `(mu, Sigma, codes)`，`:60` docstring 逐字「Sigma=年化樣本共變異數(ddof=1 ×252),decimal」） |
| **12 個月現金流** | ✅ | ✅ **已可指定任意年月** | ⚠️ **未確認有「連續跑 12 期」的畫面** | 配息歷史 ＋ 節奏推估 | `services/dividend_calendar.py:882` 逐字 `def predict_ex_for_month(schedule: dict, year: int, month: int, …)` —— **年、月都是參數**。⚠️ **本組沒有查有沒有畫面在跑 12 期迴圈 →【未驗】** |
| **配息「再投資」路徑** | ✅ | ✅ **已實作，但沒人用** | ❌ | — | ⛔ ⭐ **本組實測要更正總管材料兩處**：<br>**(1) 它不在 `services/dividend_calendar.py`，在 `services/health/dividend_calc.py`** —— `git grep -n "reinvest\|再投資" -- 'services/dividend_calendar.py'` → **0 命中**。<br>**(2) 「實作好但預設 False」只對一半** —— 參數在 `:232`（`reinvest: bool = False`），**真正的加碼邏輯在 `:335-337`**（`if reinvest and nav_at_ex > 0: cumulative_units += ccy_div_total / nav_at_ex`），而 `cumulative_units` **確實被消費**（`:317` `ccy_div_total = amt * cumulative_units`、`:324` 寫進輸出）⇒ **邏輯是真的會生效的**。<br>**但同一個檔自己說相反的話**：`:246` docstring 逐字「reinvest: True = 配息再投入（**v19.37 暫不實作**，預設 False）」、`:300` 逐字「`cumulative_units = units_held  # reinvest=True 時可成長，**v19.37 鎖死**」。<br>**(3) 「無 caller 傳 True」成立**：`git grep -n "reinvest=" -- '*.py'` → **4 行全部在該檔自己的註解裡，沒有任何呼叫端**。<br>⇒ **正確說法是：邏輯已寫、可生效、0 呼叫端，但檔案自陳「暫不實作／鎖死」—— 要用它之前得先確認那句自陳是過期的還是有效的** →【待決定】 |
| **前瞻衝擊** | — | — | — | — | ⭐ **本組實跑（決策組標「完全未複驗」，本組補量）**：`git grep -n -E "前瞻衝擊|情境模擬|scenario_impact" -- '*.py'` → **0 命中**（唯一沾邊的是 `ui/tab6_manual.py:686` 那段**靜態教學文字**，`:690` 自陳「此圖為**靜態教學示意**」）。⇒ **這一項不是「補邏輯」，是「還沒開始」。** ⚠️ **它同時是裁示 4 的標的** |
| **匯率曝險改按金額** | ✅ 金額與幣別都在 | ⚠️ **現在的算法是數檔數** | ⚠️ **在畫，但畫的是檔數** | — | `ui/tab3_portfolio.py:378-380`：`:379` 逐字 `_fx_counts[_ccy] = _fx_counts.get(_ccy, 0) + 1`（**一檔加一**）、`:380` `_total_fx = sum(_fx_counts.values()) or 1`（**分母也是檔數**）。<br>⭐ **本組補**：同段 `:375-377` 就地註解已經記著一次同型事故（「一句『組合 100% 為 USD 計價』的**錯誤風險警語**…風險揭露區塊拿捏造的幣別當輸入，比不揭露更糟」）—— **改成按金額時，`_UNKNOWN_CCY` 那一桶的處理要一起想** |

---

## 2-E｜要新資料源　狀態：**新資料源**

| 項目 | 資 | 算 | 畫 | 出處（本組實測） |
|---|---|---|---|---|
| **經理人姓名／任期** | ❌ | — | ❌ | `git grep -n -E "manager_name|fund_manager|經理人姓名|基金經理" -- 'repositories/**' 'services/**' 'ui/**'` → **唯一命中**是 `services/capture_ratio.py:3`「衡量基金**經理人操作能力**」—— **那是一個分數，不是一個人**。⇒ **全站沒有存任何基金經理人的姓名或到任日。**<br>⚠️ **單組實測到的下界；若它以別的名字存在，本組這一掃看不到** |

---

## 2-F｜⛔ 抓不到（沒有來源，或本組找不到來源）

| 項目 | 資 | 算 | 畫 | 出處（本組實測） |
|---|---|---|---|---|
| **債券久期／信評分佈／YTM** | ❌ | ❌ | ❌ | `git grep -n -E "duration|久期|信評|信用評等|YTM|存續期" -- 'repositories/**'` → **唯一命中**是 `repositories/news_repository.py:410` 的一份**新聞分類關鍵字清單**（`"bond": [… "duration", "coupon" …]`）—— **不是基金欄位**。**正對照**：同一把尺對 `nav` 命中 **12 檔** ⇒ 掃描是活的。<br>⭐ **這一項是裁示 4 甲案的擋路石** |
| **未對沖比例** | ❌ | ❌ | ❌ | ⭐ **本組實跑（決策組標【未驗】，本組補量）**：`git grep -n -E "未對沖|unhedged|hedge_ratio|避險比例" -- '*.py'` → **0 命中**（含 tests） |
| **同類使用者比較** | ❌ | ❌ | ❌ | 沒有多租戶基建。⚠️ **本組沒有查證這一句 →【未驗】**，轉述決策組。⭐ **但本組確認了它容易被誤認的那一項**：`services/peer_rank.py:1` 逐字「同類**基金**四分位排名」—— **是基金不是使用者**（見 2-H） |

---

## 2-G｜性質不同，不是資料問題

| 項目 | 說明 | 出處（本組重印確認） |
|---|---|---|
| **稅務** | 這是**規則引擎要不要做**的問題，不是資料抓不抓得到。**而且畫面上已經在誠實揭露它沒算稅** | `services/policy_fee_optimizer.py:45-50`，`:46` 逐字「⚠️ 教學參考,非賣出建議。」、**`:47` 逐字「**未計入**:海外所得/最低稅負、基金贖回/轉換費與短線交易罰則、以及贖回後放棄的複利。」**、`:48-49` 另補「高 S 往往也是已實現獲利最大、課稅最不利的一檔」 |
| **自算四分位** | 算式寫好了而且**自帶誠實護欄**（樣本不足就不給名次）；卡住的是分類與樣本量，不是算式 | `services/peer_rank.py:7-10`（`§1 Fail-Loud —— 樣本量誠實`：<2 檔不給名次；2~3 檔只給 X/n **不分四分位**；≥門檻才給真四分位） |
| **無風險利率（台灣）** | **注入口是現成的**，缺的是台灣那個數字的來源 | ⚠️ **本組未重印 `services/fund_service.py:34,39` →【未驗】**，轉述決策組 |

---

## 2-H｜⭐ 同名陷阱（獨立一節）　**⛔ 一個都不准寫成「已完成」**

> **這張表擋的是一種特定的錯**：看到一個名字很像的東西，就以為那件事已經做好了。
> **本組逐條打開看過，每一條都是「名字像、東西不同」。**
> ⚠️ **本表是分類清單，不是窮舉** —— 「沒有第 N+1 個」本組不宣稱。

| # | 看起來像 | 實際是什麼 | 出處（本組 awk 重印） | 狀態 |
|---|---|---|---|---|
| 1 ⭐ | **「TER 費用率」卡** | **最高經理費**（收費上限），不是總費用率 | 卡名 `ui/helpers/fund/checkup.py:280`；取值 `:196` ← `mgmt_fee` ← `repositories/fund/fund_orchestration.py:1082`「最高經理費(%)」 | ⛔ **未修** |
| 2 ⭐ | **同一張表裡「費用率排名」欄** | 按**最高經理費**排名（它自己的 help 就這麼說） | 欄名 `ui/helpers/fund/checkup.py:578`；help `:608` | ⛔ **未修（本組新查到）** |
| 3 | **`contribution_by_fund`** 像風險貢獻 | **報酬**貢獻 —— 逐字「各檔對組合**報酬**的貢獻 = 權重 × 該檔於共同期間報酬(百分點)」 | `services/portfolio_performance.py:193-195` | ⛔ **未修** |
| 4 | **`peer_rank`** 像同類**使用者**比較 | 同類**基金**四分位排名 | `services/peer_rank.py:1` | ⛔ **未修** |
| 5 ⭐ | **「Z 值」與大表的「σ rank／基期」** | **兩套完全不同的口徑**：Z 值＝近 3 年常態分布；σ rank＝**距 252 日高點的 σ** | `ui/tab2_single_fund.py:1556-1558` 逐字「**兩套口徑不同、同一檔可能給不同貴/便宜結論,不可直接互比**」 | ✅ **畫面上已揭露** —— ⭐ **這是全表唯一一個已經處理好的，可以當成其他項目的正例範本** |
| 6 ⭐ | **① 的「停利 Z ≥ +1.75」與 ③ 的「停利門檻」** | **① 是固定值、③ 是隨景氣燈號變動** | ① `ui/views/page_01_macro.py:785` 傳 `None` → `services/allocation_ladder.py:46-48` 回 `default`；③ `ui/tab2_single_fund.py:1545` 傳 `get_ndc_score()` → `services/zscore_engine.py:92` → `shared/signal_thresholds.py:406` 五組動態值 | ⛔ **未揭露（本組新查到）** —— **直接影響裁示 2 的 C 案** |
| 7 | **`ER`** 像 Expense Ratio（費用率） | **Kaufman 效率比** —— 量「趨勢 vs 來回震盪」，與費用無關 | `services/fund_type_classifier.py:3-4` 逐字「用 **Efficiency Ratio(Kaufman ER)**…ER = \|NAV[末] − NAV[首]\| / Σ\|NAV[i] − NAV[i−1]\|」 | ⛔ **未修** |
| 8 ⭐ | **「經理人操作評分」** 像經理人資料 | **是一個分數，不是一個人** —— 全站沒有存任何基金經理人姓名或任期 | `services/capture_ratio.py:3` vs 2-E 那一列 | ⛔ **未修** |
| 9 ⭐ | **「規模」的數字** | **格式不統一到會錯 90 倍**：2 檔就撞出 2 種字串（`266.04 億(美元)` vs `58,185.32 百萬歐元`），**單位差 100 倍 ＋ 幣別差一層，而解析錯不會報錯** | `services/health/grade.py:22-23`；同一份決議另記於 `PHASE1_FINAL_ISSUE_LIST.md:305` | ⛔ **未修（已明文決定不納入評分）** |
| 10 | **「約 3 成／約 7 成」** 像一個量測結果 | **是滑鼠提示文案，程式裡沒有任何一行在數它** | 本組實跑 `git grep "約 3 成"` → **6 行 / 4 檔**，其中只有 `ui/helpers/fund/checkup.py:613` 講同類**排名**，另 5 處（`SPEC.md:712`、`checkup.py:7`／`:407`／`:474`、`ui/tab6_manual.py:588`）講的是同類**平均**。「7 成／七成」作為覆蓋率 **0 命中**（正對照：「約」命中 219 檔） | ⛔ **未修** |
| 11 ⭐ | **`policy_tier` 與 `tier`** 像兩個欄位 | **同一個欄位的兩個名字**，程式裡有明確映射 | `repositories/policy/v2.py:927` 逐字 `_ren = {"policy_tier": "tier", "fx_avg": "avg_fx"}`；`:130` `_zh_to_v1 = {…, "級別": "policy_tier"}`；v2 欄名 `:511` `tier`，中文表頭 `:532`「級別」 | ⛔ **未統一（本組新查到）** —— **直接影響問題八第 (3) 件** |
| 12 | **`v2_holdings` / `v2_transactions`** 像現有的資料表 | **這兩個名字在整個程式庫 0 命中。** 現行的是另一份 **10 欄**結構 | `repositories/policy/v2.py:506-517`（`policy_id, fund_code, fund_name, currency, tier, invest_twd, div_cash_pct, units, avg_nav, avg_fx`）；中文表頭 `:527-538`。⚠️ **本組未重跑 `v2_holdings` 的 0 命中 →【未驗】**，轉述線框組 | ⛔ **未建** |
| 13 ⭐ | **「同類基金」與「同類使用者」** | `peer_rank` 是**基金**；**使用者比較完全不存在**（沒有多租戶基建） | 同 #4 ＋ 2-F | ⛔ **未修** |
| 14 ⭐ | **「五桶評等」** 像逐檔評等 | **本站的「五桶」是總經概念，逐檔評等是另一套 4D Grade** | `ui/views/page_02_health.py:292` 逐字「「五桶評等加權」的評等定義未定（本站的「五桶」是總經概念，逐檔評等是另一套 4D Grade）」 | ⛔ **未修（已送客戶，＝問題二）** |

---

# 第 6 節｜依賴關係圖

> ## 🔭 總判
>
> **您給的下界是「接線 4 項、補邏輯 5 項、新資料源 1 項」。本組用第 2 節實際盤到的項目重排後，
> 接線與補邏輯的分類都要調整** —— **不是您數錯，是兩項的分類本身錯了**：
> 「危機回撤」被當成接線一個死模組（**實際那個模組是活的，只有兩支函式沒畫面**），
> 「再投資」被當成補邏輯（**實際邏輯已寫完，缺的是一個決定：它的自陳「暫不實作」過期了沒**）。
> **而整張圖真正的根節點不在這十項裡 —— 是 Q2。**
> **Q2 未定之前，① 與 ③ 上任何一項都不能動，因為不知道要動到哪裡為止。**

## 6-1 本組盤到的項目 vs 您給的下界（據實對照）

| 分類 | 您給的（下界） | **本組在第 2 節實際盤到的** | 差在哪 |
|---|---|---|---|
| **接線** | 4 項 | **3 項**（總經匯率面板、危機回撤、Z 值接大表） | ⚠️ **本組只盤到 3 項。** 您的第 4 項本組**沒有對應到** —— 可能是本組漏看，也可能是它在本組沒碰的範圍（例如「三欄卡／四層」那一類版面接線）。⛔ **本組不宣稱「只有 3 項」** |
| **補邏輯** | 5 項 | **6 項**（資訊比率、風險貢獻、12 個月現金流、**再投資路徑**、前瞻衝擊、匯率曝險改金額） | ⭐ **多一項**：本組把「再投資路徑」從「12 個月現金流」裡拆出來獨立計 —— 因為它**住在另一個檔案**（`services/health/dividend_calc.py`，不是 `dividend_calendar.py`），**依賴與風險都不同** |
| **新資料源** | 1 項 | **1 項**（經理人姓名／任期） | ✅ 一致 |

⚠️ **「3／6／1」同樣是本組單組實測到的下界，不是母體數。**
⚠️ **另有 3 項本組列在「抓不到」而不是「新資料源」**（久期／信評／YTM、未對沖比例、同類使用者比較）——
**理由**：它們不是「找一個新 API 就能解決」，其中兩項（未對沖、同類使用者）在現有架構下**沒有可行的來源**。
**若您要把久期算成第 2 個新資料源，那是裁示 4 的甲案，請一起看。**

## 6-2 依賴圖

```
┌──────────────────────────────────────────────────────────────────────┐
│ ⭐ 根節點（不在那十項裡，但它擋住其中四項）                             │
│                                                                      │
│   Q2（合規紅線射程）                                                  │
│     ├─→ 決定 ① 的 A／B 兩條鏈要不要動  ……… 擋住：Z 值接大表（它要畫在 ③）│
│     ├─→ 決定 ③ 的 E／F／G 三處要不要動 ……… 擋住：Z 值接大表、危機回撤  │
│     └─→ 決定 ④ 的 C 與 ⑤ 的 D 要不要動 ……… 擋住：匯率曝險改金額（在舊 ④）│
│                                                                      │
│   裁示 1／5（舊分頁能不能動）                                          │
│     └─→ Q2 選乙／丙時，這兩道才是真正的施工閘門                        │
└──────────────────────────────────────────────────────────────────────┘

【第 1 波｜不卡任何人，Q2 怎麼答都能做】
  ① 風險貢獻（補邏輯）
      前置：無。`services/portfolio_frontier.py:59-63` 的 Sigma 已經算得出來
      產出：逐檔風險貢獻（純計算，不產生任何動作句）
      ⚠️ 落點未定 —— 要畫在哪一頁取決於 ④ 的版面，那屬設計組

  ② 資訊比率（補邏輯）
      前置：無。分子分母都在手（追蹤誤差在 `ui/tab2_single_fund.py:257`）
      ⛔ 但它的**顯示位置**在 ③（追蹤誤差現在只在那裡）→ 若 Q2 選乙／丙，③ 要先解禁
      ⇒ **算式可以先做；接畫面卡 Q2**

  ③ 12 個月現金流（補邏輯）
      前置：無。`services/dividend_calendar.py:882` 已參數化年月
      ⚠️ 「有沒有畫面在跑 12 期迴圈」本組未查 →【未驗】，開工前要先查

【第 2 波｜要先有一個決定，但不是 Q2】
  ④ 再投資路徑（補邏輯）
      前置：**一個決定** —— `services/health/dividend_calc.py:246`／`:300` 自陳「v19.37 暫不實作／鎖死」，
            但 `:335-337` 的邏輯是真的會生效的。**要先確認那句自陳是過期的還是有效的**
      依賴：③（12 個月現金流）—— 兩者都吃配息歷史，口徑要一致
      ⇒ **③ 先做，④ 跟著做**

  ⑤ 匯率曝險改按金額（補邏輯）
      前置：**裁示 1／5**（它在舊 ④ `ui/tab3_portfolio.py:379`）
      ⚠️ 另有一個設計決定：`_UNKNOWN_CCY` 那一桶怎麼算（同段 `:375-377` 有前車之鑑）

  ⑥ 總經匯率面板（接線）
      前置：**先定義「面板」是什麼** —— USDTWD 的數字已經在 ① 的熱錢卡註腳（`page_01_macro.py:600`）
      ⇒ 這是設計題不是工程題

【第 3 波｜卡 Q2】
  ⑦ 危機回撤（接線）
      前置：Q2（它要畫在哪一頁還沒定；若畫在 ③ 就卡舊分頁）
      ⭐ **難度比原本估的低**：模組已在 production import 鏈上（`capture.py:60`），
         只有 `detect_crisis_events`／`attach_fund_drawdown` 沒有畫面
      ⚠️ 開工前要先確認：它輸出的「跌幅」會不會被讀成一種買賣暗示 → 回到 Q2

  ⑧ Z 值接進健診大表（接線）
      前置：**Q2 ＋ 一個口徑決定**
      ⛔ 大表用的是另一把尺（σ rank，距 252 日高點），`ui/tab2_single_fund.py:1556-1558`
         已經就地寫明「不可直接互比」⇒ **接進去之前要先決定用哪一把尺**
      ⚠️ 而 Z 值的產出點 `services/zscore_engine.py:107/109/111` 就是動作詞
         （停利/減碼、加碼/逢低）→ 直接落在 Q2 射程內

【第 4 波｜卡裁示 4，而且要新資料源】
  ⑨ 前瞻衝擊（補邏輯 → 實際是「還沒開始」）
      前置：**裁示 4**（要久期版還是 Beta 版）
      若選甲（久期版）→ 還要等 ⑩

  ⑩ 經理人姓名／任期（新資料源）
      前置：無技術依賴，但**沒有已知來源**
      ⚠️ 它與久期／信評／YTM 是同一類問題：現有三個資料源（MoneyDJ／FundClear／TDCC）
         都沒有這些欄位（本組實測，見 2-E／2-F）
      ⇒ **這一項的第一步不是寫程式，是找來源**
```

## 6-3 誰先誰後（本組推薦的施工順序）

| 順位 | 做什麼 | 為什麼排這裡 |
|---|---|---|
| **0** | **回答 Q2** | ⭐ 它擋住 ⑦⑧，並決定 ①③④⑤ 四個分頁的改動範圍。**不答 Q2，第 3 波完全動不了** |
| **1** | **風險貢獻 ＋ 資訊比率（只做算式，不接畫面）** | 兩者都是純計算、不產生動作句、不碰任何分頁 ⇒ **Q2 怎麼答都不會白做** |
| **2** | **12 個月現金流 → 再投資路徑** | ③→④ 有真實依賴（口徑一致）。⚠️ 開工前先查「有沒有 12 期迴圈的畫面」 |
| **3** | **總經匯率面板（先定義）** | 設計題，可與 1、2 並行 |
| **4** | **匯率曝險改金額** | 卡裁示 1／5 |
| **5** | **危機回撤 ＋ Z 值接大表** | 卡 Q2；且 Z 值那一項還要先定口徑 |
| **6** | **前瞻衝擊 ＋ 經理人資料** | 卡裁示 4；且需要先找到資料源 |

⚠️ **這個排序是本組的推薦，不是事實。** 它建立在「**先做不會白做的**」這一個原則上 ——
**若您的優先序不同（例如就是要先看到危機回撤），請直接推翻本排序。**

---

# 第 8 節｜待您裁示清單

> ## 🔭 總判
>
> **這一節只放「只有您能決定」的事，共 11 項。**
> **判準（總管裁決，本組照用）**：問「這個問題我有沒有能力自己判斷？」
> **有 → 自己判，丟出去就是卸責；沒有（只有您知道業務意圖、時間與優先序）→ 才放這一節。**
> **依這把尺，本組把第 1 節的 30 項刷掉了 19 項** —— 那 19 項不是不重要，
> 是**本組判斷得出來，或者它們是另一項的子問題**（例如 M1 是 Q2 的子問題，C2／C3／C4／C8 是事實題）。
> ⭐ **而剩下的 11 項裡，有 5 項卡在同一個上游：Q2。**

## 8-A 清單（11 項）

| # | 要您定的 | **為什麼只有您能決定** | 本組有推薦嗎 |
|---|---|---|---|
| **1** ⭐ | **Q2｜合規紅線的射程**（甲窄／乙中／丙寬／丁加免責） | **這是「您要的產品是什麼」的問題。** 甲＝① 這一輪不動；乙＝要解禁四個舊分頁；丙＝等於決定換股顧問／輪動配對的去留；丁＝等於決定「免責句能不能抵銷動作句」。**本組判斷不出您要哪一個** | ⛔ **不給**（理由見 1-0） |
| **2** | **裁示 1｜問題五的甲／乙／丙** | **兩邊都是您自己的指示**（「改掉該買該賣的句子」vs「舊分頁不改內容」）。本組沒有立場替您排先後 | ⛔ **不給** |
| **3** | **裁示 5｜「舊分頁不改內容」的射程** | **那道裁示是您下的，它的射程只有您能定。** 它現在卡住問題四、六、八，**若 Q2 選乙／丙還會再卡 D／E／F／G 四處** | ⚠️ **部分給** —— 只就「這三題該不該用同一把尺」給推薦（不要用同一把尺）；**射程本身不給** |
| **4** | **裁示 2／問題九｜「📐 建議資產水位」那張卡**（A／B／C） | **您已經拍過板，而您是在錯的數字上拍的。** 依規矩本組不會自己把您拍過的板改掉 | ⭐ **給：推薦 C** —— 但附一個新警告（① 固定門檻 vs ③ 動態門檻），請連著看完 |
| **5** | **裁示 3｜SPY／QQQ 的範圍** | `docs/wireframes/ia-wireframe.html:470` 那一份是**您 2026-09-01 親自拍板的**。**動它要您點頭；不動它、改 `SPEC.md:1640` 也要您點頭。** 本組不會自己決定哪一份文件說了算 | ⭐ **給：推薦甲（不動）** —— 但標明它成立的前提（「只有這一處」）**兩組都沒驗過** |
| **6** | **裁示 4｜傳導要哪一種**（甲久期版／乙 Beta 版） | **甲和乙回答的是兩個不同的問題**，不是同一件事的兩種做法。**只有您知道您想看到的是哪一個** | ⛔ **不給** |
| **7** | **Q1｜① 的表單欄位要哪一組** | **這是「您想怎麼操作這一頁」。** ⭐ 而且**程式自己掛牌在等您**（`page_01_macro.py:2086-2088` 那行灰字現在就印在正式 ① 上） | ⛔ **不給** |
| **8** | **Q4｜舊分頁什麼時候退場**（順序與第一前置已拍板，只問時間點） | **排程與優先序，只有您知道** | ⛔ **不給** |
| **9** | **Q7｜「四色」指哪一個四** | **命名的定義權在您。** ⚠️ **這一題有前科** —— 總管自陳「我對『四色 vs 五態』的假設整個錯了」 | ⛔ **不給**（本組完全沒碰第四節 →【未驗】） |
| **10** | **Q10｜主 CTA「更新淨值與配息」要不要做、落在哪一頁** | **功能取捨 ＋ 動線決定。** ⭐ 它在 repo **0 命中**，而**「它應該存在」這件事沒有人查證過** —— 也就是它可能從來不是既有規格 | ⛔ **不給** |
| **11** | **M3｜① 這一格統一標成什麼** | **標籤會被後續每一份文件引用，標錯就會把您第 9 條禁令的界線弄模糊** | ⭐ **給：推薦「① ＝ 正式格 ＋ 新 View，無預覽緩衝」**（兩組的寫法各漏一半，理由見 M3） |

### ⛔ 本組刻意**沒有**放進這一節的（以及為什麼）

| 沒放的 | 為什麼不放 |
|---|---|
| **C2／C3／C4／C8** | **它們是事實題，不是決定題。** 本組已用 awk 重印裁定（誰的行號對），不需要您裁示 |
| **M1** | ⭐ **它是 Q2 的子問題。** 合稿組把它獨立列出，**但本組實測後認為分開答會得到互相矛盾的射程** ⇒ **併進第 1 項** |
| **M2** | ⭐ 同理 —— 它問的是「要不要等 Q2」，**答完 Q2 它就消失了** ⇒ 併進第 1 項與第 4 項 |
| **C1／C5／C6／C7** | C1→M3（已列第 11）、C5→M1（併入第 1）、C6→M2（併入第 1／4）、C7→**它用的詞正是 Q2 在問的**（併入第 1） |
| **Q3** | ⭐ **它與第 4 項（裁示 2／問題九）是同一張卡的兩個問法。** 分開答會得到兩個不一致的結論 ⇒ **併進第 4 項** |
| **Q5／Q6** | 它們是**設計紀律的鬆緊**（第四層幾個展開、按鈕字幾個詞）。⚠️ **本組沒有碰第三節，本來就不該由本組送到您面前** —— 若設計與驗收組另外送，以他們的為準 |
| **Q8** | ⭐ **本組有能力判斷，所以本組判了**（推薦：改卡名）。**依判準，有能力判斷還丟出去就是卸責** ⇒ 留在第 1 節，不進本節 |
| **Q9** | 同上（推薦：統一） |
| **Q11／Q12** | **顏色語意的定義權確實在您**，但**本組完全沒有碰第四節**，把一個沒查過的題目送到您面前只會浪費您的時間 ⇒ **交給設計與驗收組**，本組只負責**不讓它們消失**（已列在 1-A-2，標「客戶未點名、但仍未決」） |
| **問題一～八的結論** | **已定案，不重開**（禁止事項 8）。本組只更正現況 |

## 8-B 相依關係圖

```
                        ┌─────────────────────────────┐
                        │  ⭐ 1. Q2（紅線射程）        │  ← 5 項卡在它後面
                        └───────────┬─────────────────┘
              ┌─────────────────────┼─────────────────────┐
              ▼                     ▼                     ▼
   ┌──────────────────┐  ┌────────────────────┐  ┌──────────────────┐
   │ 4. 裁示 2／問題九 │  │ 2. 裁示 1（甲乙丙） │  │ 3. 裁示 5（射程） │
   │   ＋ Q3 ＋ M2     │  │   （選乙/丙時，要   │  │  （選乙/丙時，要  │
   │  （這張卡是否跨線 │  │   解禁的舊分頁      │  │   多解禁 ③ 與 ⑤） │
   │    取決於 Q2）    │  │   從 1 個變 4 個）  │  │                  │
   └──────────────────┘  └─────────┬──────────┘  └────────┬─────────┘
                                   │                       │
                         ┌─────────┴───────────────────────┴─────────┐
                         ▼                 ▼                 ▼
                    問題五（說明書）   問題六（正式 ④）   問題四（選股池搬家）
                                                        問題八（三顆鈕・急迫）

   ── 以下四項不卡任何人，可以單獨回 ──
   5. 裁示 3（SPY／QQQ）………… 獨立
   6. 裁示 4（傳導要哪一種）…… 獨立，但**範圍最大**（它決定「要不要做這件事」）
   7. Q1（① 表單欄位）………… 獨立（程式自己掛牌在等）
   8. Q4（舊分頁退場時間）…… 獨立（但它是第 2、3 項的下游施工排程）
   9. Q7（四色）……………… 獨立（設計組的題目）
  10. Q10（主 CTA）………… 獨立
  11. M3（① 的標籤）……… 獨立，但**它會被後續每一份文件引用**
```

## 8-C 回答順序建議（本組重排，並說明為什麼與決策組不同）

**決策組給的順序是**：裁示 5 → 裁示 1 → 裁示 2 → 裁示 3 → 裁示 4。
**合稿組補了一格**：Q2 卡著四個下游，而它「一句話就能答」。

### ⭐ 本組的順序（與兩者都不同）

| 順位 | 回答什麼 | 為什麼排這裡 |
|---|---|---|
| **1** | **M3（① 的標籤）** ⭐ | ⭐ **本組把它從「獨立、可以晚點答」提到第一位，理由是它最便宜而且會污染其他每一題**：它是**一句話**、**不卡任何人**、**但後面每一題的討論都要用到「① 是新的還是舊的」這個標籤**。<br>**兩份原料在這一點上不同調（C1），而它們的討論已經因此出現不一致。** 先釘死標籤，後面九題的對話成本立刻降低 |
| **2** | **Q2（紅線射程）** ⭐ | **它卡著 5 項。** ⛔ **但本組不同意合稿組「一句話就能答」的描述** —— 射程可以一句話寫完，**代價從 4 處到 71 檔候選差一個數量級**，而且乙／丙會當場改寫裁示 1 的選項。**請看完 1-0 那張表再答** |
| **3** | **裁示 5（舊分頁射程）** | ⭐ **本組同意決策組把它排前面，但理由不同。**<br>決策組的理由是「問題八現在每按一次那三顆鈕就在發生」。**本組實測後認為這個理由仍然成立，但它的急迫性被一個新事實稀釋了一點**：三個寫入點裡**有一個（`policy_admin_section.py`）根本不在舊分頁檔內**（見裁示 5），⇒ **那一個可以在不解禁舊分頁的情況下先修掉**。<br>**本組排它第 3 的真正理由是：它與 Q2 的乙／丙案是同一道閘門** —— 先答 Q2 再答它，才不會答兩次 |
| **4** | **裁示 1（甲／乙／丙）** | 它卡著問題五、六、四三題。**排在裁示 5 之後**，因為若裁示 5 把射程定寬，甲乙丙的選項會改變 |
| **5** | **裁示 2／問題九 ＋ Q3**（同一張卡，一起答） | 卡在 Q2 之後。⭐ **本組把 Q3 併進來** —— 它與問題九是同一張卡的兩個問法，分開答會得到兩個不一致的結論 |
| **6** | **Q1（① 表單欄位）** | 獨立、便宜，而且**那行「待客戶裁決」的灰字現在就印在正式 ① 上** —— 答了就能清掉一句畫面上的內部對話 |
| **7** | **裁示 3（SPY／QQQ）** | 獨立。選甲＝0 改動 |
| **8** | **Q4（舊分頁退場時間）** | 它是第 3、4 項的**下游**：舊分頁能不能改都還沒定，先談什麼時候拔會白談 |
| **9** | **裁示 4（傳導要哪一種）** | ⭐ **本組把它從決策組的第 5 位往後移到第 9 位，理由是它的前置條件根本還沒有**：甲案要久期，而久期**沒有任何已知來源**（2-F）。<br>⇒ **這一題現在只能答「乙或不做」，答「甲」等於開一個找資料源的新專案。**<br>⚠️ **若您就是要甲，請直接說 —— 那會改變整個第 6 節的排程** |
| **10-11** | **Q7（四色）／Q10（主 CTA）** | ⭐ **本組把它們排最後，理由是本組沒有查證過它們**（Q7 屬第四節，本組完全沒碰；Q10 的「它應該存在」沒有人查證過）。<br>**在一份自己沒查過的題目上催您決定，是在浪費您的時間。** 建議等設計與驗收組的第 3、4 節到齊再一起看 |

⚠️ **這個順序是本組的推薦，不是事實。** 它建立在兩個原則上：
**(a) 先答會污染其他題的**（M3 的標籤）；**(b) 先答卡最多人的**（Q2）。
**若您的原則不同（例如就是要先止血問題八），請直接推翻本排序** ——
**問題八的第 (3) 件在裁示 5 答完之前就可以先做掉一半**（那個不在舊分頁裡的寫入點）。

---

# 附錄 A｜本組推翻總管給的材料的 8 條

> **您交辦時說「今天已有十組推翻過我，每次都對」。本組又找到 8 條。**
> **每一條都附本組實際跑的東西。⚠️ 全部單組實測，未經第二組驗證。**

| # | 材料原文 | 本組實測 | 嚴重度 |
|---|---|---|---|
| **1** ⭐ | 「危機回撤（引擎 `services/crisis_backtest.py` 完整但 **production 0 caller**）」 | **不成立。** `ui/helpers/fund_grp_health/capture.py:60` 逐字 `from services.crisis_backtest import fetch_market_series`，而 `capture_by_code` 被 `unified.py:110`／`switch_advisor_section.py:232` 呼叫、`ui/tab2_single_fund.py:923` 也用它。**`app.py:174-181` 就地寫著同一件事**，逐字「它**漏掉了真正讓 crisis_backtest.py 活著的那個消費者**…**依然要保留,不是孤兒**」。<br>**成立的是較弱的版本**：`detect_crisis_events`（`:95`）與 `attach_fund_drawdown`（`:189`）沒有 production 畫面 | ⭐ **高** —— 它讓這一項的接線難度被高估 |
| **2** ⭐ | 「12 個月現金流（`services/dividend_calendar.py::predict_ex_for_month` 已參數化任意年月；**再投資路徑實作好但預設 False、無 caller 傳 True**）」 | **前半對，後半的位置錯了。** `git grep "reinvest\|再投資" -- 'services/dividend_calendar.py'` → **0 命中**。再投資在 **`services/health/dividend_calc.py:232`**（參數）／`:335-337`（邏輯，**會生效**，`cumulative_units` 於 `:317`／`:324` 被消費）／**0 個呼叫端**。<br>**而且那個檔自己說相反的話**：`:246`「v19.37 **暫不實作**」、`:300`「v19.37 **鎖死**」 | ⭐ **高** —— 派到錯的檔案會找不到東西 |
| **3** ⭐ | 「① 的兩條合規鏈（⛔ **只改一條等於沒改**）」＋「第四處（舊 ④）」＝ **合計四處** | **嚴重低估。** 本組實測到的下界是 **7 個獨立產出點，跨 ①③④⑤ 四個分頁**（1-0 那張表）。<br>**兩份原料與本材料都沒有找到 E（`services/health/action.py:69-84` 的九宮格動作矩陣）、F（`services/zscore_engine.py:107/109/111`）、G（`ui/helpers/macro/helpers.py:100-105` 的 8 組買賣指示，渲染於 `ui/tab2_single_fund.py:688`）。**<br>⭐ **而 G 直接用「買進」「賣出」兩個字，E 直接用「考慮換」** | ⭐⭐ **最高** —— 它直接決定您第一優先那件事的射程 |
| **4** ⭐ | 「總經匯率面板」列為**接線**（還沒接） | **部分不成立。** USDTWD 的最新值**今天已經印在正式 ① 上**：`ui/views/page_01_macro.py:561-566` 算出 `_fx_txt`，`:600` 放進熱錢卡的註腳 | **中** |
| **5** | 「`ui/tab3_portfolio.py:1404-1405`（**條件在 `:1403`**，偏離 >10% 才印）」 | **條件在 `:1402`（`elif _alert:`），`:1403` 是 `st.caption(`。** 門檻定義在 **`:1372`**（`_alert = abs(_diff) > 10`）。**句子的行號 `:1404-1405` 正確，「>10%」也正確** | **低**（結論不受影響） |
| **6** | 「那五句在 `composite_score.py:305-316` 與 `explain.py:247-258` **有兩份逐字相同的副本**」 | **五個動作句確實逐字相同（本組逐句對讀）。** ⚠️ **但「兩份逐字相同」不精確**：`explain.py:255` 的顏色是字面值 `"#ff8a80"`，`composite_score.py:313` 是常數 `MD_RED_A100`。**要改的是五個字串，不是整段複製** | **低** |
| **7** | 「費用與規模已有反對決議：`services/health/grade.py:20`「官方揭露率實測 0」；`PHASE1_FINAL_ISSUE_LIST.md:305`」 | **兩處都成立**，但**行號對應要換**：`grade.py:20` 講的是**費用率**、`:22-23` 才是**規模**；`PHASE1_FINAL_ISSUE_LIST.md:304` 是**費用率**、**`:305` 才是規模**。⇒ **材料把「費用」指到 `:305`（規模那一列）** | **低** |
| **8** ⭐ | 「問題八的三個寫入點**全住舊分頁**」（裁示 5 的前提） | **只對兩個成立。** 第三個（`ui/helpers/portfolio/policy_admin_section.py:385` → `ui/helpers/cloud_io.py:251`）住在 `ui/helpers/**` 共用模組，**新 ⑦ 也已接線**（`ui/views/page_05_settings.py:1355` → `policy_admin_bridge.py:62` 旗標 → `:116` 委派） | ⭐ **中高** —— 它讓「不解禁舊分頁就不能修」這個前提部分不成立 |

### 另外三條不是材料的錯 —— 兩條是兩份原料互相不一致（本組裁定），一條是 repo 自己的檔案在說謊

| # | 爭點 | 本組 awk 重印的裁定 |
|---|---|---|
| **a** | 說明書那兩句「贖回／轉入」在哪 | 決策組寫 `tab6_manual.py:478-480`，**實際在 `:481-482`**（`:478` 是 ` ``` `、`:479` 是公式、`:480` 是空行） |
| **b** | 說明書有幾章含動作句 | 決策組說 **2 章**（`:89` 再平衡公式、`:90` 核心衛星）；**本組實測 3 章** —— 多一個 `:93` 🗺️ 全局指標關聯地圖（`:686-688`）。⚠️ 另有一章的**標題本身**是動作句：`:91`「🔄 **汰弱留強**（同類 PK）」 |
| **c** ⭐ | **repo 有一個檔案的 docstring 在說謊** | `ui/tab_fund_research.py:38-42` 逐字「**⚠️ 尚未接線（刻意）** —— 本檔**還沒有**被 `app.py` 掛上去」。**今天是假的**：`app.py:201` 匯入它、`:662` 呼叫它（本組 awk 重印確認）。<br>⚠️ **這與線框組記載的 `ui/views/page_02_health.py:1802-1812` 那次是同一種失效模式**：**檔案自陳的接線狀態過期，而讀檔的人會照著它推論。**<br>⭐ **它為什麼重要**：本組的 E／F／G 三處（Q2 最重的三處）的可達路徑**正好經過這個檔** —— **若照它的 docstring 讀，會得出「③ 的單檔深掘沒有掛上去、所以那三處印不出來」的錯誤結論** |

---

# 附錄 B｜本組沒驗到什麼、為什麼

> **這一節不是免責聲明，是射程說明。下一個接手的人請從這裡開始補。**

## B-1 整類沒驗到的

| # | 沒驗什麼 | 為什麼 |
|---|---|---|
| 1 | **第 3、4、5、7 節的內容** | 那是設計與驗收組的產出，**本組依指示完全沒有讀、沒有對答案** |
| 2 | **第四節（顏色／四色／五態）整節** | 不在本組射程。⇒ **Q7／Q11／Q12 本組全部轉述，無獨立查證** |
| 3 | **第三節（四層閱讀順序／按鈕字／展開數）整節** | 同上。⇒ **Q5／Q6 本組全部轉述** |
| 4 | **`docs/wireframes/` 底下任何一個 `.html`** | **一個字都沒讀。** ⇒ 裁示 3 的 `ia-wireframe.html:470` 那一列是轉述 |
| 5 | **`ui/views/page_04_portfolio.py` 的大部分**（`:49`／`:53`／`:653`／`:665`／`:2070-2081`／`:2761`） | 本輪的查證額度用在 Q2 的射程上。⇒ **問題一、問題三的現況欄全部轉述** |
| 6 | **您的線上 Google Sheet** | 不允許讀。⇒ **問題八「有幾筆已被蓋掉」無法回答** |
| 7 | **`policy_admin_bridge` 旗標的實際預設值** | 只讀到 docstring 自陳「預設關閉」與 `:62` 的閘門，**沒有實跑 `owned_by_settings_page`** |
| 8 | **那 71 個含動作詞的檔，逐行判讀** | 只做到「檔案層級命中」。**裡面一定混著註解與變數名**，本組只人工判讀了 `ui/**` 的前 30 行樣本 |
| 9 | **「9/14 舊分頁不改內容」那道裁示的原始記錄** | `git grep` 找不到。**三組都找不到，但三組都找不到不等於它不存在** |
| 10 | **兩份原料裡標【未驗】的每一項** | 本組沒有逐項替它們補驗，只補了與 Q2／第 2 節直接相關的那些 |

## B-2 本組的全稱句清單（**一律不得被引用為已查證的事實**）

⚠️ **以下每一句都是：單組實測、未經第二組驗證、取決於有沒有漏看。**

1. 「① 上的合規動作句至少有 7 個獨立產出點」（**下界，不是母體數**）
2. 「71 個 production `.py` 含動作詞／其中 29 檔含買進·賣出·出清」（**字面 grep；別名匯入與執行期組出的字串掃不到**）
3. 「`資訊比率`／`information_ratio`／`info_ratio` 全 repo 0 命中」（同上）
4. 「`未對沖`／`unhedged`／`hedge_ratio`／`避險比例` 全 repo 0 命中」（同上）
5. 「`前瞻衝擊`／`情境模擬`／`scenario_impact` 全 repo 0 命中」（同上）
6. 「久期／信評／YTM 在 `repositories/**` 的唯一命中是新聞關鍵字清單」（同上；**若它以別的名字存在，掃不到**）
7. 「全站沒有存基金經理人的姓名或任期」（同上）
8. 「`detect_crisis_events`／`attach_fund_drawdown` 沒有 production 呼叫端」（同上）
9. 「`reinvest=` 沒有任何呼叫端」（同上）
10. 「`追蹤誤差` 在 production 只有 `ui/tab2_single_fund.py` 一個檔」（**有正對照：`Sharpe` 在 `ui/**` 命中 25 檔**）
11. 「說明書含動作句的是 3 章 7 行」（**下界** —— 本組用 15 個動作詞掃，換一個說法就掃不到）
12. 「`約 3 成` 是 6 行 / 4 檔」（**有正對照：`約` 命中 219 檔**）

⛔ **本文沒有任何一句宣稱「沒有第 N+1 個」。**
凡是「取決於有沒有漏看」的問題，本文一律只寫分類、不寫窮舉 ——
沿用 `CLAUDE.md §-1.5.1c 判定 2` 那條方法教訓：**「能被一條 grep 推翻的全稱句，就不該寫進規格。」**

## B-3 本組的方法紀律（照做的與沒做到的）

**照做的**：
- **所有 `file:line` 一律 `git show origin/main:<path> | awk 'NR>=s && NR<=e'` 直接印原始行號**，
  **不經任何 `grep -v` / `sed` 過濾**。**本組沒有從任何一份原料照抄行號。**
- **每一條掃描都附正對照**（`NAV` 115 檔／`nav` in repositories 12 檔／`Sharpe` in ui 25 檔／`約` 219 檔）。
- **沒有打開畫面** ⇒ 所有「現況」都寫成「**在程式判斷成立時會出現**」。

**沒做到的**：
- ⛔ **本組沒有做「別名匯入」的防護。** `from x import foo as _bar` 之後 `foo(` 掃不到 ——
  **本組所有函式名掃描都有這個結構性盲點，一次都沒有繞過它。**
- ⛔ **本組沒有做「執行期組出的字串」的防護。** 例如 `page_01_macro.py:2031` 用的是
  `where_to_find('health')` 而不是字面分頁名 —— **同型的動態字串，本組的動作詞掃描全部看不到。**
- ⛔ **本組只判讀了動作詞掃描結果的前 30 行樣本**，其餘 71 檔沒有逐行看。

---

**第 1、2、6、8 節到此結束。**

⚠️ **最後一句，照規矩講清楚**：
**這四節由本組單獨產出，沒有第二組複驗過。**
本組這一輪推翻了總管交辦材料的 **8 條**、裁定了兩份原料互相不一致的 **2 處**，
並找到兩份原料都沒有找到的 **3 組合規動作句**（E／F／G）——
**這個比例本身就是一個訊號：請不要假設沒被本組點到的地方都是對的。**
