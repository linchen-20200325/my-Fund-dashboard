# UI_SPEC_AUDIT_R6 —— 第六輪規格稽核（獨立驗證客戶五條裁示）

> **性質**：純唯讀稽核。本輪**未改任何檔案、未 commit／未開 PR／未建分支／未 merge**，
> **未碰共用工作樹的工作區**（只用 `git show` / `git archive` / `git ls-tree` / `git rev-parse` / `git for-each-ref`），
> **未讀寫任何 Google Sheets**、**未打任何外部 API**、**未觸發任何 workflow**、**未碰 `pr829`~`pr834`**。
> PR #845 維持 draft（本輪未動）。
> ⛔ **本輪不裁決任何一條**：矛盾一律 A/B 並陳。
> **基準**：`origin/main` ＝ `9cbf0377`（工作樹 HEAD 為 `d0c2a8d`，**全程未使用**）。
> **本輪是單組產出，沒有第二組看過。**

---

## 方法

1. **原始碼一律取自 `origin/main`**：`git archive origin/main | tar -x` 解到本組自己的唯讀工作區
   （`scratchpad/audit/r6tree`，677 個 `.py`），逐行引用另以
   `git show origin/main:<path> | awk 'NR==N'` 單獨印出後照抄。**全程未用 `grep -v` 數序號。**
2. **每條掃描同批跑正控＋負控**；**負控為本輪現編的隨機串**，依客戶紀律**不寫進本文件、不寫進回報**。
3. **一律 AST，不用字面 grep 下結論**；字面 grep 只用來產生「候選」，命中後人工逐行判讀。
4. **字表永遠追不上措辭** —— 本輪的動作詞字表在總管那份窄字表（9 詞）上再加 39 詞（抄底／接刀／進場／
   出場／布局／佈局／承接／獲利了結／落袋／續抱／觀望／該換／可換／汰弱／留強／加倉／減倉／下一步／
   可以考慮／別急著／該減碼／該補進／大幅調整／水位／換手／調整／移轉／轉出／轉入／贖回／申購／
   定期定額／再平衡／配置比例／該買／該賣／操作策略／部位／持有／清倉／加投／減投），
   **但主要手段仍是逐行語意閱讀** —— 見第 (7) 節。
5. **本組自己的掃描器也跑正控**：第 (3) 節的巢狀偵測器以自造檔驗證（內層確實標 NESTED、外層標 TOP-LEVEL）。

### 射程外揭露（本組**沒有**做的事）

- **沒跑任何測試、沒啟動 Streamlit。** 本輪所有「到得了畫面」都是靜態讀碼。
- **沒讀線框 `docs/wireframes/*.html`**、**沒讀 `SPEC.md`／`ARCHITECTURE.md`／`STATE.md`**。
  `docs/v2/CONSTITUTION.md` 只讀本輪逐條核對的片段（`:66-80` 的 F1~F6 表）——
  ⚠️ **該檔不在 `origin/main` 上**，本輪自 `origin/docs/v2-constitution-governance`（＝ #837 那條仍為 draft 的分支）讀取。
- **沒有重建盤點表的 265 列逐列事實**（本組重跑的是**計數與結構**，不是每一列的出處是否為真）。
- **窮舉句一律不宣稱**：「`build_mk_advisor_prompt` 沒有第 3 個 import 端」「`ui/views/**` 沒有第 15 個網格呼叫」
  「本檔沒有第 N 個矛盾」—— 動態 import／`getattr`／執行期組出的名字，AST 與字面掃描都看不到。
- **裁示 2（#837 合併）不在本輪射程內**，未驗。

---

## 十件事逐項結論

### (1) 裁示 1 —— `ai_prompts.py:72-79` 納入 P0 第四項 ✅ 全數成立

| 查什麼 | 結果 |
|---|---|
| §H 是否為第 4 列 | ✅ `DECISION_LOG:246` §H 表有序清單：**1 ＝ ④-6／2 ＝ ②-12／3 ＝ ①-4／4 ＝ `ai_prompts.py:72-79`**，`:266` 另以文字複述同一順序並註明「順序是客戶給的」 |
| 範圍寫死在那八行 | ✅ `DECISION_LOG:264`「**第 4 項的範圍就是 `:72-79` 那八行，寫死，不外推**」；`UI_SPEC:490`「**範圍就是 `:72-79` 這八行，客戶裁示 1 寫死，不外推**」 |
| 明寫不外推 | ✅ 同上，兩檔各一處 |
| 七列逐字引文 | ✅ **7／7 逐字相符**（本組以程式比對，非目視）：`:72`／`:73`／`:74`／`:76`／`:77`／`:78`／`:79`，每一列都與 `git show origin/main:services/ai_prompts.py` 的該行**完全相同（含全形括號與粗體標記）** |
| 掛在「現況引文・待修」表頭下 | ✅ `UI_SPEC:489` 表頭逐字「**現況引文・待修（原樣引用，⛔ 不是本規格的建議…）**」，七列在其下（`:494-500`） |
| 標「需 F1 ＋ F4 兩個豁免通道」 | ✅ `DECISION_LOG:56`（B25 標題）＋ §H 第 4 項通道欄「**同時撞 F4 與 F1，而且這兩格今天都沒有豁免 → 兩格都要走 §5 母法修正提案單**」 |

**凍結面逐行實印（全部 `git show origin/main:<path> | awk 'NR==N'`，8 行全部與宣稱相符）**：

```
services/ai_prompts.py:30     def build_mk_advisor_prompt(*, phase: str, score, alloc_str: str,
services/ai_service.py:23     from services.ai_prompts import build_mk_advisor_prompt
services/ai_service.py:130    def analyze_portfolio_mk_advisor(api_key: str, portfolio_funds: list,
services/ai_service.py:334        prompt = build_mk_advisor_prompt(
ui/tab3_t7_ledger.py:59       from services.ai_service import analyze_portfolio_mk_advisor
ui/tab3_t7_ledger.py:3290-3296  _mk_btn = _mk_cols[0].button( "🤖 生成 策略3 深度建議", key="btn_mk_advisor", … )
ui/tab3_t7_ledger.py:3355         _mk_txt = analyze_portfolio_mk_advisor(
ui/tab3_t7_ledger.py:3401         st.markdown(st.session_state["mk_advisor_txt"])
ui/tab3_t7_ledger.py:3402         st.caption("⚠️ AI 建議僅供參考，最終決策需自行判斷市場與風險承受度")
ui/tab3_portfolio.py:78       from ui.tab3_t7_ledger import T7InputAbort, render_t7_section
ui/tab3_portfolio.py:2141             render_t7_section()
app.py:673                            render_portfolio_tab()
```

**凍結格歸屬（對照 `CONSTITUTION.md:72-77`，讀自 `origin/docs/v2-constitution-governance`）**：
F1 ＝ 16 個 `ui/tab*.py`（`ui/tab3_t7_ledger.py`、`ui/tab3_portfolio.py` 都在內）；F4 ＝ `services/**`
（`ai_prompts.py`、`ai_service.py` 都在內）。**F4 ＋ F1 的判定成立。**
⚠️ 據實補一句：鏈路末端的 `app.py:673` 屬 **F3**，§H 通道欄只寫 F4 ＋ F1。
**這不是漏寫** —— `app.py` 只是上游呼叫者、不必被修改；且 §H 自己就寫明 B8 那格豁免涵蓋 `app.py`（F2／F3）。

#### ⭐ 重點打：「只改 prompt 字串則 F1 端一個位元組不必動」

**本組獨立結論：這句話在「程式會不會壞」這個意義上成立；在「畫面會不會說謊」這個意義上，它漏了兩處 F1 畫面文案。**

- **成立的那一半（逐行讀完整段 `:3299-3402`）**：F1 端**完全不解析模型輸出** ——
  只有 `:3367` 的 `_mk_txt.startswith(("❌","⚠️"))` 錯誤前綴判斷，然後 `:3401 st.markdown(...)` 原樣印出。
  `services/ai_service.py:341` 同樣是 `return call_llm(prompt, …)` 純轉發，無後處理。
  → **改 prompt 的字串字面，F1 與 `ai_service` 都不必改一個位元組，程式行為不會壞。**
- **這句話沒說到的那一半（本輪新查，兩處，兩檔皆 F1）**：
  - **`ui/tab3_t7_ledger.py:3239`**（在 `:3232-3242` 的 `st.markdown(..., unsafe_allow_html=True)` 裡，**真的印在畫面上**）：
    `"AI 4 節結構：3 大缺點 / 換股建議 / 配置比例 / 高賣低買 vs 跌就買</span>"`
    —— 這是**寫死在 F1 的四節目錄**，其中「**配置比例**」正對應 §H 第 4 項要改掉的 `:76-77`。
  - **`ui/tab3_t7_ledger.py:3263`**（`not_ready(...)` 灰態文案）：`"AI 給不出景氣位階對應的換股建議;"`。
- **`UI_SPEC` 與 `DECISION_LOG` 兩檔對 `:3239`／`:3263` 皆 0 命中**（正控：`3401` 在兩檔各命中 1／2 次；負控 0）。
- ⚠️ §H 通道欄自己已寫「**要動畫面文案／按鈕 help／那句免責，才吃到 F1**」——
  **所以這句話內部是自洽的**；缺的是**沒有任何一處登記那兩行畫面文案的存在與位置**，
  於是「只改 prompt」這條路看起來是免費的，實際會留下一個仍在承諾「配置比例」的畫面標頭。
  **這件事決定要不要開第二張提案單，故據實列為矛盾 M-1（見矛盾清單，A/B 並陳，本組不裁決）。**

### (2) import 端 ✅ 1 vs 2 的形狀差異成立

**方法**：AST 掃全 677 個 `.py`，同時收 `ImportFrom`（含 `as` 別名）、`import services.ai_prompts as X` 模組式、
`ast.Attribute` 屬性存取、以及 `ast.Call` 呼叫點。**不是字面 grep。**

| 符號 | production import | production 呼叫 | tests |
|---|---|---|---|
| `build_mk_advisor_prompt` | **1**：`services/ai_service.py:23` | 1：`services/ai_service.py:334` | `tests/test_ai_prompts.py`（`ImportFrom` 起於 `:7`，符號本身在 `:8` — B25 寫 `:8`，**指的是符號所在行，不是錯的**） |
| `analyze_portfolio_mk_advisor` | **1**：`ui/tab3_t7_ledger.py:59` | 1：`ui/tab3_t7_ledger.py:3355` | 0 |
| `build_structured_summary_prompt` | **2**：`ui/helpers/ai_summary.py:37` ＋ `ui/views/page_01_macro.py:146` | 2：`ai_summary.py:109`、`page_01_macro.py:1873` | 1 import／3 呼叫 |

- **別名 import：全 repo 0**；**模組式 `import services.ai_service as ai`：只有 `tests/test_ai_service.py:2`**；
  **屬性存取形態（`mod.build_mk_advisor_prompt`）：0**。
- → **「1 vs 2」成立**，`build_mk_advisor_prompt` 的下游只有舊 ④ 一條，**不波及 ①-11**；
  `build_structured_summary_prompt` 確實兩個端、其中一個就是 ①-11（F2）。
- **正控**：`build_structured_summary_prompt` 在 `ui/views/**` → 3 行（`:146`／`:1818` docstring／`:1873`），與規格記載逐字相同。
  **`mk_advisor`／`analyze_portfolio_mk` 在 `ui/views/**` → 0 行。** 負控 0。
- ⚠️ **一處措辭不一致（不影響結論，列在矛盾清單 M-5）**：`DECISION_LOG` §H 末段把
  `page_01_macro.py:1873` 稱為「另一個 **import 端**」，而 `:1873` 實測是**呼叫點**，import 在 `:146`。
  同一件事在 B25 與 `UI_SPEC` ②-13 都寫成「`:146`／`:1873`」或只寫 `:146`。

### (3) 裁示 5 —— 尺補「頂層」 ✅ 補上了；兩方向 0 塊 ✅ 本組獨立重跑完全重現

- **§1.5 第 1 條第三句已補**（`UI_SPEC:72`）：「…或一次**頂層**卡片網格呼叫」。
- **「頂層」已釘成「不被另一個同型容器包住」**（`:74`）：頂層 `safe_section` ＝ 不被另一個 `safe_section` 包住；
  頂層卡片網格 ＝ **不被另一個卡片網格包住**。
- **另一讀法已明文排除**（`:74`）：「⛔ **不是**『不在任何 `safe_section` 裡』：那個讀法會讓 `:2176` 併進 ④-3、
  區塊數掉到 **41**，正是客戶用『維持 42 塊』排除掉的那一個。」

**本組自寫 AST 掃描重跑（不沿用生產組的腳本）**：

| 檔 | `safe_section` | `render_cards` | `card_row`／`card_grid` | `st.tabs` |
|---|---|---|---|---|
| `page_01_macro.py` | 1 | 8 | 0 | 0 |
| `page_02_health.py` | 13 | 1 | 0 | 0 |
| `page_03_research.py` | 5 | 1 | 1 | 0 |
| `page_04_portfolio.py` | 10 | 0 | 2 | 0 |
| `page_05_settings.py` | 7 | 1 | 0 | 0 |
| **合計** | **36** | **11** | **3** | **0** |

- **卡片網格 11 ＋ 3 ＝ 14**（①8／②1／③2／④2／⑤1）—— **與規格逐頁逐格相同。**
- **巢狀判定：`render_cards`／`card_row`／`card_grid` 落在另一個 `with card_row/card_grid(...)` 的 body 內者 ＝ 0／14。**
- **`safe_section` 落在另一個 `safe_section` 的 callback 函式內者 ＝ 0／36。**
- → **該合 0 塊、該拆 0 塊，本組獨立重現。**
- **偵測器正控**（自造檔）：內層 `render_cards`／`card_grid`／`card_row` 三個確實被標 NESTED、外層標 TOP-LEVEL；
  巢狀 `safe_section` callback 也確實被標 NESTED。**負控 0。**
- **讀法 B 的後果也實測到了**：`page_04_portfolio.py:2176 card_row` 的 enclosing function 是 `_render_policy`，
  而 `_render_policy` 正是 `:2771`／`:2781` 兩處 `safe_section(BLOCK_POLICY, _render_policy)` 的 callback
  → **讀法 B 之下它確實不是頂層、確實會併進 ④-3 → 41。規格對這一句的描述屬實。**
- **順帶把 §1.6 的另一半也重跑了**：五種掃描前四種 ＝ **50** 個呼叫點（9／14／7／12／8）；
  第 5 種 H2~H4 標題 ＝ **34**（8／5／4／9／8），**逐頁行號與 `UI_SPEC:152-165` 的清單完全相同**；50 ＋ 34 ＝ **84** ✅。
  ⚠️ **本組第一次跑只得 31**，原因是**本組自己的常數解析器只處理 `ast.Assign`、漏了 `ast.AnnAssign`**
  （`CONCLUSION_HEADING: str = "### …"`／`EVIDENCE_HEADING: str = "### …"`）。**修好後即為 34。**
  **那是本組的工具缺陷，不是規格的錯** —— 據實記下，因為它正是本 repo 反覆出現的那個形狀：**工具選錯 → 結論必錯。**

### (4) M1 —— ④-8 來源欄 ✅ 兩鍵有先後、截字已記；⚠️ 盤點狀態仍留位（見矛盾 M-2）

- **兩鍵有先後**：`UI_SPEC:595` 逐字「讀**兩個 session 鍵，有先後**……先讀 `_t3_cur_sheet_title`……**讀不到才退** `policy_sheet_id`」。
- **實印核對（`git show origin/main:ui/views/page_04_portfolio.py`）**：

```
1697|     _title = str(st.session_state.get("_t3_cur_sheet_title") or "").strip()
1698|     if _title:
1699|         return _title
1700|     _sid = str(st.session_state.get("policy_sheet_id") or "").strip()
1701|     # 沒有標題但有 ID → 給 ID 的前段（**事實**），不是給一個猜出來的名字。
1702|     return f"{_sid[:14]}…" if _sid else ""
```

  → **順序寫死在程式碼裡，規格描述屬實。** `:1694-1695` 的「不編一個『我的投資組合』出來」亦逐字相符。
- **退第二鍵印前段截字 ＋ `…`** ：✅ `UI_SPEC:595` 已記，指向 `:1702`，與實作相符。
- **⛔ 兩個鍵的值一律不列出**：✅ 規格與本報告皆未列出任何實際值。
- **盤點狀態留位**：`UI_SPEC:595` 寫「**盤點表狀態：盤點組本輪查證中，結論回填此處**」，`:607` 寫「本組不代判、不代填」。
  **這一半沒有完成 → 矛盾 M-2。**

### (5) 裁示 3／4 —— 盤點表 ✅ 增列與 M2 兩處都做了；四組數字本組獨立重算全部重現

**`_t3_cur_sheet_title` 增列（§3.5，`10_db_inventory.md:1758`）**：

| 欄 | 表上寫的 | 本組實測 |
|---|---|---|
| 狀態 | `未落地` | ✅ `ui/helpers/io/json_backup.py:44-52` 回傳 **7 個鍵**（`schema_version`／`exported_at`／`portfolio_funds`／`t7_ledgers`／`t7_scenarios`／`active_policy_id`／`policy_sheet_id`）—— **不含它** → 重繪即消失，`未落地` 站得住 |
| 誰讀它 | `UI` | ✅ `_book_title()` 由 `page_04_portfolio.py:1894` 呼叫 → `:1903-1909` 進 `STATUS_BOOK_LABEL`（`:649` ＝「📒 目前帳本」）狀態格 → **使用者看得到** |
| 寫入端 | `policy_admin_section.py:181` | ✅ `st.session_state["_t3_cur_sheet_title"] = _sheet_title_q`，且 `:180` 確有 `if _sheet_title_q:` 守著 |
| 讀取端 | `page_04_portfolio.py:1697` ＋ `policy_admin_section.py:174` | ✅ 兩處逐行實印相符 |
| 失效三條路 | `:326`／`:461` `pop`；`infra/cache.py:728` | ✅ 三處全部實印相符 |

**M2 兩處自述由 12 改 11 ✅，且都附出處**：
- `10_db_inventory.md:613`（§0.4 (4) 登記表）：「（~~12~~ → **11**-key 群組列）」＋ 三條實測出處。
- `10_db_inventory.md:2023`（§5.2 `查不到` 桶輪翻轉表）：「~~本列標題寫 12 key~~ → **標題已於 2026-09-16 收斂輪改為 11 key**」。

**第三處活的同型自述：查不到。** 另外兩處（`:195-199` §0 收尾登記 (c)、`:2747-2751` §9.3）
**是歷史輪次紀錄，且兩處都已就地加上「→ ✅ 已於 2026-09-16 收斂輪改完」的狀態更新註**，
並各自寫明「本段原文一字未改 —— 它記的是**那一輪**為什麼停手」。**依本 repo 的既有慣例，那不是活的自述。**
`:1371`（§2.15 標題）與 `:1868`（§5.1）今日皆為 **11**，三處一致。

**四組數字 —— 本組自寫 parser 獨立重算，四組全部重現**：

| 量的是什麼 | 表上宣稱 | 本組實測 |
|---|---|---|
| 逐節資料列數 | 89／82／61／33 ＝ 265 | **89／82／61／33 ＝ 265** ✅ |
| 狀態分布 | 活的 111／未落地 27／只寫不讀 112／只讀不寫 9／查不到 4／已停用 2 ＝ 265 | **111／27／112／9／4／2 ＝ 265** ✅ |
| 誰讀它 | UI 114／內部 63／無 55 ＝ 232 | **114／63／55 ＝ 232** ✅（§1 43/28/18、§2 23/22/37、§3 48/13/0） |
| 不變量 | 0 | **0** —— 兩組總和皆 ＝ 265；誰讀它總和 ＝ §1＋§2＋§3 ＝ 232；**欄數不齊的資料列 0 列** |

**兩個解析陷阱本組都處理了，並各自驗過**：
- **陷阱一（`~~查不到（production）~~` 的刪除線）**：本組的狀態解析先**剝掉 `~~…~~` 區段再取箭頭後的現行值**；
  若改用字面 grep，`查不到（production）` 那個變體會被劃掉的文字遮蔽。實測該變體**現行 0 列**，與表上宣稱一致。
- **陷阱二（`split("|")` 靜默吃掉含 `\|` 的列）**：本組改以 `re.split(r'(?<!\\)\|', line)`（**不切跳脫的 `\|`**）。
  **實測：全檔 §1~§4 的資料列，欄數與各自表頭完全一致，0 列被丟掉。**
- **另有一個本組差點踩到的**：`位元組讀回？` 欄讓 5 張表變成 **6 欄**，若用「只認 5 欄」的寫法會**漏掉 43 列**
  （本組第一版就是這樣，誰讀它一度只數到 189）。**改以表頭索引定位欄位後即為 232。**
  這 43 列與 §5.3 自陳的「5 張表 43 列」相符。

### (6) 規格計數 ✅ 全數重現，灌水無殘留

**本組自寫 parser（未沿用 `UI_SPEC §1` 那一行）**：

| 量的是什麼 | 宣稱 | 本組實測 |
|---|---|---|
| 區塊 | 42（①10 ②12 ③4 ④8 ⑤8） | **42（10／12／4／8／8）** ✅ |
| 標籤 | 沿用 27／沿用（部分）4／新建 10／不標標籤 9／查不到 0 ＝ 50 | **27／4／10／9／0 ＝ 50** ✅ |
| 四件 | 42 × 4 ＝ 168，實測 168，缺漏 0、重複 0 | **168，42 塊每塊 `- 來源`／`- 規則`／`- 空狀態`／`- 回答` 各恰好 1 次，異常 0** ✅ |
| `grep -c '^### '` | 50 | **50** ✅ |
| 差額 | 0 | **0**：50 ＝ 42 活的區塊標題 ＋ 6 個排除清單小標（甲乙丙丁戊己）＋ 2 個退役標題（~~①-5~~／~~②-2~~） ✅ |
| 多標籤區塊 | ①-1／①-4／②-1／②-13／④-2／④-5 六塊，多出 8 | **同上六塊，42 ＋ 8 ＝ 50** ✅ |

- **灌水殘留：無。** 生產組自陳曾在 ④-8 來源行寫了粗體 `**沿用**` 導致算成 28／51（`DECISION_LOG:490`）。
  **本組今日實測為 27／50**，該粗體字串已改成「」引號，**殘留 0**。
- ⚠️ **順帶抓到一處過期的量測值（列為矛盾 M-6）**：`UI_SPEC:49` 寫「實測naive grep：沿用 **33**、
  沿用（部分）**6**、新建 **10**、不標標籤 **11**、查不到 **2**」，**本組今日以 `grep -oF` 重跑得 39／7／15／12／3**。
  那一行**沒有量測日**。

### (7) 母法禁令 —— 獨立重掃三檔 ✅ 新增引文全部合規

**(a) 六個逐字禁用詞**（`建議買進`／`立即出清`／`應該加碼`／`推薦`／`必漲`／`目標價`）掃
`services/ai_prompts.py`／`services/ai_service.py`／`ui/tab3_t7_ledger.py`／
`ui/helpers/fund_grp_health/ai.py`／`ui/helpers/ai_summary.py`／`ui/helpers/fund_grp_health/risk.py`：
**全部 0 命中，唯一例外是 `ui/tab3_t7_ledger.py:1990` 的 `help="A 推薦：其他保單已有的基金直接借過來，免抓取；…"`**
—— 與規格記載逐字相同（另一個功能的說明文案，不在本鏈上）。

**(b) 寬字表（48 詞）逐檔命中數 ＋ 同批負控**：

| 檔 | 寬字表命中行 | 負控 |
|---|---|---|
| `services/ai_prompts.py` | 18 | 0 |
| `services/ai_service.py` | 5 | 0 |
| `ui/tab3_t7_ledger.py` | 98 | 0 |
| `ui/helpers/fund_grp_health/ai.py` | 5 | 0 |
| `ui/helpers/ai_summary.py` | 1 | 0 |
| `ui/helpers/fund_grp_health/risk.py` | **3** | 0 |

**窄字表（`加碼|減碼|買進|賣出|停利|停損|換出|逢低|建議`）對 `risk.py` 得 0 的那個坑，本輪重現並補上**：
寬字表在 `risk.py` 抓到 `:156`（docstring）、`:203`「可考慮抄底」、`:204`「不要接刀」——
**三處規格都已登記**（`UI_SPEC:353-355` ②-11 的現況引文表）。

**(c) 逐行語意閱讀 —— 本輪新增引文是不是以規格口吻寫成建議？結論：不是。**
本組把兩份文件的行**先剝掉所有反引號 code span**，再用寬字表掃剩下的文字：
`UI_SPEC` 17 行、`DECISION_LOG` 11 行命中。**逐行讀完，28 行全部落在四類，沒有一行是投資建議**：
① 引文表的「命中」欄在說明那句引文違反母法哪一條；② 在描述字表本身；
③ 工程指令（「把這八行改成不指名標的、不給配置百分比、不給寫死水位的形式」）；④ 記帳式敘述。

**(d) 引文形式**：`ai_prompts.py` 的 `:72`／`:73`／`:74`／`:76`／`:77`／`:78`／`:79` 七句
在 `UI_SPEC` 出現時**七句全部在反引號內**（程式檢查，非目視）。
四張引文表（①-4 `:218`、②-11 `:351`、②-12 `:385`、②-13 `:442`、P0 第 4 項 `:492`）
**表頭全部是「現況引文・待修（原樣引用，不是本規格的建議）」**。
`DECISION_LOG` 只有一處出現那七句之一（`:448` 的 `:76`，**在反引號內**），
而它在 **§G4**，不在 §H 也不在 B25 —— **`DECISION_LOG:492` 那句「§H 與 B25 只描述性質、不複述那些句子」因此為真。**

### (8) 三件 scope 外事項

**(a) `:67-69` 與 `:81-89` 同型但未裁示、已只記位置 —— ✅ 成立。**
兩處在 `UI_SPEC:508` 與 `DECISION_LOG:264` **只寫位置與性質**；本組程式比對確認
**`:67`~`:69`／`:81`~`:89` 的十一句原文，在兩份文件中一句都沒有被複述**。
同型判定也成立：`:69` 要求每點以「**建議行動**：…」收尾；`:83-84` 何時賣／何時買；`:89` 必要現金水位／不超過總資產 X%。
⚠️ **但那個「另有**兩段**」的列舉不完整 → 矛盾 M-3。**

**(b) `_book_title()` 退第二鍵把 sheet id 前 14 字印上畫面 vs §C-3「永不回顯值或片段」—— ✅ 張力真實存在，本組不裁決，見矛盾 M-4。**

**(c) `DECISION_LOG` F-14 那一列欄數與其餘 F 列不同 —— ✅ 成立，且是全檔唯一一處。**
表頭（`:168`）`| # | 待裁示事項 | 兩邊各是什麼 | 誰該決定 |` ＝ **4 個內容欄**；
`F-1`~`F-13`、`F-15` 十四列皆 **4 欄**；**`F-14`（`:183`）只有 3 欄** ——
「待裁示事項」與「兩邊各是什麼」兩欄之間**少一根 `|`，兩欄併成一欄**。
**本組把 `DECISION_LOG` 全檔所有表格掃了一次：欄數不齊的只有這 1 列；`UI_SPEC` 全檔 0 列。**
⚠️ 欄數的絕對值取決於數法（本組剝掉首尾空欄後是 4 對 3；含首尾空欄則是 5 對 4）——
**「F-14 比其餘 F 列少一欄」這個事實兩種數法都成立。**

### (9) 字面清除清單 vs 動態鍵 —— ✅ 落差確認；全 repo 同型碰撞只有這一處

- **`infra/cache.py:721-731`** 的 `_GLOBAL_REFRESH_SESSION_KEYS` 是**字面元組**，`:728` 放的是
  `"_t3_cur_sheet_title"`（不帶後綴）。
- **消費端 `infra/cache.py:820-826`** 是 `for _k in _GLOBAL_REFRESH_SESSION_KEYS: … if _k in session_state: session_state.pop(_k, None)`
  —— **精確比對，沒有任何前綴或萬用字元比對。**
- **`ui/sidebar.py:274`** 組的是 `f"_t3_cur_sheet_title:{_sid_sb}"`（**每本帳本一個**），`:275`／`:280` 讀寫它，`:284` 顯示。
  → **字面清單清不到它。落差確認。**
- **有沒有別的？** 本組 AST 掃全 repo 的 `st.session_state[...]`／`.get`／`.pop`／`.setdefault`，
  **並把「先把 f-string 指派給區域變數、再拿變數當 key」這種形態一併解開**（`sidebar.py:274` 正是這一種，
  只認 inline f-string 的掃描**看不到它**）。結果：**production 有 9 族動態 key、26 個存取點**：

| 動態 key 前綴 | 位置 | 在清除清單裡嗎 |
|---|---|---|
| `_t3_cur_sheet_title:` | `ui/sidebar.py:275`／`:280` | **前綴在，帶後綴的不在 → 清不到** ⚠️ |
| `_ai_struct` | `ui/helpers/ai_summary.py:71`／`:89`／`:126` | 不在（清單根本沒有這個名字） |
| `_tab5grp_shield_` | `ui/helpers/fund_grp_health/ai.py` 6 處 | 不在 |
| `shield_` | `ui/tab2_single_fund.py` 4 處 | 不在 |
| `_stknews_` | `ui/tab2_single_fund.py` 4 處 | 不在 |
| `_calc_invest_` | `ui/tab2_single_fund.py` 4 處 | 不在 |
| `corr_result_` | `ui/tab3_portfolio.py:639`／`:640` | 不在 |
| `t7c_sell_codes__` | `ui/tab3_t7_ledger.py:2276`／`:2278` | 不在 |

- → **「字面清單 vs 動態名字」這個形狀，在本 repo 只有 `_t3_cur_sheet_title` 這一處是真正的碰撞**
  （清單**點名了**那個前綴，卻清不到帶後綴的那一族）；其餘八族**清單從來沒有主張要清它們**，是另一個問題。
- ⚠️ **`_ai_struct` 值得單獨記一筆（本輪順帶查到，不在派工射程內，不下判定）**：
  它是 ②-13／①-11 那條 AI 輸出的快取 key，同樣是動態組出來的
  → **側欄「全域刷新」清不到已快取的 AI 文字。**
- ⚠️ **本組另有 135 個「以變數當 key、但那個變數不是 f-string 指派」的存取點未逐一解析**，
  它們在執行期才決定名字，**AST 看不到**。**「只有這 9 族」本組不宣稱。**
- ✅ **盤點表對這件事的登記是對的**：`10_db_inventory.md:1758` 與 `:2460` 兩處都已就地寫明
  「`infra/cache.py:728` **清不到它**（那條清的是不帶後綴的這一個）」「**本輪沒有盤那一族**」。

### (10) 四個檔的尺寸 ✅ 全部與宣稱相符

⚠️ **本機 `LANG` 是空的，預設 `wc -m` 確實退化成位元組數**（下表第 2、3 欄逐檔相同即為證據）。

| 檔 | `wc -l` | `wc -c` | 預設 `wc -m` | `LC_ALL=C.UTF-8 wc -m` | python `len()` | 宣稱 |
|---|---|---|---|---|---|---|
| `spec/UI_SPEC.md` | **787** | 127,357 | 127,357 | **67,220** | **67,220** | 787 行／67,220 字元 ✅ |
| `spec/DECISION_LOG.md` | **502** | 101,077 | 101,077 | **55,169** | **55,169** | 502 行／55,169 字元 ✅ |
| `docs/v2/10_db_inventory.md` | **2,824** | **307,558** | 307,558 | 190,188 | 190,188 | 2,824 行／307,558 bytes ✅ |
| `spec/UI_PAGE_TODAY.md` | **149** | 16,359 | 16,359 | 8,455 | 8,455 | 149 行 ✅ |

`UI_PAGE_TODAY.md` 的 mtime 為 `2026-09-16 10:57`，另兩檔為 `14:12`／`14:13`
—— **與「本輪未動」的宣稱一致**（這是檔案時戳，不是內容比對）。

---

## 矛盾清單（A／B 並陳，⛔ 本組不裁決）

### M-1｜「只改 prompt 則 F1 一個位元組不必動」vs 兩行寫死在 F1 的畫面文案

- **A**：`DECISION_LOG` §H 第 4 項通道欄逐字 ——「⚠️ **若只改 prompt 文字，F1 那端一個位元組都不必動**
  （它只是把模型輸出原樣印出）；**要動畫面文案／按鈕 help／那句免責，才吃到 F1。**」
- **B**：`ui/tab3_t7_ledger.py:3239`（F1）在 `st.markdown(..., unsafe_allow_html=True)` 內**印在畫面上**：
  `"AI 4 節結構：3 大缺點 / 換股建議 / 配置比例 / 高賣低買 vs 跌就買"`；
  同檔 `:3263`（F1）灰態文案：`"AI 給不出景氣位階對應的換股建議;"`。
  §H 第 4 項要改掉的 `:76-77` 正是那個「**配置比例**」節。
  **兩份文件對 `:3239`／`:3263` 皆 0 命中**（正控 `3401` 命中、負控 0）。
- **兩者不能同時為真**：若「只改 prompt」被當成一條可走的路，改完之後 F1 仍在畫面上承諾「配置比例」這一節；
  要讓畫面不說謊就得動 `:3239`，那就吃到 F1 ——
  **也就是「這件事要一張提案單還是兩張」，取決於 `:3239` 算不算本項的一部分，而規格沒有記載過它的存在。**

### M-2｜裁示 3 的兩半：盤點表已答，④-8 來源欄仍寫「查證中」

- **A**：`UI_SPEC:595` 逐字「**盤點表狀態：盤點組本輪查證中，結論回填此處** —— 本組不代判；
  在結論回填之前，本格那個『沿用』是靠**第二鍵**成立的」；`:607`「結論回填上面那一格的括號」；
  `DECISION_LOG:497`「規格該處**留位**」。
- **B**：盤點表 `10_db_inventory.md:1758` **已增列** `_t3_cur_sheet_title`，狀態 **`未落地`**、誰讀它 **`UI`**，
  並已把 §3 標題、§5.1、§5.2、§5.4 全部計數同步（264 → **265**；未落地 26 → **27**；誰讀它 231 → **232**）。
  該節 `:1765` 並寫明「**④-8 來源欄那一半由規格組做，本組只動盤點表**」。
- **兩者不能同時為真**：查證要嘛還在進行、要嘛已完成。
  **客戶裁示 3 逐字是「查出後更新盤點表**與 ④-8 來源欄**」—— 兩半只完成了一半。**

### M-3｜「同一支 builder 內另有**兩段**同型的東西」vs `:71`／`:91`

- **A**：`DECISION_LOG:264` 與 `UI_SPEC:508` 都寫「同一支 `build_mk_advisor_prompt`（`:30-93`）內
  **另有兩段同型的東西**」，並只列 **`:67-69`** 與 **`:81-89`**。
- **B**（本組實印）：
  - **`:71`** ＝ `### 🔄 二、根據景氣位階「{phase}」+ 領先指標排名 + 近期新聞 + 底層持股的換股建議`
    —— 與 `:76` **同一種形態**（印在畫面上的 `###` 章節標題，標題字面本身就是動作導向）。
    而 §H 把 `:76` 納入範圍的理由（`DECISION_LOG:449`）逐字就是「它是**畫面上真的印出來的章節標題**，
    那一行的字面本身就是一句配置推薦」。`:71` 的字面以「**換股建議**」收尾。
  - **`:91`** ＝「最後一段用「⚠️ **系統性風險警示**」開頭……給「**目前該偏向 A 還是 B**」的明確結論。」
    —— 與 `:81-89`（兩種操作策略）同一節、同一族。**`:91` 在兩份文件中 0 命中，完全沒有被登記。**
  - ⚠️ `:71` **有被登記，但在另一個地方**：`DECISION_LOG:447` 寫「`:71` 是第二節的 `### ` 標題行 ——
    **在舊條裡、不在裁定範圍裡**」。**那是 §G4 的範圍對帳，不是「範圍外只記位置」那份清單。**
- **兩者不能同時為真**：「另有兩段同型」與「`:71`／`:91` 同型但不在那兩段裡」。
  ⛔ **本組不裁決 `:71`／`:91` 該不該進 P0** —— 客戶沒有就它們裁示，**本組只指出那個列舉不完整。**

### M-4｜§C-3「永不回顯值或片段」vs ④-8 印出 sheet id 前 14 字

- **A（§C-3／⑤-2）**：`UI_SPEC:639`「規則：逐項只印「有設／沒設」與它擋住哪一塊，**永不回顯值或片段**」；
  `DECISION_LOG:90`／`UI_SPEC:644`「**印值＝把 id 攤在畫面上（違本區「永不回顯值或片段」）**」。
- **B（④-8 ＋ §1 不捏造）**：`page_04_portfolio.py:1702` `return f"{_sid[:14]}…"`，
  `:1701` 就地註解「給 ID 的前段（**事實**），不是給一個猜出來的名字」；
  `UI_SPEC:595` 把它寫進規格並以 **§1**（不編一個「我的投資組合」出來）背書。
- **這是兩條自訂規則打架**：**§1「不捏造」要求印事實（id 片段），§C-3「不回顯片段」要求什麼都別印。**
  ⛔ **本組不選邊。** 只補三個事實：
  1. §C-3 那句的字面射程寫的是「**本區**」（＝ ⑤-2），**規格沒有任何一處寫出它是不是全規格通則**；
  2. **④-8 那一格沒有就地標明它是 §C-3 的例外**；
  3. **同型寫法不只一處**：`ui/sidebar.py:286` `st.caption(f"📂 ID \`{_sid_sb[:14]}…\`")`、
     `ui/helpers/portfolio/policy_admin_section.py:311`／`:337`／`:380`、`services/fund_service.py:1109`
     —— **至少 6 處在畫面上印 sheet id 的前 14 字**。

### M-5｜`page_01_macro.py:1873` 被稱為「import 端」

- **A**：`DECISION_LOG` §H 末段 ——「同一支 prompt builder 另有一個 **import 端** `ui/views/page_01_macro.py:1873`（＝ ①-11，F2）」。
- **B**：實印 `:1873` 是 `_prompt = build_structured_summary_prompt(` ＝ **呼叫點**；
  **import 在 `:146`**（`from services.ai_prompts import build_structured_summary_prompt`）。
  同一件事在 **B25** 寫的是「`ui/views/page_01_macro.py:146`」、在 **`UI_SPEC` ②-13** 寫的是「`:146`／`:1873`」。
- **影響**：**不改變任何結論**（兩個 production import 端這件事成立），但同一個事實在同一對文件裡有三種寫法，
  其中一種把呼叫點叫成 import 端。

### M-6｜`UI_SPEC:49` 的「naive grep」數字今天重跑不出來

- **A**：`UI_SPEC:49` 逐字「實測naive grep：沿用 **33**、沿用（部分）**6**、新建 **10**、不標標籤 **11**、查不到 **2**」。
- **B**：本組今日以 `grep -oF` 對 `spec/UI_SPEC.md` 重跑 → **39／7／15／12／3**。
- **性質**：那一行是拿來論證「舊方法會超算、不要再用」的**示例數字**，**結論不受影響**
  （今天重跑仍然每一個都比真值 27／4／10／9／0 大）。
  **問題是那一行沒有量測日** —— 它是會隨文件增修而漂移的量測值。

### M-7｜§3 標題自陳「82 個不同的 session key」vs 重跑得 81

- **A**：`10_db_inventory.md:1629`「實測 **82 個**不同的 session key（AST 掃 `st.session_state[...]` / `.get` / `.setdefault` / `.pop`，
  排除 `tests/`）」；`:1760` 亦寫「完整 **82** 鍵清單」。
- **B**：本組自寫 AST 以完全相同的四種存取形態重跑，**排除 `tests/` → 81**（與盤點組自己在 `:1782` 登記的重跑結果相同）。
- **狀態**：**盤點表自己已就地登記這個差異**（`:1782-1783`「本組那支掃描回的是 81，不是 82 —— 差 1。
  **本輪不動『82』這個數字**（重建它不在裁示 3 的射程內），只據實登記」），**並非隱瞞**。
  本條只是把「那個差異今天仍然存在」記下來。

---

## 查了、沒問題的（一併列出）

1. **§H 第 4 項的七列引文** —— 七句逐字比對，**7／7 完全相同**，含全形括號、粗體標記、分號與空白。
2. **`build_mk_advisor_prompt` 鏈路的 12 個行號** —— 逐行實印，**12／12 與宣稱相符**。
3. **F1／F4 凍結格歸屬** —— 對照 `CONSTITUTION.md:72`／`:75`，**兩格判定都對**。
4. **`ai_service.py` 不解析模型輸出** —— `:341 return call_llm(prompt, …)`，純轉發。
5. **§1.5 第 1 條的兩個限定詞今天都是防呆不是篩子** —— 14／14 網格與 36／36 `safe_section` 全部頂層，**與規格逐字相同**。
6. **§1.6 的 84 ＝ 50 ＋ 34** —— 本組獨立重跑，**逐頁數字與逐頁行號清單全部相同**。
7. **`st.tabs` 全 repo 五頁 0 個** —— 與 §1.6 表格相同。
8. **④-8 四格標籤常數** —— `page_04_portfolio.py:649-653` 四個常數逐字與規格相同，`STATUS_COLS` 亦相符。
9. **`json_backup.py` 的 7 鍵** —— 逐行實印，7 個、且不含 `_t3_cur_sheet_title`。
10. **`_t3_cur_sheet_title` 的三條失效路徑** —— `:326`／`:461`／`infra/cache.py:728` 三處全部實印相符。
11. **盤點表全檔表格結構** —— §1~§4 資料列**欄數全部與各自表頭一致，0 列被跳脫管線符吃掉**。
12. **六個逐字禁用詞** —— 六檔全掃，唯一命中是已登記的 `tab3_t7_ledger.py:1990`。
13. **四張現況引文表的表頭** —— 五處全部是「現況引文・待修（原樣引用，不是本規格的建議）」。
14. **`DECISION_LOG:492` 那句「§H 與 B25 不複述那些句子」** —— 實測為真（`:448` 那處在 §G4，不在 §H／B25）。
15. **`:67-69`／`:81-89` 只記位置** —— 十一句原文在兩份文件中**一句都沒有被複述**。
16. **`UI_SPEC` 全檔表格 0 列欄數不齊**（對照 `DECISION_LOG` 的 1 列）。
17. **盤點表對「動態鍵清不到」的登記** —— `:1758`／`:2460` 兩處都已就地寫明，**且寫對了**。
18. **標籤計數灌水殘留 0** —— 今日 27／50。
19. **`UI_PAGE_TODAY.md` 149 行**、mtime 早於另兩檔三小時，與「本輪未動」一致。

---

## 本組自己犯的、就地登記（不是別人抓的）

**本組第一版的 H2~H4 掃描只得 31（宣稱 34），差 3。** 原因不是規格錯，是**本組的常數解析器只處理 `ast.Assign`、
漏了 `ast.AnnAssign`** —— `CONCLUSION_HEADING: str = "### …"`／`EVIDENCE_HEADING: str = "### …"` 三處因此看不見。
**修好後 34，逐頁行號與規格清單完全相同。**
**同一輪還犯了第二次**：誰讀它一度只數到 189（差 43），因為本組寫死「5 欄表」，
而帶「位元組讀回？」欄的 5 張表是 **6 欄**。**改以表頭索引定位後即為 232。**
→ **兩次都是「工具結構上看不見它」，不是「數錯」。再跑一百次也一樣。**
