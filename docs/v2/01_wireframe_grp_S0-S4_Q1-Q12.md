# 基金儀表板 v2 · UI 草稿（重寫版）第一～四節

> **狀態：草稿。未經客戶同意，不得依本文修改任何檔案。**
> 產出者：線框組（單組）　產出日：2026-09-15
> 基準：`origin/main` = `9cbf03776f2a0ee6bdb5a649efb93352c40baba6`（開工時 `git rev-parse` 實測）
> 工作樹 HEAD = `d0c2a8d`，**與基準不同**；本文一切引用一律走 `git show origin/main:<path>`，未讀工作樹。

---

## 〇、這份文件怎麼讀（先看這一節，否則會誤讀後面每一個數字）

### 0-1 我沒有打開過畫面 —— 這句話決定本文所有「現況」的性質

**打開這個 App 需要憑證（FRED / GEMINI / Google Sheets）＋ 對外 API，本次一律被禁。**
所以本文所有標 ⭐ 現況 的敘述，**全部是「讀渲染碼推論」出來的，不是我看到畫面**。

具體差別（這不是謙虛，是射程）：

| 我做得到 | 我做不到 |
|---|---|
| 指出某一行 `st.markdown("…")` 寫著什麼字 | 保證那一行在真實 session 裡真的被執行到 |
| 從 `if` 分支推斷「什麼情況會走到這一行」 | 保證那個情況在真實資料下會發生 |
| 數出一個檔案裡有幾個 `st.checkbox(` | 保證它們同時出現在同一個畫面上 |
| 讀出一個常數的值 | 保證畫面上顯示的就是那個值（中間可能有格式化） |

⛔ **本文沒有任何一句「我看到畫面上…」。** 有的話就是我違規，請當場退件。

### 0-2 標記體系（全文一致）

| 標記 | 意思 |
|---|---|
| ⭐ **現況** ＋ `file:line` | 我實際開檔讀過那一行。可逐行覆核。 |
| **未複驗** | 我沒有查證。**不得**被當成事實引用。 |
| **（這是我編的）** | 示意值／示意文案，**不是**從 repo 量出來的。 |
| **【已定案】** | 客戶已拍板，本文照抄，不重開。 |
| **【待決定】** | 客戶還沒決定。本文**只列題、不代答**。 |
| `[舊正式分頁]` / `[新預覽分頁]` / `[兩邊都有]` / `[都沒有]` | 雙軌軌別。 |

### 0-3 單組實測聲明（`CLAUDE.md §-2` 規則 6）

本文**由線框組單組產出，未經第二組獨立複驗**。
凡本文出現「只有 N 處」「全部是」「沒有第 N+1 個」形式的句子，一律**已就地標明**
「單組實測、未經第二組驗證、取決於有沒有漏看」。**沒有標的地方，就是我不宣稱窮舉。**

### 0-4 逐字句子 vs 我的註記 —— 兩者嚴格分離（客戶禁止事項 7）

本文每個區塊有一欄叫「**畫面上會出現的句子（逐字）**」。
**那一欄裡的東西，才是要進產品畫面的。**
所有 ⚠️ ⭐ ⛔ 標記、所有 `file:line`、所有「未複驗」，**一律不進畫面**。

---

## 一、總判

### 1-1 這份草稿是什麼

**一份「五個分頁上，每一格會出現哪一句話」的逐字清單，附上每一格現在長什麼樣的出處。**

它的用途只有一個：**讓客戶在動工前，逐句看過並拍板。**
拍板之後它才會變成 `UI_SPEC` 的來源；在那之前它一個字都不能被當成規格。

### 1-2 這份草稿不是什麼

- **不是規格書。** 它沒有欄位型別、沒有 API 契約、沒有驗收條件 —— 那是 `DATA_DICTIONARY` 與 `ACCEPTANCE` 的事。
- **不是現況稽核報告。** 它只查到「足以寫出線框」的程度就停了，**大量區塊標著「未複驗」**。
- **不是實作計畫。** 它不談先做哪一頁、不談工期、不談檔案怎麼拆。
- **不是「新舊合併方案」。** 舊分頁在本文裡只作為**對照**出現，本文一句都沒有提議改它。

### 1-3 客戶要做的事

**逐句讀第二節那一欄「畫面上會出現的句子（逐字）」，然後回答第 1-5 節列的那幾題。**

不必讀 `file:line`，那是給執行組覆核用的。

### 1-4 治理前提（【已定案】，本文照抄，不重開）

| # | 已定案 | 本文怎麼處理 |
|---|---|---|
| 1 | **基金為唯一主線；台股／ETF 已移除** —— **只移除「標的類型」**（不再處理個股與 ETF **部位**）；台股的**總經指標**照舊留在 ① 市場總覽（客戶 2026-09-15 裁示） | ① 的總經卡照留；② ③ ④ 不出現任何個股／ETF 部位欄位 |
| 2 | **v1 凍結，v2 獨立 `src/v2/fund/`** | 見下方 ⚠️ |
| 3 | **持倉 SSOT `v2_holdings`；成本法加權移動平均** | 見下方 ⚠️ |
| 4 | **合規紅線：禁建議買進／出清** | 見 1-6，這是本文最重的一項發現 |
| 5 | **9 狀態已撤銷；基金五態維持不動** | 第四節只談顏色狀態，不談基金五態 |
| 6 | **母法＋四大子法**：CONSTITUTION、DATA_DICTIONARY、UI_SPEC、ACCEPTANCE | 本文是 UI_SPEC 的**前置草稿**，不是 UI_SPEC 本身 |

⚠️ **第 2 項的實測事實**：本 repo **目前沒有 `src/` 目錄**
（⭐ 實測 `git ls-tree origin/main --name-only` 的頂層清單裡無 `src`；頂層是
`app.py` / `infra` / `repositories` / `services` / `shared` / `ui` / `scripts` / `models` / `config` / `mcp_server` 等）。
→ **`src/v2/fund/` 是新種一棵樹，不是搬既有的。** 本文不對「怎麼種」提任何方案（那是架構組的題目）。

⚠️ **第 3 項的實測事實 —— 目標 schema 尚不存在**：
- ⭐ `v2_holdings`、`v2_transactions` **全 repo 0 命中**（`git grep` 兩個字串，無輸出）。
  正對照：同一把 grep 查 `ALL_COLS_V2` → **14 檔命中**，指令是活的。
- ⭐ **現行持倉 SSOT 是 `repositories/policy/v2.py:506-517` 的 `ALL_COLS_V2`，10 欄**：
  `policy_id` / `fund_code` / `fund_name` / `currency` / `tier` / `invest_twd` /
  `div_cash_pct` / `units` / `avg_nav` / `avg_fx`
- ⭐ 對應中文表頭在同檔 `:527-538` 的 `ZH_HEADERS_V2`：
  保單編號／基金代號／基金名稱／幣別／級別／**淨投資金額**／現金給付%／持有單位數／平均買入單位成本／平均買入匯率
- ⭐ 同檔 `:501-505` 註解自陳 v19.436 由 **13 欄精簡為 10 欄**，退役的是 `item_type` / `avg_nav_with_div` / `amount`。

→ **本文一律據實寫「現況是這 10 欄」，不假裝 `v2_holdings` 已經有了。**
→ ⚠️ 「成本法加權移動平均」與現行 `avg_nav` / `avg_fx` 兩欄的關係 **未複驗** ——
   我沒有去讀那兩欄現在是怎麼被寫入與更新的。

### 1-5 【待決定】—— 客戶還沒決定的題目

**本文只列題，不代答。每一題都附「為什麼需要客戶決定」。**

| # | 題目 | 為什麼非客戶決定不可 |
|---|---|---|
| **Q1** | **① 的表單欄位要哪一組？** 兩份線框給了兩套：一套是「觀察區間 ＋ 資料源」，另一套是「四個資料類別勾選框 ＋ 強制重抓」。 | ⭐ **這題是程式碼自己掛牌待決的**：`ui/views/page_01_macro.py:2088-2090` 的 caption 逐字寫著「⬜ 表單欄位待客戶裁決：兩份線框一份是『觀察區間 ＋ 資料源』，另一份是『四個資料類別勾選框 ＋ 強制重抓』。本批只做骨架與送出鈕。」 —— **這行字現在就印在畫面上**。 |
| **Q2** | **禁建議買進／出清這條紅線，射程到哪？** 只禁「買進／出清」這兩個動作詞，還是連「加碼／減碼／停利／逢低／滿倉／拉高現金水位」一起禁？ | 見 1-6：現況畫面上出現的是**後面那一整組詞**。只禁前兩個等於沒禁。 |
| **Q3** | **「📐 建議資產水位」這張卡要留、要改名、還是要拿掉？** | ⭐ 卡名逐字含「建議」（`page_01_macro.py:779`／`:809`），且它輸出的是股／債／現金三個百分比。它是否落在紅線內，取決於 Q2。 |
| **Q4** | **舊分頁什麼時候退場？**（⚠️ **順序與第一前置已經拍板了，這一題只問時間點**） | ⭐ `page_04_portfolio.py:10-12` 逐字引客戶 2026-09-08 原話：「…退場順序完全同意採「**⑦ → ⑥ → ⑧ → ⑨**」，並**以「⑨ 建立寫入入口」為第一前置**。」<br>→ **順序：已定案。第一前置：已定案，且 ⭐ `:6` 自陳該前置已於 2026-09-08 落地（「本頁第一次能寫東西」）。**<br>→ **仍未定的只有時間點**，而 ⭐ `:2761` 登記著「走到底時，那句指路會指向不存在的地方」—— 也就是**每拔一格都會有既有指路失效**（另見 `:1219`、`:2324` 兩處同樣的登記）。 |
| **Q5** | **「第四層最多一個展開 ＋ 一個勾選方框」這條，是硬規則還是目標值？** | 見第三節：現況 ⑤ 有 **6 個勾選方框 ＋ 3 個展開**。若是硬規則，⑤ 要重新設計；若是目標值，本文要改寫成目標。 |
| **Q6** | **按鈕字「只准試算／搜尋／套用」，是否含下載鈕與導覽鈕？** | 見第三節表：現況有 5 個按鈕字不在這三個之內，其中 1 個是 CSV 下載鈕、2 個是「查這一檔」這種導覽性質的。 |
| **Q7** | **「四色」指的是哪一個四？** | 見第四節 —— repo 裡同時有**三個不同的「四」**，它們**不是同一個四**。這題不決定，第四節寫不下去。 |
| **Q8** | **TER 卡要修數字、改卡名，還是整張拿掉？** | 見 1-6 第 (3) 點。這是一張**畫面在說謊**的卡，三種修法後果完全不同。 |

### 1-6 ⛔ 我在查證過程中撞到的三件事，要先講（它們會改變前面每一題的答案）

#### (1) ⛔ 合規紅線**現在就在被跨過**，而且是在**正式分頁**上，不是預覽分頁

這是本文最重的一項發現。鏈路我逐段實測過：

- ⭐ `app.py:619` `with tab_macro:` → `:636` `render_market_overview()`
- ⭐ 該函式住在 `ui/views/page_01_macro.py:2060`
- ⭐ `page_01_macro.py:677` `_light = macro_action_light(ind, phase.get("score"))`
- ⭐ `page_01_macro.py:698` `business_alert(f"{_light['light']} {_light.get('action','')}", …)`
- ⭐ `page_01_macro.py:702` `st.markdown(f"**{_light.get('light','')} {_light.get('action','')}**")`
- ⭐ 那個 `action` 的值來自 `services/macro/action_light.py:185-188`，三選一：
  - **「可加碼 —— 核心持有不動 + 衛星積極佈局，定期收息再投」**
  - **「持有 —— 分批進場、避免重押單一題材」**
  - **「減碼 —— 景氣位階偏弱,拉高現金水位」**

同一頁的第二條鏈路：

- ⭐ `page_01_macro.py:736` `_icon, _level, _color, _action = composite_verdict(_score)`
- ⭐ `:745-750` 把 `composite_action=_action` 傳進 `build_evidence_rows()` 與 `build_evidence_footnotes()`
- ⭐ 那個 `_action` 來自 `services/macro/composite_score.py:305/308/311/314/316`，五選一，含：
  - **「多頭市場強勁：可滿倉持有，衛星部位積極佈局成長題材」**
  - **「風險正在集結：拉高現金水位至 15-25%，衛星部位設停利」**
  - **「避險情緒高漲：現金 30%+，核心轉防守型（投資等級債/全球均衡）」**

⭐ **而且這五句在 repo 裡有兩份逐字相同的副本**：
`services/macro/composite_score.py:305/308/311/314/316` 與
`services/macro/explain.py:247/250/253/256/258` —— 我逐句對讀過，**一字不差**。

⚠️ **現況有一道部分閘門，但它擋的不是合規**：
⭐ `page_01_macro.py:739-741`，當 `is_sufficient(...)` 為假時 `_action` 被清成 `""`。
→ **資料不足時那幾句話不印；資料足夠時照印。** 那是**品質**閘門，不是**合規**閘門。

→ **這一項直接決定 Q2／Q3，而且它不在「新分頁要改的內容」範圍內 —— 它在正式 ① 上。**

#### (2) ⛔ 雙軌的地圖跟我拿到的說法不一樣 —— **① 沒有預覽雙胞胎，它本身就是新的**

我拿到的說法是「新頁在 `ui/views/page_0X_*.py`，舊頁在 `ui/tab*.py`」。
**⭐ 實測：對 ②③④⑤ 成立，對 ① 不成立。**

⭐ `app.py:583-588` 的 `st.tabs(...)` 拆包出 **9 個** 變數，逐格對應如下（全部逐行讀過）：

| 格 | 變數 | 標籤（SSOT） | `app.py` 呼叫 | 實作模組 | 軌別 |
|---|---|---|---|---|---|
| ① | `tab_macro` | 🌐 市場總覽 | `:636` `render_market_overview()` | **`ui/views/page_01_macro.py`** | ⛔ **正式格，掛的是新 View** |
| ② | `tab_health` | 💊 持倉體檢 | `:648` `render_fund_grp_health_tab()` | `ui/tab_fund_grp_health.py` | 正式格，舊模組 |
| ③ | `tab_research` | 🔍 標的探索 | `:662` `render_fund_research_tab()` | `ui/tab_fund_research.py` | 正式格，舊模組 |
| ④ | `tab_portfolio` | 📊 資產配置 | `:673` `render_portfolio_tab()` | `ui/tab3_portfolio.py` | 正式格，舊模組 |
| ⑤ | `tab_settings` | ⚙️ 設定與診斷 | `:688` `render_settings_diag_tab()` | `ui/tab_settings_diag.py` | 正式格，舊模組 |
| ⑥ | `tab_preview_health` | [新] 💊 持倉體檢 | `:720` `render_holdings_health()` | `ui/views/page_02_health.py` | 預覽格 |
| ⑦ | `tab_preview_settings` | [新] ⚙️ 設定與診斷 | `:728` `render_settings_and_diagnostics()` | `ui/views/page_05_settings.py` | 預覽格 |
| ⑧ | `tab_preview_research` | [新] 🔍 標的探索 | `:752` `render_fund_research()` | `ui/views/page_03_research.py` | 預覽格 |
| ⑨ | `tab_preview_portfolio` | [新] 📊 資產配置 | `:776` `render_asset_allocation()` | `ui/views/page_04_portfolio.py` | 預覽格 |

⭐ 標籤 SSOT：`ui/helpers/story_nav.py:88-94` 的 `_TAB_LABELS`（5 個）
＋ `:131-136` 的 `PREVIEW_TAB_LABELS`（4 個：health / settings / research / portfolio）。
⭐ `PREVIEW_PREFIX: str = "[新] "` 在同檔 `:119`。

⛔ **`PREVIEW_TAB_LABELS` 沒有 `macro` 這個 key。①「市場總覽」沒有預覽雙胞胎。**

**這件事的後果，直接打在客戶禁止事項 9 上：**
> 「不准把『舊分頁不改內容』與『新分頁要改內容』混為一談。」

**① 兩邊都是。** 改 `page_01_macro.py` **就是改正式分頁**，沒有預覽期可以緩衝。
→ 本文第二節把 ① 的軌別一律標 `[兩邊都有]`，並在每一區塊就地重述這句話。

⭐ 旁證（不是我推論的，是 repo 自己寫的）：`ui/views/page_02_health.py:1802-1812`
記載同一個誤會在 2026-09-07 發作過一次 —— 當時該檔自陳「`app.py` 的 `with tab_health:` 呼叫本函式」，
被獨立稽核實測推翻，改成「呼叫本函式的是 `with tab_preview_health:`」。
**我這次撞到的是同一張地圖的另一格。**

#### (3) ⛔ 一張**畫面在說謊**的卡：標題寫 TER，數字餵的是「最高經理費」

逐段實測：

- ⭐ `ui/helpers/fund/checkup.py:280` 卡片標題逐字：`💰 TER 費用率{_ter_lbl}`
- ⭐ 同檔 `:194` 註解：「5) TER 費用率 — 只取本檔真實 mgmt_fee + category」
- ⭐ 同檔 `:195` `_ter_val = _safe_num(mj.get("mgmt_fee"))`
- ⭐ `repositories/fund/fund_orchestration.py:1082`
  `result["mgmt_fee"] = rows_map.get("最高經理費(%)", "")`
  → **抓的是「最高經理費」，那是一個收費上限、而且只是 TER 的其中一個成分**
- ⭐ **真正的 TER 同一張表裡就有抓，只是這張卡沒用它**：同檔 `:1088-1091`
  `result["total_expense_ratio"] = (rows_map.get("總費用率(%)") or rows_map.get("總費用率")
   or rows_map.get("總開支比率(%)") or rows_map.get("經常性費用(%)") or "")`
- ⭐ 同檔 `:1083-1086` 另有 `custody_fee`（保管費），註解自陳是「TER 估計第 2 主成分」

⭐ **最誠實的一行反而是它的失敗文案**：同檔 `:297`
`st.caption(f"💰 TER 費用率 — ⬜ {_code} 缺 mgmt_fee 資料")`
—— **卡名說 TER，缺料訊息說缺 mgmt_fee。同一張卡自己把矛盾印在畫面上。**

⭐ 同型卡在另一處也有：`ui/helpers/fund_grp_health/investment.py:146` 「💰 TER 費用率」、
`:166`「💰 TER 費用率 — ⬜ 缺 mgmt_fee 資料」。

⚠️ **這兩處都在 `ui/helpers/**`，不在 `ui/views/**`** —— 也就是說它們現在掛在**哪幾格**，
**未複驗**（我沒有回頭追 `checkup.py` / `investment.py` 的呼叫鏈）。
**不得**據本段主張「新 ② 上有這張卡」或「新 ② 上沒有」。

### 1-7 我在總管給的材料裡查到的五處不符（M1~M5）

**逐條列出，含我實際跑的東西。**

| # | 材料原文 | 實測 | 性質 |
|---|---|---|---|
| **M1** | 「新頁在 `ui/views/page_0X_*.py`，舊頁在 `ui/tab*.py`」 | **對 ②③④⑤ 成立，對 ① 不成立** —— ① 正式格掛的就是 `ui/views/page_01_macro.py`（`app.py:619`→`:636`），且 `PREVIEW_TAB_LABELS`（`story_nav.py:131-136`）**沒有 macro key** | **射程錯**，見 1-6(2)。影響禁止事項 9 |
| **M2** | 「四層閱讀順序：**結論 → 三欄卡 → 依據 → 詳細**」 | 順序位置對，**數量與完整度不對**。⭐ 正典寫在 `ui/views/page_02_health.py:57`：「`### 🧾 ① 結論` → **卡片** → `### 🧾 ② 依據` → 詳細區」。① 實際是 **結論 → 6 張卡 → 依據 → 3 張卡 → 詳細**（多一組卡格）；**③ 與 ④ 兩個 heading 一個都沒有**（`grep -c` 皆 0） | **不完整**，見第三節 |
| **M3** | 「『🟢 逢低買進』在 `ui/helpers/macro/helpers.py:99,104` ＋ `ui/helpers/fund_grp_health/signals.py:144`」 | **`helpers.py:104` 對；另兩處錯。** ⭐ `helpers.py:99` 是 `asset_class = "核心資產 🛡️" if is_core …`，不含該字串；⭐ `signals.py:144` 是 `"買 3 (深跌)": …`，全檔無 `逢低`（`git grep 逢低 -- '*.py'` 未命中該檔）。<br>⚠️ **但材料低估了問題**：`helpers.py:100-105` 是一整個 `RECS` dict，**8 組買賣指示**，含 **「🔴 賣出獲利」「🟢 積極買進」「🟡 持有設停利」「🟡 持有減碼」**；而 `signals.py:144-148` 給的是 **「買 3 (深跌)／買 1 (小跌)／現價／賣 1 (小漲)」買賣價格階梯** ＋ `:143`「操作訊號」欄 —— **價格階梯比動詞更難辯護** | **行號錯、方向對、嚴重度被低估** |
| **M4** | 「『同類排名約 7 成有值』是假的；唯一出處 `checkup.py:613` 是一句 tooltip 文案」 | **結論對，敘述要修一格。** ⭐ `git grep "約 7 成\|約七成\|7 成\|七成"` → **0 命中**，「7 成」這個數字**全 repo 不存在**。⭐ `checkup.py:612-613` 實際寫的是「**約 3 成**基金 MoneyDJ **無**同類排名資料 → 顯示 —」。<br>⭐ 材料說「另外 5 處講的是同類平均」—— **這一半完全正確**：`SPEC.md:712` / `checkup.py:7` / `:407` / `:474` / `tab6_manual.py:588` 五處皆為「同類**平均**」。<br>⚠️ **因此更強的說法是**：同一個沒有出處的「約 3 成」被**套在兩個不同欄位上**（同類排名、同類平均），而全 repo 找不到任何量測 | **結論成立，敘述可再精確** |

| **M5** | 「舊草稿第五節表共 27 列，其中含『請先到：』恰好 **22** 條（①4 ②4 ③12 ④5 ⑤2）」 | **我沒有複驗那 22**（那是**舊草稿**的統計，不是 repo 的）。⭐ **repo 端的實測是另一組數字**：`git grep -c 請先到 -- '*.py'` → production **13 行 / 9 檔**、tests **33 行 / 7 檔**。<br>⛔ **兩個數字不能互相比對** —— 見 4-6：`render_state.py:130` 才是「（請先到：」的**產生器**，畫面上出現幾次取決於 `not_ready(where=...)` 被呼叫幾次，**遠大於 13**。 | **不是材料的錯，是兩個不同的東西被放在一起比** |

⚠️ **M1~M5 皆為本組單組實測，未經第二組驗證。**
⚠️ **材料裡我逐條覆核過、確認為真的**（不只報錯，也報對）：
- ⭐ 「主 CTA『更新淨值與配息』repo 0 命中」—— **確認**。我另外放寬查了「更新淨值」（去掉「與配息」）→ **同樣 0 命中**。正對照：「淨值」在 `fund_fetcher.py` 單檔就 8 行命中，指令是活的。
- ⭐ `POLICY_TABLE_COLUMNS` 7 欄在 `page_04_portfolio.py:687-688` —— **逐字確認**。
- ⭐ 配息月曆四處（`:2612` checkbox、`:2615` help「不勾就完全不算」、`:2659`「本月有 N 檔推不出除息日（是推不出，不是沒配息）」、`:2661`「本月推估除息 N 檔」、`:802` gate 字面）—— **五處全中**。
- ⭐ `ZSCORE_STOP_GAIN_DEFAULT=1.75` / `ZSCORE_ADD_DEFAULT=-1.0` 在 `shared/signal_thresholds.py:409-410` —— **逐字確認**。
- ⭐ `page_01_macro.py:785` 第二引數傳 `None` ⇒ 門檻固定 —— **確認**，並補上機制：`services/allocation_ladder.py:45-48`，`ndc_light(None)` 回 `(None, None)` → 走 `ZSCORE_*_DEFAULT`、`source='default'`。
- ⭐ `ER` ＝ Kaufman 效率比（`switch_advisor_section.py:705` 逐字「效率比(Efficiency Ratio):這段期間的淨漲跌 ÷ 一路上上下下的總移動距離」）—— **確認**。
- ⭐ `contribution_by_fund` 是**報酬**貢獻（`services/portfolio_performance.py:193-194` docstring 逐字「各檔對組合報酬的貢獻 = 權重 × 該檔於共同期間報酬(百分點)」）—— **確認**。
- ⭐ `services/peer_rank.py:1` 是同類**基金**排名 —— **確認**。
- ⭐ TER 卡 `checkup.py:195-196` ＋ `fund_orchestration.py:1082`、真 TER 在 `:1088` —— **確認**，見 1-6(3)。


---

## 二、五真線框

### 讀法（每一頁都一樣）

每個區塊四欄：**線框** ／ **畫面上會出現的句子（逐字）** ／ **⭐ 現況 ＋ `file:line`** ／ **軌別**。

⚠️ **「逐字」的兩種來源，本文嚴格分開標**：
- **【沿用】** ＝ 這句話**現在就在 repo 裡**，我從 `file:line` 抄下來的。
- **【新寫】（這是我編的）** ＝ repo 裡沒有，我為了補線框的洞寫的。**客戶不拍板就不算數。**

⚠️ **軌別標記的含意**（客戶禁止事項 9）：
| 標記 | 意思 |
|---|---|
| `[舊正式分頁]` | 只存在於 `ui/tab*.py`，掛在正式格。**凍結，不改內容。** |
| `[新預覽分頁]` | 只存在於 `ui/views/page_0X_*.py`，掛在 ⑥⑦⑧⑨ 預覽格。**這才是要改的。** |
| `[兩邊都有]` | 新舊各有一份，**同時渲染在畫面上**。 |
| `[都沒有]` | 兩邊都沒有。**要新建。** |

⛔ **① 全頁是特例，見下方 ①-0。**

---

## ① 市場總覽

### ①-0 ⛔ 先讀這一段：① 的軌別是全頁層級的例外

⭐ `app.py:619` `with tab_macro:` → `:636` `render_market_overview()` → `ui/views/page_01_macro.py:2060`。
⭐ `ui/helpers/story_nav.py:131-136` 的 `PREVIEW_TAB_LABELS` 只有 health / settings / research / portfolio。

> **① 的「新頁」就掛在正式格上，沒有預覽雙胞胎。**
> **改 `page_01_macro.py` ＝ 改正式 ①，使用者下一次開站就看得到。**

⚠️ 舊的 `ui/tab1_macro.py` **檔案還在**，但 ⭐ 我用 `git grep tab1_macro -- '*.py'` 逐行判讀後，
**production 端沒有任何一處 import 它**（命中全是註解、`services/` 的字串名、`tests/`）。
正對照：同一把 grep 查 `tab3_portfolio` → `app.py:157` 有真 import。
→ ⚠️ **「`tab1_macro.py` 已無 production caller」是單組實測、未經第二組驗證、取決於有沒有漏看**
（別名匯入 `from x import y as _z` 這類寫法我掃不到）。
⚠️ 但 ⭐ `page_01_macro.py:1117` **仍 lazy import `ui.tab1_macro_midcycle`**（**另一個檔**，不是 `tab1_macro.py`）。

**本頁以下所有區塊，軌別一律 `[兩邊都有]`**，含意是：**它同時是舊的正式格、也是新的 View。**

### ①-1 頁首

```
┌──────────────────────────────────────────────────────────────┐
│  ## 🌐 市場總覽                                                │
│  〔動線列：① 市場總覽 ▸ ② 持倉體檢 ▸ ③ 標的探索 ▸ ④ 資產配置 ▸ ⑤ 設定與診斷〕│
│  回答一個問題：現在市場環境該進攻還是防守？                        │
│  這裡只有大盤與總經，不出現任何一檔你持有的基金。                    │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】`🌐 市場總覽`
- 【沿用】`回答一個問題：**現在市場環境該進攻還是防守？** 這裡只有大盤與總經，不出現任何一檔你持有的基金。`

⭐ **現況**：
- 大標 `ui/views/page_01_macro.py:2064` `st.markdown(f"## {tab_label('macro')}")`；字串 SSOT `ui/helpers/story_nav.py:89` `"macro": "🌐 市場總覽"`
- 動線列 `page_01_macro.py:2065` `render_story_nav("macro")`
- 副標 `page_01_macro.py:2067-2068`（逐字如上）
- ⭐ 副標與客戶拍板的 IA 線框**逐字相符**：`docs/wireframes/ia-wireframe.html:344`（去 HTML 標籤後）
  「回答一個問題： 現在市場環境該進攻還是防守？ 這裡只有大盤與總經，不出現任何一檔我持有的基金。」
  （唯一差別是「**我**持有」vs 程式的「**你**持有」，人稱不同）

**軌別**：`[兩邊都有]`（見 ①-0）

---

### ①-2 載入閘門（表單）

```
┌──────────────────────────────────────────────────────────────┐
│  資料源　FRED ＋ FinMind。                                     │
│  ⬜ 表單欄位待客戶裁決：…（見 Q1）                               │
│                                       [ 📡 載入總經資料 ]      │
│                                    （載過一次之後 → 🔄 更新總經資料）│
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】`資料源　FRED ＋ FinMind。⬜ 表單欄位待客戶裁決：兩份線框一份是「觀察區間 ＋ 資料源」，另一份是「四個資料類別勾選框 ＋ 強制重抓」。本批只做骨架與送出鈕。`
- 【沿用】按鈕字：第一次 `📡 載入總經資料`／之後 `🔄 更新總經資料`

⭐ **現況**：
- 表單 `page_01_macro.py:2082-2085` `applied_form(_FORM_KEY, submit_label=_submit_label)`；caption `:2086-2088`
- 按鈕字 `page_01_macro.py:2074` `_submit_label = MACRO_LOAD_BTN_AGAIN if _loaded else MACRO_LOAD_BTN_FIRST`
- 兩個常數 `shared/ui_control_labels.py:30-31`：`MACRO_LOAD_BTN_FIRST = "📡 載入總經資料"`、`MACRO_LOAD_BTN_AGAIN = "🔄 更新總經資料"`
- ⛔ **這段 caption 就是 Q1 的掛牌**。客戶決定之前，畫面上會一直印著「待客戶裁決」這幾個字。
- ⛔ **按鈕字不在「試算／搜尋／套用」之內** —— 見第三節與 Q6。

**軌別**：`[兩邊都有]`

**未設定金鑰時**
- 【沿用】`尚未設定 FRED 金鑰，無法載入總經資料`
- 【沿用】去哪補：`Streamlit Cloud → Settings → Secrets 的 \`FRED_API_KEY\``
- ⭐ `page_01_macro.py:2092-2093` `not_ready(..., where=...)`
- ⛔ ⭐ 這是**全頁唯一一個寫死字面值的「去哪補」**（`page_01_macro.py` 其餘 `where=` 全走 `_where_to_load()` / `where_to_find()` 執行期組值）。這一點與 `CLAUDE.md §8.3.P` 的 `P-WHERECONTENT-1` 登記相符，**該列本身我未複驗**。

---

### ①-3 層 1｜🧾 ① 結論（全寬）

```
┌──────────────────────────────────────────────────────────────┐
│  ### 🧾 ① 結論 — 現在該加碼還是防禦                             │
│  **🟢 〔一句總結〕**                                            │
│  ・〔理由 1〕  ・〔理由 2〕  ・〔理由 3〕                          │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】區頭：`### 🧾 ① 結論 — 現在該加碼還是防禦`
- 【沿用】資料撐不住時：`這一輪的資料撐不起一個結論：{原因}`
- ⛔ **總結那一行的現行內容，是 Q2／Q3 的直接標的**：
  【沿用】`🟢 可加碼 —— 核心持有不動 + 衛星積極佈局，定期收息再投`
  【沿用】`🟡 持有 —— 分批進場、避免重押單一題材`
  【沿用】`🔴 減碼 —— 景氣位階偏弱,拉高現金水位`

⭐ **現況**：
- 區頭 `page_01_macro.py:676`（函式內）與 `:2123`（未載入骨架）**兩處逐字相同**
- 撐不住分支 `page_01_macro.py:681-684` `not_ready(...)` ＋ `where=_where_to_load()`
- 🔴 分支 `page_01_macro.py:693-694` `business_alert(...)`；其餘 `page_01_macro.py:696` `st.markdown(f"**{light} {action}**")`
- 理由逐條 `page_01_macro.py:697-700` `for _r in _reasons: st.caption(f"・{_r}")`
- 三句 `action` 字面值 `services/macro/action_light.py:185-188`；燈號選擇 `:184` `action_light_band(phase_score_10)`

⚠️ **這三句是【待決定】，本文不代客戶改寫。**
⚠️ 我**沒有**提出替代文案 —— 那要等 Q2 決定射程之後才寫得出來，
現在寫等於**用一個未定的前提去生規格**。

**軌別**：`[兩邊都有]` ⛔ **正式格上就是這個樣子。**

---

### ①-4 六張卡（3 欄自適應網格，2 列）

```
┌───────────────┬───────────────┬───────────────┐
│ 景氣位階       │ 波動與信用     │ 通膨與利率     │
├───────────────┼───────────────┼───────────────┤
│ 熱錢動向       │ 極端風險警語   │ 新聞情緒       │
└───────────────┴───────────────┴───────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】六個卡名：`景氣位階`／`波動與信用`／`通膨與利率`／`熱錢動向`／`極端風險警語`／`新聞情緒`
- 【沿用】未載入時每張卡：`尚未載入總經資料。` ＋ 去哪補

⭐ **現況**：
- 六張卡的 builder `page_01_macro.py:2157-2164` `render_cards([_card_phase(...), _card_vol_credit(...), _card_infl_rate(...), _card_hot_money(), _card_risk_radar(), _card_news()])`
- 未載入骨架的六個卡名 `page_01_macro.py:2128-2129`（tuple 逐字如上）；灰卡 dict `:2125-2130`
- 3 欄網格 `ui/helpers/ia/cards.py:125-129` → `ui/helpers/ia/layout.py::card_grid(len(cards), cols or GRID_COLS)`
- ⭐ `cards.py:120-121` docstring：「空清單**不畫任何東西**（鐵則 04：不留冗餘占位）」

⚠️ **每張卡的 `value` 與 `note` 逐字內容 —— 未複驗。** 我只讀到 builder 的名字，沒有逐一展開六個 `_card_*` 函式。

**軌別**：`[兩邊都有]`

---

### ①-5 層 2｜🧾 ② 依據（全寬表）

```
┌──────────────────────────────────────────────────────────────┐
│  ### 🧾 ② 依據 — 憑什麼這樣說                                   │
│  〔五桶證據表〕                                                 │
│  〔表下註記〕＋〔可摺疊註記〕                                     │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】區頭：`### 🧾 ② 依據 — 憑什麼這樣說`
- 【沿用】撐不住時的表下註記：`⬜ 綜合健康度這一列只印分數、不給等級與行動：{原因}。`

⭐ **現況**：
- `page_01_macro.py:733`（函式內）與 `:2131`（未載入骨架）兩處逐字相同
- 表格由 `ui/helpers/macro/beginner_view.py` 的既有實作畫（`compute_five_bucket_summary` / `build_evidence_rows` / `render_evidence_table`）；⭐ `page_01_macro.py:709-711` docstring 自陳「**不重寫一份表**」
- ⭐ `page_01_macro.py:712` docstring 逐字：欄位「以該檔的 `EVIDENCE_COLUMNS`（5 欄）為準，**不照線框那張 6 欄示意表**」（模組 docstring `:117` 另有同義一句）
  → ⚠️ **線框與實作在欄數上本來就不一致（6 vs 5），這是已知且有紀錄的**；五欄的**欄名** —— **未複驗**。
- ⭐ `page_01_macro.py:714` 起：`news_items` 一律傳 `None`（實際呼叫 `:742`），新聞桶標 ⬜「未掃描」，**不是綠燈**
- ⛔ `page_01_macro.py:736` 取得 `_action`、`:748` 傳進 `build_evidence_rows`、`:751` 傳進 `build_evidence_footnotes` → **五句投資指示會出現在這張表裡**（見 1-6(1)）

**軌別**：`[兩邊都有]`

---

### ①-6 層 3｜三欄卡

```
┌───────────────┬───────────────┬───────────────┐
│ 📐 建議資產水位 │ ⚡ ③ 例外      │ 🔍 ④ 可信度    │
└───────────────┴───────────────┴───────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】卡名：`📐 建議資產水位`／`⚡ ③ 例外`／`🔍 ④ 可信度`
  ⚠️ **「建議」二字是現有元件名的一部分，本文照原名引述**（客戶禁止事項 6 的例外）。**它是不是要改，是 Q3。**
- 【沿用】水位值：`股票 {n}％ ・債券 {n}％ ・現金 {n}％`
- 【沿用】水位說明：`錢該放在哪一類資產的建議（不是「核心／衛星」那種角色分配）。`
- 【沿用】門檻句：`停利 Z ≥ {+n.nn}、加碼 Z ≤ {+n.nn}（Z 是「一檔基金現在的淨值，離它自己近三年的平均有多遠」，0 就是剛好在平均、正的偏貴、負的偏便宜）`
- 【沿用】門檻來源句：`這兩個門檻是固定的預設值，不會跟著台灣景氣燈號變動。`
- 【沿用】撐不住時：`這一輪取到的總經指標還撐不起一個總分，先不給配置建議。`

⭐ **現況**：
- 三張卡 `page_01_macro.py:2185-2189` `render_cards([_card_allocation(_ev), _card_exceptions(_ev), _card_credibility(_ind,_ev)])`
- 卡名 `page_01_macro.py:780`／`:788`（兩條撐不住路徑）／`:808`（正常路徑）／`:2136`（未載入骨架）
- 水位值 `page_01_macro.py:809-810`；說明 `:811-812`；撐不住文案 `:782`
- 門檻句 `page_01_macro.py:801-803`；來源句 `page_01_macro.py:804-806`
- ⛔ ⭐ **門檻永遠是固定值**：`page_01_macro.py:785` `allocation_from_composite(ev.get("score"), None)` —— 第二引數（`ndc_score`）寫死 `None`
  → `services/allocation_ladder.py:45-48`：`ndc_light(None)` 回 `(None, None)` → 回 `ZSCORE_STOP_GAIN_DEFAULT` / `ZSCORE_ADD_DEFAULT`、`light=None`、`source='default'`
  → 因為 `light` 是 `None`，`page_01_macro.py:804-806` 的三元式恆走 `else` 分支 → **畫面上恆印「這兩個門檻是固定的預設值」那一句**
- 兩個常數 `shared/signal_thresholds.py:409-410`：`ZSCORE_STOP_GAIN_DEFAULT = 1.75`、`ZSCORE_ADD_DEFAULT = -1.0`
  → **所以畫面上那兩個數字恆為 `+1.75` 與 `-1.00`**（格式化字串 `{:+.2f}`）
- ⚠️ `shared/signal_thresholds.py:406-407` 另有 `ZSCORE_STOP_GAIN_BY_LIGHT` / `ZSCORE_ADD_BY_LIGHT` 兩張隨景氣燈變動的表 —— **目前這條路徑走不到它們**。
- ⚠️ ⚡ ③ 例外（`page_01_macro.py:817`）與 🔍 ④ 可信度（`:900`）兩張卡的逐字內容 —— **未複驗**。

**軌別**：`[兩邊都有]`

---

### ①-7 層 4｜🔎 詳細資料與說明

```
┌──────────────────────────────────────────────────────────────┐
│  ─────────────────────────（分隔線）──────────────────────────  │
│  ### 🔎 詳細資料與說明                                          │
│    🌳 長期座標　　│  內含 ▸ 展開：📋 原始讀數與來源                │
│    📈 中期循環                                                 │
│    🎯 短線雷達                                                 │
│    ⚠️ 拐點警報                                                 │
│    🤖 AI 景氣判斷總結　│ 內含表單 [ ▶️ 生成 AI 總結 ]             │
│  📋 逐檔的加減碼建議請到 ② 💊 持倉體檢。…                        │
│  ⬜ 總經燈號全表（值／位階／資料日期／來源）—— 已拍板不做            │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】區頭：`### 🔎 詳細資料與說明`
- 【沿用】五個小標：`🌳 長期座標`／`📈 中期循環`／`🎯 短線雷達`／`⚠️ 拐點警報`／`🤖 AI 景氣判斷總結`
- 【沿用】展開器：`📋 原始讀數與來源`
- 【沿用】AI 按鈕：`▶️ 生成 AI 總結`（生過一次後 `🔄 重新生成 AI 總結`）
- 【沿用】AI 說明：`把這一頁已經取到的讀數（{n} 行快照）交給 AI，逐段用白話講「現在是好是壞、下一步怎麼做」。　⚠️ AI 只會看到上面那些數字，缺的項目是「—」。`
- 【沿用】指路：`📋 **逐檔的加減碼建議請到 {② 分頁名}。**　這一頁只講市場（大盤與總經），不出現任何一檔你持有的基金；「我手上這幾檔該加該減」是 {② 分頁名} 的題目。`
- 【沿用】已拍板不做：標題 `總經燈號全表（值／位階／資料日期／來源）—— 已拍板不做`；
  缺什麼 `「來源」欄目前**只有 1 項指標**帶得回來源標記，其餘全部會是「—」；而「位階」欄兩份線框都沒有定義過它的意思`；
  footer `這不是壞掉：資料層有記來源，是計算層沒有把它一起帶下來；補齊要動到底層，不在本頁的範圍內。本頁的主要大表是上面那張「② 依據」五桶證據表。`

⭐ **現況**：
- 區頭常數 `page_01_macro.py:1036` `_DETAIL_HEADING = "🔎 詳細資料與說明"`；渲染 `:1969`（其上 `:1968` `st.divider()`）
- 五個小標 `page_01_macro.py:1028-1034` `_DETAIL_TITLES`
- 順序來源 `page_01_macro.py:1024-1026` `_DETAIL_HORIZON_KEYS`（從 `shared.macro_buckets.BUCKET_ORDER` 濾掉 `news` 推導）＋ `:1947` 末尾 append `_DETAIL_AI_KEY`
- 逐塊隔離 `page_01_macro.py:1970`（`for … in _DETAIL_ZONE`）＋ `:1986` `safe_section(_title, _render)`；`:1987` 接 `_render_matrix_signpost()`
- 展開器 `page_01_macro.py:1292` `with st.expander("📋 原始讀數與來源", expanded=False)` —— ⭐ **全頁唯一的 `st.expander`**（AST 實測，`page_01_macro.py` 的 expander 呼叫數 ＝ 1）
- AI 表單 `page_01_macro.py:1866` `applied_form(_AI_FORM_KEY, submit_label=_ai_label)`；標籤常數 `:248-249`
- AI 說明 `page_01_macro.py:1868-1871`
- 指路 `page_01_macro.py:2030-2034` `st.caption(...)`；⭐ `:2022` 就地註明**時態刻意是中性的（「請到」不是「已搬到」）**，因為逐檔決策矩陣目前只存在於 `ui/tab1_macro.py`，而該檔不接線到任何分頁 —— 也就是**它現在哪裡都到不了**
- 已拍板不做 `page_01_macro.py:2046-2054` `empty_state(...)`（`where=where_to_find("diag")` 在 `:2050`）

⛔ **這一塊直接踩到第三節那條「每真第四層最多一個展開 ＋ 一個勾選方框」**：
⭐ AST 實測 `page_01_macro.py`：**展開 1 個、勾選方框 0 個** → **符合**。
（這是五頁裡唯一乾淨的一頁，見第三節表。）

⚠️ **五個詳細塊各自的內文 —— 未複驗。** 我只讀到 `_detail_mid`（`:1066`）的委派方式
（`page_01_macro.py:1117` lazy import `ui.tab1_macro_midcycle::render_mid_cycle_section`，`:1118` 呼叫），
其餘四塊（`_detail_long` `:1235`／`_detail_short` `:1347`／`_detail_inflection` `:1512`／`_detail_ai` `:1805`）**沒有逐行讀**。

⚠️ ⭐ **一個已知的視覺不一致，repo 自己登記過**：`page_01_macro.py:1959-1963`（關鍵句在 `:1962`）寫著
📈 中期循環由 `ui/tab1_macro_midcycle.py` **自己印 `## 📈 中期循環`**，比它上面的區頭 `###` **還大一級**；
同段自陳「修它要動 `ui/tab*.py`，那是客戶方針第 1 條明禁的，所以本批不動」。

**軌別**：`[兩邊都有]`

---

## ② 持倉體檢

### ②-0 軌別總表

⭐ `app.py:646` `with tab_health:` → `:648` `render_fund_grp_health_tab()` → `ui/tab_fund_grp_health.py` `[舊正式分頁]`
⭐ `app.py:718` `with tab_preview_health:` → `:720` `render_holdings_health()` → `ui/views/page_02_health.py` `[新預覽分頁]`

→ **本頁以下所有區塊，指的都是 `[新預覽分頁]`（⑥ 格）。舊 ② 凍結，本文一句都沒有提議改它。**

### ②-1 頁首

```
┌──────────────────────────────────────────────────────────────┐
│  ## 💊 持倉體檢                                                │
│  〔動線列〕                                                     │
│  回答一個問題：我手上這些，哪一檔出問題了？                        │
│  只診斷、不決策 —— 要換什麼、怎麼配，在 ④ 📊 資產配置。            │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】`💊 持倉體檢`
- 【沿用】`回答一個問題：**我手上這些，哪一檔出問題了？** 只診斷、不決策 —— 要換什麼、怎麼配，在 {④ 分頁名}。`

⭐ **現況**：`ui/views/page_02_health.py:1827`（大標）／`:1828`（動線列）／`:1836-1837`（副標）；④ 的名字走 `where_to_find('portfolio')`，不手抄。頁內呼叫序見 `:1839`→`:1858`。
⭐ 與 IA 線框逐字相符：`docs/wireframes/ia-wireframe.html:437`「回答一個問題：**我手上這些，哪一檔出問題了？**只診斷、不決策 ─ 要換什麼、怎麼配，在 04。」

**軌別**：`[新預覽分頁]`

---

### ②-2 診斷條件（表單）

```
┌──────────────────────────────────────────────────────────────┐
│  條件改完按「套用」才重算 —— 拖滑桿的當下不會觸發任何取數。          │
│  輪動門檻　σ  [────●────]                                      │
│  回看窗（月）  [   6   ]      ☐ 只看衛星                        │
│  本金（TWD）   [ 100000 ]                                      │
│                                          [ 套用 ]              │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】`條件改完按「套用」才重算 —— 拖滑桿的當下不會觸發任何取數。`
- 【沿用】欄位名：`輪動門檻　σ`／`回看窗（月）`／`只看衛星`／`本金（TWD）`
- 【沿用】按鈕字：`套用`

⭐ **現況**：
- 表單 `page_02_health.py:1019` `applied_form(_FORM_KEY)` —— **沒傳 `submit_label`**，吃預設值；區塊呼叫 `:1839` `safe_section("診斷條件", _render_filter_form)`
- ⭐ 預設值 `ui/helpers/ia/gated_form.py:52` `APPLY_LABEL: str = "套用"`；同檔 `:50-51` 註解逐字「線框 Tab 02 用的就是『套用』。其他分頁有自己的動詞（『載入總經資料』／『搜尋』／『試算』），那些**是有意義的差異**」
- caption `page_02_health.py:1020`（逐字如上）
- 滑桿 `page_02_health.py:1021`；`回看窗（月）` `:1026`；`只看衛星` `:1031`；`本金（TWD）` `:1050`
- ⭐ `page_02_health.py:1014-1016` docstring 逐字：「**沒有基金代號輸入框，這是客戶 2026-09-05 的裁決，不是漏做**：持股一律從組合帶入」

⚠️ 四個欄位的預設值（`_DEFAULT_SIGMA` 等）—— **未複驗**，我沒有展開常數。
線框圖裡的 `6` 與 `100000` **（這是我編的）**，不是量出來的。

**軌別**：`[新預覽分頁]`　⛔ **這是五頁裡唯一一個按鈕字完全符合客戶三字規則的表單。**

---

### ②-3 沒有持倉時（空狀態，取代整頁）

**畫面上會出現的句子（逐字）**
- ⚠️ **未複驗** —— 我讀到 `page_02_health.py:1844-1845`
  `safe_section("尚未設定持倉", _render_no_holdings)` ＋ `return`，
  但**沒有展開 `_render_no_holdings`（`:1068`）的逐字內容**。
- ⭐ 唯一可確認的：`page_02_health.py:1842-1843` 註解逐字「沒有持倉時，下面三塊沒有任何東西可以診斷 —— 直接走空狀態，**不要**把三塊各印一次灰（那會變成四份在講同一件事的灰字）」
- ⭐ 且 `page_02_health.py:1847-1849` 註明：結論層放在 `return` **之後**，所以**沒有持倉時連結論都不印**。
- ⭐ 線框給過一句：`docs/wireframes/ia-wireframe.html:490`「去哪補 ─ 到『04 資產配置 › 保單與扣款標的』新增」。⚠️ 它和實作是否一致 —— **未複驗**。

**軌別**：`[新預覽分頁]`

---

### ②-4 層 1｜🧾 ① 結論

```
┌──────────────────────────────────────────────────────────────┐
│  ### 🧾 ① 結論 — 我手上這些，現在怎麼了                          │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】`### 🧾 ① 結論 — 我手上這些，現在怎麼了`

⭐ **現況**：常數 `page_02_health.py:623` `CONCLUSION_HEADING`；渲染 `:932`（在 `_render_conclusion`，`:925` 起）；呼叫 `:1850` `safe_section("結論", _render_conclusion)`
⚠️ 結論本文的逐字內容 —— **未複驗**。

**軌別**：`[新預覽分頁]`

---

### ②-5 組合健康總分

**畫面上會出現的句子（逐字）**
- 【沿用】小標：`#### 組合健康總分`

⭐ **現況**：`page_02_health.py:1485`（小標）／`:1464` `_render_health_score`／呼叫 `:1851`
⚠️ 分數本身與其說明 —— **未複驗**。

**軌別**：`[新預覽分頁]`

---

### ②-6 警示卡片（3 欄）

```
┌───────────────┬───────────────┬───────────────┐
│ 吃本金警示     │ 衛星連續落後   │ 影子基金重疊   │
│ 〔n 檔〕       │ ⬜（灰態）     │ 〔n 對〕       │
└───────────────┴───────────────┴───────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】三個卡名：`吃本金警示`／`衛星連續落後`／`影子基金重疊`
- 【沿用】吃本金・判不動時：`{n} 檔都算不出「近一年含息報酬」或「年化配息率」，**無法判定**（不是判定為沒有吃本金）。`
- 【沿用】影子基金・比不出時：`需要至少兩檔、且有持股或產業資料才比得出重疊度；目前 {n} 檔缺這兩種資料。`（或 `目前只有 {n} 檔可比。`）
- 【沿用】影子基金・有結果時：`{算式}；最高一對 {A}／{B}：{n.nn}；{n} 檔缺持股／產業資料未列入比對（{代號…}）。`

⭐ **現況**：
- 三張卡 `page_02_health.py:1585` `render_cards([_eat_card, _lag_card, _shadow_card])`；函式 `:1490`；呼叫 `:1852`
- 吃本金判不動 `page_02_health.py:1536-1542`；有結果 `:1544-1548`
- 衛星連續落後 `page_02_health.py:1550-1554` —— **恆為 `STATE_NOT_READY`（灰）**
- 影子基金 `page_02_health.py:1560-1566`（比不出）／`:1569-1582`（有結果）

⛔ **「衛星連續落後」是刻意留灰的，不是漏做 —— 這一段值得客戶看**：
⭐ `page_02_health.py:1505-1514` docstring 逐字記載兩道閘門：
(1) 客戶裁決波段觀測站（`ui/components/mk_dashboard.py`）的搬遷是**本頁上線之後的獨立批次**；
(2) 層級 —— 它是 L3 UI，要的基準序列來自 L1 `repositories/macro/yf.py::fetch_benchmark_close`，而本檔禁 import `repositories/**`。
⭐ 同段 `:1516-1521` 還明寫**為什麼不拿現成的替代品頂替**（關鍵句在 `:1518` 與 `:1521`）：
「`services/benchmark_compare.py::excess_return` 算的是『**近 1 年**純價格超額報酬』，**不是**『連續兩季』…
把 1 年超額報酬接到一張寫著『連兩季落後』的卡上，就是 `Principal_Erosion` 那個錯誤換一個方向再犯一次 —— **名字對、數字真、意思錯**。」

⛔ ⭐ **線框的示意值被明文拒絕搬進畫面**：`page_02_health.py:1502-1504` 逐字
「**本批不畫『2 檔』『1 檔』『0.78』那三個示意值** —— 卡片上的數字**一律由 SSOT 算出**，算不出來就走灰態。線框的數字是版面示意，不是資料。」
→ **這正是客戶禁止事項 1 的現成正例。本文沿用同一條紀律。**

**軌別**：`[新預覽分頁]`

---

### ②-7 逐檔體檢表

**畫面上會出現的句子（逐字）**
- 【沿用】小標：`#### 逐檔體檢表`

⭐ **現況**：`page_02_health.py:1606`（小標）／`:1588` `_render_health_table`（docstring 自陳「**全寬 + 橫向捲動**」「本批接上真資料」）／呼叫 `:1853`
⚠️ **表格欄位清單 —— 未複驗。** ⛔ 本文**不列欄名**，列了就是發明（客戶禁止事項 4）。

**軌別**：`[新預覽分頁]`

---

### ②-8 委派區（排在最後）

**畫面上會出現的句子（逐字）**
- 【沿用】小標：`#### 🔬 逐檔健診與互斥分析`

⭐ **現況**：`page_02_health.py:1704`（小標）／`:1624` `_render_delegated_sections`／呼叫 `:1858`
⭐ 位置理由 `page_02_health.py:1854-1857` 逐字：「線框 Tab 02 釘死的順序是『總分 → 三張卡 → 逐檔表』，那三塊是本頁自己的版面，委派進來的是**既有模組自帶的版面**（它們自己會 `st.divider()` + 下標題）。夾在中間會把線框指定的動線切斷」
⭐ **委派進來的是七塊，各自一層 `safe_section`**（`page_02_health.py:1750` / `:1751` / `:1757` / `:1758` / `:1759` / `:1763` / `:1790`）：
`基金體檢`／`持倉互斥避險`／`真實收益矩陣`／`持股相關性矩陣`／`−2σ 超跌警示`／`配置回測`／`AI 跨檔評論`。
⚠️ 這七個字串是 `safe_section` 的 **label 引數**（渲染失敗時才會出現在紅框裡），**不保證**它們就是畫面上的小標 —— 各塊真正的標題由被委派模組自己畫，**未複驗**。

⛔ **② 沒有 `### 🧾 ② 依據` 這個區頭，而它自己的 docstring 說有** ——
⭐ `page_02_health.py:57` 寫著四層正典「（`### 🧾 ① 結論` → 卡片 → `### 🧾 ② 依據` → 詳細區）—— **不另發明第二套**」，
但 ⭐ 實測全檔 `EVIDENCE_HEADING` / `🧾 ② 依據` 只有**這一行 docstring**，**沒有任何一處渲染**。
→ **登記為 Q5 的附帶題：② 要不要補一個 ② 依據區頭。本文不代答。**

**軌別**：`[新預覽分頁]`


---

## ③ 標的探索

### ③-0 軌別總表

⭐ `app.py:660` `with tab_research:` → `:662` `render_fund_research_tab()` → `ui/tab_fund_research.py` `[舊正式分頁]`
⭐ `app.py:750` `with tab_preview_research:` → `:752` `render_fund_research()` → `ui/views/page_03_research.py` `[新預覽分頁]`

⭐ `app.py:749` 就地註明：「⛔ 新舊 ③ 同時出現在畫面上是**雙軌的正常狀態**，不要順手去『解決重複』。」

→ **本頁以下所有區塊指 `[新預覽分頁]`（⑧ 格）。**

### ③-1 頁首

```
┌──────────────────────────────────────────────────────────────┐
│  ## 🔍 標的探索                                                │
│  〔動線列〕                                                     │
│  回答一個問題：有沒有更好的標的？                                 │
│  這裡的基金不預設我有持有 —— 我持有部位的健康度在 ② 💊 持倉體檢，   │
│  「要不要換成這檔」的試算在 ④ 📊 資產配置。                        │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】`🔍 標的探索`
- 【沿用】`回答一個問題：**有沒有更好的標的？** 這裡的基金不預設我有持有 —— 我持有部位的健康度在 {② 分頁名}，「要不要換成這檔」的試算在 {④ 分頁名}。`

⭐ **現況**：`page_03_research.py:2730`（大標）／`:2731`（動線列）／`:2736-2740`（副標）
⭐ 對照 IA 線框 `docs/wireframes/ia-wireframe.html:525`：
「回答一個問題： 有沒有更好的標的？ 這裡的基金 **不預設我有持有** 。目前搜尋入口散在三個地方，此次收成一個。」
→ ⚠️ **前半逐字相符；後半（「搜尋入口散在三個地方，此次收成一個」）實作沒有照抄**，改成了兩個指路。**這是一處新舊差異，不是錯誤**，但客戶可能想知道。

**軌別**：`[新預覽分頁]`

---

### ③-2 搜尋條件（表單）

```
┌──────────────────────────────────────────────────────────────┐
│  搜尋條件                                                      │
│  〔文字輸入框〕            〔下拉選單〕                           │
│                                          [ 搜尋 ]              │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】區塊名：`搜尋條件`
- 【沿用】按鈕字：`搜尋`

⭐ **現況**：`page_03_research.py:375` `BLOCK_FORM = "搜尋條件"`；呼叫 `:2741` `safe_section(BLOCK_FORM, _render_search_form)`；表單 `:882` `applied_form(..., submit_label=SUBMIT_LABEL)`；`SUBMIT_LABEL` `:351` `= "搜尋"`；文字框 `:885`；下拉 `:898`
⚠️ 文字框與下拉的**欄位名與選項** —— **未複驗**。⛔ 本文不列，列了就是發明。

**軌別**：`[新預覽分頁]`　✅ 按鈕字符合三字規則

---

### ③-3 還沒搜尋時（空狀態，取代整頁）

**畫面上會出現的句子（逐字）**
- ⚠️ **未複驗** —— 我讀到 `page_03_research.py:2743` `if _applied_query() is None:` → `:2747`
  `safe_section("還沒開始搜尋", _render_not_searched_yet)` ＋ `return`，
  但**沒有展開 `_render_not_searched_yet` 的逐字內容**。
- ⭐ 可確認的：`page_03_research.py:2744-2746` 註解逐字「還沒送出查詢 —— 下面三塊沒有任何東西可畫，直接走空狀態，**不要**把三塊各印一次灰（那會變成四份在講同一件事的灰字，違鐵則 04），也順帶滿足線框給批次的『Form 後才跑』。」

**軌別**：`[新預覽分頁]`

---

### ③-4 搜尋結果

**畫面上會出現的句子（逐字）**
- 【沿用】小標：`#### 搜尋結果`
- 【沿用】結果列的按鈕字：`查這一檔`
- 【沿用】另一顆按鈕字：`直接用我輸入的內容查`

⭐ **現況**：`page_03_research.py:379` `BLOCK_RESULTS = "搜尋結果"`；小標 `:2750`；呼叫 `:2751`
⭐ 按鈕 `page_03_research.py:1083` `st.button(SELECT_LABEL)`，`SELECT_LABEL` `:540` `= "查這一檔"`
⭐ 按鈕 `page_03_research.py:1624` `st.button(DIRECT_LABEL)`，`DIRECT_LABEL` `:545` `= "直接用我輸入的內容查"`
⛔ **這兩個按鈕字不在「試算／搜尋／套用」之內** → Q6

**軌別**：`[新預覽分頁]`

---

### ③-5 單一基金深度

**畫面上會出現的句子（逐字）**
- 【沿用】小標：`#### 單一基金深度`
- 【沿用】展開器：`怎麼算出來的（把數字代進算式）`
- 【沿用】按鈕字：`試算`

⭐ **現況**：`page_03_research.py:404` `BLOCK_DEEP = "單一基金深度"`；小標 `:2752`／呼叫 `:2753`
⭐ 展開器 `page_03_research.py:2247` `st.expander('怎麼算出來的（把數字代進算式）')` —— ⭐ **全頁唯一的 `st.expander`**（AST 實測）
⭐ 表單 `page_03_research.py:2215` `applied_form(..., submit_label=INVEST_SUBMIT_LABEL)`；`INVEST_SUBMIT_LABEL` `:438` `= "試算"`
⭐ 數字輸入 `page_03_research.py:2218`（欄位名 **未複驗**）
⚠️ 深度區塊的內容（指標、欄位、卡片）—— **未複驗**。

⚠️ ⛔ **這一塊很可能是 TER 卡的所在地之一，但我沒有查證。**
1-6(3) 提到的兩處 TER 卡住在 `ui/helpers/fund/checkup.py` 與 `ui/helpers/fund_grp_health/investment.py`，
**我沒有追它們與 `page_03_research.py` 的呼叫關係** → **未複驗**，不得據此推論。

**軌別**：`[新預覽分頁]`　✅ 「試算」符合；展開 1 個符合

---

### ③-6 批次分析

**畫面上會出現的句子（逐字）**
- 【沿用】小標：`#### 批次分析`
- 【沿用】勾選方框：`連同上一輪失敗 / 部分成功的檔一起重抓`
- 【沿用】按鈕字：`開始 / 繼續批次分析`
- 【沿用】下載鈕：`⬇️ 下載這張表（CSV）`

⭐ **現況**：`page_03_research.py:407` `BLOCK_BATCH = "批次分析"`；小標 `:2754`／呼叫 `:2755`
⭐ 勾選 `page_03_research.py:2586` `st.checkbox(_LABEL_BATCH_RETRY)`；常數 `:677`
⭐ 表單 `page_03_research.py:2572` `applied_form(..., submit_label=BATCH_SUBMIT_LABEL)`；`BATCH_SUBMIT_LABEL` `:675` `= "開始 / 繼續批次分析"`
⭐ 下載 `page_03_research.py:2688` `st.download_button('⬇️ 下載這張表（CSV）')`
⛔ **按鈕字「開始 / 繼續批次分析」與下載鈕字都不在三字之內** → Q6

**軌別**：`[新預覽分頁]`

---

## ④ 資產配置

### ④-0 軌別總表

⭐ `app.py:671` `with tab_portfolio:` → `:673` `render_portfolio_tab()` → `ui/tab3_portfolio.py` `[舊正式分頁]`
⭐ `app.py:774` `with tab_preview_portfolio:` → `:776` `render_asset_allocation()` → `ui/views/page_04_portfolio.py` `[新預覽分頁]`

⭐ `app.py:772` 就地註明：「⛔ **新舊 ④ 同時出現在畫面上是雙軌的正常狀態**，不要順手去『解決重複』。」
⭐ `app.py:760-765` 另註明：⑨ **刻意沒有 Checkbox Gate**（⑥⑦ 有），理由是掃描實測新 ④ 與舊 ④ 沒有共用任何會註冊 element id 的函式。

→ **本頁以下所有區塊指 `[新預覽分頁]`（⑨ 格）。**

### ④-1 頁首

**畫面上會出現的句子（逐字）**
- 【沿用】`📊 資產配置`
- 【沿用】`回答一個問題：**那我要怎麼調？** 所有會改變我部位的動作都集中在這裡 —— 診斷「哪裡有問題」在 {② 分頁名}，研究「這檔好不好」在 {③ 分頁名}。`

⭐ **現況**：`page_04_portfolio.py:2729`（大標）／`:2730`（動線列）／`:2734-2737`（副標）
⭐ IA 線框 `ia-wireframe.html:598` 後半是「**含目前埋在巢狀分頁裡的交易帳本**」，實作改成了兩個指路 —— ⚠️ 同 ③-1，**是差異不是錯誤**。

**軌別**：`[新預覽分頁]`

---

### ④-2 沒有持倉時 —— ⛔ 這一頁的空狀態與其他頁不同

```
┌──────────────────────────────────────────────────────────────┐
│  〔空狀態說明〕                                                 │
│  #### 保單與扣款標的         ← ⛔ 沒有持倉時「這一塊照畫」         │
│     ➕ 加入標的  〔就地展開的輸入表單〕      [ 加入 ]             │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】空狀態標題有**兩種**：`尚未設定持倉`／`標的有了，還沒有淨值資料`
- 【沿用】小標：`#### 保單與扣款標的`
- 【沿用】加入區標題：`➕ 加入標的`
- 【沿用】按鈕字：`加入`

⭐ **現況**：
- 兩種空狀態標題 `page_04_portfolio.py:1177` `EMPTY_TITLE_NONE = "尚未設定持倉"`／`:1179` `EMPTY_TITLE_PENDING = "標的有了，還沒有淨值資料"`
- 區塊標籤 `page_04_portfolio.py:1183` `EMPTY_SECTION_LABEL = "目前的持倉狀態"`
- 空狀態 ＋ 保單塊 `page_04_portfolio.py:2769-2771`（`:2739` `if not _holdings():` 起）
- `BLOCK_POLICY` `page_04_portfolio.py:609` `= "保單與扣款標的"`
- `ADD_FUND_HEADING` `page_04_portfolio.py:720` `= "➕ 加入標的"`
- 表單 `page_04_portfolio.py:2383` `applied_form(..., submit_label=ADD_SUBMIT_LABEL)`；`ADD_SUBMIT_LABEL` `:744` `= "加入"`；文字框 `:2386`

⭐ **這個例外是客戶自己拍板的**：`page_04_portfolio.py:11`（模組 docstring）與 `:694-695`（區塊註解）**兩處**逐字引客戶原話
「**加入基金入口：拍板整合至 ⑨「保單與扣款標的」，無持倉時直接就地展開輸入表單。**」

⛔ ⭐ **同段登記了一個現在就成立的缺口**（`page_04_portfolio.py:2759-2761`，逐字）：
「**這一頁在此之前物理上加不了一檔基金**（實測：新五頁寫進 `portfolio_funds` 的呼叫點是 0 個），
而空狀態指去舊 ④ —— 客戶已拍板的退場順序（⑦→⑥→⑧→⑨）走到底時，**那句指路會指向不存在的地方**。」
→ **這就是 Q4 為什麼非決定不可。**
⚠️ 「寫進 `portfolio_funds` 的呼叫點是 0 個」是**該檔自陳的實測，我沒有複驗**。

⛔ **按鈕字「加入」不在三字之內** → Q6

**軌別**：`[新預覽分頁]`

---

### ④-3 有持倉時的區塊順序（七塊，全寬）

⭐ **現況**：`page_04_portfolio.py:2774-2789`，逐行對應如下（順序即畫面順序）。

| 序 | 小標（逐字） | 常數 `file:line` | 渲染 `file:line` |
|---|---|---|---|
| 1 | `#### 核心 ／ 衛星現況 vs 建議` | `page_04_portfolio.py:590` `BLOCK_MIX` | `:2774-2775` |
| 2 | `#### 再平衡試算` | `:604` `BLOCK_FORM` | `:2776-2777` |
| 3 | （動作卡，**無小標**） | — | `:2778` `safe_section("動作卡", _render_action_cards)` |
| 4 | `#### 保單與扣款標的` | `:609` `BLOCK_POLICY` | `:2780-2781` |
| 5 | `#### {組合績效}` | `:627` `perf_block_label()` → `story_nav._SECTION_LABELS["pf_perf"]` | `:2782-2783` |
| 6 | `#### 穿透式持股 ／ 產業集中度` | `:622` `BLOCK_CONCENTRATION` | `:2784-2785` |
| 7 | `#### 總經曝險聯動` | `:624` `BLOCK_MACRO_LINK` | `:2786-2787` |
| 8 | `#### 交易帳本` | `:613` `BLOCK_LEDGER` | `:2788-2789` |

⚠️ 上表是 **8 列不是 7 列**（第 3 列沒有小標）。
⚠️ `perf_block_label()` 實際回傳的中文字 —— **未複驗**（我讀到它走 `section_label("pf_perf")`，沒有展開 SSOT 值）。
⚠️ ⭐ `page_04_portfolio.py:637-639` 附帶記載：「效率前緣沒有自己的 SSOT key，所以它不另立區塊名 —— 它畫在本塊之內，小標由被委派的 `render_efficient_frontier()` 自己出（`### 🎯 效率前緣診斷（教學・非建議）`）」

⛔ **注意第 1 塊的名字含「建議」** —— `核心 ／ 衛星現況 vs 建議`。**現有元件名，本文照原名引述。** 它是否落在 Q2 紅線內，一併送客戶。

**軌別**：`[新預覽分頁]`

---

### ④-4 再平衡試算（表單）

**畫面上會出現的句子（逐字）**
- 【沿用】小標：`#### 再平衡試算`
- 【沿用】勾選方框：`只調衛星`
- 【沿用】按鈕字：`試算`

⭐ **現況**：表單 `page_04_portfolio.py:1597` `applied_form(..., submit_label=SUBMIT_LABEL)`；`SUBMIT_LABEL` `:569` `= "試算"`；滑桿 `:1600`；數字框 `:1607`；勾選 `:1613` `st.checkbox(_LABEL_SATELLITE_ONLY)`，常數 `:572` `= "只調衛星"`
⚠️ 滑桿與數字框的欄位名 —— **未複驗**。

**軌別**：`[新預覽分頁]`　✅ 「試算」符合

---

### ④-5 保單與扣款標的（表格）

```
┌──────────────────────────────────────────────────────────────┐
│  #### 保單與扣款標的                                            │
│  保單編號│基金代號│基金名稱│幣別│級別│金額│現金%                  │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】七個欄名：`保單編號`／`基金代號`／`基金名稱`／`幣別`／`級別`／`金額`／`現金%`

⭐ **現況**：`page_04_portfolio.py:687-688`
`POLICY_TABLE_COLUMNS: tuple[str, ...] = ("保單編號", "基金代號", "基金名稱", "幣別", "級別", "金額", "現金%")`
⭐ 線框對照 `docs/wireframes/draft-four-page-content.html:741` —— **一致**（材料所述，我未逐字比對該行，**未複驗**）

⭐ **七欄全部有對應的既有欄位**（`page_04_portfolio.py:682-685` 逐字自陳）：
`policy_id` / `code` / `name` / `currency` / `policy_tier` / `invest_twd` / `div_cash_pct`
—— 前六個由 `ui/helpers/portfolio/load.py` 寫進 session，第七個來自 `repositories/policy/_helpers.py::ALL_COLS`。
⭐ 同段 `:686` 逐字：「⛔ **不要為了讓表好看而加第八欄** —— 那一刻起這份清單就變成自己發明的規格。」

⛔ **「扣款日」與「進場匯率」不在這七欄裡**：
- ⭐ 「扣款日」**全 repo 0 命中**（`git grep` 無輸出；正對照見 0-3 的方法說明）
- ⚠️ 「進場匯率」—— ⭐ 現行 v2 schema（`repositories/policy/v2.py:527-538`）有 `avg_fx`「平均買入匯率」，**那不是「進場匯率」**（一個是加權平均，一個是單筆）。**本文不把它當同一件事。**

⚠️ **一個欄名不一致，登記不裁決**：顯示表第 6 欄叫 `金額`（`page_04_portfolio.py:688`），
而 Sheet schema 同一個欄位 `invest_twd` 的中文表頭是 **`淨投資金額`**（`repositories/policy/v2.py:533`）。
**兩個名字指同一個欄位。要不要統一 —— 本文不代答，登記為 Q9。**

**軌別**：`[新預覽分頁]`

---

### ④-6 配息月曆（勾選方框 gate）

```
┌──────────────────────────────────────────────────────────────┐
│  ☐ 載入本月配息月曆（要算幾秒）                                  │
│     〔沒勾 → ⬜ 灰色說明 ＋ 去哪補：上方「載入本月配息月曆…」〕      │
│     〔勾了 → 本月推估除息 N 檔 ／ 本月有 N 檔推不出除息日〕         │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】勾選方框：`載入本月配息月曆（要算幾秒）`
- 【沿用】說明：`不勾就完全不算。這一項是純 CPU 計算，不會連線、也不會寫任何東西。`
- 【沿用】有結果：`本月推估除息 **{n}** 檔`
- 【沿用】全推不出：`本月有 **{n}** 檔推不出除息日（是推不出，不是沒配息）`
- 【沿用】附註：`另有 {n} 檔推不出日期（**不是**沒配息）；{n} 檔累積型／查無配息。推估用過往配息節奏推算，**非官方公告**。`

⭐ **現況**：
- gate 字面常數 `page_04_portfolio.py:802` `DIVCAL_GATE_LABEL = "載入本月配息月曆（要算幾秒）"`
- 勾選 `page_04_portfolio.py:2612-2616`；help 文字 `:2615`
- 沒勾時 `page_04_portfolio.py:2618-2620` `not_ready(..., where=f"上方「{DIVCAL_GATE_LABEL}」")` —— ⭐ **指路與勾選框吃同一個常數**，`:800-801` 就地註明理由（「指到一個不存在的勾選框，正是本 repo 發作過三次的死指路」）
- 全推不出 `page_04_portfolio.py:2659`；有結果 `:2661`；附註 `:2663-2666`
- ⭐ `page_04_portfolio.py:2654-2655` 逐字：「⛔ 這裡**不得**寫『本月推估除息 0 檔』——那讀起來是『這個月沒配息』，而事實是『推不出來』。同一畫面兩個口徑，正是 §1 最在意的形狀。」
  → **這是客戶禁止事項 1／2 的現成正例，本文沿用。**
- ⭐ `page_04_portfolio.py:803-806` 記載 `DIVCAL_GATE_KEY` 已於 2026-09-07 移除，理由是 `key=` 會多開一條 session 寫入管道

**軌別**：`[新預覽分頁]`

---

### ④-7 ⚠️ 本頁未複驗的區塊（據實列出，不臆測）

以下四塊我**只讀到小標與呼叫點，沒有展開內容**：
- `#### {組合績效}`（`page_04_portfolio.py:2782-2783`）
- `#### 穿透式持股 ／ 產業集中度`（`:2784-2785`）
- `#### 總經曝險聯動`（`:2786-2787`）
- `#### 交易帳本`（`:2788-2789`）

⛔ **本文不為它們寫任何「畫面上會出現的句子」。** 寫了就是發明（客戶禁止事項 4）。

⚠️ ⭐ **交易帳本有一條線框明令，我登記但未複驗其實作**：
`docs/wireframes/ia-wireframe.html:643`「**目前埋在『持股』分頁裡的第二層 `st.tabs`，此次拉到同一層。**買賣紀錄 / 成本 / 已實現損益 / 對帳。欄位多，全寬橫向捲動。」
⚠️ 同檔另有一句「巢狀 st.tabs 一律不留；分頁只有一層」，**我第一次抄下的行號是錯的**（見 1-7 M5），現**不標行號** —— 它在 `:643` 之後的同一區塊內，**未複驗精確行號**。
⚠️ 新 ④ 是否真的沒有巢狀 `st.tabs` —— **未複驗**。
⚠️ ⭐ 但 `app.py:469-471` 記載**舊** ④（`ui/tab3_t7_ledger.py` 的 A/B/C 再平衡）**仍有一層巢狀 `st.tabs` 且會渲染** —— 那是 `[舊正式分頁]` 的事，**凍結，不在本文射程**。

---

## ⑤ 設定與診斷

### ⑤-0 軌別總表

⭐ `app.py:686` `with tab_settings:` → `:688` `render_settings_diag_tab()` → `ui/tab_settings_diag.py` `[舊正式分頁]`
⭐ `app.py:726` `with tab_preview_settings:` → `:728` `render_settings_and_diagnostics()` → `ui/views/page_05_settings.py` `[新預覽分頁]`

⛔ ⭐ **⑤ 與 ⑦ 有一塊真的畫兩份**（`app.py:545-556`，逐字實測記載）：
「這一塊現在**有兩份**，不是一份 —— (1) 舊 ⑤ … → `render_fetch_diag_from_session()`；(2) 新 ⑤（掛在 ⑦）… → `render_fetch_diag_from_session()`。**兩條路徑都不在任何 `if` 底下**，所以 ⑤ 與 ⑦ **各畫一份**。」
⭐ 同段 `:561-562`：「這個『⑤ 與 ⑦ 各一份』的**行為**要怎麼處置，正由另一批在 `ui/views/page_05_settings.py` 上進行，**本輪不知道、也不假設它會怎麼修**。」
→ ⚠️ **也就是說本頁的內容可能正在被別組動。本文的 `file:line` 是 `9cbf037` 當下的快照。**

### ⑤-1 頁首

**畫面上會出現的句子（逐字）**
- 【沿用】`⚙️ 設定與診斷`
- 【沿用】`回答一個問題：**資料本身可不可信、要不要我補？** 平常不用進來；出事時第一個進來 —— 任何投資判斷在 {① 分頁名} 到 {④ 分頁名} 之間。`

⭐ **現況**：`page_05_settings.py:1710`（大標）／`:1713`（副標）
⛔ ⭐ **⑤ 沒有動線列**：`page_05_settings.py:1700` docstring 逐字「**沒有 `render_story_nav("settings")`** —— 它會靜默 no-op」
⛔ ⭐ **⑤ 沒有頁面層級的空狀態**：同段 `:1701`「**沒有頁面層級的空狀態**（D-2）—— 與 ④ 刻意不同。」
⭐ IA 線框 `ia-wireframe.html:676` 逐字相符（前半）。

**軌別**：`[新預覽分頁]`

---

### ⑤-2 層 1｜🧾 ① 結論

**畫面上會出現的句子（逐字）**
- 【沿用】區頭：`### 🧾 ① 結論 — 現在能不能信畫面上的數字`
- 【沿用】gate 沒勾時：`**這一格只回答一件事：「🔭 查一次資料來源狀態」這個開關現在是關的，所以資料來源狀態還沒查。** 其餘各塊**各自**寫了自己現在的狀態 —— 這一格**看不到它們，也不替它們發言**（它們的開關沒有留下這一格讀得到的痕跡）。`
- 【沿用】gate 勾了時：`**這一格只回答一件事：「🔭 查一次資料來源狀態」這個開關已經打開了。** 查到什麼、{fail}，在底下「資料來源健康度」那一塊 —— 這一格**不**替它壓成一句話（壓了就會變成第二份跟底下不同步的真相，而使用者會信排在前面的那一句）。其餘各塊同樣**各自**寫自己的狀態，這一格看不到它們。`

⭐ **現況**：常數 `page_05_settings.py:509` `CONCLUSION_HEADING`；渲染 `:1166`；呼叫 `:1721`
⭐ 兩段 verdict `page_05_settings.py:563-566`（未勾）／`:581-585`（已勾）；渲染 `:1178`／`:1182`
⭐ 結論卡 `page_05_settings.py:1184` `render_cards(_conclusion_cards())` —— ⚠️ 卡的內容 **未複驗**
⚠️ `{fail}` 佔位符實際會填什麼 —— **未複驗**。

**軌別**：`[新預覽分頁]`

---

### ⑤-3 層 2｜🧾 ② 依據 ＋ 六個區塊

```
┌──────────────────────────────────────────────────────────────┐
│  ### 🧾 ② 依據 — 逐項狀態                                       │
│  ### 資料來源健康度            ☐ 🔭 查一次資料來源狀態            │
│  ### NAV 累積狀態              ☐ 讀一次雲端累積狀態               │
│                                ▸ 逐檔明細                       │
│  ### 連線與金鑰                ☐ 🔍 載入抓取診斷細節              │
│  ### 手動補資料                ☐ 🗄️ 載入手動補資料                │
│  ### 使用手冊                  ☐ 📖 載入使用手冊　▸ 使用手冊       │
│  ### 🗄️ 資料維護與通報         ☐ 🗄️ 載入資料維護與通報            │
│                                ▸ 🗄️ 資料維護與通報               │
└──────────────────────────────────────────────────────────────┘
```

**畫面上會出現的句子（逐字）**
- 【沿用】區頭：`### 🧾 ② 依據 — 逐項狀態`
- 【沿用】六個區塊名：`資料來源健康度`／`NAV 累積狀態`／`連線與金鑰`／`手動補資料`／`使用手冊`／`🗄️ 資料維護與通報`
- 【沿用】六個勾選方框：`🔭 查一次資料來源狀態`／`讀一次雲端累積狀態`／`🔍 載入抓取診斷細節`／`🗄️ 載入手動補資料`／`🗄️ 載入資料維護與通報`／`📖 載入使用手冊`
- 【沿用】三個展開器：`逐檔明細`／`🗄️ 資料維護與通報`／`使用手冊`

⭐ **現況（逐一實測）**：

| 元件 | 逐字 | 常數 `file:line` | 使用 `file:line` |
|---|---|---|---|
| 區頭 | `### 🧾 ② 依據 — 逐項狀態` | `page_05_settings.py:513` `EVIDENCE_HEADING` | `:1723` |
| 區塊 1 | `資料來源健康度` | `:357` `BLOCK_HEALTH` | 標題 `:1730`／渲染 `:1731` |
| 區塊 2 | `NAV 累積狀態` | `:735` `nav_status_label()` → `story_nav._SECTION_LABELS["nav_status"]`（`story_nav.py:236`） | 標題 `:1733`／渲染 `:1734` |
| 區塊 3 | `連線與金鑰` | `:359` `BLOCK_KEYS` | 標題 `:1736`／渲染 `:1737` |
| 區塊 4 | `手動補資料` | `:745` `nav_manual_label()` → `story_nav.py:237` | 渲染 `:1748`（**標題由被委派模組自己畫，本檔不畫**） |
| 區塊 5 | `使用手冊` | `:366` `BLOCK_MANUAL` | 標題 `:1750`／渲染 `:1751` |
| 區塊 6 | `🗄️ 資料維護與通報` | `:750` `maintain_label()` → `story_nav.py:226` | 標題 `:1762`／渲染 `:1763` |
| 勾選 1 | `🔭 查一次資料來源狀態` | `:392` `DIAG_GATE_LABEL` | `:1222` |
| 勾選 2 | `讀一次雲端累積狀態` | `:411` `NAV_GATE_LABEL` | `:1283` |
| 勾選 3 | `🔍 載入抓取診斷細節` | `:484` `FETCH_DIAG_GATE_LABEL` | `:1385` |
| 勾選 4 | `🗄️ 載入手動補資料` | `:459` `BACKFILL_GATE_LABEL` | `:1483` |
| 勾選 5 | `🗄️ 載入資料維護與通報` | `:462` `MAINTAIN_GATE_LABEL` | `:1602` |
| 勾選 6 | `📖 載入使用手冊` | `:472` `MANUAL_GATE_LABEL` | `:1676` |
| 展開 1 | `逐檔明細` | `:397` `NAV_DETAIL_LABEL` | `:1326` |
| 展開 2 | `🗄️ 資料維護與通報` | `:750` `maintain_label()` | `:1594` |
| 展開 3 | `使用手冊` | `:366` `BLOCK_MANUAL` | `:1634` |

⛔ **⑤ 的勾選方框 6 個、展開 3 個 —— 這是五頁裡唯一嚴重超出第三節那條規則的一頁。** 見 Q5。

⭐ **區塊 4 刻意不由本檔畫標題**（`page_05_settings.py:1739-1747` 逐字）：
`render_nav_manual_section()` 自己會畫同一份 SSOT、同一級、逐字相同的標題；本檔再畫一次會「連著出現兩個一模一樣的『手動補資料』」。
⭐ 守衛 `test_each_block_heading_is_drawn_exactly_once`（**0 次與 2 次兩個方向都紅**）。

⭐ **區塊 6 的位置是客戶拍板的**（`page_05_settings.py:1755` 逐字引客戶原話）：
「**系統維護與通報屬管理設定範疇，折疊收攏即可，不影響主視覺。**」
⭐ 同段 `:1756-1757` 登記：「線框沒有給這一塊位置（(D-5)），本次裁決**沒有推翻那個登記**」。

⚠️ **六個區塊各自的內文** —— **未複驗**。⭐ 它們全部走 `safe_section(...)` 委派給舊模組
（`page_05_settings.py:1691` docstring 逐字「五塊照線框順序，**功能全部委派舊模組**」）。
⚠️ **注意 docstring 說「五塊」，實際渲染是「六塊」**（多了客戶 2026-09-07 加的第 6 塊）——
⚠️ 這是**該檔自己的敘述與自己的實作不一致**，我只登記，不裁決。

**軌別**：`[新預覽分頁]`

---

### ⑤-4 ⚠️ 本頁一個結構性事實，客戶可能想先知道

⭐ `page_05_settings.py:1694-1698` 逐字：
「合併成一頁之後六個區塊共用同一次 script run：管理室當掉會一併帶走 🔭 資料診斷與 📖 說明書，
而那兩塊正是使用者出事時要去的地方（本頁一句話職責：『出事時第一個進來』）——
**把診斷跟故障綁在同一條命上，等於在最需要它的時候把它拿走。**」

→ 現行解法是**每個區塊各自 `safe_section()`**。**這一點我複驗過**：
⭐ 六個區塊各一個：`:1731` / `:1734` / `:1737` / `:1748` / `:1751` / `:1763`，
＋ 結論層 `:1721` 另一個 —— **共 7 個**（AST 與逐行 grep 兩法一致）。

**軌別**：`[新預覽分頁]`


---

## 三、四層閱讀順序

### 3-1 四層是什麼（正典出處）

⭐ **正典寫在 `ui/views/page_02_health.py:57`，逐字**：
> （`### 🧾 ① 結論` → 卡片 → `### 🧾 ② 依據` → 詳細區）—— **不另發明第二套**。

⭐ ① 的實作把同一件事寫成四個層 ＋ 一組插入（`ui/views/page_01_macro.py:2141-2192` 的區塊註解）：
`層 1：🧾 ① 結論（全寬）` → `卡片網格（插在 ① 與 ② 之間）` → `層 2：🧾 ② 依據（全寬表）` → `層 3：三欄` → `層 4：🔎 詳細區`

⚠️ ⛔ **我拿到的說法是「結論 → 三欄卡 → 依據 → 詳細」，與實作有兩處差**（M2）：
1. **① 有兩組卡格，不是一組**：`:2157-2164` 六張（在結論與依據**之間**）＋ `:2185-2189` 三張（在依據**之後**）。
2. **卡格的位置是「結論之後、依據之前」**，所以「結論 → 三欄卡 → 依據」的**順序是對的**，錯的是「三欄」這個數量詞 —— ① 那一組是**六張**。

**本文採用的寫法**（供客戶拍板）：
> **層 1 結論 → 卡片網格 → 層 2 依據 → 層 3 三欄 → 層 4 詳細區**
> 其中 **層 3 三欄是 ① 才有的**；②③④⑤ 的卡片只有一組，就落在層 1 與層 2 之間。

⚠️ **這是我的歸納，不是 repo 裡寫好的一句話。**【待決定】— 併入 Q5 送客戶。

### 3-2 ⛔ 四層在五頁上的落地程度極不平均（實測）

⭐ 實測方式：`grep -c` 兩個區頭字串（`CONCLUSION_HEADING` / `🧾 ① 結論`、`EVIDENCE_HEADING` / `🧾 ② 依據`）逐檔數，再逐一開檔判讀是渲染還是註解。

| 頁 | 層 1 結論 | 層 2 依據 | 層 3 三欄 | 層 4 詳細區 | 實測 |
|---|---|---|---|---|---|
| ① 市場總覽 | ✅ `page_01_macro.py:676`／`:2123` | ✅ `:733`／`:2131` | ✅ `:2185-2189` | ✅ `:1969` `### 🔎 詳細資料與說明` | **四層俱全，唯一一頁** |
| ② 持倉體檢 | ✅ `page_02_health.py:623` 常數／`:932` 渲染 | ⛔ **沒有** | ～ `:1585` 三張警示卡 | ～ `:1858` 委派區（**不叫「詳細區」**） | ⛔ 見下方 |
| ③ 標的探索 | ⛔ **沒有** | ⛔ **沒有** | ⛔ 沒有卡格 | ⛔ 沒有 | **零層** |
| ④ 資產配置 | ⛔ **沒有** | ⛔ **沒有** | ～ `:2778` 動作卡（**無小標**） | ⛔ 沒有 | **零層** |
| ⑤ 設定與診斷 | ✅ `page_05_settings.py:509` 常數／`:1166` 渲染／`:1721` 呼叫 | ✅ `:513` 常數／`:1723` 渲染 | ～ `:1184` `render_cards(_conclusion_cards())`（在結論**之內**） | ⛔ 沒有 | **兩層 ＋ 一組卡** |

⛔ **② 的 docstring 與 ② 的實作互相矛盾**：
⭐ `page_02_health.py:57` 宣告四層含 `### 🧾 ② 依據`，但 ⭐ 實測該字串在全檔**只出現這一次，而且是在 docstring 裡** —— **沒有任何一處渲染**。
⚠️ **單組實測、未經第二組驗證、取決於有沒有漏看**（我用 `grep -nE "EVIDENCE_HEADING|🧾 ② 依據"` 掃，`f"{變數}"` 動態組出的區頭掃不到）。

⛔ **③ 與 ④ 一層都沒有。** 它們直接從頁首跳到 `#### 區塊名`。
→ **【待決定】：四層要不要補到 ③ ④ 上？補的話，③ 的「結論」與「依據」各是什麼？**
→ ⚠️ **本文不代答** —— 那是「這一頁的結論句該講什麼」，屬業務語意，不是版面問題。

### 3-3 三種「點開」方式

⭐ 三種在 repo 裡都有實例，逐一給出處：

| 方式 | 機制 | 實例 `file:line` | 逐字 |
|---|---|---|---|
| **點標題展開** | `st.expander(...)` | `page_01_macro.py:1292` | `📋 原始讀數與來源` |
| **勾選方框** | `st.checkbox(...)` ＋ 緊接 `if not X: not_ready(...); return` | `page_04_portfolio.py:2612-2620` | `載入本月配息月曆（要算幾秒）` |
| **填完按鈕** | `applied_form(key, submit_label=...)` | `page_02_health.py:1019` | 送出鈕 `套用` |

⭐ **勾選方框這一種有一條寫死的紀律，值得客戶知道**：
`page_04_portfolio.py:800-801` 逐字「gate 的字面。**畫面上與灰態指路吃的是同一個常數** —— 指到一個不存在的勾選框，正是本 repo 發作過三次的死指路」。
→ 也就是 ⭐ `:2619` 的 `where=f"上方「{DIVCAL_GATE_LABEL}」"` 與 `:2613` 的勾選框**吃同一個常數**，不可能分岔。

⭐ **勾選方框刻意不帶 `key=`**：`page_04_portfolio.py:2610-2611` 逐字「⛔ **不帶 `key=`**…帶了就等於多開一條『每次渲染、不經閘門』的 session 寫入管道」。

⭐ **表單這一種也有一條紀律**：`page_02_health.py:1017-1018` 逐字
「下游一律讀這個（已套用的值），**不要讀 widget 的回傳值** —— 讀了就等於沒有 form（鐵則 02 的重點不是『有沒有 form』，是**重運算有沒有被 gate 住**）」。

### 3-4 ⛔ 「每真第四層最多一個展開 ＋ 一個勾選方框」vs 現況

⭐ **實測方法**：對五個 `ui/views/page_0*.py` 跑 AST（`ast.walk` 找 `Call`，比對 `func.attr`），
**不用字串 grep** —— 理由：⭐ `ui/helpers/macro/ndc.py` 用的是 `@_st_mod.cache_data`（別名匯入），
`CLAUDE.md §8.2.A.1` 的驗證段 ① 就地記載過「`@st\.cache_data` 這種寫法掃不到它」。同一個病會咬 `st.expander(`。

| 頁 | `expander` | `checkbox` | 合計 | 對「1 ＋ 1」 |
|---|---|---|---|---|
| ① 市場總覽 | **1**（`:1292`） | **0** | 1 | ✅ **符合** |
| ② 持倉體檢 | **0** | **2**（`:1031` `只看衛星`、`:1717` `載入逐檔健診與互斥分析（與舊分頁同一份程式碼）`） | 2 | ⚠️ 勾選多 1 |
| ③ 標的探索 | **1**（`:2247` `怎麼算出來的（把數字代進算式）`） | **1**（`:2586` `連同上一輪失敗 / 部分成功的檔一起重抓`） | 2 | ✅ **符合** |
| ④ 資產配置 | **0** | **2**（`:1613` `只調衛星`、`:2612` `載入本月配息月曆（要算幾秒）`） | 2 | ⚠️ 勾選多 1 |
| ⑤ 設定與診斷 | **3**（`:1326` `逐檔明細`、`:1594` `🗄️ 資料維護與通報`、`:1634` `使用手冊`） | **6**（`:1222`／`:1283`／`:1385`／`:1483`／`:1602`／`:1676`，逐字見 ⑤-3 表） | **9** | ⛔ **嚴重超出** |

⚠️ **這張表數的是「整個頁檔裡有幾個」，不是「第四層裡有幾個」** ——
因為 ③ ④ ⑤ **根本沒有第四層**（見 3-2），所以「第四層裡有幾個」這個問題**對它們沒有定義**。
⛔ **我不把它硬算成 0 或硬算成全部。** 那會是拿一個不存在的層去下結論。

⚠️ ② 的 `只看衛星`（`:1031`）與 ④ 的 `只調衛星`（`:1613`）**都在表單裡**，不是獨立閘門 ——
**它們是不是這條規則要數的東西，取決於規則怎麼定義。併入 Q5。**

⛔ **⑤ 是這條規則的真正衝突點，而且它的 9 個是設計出來的，不是堆出來的**：
⭐ `page_05_settings.py:1694-1698` 逐字寫著六區塊共用一次 script run 的隔離問題；
六個勾選方框各自 gate 一塊，正是為了**讓每一塊都能單獨不載入**。
→ **要縮到「1 ＋ 1」，等於要重新設計 ⑤ 的載入模型。這是 Q5 為什麼是客戶題而不是執行題。**

### 3-5 ⛔ 按鈕字「只准試算／搜尋／套用」vs 現況

⭐ **實測方法**：AST 掃 `applied_form` / `st.button` / `st.form_submit_button` / `st.download_button`，
取 `submit_label=` 或第一個位置引數，再逐一展開常數。

| 頁 | 按鈕字（逐字） | 出處 | 對三字 |
|---|---|---|---|
| ① | `📡 載入總經資料` | `shared/ui_control_labels.py:30`（用於 `page_01_macro.py:2082-2084`） | ⛔ |
| ① | `🔄 更新總經資料` | `shared/ui_control_labels.py:31`（同上，載過一次後） | ⛔ |
| ① | `▶️ 生成 AI 總結` | `page_01_macro.py:248`（用於 `:1866`） | ⛔ |
| ① | `🔄 重新生成 AI 總結` | `page_01_macro.py:249`（同上） | ⛔ |
| ② | `套用` | `ui/helpers/ia/gated_form.py:52` `APPLY_LABEL`（`page_02_health.py:1019` 吃預設值） | ✅ |
| ③ | `搜尋` | `page_03_research.py:351`（用於 `:882`） | ✅ |
| ③ | `查這一檔` | `page_03_research.py:540`（用於 `:1083` **裸鈕**） | ⛔ |
| ③ | `直接用我輸入的內容查` | `page_03_research.py:545`（用於 `:1624` **裸鈕**） | ⛔ |
| ③ | `試算` | `page_03_research.py:438`（用於 `:2215`） | ✅ |
| ③ | `開始 / 繼續批次分析` | `page_03_research.py:675`（用於 `:2572`） | ⛔ |
| ③ | `⬇️ 下載這張表（CSV）` | `page_03_research.py:2688` **字面值**（下載鈕） | ⛔ |
| ④ | `試算` | `page_04_portfolio.py:569`（用於 `:1597`） | ✅ |
| ④ | `加入` | `page_04_portfolio.py:744`（用於 `:2383`） | ⛔ |
| ⑤ | （**0 個按鈕**） | AST 實測：`page_05_settings.py` 的 `button` / `form_submit_button` / `applied_form` / `download_button` **全部 0 次** | ✅（空集合） |

**統計：13 個按鈕字，4 個在三字之內，9 個不在。**
⚠️ **單組實測、未經第二組驗證、取決於有沒有漏看** ——
AST 只解析**字面值與可追到的模組層常數**；
⛔ **執行期組出來的按鈕字（例如 `f"…{變數}"`、或從 dict 取值）我掃不到，本文不宣稱窮舉。**
⭐ 已知的一個執行期案例：`page_01_macro.py:2074` 的 `_submit_label` 是三元式 —— **兩個分支我都追到了**，所以上表 ① 列了兩行。

⛔ ⭐ **repo 自己對這件事有立場，而且與客戶的三字規則**不一致****：
`ui/helpers/ia/gated_form.py:50-51` 逐字：
> 「其他分頁有自己的動詞（『載入總經資料』／『搜尋』／『試算』），**那些是有意義的差異**，用 `submit_label=` 傳，不要改這個預設值。」

→ **也就是說：現行程式碼把「按鈕字各頁不同」當成刻意設計，客戶的規則把它當成要收斂的東西。**
→ **這是一組真的規則衝突，不是漏做。併入 Q6，本文不代答。**

### 3-6 ⛔ 「主 CTA『更新淨值與配息』repo 0 命中」—— 我重驗了，成立，而且更廣

⭐ 我跑了三層：

| 查什麼 | 結果 |
|---|---|
| `更新淨值與配息`（完整詞） | **0 命中** |
| `更新淨值`（去掉「與配息」，放寬） | **0 命中** |
| `淨值`（正對照，確認指令是活的） | 大量命中（例：`fund_fetcher.py` 單檔 8 行、`repositories/fund/fund_orchestration.py` 18 行） |

→ **「更新淨值與配息」這個主 CTA，現在不存在於 repo。**
⚠️ **單組實測、未經第二組驗證、取決於有沒有漏看** —— 我查的是**中文字面值**；
⛔ 若它是由變數組出來的（例如 `f"更新{_x}與{_y}"`），這三層全部掃不到。

⚠️ **我沒有查證「它應該存在」這件事。** 我不知道它是線框裡的、舊草稿裡的、還是客戶口述的。
→ **【待決定】Q10：這顆主 CTA 要不要做？它落在哪一頁？**
⛔ **本文不替它寫線框** —— 寫了就是發明一顆不存在的按鈕（客戶禁止事項 4）。

---

## 四、四色資料不足

### 4-1 先解開那個矛盾：**repo 裡有三個不同的「四」，它們不是同一個四**

我把 `ui/helpers/render_state.py`（239 行）整檔讀完了。結論如下 —— **每一個都有出處**。

| 候選 | 是幾 | 出處 | 這是什麼 |
|---|---|---|---|
| **(A)** `render_state.py` 的「五態」 | **5** | `render_state.py:1` 模組 docstring 第一行「顏色**五態** SSOT」；表在 `:18-26` | **全站的顏色語彙表** |
| **(B)** `cards.py` 的 `CARD_STATES` | **4** | `ui/helpers/ia/cards.py:46-47` `CARD_STATES = (STATE_OK, STATE_NOT_READY, STATE_BUSINESS, STATE_ERROR)`；`:45` 就地註「順序即『嚴重度』由低到高，僅供閱讀，**程式不依賴順序**」 | **一張卡能帶的狀態** |
| **(C)** `render_state.py` 的 public 函式 | **4** | `:112 not_ready` / `:134 system_error` / `:186 safe_section` / `:216 business_alert` | **模組的 API** |
| **(D)** 線框 §03 的標題 | **3** | `docs/wireframes/fund-empty-state-wireframe.html:357`「03 顏色：**三態**統一規則」 | **線框當時的講法** |

⛔ **(C) 的四個裡有一個不是狀態 —— 我確認過了。**
⭐ `render_state.py:186-215` `safe_section(label, fn, *args, **kwargs)`：它 `try: fn(...)` ，
`except` 時呼叫 `system_error(f"「{label}」區塊渲染失敗", _sec_e)`。
→ **它是區塊級隔離的 wrapper，本身不畫任何狀態；它只是在失敗時「發出」狀態 (B) 的 `STATE_ERROR`。**
→ **(C) 這個四是巧合，不該拿來當「四色」的解釋。**

### 4-2 那 (D) 的「三」是怎麼變成今天的「四」的 —— 有紀錄可查

⭐ **線框 §03 的標題寫「三態」，但它自己下面那張表有五列**（`fund-empty-state-wireframe.html:357` 標題「03 顏色：三態統一規則」、`:386` 小標「建議的統一規則」、表列起於 `:390` 前後至 `:427`）。
逐列讀完之後，那個「三」講得通 —— **它數的是顏色，不是狀態**：

| 線框那張表的列 | 線框給的視覺 | 是幾種顏色 |
|---|---|---|
| 未載入／未執行 | ⬜ 灰色說明 | 灰 |
| 不適用（`:398`） | **➖ 或不顯示** | **不是顏色** |
| 業務警訊 | 🔴 **紅字**，但用卡片／表格列 | 紅 |
| 系統真出錯 | 🔴 **紅色**錯誤框 | 紅（**與上一列同色**） |
| 破壞性操作提醒（`:419`） | 🟠 常駐橘框 | 橘 |

→ **線框寫的時候，業務紅與系統紅是同一個紅，所以顏色只有三種：灰／紅／橘 = 「三態」。**
⭐ 線框自己就寫了這件事（`:407`）：業務警訊那一列的「現況要改的」欄逐字
「『🔴 淘汰候選 N 檔』現在用的是**和系統崩潰同一個紅框**。」

⭐ **然後 2026-08-31 把那兩個紅拆開了**，`render_state.py:28-33` 逐字：
> ⚠️ **2026-08-31 更新：業務警訊改用「莓紅」，與系統紅正式脫鉤。**
> 在此之前，業務警訊與系統紅框**共用 `MATERIAL_RED`**，兩者的差別**只剩形狀**
> （卡片 vs 錯誤框）—— 而形狀是最容易在餘光裡看丟的一種差異…
> 現在業務色是 `shared.colors.BUSINESS_ALERT_ON_DARK`（`#f294b6`），左軌加粗到 `BUSINESS_ALERT_RAIL_PX`（6px）。

→ ⭐ 常數 `render_state.py:109` `BUSINESS_ALERT_RAIL_PX: int = 6`（`:106-108` 就地註明「系統紅框（`st.error`）**沒有左軌**…故 4px → 6px」）；套用在 `business_alert()`（`:216`）的 HTML 內。

**所以：三色（灰／紅／橘）＋ 紅色一分為二 ＝ 四色（灰／莓紅／紅／橘）。**

### 4-3 ⭐ 我的答案：「四色」＝ **(A) 五態裡帶顏色的那四個**，而 (B) 恰好也是四但成員不同

**四色（含資料不足）**：

| # | 顏色 | 狀態名 | 入口 | `file:line` |
|---|---|---|---|---|
| 1 | **⬜ 灰** | 未載入／未設定 —— **這一格就是「資料不足」** | `not_ready(message, *, where="")` | `render_state.py:112`；標記常數 `:101` `NOT_READY_MARK = "⬜"`（`:100` 就地註「⬜ 在 `ui/` 全層已是家規，量測日 2026-08-28：299 處」—— ⚠️ 該數字我**未複驗**） |
| 2 | **🫐 莓紅** | 業務警訊（資料可信，但很難看） | `business_alert(title, lines, *, footer="")` | `:216`；色 `shared.colors.BUSINESS_ALERT_ON_DARK`（`#f294b6`）；左軌 6px `:109` |
| 3 | **🔴 紅** | 系統真出錯（這個數字不可信） | `system_error(what, exc, *, hint="", degraded=...)` | `:134` |
| 4 | **🟠 橘** | 破壞性操作提醒 | **`st.warning()`，沒有 helper** | `:26` 表列逐字「`st.warning()`（無例外處理，不需 helper）」 |

**第五態不是顏色，所以它不在「四色」裡**：
| 5 | **➖** | 不適用（結構上本來就沒有這件事） | `NOT_APPLICABLE_MARK` **常數，不是函式** | `:104` |

⭐ `render_state.py:102-103` 逐字講清楚 ⬜ 與 ➖ 的差別：
> 「⬜ 是『現在沒有、之後會有』，➖ 是『這件事對它本來就不存在』。」
⭐ 線框同一列也明令（`fund-empty-state-wireframe.html:398` 列名、`:401` 指示）：
> 「現況已經分對了，不要動。**特別注意不要把 ➖ 併進 ⬜**」

⚠️ ⛔ **我拿到的假設是「四個有格子 ＋ 一個沒格子」—— 那個假設不成立，我實測推翻它。**
⭐ `render_state.py:18-26` 的五態表，**五列全部有格子**（每列都有「狀態／視覺／本模組入口」三欄）。
**沒有格子的是第六個東西，不是這五個裡的一個。** 見 4-4。

⚠️ **這一節的判定是我的歸納，不是 repo 裡寫好的一句話。**
⛔ **(B)（`CARD_STATES` 四態）同樣是四，而且它是「卡片」的狀態表，與『四色資料不足』這個題名更貼。**
**兩種讀法我都列在下面，Q7 由客戶拍。**

**(B) 讀法下的四**（`ui/helpers/ia/cards.py:36-47`，逐字）：

| # | 常數 | 值 | docstring 逐字 | 實際走誰 |
|---|---|---|---|---|
| 1 | `STATE_OK` | `"ok"`（`:37`） | 「一切正常：可信、而且不難看。走 `st.metric`。」（`:36`） | `st.metric` |
| 2 | `STATE_NOT_READY` | `"not_ready"`（`:39`） | 「灰：未載入／前提不足。**不是**故障。走 `render_state.not_ready()`。」（`:38`） | ⬜ |
| 3 | `STATE_BUSINESS` | `"business"`（`:41`） | 「莓紅左軌：資料可信，但表現差（業務警示）。走 `render_state.business_alert()`。」（`:40`） | 🫐 |
| 4 | `STATE_ERROR` | `"error"`（`:43`） | 「紅框：系統真出錯，這個數字不可信。走 `render_state.system_error()`。」（`:42`） | 🔴 |

**兩種讀法的差別，就三格**：
- (B) 多了 `STATE_OK`（正常，不是「異常顏色」）
- (B) 少了 **➖ 不適用**（它是記號常數，塞不進 `state_card` 的 `state` 引數）
- (B) 少了 **🟠 破壞性操作提醒**（它走 `st.warning()`，沒有 helper，也不是卡）

⭐ **(A) 與 (B) 不衝突，它們是兩個射程**：
`cards.py:34` 逐字 `from ui.helpers.render_state import business_alert, not_ready, system_error`
—— **(B) 是 (A) 的消費端，三個函式全部從 (A) import**。

### 4-4 ⛔ 那個「沒有格子的狀態」：**持久化失敗**（它是第六個，不是五個裡的一個）

⭐ `render_state.py:74-92`，逐字摘要：
> ⚠️ **五態表有一個沒有格子的狀態：「持久化失敗」**（2026-08-28 顏色批次二之一登記）
> `nav_history` 寫入失敗、批次分析的磁碟 checkpoint 失敗、CSV 匯入後雲端同步失敗 ——
> 這一族的形狀是：**畫面上每一個數字都還在、也都是對的，壞掉的是「留得住」**。
> - 它不符合 `degraded=True` 的通過條件（掉的**不是**一張圖，而且使用者**會**因此在幾週後做出錯誤判斷 —— 以為序列在累積、其實沒有）。
> - 它也不是 ⬜（那不是「還沒設定」，是**真的寫失敗了**，按幾次都一樣）。

⭐ **現行處置**（`:85-87`，逐字）：
> **本批的處置：一律 🔴 `degraded=False`，並在 `hint` 裡明講「本次數字不受影響、壞的是保存」。**
> ⚠️ **這是判斷，不是事實** —— 「系統紅燈＝這個數字不可信」照字面只涵蓋畫面上的數字，這裡的紅指的是「**未來的序列**不可信」。

⭐ **repo 自己留了推翻的路**（`:88-92`）：
> 若客戶認為這一族該用 🟠 或別的視覺，推翻這一段即可，三處會一起改
> （查證這三處，量測日 2026-08-28 各 1 命中，共 3：
> `grep -rnE 'system_error[(]"(NAV 累積寫入失敗|磁碟續存失敗|雲端 nav_history 同步失敗)' ui/ --include=*.py`）

⚠️ **我沒有重跑那條指令。** 那三處現在還是不是 3 —— **未複驗**。
→ **【待決定】Q11：持久化失敗要不要有自己的顏色？** 現行是「借用 🔴」，而 repo 自己標它是判斷不是事實。

### 4-5 ⛔ 另一個已知的、尚未收斂的模糊：🟠 現在有三個意思

⭐ `render_state.py:42-49`（前言 `:42-43`、表 `:45-49`），逐字：

| 🟠 的來源 | 意思 | 怎麼分辨 |
|---|---|---|
| `st.warning()` 常駐橘框 | **破壞性操作提醒** —— 永遠有效的警語，沒有任何事情出錯 | 不帶錯誤內容、每次都在 |
| `system_error(degraded=True)` | **系統失敗，但數字全在且全對** —— 掉的只有一張圖 | 帶例外類型與技術細節 |
| `st.warning(_res["caveat"])` | **§1 業務 caveat** —— 結果正確，但方法論有限制（樣本少／回測非未來） | 不帶錯誤內容、跟在結果旁邊 |

⭐ `render_state.py:51-54` 逐字：
> ⚠️ **這是已知的、尚未收斂的模糊**，據實登記而不是假裝沒有：本批把「紅色」拆乾淨了（業務紅 vs 系統紅），**橘色沒有拆**。要拆需要新增視覺語彙或改版面，屬 §-1.5 v3 §03-2 ① 的**客戶 gate**…
> 現況唯一可靠的辨識點是**內容**：**只有第二種帶技術細節**（`🔧 技術細節` 區塊）。

→ **【待決定】Q12：🟠 要不要拆？** ⭐ repo 已經標明它是客戶 gate，不是內部自決。

### 4-6 「資料不足」這一格的寫法紀律（這是可以直接照做的部分）

⭐ **三要素，`render_state.py:112-121` 的 `not_ready()` 簽名就釘死了**：
```
not_ready(message, *, where="")
```
- `message` ＝ **缺什麼**。⭐ docstring `:117` 逐字：「（寫具體的東西，不要只寫「無資料」）」
- `where` ＝ **去哪裡補**。⭐ docstring `:118-120` 逐字：
  > 線框 §02：這是最容易省掉、也最有價值的一項 —— 沒有它，占位只是把「消失」換成「灰色的消失」。
- **標題** ＝ 由呼叫端自己畫（`#### …`），三要素才完整。⭐ `page_01_macro.py:1046` 逐字：「**三要素一項不少**：標題（本行）＋ 缺什麼＋去哪補（下一行的 `not_ready()`）」

⭐ **`not_ready()` 不吃 `Exception`，這是型別層防呆**（`render_state.py:90-92` 逐字）：
> 「還沒載入」永遠不會有 exception 可報；一旦手上有 exception，就是 `system_error()`。
> `tests/test_render_state_color_separation.py` 以 AST 守住這條分界（守形狀，不守字面）。
⭐ **而且它是真的會炸的**：`:122-127` `if isinstance(message, BaseException): raise TypeError(...)`，
就地註解 `:123-124` 逐字：「卻走到這裡,代表把『系統真出錯』畫成了『還沒載入』—— 那正是本模組要擋的 bug。」

⛔ ⭐ **一個我原本沒預期、但很重要的發現：「（請先到：…）」這幾個字是機器加的，不是人寫的。**
⭐ `render_state.py:128-131`，逐字：
```
_msg = f"{NOT_READY_MARK} {message}"
if where:
    _msg += f"（請先到：{where}）"
st.caption(_msg)
```
→ **也就是：`⬜` 前綴與「（請先到：」外框，兩者都由 `not_ready()` 自動加上；呼叫端只給 `message` 與 `where` 兩段內容。**

**這件事對本文有三個後果**：
1. **灰態的外觀不可能各頁不一致** —— 它只有一份實作。
2. **我拿到的材料說「舊草稿第五節表 27 列、其中含『請先到：』22 條（①4 ②4 ③12 ④5 ⑤2）」** ——
   ⚠️ 那是**舊草稿那份表**的統計，**不是 repo 的統計**，我**沒有複驗那 22 這個數字**。
3. ⭐ **repo 端的實測是另一組數字**：`git grep -c 請先到 -- '*.py'` →
   **production 13 行（9 檔）、tests 33 行（7 檔）**，合計 46。
   production 那 13 行分佈：`ui/helpers/ia/empty_state.py` 1、`ui/helpers/render_state.py` 1、
   `ui/tab2_single_fund.py` 1、`ui/tab3_t7_ledger.py` 2、`ui/tab6_manual.py` 1、
   `ui/views/page_01_macro.py` 1、`ui/views/page_03_research.py` 3、
   `ui/views/page_04_portfolio.py` 1、`ui/views/page_05_settings.py` 2。
   ⚠️ ⛔ **這 13 不能拿來跟 22 比** —— `git grep -c` 數的是**原始碼裡寫著這三個字的行**，
   而畫面上真正出現「請先到：」的次數是**每一次 `not_ready(where=...)` 被呼叫**，
   那個數字**遠大於 13、我沒有數、也不宣稱**。
   **`render_state.py:130` 那一行就是產生器本身，它一行抵無數次。**
   ⚠️ **單組實測、未經第二組驗證、取決於有沒有漏看。**

⭐ **`empty_state()` 是另一個入口，不是第五種顏色**：
`ui/helpers/ia/empty_state.py:38` `empty_state(title, missing, *, where="", ...)`
—— ⭐ `page_01_macro.py:1047-1048` 逐字說明它與 `not_ready` 的關係：
> 「`not_ready()` 正是 `empty_state()` 內部委派的**同一支 SSOT**，所以這裡**沒有**另起一套灰態」。
⚠️ `empty_state` 的完整簽名與其餘引數 —— **未複驗**（我只讀到 `:38` 那一行）。

### 4-7 ⛔ 一條 repo 自己寫下、我打算沿用的規則

⭐ `render_state.py:63-65`，逐字：
> ⭐ **一條給後人（含我自己）的規則 —— 這是三輪稽核連續抓到的同一種病**：
> > **任何「X 不會發生／三者不會同時／已經全部／繞不過去」形式的句子，必須就地附上證明它的那條 `grep`／AST 指令；附不出來就不要寫。**

→ **本文第三節的兩張統計表（3-4 與 3-5）就是照這條寫的**：AST 方法寫在表前、射程與盲點寫在表後。
→ **本文其餘任何「只有 N 個」「全部是」，一律已標「單組實測、未經第二組驗證、取決於有沒有漏看」。**


---

## 五、⚠️ 我沒有驗到什麼、為什麼（`CLAUDE.md §-2` 規則 6）

**這一節不是免責聲明，是射程說明。下一個接手的人請從這裡開始補。**

### 5-1 我犯過、而且自己抓到並修正的一個方法錯誤（先講這個）

⛔ **我第一次抄線框 HTML 的行號時，抄的是「被過濾後的資料流」的行號，不是檔案的行號。**

當時的指令長這樣：
```
git show origin/main:docs/wireframes/ia-wireframe.html | sed 's/<[^>]*>//g' | grep -vE '^\s*$' | grep -nE "…"
```
—— `grep -v` **先把空行刪掉**，`grep -n` 才編號。**編出來的是刪空行之後的序號。**

實際差距很大：ia-wireframe 的 ① 職責句，我第一次寫 **`第 279 行`**，真實是 **`:344`**。
**全部 7 個 ia-wireframe 行號與 4 個 empty-state 行號都錯**，已於定稿前用 `git show … | grep -n`（**不經任何過濾**）重抄。

**留這一筆的理由**：這正是客戶禁止事項 10 要防的形狀 ——
**行號長得像證據，但它可以是從一個被改造過的座標系裡抄來的。**
→ **本文其餘所有 `.py` 的 `file:line`，一律用 `git show origin/main:<path> | awk 'NR>=s && NR<=e'` 直接印原始行號**，不經 `sed`／`grep -v` 過濾。

⚠️ **同型錯誤我還修了第二批，成因不同**：五個 `ui/views/page_0*.py` 的行號，我第一次是
**從 `sed -n 'a,bp'` 的輸出目測數行**取得的，偏移 **1~6 行**
（最大的一筆：`page_01_macro.py` 的 `business_alert` 我寫 `:698`，真實是 `:693`）。
**定稿前已逐檔用 `awk` 直接印原始行號、逐條重抄。**
⚠️ **我不報「我改了 N 個」這個數字** —— 我沒有逐一計數，報一個沒數過的數字正是本節在講的病。
⛔ **我不宣稱現在一個都沒錯** —— 我只宣稱**每一個都是用「直接印原始行號」的方法取得的**。

### 5-2 整類沒驗到的東西

| # | 沒驗什麼 | 為什麼 |
|---|---|---|
| 1 | **所有卡片、表格、區塊的「內文」** | 我讀的是**骨架**（區頭、卡名、常數、呼叫點）。逐一展開 `_card_*` / `_render_*` 大約要再讀 8,000 行。已在每一處就地標「未複驗」 |
| 2 | **五頁的表格欄位清單**（②-7 逐檔體檢表、③ 搜尋結果、④ 三張大表） | 同上。⛔ **我刻意不列** —— 列了就是發明（客戶禁止事項 4） |
| 3 | **舊分頁（`ui/tab*.py`）的內容** | 它們是凍結的，本文只查了它們**掛在哪一格** |
| 4 | **TER 卡掛在哪幾格** | 1-6(3) 的兩個 TER 卡住在 `ui/helpers/**`，我沒有回頭追它們與五個 View 的呼叫關係 |
| 5 | **`v2_holdings` 的目標 schema 長什麼樣** | repo 裡 0 命中。我只能報「現況是 `ALL_COLS_V2` 10 欄」，**沒有任何目標 schema 的出處** |
| 6 | **「成本法加權移動平均」與現行 `avg_nav` / `avg_fx` 的關係** | 沒讀那兩欄的寫入與更新路徑 |
| 7 | **`empty_state()` 的完整簽名** | 只讀到 `ui/helpers/ia/empty_state.py:38` 那一行 |
| 8 | **`perf_block_label()` 實際回傳的中文** | 只追到它走 `section_label("pf_perf")`，沒展開 SSOT 值 |
| 9 | **`render_state.py:88` 那條「共 3 命中」的查證指令** | 沒重跑。4-4 已標未複驗 |
| 10 | **`render_state.py:100` 的「⬜ 299 處（量測日 2026-08-28）」** | 沒重跑。那是會漂移的量測值 |
| 11 | **新 ④ 有沒有巢狀 `st.tabs`** | 線框明令不留，我沒驗實作 |
| 12 | **舊草稿那 27 列 / 22 條** | 那是舊草稿的統計，不是 repo 的（M5） |
| 13 | **`docs/wireframes/` 另外 8 個 `.html`** | 我只讀了 `ia-wireframe.html` 與 `fund-empty-state-wireframe.html` 兩個，外加 `draft-four-page-content.html:741` 一行（**該行是材料轉述，我未逐字比對**）。⛔ 其餘 7 個**一個字都沒讀** |

⛔ **第 13 項最嚴重**：`docs/wireframes/` 有 **10 個 `.html` ＋ 1 個 `.md`**（⭐ `git ls-tree origin/main --name-only docs/wireframes/` 實測）。
**我讀了其中 2 個半。** 剩下的
`draft-p03-batch-input.html` / `fund-redesign-wireframe.html` / `fund-wireframe-final.html` /
`policy-split-wireframe.html` / `rotation-components-wireframe.html` /
`wireframe-fund-research.html` / `wireframe-macro-health.html` / `README.md`
**可能含有與本文牴觸的客戶拍板內容，我不知道。**

### 5-3 本文的全稱句清單（逐條列出，**一律不得被引用為已查證的事實**）

⚠️ 以下每一句都是**單組實測、未經第二組驗證、取決於有沒有漏看**：

1. 「`v2_holdings` / `v2_transactions` 全 repo 0 命中」（中文與英文字面 grep；動態組出的名稱掃不到）
2. 「`更新淨值與配息` / `更新淨值` repo 0 命中」（同上）
3. 「`扣款日` 全 repo 0 命中」（同上）
4. 「`約 7 成` / `約七成` / `7 成` / `七成` 全 repo 0 命中」（同上）
5. 「`ui/tab1_macro.py` 已無 production caller」（⛔ **別名匯入 `from x import y as _z` 結構上掃不到**）
6. 「`PREVIEW_TAB_LABELS` 沒有 `macro` key，① 沒有預覽雙胞胎」（讀單一 dict 字面；**風險低**）
7. 「② 全檔沒有渲染 `### 🧾 ② 依據`」（`f"{變數}"` 動態組出的區頭掃不到）
8. 「③ 與 ④ 一層四層區頭都沒有」（同上）
9. 「五頁的 expander / checkbox / button 計數」（⛔ **AST 只解析字面值與可追到的模組層常數；執行期組出的標籤掃不到**）
10. 「⑤ 有 0 個按鈕」（同上）
11. 「`render_state.py` 的 public 函式有 4 個」（讀 `^def ` 開頭；**巢狀定義或動態指派掃不到**）
12. 「`CARD_STATES` 是 4 個」（讀單一 tuple 字面；**風險低**）
13. 「② 委派區是七塊」（讀 `safe_section(` 的 label 引數；**迴圈或條件式產生的區塊掃不到**）

⛔ **本文沒有任何一句宣稱「沒有第 N+1 個」。** 凡是「取決於有沒有漏看」的問題，本文一律只寫分類、不寫窮舉 ——
沿用 `CLAUDE.md §-1.5.1c 判定 2` 那條方法教訓：**「能被一條 grep 推翻的全稱句，就不該寫進規格。」**

### 5-4 本文的所有【待決定】彙總（12 題，客戶只需要回答這些）

| # | 一句話 | 出處 |
|---|---|---|
| Q1 | ① 的表單欄位要哪一組？ | 1-5；⭐ `page_01_macro.py:2086-2088` 就印在畫面上 |
| Q2 | 「禁建議買進／出清」的射程到哪？只禁那兩個動作詞，還是連加碼／減碼／停利／逢低／滿倉／拉高現金水位一起？ | 1-6(1) |
| Q3 | 「📐 建議資產水位」要留、改名、還是拿掉？ | ①-6 |
| Q4 | 舊分頁**什麼時候**退場？（順序與第一前置已拍板） | 1-5；⭐ `page_04_portfolio.py:10-12` |
| Q5 | 「第四層最多一個展開 ＋ 一個勾選方框」是硬規則還是目標值？ ＋ 四層要不要補到 ③ ④？ ＋ ② 要不要補 ② 依據區頭？ | 3-2／3-4 |
| Q6 | 按鈕字三字規則含不含下載鈕與導覽鈕？ ＋ 與 `gated_form.py:50-51`「那些是有意義的差異」的衝突怎麼裁？ | 3-5 |
| Q7 | 「四色」指 (A) 五態裡帶顏色的四個，還是 (B) `CARD_STATES` 四個？ | 4-1／4-3 |
| Q8 | TER 卡要修數字、改卡名，還是整張拿掉？ | 1-6(3) |
| Q9 | 顯示表的 `金額` 與 Sheet schema 的 `淨投資金額` 要不要統一？ | ④-5 |
| Q10 | 主 CTA「更新淨值與配息」要不要做？落在哪一頁？ | 3-6 |
| Q11 | 「持久化失敗」要不要有自己的顏色？（現行借用 🔴，repo 自標「是判斷不是事實」） | 4-4 |
| Q12 | 🟠 的三個意思要不要拆？（repo 已標為客戶 gate） | 4-5 |

⛔ **這 12 題本文一題都沒有代答。** 每一題都附了出處，客戶可以逐條覆核再拍。

### 5-5 這份草稿的性質，最後再講一次

- **未經客戶同意，不得依本文修改任何檔案。**
- 本文**沒有**修改 repo 任何檔案，**沒有** commit、push、merge；只用唯讀 git 動詞
  （`show` / `grep` / `ls-tree` / `rev-parse`）。
- 本文**由線框組單組產出，未經第二組獨立複驗**（`CLAUDE.md §-2` 規則 6）。
- 本文所有「現況」皆為**讀渲染碼推論**，**不是**我看到畫面（見 0-1）。
