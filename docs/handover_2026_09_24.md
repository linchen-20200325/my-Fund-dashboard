# 交接存檔 — 2026-09-24（線 2：基金儀表板 UI v2）

> 本檔是**唯一的交接介面**。所有數字都是**本輪現場量的**，量測日一律標在括號裡。
> ⚠️ 凡標 `（量測日 2026-09-24）` 的數字**會漂移**，要用請現場重跑，不要引用本檔的數字。
> ⚠️ 本檔**不動任何既有檔**，只新增這一份。

---

## 1. 現在停在哪

### 分支與 PR
- **分支**：`ui-v2`（量測日 2026-09-24）
- **PR #846**：`state=open`、**`draft=true`（維持 draft，不轉 ready、不 merge）**
  - head：`ui-v2` @ `ab561c2a3c23b6d677fb01c8220a8c67de1915d3`（＝本地 `ab561c2`）
  - base：`main` @ `fc9e8f9`
  - 50 commits／64 changed files／+62,148 −5（量測日 2026-09-24）
  - 標題：`feat(ui-v2): 市場總覽頁 Streamlit 實作（線 2 第一頁，draft，只為跑 CI）`
  - ⚠️ **PR 內文的數字已過期**：內文寫「領先 main 28 顆 commit／44 檔 /+31225」，現況是
    50 顆／64 檔 /+62148。**內文本輪沒有改，下一輪若要改請先請示。**

### 最後八顆 commit（量測日 2026-09-24）
```
ab561c2 標的探索頁落地：排序下拉接上線不再說假話、缺值比較逐方向釘住，勾選框先停用待裁
a3865a7 配置頁線框撤下導覽鈕的失效自述，並登記它畫的是 44 未裁的第三種出現條件
118ebb8 草稿併入標的探索頁並撤下兩條失效缺口，另拆掉市場總覽頁自創的嚴重度先後與補上失敗訊息的路
fd5e41b 持倉體檢頁拿掉三枚無效按鈕、單檔展開接上狀態、空持倉取數失敗改紅，並拆掉自創的嚴重度先後
f8b2140 線框輪撤下標的探索頁二十三處失效宣稱，並把勾選框與存檔鈕的草稿留待拍板
a7f8c1b SSOT 第十三輪修句首切段與逃網標籤、線框具名雙向驗，並撤下線框十三處假宣稱
2f2c238 持倉體檢頁對齊已凍結的 44：撤回三筆過度修正、補四筆偏差、修四支空掃測試
ce2d307 守衛剝行首標籤並釘 31 份文件身分，線框四句改寫、七句登記豁免
```

### 三頁 Streamlit 已落地
每頁五檔同構：`__init__.py` / `fixtures.py` / `logic.py` / `page.py` / `theme.py`

| 頁 | 套件 | 進入點 |
|---|---|---|
| 市場總覽 | `ui_v2/mkt/` | `ui_v2/app_mkt.py` |
| 持倉體檢 | `ui_v2/hld/` | `ui_v2/app_hld.py` |
| 標的探索 | `ui_v2/exp/` | `ui_v2/app_exp.py` |

### 工作樹狀態
- `git status --porcelain`（量測日 2026-09-24）**只有一行**：
  `?? docs/wireframes/draft_set_savefail.html`
- 該檔 **73,272 bytes**（量測日 2026-09-24）＝ **S-19 草稿，等客戶拍板**。
- **客戶規矩**：**拍板後當輪提交、不刪；拍板前不得提交。**
- 除此之外**零修改、零暫存**。

---

## 2. ⭐ 驗收指令（本輪最貴的教訓，先讀這一節）

### 唯一的驗收基準
```bash
cd /home/user/my-Fund-dashboard
/tmp/claude-0/-home-user-my-Fund-dashboard/c73da973-7365-568e-a156-e35eacf720bd/scratchpad/stvenv/bin/python \
    -m pytest tests/ui_v2/ -q --noconftest -p no:cacheprovider
```
**→ `444 passed`（量測日 2026-09-24，94.52s）**

### ⛔ 系統 pytest 那條**不可**當驗收
```bash
/root/.local/bin/pytest tests/ui_v2/ -q --noconftest -p no:cacheprovider
```
**→ `366 passed, 3 skipped`（量測日 2026-09-24，45.96s）**

**為什麼不能用它驗收**：三個畫面測試檔**整檔零收集**。

- `tests/ui_v2/test_exp_page.py:20`
- `tests/ui_v2/test_hld_page.py:16`
- `tests/ui_v2/test_mkt_page.py:15`

三處都是同一句（量測日 2026-09-24）：
```python
pytest.importorskip("streamlit", reason="本環境系統 python3 匯入不到 streamlit")
```
系統 python3 匯入不到 `streamlit` ⇒ 整個模組在 collect 階段被跳掉，
**報表上只長出 3 個 skip，78 條測試一條都沒有被收集。**

### 兩條 lane 的逐檔收集數（`--collect-only`，量測日 2026-09-24）

| 測試檔 | stvenv lane | 系統 lane |
|---|---:|---:|
| `test_exp_logic.py` | 133 | 133 |
| `test_hld_logic.py` | 144 | 144 |
| `test_mkt_logic.py` | 89 | 89 |
| `test_exp_page.py` | **45** | **0**（整檔 skip） |
| `test_hld_page.py` | **24** | **0**（整檔 skip） |
| `test_mkt_page.py` | **9** | **0**（整檔 skip） |
| **合計** | **444** | **366** |

- 444 − 366 = **78** ＝ 45 + 24 + 9（三個畫面測試檔）
- 366 ＝ 133 + 144 + 89（三個邏輯測試檔）
- **那 78 條不是「跑過了通過」，是「從來沒有被跑過」。**

### `--noconftest` 為什麼要帶
`tests/conftest.py` 這條鏈在系統 lane 會炸（量測日 2026-09-24）：
```
tests/conftest.py:45   import fund_fetcher
fund_fetcher.py:44     from infra.proxy import (...)
infra/proxy.py:12      import requests
E   ModuleNotFoundError: No module named 'requests'
```
- **系統 lane 一定要帶 `--noconftest`**，否則連 collect 都進不去。
- **stvenv lane 不帶也能跑**（該 venv 有 `requests`，實測拿掉 `--noconftest` 仍收到 133 條）。
  **仍然建議照帶** —— 兩條 lane 用同一組旗標，比較才有意義。
- `python3 -m pytest` 回 `No module named pytest`；**要用絕對路徑**
  （`/root/.local/bin/pytest` 由 uv tool 安裝，不在 `python3` 的 site-packages 裡）。

### ⛔ 只跑 `tests/ui_v2/`，不要跑 `tests/`
全跑（stvenv，量測日 2026-09-24）：**`8355 tests collected, 29 errors`，被 `Interrupted`。**

29 個 collection error 的成因（量測日 2026-09-24）：

| 成因 | 檔數 |
|---|---:|
| `No module named 'bs4'` | 12 |
| `No module named 'pandera'` | 10 |
| `No module named 'plotly'` | 4 |
| `No module named 'scipy'` | 1 |
| `ImportError: cannot import name 'canonicalize_moneydj_url' from 'fund_fetcher'` | 1 |
| `ImportError: cannot import name '_src_cache_files' from 'fund_fetcher'` | 1 |

**最後兩個看起來像別的病，其實是同一個**：`fund_fetcher.py:380` 那個
`try: from repositories.fund import (...)` 區塊會匯入 `repositories/fund/sources.py:18`
的 `from bs4 import BeautifulSoup`；缺 `bs4` ⇒ 整個 try 失敗 ⇒ 那兩個名字從來沒被綁上。
**⇒ 29 個全部可追到那 4 個缺的第三方套件，與這三頁無關。**

### 兩個直譯器的套件差異（量測日 2026-09-24）

| 套件 | 系統 `/root/.local/...pytest/bin/python` | stvenv |
|---|---|---|
| `requests` | ✗ | ✓ |
| `streamlit` | ✗ | ✓ |
| `pandas` | ✗ | ✓ |
| `numpy` | ✗ | ✓ |
| `bs4` | ✗ | ✗ |
| `pandera` | ✗ | ✗ |
| `plotly` | ✗ | ✗ |
| `scipy` | ✗ | ✗ |

---

## 3. 待客戶裁決（兩件，卡住下一輪）

### 3.1 S-19 草稿拍板
- 檔：`docs/wireframes/draft_set_savefail.html`（未追蹤，73,272 bytes，量測日 2026-09-24）
- 草稿 **§5「這一份沒有動什麼，以及拍板後要改哪裡」**（該檔第 686 行，量測日 2026-09-24）
- 該檔自陳：**「本草稿改動的三處已就地標註（另見註記層 §5）」**（第 802 行）
- **拍板後才能**依那三處重繪 `docs/v2/prototype/ui_prototype_set.html`。
- 爭點（草稿自陳）：「**存檔寫入失敗**」算不算 `44` 5.5 那四種空狀態
  （`來源缺`／`部分缺`／`算不出來`／`系統錯誤`）的哪一種 —— **`44` 沒有分。**

### 3.2 A-02 導覽鈕的出現條件
出處：`docs/v2/prototype/ui_prototype_alo.html` 第 639–652 行（量測日 2026-09-24）

三種寫法並存，**線框畫的是第三種**：

| 出處 | 出現條件 |
|---|---|
| `44` `ALO-4` 規則欄 | **恆在** |
| `44` `HLD-0` 空狀態欄 | 只在**持倉表為空**時出現 |
| **本線框實際畫的** | **只在持倉表不為空時出現**（剛好相反的第三種） |

- 線框原本自陳「本草稿把它畫成恆在，與 `44` `ALO-4` 的寫法一致」——
  **那句已於 `a3865a7` 劃掉**，改成誠實敘述。
- 「恆在 vs 只在空狀態出現」**正是 `44` 就地登記、待客戶裁決的那一點**。
- **已登記、未重繪。**
- 📌 同一框另有一筆**本輪刻意沒動**的：`§D` 那張按鈕表的**判欄** ——
  改判欄是重新判讀，不是把假話改成誠實，故留給下一輪。

---

## 4. 下一輪必做（已查證，附出處）

### 4.1 禁詞原始碼掃描**不含 `page.py`**
`tests/ui_v2/test_exp_logic.py`（量測日 2026-09-24）：
- 第 45 行：`def test_logic與fixtures與theme都沒有import_streamlit或舊repo或網路():`
- 第 67 行：`files = [logic.__file__, fixtures.__file__, theme.__file__]`
- 第 68 行：`assert len(files) == 3`

**→ `page.py` 與 `app_exp.py` 不在這個掃描的母體裡。**
凍結組用突變證明：把禁方向詞塞進畫面 → **正確 lane 紅、系統 lane 全綠。**

⚠️ **不要跟另一個掃描搞混**：同檔第 90–96 行的 `_QUOTE_SCAN_FILES`
（逐字引文守衛）**有**涵蓋 `page.py` 與 `app_exp.py`。
**漏的只有「禁詞／禁方向詞」這一支。**

### 4.2 `error_lines` 混裝（本輪只拆了一處，其他鍵要回頭看）
`ui_v2/exp/logic.py`（量測日 2026-09-24）：
- **已拆**：第 1342–1374 行（`EXP-4`）—— 登記註記自本輪起住在自己的鍵 `save_fail_notes`，
  原檔就地寫明「`error_lines` 原本同時裝兩種東西……當時沒出錯，是因為測試數的是
  **字面** `存檔寫入失敗` 而不是 `len(error_lines)`」。
- **同型未看**：第 897–915 行、第 1411–1437 行 —— **這兩處仍然只有 `error_lines`，沒有 `save_fail_notes`。**
- 渲染端：`ui_v2/exp/page.py:213 _error_lines()`，兩個鍵都讀（第 218、220 行）。

### 4.3 `EXP-7` 的 44-矛盾登記註記出現在**每一個**情境
- 位置：`ui_v2/exp/logic.py` 第 1575–1580 行，直接 append 進 `detail_lines`，**沒有任何條件包著**。
- **實測（量測日 2026-09-24）：46 / 46 個情境全部出現**
  （23 個 `ALL_SCENARIO_NAMES` × 2 個 `SAVE_FAIL_CHOICES`）。
- 註記內容：`44` 把「尚未設定條件」掛上「來源缺」這個種類名，而那其實是使用者還沒輸入。
- 相關的第二筆：`BLOCK_SOURCE_TABLES["EXP-7"]`（同檔第 617–622 行）自陳
  **「這是本組判讀，`44` 沒有逐字寫」**。
- **下一輪要先查 `44` 要不要它是條件式的，再決定改不改。**

### 4.4 `ui_v2/exp` 在 `docs/v2/46_fund_live_dead.md` 上 **0 列**
實測（量測日 2026-09-24）：

| 套件 | 在 `46` 登記表上的資料列 |
|---|---:|
| `ui_v2/hld` | **6**（該檔第 103–108 行） |
| `ui_v2/mkt` | **1** |
| `ui_v2/exp` | **0**（全檔 0 次提及） |

- `44` §6「死碼處置」在 `docs/v2/44_fund_ui_ssot.md` **第 2471 行**，明文：
  「本節只寫機制……死碼的逐筆登記在 `46_fund_live_dead.md`」。
- **⛔ `ui_v2/exp/logic.py:1856` 的 `KNOWN_NO_CALLER` 不是 `44` §6 的登記表**
  （量測日 2026-09-24 共 9 筆），**不得被引用成「已經登記過了」**。
- **⛔ `44` §6 是「不刪，只標」**（該檔第 2476 行逐字）——
  **總管上一輪派工單寫過「或刪除」，那是錯的，已撤回。**
- `44` §6 的**三層確認**（第 2477 行逐字）：
  先 grep caller → 再查那個 caller 自己是不是 live → 最後用 AST 掃 import。
- **要補必須另開一輪**，把 `46_fund_live_dead.md` 放進檔案邊界，而且**真的做完三層確認**。

### 4.5 Sheet ID 轉 `os.getenv` — ⚠️ **與線索不一致，先看這一段**
實測（量測日 2026-09-24）：

- **`ui_v2/` 底下 `POOL_SHEET_ID` / `NAV_SHEET_ID` 都是 0 命中；`os.getenv` / `os.environ` 也是 0 命中。**
- 這兩個名字全部住在**舊 repo**：
  - `services/nav_history_gs.py` —— `_NAV_SHEET_ID_DEFAULT = "1b92…"`（第 73 行），
    取值走 `get_secret("NAV_SHEET_ID")` → baked 預設（第 84–85 行）
  - `repositories/pool_repository.py` —— `_POOL_SHEET_ID_DEFAULT = "1bxz…"`（第 39 行）
  - 另有 `ui/tab_manage.py`、`ui/views/page_05_settings.py` 提及
- **現況不是「寫死、完全沒有外部覆寫」**：兩者都已經是
  「secret 覆寫 → baked 預設」兩層，寫死的是那個 **baked fallback 常數**。

**⛔ 這件事做下去必然要動舊 repo 的 `.py`，而客戶常設規矩是「不碰舊 repo 的 `.py`」。**
**⇒ 下一輪動工前必須先請示：要嘛客戶明確放寬檔案邊界，要嘛這一步不做。**
**不要自行把它讀成「已授權」。**

---

## 5. 只登記、不動工（引用時**不得**當成缺陷）

### 5.1 `logic.py` 裡帶著 `44` 逐字引文的死分支
- `ui_v2/exp/logic.py:433 empty_source_text()`、`:437 not_applicable_text()`
- 兩支都在 `KNOWN_NO_CALLER` 裡（量測日 2026-09-24）：
  「空來源的文案產生器；現行空狀態走 `44` §5.5 的四種 kind，沒有走這一支。」
- **同一條規則兩份實作，活的是另一份**（`44` §5.5 那四種 kind 的常數）。
- 另一筆同型：`KNOWN_CURRENCIES`（同檔第 87 行）——
  舊註解宣稱它是跨幣別守衛用的，而真正做那件事的 `cross_currency_nodes()`
  **從頭到尾沒讀過它一次**；那句假宣稱已於本輪改掉，常數依 `44` §6「不刪，只標」保留。

### 5.2 `E-15`：窄寬度堆疊在任何情境下都畫不出來
- 登記在 `ui_v2/exp/logic.py` 第 410–418 行。
- `viewport_width` 預設 **1280**（同檔第 1724 行），**`page.py` 從不傳它**。
- `block["_stacked"]` **沒有任何渲染端讀它**；畫面上真正會堆疊，靠的是 `page.py` 的 CSS media query。
- **它仍然有一個真實用途**：`page.py` 用它反推那個 CSS 斷點該寫多少
  （見 `page.py` 的 `_MAX_PROBE_WIDTH` 那一段），**拿掉它 CSS 會失去唯一真相源**。
- **危險在於**：讀模型的人會以為堆疊是由這支決定的，**改它卻什麼都不會變**。

### 5.3 逐字守衛抓不到 `44` 的**刪除線舊條文**
- `tests/ui_v2/test_exp_logic.py:174 _flat44()`，關鍵那一行（量測日 2026-09-24）：
  ```python
  return re.sub(r"\s+", "", re.sub(r"\*\*|~~|`|\*|_", "", text))
  ```
- **它只剝 `~~` 這個標記，把被劃掉的字留在母體裡**
  ⇒ 拿 `44` 裡**已經退役的舊條文**當逐字引文，**本守衛會判它合法**。
- 登記在同檔第 165–173 行。
- ✅ **目前 0 句踩到**：稽核用不同演算法獨立重抽 **116 句**，落在劃線內的 **0**（量測日 2026-09-24）。
  **⇒ 是潛在缺口，不是現行錯誤。**
- 本輪不動它的理由：修法要先決定「引一句已退役的條文算不算違規」——
  **那是 `44` 沒有寫的事，不該由實作組順手定。**
- ⚠️ **這個病本專案犯過一次**，記錄在
  `docs/v2/prototype/ui_prototype_exp.html` **第 814 行**：「**錯的是授權它的那句引文。**」

### 5.4 對比餘裕只剩 0.006，任何色票微調都會翻掉
實測（`ui_v2/exp/theme.py`，量測日 2026-09-24）：
- 對比組共 **38** 組（`CONTRAST_PAIRS`）
- 樓地板：`TEXT_FLOOR = 4.5`（第 124 行）／`UI_FLOOR = 3.0`（第 125 行）
- **最緊的一組：「青色髮絲線／頁底」ratio = 3.0056，樓地板 3.0，餘裕 0.0056**
- 次緊：「失敗訊息框線／訊息底」3.106（餘裕 0.106）、「控制項邊界／按鈕底」3.2895（餘裕 0.2895）
- **⇒ 改任何一個色之前，先跑 `test_對比逐組算過而且未達標為零`。**

---

## 6. ⭐ 本輪的方法教訓（最值得留給下一個人的東西）

### 6.1「綠燈而沒有正控 ＝ 沒有檢查」
本輪十幾條必修有**超過一半**是「測試存在、但結構上驗不到東西」。已見的形狀：
- 迴圈本體**從沒執行**過
- **恆假**條件
- **恆真**斷言（例：`None not in [全是 str 的清單]`）
- 對**空集合**做 `all()`
- AST 守衛只驗「**有沒有寫**」，不驗「**跑不跑得到**」
- **整檔零收集**卻報綠（第 2 節那 78 條）

**最刺眼的一例**（`tests/ui_v2/test_exp_logic.py:1889` 該條的 docstring 自陳）：
一條寫著「炸掉比較誠實」的 §1 承重守衛 —— 把 `if left is None: raise` **整段關掉**，
六個方向裡**四個靜默選邊**（等於→False、不等於→True、早於或等於→False、晚於→True），
而整套測試 **131 passed、零紅**。
**⇒ 那道守衛可以整個拿掉而全綠 ＝ 它從來沒有被驗到。**
成因：原本只用「小於」一個方向，而「小於」拿掉守衛**也還是會炸**（掉進 `float(None)` 的
`except`，炸在**另一道**守衛上），於是 `pytest.raises` 照過。

**⇒ 可操作規則：每加一道守衛，就把它關掉跑一次（突變測試）。不轉紅 ＝ 沒守到東西。**

### 6.2「工具選錯 → 結論必錯」
本輪光是總管自己就犯了四次同型：
- `find -newermt` 的**時間窗開太寬**
- 把**取樣值**寫成**恆定值**
- `grep` **抓到自己的指令列**
- `pgrep -f` **自 match**

**⇒ 量「檔案有沒有被動過」要用 `md5sum` 或絕對 `mtime`，不要用相對時間窗。**

### 6.3 連續七輪的結構性發現
**客戶裁示的修復本身多半是對的；出事的一律是各組繞著修復寫的「自述文字」** ——
註解、docstring、測試函式名。

本輪抓到**三隻測試名與斷言相反**的（量測日 2026-09-24）：
- `tests/ui_v2/test_exp_logic.py:941`（自陳「**斷言是對的，錯的是名字**」，並註明
  「本輪第三隻同型：另兩隻在 `logic.py` 與 `test_exp_page.py`」）
- `tests/ui_v2/test_exp_logic.py:1333`（「而它自己斷言的是 `len(hits) == 2` —— 測試名與它的斷言方向相反」）
- `ui_v2/exp/logic.py:646`（「**測試名與它的斷言方向相反**。兩者一起修。」）

**⇒ 改完程式碼，回頭把旁邊那段話也讀一遍。**

---

## 7. 凍結中的東西（不得改；要改另開一輪）

| 檔 | 大小（量測日 2026-09-24） | 備註 |
|---|---:|---|
| `docs/v2/44_fund_ui_ssot.md` | 500,692 bytes | **md5 `7ef41e2996828aaf76986090adf25b06`**；`wc -l` ＝ 4,259 行；守衛另斷言 `split("\n")` 長度 ＝ 4,260 |
| `docs/v2/prototype/ui_prototype_alo.html` | 251,285 bytes | |
| `docs/v2/prototype/ui_prototype_exp.html` | 271,594 bytes | |
| `docs/v2/prototype/ui_prototype_set.html` | 224,221 bytes | S-19 拍板後要重繪的就是這一份 |

- `44` 的 md5 由 `tests/ui_v2/test_exp_logic.py:81 test_44還是凍結的那一份()` **釘住**，
  動一個位元組就紅燈。
- 📌 `docs/v2/prototype/` 底下另有 **`ui_prototype_hld.html`（199,704 bytes）**
  與 **`ui_prototype_today.html`（87,922 bytes）**，**不在上列凍結清單內**。

---

## 8. 客戶的常設規矩（照抄，不要改寫）

- 未經同意不准 **commit／push／merge**
- **commit message 識別碼 0**
- 不准 **force-push**、不改 author、不改歷史
- **#846 維持 draft**
- **不碰舊 repo 的 `.py`**、**不碰 `ui/tab*.py`**
- **不准寫「未複驗」，查不到就寫「查不到」**
- **凍結後不得改內容**
- 不准發明**不存在的資料源／按鈕／欄位**
- 不准給**買賣建議、目標價、最佳配置**
- 遇**新矛盾只登記，不回頭改規格**
- 修法照體例**加刪除線、不重寫**

**git 身分**（實測相符，量測日 2026-09-24）：
```
user.name  = my-Fund-dashboard
user.email = noreply@my-Fund-dashboard.local
```

---

## 9. 交接時發現的三點不一致（與上一輪口頭線索不同，以本檔實測為準）

1. **Sheet ID 那一步的落點不同** —— 線索寫成 `ui_v2` 的待辦，實測 `ui_v2/` **0 命中**，
   兩個名字全在**舊 repo**，且已是「secret → baked 預設」兩層。**見 §4.5。**
2. **本檔不在 `docs/v2/`，也不帶序號** —— 它住在 **`docs/handover_2026_09_24.md`**（`docs/` 根目錄）。
   **理由是守衛射程，不是編號美觀**：`tests/test_doc_counters.py` 的受檢範圍是
   `DOCS_DIR ＝ docs/v2/` ＋ `DOCS_GLOBS` 含 `*.md`，凡放進 `docs/v2/` 的 `.md` 都要過三道檢查
   （計數句要釘 commit SHA、句首全稱句要附反向檢查、還要登記進守衛裡的具名清單）。
   **一份會過期的交接存檔不該變成那支守衛長期維護的對象**，而為了過檢查去改存檔的文字更是本末倒置
   —— **存檔的用途是讓人五分鐘內接手，不是通過文件守衛。**
   **`47_` / `48_` 這類序號一併拿掉** —— 那是 `docs/v2/` 系列的號，搬出去之後留著只會讓人
   以為它還在那個系列裡。
   **⇒ 若有人叫你去找 `docs/v2/` 底下 `47_` 或 `48_` 的交接存檔，那是舊指路，看這一份。**
   （`docs/v2/47_fund_wireframe_mkt.md`〔67,155 bytes，量測日 2026-09-24〕是**線框**，不是交接檔。）
   **本輪已實測（量測日 2026-09-24）**：`test_doc_counters.py` 只看 `docs/v2/`；
   `test_constitution_file_refs.py` 的 `CONSTITUTION_FILES` 只有 `CLAUDE.md` / `EXCEPTIONS.md`，
   它另一條全 repo `.md` 掃描要求檔頭 20 行內有**一行憲法檔標記**（**標記字串本身刻意不寫進本檔**
   —— 依 `CLAUDE.md` §-2.A 第 8 款，掃描用的字串一旦落進文件就會自己命中，
   連帶讓靠它背書的結論失效；本檔對該標記 0 命中）；`test_retired_exception_ids.py` 只掃 `*.py`。
   ⚠️ **不宣稱窮舉** —— 「沒有第四支守衛看得到 `docs/` 根」取決於有沒有漏看；
   本輪做到的是上列三支逐支讀過，加上 `grep -rn` 掃過 `tests/*.py`。
3. **`46` 上 `ui_v2/hld` 是 6 列，不是 7** —— 該檔第 155 行也有 `ui_v2/hld` 字樣，
   但那是**另一張表的欄位標題**，不是登記列。**資料列就是第 103–108 行那 6 列。**
